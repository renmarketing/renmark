<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>{{PROJECT_NAME}} — Prototype</title>
    <style>
        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
        }

        body {
            font-family: -apple-system, BlinkMacSystemFont, "Segoe UI", Roboto, "Helvetica Neue", Arial, sans-serif;
            background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
            min-height: 100vh;
            display: flex;
            align-items: center;
            justify-content: center;
            padding: 20px;
        }

        .prototype-container {
            background: white;
            border-radius: 8px;
            box-shadow: 0 20px 60px rgba(0, 0, 0, 0.3);
            padding: 40px;
            max-width: 600px;
            text-align: center;
        }

        .prototype-container h1 {
            color: #333;
            margin-bottom: 16px;
            font-size: 28px;
        }

        .prototype-container p {
            color: #666;
            line-height: 1.6;
            margin-bottom: 12px;
            font-size: 16px;
        }

        .prototype-container .date {
            color: #999;
            font-size: 14px;
            margin-top: 24px;
        }
    </style>
</head>
<body>
    <!-- Human edits outside the RENMARK:GENERATED markers below are preserved during regeneration. -->

    <!-- RENMARK:GENERATED:PROTOTYPE:START source-sha=PENDING -->
    <div class="prototype-container">
        <h1>{{PROJECT_NAME}}</h1>
        <p>Prototype under development</p>
        <p>This interactive mockup will be generated for <strong>{{PROJECT_NAME}}</strong>.</p>
        <div class="date">Generated on {{DATE}}</div>
    </div>
    <!-- RENMARK:GENERATED:PROTOTYPE:END -->

</body>
</html>
