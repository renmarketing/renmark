---
artifact_type: prd
schema_version: 1
created_at: {{DATE}}
last_reviewed: {{DATE}}
status: draft
---

<!-- Living source of truth; updated only on reviewed, approved change.
     This is a HUMAN-OWNED doc, not a generated artifact: the lean header above is
     intentional and exempt from the generated-artifact provenance fields
     (source_sha, generator, dependency_refs) required of machine-emitted artifacts. -->

# {{PROJECT_NAME}} — Product Requirements Document

## Vision / Problem
(Describe the WHY: the user problem, business need, or opportunity this project exists to address.)

## Target users
(Identify the primary users, stakeholders, and who this document is intended to serve.)

## Goals & Non-goals
(List the intended outcomes and the explicit non-goals that prevent scope creep.)
<!-- Non-goals here are PRODUCT-level and durable (what the product will never be).
     A single build's MVP cut belongs in the scope contract (CHANGELOG scope entry
     + stack.md), not here — cross-reference, don't duplicate. -->


## Requirements
(Capture required behaviors and outcomes as numbered requirements, not proposed implementations or technical solutions.)
Each requirement MAY carry optional **acceptance criteria** — short, plain-English, product-level "done when…" outcome bullets (NOT task/implementation steps, NOT Gherkin); omit them when the outcome is self-evident.
<!-- Acceptance criteria are per-REQ "done when…" outcomes that confirm THAT requirement is met;
     Success metrics (below) are project-wide signals of overall success. Don't conflate the two. -->

1. `REQ-1` (Describe the first required behavior or outcome in user-facing or business-facing terms.)
   - *Acceptance:* done when (an observable product outcome); done when (another).
2. `REQ-2` (Describe the next required behavior or outcome in user-facing or business-facing terms.)
3. `REQ-3` (Describe the next required behavior or outcome in user-facing or business-facing terms.)

## Success metrics
(Define how success will be measured, including user, business, or operational signals.)

## Scope boundaries
(Clarify what is in scope, out of scope, and explicitly deferred for later.)

## Open questions
(List unresolved decisions, assumptions to validate, or information still needed.)
