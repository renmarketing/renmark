---
description: "Use when /renmark:* commands aren't appearing, the plugin seems broken, or the user wants a sanity check on the install — typed as /renmark:doctor (--fix to apply safe auto-fixes)."
argument-hint: "[--fix]"
---

Read `${CLAUDE_PLUGIN_ROOT}/skills/doctor/SKILL.md` and follow its instructions exactly. The user provided this input: $ARGUMENTS

If `$ARGUMENTS` is empty, begin the doctor skill's flow.
