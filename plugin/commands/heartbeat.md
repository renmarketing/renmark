---
description: "Use to monitor or schedule recovery when a renmark run is paused by a usage limit — typed as /renmark:heartbeat (--emit-cron to set up external monitoring, --auto-resume for unattended recovery). Zero LLM calls."
argument-hint: '[--emit-cron | --auto-resume]'
---

Read `${CLAUDE_PLUGIN_ROOT}/skills/heartbeat/SKILL.md` and follow its instructions exactly. The user provided this input: $ARGUMENTS

If `$ARGUMENTS` is empty, begin the heartbeat skill's flow.
