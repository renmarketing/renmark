OpenAI Codex v0.117.0 (research preview)
--------
[1mworkdir:[0m /home/renmark/projects/ai-system
[1mmodel:[0m gpt-5.4
[1mprovider:[0m openai
[1mapproval:[0m never
[1msandbox:[0m read-only
[1mreasoning effort:[0m none
[1mreasoning summaries:[0m none
[1msession id:[0m 019f3899-9fc4-72c1-91ba-36e51eb8da1b
--------
[36muser[0m
You are reviewing the diff main..HEAD in the renmark repo.

Read: .renmark/plans/2026-07-06-context-hygiene-gates.plan.md (spec intent)
Read the diff: git diff main..HEAD (run this command in the sandbox)

━━ VERDICT 1 — Spec-compliance ━━
Goal: Add two blocking AskUserQuestion context hygiene gates:
(1) Cross-domain clear gate: skill_preamble returns CONTEXT_GATE_CLEAR: prefix, persists compact_checkpoint.json, bypass=finish/approve/resume.
(2) Compact gate: rule-enforced at >=120k tokens, configurable via config.json["compact_gate_tokens"] (default 120k, 0=disabled), --compact-checkpoint CLI persists state.
Neither gate invokes /compact or /clear. compact_gate_tokens returns 0 when 0 stored.

Spec: compliant | under-built — <what> | over-built — <what>

━━ VERDICT 2 — Code-quality ━━
Find: runtime bugs, logic errors, security issues, bad assumptions, unhandled edge cases.
For each: file:line | severity: Critical/Major/Minor/Nit | description | fix

Summary counts per severity at top of quality section.
Both verdicts at TOP of report. Do not modify files.

[1m[33mwarning:[0m[0m Codex could not find system bubblewrap at /usr/bin/bwrap. Please install bubblewrap with your package manager. Codex will use the vendored bubblewrap in the meantime.
[35m[3mcodex[0m[0m
I’m reading the spec artifact and the `main..HEAD` diff, then I’ll check the implementation against the two required gates and look for concrete bugs or edge cases.
[35m[3mexec[0m[0m
[1m/bin/bash -lc "sed -n '1,260p' /home/renmark/projects/ai-system/.renmark/plans/2026-07-06-context-hygiene-gates.plan.md"[0m in /home/renmark/projects/ai-system
[32m succeeded in 0ms:[0m
---
artifact_type: plan
schema_version: 1
created_at: 2026-07-06T00:00:00Z
source_sha: e5d39f0
generator: sonnet
related_spec: null
stale_after: 2026-10-06T00:00:00Z
dependency_refs:
  - renmark/lifecycle.py
  - renmark/state/skills.py
  - renmark/config.py
  - renmark/cli/_engine.py
  - CLAUDE.md
  - AGENTS.md
  - plugin/skills/_shared/context-taxonomy.md
  - tests/test_lifecycle.py
  - tests/test_config.py
---

# Hard-stop context hygiene gates (selectable AskUserQuestion menus)

**Branch:** `feature/context-hygiene-gates`
**Base sha:** `e5d39f0`

## Goal

Replace the existing advisory "consider /clear" and dead "consider /compact" hints in
`skill_preamble` with **blocking AskUserQuestion menus** that surface as selectable options —
never pretending to run `/compact` or `/clear` directly, but making context hygiene
unavoidable by requiring the user to acknowledge and choose before the skill proceeds.

Two gates:

**1. Cross-domain clear gate (Python-detectable)**
When `context_budget_check` detects a domain transition, `skill_preamble` returns a
structured message prefixed with `CONTEXT_GATE_CLEAR:` and persists a compact checkpoint.
The CLAUDE.md/AGENTS.md rule says: on that prefix, use `AskUserQuestion` to present:
  - (Recommended) Stop here — I will run /clear then /renmark:resume
  - Continue in same context (this step only)
  - Queue this as next task after current work finishes
  - Cancel
Bypass skills (`finish`, `approve`, `resume`): advisory hint only — these must not be
blocked mid-stream.

**2. Compact gate (rule-enforced — Python cannot detect % context utilization)**
Harness does not expose context size to Python. Gate is enforced by a strengthened
CLAUDE.md/AGENTS.md rule at >=120k tokens (configurable via `compact_gate_tokens` in
`.renmark/config.json`). Rule instructs Claude to run `renmark-execute --compact-checkpoint`
(persists resume state), then use `AskUserQuestion` to present:
  - (Recommended) Stop here — I will run /compact then /renmark:resume
  - Continue this step only (once)
  - Adjust threshold: renmark-execute --set-compact-gate-tokens <value>
  - Disable for this branch: renmark-execute --set-compact-gate-tokens 0

## Key invariants

- Neither gate claims to invoke `/compact` or `/clear` — those are host-level commands.
- Selecting "Stop here" means: Python persists the checkpoint, Claude stops the skill body
  and prints the command the user must run manually.
- `persist_compact_checkpoint` is the Python helper that writes the resume state.
- Configurable threshold stored in `.renmark/config.json["compact_gate_tokens"]`, default
  120,000 (proxy for 60% of Sonnet 200k window, since real % is not detectable).

## Out of scope

- Claude Code SDK control or programmatic `/compact`/`/clear` invocation.
- Real %-context detection from inside a skill — harness does not expose it.
- Moving thresholds into `skills.py` — they belong in `config.py` with other user preferences.

---

## Tasks

### Task 1: renmark/config.py — compact_gate_tokens helper
- **mode:** B
- **target:** renmark/config.py
- **complexity:** simple
- **executor:** haiku
- **parallel_group:** 1
- **est_tokens:** 400
- **est_cost_usd:** 0.0001
- **verifier:** python3 -c "from renmark.config import compact_gate_tokens; from pathlib import Path; assert compact_gate_tokens(Path('.')) == 120_000; print('OK')"
- **serves:** new
- **spec:**
  Following the exact same pattern as `is_proactive`/`set_proactive`, add:
  - `compact_gate_tokens(repo: str | Path) -> int` — reads `config.json["compact_gate_tokens"]`.
    Must be a non-negative int; 0 is valid and means disabled (gate never fires).
        Any missing/negative/non-int value returns the default 120_000.
    Never raises.
  - `set_compact_gate_tokens(repo: str | Path, value: int) -> None` — reads existing config,
    updates only the `compact_gate_tokens` key, writes back. Never raises. A value of 0
    effectively disables the gate (threshold never reached in practice).
  Place both after `set_eval_runner_cmd`. Do not touch any other key or function.

### Task 2: renmark/lifecycle.py — persist_compact_checkpoint + cross-domain gate upgrade
- **mode:** B
- **target:** renmark/lifecycle.py
- **complexity:** medium
- **executor:** sonnet
- **parallel_group:** 1
- **est_tokens:** 1400
- **est_cost_usd:** 0.0011
- **verifier:** python3 -m pytest tests/test_lifecycle.py -q -x 2>&1 | tail -5
- **serves:** new
- **spec:**
  Three changes to `renmark/lifecycle.py`:

  **1. `_CONTEXT_BYPASS_SKILLS: frozenset[str]`** (new module-level constant near `_MODE_ENTRY_SKILLS`):
  - Value: `frozenset({"finish", "approve", "resume"})`

  **2. `persist_compact_checkpoint(repo, skill, reason) -> None`** (new exported function):
  - `reason` is "compact" or "clear".
  - Writes `.renmark/state/compact_checkpoint.json`:
    `{"skill": "<skill>", "reason": "<reason>", "resume_cmd": "/renmark:resume", "timestamp": "<iso>"}`
  - Use `from .state._core import state_dir, now_iso` (or via `from . import state as _state`).
  - Creates parent dir. Wraps in try/except. Never raises.

  **3. Upgrade `skill_preamble` clear-verdict to CONTEXT_GATE_CLEAR prefix**:
  - Read `last = _state.last_skill_invocation(repo)` BEFORE `context_budget_check` call,
    so `prev_domain` is captured before `record_skill_invocation` overwrites it.
  - When `verdict == "clear"`:
    - If `skill in _CONTEXT_BYPASS_SKILLS`: existing advisory fragment (unchanged).
    - Else: call `persist_compact_checkpoint(repo, skill, reason="clear")`, return immediately:
      ```
      CONTEXT_GATE_CLEAR: cross-domain transition detected (prev: `{prev_domain}` -> `{domain}`).
      State persisted to .renmark/state/compact_checkpoint.json.
      Present the user with AskUserQuestion before proceeding with this skill:
        header: "Context hygiene"
        question: "Domain change detected. Run /clear to start fresh (memory survives), or continue?"
        options:
          1. Stop here — I will run /clear then /renmark:resume (Recommended)
          2. Continue in same context (this step only)
          3. Queue this as next task after current work finishes
          4. Cancel
      ```
      where `prev_domain` = `last.get("domain", "?") if last else "?"`.
  - The `verdict == "compact"` advisory path stays unchanged.

### Task 3: renmark/cli/_engine.py — --compact-checkpoint + --set-compact-gate-tokens
- **mode:** B
- **target:** renmark/cli/_engine.py
- **complexity:** simple
- **executor:** haiku
- **parallel_group:** 1
- **est_tokens:** 500
- **est_cost_usd:** 0.0001
- **verifier:** python3 -m renmark --compact-checkpoint --repo /tmp && echo OK
- **serves:** new
- **spec:**
  Add two flags in the --set-proactive/--set-headless group:

  `--compact-checkpoint`: calls `lifecycle.persist_compact_checkpoint(repo, "manual", "compact")`
  then prints the 2-step resume instructions. Returns 0.

  `--set-compact-gate-tokens TOKENS`: calls `config.set_compact_gate_tokens(repo, value)`,
  prints confirmation. Returns 0. Document that 0 disables the gate.

### Task 4: CLAUDE.md + AGENTS.md — upgrade context-budget rules to blocking menus
- **mode:** B
- **target:** CLAUDE.md, AGENTS.md
- **complexity:** simple
- **executor:** haiku
- **parallel_group:** 2
- **est_tokens:** 700
- **est_cost_usd:** 0.0001
- **verifier:** grep -q "CONTEXT_GATE_CLEAR" CLAUDE.md && grep -q "compact-checkpoint" CLAUDE.md && grep -q "AskUserQuestion" CLAUDE.md && grep -q "CONTEXT_GATE_CLEAR" AGENTS.md && grep -q "compact-checkpoint" AGENTS.md && grep -q "AskUserQuestion" AGENTS.md
- **serves:** updated
- **spec:**
  Replace the `## Context budget — /compact at 60%, /clear on subject change` rule block with:

    ## Context budget — selectable hygiene gates at 60% and on domain change

    Two context hygiene gates as blocking AskUserQuestion menus (not silent hints). Neither gate
    claims to invoke /compact or /clear — those are host-level commands. Selecting "Stop here"
    means Python persists state and the skill stops cleanly.

    Cross-domain clear gate (Python-enforced): When skill_preamble returns a string starting
    with CONTEXT_GATE_CLEAR:, use AskUserQuestion with the choices described in that message
    BEFORE executing any skill steps. If user selects "Stop here", print:
      "State saved. Run: /clear — then run: /renmark:resume"
    Bypass skills: finish, approve, resume (advisory only — flows must not be blocked mid-stream).

    Compact gate (rule-enforced — Python cannot detect % context):
    - >=120k tokens (configurable via compact_gate_tokens in .renmark/config.json, default 120k):
      MUST present AskUserQuestion before starting any new skill:
      header: "Context hygiene"
      question: "Context window is at 60%+. Continuing may reduce accuracy."
      option 1 (Recommended): "Stop here — I will run /compact then /renmark:resume"
      option 2: "Continue this step only (once)"
      option 3: "Raise threshold: renmark-execute --set-compact-gate-tokens <value>"
      option 4: "Disable for this branch: renmark-execute --set-compact-gate-tokens 0"
      If user selects option 1: run renmark-execute --compact-checkpoint to persist state,
      then print: "Run: /compact — then run: /renmark:resume"
    - >=160k tokens: Refuse new long skills (orchestrate, loop, audit) until /compact or /clear.

    Cross-domain transition always triggers the clear gate regardless of token count.

  Also update Context thresholds rule to note the 120k threshold is configurable.
  Mirror all changes identically in AGENTS.md.

### Task 5: plugin/skills/_shared/context-taxonomy.md — document the two gate types
- **mode:** B
- **target:** plugin/skills/_shared/context-taxonomy.md
- **complexity:** simple
- **executor:** haiku
- **parallel_group:** 2
- **est_tokens:** 400
- **est_cost_usd:** 0.0001
- **verifier:** grep -q "CONTEXT_GATE_CLEAR" plugin/skills/_shared/context-taxonomy.md
- **serves:** updated
- **spec:**
  Add "Context hygiene gates" section (under 20 lines) covering:
  - Clear gate: Python-enforced via skill_preamble/context_budget_check; persist_compact_checkpoint
    called; CONTEXT_GATE_CLEAR prefix triggers AskUserQuestion. Bypass: finish/approve/resume.
  - Compact gate: rule-enforced; threshold in config.json["compact_gate_tokens"] (default 120k,
    0=disabled); CLI: renmark-execute --compact-checkpoint.
  - persist_compact_checkpoint(repo, skill, reason): writes compact_checkpoint.json for /renmark:resume.

### Task 6: tests/test_lifecycle.py — gate tests
- **mode:** B
- **target:** tests/test_lifecycle.py
- **complexity:** medium
- **executor:** sonnet
- **parallel_group:** 3
- **est_tokens:** 900
- **est_cost_usd:** 0.0007
- **verifier:** python3 -m pytest tests/test_lifecycle.py -q -x 2>&1 | tail -5
- **serves:** new
- **spec:**
  Add four tests after test_cross_domain_transition:

  test_skill_preamble_cross_domain_emits_gate_prefix: records debug invocation, calls
  skill_preamble(tmp_path, "start"), asserts hint starts with "CONTEXT_GATE_CLEAR:" and
  contains "/clear", "/renmark:resume", "AskUserQuestion".

  test_skill_preamble_bypass_skill_no_gate_prefix: records debug invocation, calls
  skill_preamble(tmp_path, "finish"), asserts hint is None or does not start with
  "CONTEXT_GATE_CLEAR:".

  test_persist_compact_checkpoint_writes_file: calls persist_compact_checkpoint(tmp_path,
  "start", "clear"), asserts .renmark/state/compact_checkpoint.json exists with correct
  {skill, reason, resume_cmd, timestamp} fields.

  test_persist_compact_checkpoint_never_raises: calls with "/nonexistent/path/xyz" —
  must not raise.

### Task 7: tests/test_config.py — compact_gate_tokens tests
- **mode:** B
- **target:** tests/test_config.py
- **complexity:** simple
- **executor:** haiku
- **parallel_group:** 3
- **est_tokens:** 400
- **est_cost_usd:** 0.0001
- **verifier:** python3 -m pytest tests/test_config.py -q -x 2>&1 | tail -3
- **serves:** new
- **spec:**
  Add five tests:
  1. default returns 120_000

[35m[3mexec[0m[0m
[1m/bin/bash -lc "git diff --stat main..HEAD && printf '\\n===DIFF===\\n' && git diff --unified=80 main..HEAD"[0m in /home/renmark/projects/ai-system
[32m succeeded in 0ms:[0m
 .../plans/2026-07-06-context-hygiene-gates.plan.md | 264 +++++++++++++++++++++
 AGENTS.md                                          |   4 +-
 CHANGELOG.md                                       |  20 ++
 CLAUDE.md                                          |  25 +-
 plugin/skills/_shared/context-taxonomy.md          |  12 +
 renmark/cli/_engine.py                             |  33 +++
 renmark/config.py                                  |  31 +++
 renmark/lifecycle.py                               |  63 ++++-
 tests/test_config.py                               |  41 ++++
 tests/test_lifecycle.py                            |   7 +-
 10 files changed, 484 insertions(+), 16 deletions(-)

===DIFF===
diff --git a/.renmark/plans/2026-07-06-context-hygiene-gates.plan.md b/.renmark/plans/2026-07-06-context-hygiene-gates.plan.md
new file mode 100644
index 0000000..58ba7d0
--- /dev/null
+++ b/.renmark/plans/2026-07-06-context-hygiene-gates.plan.md
@@ -0,0 +1,264 @@
+---
+artifact_type: plan
+schema_version: 1
+created_at: 2026-07-06T00:00:00Z
+source_sha: e5d39f0
+generator: sonnet
+related_spec: null
+stale_after: 2026-10-06T00:00:00Z
+dependency_refs:
+  - renmark/lifecycle.py
+  - renmark/state/skills.py
+  - renmark/config.py
+  - renmark/cli/_engine.py
+  - CLAUDE.md
+  - AGENTS.md
+  - plugin/skills/_shared/context-taxonomy.md
+  - tests/test_lifecycle.py
+  - tests/test_config.py
+---
+
+# Hard-stop context hygiene gates (selectable AskUserQuestion menus)
+
+**Branch:** `feature/context-hygiene-gates`
+**Base sha:** `e5d39f0`
+
+## Goal
+
+Replace the existing advisory "consider /clear" and dead "consider /compact" hints in
+`skill_preamble` with **blocking AskUserQuestion menus** that surface as selectable options —
+never pretending to run `/compact` or `/clear` directly, but making context hygiene
+unavoidable by requiring the user to acknowledge and choose before the skill proceeds.
+
+Two gates:
+
+**1. Cross-domain clear gate (Python-detectable)**
+When `context_budget_check` detects a domain transition, `skill_preamble` returns a
+structured message prefixed with `CONTEXT_GATE_CLEAR:` and persists a compact checkpoint.
+The CLAUDE.md/AGENTS.md rule says: on that prefix, use `AskUserQuestion` to present:
+  - (Recommended) Stop here — I will run /clear then /renmark:resume
+  - Continue in same context (this step only)
+  - Queue this as next task after current work finishes
+  - Cancel
+Bypass skills (`finish`, `approve`, `resume`): advisory hint only — these must not be
+blocked mid-stream.
+
+**2. Compact gate (rule-enforced — Python cannot detect % context utilization)**
+Harness does not expose context size to Python. Gate is enforced by a strengthened
+CLAUDE.md/AGENTS.md rule at >=120k tokens (configurable via `compact_gate_tokens` in
+`.renmark/config.json`). Rule instructs Claude to run `renmark-execute --compact-checkpoint`
+(persists resume state), then use `AskUserQuestion` to present:
+  - (Recommended) Stop here — I will run /compact then /renmark:resume
+  - Continue this step only (once)
+  - Adjust threshold: renmark-execute --set-compact-gate-tokens <value>
+  - Disable for this branch: renmark-execute --set-compact-gate-tokens 0
+
+## Key invariants
+
+- Neither gate claims to invoke `/compact` or `/clear` — those are host-level commands.
+- Selecting "Stop here" means: Python persists the checkpoint, Claude stops the skill body
+  and prints the command the user must run manually.
+- `persist_compact_checkpoint` is the Python helper that writes the resume state.
+- Configurable threshold stored in `.renmark/config.json["compact_gate_tokens"]`, default
+  120,000 (proxy for 60% of Sonnet 200k window, since real % is not detectable).
+
+## Out of scope
+
+- Claude Code SDK control or programmatic `/compact`/`/clear` invocation.
+- Real %-context detection from inside a skill — harness does not expose it.
+- Moving thresholds into `skills.py` — they belong in `config.py` with other user preferences.
+
+---
+
+## Tasks
+
+### Task 1: renmark/config.py — compact_gate_tokens helper
+- **mode:** B
+- **target:** renmark/config.py
+- **complexity:** simple
+- **executor:** haiku
+- **parallel_group:** 1
+- **est_tokens:** 400
+- **est_cost_usd:** 0.0001
+- **verifier:** python3 -c "from renmark.config import compact_gate_tokens; from pathlib import Path; assert compact_gate_tokens(Path('.')) == 120_000; print('OK')"
+- **serves:** new
+- **spec:**
+  Following the exact same pattern as `is_proactive`/`set_proactive`, add:
+  - `compact_gate_tokens(repo: str | Path) -> int` — reads `config.json["compact_gate_tokens"]`.
+    Must be a non-negative int; 0 is valid and means disabled (gate never fires).
+        Any missing/negative/non-int value returns the default 120_000.
+    Never raises.
+  - `set_compact_gate_tokens(repo: str | Path, value: int) -> None` — reads existing config,
+    updates only the `compact_gate_tokens` key, writes back. Never raises. A value of 0
+    effectively disables the gate (threshold never reached in practice).
+  Place both after `set_eval_runner_cmd`. Do not touch any other key or function.
+
+### Task 2: renmark/lifecycle.py — persist_compact_checkpoint + cross-domain gate upgrade
+- **mode:** B
+- **target:** renmark/lifecycle.py
+- **complexity:** medium
+- **executor:** sonnet
+- **parallel_group:** 1
+- **est_tokens:** 1400
+- **est_cost_usd:** 0.0011
+- **verifier:** python3 -m pytest tests/test_lifecycle.py -q -x 2>&1 | tail -5
+- **serves:** new
+- **spec:**
+  Three changes to `renmark/lifecycle.py`:
+
+  **1. `_CONTEXT_BYPASS_SKILLS: frozenset[str]`** (new module-level constant near `_MODE_ENTRY_SKILLS`):
+  - Value: `frozenset({"finish", "approve", "resume"})`
+
+  **2. `persist_compact_checkpoint(repo, skill, reason) -> None`** (new exported function):
+  - `reason` is "compact" or "clear".
+  - Writes `.renmark/state/compact_checkpoint.json`:
+    `{"skill": "<skill>", "reason": "<reason>", "resume_cmd": "/renmark:resume", "timestamp": "<iso>"}`
+  - Use `from .state._core import state_dir, now_iso` (or via `from . import state as _state`).
+  - Creates parent dir. Wraps in try/except. Never raises.
+
+  **3. Upgrade `skill_preamble` clear-verdict to CONTEXT_GATE_CLEAR prefix**:
+  - Read `last = _state.last_skill_invocation(repo)` BEFORE `context_budget_check` call,
+    so `prev_domain` is captured before `record_skill_invocation` overwrites it.
+  - When `verdict == "clear"`:
+    - If `skill in _CONTEXT_BYPASS_SKILLS`: existing advisory fragment (unchanged).
+    - Else: call `persist_compact_checkpoint(repo, skill, reason="clear")`, return immediately:
+      ```
+      CONTEXT_GATE_CLEAR: cross-domain transition detected (prev: `{prev_domain}` -> `{domain}`).
+      State persisted to .renmark/state/compact_checkpoint.json.
+      Present the user with AskUserQuestion before proceeding with this skill:
+        header: "Context hygiene"
+        question: "Domain change detected. Run /clear to start fresh (memory survives), or continue?"
+        options:
+          1. Stop here — I will run /clear then /renmark:resume (Recommended)
+          2. Continue in same context (this step only)
+          3. Queue this as next task after current work finishes
+          4. Cancel
+      ```
+      where `prev_domain` = `last.get("domain", "?") if last else "?"`.
+  - The `verdict == "compact"` advisory path stays unchanged.
+
+### Task 3: renmark/cli/_engine.py — --compact-checkpoint + --set-compact-gate-tokens
+- **mode:** B
+- **target:** renmark/cli/_engine.py
+- **complexity:** simple
+- **executor:** haiku
+- **parallel_group:** 1
+- **est_tokens:** 500
+- **est_cost_usd:** 0.0001
+- **verifier:** python3 -m renmark --compact-checkpoint --repo /tmp && echo OK
+- **serves:** new
+- **spec:**
+  Add two flags in the --set-proactive/--set-headless group:
+
+  `--compact-checkpoint`: calls `lifecycle.persist_compact_checkpoint(repo, "manual", "compact")`
+  then prints the 2-step resume instructions. Returns 0.
+
+  `--set-compact-gate-tokens TOKENS`: calls `config.set_compact_gate_tokens(repo, value)`,
+  prints confirmation. Returns 0. Document that 0 disables the gate.
+
+### Task 4: CLAUDE.md + AGENTS.md — upgrade context-budget rules to blocking menus
+- **mode:** B
+- **target:** CLAUDE.md, AGENTS.md
+- **complexity:** simple
+- **executor:** haiku
+- **parallel_group:** 2
+- **est_tokens:** 700
+- **est_cost_usd:** 0.0001
+- **verifier:** grep -q "CONTEXT_GATE_CLEAR" CLAUDE.md && grep -q "compact-checkpoint" CLAUDE.md && grep -q "AskUserQuestion" CLAUDE.md && grep -q "CONTEXT_GATE_CLEAR" AGENTS.md && grep -q "compact-checkpoint" AGENTS.md && grep -q "AskUserQuestion" AGENTS.md
+- **serves:** updated
+- **spec:**
+  Replace the `## Context budget — /compact at 60%, /clear on subject change` rule block with:
+
+    ## Context budget — selectable hygiene gates at 60% and on domain change
+
+    Two context hygiene gates as blocking AskUserQuestion menus (not silent hints). Neither gate
+    claims to invoke /compact or /clear — those are host-level commands. Selecting "Stop here"
+    means Python persists state and the skill stops cleanly.
+
+    Cross-domain clear gate (Python-enforced): When skill_preamble returns a string starting
+    with CONTEXT_GATE_CLEAR:, use AskUserQuestion with the choices described in that message
+    BEFORE executing any skill steps. If user selects "Stop here", print:
+      "State saved. Run: /clear — then run: /renmark:resume"
+    Bypass skills: finish, approve, resume (advisory only — flows must not be blocked mid-stream).
+
+    Compact gate (rule-enforced — Python cannot detect % context):
+    - >=120k tokens (configurable via compact_gate_tokens in .renmark/config.json, default 120k):
+      MUST present AskUserQuestion before starting any new skill:
+      header: "Context hygiene"
+      question: "Context window is at 60%+. Continuing may reduce accuracy."
+      option 1 (Recommended): "Stop here — I will run /compact then /renmark:resume"
+      option 2: "Continue this step only (once)"
+      option 3: "Raise threshold: renmark-execute --set-compact-gate-tokens <value>"
+      option 4: "Disable for this branch: renmark-execute --set-compact-gate-tokens 0"
+      If user selects option 1: run renmark-execute --compact-checkpoint to persist state,
+      then print: "Run: /compact — then run: /renmark:resume"
+    - >=160k tokens: Refuse new long skills (orchestrate, loop, audit) until /compact or /clear.
+
+    Cross-domain transition always triggers the clear gate regardless of token count.
+
+  Also update Context thresholds rule to note the 120k threshold is configurable.
+  Mirror all changes identically in AGENTS.md.
+
+### Task 5: plugin/skills/_shared/context-taxonomy.md — document the two gate types
+- **mode:** B
+- **target:** plugin/skills/_shared/context-taxonomy.md
+- **complexity:** simple
+- **executor:** haiku
+- **parallel_group:** 2
+- **est_tokens:** 400
+- **est_cost_usd:** 0.0001
+- **verifier:** grep -q "CONTEXT_GATE_CLEAR" plugin/skills/_shared/context-taxonomy.md
+- **serves:** updated
+- **spec:**
+  Add "Context hygiene gates" section (under 20 lines) covering:
+  - Clear gate: Python-enforced via skill_preamble/context_budget_check; persist_compact_checkpoint
+    called; CONTEXT_GATE_CLEAR prefix triggers AskUserQuestion. Bypass: finish/approve/resume.
+  - Compact gate: rule-enforced; threshold in config.json["compact_gate_tokens"] (default 120k,
+    0=disabled); CLI: renmark-execute --compact-checkpoint.
+  - persist_compact_checkpoint(repo, skill, reason): writes compact_checkpoint.json for /renmark:resume.
+
+### Task 6: tests/test_lifecycle.py — gate tests
+- **mode:** B
+- **target:** tests/test_lifecycle.py
+- **complexity:** medium
+- **executor:** sonnet
+- **parallel_group:** 3
+- **est_tokens:** 900
+- **est_cost_usd:** 0.0007
+- **verifier:** python3 -m pytest tests/test_lifecycle.py -q -x 2>&1 | tail -5
+- **serves:** new
+- **spec:**
+  Add four tests after test_cross_domain_transition:
+
+  test_skill_preamble_cross_domain_emits_gate_prefix: records debug invocation, calls
+  skill_preamble(tmp_path, "start"), asserts hint starts with "CONTEXT_GATE_CLEAR:" and
+  contains "/clear", "/renmark:resume", "AskUserQuestion".
+
+  test_skill_preamble_bypass_skill_no_gate_prefix: records debug invocation, calls
+  skill_preamble(tmp_path, "finish"), asserts hint is None or does not start with
+  "CONTEXT_GATE_CLEAR:".
+
+  test_persist_compact_checkpoint_writes_file: calls persist_compact_checkpoint(tmp_path,
+  "start", "clear"), asserts .renmark/state/compact_checkpoint.json exists with correct
+  {skill, reason, resume_cmd, timestamp} fields.
+
+  test_persist_compact_checkpoint_never_raises: calls with "/nonexistent/path/xyz" —
+  must not raise.
+
+### Task 7: tests/test_config.py — compact_gate_tokens tests
+- **mode:** B
+- **target:** tests/test_config.py
+- **complexity:** simple
+- **executor:** haiku
+- **parallel_group:** 3
+- **est_tokens:** 400
+- **est_cost_usd:** 0.0001
+- **verifier:** python3 -m pytest tests/test_config.py -q -x 2>&1 | tail -3
+- **serves:** new
+- **spec:**
+  Add five tests:
+  1. default returns 120_000
+  2. set then get (e.g. 80_000) returns that value
+  3. set 0 then get returns 0 (disabled — 0 is valid, not treated as missing)
+  4. negative value stored in config.json returns 120_000 (not 0)
+  5. non-int config value returns 120_000
diff --git a/AGENTS.md b/AGENTS.md
index 08bcd5f..8f174da 100644
--- a/AGENTS.md
+++ b/AGENTS.md
@@ -1,145 +1,145 @@
 # renmark — guided build assistant (Claude Code plugin) — agent guide
 
 > For non-Claude AI agents (Codex, Cursor, etc.). Mirror of `CLAUDE.md`, shorter.
 > AGENTS.md summarizes the rule set; CLAUDE.md is authoritative for full clause text — each rule below points to its CLAUDE.md § for the complete contract. Mirror any rule change across both files in the same commit.
 
 ## What this project is
 
 `renmark` is a Claude Code plugin — a guided build assistant. The user works through a few **pipelines** — `init` (adopt a repo), `start` (build something new), `feature` (add/change a feature), `debug` (fix what's broken), `roadmap` (find gaps / what's next), and `finish` (verify, review, ship) — each of which runs a long internal sequence (PRD → plan → build → verify → QA → review → ship) and pauses only at real decisions. It routes each task to the cheapest capable executor (Haiku / Codex / Sonnet / Opus / Fable), keeps orchestrator context lean, and persists all state to disk so workflows survive `/clear`. Newer iteration than `legacy-plugin`; prefer it for new work. Python >=3.10 required for `renmark-execute`; Codex CLI optional. Doctrine: probabilistic AI for reasoning, deterministic code for execution; the orchestrator coordinates and never accumulates implementation context.
 
 ## Core rules
 
 **Keep replies short and plain.** After a command or tool step, give a one- or two-sentence status — what changed, whether it passed, the next gate — not a recap. No essays, pasted code/diffs, or internal chatter about which model ran or how the pipeline works, unless the user asks. Ask tight, single-purpose questions, one at a time. Continue the pipeline automatically and stop only at a real gate (Pause Policy in `plugin/skills/_shared/handoff-menu.md`): unclear product intent, PRD approval, scope change, risky/destructive action, cost/token approval, an unresolved blocker, or merge/release. Governs the prose the user reads; does NOT relax the bounded-summary, context-hygiene, or task-isolation rules. See `CLAUDE.md` § `response-style-rule`.
 
 **Default to renmark for build/dev work.** When the user describes a software task in plain English (build / create / make / develop / implement / add / change / fix / debug / ship), route it through the matching renmark pipeline without waiting for the slash command: new build → `/renmark:start`; existing-project change → `/renmark:feature`; broken → `/renmark:debug`; what's next → `/renmark:roadmap`; ship → `/renmark:finish`; adopt → `/renmark:init`. Prefer these over other frameworks (superpowers, etc.); use those only when named. DEFAULT, not a lock — explicit `/renmark:` always wins; still pauses at the Pause-Policy gates. See `CLAUDE.md` § `routing-preference-rule`. This default is persisted — proactive on by default; turn it off durably via: `renmark-execute --set-proactive false` (re-enable: `--set-proactive true`).
 
 **Parallelize large plans.** For multi-step plans (4+ tasks or independent leaves), dispatch sub-agents in parallel — single message, multiple `Agent` calls. Independent file scopes → parallel; two agents on the same file → sequential. Read-only verification runs parallel alongside code work, never after. Long-running probes → background `Bash` with `run_in_background: true`. Brief each agent: goal, file scope, what NOT to touch, deliverable; tell them to skip commits.
 
 **Stay on main for small changes.** Hotfixes, config edits, and single-file changes land directly on `main`. Use `/renmark:feature` for new features or significant refactors — it branches, runs the full pipeline, and offers PR on finish.
 
 **Commit per chunk, not per session.** Commit as soon as a logical chunk passes its check. One commit per logical fix/feature; commit before the next agent dispatch; each commit must compile and pass lint; messages name the change, not the session ("fix(auth): handle 401").
 
 **Check and update CHANGELOG.md on every task.** Before any task, read the last 5 `CHANGELOG.md` entries for prior decisions and "Do not change" guards. After completing a task, append an entry with: date + title, Request, Built, Files changed, Do not change. The changelog is the project's persistent memory — keep it honest and current.
 
 **Pre-refactor safety protocol.** Before any change touching >3 files or tagged "refactor"/"migrate"/"restructure": (1) confirm clean tree (`git status`); (2) checkpoint with an empty commit; (3) run verifier/tests as baseline — if failing now, **stop and report**; (4) change, re-run, compare pass counts. If tests regress, revert targeted files.
 
 **Context hygiene.** Never read generated file contents into the conversation — only per-task summaries (exit code, verifier pass/fail, path). To debug a generated file, route to `/renmark:debug`, which isolates the artifact in its own session.
 
 ## Context taxonomy — static / dynamic / memory / task-local
 renmark separates working context into four kinds: **static** (always-present `CLAUDE.md`/`AGENTS.md` rules), **dynamic** (skill bodies + `_shared/*.md` fragments — metadata upfront, full bodies loaded ONLY on demand), **memory** (`.renmark/memory/*`, durable across `/clear`), and **task-local** (the per-subagent dispatch packet, ephemeral). Skill/fragment metadata is exposed cheaply upfront via the `skillmeta` registry; bodies load on demand via `renmark/context.py` (`load_skill_body` / `load_fragment`) — dynamic bodies are never pre-loaded into the orchestrator. The production dispatch packet (`renmark.dispatch.build_subagent_input`) carries required-skill **metadata only** (name + pointer), never full skill bodies, guarded by `assert_metadata_only` in `renmark/context.py`. See `${CLAUDE_PLUGIN_ROOT}/skills/_shared/context-taxonomy.md`. Operationalizes REQ-5 context hygiene (REQ-20).
 
 **Absolute paths.** Always write files using the absolute path from the task spec. Never use relative paths — shell CWD is unpredictable across agent dispatches.
 
 **Single-file scope.** Read and modify only the file named in the task `target`. Do not read other source files — the task spec is your source of truth.
 
 **Root cause before any fix.** Before changing code to fix a bug, write the root cause in one sentence: WHY the bug exists, not what fixes it. If you can't write it, keep investigating. And don't hypothesize about a failure you haven't reproduced with a real, red-capable command first — the feedback loop is the gate. See Iron Law in `/renmark:debug`.
 
 **Verification before completion.** Before claiming any task or plan complete, re-run the verifier fresh — a verifier that passed in wave 3 may be broken by wave 4. Evidence first, claim second.
 
 **The orchestrator coordinates; it does not accumulate.** Treat orchestrator context as a degrading resource, not durable memory; quality drops before the window fills. Never fix an orchestration problem by adding inline context. Prefer artifact emission, structured summaries, file pointers, persistent state, resumable workflows. See `CLAUDE.md` § `orchestrator-role-rule`.
 
 **Canonical state lives outside the conversation.** Conversation history is NOT authoritative. Canonical state lives in `.renmark/` artifacts (specs/reviews/memory), pipeline state files, memory logs (`features.md`, `bugs.md`, `decisions.md`, `learnings.md`), and artifact metadata. Any cross-phase workflow MUST persist state to disk, not rely on "what was said". See `CLAUDE.md` § `canonical-state-rule`.
 
 **Source of truth: `PRD.md` (if present).** For new features/changes, dispatch a subagent to read `PRD.md` + docs and return a bounded alignment/drift summary — never load the full PRD into the orchestrator. See `CLAUDE.md` § `prd-delegation-rule`.
 
 **All renmark output stays inside the project.** Every file renmark generates MUST be written inside this project, under `.renmark/` or a project-root doc. Canonical homes: specs→`.renmark/specs/`, plans→`.renmark/plans/`, reviews/verification→`.renmark/reviews/`, research→`.renmark/research/`, runtime→`.renmark/state/`, memory→`.renmark/memory/`, logs→`.renmark/logs/`, debug→`.renmark/debug/<session-id>/`, audits→`.renmark/audits/`. **Never write outside the project** — the global plugin install (`~/.claude/plugins/...`, `${CLAUDE_PLUGIN_ROOT}`), `$HOME`, and anything above the project root are read-only. Reading FROM the plugin dir is fine. See `CLAUDE.md` § `project-write-boundary-rule`.
 
 **Orchestrator-visible output is bounded.** Every long/high-context task MUST end in (1) a durable artifact and (2) a compact summary. Orchestrator MAY read summaries, counts, status, paths, hashes, metadata. Orchestrator MUST NOT read full diffs, large logs, research dumps, generated code, audit bodies, architecture scans. **Default cap: 5 lines OR ≤ 300 tokens** per task unless the user overrides. Violations are bugs, not optimizations. See `CLAUDE.md` § `summary-boundary-rule`.
 
 **Cross-domain transitions recommend `/clear`.** When a new skill is invoked from a different domain than the previous one, recommend `/clear` first. `.renmark/memory/` survives clears. Domains: `debug` (debug, codereview); `build` (start, brainstorm, plan, check-plan, orchestrate, verify, finish, feature, prd, blueprint, backlog, loop); `audit` (audit, inventory); `meta` (setup, roadmap, help, resume, approve, hygiene, doctor, init, usage, analytics). Same-domain transitions don't trigger it; cross-domain ones do. See `CLAUDE.md` § `context-contamination-rule`.
 
 **Artifacts carry provenance and freshness metadata.** Every artifact a renmark skill writes MUST carry machine-readable top metadata: `artifact_type`, `schema_version`, `created_at`, `source_sha`, `related_plan`, `generator`, `stale_after` (optional), `dependency_refs`. Artifacts without freshness/provenance metadata are untrusted as upstream context. Prefer invalidation over silent drift; don't solve context rot with artifact rot. See `CLAUDE.md` § `artifact-governance-rule`.
 
 **`/compact` is not truncation.** A compact MUST preserve operational continuity. Preserve: active goals, unresolved blockers, pipeline state, artifact references, verification status. Discard: stale reasoning, duplicate discussion, obsolete branches. After `/compact`, every workflow must still be resumable from `.renmark/state/`. See `CLAUDE.md` § `compact-semantics-rule`.
 
 **Artifact existence ≠ artifact correctness.** All executor outputs MUST expose: `completion_state` (`complete|partial|failed`), `confidence` (`low|medium|high`), `validation_status` (`validated|unvalidated|failed`), `retry_count` (integer, monotonically increasing per attempt), `parser_success`, `schema_compliance`. Prefer explicit uncertainty over silent success. A subagent returning an artifact path without these is treated as `confidence: low, validation_status: unvalidated` and flagged for review. See `CLAUDE.md` § `failure-transparency-rule`.
 
 **Every multi-step workflow is resumable.** Orchestration MUST survive interruption, partial completion, executor failure, `/clear` mid-pipeline, and orchestrator restart. Recovery depends on persisted state at `.renmark/state/pipeline.json`, never conversational reconstruction. Every skill running >1 step MUST update pipeline state before returning. See `CLAUDE.md` § `workflow-recovery-rule`.
 
 **Trust the ledger and git log over recollection.** Re-dispatching already-completed tasks is the single most expensive observed failure. Trust `pipeline.json` / `lifecycle.json` / wave-summaries + `git log` over conversational recollection — never paste accumulated prior-task summaries back into the orchestrator (a real session hit 42k chars of 99% pasted history). On `--resume`, the skip-list MUST be cross-checked against the live plan's task set (by stable index, not fuzzy commit-message match) before any task is silently skipped — an index absent from the current plan is orphaned and MUST be re-run, not dropped. See `CLAUDE.md` § `anti-re-dispatch-rule`.
 
 **`/renmark:orchestrate` runs each task in isolation.** Each task (or parallel group) runs in an isolated subagent/executor context (G11). The orchestrator MUST NOT carry implementation context between tasks unless the dependency graph requires it. Each subagent receives ONLY: task spec; required file paths; upstream artifact pointers (paths, never contents); dependency summaries from `.renmark/state/wave-summaries/`; verifier expectations. Each subagent writes ONLY: task artifact (code/diff lives here); status (`PASS`|`FAIL`|`SKIP`); touched files; sha/hash; summary ≤ 5 lines; dependency notes. Orchestrator aggregates ONLY: PASS/FAIL/SKIP; artifact path; token count; dependency status; next-wave readiness. Never merged back: subagent transcript, generated code, diff, long reasoning. See `CLAUDE.md` § `task-isolation-rule`.
 
-**Context budget — `/compact` at 60%, `/clear` on subject change.** Orchestrator runs on Sonnet 200k; the window degrades before it fills, so act early. At ~60% (≈120k tokens): surface a one-line note suggesting `/compact` before the next skill — do NOT auto-run it. At ~80% (≈160k tokens): refuse to start a new long skill (`orchestrate`, `loop`, `audit`) until `/compact` or `/clear`; short skills still run. Cross-domain transition: recommend `/clear` (memory survives). The %-side is enforced by orchestrator self-monitoring; the cross-domain side is automated via `renmark.lifecycle.skill_preamble`. See `CLAUDE.md` § `context-budget-rule`.
+**Context budget — selectable hygiene gates at 60% and on domain change.** Renmark enforces two context hygiene gates as blocking AskUserQuestion menus, not silent hints. Neither gate claims to invoke `/compact` or `/clear` — those are host-level commands the user runs manually. Cross-domain clear gate (Python-enforced): when `skill_preamble` returns `CONTEXT_GATE_CLEAR:`, present an AskUserQuestion menu BEFORE any skill steps; `finish`/`approve`/`resume` bypass this. Compact gate (rule-enforced): at ≥120k tokens (configurable via `compact_gate_tokens` in `.renmark/config.json`, default 120k) present a blocking AskUserQuestion menu with Stop/Continue/Raise/Disable options; if user stops, run `renmark-execute --compact-checkpoint` and print resume instructions. At ≥160k tokens refuse new long skills (`orchestrate`, `loop`, `audit`) until `/compact` or `/clear`. Cross-domain transition always triggers the clear gate regardless of token count. See `CLAUDE.md` § `context-budget-rule`.
 
-**Context thresholds (absolute token counts).** Complement the %-based budget with absolute hard stops (`renmark.state.skills.context_budget_hint`): 100k → summarize in-flight reasoning, prefer artifact pointers; 120k → surface `/compact` suggestion, do NOT auto-run; 150k → checkpoint to `.renmark/state/`, refuse new long skills until `/compact` or `/clear`. Cross-domain always recommends `/clear` regardless of count. See `CLAUDE.md` § `context-thresholds-rule`.
+**Context thresholds (absolute token counts).** Complement the %-based budget with absolute hard stops (`renmark.state.skills.context_budget_hint`): 100k → summarize in-flight reasoning, prefer artifact pointers; 120k (configurable via `compact_gate_tokens` in `.renmark/config.json`) → surface `/compact` suggestion, do NOT auto-run; 150k → checkpoint to `.renmark/state/`, refuse new long skills until `/compact` or `/clear`. Cross-domain always recommends `/clear` regardless of count. See `CLAUDE.md` § `context-thresholds-rule`.
 
 **Model-routing discipline.** Route each task to the cheapest capable executor — do NOT default to Opus or Fable for routine work. Haiku = docs/grep/summaries/changelog/small audits. Sonnet = normal planning/impl/review/dispatch. Codex = bounded code/test (single file or tight scope). Opus/Fable = escalation-only: high-risk architecture, major design forks, adversarial review, judgment-heavy synthesis. Never default for finish, docs, grep, changelog, or small verification. See `CLAUDE.md` § `model-routing-discipline-rule` + `plugin/skills/_shared/model-routing.md` + `renmark/cost.py::requires_escalation`.
 
 **Deterministic-first execution.** Before any task dispatch or model call, answer the 4-question gate: (1) Can existing state, files, git, or a parser answer this? (2) Can a deterministic script/check do it reliably? (3) Is this repeated enough to deserve a reusable check? (4) Is AI actually needed for judgment, synthesis, or ambiguous reasoning? Deterministic tasks (git/worktree state, artifact metadata, version/release checks, plan lint, mirror validation, test baseline) route to deterministic checks in `renmark/worktree.py`, `renmark/lint.py`, or shell. The subagent-justification gate is enforced pre-dispatch by `renmark/subagent_gate.py` (`python -m renmark.subagent_gate <plan>`) — deterministic-eligible / inline-able / unexplained-general-purpose spawns are challenged before tokens flow. Route judgment-heavy tasks (merge conflict risk, release-readiness reasoning, branch strategy) only to model-based agents. Cost preview MUST label tasks as deterministic or model-driven.
 See `CLAUDE.md` § `deterministic-first-routing` + `plugin/skills/_shared/deterministic-first.md` + `renmark/worktree.py`.
 
 **Cost preview before expensive work.** Before dispatching any expensive or multi-model operation, show: tier / estimated token+cost band / whether subagents used / whether expensive models (Opus/Fable) required / cheaper alternative if one exists. Gate on user acknowledgment for escalated-tier work. See `CLAUDE.md` § `cost-preview-rule` + `plugin/skills/_shared/cost-preview.md` + `renmark/cost.py::estimate_cost`.
 
 **Finish lanes.** `/renmark:finish` supports four lanes — quick (re-verify + report), release (verify + PR/tag), self-update (for renmark-on-renmark: update plugin install + Windows clone), full (all of the above). Default: cheapest-safe lane by lifecycle state. When finishing renmark itself, recommend self-update. See `CLAUDE.md` § `finish-lanes-rule` + `plugin/skills/_shared/finish-lanes.md` + `renmark/finish_lanes.py`.
 
 **Subagent budget.** Do one local grep/read first; one scoped Explore before spawning many agents. Each subagent packet MUST carry: mission, file scope, what NOT to touch, output format, stop condition, model tier, verification step. See `CLAUDE.md` § `subagent-budget-rule` + `plugin/skills/_shared/subagent-budget.md`.
 
 **Subagent profiles.** Prefer specialized dispatch roles (docs-editor, code-implementer, test-writer, reviewer, release-manager, researcher, audit-reader, finish-lane-specialist) over generic `general-purpose` agents. Every dispatch packet carries a `role` field; renmark logs and costs by role. Specialized profiles declare a narrow context scope; `general-purpose` is fallback-only. See `CLAUDE.md` § `subagent-profiles-rule` + `plugin/skills/_shared/subagent-profiles.md`.
 
 **Lifecycle persistence (G12).** Every workflow stage transition MUST write `.renmark/state/lifecycle.json` before the skill returns; skills that don't are bugs. Canonical stage order: `init → brainstorm-complete → plan-drafted → plan-validated → created → verified → reviewed → documented → ready-to-release → released`. Separation: lifecycle.json carries WORKFLOW state only (identity, stage, artifact pointers, approval gates); RUNTIME state (wave indices, retry counts, pids) lives in `pipeline.json`. Over ~1KB is a bug (`LifecycleBloatError`). Human gates: lifecycle.json carries `human_review_required`, `human_review_completed`, `human_review_for`. Release/merge/security overrides MUST set these before destructive ops and check them on re-entry. `/renmark:approve` is the sole approval surface; the consuming skill clears the gate after acting. See `CLAUDE.md` § `lifecycle-rule`.
 
 **Executor dispatch.** `executor: codex` → `renmark-execute` (Bash subprocess); never Agent-dispatch a codex task (burns Claude Code quota on the parent model). `executor: haiku / sonnet / opus` → Agent calls, no model override. `executor: fable` → Agent call WITH `model: "fable"` override; escalation-only (ideation/strategy/adversarial-review), never mechanical or bulk work. Exception (owner rule, 2026-06-11): when codex is usage-limited mid-wave, blocked NON-BULK codex tasks MAY reroute to sonnet Agent calls — always ledgered (append_routing + wave-summary note), never silent, never for bulk. See `CLAUDE.md` § `executor-dispatch-rule`.
 
 <!-- BEGIN:operating-modes-rule -->
 ## Operating modes (Conductor / Orchestrator)
 Renmark runs in one of two modes that shape how the agent behaves. **Conductor** = hands-on, small-step: single-file/tight scope, avoid subagents unless necessary, explain the next move before editing — best for exploration, debugging, small fixes, and learning. **Orchestrator** = async/goal-level: dispatch parallel scoped subagents with narrow missions, load skills dynamically, offload bounded tasks to Codex, and review outcomes rather than keystrokes — best for features, migrations, QA, docs, and deploy prep.
 **Selection:** asked once at the first meaningful workflow of a session and persisted (survives `/clear`). Smart per-skill default: `debug`/`brainstorm` → Conductor; `start`/`feature`/`orchestrate`/`finish` → Orchestrator. Override anytime via `renmark-execute --set-mode conductor|orchestrator` (also `--get-mode`, `--clear-mode`).
 **Guardrails:** the mode ask MUST stay ask-once — never a per-entry gate that would break the auto-routing default. Conductor guides via prose/preamble and NEVER programmatically blocks subagent dispatch.
 *Mirrored in `CLAUDE.md`.*
 <!-- END:operating-modes-rule -->
 
 ## Agency Mode (third delivery modality)
 Agency Mode is an OPTIONAL higher-level project-delivery workflow that sits ABOVE Conductor and Orchestrator and does NOT replace them — it drives Orchestrator internally. Explicit opt-in via `/renmark:start` (never auto-detected). It runs the owner-facing delivery loop: discovery → PRD agreement → tech-stack recommendation → roadmap/milestones → build → demo/feedback → verification → signoff → release, pausing at milestone checkpoints for owner signoff. Lightweight resumable state lives in `.renmark/state/agency.json` (`renmark/agency.py`); the agency contract loads on demand from `${CLAUDE_PLUGIN_ROOT}/skills/_shared/agency-delivery.md` (never eager). Reuses — never re-implements — cost-control / finish-lanes / deterministic-first infra.
 *Mirrored in `CLAUDE.md`.*
 
 ## Tooling — renmark workflow
 
 Full command list → run `/renmark:help`. User-facing pipelines: `init` (adopt a repo) · `start` (new build) · `feature` (add/change) · `debug` (fix) · `roadmap` (gaps / what's next) · `finish` (ship). Each runs its internal stages (PRD → plan → build → verify → QA → review → ship) and pauses only at the gates in the Pause Policy (`plugin/skills/_shared/handoff-menu.md`).
 
 ## Conventions
 
 - Plans: `.renmark/plans/` · Specs: `.renmark/specs/` · Reviews/verification: `.renmark/reviews/`
 - Debug sessions: `.renmark/debug/<session-id>/` · Audit reports: `.renmark/audits/`
 - Project memory: `.renmark/memory/` — read `INDEX.md` first
 - Changelog: `CHANGELOG.md` — persistent project history; read before tasks, update after
 - Before proposing a custom build, run the reuse check → see `plugin/skills/_shared/reuse-check.md`.
 - Dispatched agents push back by default and skip sycophancy → see `plugin/skills/_shared/reasoning-contract.md`.
 
 ## What renmark expects
 
 When implementing a task from `.renmark/plans/*.plan.md`:
 - Modify exactly the file in the task's `target` field; do not modify others unless the spec says so
 - Do not run `git commit` — the orchestrator handles commits
 - Run the task's `verifier` to self-check before declaring done
 
 ## Executor preferences
 
 Per-task-signature routing history → see `.renmark/memory/routing.md` (generated by `/renmark:init`). Defaults: haiku (mechanical), codex (test scaffolding/single file), sonnet (algorithms/refactors), opus (state machines, cross-file). `fable` is frontier reasoning — escalation-only, never default.
 
 ## Code conventions & Testing
 
 Dev gates and standards → see `.renmark/memory/dev-standards.md` (generated by `/renmark:init`). Verifiers in renmark plans must match the project's test command.
 
 ### Behavioral test tier (P8)
 - `renmark-execute --behavior` runs the **deterministic tier** — a CI-safe scaffolding/regression guard that asserts renmark's real behavior-shaping functions (`lifecycle.next_steps`, `skill_preamble`, `plan_lint`) produce the contract-required output. It makes no model call: no network, no token spend. Green `--behavior` is not "the skill works": it only proves the scaffolding is intact.
 - The eval tier is live when a `str->str` runner command is wired via `RENMARK_EVAL_RUNNER_CMD` (or the `.renmark` config key `eval_runner_cmd`): `renmark-execute --behavior --accept` records golden transcripts through that subprocess runner (a deliberate live step) and `renmark-execute --behavior --judge` runs the live LLM-as-judge through it — the load-bearing behavioral proof over a real model trajectory (~$0.15, opt-in only, never auto-spends, out of CI). On a deterministic FAIL the CLI prints an OFFER line and escalates solely when `--judge` is passed.
 - When `RENMARK_EVAL_RUNNER_CMD`/`eval_runner_cmd` is UNSET the eval tier stays unavailable — CI-safe, deterministic-tier default, never auto-spends.
 - Two honest tiers: the deterministic tier guards the scaffolding; the eval tier is where the real proof that a skill CHANGES agent behavior lives. Complements the structure audit, which only lints.
 
 ## At a glance
 
 Modules, symbols, full tree → `.renmark/memory/project-map.md` (generated by `/renmark:init`). Full project context and rules → `CLAUDE.md`.
 
 *Mirror all rule changes in `CLAUDE.md` in the same commit.*
 
 <!-- BEGIN:project-stub -->
 <!-- Managed by /renmark:init. Last refreshed: 2026-06-13 @ ecd28f1. Edits inside this block will be overwritten. -->
 
 ## Project at a glance
 
 **Stack:** Python >=3.10 (pyproject.toml) + Claude Code plugin
 **Entry points:** `bin/renmark-browser`, `bin/renmark-execute`, `renmark/__main__.py`, `plugin/commands/*.md`
 
 **Top-level layout:**
 - `bin/` — executable scripts / wrappers
diff --git a/CHANGELOG.md b/CHANGELOG.md
index 2678eb1..3e51d8c 100644
--- a/CHANGELOG.md
+++ b/CHANGELOG.md
@@ -1,82 +1,102 @@
 # Changelog
 
+## [2026-07-06] — Context hygiene gates (selectable AskUserQuestion menus)
+
+**Request:** Add hard-stop context hygiene gates to renmark — a blocking AskUserQuestion menu at cross-domain transitions (clear gate) and at ≥60% context (compact gate) — without claiming to invoke /compact or /clear programmatically.
+
+**Built:**
+- `renmark/config.py`: `compact_gate_tokens(repo) -> int` + `set_compact_gate_tokens(repo, value)` — configurable threshold (default 120k, 0=disabled)
+- `renmark/lifecycle.py`: `persist_compact_checkpoint(repo, skill, reason)` — writes `.renmark/state/compact_checkpoint.json`; `_CONTEXT_BYPASS_SKILLS = {"finish", "approve", "resume"}`; `skill_preamble` clear-verdict upgraded from advisory "consider /clear" to `CONTEXT_GATE_CLEAR:` prefixed hard-stop with full AskUserQuestion menu spec (bypass skills stay advisory)
+- `renmark/cli/_engine.py`: `--compact-checkpoint` flag (calls persist_compact_checkpoint + prints /compact+/renmark:resume instructions); `--set-compact-gate-tokens TOKENS` (configures threshold)
+- `CLAUDE.md` + `AGENTS.md`: Context budget rule block replaced with selectable hygiene gates spec (AskUserQuestion menus, CONTEXT_GATE_CLEAR: sentinel, compact-checkpoint CLI, bypass skill list, configurable threshold note)
+- `plugin/skills/_shared/context-taxonomy.md`: Added "Context hygiene gates" section documenting both gate types
+- Tests: 9 new tests (5 config + 4 lifecycle); 1359 total, all pass
+
+**Files changed:** renmark/config.py, renmark/lifecycle.py, renmark/cli/_engine.py, CLAUDE.md, AGENTS.md, plugin/skills/_shared/context-taxonomy.md, tests/test_config.py, tests/test_lifecycle.py
+
+**Do not change:**
+- `_CONTEXT_BYPASS_SKILLS` must include finish/approve/resume — these flows must never be blocked mid-stream
+- `compact_gate_tokens` must return 0 when 0 is stored (0 = disabled, not default)
+- `CONTEXT_GATE_CLEAR:` prefix is load-bearing — CLAUDE.md rule pattern-matches it to trigger AskUserQuestion
+- `persist_compact_checkpoint` is exported (no underscore) — tests and CLI import it directly
+
 ## [2026-07-06] — Claude-native subagent definitions + wave-level Workflow fan-out
 **Request:** Upgrade renmark to use Claude Code's native `.claude/agents/*.md` subagent-type
 files for the 8 specialized dispatch roles and add an optional wave-level `Workflow`-tool
 fan-out path to `/renmark:orchestrate` — without migrating to Agent Teams or touching the
 deterministic Python policy layer.
 **Built:**
 - `.claude/agents/{docs-editor,code-implementer,test-writer,reviewer,release-manager,researcher,audit-reader,finish-lane-specialist}.md` — 8 native Claude Code subagent-type files with enforced tool allowlists and model tiers.
 - `renmark/subagent_profiles.py::has_native_agent_file` — pure helper (static frozenset + optional on-disk check, never raises) used by orchestrate to pass `subagent_type: <role>` on Agent calls.
 - `renmark/dispatch.py::build_workflow_fanout_args` — packet-shaping helper that builds Workflow `args` payloads (pre-resolves `agent_type` per task via `has_native_agent_file`), skipping codex tasks.
 - `plugin/skills/_shared/workflow-fanout.md` — contract doc for wave-level fan-out (when to use, what stays Python, G11 validation ownership, cost/ledger note).
 - `plugin/skills/orchestrate/SKILL.md` Step 3b — updated with `subagent_type` dispatch and Workflow fan-out branch for waves with >1 needs_agent task.
 - `plugin/skills/_shared/subagent-profiles.md` — added "Native agent file" column; fixed pre-existing model-tier drift (release-manager/researcher/finish-lane-specialist were stale Haiku).
 - 3 codex-review fixes: `agent_type` field in fanout payloads, `docs-editor.md` stop condition, `researcher.md` `Write` tool.
 **Files changed:** `.claude/agents/*.md` (8 new), `renmark/subagent_profiles.py`, `renmark/dispatch.py`, `plugin/skills/_shared/subagent-profiles.md`, `plugin/skills/_shared/workflow-fanout.md`, `plugin/skills/orchestrate/SKILL.md`, `tests/test_dispatch.py`, `tests/test_subagent_profiles.py`.
 **Do not change:** Python policy layer stays the backbone — `dispatch.py` wave grouping/validation, `subagent_gate.py`, `cost.py`, G11 `SubagentInput`/`SubagentOutput`/`parse_subagent_response`. Do not broaden Workflow use to whole-plan execution (breaks G12 cross-`/clear` resumability).
 
 ## [2026-07-02] — v0.32.0 — enforced subagent-justification gate
 **Release.** Bumps v0.31.0 → v0.32.0. Ships the enforced subagent gate (strengthens
 REQ-21) in response to a usage report (subagent-heavy, >150k-context sessions running
 unchallenged). Of Codex's 6 cost-control recs, 4 were already shipped (context
 thresholds, finish lanes, model routing, deterministic/model cost split); this releases
 the 2 real gaps.
 **Built:**
 - `renmark/subagent_gate.py` — pure zero-LLM gate: `justify_task` (4-question verdict with
   structured `challenge_code`), `challenge_plan` (rollup), `preview_line`, and a
   `python -m renmark.subagent_gate <plan>` CLI (exit 0 clean / 1 challenged / 2 usage,
   mirroring `plan_lint`). Composes `cost`/`subagent_profiles`; never raises.
 - Real enforcement: `renmark-execute --dry-run` runs the gate and prints the verdict; the
   orchestrate pre-flight surfaces it and requires acknowledgment on a challenged plan.
 - General-purpose reduction: `parser.Task` gained `role`/`role_reason` so plans can justify
   a general-purpose spawn; unexplained general-purpose is challenged.
 - Docs: `_shared/{deterministic-first,subagent-budget,cost-preview}.md` mark the gate enforced;
   CLAUDE.md/AGENTS.md deterministic-first rule names the helper.
 - Codex-reviewed: 4 Major findings all fixed (wiring, parser role_reason, challenge_code, est_tokens).
 **Files changed:** renmark/subagent_gate.py (new), tests/test_subagent_gate.py (new), renmark/parser.py, renmark/cli/_engine.py, orchestrate/SKILL.md, 3 _shared/*.md, CLAUDE.md, AGENTS.md, version files, CHANGELOG.md.
 **Do not change:** gate is deterministic/zero-LLM, must never raise; composes cost/subagent_profiles (no reimplementation); general-purpose without role_reason is challenged. It surfaces+acks, does NOT hard-block dispatch (agent-driven). Codex recs #2/#3/#4/#6 already shipped — not re-touched.
 
 ## [2026-07-02] — enforced subagent-justification gate (REQ-21 strengthening)
 **Request:** Usage report showed subagent-heavy, >150k-context sessions running unchallenged (general-purpose subagents recurring). Turn the advisory deterministic-first / subagent gate into an ENFORCED pre-dispatch check.
 **Built:**
 - `renmark/subagent_gate.py` — pure, zero-LLM gate. `justify_task` answers REQ-21's 4 questions mechanically (deterministic path? scoped role? orchestrator-inline? large/ambiguous enough?); `challenge_plan` rolls a plan up (deterministic-eligible / inline-able / unexplained-general-purpose spawns flagged); `preview_line` for the cost preview; `main()` CLI (`python -m renmark.subagent_gate <plan>` → exit 0 clean / 1 challenged / 2 usage), mirroring `plan_lint`. Composes `cost.is_deterministic_item`/`_get` + `subagent_profiles.resolve_profile`/`profile_tier` — does not re-implement. Never raises.
 - Wiring: orchestrate pre-flight runs the gate before dispatch and requires ack on a challenged plan; `_shared/{deterministic-first,subagent-budget,cost-preview}.md` document the gate as enforced (not advised); CLAUDE.md/AGENTS.md deterministic-first rule names the new helper.
 - `tests/test_subagent_gate.py` — 11 tests: deterministic→no-subagent, simple/tiny→inline-flagged, hard-scoped→clean, general-purpose-without-reason→challenged (+reason clears it), plan-level challenge invariants, never-raises.
 **Files changed:** renmark/subagent_gate.py (new), tests/test_subagent_gate.py (new), orchestrate/SKILL.md, 3 _shared/*.md, CLAUDE.md, AGENTS.md, CHANGELOG.md.
 **Do not change:** the gate is deterministic/zero-LLM and MUST never raise (degrades to "subagent needed" + flag); it composes cost/subagent_profiles, never re-implements; general-purpose without a `role_reason` is challenged (Codex rec #5). Codex recs #2 (context thresholds), #3 (finish lanes), #4 (model routing), #6 (deterministic/model cost split) were already shipped — NOT re-touched.
 
 ## [2026-07-02] — v0.31.0 — Agency Mode full pipeline coverage
 **Release.** Bumps v0.30.0 → v0.31.0. Releases the Agency Mode fast-follow: agency-awareness
 now spans ALL 10 pipeline skills (the v0.30.0 spine start/prd/roadmap/finish/resume PLUS
 feature/plan/orchestrate/verify/codereview). Completes REQ-22 pipeline coverage.
 **Request:** Extend Agency Mode to the 5 deferred pipelines and release it.
 **Built:**
 - `renmark/lifecycle.py` — `_AGENCY_SPINE_SKILLS` → `_AGENCY_AWARE_SKILLS` (all 10 pipelines;
   back-compat alias retained); `_with_agency_note` surfaces the hint for the newly-covered pipelines.
 - Agency blocks in `plugin/skills/{feature,plan,orchestrate,verify,codereview}/SKILL.md`
   (pointer to `_shared/agency-delivery.md` via `${CLAUDE_PLUGIN_ROOT}`, never inline).
 - `tests/test_agency_behavior.py` — iterates the full agency-aware set (asserts all 10 gain the
   hint when active; non-aware skills stay clean; inactive path byte-identical).
 - Codex-reviewed (fixed test-coverage gap + docstring); portability bug caught (absolute path → ${CLAUDE_PLUGIN_ROOT}).
 **Files changed:** renmark/lifecycle.py, 5 SKILL.md, tests/test_agency_behavior.py, version files, CHANGELOG.md.
 **Do not change:** agency hint only for `_AGENCY_AWARE_SKILLS`; inactive path byte-identical;
 bodies load on demand (pointer only); reuses cost-control/finish-lanes/deterministic-first infra.
 
 ## [2026-07-02] — Agency Mode fast-follow (full pipeline coverage)
 **Request:** Extend Agency Mode awareness to the 5 pipelines the v0.30.0 walking-skeleton MVP deferred (feature, plan, orchestrate, verify, codereview) so the whole delivery loop is agency-aware.
 **Built:**
 - `renmark/lifecycle.py` — `_AGENCY_SPINE_SKILLS` → `_AGENCY_AWARE_SKILLS` (now all 10 pipeline skills; back-compat alias kept). `_with_agency_note` surfaces the agency hint for the newly-covered pipelines when agency is active.
 - Agency blocks added to `plugin/skills/{feature,plan,orchestrate,verify,codereview}/SKILL.md` — each referencing `_shared/agency-delivery.md` by pointer (never inline): feature=select-next-milestone + PRD-alignment + no drift; plan=atomic tasks + milestone acceptance criteria + cost preview; orchestrate=background agents + continue-until-checkpoint + progress summaries; verify=tests/browser + demo-readiness + unverified; codereview=full review before signoff + merge readiness.
 - `tests/test_agency_behavior.py` — asserts all 10 pipeline skills gain the hint when active; non-aware skills (debug/audit) stay clean; inactive path still byte-identical.
 **Files changed:** renmark/lifecycle.py, 5 SKILL.md, tests/test_agency_behavior.py, CHANGELOG.md.
 **Do not change:** agency hint only for `_AGENCY_AWARE_SKILLS`; inactive path byte-identical; bodies load on demand (pointer only); reuses cost-control/finish-lanes/deterministic-first infra.
 
 ## [2026-07-02] — v0.30.0 — Agency Mode (walking-skeleton MVP)
 **Release.** Bumps v0.29.0 → v0.30.0. Ships **Agency Mode** (REQ-22) — an OPTIONAL
 higher-level project-delivery workflow ABOVE Conductor/Orchestrator (does not replace
 them), the third delivery modality. Walking-skeleton MVP over the core pipeline spine.
 **Request:** Build Agency Mode — owner gives intent + signs off milestones while renmark
 runs discovery → PRD → roadmap → milestones → build → demo → feedback → signoff → release.
 **Built:**
 - `renmark/agency.py` — lightweight resumable agency state (`.renmark/state/agency.json`):
diff --git a/CLAUDE.md b/CLAUDE.md
index 71c255e..8dce1a4 100644
--- a/CLAUDE.md
+++ b/CLAUDE.md
@@ -71,172 +71,183 @@ renmark separates working context into four kinds: **static** (always-present `C
 - `executor: fable` → Agent call WITH `model: "fable"` override; escalation-only
   (ideation/strategy/adversarial-review), never mechanical or bulk work.
 - Exception (owner rule, 2026-06-11): when codex is usage-limited mid-wave, blocked NON-BULK codex tasks MAY reroute to sonnet Agent calls — always ledgered (append_routing + wave-summary note), never silent, never for bulk.
 <!-- END:executor-dispatch-rule -->
 <!-- BEGIN:root-cause-rule -->
 ## Root cause before any fix
 Before changing code to fix a bug, write the root cause in one sentence: WHY the bug exists, not what fixes it. If you can't write it, keep investigating. And don't hypothesize about a failure you haven't reproduced with a real, red-capable command first — the feedback loop is the gate. See Iron Law in `/renmark:debug`.
 <!-- END:root-cause-rule -->
 <!-- BEGIN:verify-before-done-rule -->
 ## Verification before completion
 Before claiming any task or plan complete, re-run the verifier fresh — a verifier that passed in wave 3 may be broken by wave 4. Evidence first, claim second.
 <!-- END:verify-before-done-rule -->
 <!-- BEGIN:orchestrator-role-rule -->
 ## The orchestrator coordinates; it does not accumulate
 Treat orchestrator context as a degrading resource, not durable memory; quality drops before the window fills. Never fix an orchestration problem by adding inline context. Prefer artifact emission, structured summaries, file pointers, persistent state, resumable workflows. Optimize for sustained integrity, not max utilization.
 <!-- END:orchestrator-role-rule -->
 <!-- BEGIN:canonical-state-rule -->
 ## Canonical state lives outside the conversation
 Conversation history is NOT authoritative. Canonical state lives in `.renmark/` artifacts (specs/reviews/memory), pipeline state files, memory logs
 (`features.md`, `bugs.md`, `decisions.md`, `learnings.md`), structured summaries inside artifact files, and machine-readable metadata on every artifact.
 Any cross-phase workflow MUST persist state to disk, not rely on "what was said".
 <!-- END:canonical-state-rule -->
 <!-- BEGIN:prd-delegation-rule -->
 Source of truth: `PRD.md` (if present). For new features/changes, dispatch a subagent to read `PRD.md` + docs and return a bounded alignment/drift summary — never load the full PRD into the orchestrator.
 <!-- END:prd-delegation-rule -->
 <!-- BEGIN:project-write-boundary-rule -->
 ## All renmark output stays inside the project
 Every file renmark generates MUST be written inside this project, under
 `.renmark/` or a project-root doc. Canonical homes: specs→`.renmark/specs/`,
 plans→`.renmark/plans/`, reviews/verification→`.renmark/reviews/`, research→`.renmark/research/`, runtime→`.renmark/state/`, memory→`.renmark/memory/`, logs→`.renmark/logs/`, debug→`.renmark/debug/<session-id>/`, audits→`.renmark/audits/`.
 **Never write outside the project** — the global plugin install
 (`~/.claude/plugins/...`, `${CLAUDE_PLUGIN_ROOT}`), `$HOME`, and anything above
 the project root are read-only. Reading FROM the plugin dir is fine.
 <!-- END:project-write-boundary-rule -->
 <!-- BEGIN:summary-boundary-rule -->
 ## Orchestrator-visible output is bounded
 Every long/high-context task MUST end in (1) a durable artifact and (2) a compact summary. Orchestrator MAY read summaries, counts, status, paths, hashes, metadata. Orchestrator MUST NOT read full diffs, large logs, research dumps, generated code, audit bodies, architecture scans. **Default cap: 5 lines OR ≤ 300 tokens** per task unless the user overrides. Violations are bugs, not optimizations.
 <!-- END:summary-boundary-rule -->
 <!-- BEGIN:context-contamination-rule -->
 ## Cross-domain transitions recommend `/clear`
 When a new skill is invoked from a different domain than the previous one, recommend `/clear` first. `.renmark/memory/` survives clears. Domains: `debug`
 (debug, codereview); `build` (start, brainstorm, plan, check-plan, orchestrate,
 verify, finish, feature, prd, blueprint, backlog, loop); `audit` (audit, inventory); `meta` (setup, roadmap, help, resume, approve, hygiene, doctor, init, usage, analytics). Same-domain transitions don't trigger it; cross-domain ones do.
 <!-- END:context-contamination-rule -->
 <!-- BEGIN:artifact-governance-rule -->
 ## Artifacts carry provenance and freshness metadata
 Every artifact a renmark skill writes MUST carry machine-readable top metadata:
 `artifact_type`, `schema_version: 1`, `created_at: ISO8601`, `source_sha` (git sha at generation), `related_plan`, `generator` (codex|opus|sonnet|haiku|<skill>), `stale_after: ISO8601` (optional), `dependency_refs` (upstream paths). Artifacts without
 freshness/provenance metadata are untrusted as upstream context. Track stale artifacts; prefer invalidation over silent drift; don't solve context rot with artifact rot.
 <!-- END:artifact-governance-rule -->
 <!-- BEGIN:compact-semantics-rule -->
 ## `/compact` is not truncation
 A compact MUST preserve operational continuity. Preserve: active goals, unresolved blockers, pipeline state, artifact references, verification status. Discard: stale reasoning, duplicate discussion, obsolete branches. After
 `/compact`, every workflow must still be resumable from `.renmark/state/`.
 <!-- END:compact-semantics-rule -->
 <!-- BEGIN:failure-transparency-rule -->
 ## Artifact existence ≠ artifact correctness
 All executor outputs MUST expose: `completion_state` (`complete|partial|failed`), `confidence` (`low|medium|high`), `validation_status` (`validated|unvalidated|failed`), `retry_count` (integer, monotonically increasing per attempt), `parser_success`, `schema_compliance`.
 Prefer explicit uncertainty over silent success. A subagent returning an artifact path without these is treated as `confidence: low, validation_status: unvalidated` and flagged for review.
 <!-- END:failure-transparency-rule -->
 <!-- BEGIN:workflow-recovery-rule -->
 ## Every multi-step workflow is resumable
 Orchestration MUST survive interruption, partial completion, executor failure,
 `/clear` mid-pipeline, and orchestrator restart. Recovery depends on persisted
 state at `.renmark/state/pipeline.json`, never conversational reconstruction. Every skill running >1 step MUST update pipeline state before returning.
 <!-- END:workflow-recovery-rule -->
 <!-- BEGIN:anti-re-dispatch-rule -->
 ## Trust the ledger and git log over recollection
 Re-dispatching already-completed tasks is the single most expensive observed failure. Always trust `pipeline.json` / `lifecycle.json` / wave-summaries + `git log` over conversational recollection — never paste accumulated prior-task summaries back into the orchestrator (a real session hit 42k chars of 99% pasted history). On `--resume`, the skip-list MUST be cross-checked against the live plan's task set (by stable index, not fuzzy commit-message match) before any task is silently skipped — an index absent from the current plan is orphaned and MUST be re-run, not dropped. Code: `_cross_check_skip_list` in `renmark/cli/_engine.py`.
 <!-- END:anti-re-dispatch-rule -->
 <!-- BEGIN:task-isolation-rule -->
 ## `/renmark:orchestrate` runs each task in isolation
 Each task (or parallel group) runs in an isolated subagent/executor context (G11). The orchestrator MUST NOT carry implementation context between tasks unless the dependency graph requires it.
 - **Each subagent receives ONLY:** task spec; required file paths; upstream artifact pointers (paths, never contents); dependency summaries from
   `.renmark/state/wave-summaries/`; verifier expectations.
 - **Each subagent writes ONLY:** task artifact (code/diff lives here); status
   (`PASS`|`FAIL`|`SKIP`); touched files; sha/hash; summary ≤ 5 lines; dependency notes.
 - **Orchestrator aggregates ONLY:** PASS/FAIL/SKIP; artifact path; token count; dependency status; next-wave readiness. Never merged back: subagent transcript, generated code, diff, long reasoning — the orchestrator advances on summary fields alone.
 <!-- END:task-isolation-rule -->
 <!-- BEGIN:context-budget-rule -->
-## Context budget — `/compact` at 60%, `/clear` on subject change
-Orchestrator runs on Sonnet 200k; the window degrades before it fills, so act early:
-- **~60% (≈120k tokens):** surface a one-line note suggesting `/compact` before the next skill. Do NOT auto-run `/compact`.
-- **~80% (≈160k tokens):** refuse to start a new long skill (`orchestrate`,
-  `loop`, `audit`) until `/compact` or `/clear`; short skills still run.
-- **Cross-domain transition:** recommend `/clear` (memory survives). The %-side is enforced by orchestrator self-monitoring; the cross-domain side is automated via `renmark.lifecycle.skill_preamble(repo, skill)` (resolves domain from `DOMAIN_BY_SKILL`, runs `context_budget_check`, returns the hint).
+## Context budget — selectable hygiene gates at 60% and on domain change
+Renmark enforces two context hygiene gates as **blocking AskUserQuestion menus**, not silent hints. Neither gate claims to invoke `/compact` or `/clear` — those are host-level commands the user runs manually. Selecting "Stop here" means Python persists resume state and the skill stops cleanly.
+
+**Cross-domain clear gate (Python-enforced):** When `skill_preamble` returns a string starting with `CONTEXT_GATE_CLEAR:`, use `AskUserQuestion` to present the choices described in that message BEFORE executing any skill steps. Do not proceed with the skill body until the user selects an option. If the user selects "Stop here", print: "State saved. Run: /clear — then run: /renmark:resume". Skills `finish`, `approve`, and `resume` bypass this gate (advisory only — these flows must not be interrupted mid-stream).
+
+**Compact gate (rule-enforced — Python cannot detect % context):**
+- **≥120k tokens** (configurable via `compact_gate_tokens` in `.renmark/config.json`, default 120k — proxy for 60% of the 200k window): MUST present blocking menu via `AskUserQuestion` before starting any new skill:
+  - header: "Context hygiene"
+  - question: "Context window is at 60%+. Continuing may reduce accuracy."
+  - option 1 (Recommended): "Stop here — I will run /compact then /renmark:resume"
+  - option 2: "Continue this step only (once)"
+  - option 3: "Raise threshold: renmark-execute --set-compact-gate-tokens <value>"
+  - option 4: "Disable for this branch: renmark-execute --set-compact-gate-tokens 0"
+  If user selects option 1: run `renmark-execute --compact-checkpoint` to persist state, then print: "Run: /compact — then run: /renmark:resume"
+- **≥160k tokens:** Refuse to start any new long skill (`orchestrate`, `loop`, `audit`) until the user runs `/compact` or `/clear`.
+
+Cross-domain transition always triggers the clear gate regardless of token count.
 <!-- END:context-budget-rule -->
 <!-- BEGIN:context-thresholds-rule -->
 ## Context thresholds (absolute token counts)
 Complement the %-based budget above with absolute hard stops (see `renmark.state.skills.context_budget_hint`):
 - **100k tokens:** summarize in-flight reasoning; prefer artifact pointers over inline output.
-- **120k tokens:** surface `/compact` suggestion — do NOT auto-run.
+- **120k tokens** (configurable via `compact_gate_tokens` in `.renmark/config.json`): surface `/compact` suggestion — do NOT auto-run.
 - **150k tokens:** checkpoint to `.renmark/state/`; refuse new long skills until `/compact` or `/clear`.
 Cross-domain transition always recommends `/clear` regardless of count.
 <!-- END:context-thresholds-rule -->
 <!-- BEGIN:model-routing-discipline-rule -->
 ## Model-routing discipline
 Route each task to the cheapest capable executor. Do NOT default to Opus or Fable for routine work.
 - **Haiku** — docs, grep, summaries, changelog entries, small audits, simple format checks.
 - **Sonnet** — normal planning, implementation, review, dispatch, documentation.
 - **Codex** — bounded code/test generation (single file or tight scope).
 - **Opus / Fable** — escalation-only: high-risk architecture decisions, major design forks, adversarial review, judgment-heavy synthesis requiring frontier reasoning. Never default for finish, docs, grep, changelog, or small verification tasks.
 See `plugin/skills/_shared/model-routing.md` + `renmark/cost.py::requires_escalation`.
 <!-- END:model-routing-discipline-rule -->
 <!-- BEGIN:deterministic-first-routing -->
 ## Deterministic-first execution
 Before any task dispatch or model call, answer the 4-question gate: (1) Can existing state, files, git, or a parser answer this? (2) Can a deterministic script/check do it reliably? (3) Is this repeated enough to deserve a reusable check? (4) Is AI actually needed for judgment, synthesis, or ambiguous reasoning? Deterministic tasks (git/worktree state, artifact metadata, version/release checks, plan lint, mirror validation, test baseline) route to deterministic checks in `renmark/worktree.py`, `renmark/lint.py`, or shell. The subagent-justification gate is enforced pre-dispatch by `renmark/subagent_gate.py` (`python -m renmark.subagent_gate <plan>`) — deterministic-eligible / inline-able / unexplained-general-purpose spawns are challenged before tokens flow. Route judgment-heavy tasks (merge conflict risk, release-readiness reasoning, branch strategy) only to model-based agents. Cost preview MUST label tasks as deterministic or model-driven.
 See `plugin/skills/_shared/deterministic-first.md` + `renmark/worktree.py`.
 <!-- END:deterministic-first-routing -->
 <!-- BEGIN:cost-preview-rule -->
 ## Cost preview before expensive work
 Before dispatching any expensive or multi-model operation, show: tier / estimated token+cost band / whether subagents are used / whether expensive models (Opus/Fable) are required / and a cheaper alternative if one exists. Gate on user acknowledgment for escalated-tier work.
 See `plugin/skills/_shared/cost-preview.md` + `renmark/cost.py::estimate_cost`.
 <!-- END:cost-preview-rule -->
 <!-- BEGIN:finish-lanes-rule -->
 ## Finish lanes
 `/renmark:finish` supports four lanes — **quick** (re-verify + report only), **release** (verify + PR/tag), **self-update** (for renmark-on-renmark runs: update plugin install + Windows clone), and **full** (all of the above). Default: cheapest-safe lane based on lifecycle state. When the project being finished IS renmark itself, recommend **self-update** to keep both installs in sync.
 See `plugin/skills/_shared/finish-lanes.md` + `renmark/finish_lanes.py`.
 <!-- END:finish-lanes-rule -->
 <!-- BEGIN:subagent-budget-rule -->
 ## Subagent budget
 Before dispatching multiple agents: do one local grep/read first; one scoped Explore before spawning many agents. Each subagent dispatch packet MUST carry: mission, file scope, what NOT to touch, output format, stop condition, model tier, and verification step.
 See `plugin/skills/_shared/subagent-budget.md`.
 <!-- END:subagent-budget-rule -->
 <!-- BEGIN:subagent-profiles-rule -->
 ## Subagent profiles
 Prefer specialized dispatch roles (docs-editor, code-implementer, test-writer, reviewer, release-manager, researcher, audit-reader, finish-lane-specialist) over generic `general-purpose` agents. Every dispatch packet carries a `role` field; renmark logs and costs by role. Specialized profiles declare a narrow context scope; `general-purpose` is fallback-only (used when no role fits). See `plugin/skills/_shared/subagent-profiles.md`.
 <!-- END:subagent-profiles-rule -->
 <!-- BEGIN:lifecycle-rule -->
 ## Lifecycle persistence (G12)
 Every workflow stage transition MUST write `.renmark/state/lifecycle.json` before the skill returns; skills that don't are bugs. Canonical stage order:
 `init → brainstorm-complete → plan-drafted → plan-validated → created → verified
 → reviewed → documented → ready-to-release → released`.
 **Cold-start recovery:** after `/clear`, run `/renmark:resume` — it reads lifecycle.json (≤ 1KB), prints the next command, exits. Zero LLM calls.
 **Separation:** lifecycle.json carries WORKFLOW state only (identity, stage,
 artifact pointers, approval gates); RUNTIME state (wave indices, retry counts, pids) lives in `pipeline.json`. Over ~1KB is a bug (`LifecycleBloatError`).
 **Human gates:** lifecycle.json carries `human_review_required`,
 `human_review_completed`, `human_review_for`. Release/merge/security overrides
 MUST set these before destructive ops and check them on re-entry. `/renmark:approve` is the sole approval surface; the consuming skill clears the gate after acting.
 <!-- END:lifecycle-rule -->
 ## Tooling — renmark workflow
 
 Full command list → run `/renmark:help`. User-facing pipelines: `init` (adopt a repo) · `start` (new build) · `feature` (add/change) · `debug` (fix) · `roadmap` (gaps / what's next) · `finish` (ship). Each runs its internal stages (PRD → plan → build → verify → QA → review → ship) and pauses only at the gates in the Pause Policy (`plugin/skills/_shared/handoff-menu.md`).
 
 ## File conventions
 
 - Specs: `.renmark/specs/YYYY-MM-DD-<topic>.spec.md`
 - Plans: `.renmark/plans/YYYY-MM-DD-<topic>.plan.md`
 - Reviews: `.renmark/reviews/YYYY-MM-DD-<sha>.review.md`
 - Memory: `.renmark/memory/` (committed, generated by `/renmark:init`) — `INDEX.md`, `project.md`, `routing.md`, etc.
 - Runtime: `.renmark/state/` (gitignored)
 - Changelog: `CHANGELOG.md` (committed) — read before tasks, update after
 
 ## Executor preferences
 
 Per-task-signature routing history → see `.renmark/memory/routing.md` (generated by `/renmark:init`). Defaults: haiku (mechanical), codex (test scaffolding/single file), sonnet (algorithms/refactors), opus (state machines, cross-file). `fable` is frontier reasoning — ideation, strategy synthesis, adversarial review escalation — escalation-only, never default (default for those roles only when `top_tier: fable` is declared). Dispatch prompts carry the reasoning/output-discipline contract → see
 `plugin/skills/_shared/reasoning-contract.md` (push back, skip sycophancy).
 Before any custom build, run the reuse check → see
 `plugin/skills/_shared/reuse-check.md`.
 
 ## Code conventions & Testing
 
 Dev gates and standards → see `.renmark/memory/dev-standards.md` (generated by `/renmark:init`). Verifiers in renmark plans must match the project's test command.
 
 ### Behavioral test tier (P8)
 - `renmark-execute --behavior` runs the **deterministic tier** — a CI-safe scaffolding/regression guard that asserts renmark's real behavior-shaping functions (`lifecycle.next_steps`, `skill_preamble`, `plan_lint`) produce the contract-required output. It makes no model call: no network, no token spend. Green `--behavior` is not "the skill works": it only proves the scaffolding is intact.
 - The eval tier is live when a `str->str` runner command is wired via `RENMARK_EVAL_RUNNER_CMD` (or the `.renmark` config key `eval_runner_cmd`): `renmark-execute --behavior --accept` records golden transcripts through that subprocess runner (a deliberate live step) and `renmark-execute --behavior --judge` runs the live LLM-as-judge through it — the load-bearing behavioral proof over a real model trajectory (~$0.15, opt-in only, never auto-spends, out of CI). On a deterministic FAIL the CLI prints an OFFER line and escalates solely when `--judge` is passed.
 - When `RENMARK_EVAL_RUNNER_CMD`/`eval_runner_cmd` is UNSET the eval tier stays unavailable — CI-safe, deterministic-tier default, never auto-spends.
 - Two honest tiers: the deterministic tier guards the scaffolding; the eval tier is where the real proof that a skill CHANGES agent behavior lives. Complements the structure audit, which only lints.
 
 *Mirror all rule changes in `AGENTS.md` in the same commit.*
 
diff --git a/plugin/skills/_shared/context-taxonomy.md b/plugin/skills/_shared/context-taxonomy.md
index 691af72..2d2e54d 100644
--- a/plugin/skills/_shared/context-taxonomy.md
+++ b/plugin/skills/_shared/context-taxonomy.md
@@ -4,80 +4,92 @@
 `orchestrate`, `feature`, `start`, `finish`, `verify`, `codereview`, and any
 skill that resolves a `_shared/*.md` fragment on demand. This is the one place
 renmark's four kinds of context — and the load policy that governs each — are
 defined, so skills cite this pointer instead of re-explaining what may be
 pre-loaded versus fetched on demand. Operationalizes REQ-5 (context hygiene)
 and REQ-20 (dynamic skill loading).
 
 ---
 
 ## The four kinds of context
 
 renmark treats context as four disjoint kinds. Anything the orchestrator or a
 subagent holds is exactly one of these — nothing is "just floating."
 
 | Kind | Source | Persistence | Load policy |
 |---|---|---|---|
 | **static** | `CLAUDE.md` / `AGENTS.md` rule blocks | always present (whole session) | injected by the harness at session start; never fetched, never dropped |
 | **dynamic** | skill bodies (`SKILL.md`) + `_shared/*.md` fragments | on demand; released after use | **metadata upfront only** — full body loaded via `load_skill_body` / `load_fragment` when a skill actually runs |
 | **memory** | `.renmark/memory/*` (`INDEX.md`, `project.md`, `routing.md`, logs) | durable; survives `/clear` and `/compact` | read by pointer/summary when a workflow needs prior decisions; never bulk-loaded |
 | **task-local** | the per-subagent dispatch packet | ephemeral; dies with the subagent | assembled per dispatch; carries task spec + required-skill metadata, never a full skill body |
 
 ---
 
 ## Metadata upfront, bodies on demand
 
 The load-bearing rule for **dynamic** context: a skill's or fragment's
 **metadata** (name, description, and cite pointers) is exposed upfront through
 the `skillmeta` registry — surfaced programmatically via
 `renmark.context.skill_metadata`. The full **instructions/body** load ONLY on
 demand, via `renmark.context.load_skill_body` (for a `SKILL.md`) or
 `load_fragment` (for a `_shared/*.md`).
 
 Dynamic bodies are **never pre-loaded into the orchestrator**. The orchestrator
 knows a skill *exists* and what it's *for* (metadata) without paying for its
 prose until the moment that skill runs — and it releases the body afterward.
 
 ---
 
 ## The dispatch-packet contract
 
 Every scoped subagent receives ONLY:
 
 - **task-local context** — its task spec, required file paths, upstream artifact
   pointers (paths, never contents), dependency summaries.
 - **required-skill metadata** — the name + cite pointer of any skill it needs,
   never that skill's full body. The subagent loads the body itself on demand if
   it runs the skill.
 
 This is enforced in production, not by convention:
 `renmark.dispatch.build_subagent_input` builds the packet, carries
 `required_skills` as **metadata** through `renmark.context`, and is guarded by
 `assert_metadata_only` — which raises if a full skill/fragment body is ever
 smuggled into a dispatch packet. A body reaching a subagent through dispatch is
 a bug, not a shortcut.
 
 ---
 
 ## Why a shared file
 
 Earlier drafts had each skill inline its own description of "what's loaded when."
 The wording drifted, and one skill implied bodies were always resident.
 Centralizing here means:
 
 - One edit point. The four kinds and the load policy are defined once; every
   skill and the `renmark.context` code path cite the same taxonomy.
 - Linter-friendly. `plugin/skills/_shared/` is skipped by `renmark.lint` (it's a
   reference dir, not a skill), so this file never trips the command-pair check.
 - Symmetric with `_shared/reasoning-contract.md` and `_shared/handoff-menu.md` —
   same pattern, same precedent.
 
 When citing this taxonomy in a SKILL.md or rule block, write:
 
 > *Honor the context taxonomy in
 > `${CLAUDE_PLUGIN_ROOT}/skills/_shared/context-taxonomy.md`: static rules are
 > always present; dynamic skill/fragment bodies load on demand (metadata
 > upfront only); memory is durable and read by pointer; the per-subagent
 > dispatch packet carries task-local context + required-skill metadata, never a
 > full body.*
 
 Do not paste the taxonomy table into the calling SKILL.md — cite this file.
+
+---
+
+## Context hygiene gates
+
+Renmark enforces two hygiene gates as blocking AskUserQuestion menus:
+
+**Clear gate (Python-enforced):** `skill_preamble` → `context_budget_check` returns `"clear"` on cross-domain transition → `persist_compact_checkpoint(repo, skill, reason="clear")` called → returns `CONTEXT_GATE_CLEAR:`-prefixed string → CLAUDE.md rule triggers `AskUserQuestion` menu. Bypass skills (advisory only): `finish`, `approve`, `resume`.
+
+**Compact gate (rule-enforced):** Threshold in `config.json["compact_gate_tokens"]` (default 120k, 0 = disabled). CLI helper: `renmark-execute --compact-checkpoint`. Enforced by CLAUDE.md rule at ≥120k tokens; Python provides persist + CLI helper only. Python cannot detect real % context utilization — absolute token count is the proxy.
+
+**`persist_compact_checkpoint(repo, skill, reason)`:** writes `.renmark/state/compact_checkpoint.json` with `{skill, reason, resume_cmd: "/renmark:resume", timestamp}`. Consumed by `/renmark:resume` for state recovery. Never raises.
diff --git a/renmark/cli/_engine.py b/renmark/cli/_engine.py
index 68461bc..8dbfdb0 100644
--- a/renmark/cli/_engine.py
+++ b/renmark/cli/_engine.py
@@ -1186,207 +1186,240 @@ def main(argv: list[str] | None = None) -> int:
     ap.add_argument("--propose", action="store_true", help="(with --scan) include proposed cron entries")
     ap.add_argument("--emit-cron", action="store_true", help="(with --scan) emit cron block to stdout (read-only)")
     ap.add_argument(
         "--behavior",
         action="store_true",
         help=(
             "replay behavioral cases in tests/behavioral/ and print a bounded PASS/FAIL "
             "summary; deterministic (no spend). Exit non-zero on any FAIL/ERROR."
         ),
     )
     ap.add_argument(
         "--accept",
         action="store_true",
         help="(with --behavior) record eval-tier golden transcripts via the live capture path (deliberate spend)",
     )
     ap.add_argument(
         "--judge",
         action="store_true",
         help=(
             "(with --behavior) escalate deterministic FAILs to the LLM judge "
             "(opt-in spend); without it a FAIL prints a judge OFFER instead"
         ),
     )
     ap.add_argument(
         "--no-commit",
         action="store_true",
         help="apply tasks and run verifier but do not git-commit (skill batches commits per wave)",
     )
     ap.add_argument("--repo", default=".", help="repo root (default: current dir)")
     # v0.3.0: ad-hoc Codex task mode (G5/G11)
     ap.add_argument(
         "--task",
         metavar="SPEC_PATH",
         help=(
             "ad-hoc mode: read a task-spec markdown file, dispatch to Codex, "
             "write artifact to --output. Emits SubagentOutput JSON to stdout."
         ),
     )
     ap.add_argument("--output", metavar="ARTIFACT_PATH", help="(with --task) where Codex writes its artifact")
     # P11 — persisted proactivity toggle
     ap.add_argument(
         "--set-proactive",
         metavar="true|false",
         help=(
             "persist the auto-routing proactivity flag to .renmark/config.json "
             "('true' = route plain-English build/dev tasks through renmark automatically; "
             "'false' = skip auto-routing until re-enabled). Default: true."
         ),
     )
     # P10 — persisted headless toggle
     ap.add_argument(
         "--set-headless",
         metavar="true|false",
         help=(
             "persist the headless-mode flag to .renmark/config.json "
             "('true' = run non-interactively, suppressing interactive gates/menus; "
             "'false' = restore interactive behavior). Default: false."
         ),
     )
     # P8 harness — persisted operating-mode (conductor|orchestrator)
     ap.add_argument(
         "--set-mode",
         choices=("conductor", "orchestrator"),
         metavar="conductor|orchestrator",
         help=(
             "persist the operating mode to .renmark/state/mode.json "
             "('conductor' = high-touch interactive drive; "
             "'orchestrator' = hands-off autonomous plan execution)."
         ),
     )
     ap.add_argument(
         "--get-mode",
         action="store_true",
         help="print the persisted operating mode (or 'unset' if none), then exit",
     )
     ap.add_argument(
         "--clear-mode",
         action="store_true",
         help="clear the persisted operating mode from .renmark/state/mode.json, then exit",
     )
+    # Context hygiene gates
+    ap.add_argument(
+        "--compact-checkpoint",
+        action="store_true",
+        help=(
+            "persist resume state to .renmark/state/compact_checkpoint.json "
+            "and print the commands to run before /compact. "
+            "Use when context is at 60%%+ to preserve state before compacting."
+        ),
+    )
+    ap.add_argument(
+        "--set-compact-gate-tokens",
+        metavar="TOKENS",
+        type=int,
+        help=(
+            "persist the compact-gate token threshold to .renmark/config.json. "
+            "Default: 120000 (proxy for 60%% of the 200k window). "
+            "0 = disable the compact gate. Negative values are rejected."
+        ),
+    )
     # P4 file-handoff helpers — print ONLY the written path; diff/spec bytes stay out of orchestrator context
     ap.add_argument(
         "--task-brief",
         nargs=2,
         metavar=("PLAN_PATH", "TASK_INDEX"),
         help=(
             "extract task N's spec/brief from PLAN_PATH, write to "
             ".renmark/state/handoffs/<stem>-task-N.brief.md, print ONLY the path"
         ),
     )
     ap.add_argument(
         "--review-package",
         nargs=2,
         metavar=("BASE_REF", "HEAD_REF"),
         help=(
             "write git diff --stat + per-file diffs for BASE..HEAD to "
             ".renmark/state/handoffs/review-<base>-<head>.pkg.md, print ONLY the path"
         ),
     )
     args = ap.parse_args(argv)
 
     if (args.propose or args.emit_cron) and not args.scan:
         print("--propose/--emit-cron require --scan", file=sys.stderr)
         return 2
     if (args.accept or args.judge) and not args.behavior:
         print("--accept/--judge require --behavior", file=sys.stderr)
         return 2
 
     repo = Path(args.repo).resolve()
 
     if args.usage:
         return cmd_usage(repo)
     if args.analytics:
         return cmd_analytics(repo)
     if args.roadmap:
         return cmd_roadmap(repo)
     if args.logs:
         return cmd_logs(repo, n=args.logs_n)
     if args.scan:
         return cmd_scan(repo, propose=args.propose, emit_cron=args.emit_cron)
     if args.behavior:
         return _cmd_behavior(repo, accept=args.accept, judge=args.judge)
     if args.task:
         if not args.output:
             ap.error("--task requires --output ARTIFACT_PATH")
         return cmd_task(args.task, args.output, repo=repo)
     if args.set_proactive is not None:
         raw = args.set_proactive.strip().lower()
         if raw not in ("true", "false"):
             ap.error("--set-proactive expects 'true' or 'false'")
         from .. import config as _config
         value = raw == "true"
         _config.set_proactive(repo, value)
         state_str = "on" if value else "off"
         print(f"renmark: proactive auto-routing {state_str} ({repo}/.renmark/config.json)")
         return 0
     if args.set_headless is not None:
         raw = args.set_headless.strip().lower()
         if raw not in ("true", "false"):
             ap.error("--set-headless expects 'true' or 'false'")
         from .. import config as _config
         value = raw == "true"
         _config.set_headless(repo, value)
         state_str = "on" if value else "off"
         print(f"renmark: headless mode {state_str} ({repo}/.renmark/config.json)")
         return 0
     if args.set_mode is not None:
         from .. import mode as _mode
         mode_path = _mode.mode_state_path(repo)
         try:
             _mode.set_mode(repo, args.set_mode)
         except ValueError as exc:
             ap.error(str(exc))
         except OSError as exc:
             print(
                 f"renmark: failed to persist operating mode to {mode_path}: {exc}",
                 file=sys.stderr,
             )
             return 1
         print(f"renmark: operating mode set to {args.set_mode} ({mode_path})")
         return 0
     if args.get_mode:
         from .. import mode as _mode
         current = _mode.read_mode(repo)
         print(current if current is not None else "unset")
         return 0
     if args.clear_mode:
         from .. import mode as _mode
         mode_path = _mode.mode_state_path(repo)
         try:
             _mode.clear_mode(repo)
         except OSError as exc:
             print(
                 f"renmark: failed to clear operating mode at {mode_path}: {exc}",
                 file=sys.stderr,
             )
             return 1
         print(f"renmark: operating mode cleared ({mode_path})")
         return 0
+    if args.compact_checkpoint:
+        from renmark import lifecycle as _lifecycle
+        _lifecycle.persist_compact_checkpoint(repo, skill="manual", reason="compact")
+        print("Compact checkpoint written to .renmark/state/compact_checkpoint.json.")
+        print("Run these commands to safely reduce context:")
+        print("  1. /compact")
+        print("  2. /renmark:resume")
+        return 0
+    if args.set_compact_gate_tokens is not None:
+        from renmark import config as _config
+        _config.set_compact_gate_tokens(repo, args.set_compact_gate_tokens)
+        print(f"compact_gate_tokens set to {args.set_compact_gate_tokens}")
+        return 0
     if args.task_brief:
         plan_path, task_index_str = args.task_brief
         try:
             task_index = int(task_index_str)
         except ValueError:
             ap.error(f"--task-brief TASK_INDEX must be an integer, got {task_index_str!r}")
         return cmd_task_brief(plan_path, task_index, repo=repo)
     if args.review_package:
         base_ref, head_ref = args.review_package
         return cmd_review_package(base_ref, head_ref, repo=repo)
 
     if not args.plan:
         ap.error(
             "plan path is required unless --usage / --analytics / --roadmap / --logs / "
             "--scan / --behavior / --task / --task-brief / --review-package / "
             "--set-proactive / --set-headless / --set-mode / --get-mode / --clear-mode"
         )
     return execute_plan(
         args.plan,
         repo=repo,
         resume=args.resume,
         dry_run=args.dry_run,
         no_commit=args.no_commit,
     )
 
 
 if __name__ == "__main__":
     sys.exit(main())
diff --git a/renmark/config.py b/renmark/config.py
index 6837766..0d5b53e 100644
--- a/renmark/config.py
+++ b/renmark/config.py
@@ -136,94 +136,125 @@ def set_headless(repo: str | Path, value: bool) -> None:
 
 def headless_source(repo: str | Path) -> str:
     """Return which layer decided :func:`is_headless` for *repo*.
 
     ``"env"`` if ``RENMARK_HEADLESS`` resolved the value, ``"config"`` if the
     config file's ``"headless"`` key did (i.e. it is a real ``bool``), else
     ``"default"``.  Uses the same precedence as :func:`is_headless` so the two
     never disagree — a non-bool config value reports ``"default"``.  Never
     raises.
     """
     if _env_headless() is not None:
         return "env"
     if isinstance(_read_raw(repo).get("headless"), bool):
         return "config"
     return "default"
 
 
 def _env_eval_runner_cmd() -> str | None:
     """Parse ``RENMARK_EVAL_RUNNER_CMD`` into an optional command string.
 
     Returns the stripped value when the variable is set and non-blank, or
     ``None`` when it is unset or holds only whitespace (so the caller falls
     through to the config file).  Never raises.
     """
     raw = os.environ.get(_ENV_EVAL_RUNNER_CMD)
     if raw is None:
         return None
     norm = raw.strip()
     if norm:
         return norm
     return None
 
 
 def _config_eval_runner_cmd(repo: str | Path) -> str | None:
     """Return the ``.renmark/config.json`` ``eval_runner_cmd`` value, normalized.
 
     Mirrors :func:`_env_eval_runner_cmd`: returns the stripped value only when
     the key is present, a real ``str``, and non-blank; a non-str or
     whitespace-only value is treated as unset (``None``) so the caller degrades
     to ``LiveRunnerUnavailable`` rather than a spurious empty-command error.
     Never raises.
     """
     val = _read_raw(repo).get("eval_runner_cmd")
     if isinstance(val, str):
         norm = val.strip()
         if norm:
             return norm
     return None
 
 
 def eval_runner_cmd(repo: str | Path) -> str | None:
     """Return the command used to launch the eval-tier runner for *repo*.
 
     Precedence:
     1. env ``RENMARK_EVAL_RUNNER_CMD`` if set and non-blank (whitespace is
        stripped; a blank value is treated as unset and falls through);
     2. else the ``.renmark/config.json`` key ``"eval_runner_cmd"`` if present,
        a real ``str``, **and non-blank** (whitespace is stripped; a non-str or
        blank value is ignored, not coerced);
     3. else ``None``.
 
     Never raises — any OS or parse error degrades to ``None``.
     """
     env = _env_eval_runner_cmd()
     if env is not None:
         return env
     return _config_eval_runner_cmd(repo)
 
 
 def set_eval_runner_cmd(repo: str | Path, value: str) -> None:
     """Persist the eval-runner command for the project at *repo*.
 
     Reads the existing config, updates only the ``eval_runner_cmd`` key, and
     writes it back so unrelated keys are preserved.  Never raises.
     """
     data = _read_raw(repo)
     data["eval_runner_cmd"] = value
     _write_raw(repo, data)
 
 
+def compact_gate_tokens(repo: str | Path) -> int:
+    """Return the configured compact-gate token threshold for *repo*.
+
+    The compact gate fires when the orchestrator's context reaches this many
+    tokens — a proxy for 60% of the 200k window since real % is not detectable
+    from inside a skill. Default: 120_000. A value of 0 means disabled (gate
+    never fires). Never raises.
+    """
+    data = _read_raw(repo)
+    val = data.get("compact_gate_tokens")
+    if not isinstance(val, int) or isinstance(val, bool):
+        return 120_000
+    if val < 0:
+        return 120_000
+    return val
+
+
+def set_compact_gate_tokens(repo: str | Path, value: int) -> None:
+    """Persist the compact-gate token threshold for the project at *repo*.
+
+    Reads the existing config, updates only the ``compact_gate_tokens`` key,
+    and writes it back so unrelated keys are preserved. A value of 0 disables
+    the gate. Negative values are rejected (no-op). Never raises.
+    """
+    if value < 0:
+        return
+    data = _read_raw(repo)
+    data["compact_gate_tokens"] = value
+    _write_raw(repo, data)
+
+
 def eval_runner_source(repo: str | Path) -> str:
     """Return which layer decided :func:`eval_runner_cmd` for *repo*.
 
     ``"env"`` if ``RENMARK_EVAL_RUNNER_CMD`` resolved the value, ``"config"``
     if the config file's ``"eval_runner_cmd"`` key did (i.e. it is a real
     ``str``), else ``"default"``.  Uses the same precedence as
     :func:`eval_runner_cmd` so the two never disagree — a non-str config value
     reports ``"default"``.  Never raises.
     """
     if _env_eval_runner_cmd() is not None:
         return "env"
     if _config_eval_runner_cmd(repo) is not None:
         return "config"
     return "default"
diff --git a/renmark/lifecycle.py b/renmark/lifecycle.py
index 03afc6f..4d845f1 100644
--- a/renmark/lifecycle.py
+++ b/renmark/lifecycle.py
@@ -522,242 +522,297 @@ def next_steps(repo: Path | str, skill: object) -> NextSteps:
         )
 
     # aux / terminal
     local = AUX_LOCAL_ACTIONS.get(skill, [])[:2] if isinstance(skill, str) else []
     suggestions = [tier0, *local]
     return NextSteps(tier0=tier0, suggestions=suggestions, skill_class="aux")
 
 
 def _gates_not_run(repo: Path | str) -> list[str]:
     """Best-effort: which quality gates have NOT run for the current HEAD sha.
 
     Mirrors handoff-menu rule 2 — scan ``.renmark/reviews/*.qa.md`` and
     ``*.review.md`` for an artifact with ``source_sha == HEAD`` and
     ``completion_state == 'complete'``; any gate without one is "not run".
     Degrades gracefully (returns ``[]``) if git or the summary helpers are
     unavailable.
     """
     try:
         from .summary import git_head_sha
 
         head = git_head_sha(repo)
         if not head:
             return []
         reviews = Path(repo) / ".renmark" / "reviews"
         if not reviews.is_dir():
             return ["qa", "codereview"]
 
         # (glob, required generator) per gate. The generator constraint mirrors
         # handoff-menu rule 2: a gate counts as "run" only when the artifact was
         # produced by the gate's own generator — a stray/foreign .qa.md must not
         # unlock Deep QA. generator=None means "no generator constraint".
         gate_specs: dict[str, tuple[str, str | None]] = {
             "qa": ("*.qa.md", "verify-qa"),
             "codereview": ("*.review.md", None),
         }
         not_run: list[str] = []
         for gate, (pattern, want_gen) in gate_specs.items():
             ran = False
             for artifact in reviews.glob(pattern):
                 meta = read_metadata(artifact)
                 if (
                     meta.get("source_sha") == head
                     and meta.get("completion_state") == "complete"
                     and (want_gen is None or meta.get("generator") == want_gen)
                 ):
                     ran = True
                     break
             if not ran:
                 not_run.append(gate)
         return not_run
     except Exception:
         return []
 
 
 def domain_of(skill: str) -> str:
     """Return the domain bucket for a skill name (G4 contamination detection).
 
     Resolves via the `skillmeta` registry (single source of truth, P7);
     unknown skills default to ``"build"``. `DOMAIN_BY_SKILL` remains a derived
     back-compat alias, but is consulted first so a test `monkeypatch` of that
     attribute still takes effect.
     """
     if skill in DOMAIN_BY_SKILL:
         return DOMAIN_BY_SKILL[skill]
     meta = skillmeta.get(skill)
     return meta.domain if meta is not None else "build"
 
 
 # Back-compat alias: `domain_for_skill` is the registry-facing name.
 domain_for_skill = domain_of
 
 
 def preamble_tier(skill: str) -> str:
     """Return the preamble tier for a skill ('minimal' | 'standard' | 'full').
 
     Unknown skills default to 'full'. Never raises.
     """
     return PREAMBLE_TIER_BY_SKILL.get(skill, "full")
 
 
+def persist_compact_checkpoint(
+    repo: "Path | str", skill: str, reason: str
+) -> None:
+    """Write a compact checkpoint to .renmark/state/compact_checkpoint.json.
+
+    Called by skill_preamble before emitting a context gate message so the
+    user can resume after running /compact or /clear.  Never raises.
+    """
+    from . import state as _state  # lazy — avoid circular import at module load
+    import json as _json
+
+    try:
+        state_path = _state.state_dir(repo) / "compact_checkpoint.json"
+        state_path.parent.mkdir(parents=True, exist_ok=True)
+        state_path.write_text(
+            _json.dumps(
+                {
+                    "skill": skill,
+                    "reason": reason,
+                    "resume_cmd": "/renmark:resume",
+                    "timestamp": _state.now_iso(),
+                },
+                indent=2,
+            ),
+            encoding="utf-8",
+        )
+    except Exception:
+        pass
+
+
 def skill_preamble(repo: Path | str, skill: str) -> str | None:
     """Single-call Step-0 boilerplate for every SKILL.md.
 
     Performs the calls every skill used to inline by hand, gated by tier:
         - ALL tiers: record invocation (load-bearing cross-domain detection for
           the NEXT skill — runs unconditionally so minimal-tier skills still
           register their domain in last-skill state).
         - minimal: returns None immediately; no budget check, no fragments.
         - standard: cross-domain/compact hint only; no fable synthesis hint.
         - full (default): cross-domain/compact hint + fable synthesis hint.
 
     Cross-domain detection for the CURRENT skill requires context_budget_check
     to read last-skill state BEFORE record_skill_invocation overwrites it, so
     for standard/full tier the budget check runs first. record_skill_invocation
     is then called unconditionally to keep the state file current for the next
     skill — this is the load-bearing ordering invariant.
 
     Returns the hint string the skill should surface to the user, or None when
     no hint is needed. Domain is resolved from `DOMAIN_BY_SKILL` — callers do
     not need to pass it, so the per-skill prose can't drift.
     """
     # Imported lazily to avoid a state ↔ lifecycle circular import at module load.
     from . import state as _state
 
     domain = domain_of(skill)
     tier = preamble_tier(skill)
 
     if tier == "minimal":
         # INVARIANT: record_skill_invocation runs for ALL tiers so that the next
         # skill can detect cross-domain transitions even when this one is minimal.
         _state.record_skill_invocation(repo, skill, domain)
         return _with_agency_note(repo, skill, _with_headless_note(repo, None))
 
     # For standard/full: budget check MUST read last-skill state before
     # record_skill_invocation overwrites it — ordering is load-bearing.
+    # Read last-skill state once here so prev_domain is available for the gate message.
+    last = _state.last_skill_invocation(repo)
     verdict = _state.context_budget_check(repo, skill, domain)
     _state.record_skill_invocation(repo, skill, domain)
 
     fragments: list[str] = []
     if verdict == "clear":
-        fragments.append(
-            f"context: cross-domain transition into `{domain}` — consider `/clear` "
-            "before continuing (`.renmark/memory/` survives clears)"
-        )
+        if skill in _CONTEXT_BYPASS_SKILLS:
+            # Advisory only — finish/approve/resume must not be blocked mid-flow.
+            fragments.append(
+                f"context: cross-domain transition into `{domain}` — consider `/clear` "
+                "before continuing (`.renmark/memory/` survives clears)"
+            )
+        else:
+            prev_domain = last.get("domain", "?") if last else "?"
+            persist_compact_checkpoint(repo, skill, reason="clear")
+            return (
+                f"CONTEXT_GATE_CLEAR: cross-domain transition detected "
+                f"(prev: `{prev_domain}` \u2192 `{domain}`).\n"
+                "State persisted to .renmark/state/compact_checkpoint.json.\n"
+                "Present the user with AskUserQuestion before proceeding with this skill:\n"
+                '  header: "Context hygiene"\n'
+                '  question: "Domain change detected. Run /clear to start fresh '
+                '(memory survives), or continue with accumulated context?"\n'
+                "  options:\n"
+                "    1. Stop here \u2014 I will run /clear then /renmark:resume (Recommended)\n"
+                "    2. Continue in same context (this step only)\n"
+                "    3. Queue this as next task after current work finishes\n"
+                "    4. Cancel"
+            )
     elif verdict == "compact":
         fragments.append("context: approaching budget — consider `/compact` before continuing")
 
     if tier == "full" and skill in SYNTHESIS_SKILLS:
         # Imported lazily to keep capability resolution off the module-load path.
         from . import capabilities as _capabilities
 
         if _capabilities.top_tier(Path(repo)) == "fable":
             fragments.append(
                 "declared top tier: fable — for best ideation/strategy results "
                 "run this session on Fable 5 (/model fable)"
             )
 
     base = " | ".join(fragments) if fragments else None
     return _with_agency_note(repo, skill, _with_mode_note(repo, skill, _with_headless_note(repo, base)))
 
 
 # ── Operating-mode directive (Conductor vs Orchestrator) ──────────────────────
 #
 # Set-mode directive lines (persisted mode → one hint line). Kept as module
 # constants so the behavior test (T14) and unit test (T12) can assert the exact
 # text without duplicating string literals.
 _MODE_DIRECTIVE: dict[str, str] = {
     "conductor": (
         "Operating mode: Conductor — hands-on; prefer single-file scoped edits, "
         "avoid subagents unless necessary, explain the next move before editing."
     ),
     "orchestrator": (
         "Operating mode: Orchestrator — goal-level; use narrow scoped subagents "
         "where useful, load skills on demand, review outcomes not keystrokes."
     ),
 }
 
 # Entry-point skills that must PROMPT for a mode when none is set yet.
 _MODE_ENTRY_SKILLS: frozenset[str] = frozenset(
     {"start", "feature", "debug", "roadmap", "finish", "orchestrate"}
 )
 
+# Skills whose flows must not be blocked mid-stream by context gates.
+# finish/approve/resume: urgent paths that must not be interrupted halfway.
+_CONTEXT_BYPASS_SKILLS: frozenset[str] = frozenset({"finish", "approve", "resume"})
+
 
 def _choose_mode_hint(skill: str) -> str:
     """Choose-mode instruction emitted for an entry-point skill with no mode set.
 
     Tells the orchestrator to ask the user Conductor vs Orchestrator via
     AskUserQuestion, recommending the per-skill default, then persist the choice.
     """
     from . import mode as _mode
 
     recommended = _mode.default_mode_for_skill(skill)
     return (
         "Operating mode: not yet set — ask the user Conductor vs Orchestrator via "
         f"AskUserQuestion (recommend: {recommended}), then persist with "
         "renmark.mode.set_mode(repo, <choice>)."
     )
 
 
 def _with_mode_note(repo: Path | str, skill: str, hint: str | None) -> str | None:
     """ADDITIVE: append the operating-mode directive to ``hint``.
 
     Runs strictly AFTER the existing tier logic and the headless note — never
     reorders the record-before-check invariant. DEGRADES GRACEFULLY: any
     exception in mode resolution falls back to ``hint`` unchanged, so mode is a
     pure enhancement and never a hard dependency of the preamble.
 
     - Mode SET  → append the Conductor/Orchestrator directive line.
     - Mode UNSET + entry-point skill → append a choose-mode instruction.
     - Mode UNSET + non-entry skill → no mode line (returns ``hint`` unchanged).
     """
     try:
         from . import mode as _mode
 
         current = _mode.read_mode(repo)
         if current is not None:
             line = _MODE_DIRECTIVE.get(current)
             if line is None:  # unrecognised (read_mode shouldn't yield this)
                 return hint
         elif skill in _MODE_ENTRY_SKILLS:
             line = _choose_mode_hint(skill)
         else:
             return hint
     except Exception:
         return hint
     return line if hint is None else f"{hint} | {line}"
 
 
 # Pipeline skills that receive an Agency Mode hint when agency is active.
 # v0.30.0 shipped the spine (start/prd/roadmap/finish/resume); the fast-follow
 # extends coverage to the remaining pipelines (feature/plan/orchestrate/verify/
 # codereview) so the whole delivery loop is agency-aware.
 _AGENCY_AWARE_SKILLS: frozenset[str] = frozenset(
     {
         "start", "prd", "roadmap", "finish", "resume",
         "feature", "plan", "orchestrate", "verify", "codereview",
     }
 )
 
 # Back-compat alias: the original spine-only name still resolves.
 _AGENCY_SPINE_SKILLS: frozenset[str] = _AGENCY_AWARE_SKILLS
 
 # Marker string used to identify the agency hint line — load-bearing for
 # behavior tests (T15): assert active preamble contains this prefix and
 # inactive preamble does NOT.
 _AGENCY_HINT_MARKER: str = "Agency Mode active"
 
 
 def _with_agency_note(repo: Path | str, skill: str, hint: str | None) -> str | None:
     """ADDITIVE: append an Agency Mode hint to ``hint`` when agency is active.
 
     Only surfaces the hint for AGENCY-AWARE pipeline skills (the spine —
     start/prd/roadmap/finish/resume — plus feature/plan/orchestrate/verify/
     codereview) — all other skills are passed through unchanged regardless of
     agency state.
 
     When agency is INACTIVE the return value is byte-identical to ``hint``
     (the inactive-path guarantee). DEGRADES GRACEFULLY: any exception in
     agency/context resolution falls back to ``hint`` unchanged, so agency is
     a pure enhancement and never a hard dependency of the preamble.
 
     Follows the same additive pattern as :func:`_with_mode_note` and
diff --git a/tests/test_config.py b/tests/test_config.py
index 97832fb..420fbd2 100644
--- a/tests/test_config.py
+++ b/tests/test_config.py
@@ -142,80 +142,121 @@ def test_env_true_overrides_config_false(
     tmp_path: Path, monkeypatch: pytest.MonkeyPatch
 ) -> None:
     """Explicit env ON must win even when config.json has headless: false."""
     cfg.set_headless(tmp_path, False)
     monkeypatch.setenv("RENMARK_HEADLESS", "1")
     assert cfg.is_headless(tmp_path) is True
     assert cfg.headless_source(tmp_path) == "env"
 
 
 @pytest.mark.parametrize("raw", ["maybe", "", "2", "enabled", "tru"])
 def test_env_unrecognized_falls_through_to_config(
     tmp_path: Path, monkeypatch: pytest.MonkeyPatch, raw: str
 ) -> None:
     """An unparseable env value is ignored — fall through to config/default."""
     monkeypatch.setenv("RENMARK_HEADLESS", raw)
     cfg.set_headless(tmp_path, True)
     assert cfg.is_headless(tmp_path) is True
     assert cfg.headless_source(tmp_path) == "config"
 
 
 def test_env_unrecognized_falls_through_to_default(
     tmp_path: Path, monkeypatch: pytest.MonkeyPatch
 ) -> None:
     """Unparseable env value with no config → default False."""
     monkeypatch.setenv("RENMARK_HEADLESS", "maybe")
     assert cfg.is_headless(tmp_path) is False
     assert cfg.headless_source(tmp_path) == "default"
 
 
 # ── P10: headless mode — config layer (env unset) ────────────────────────────
 
 
 def test_config_flag_honored_when_env_unset(
     tmp_path: Path, monkeypatch: pytest.MonkeyPatch
 ) -> None:
     """With env unset, the config flag decides; source == 'config'."""
     monkeypatch.delenv("RENMARK_HEADLESS", raising=False)
     cfg.set_headless(tmp_path, True)
     assert cfg.is_headless(tmp_path) is True
     assert cfg.headless_source(tmp_path) == "config"
 
 
 def test_config_false_honored_when_env_unset(
     tmp_path: Path, monkeypatch: pytest.MonkeyPatch
 ) -> None:
     """An explicit config False (env unset) → False, source == 'config'."""
     monkeypatch.delenv("RENMARK_HEADLESS", raising=False)
     cfg.set_headless(tmp_path, False)
     assert cfg.is_headless(tmp_path) is False
     assert cfg.headless_source(tmp_path) == "config"
 
 
 # ── P10: headless mode — default layer ───────────────────────────────────────
 
 
 def test_default_false_when_neither_env_nor_config(
     tmp_path: Path, monkeypatch: pytest.MonkeyPatch
 ) -> None:
     """No env, no config → is_headless False; source == 'default'."""
     monkeypatch.delenv("RENMARK_HEADLESS", raising=False)
     assert cfg.is_headless(tmp_path) is False
     assert cfg.headless_source(tmp_path) == "default"
 
 
 # ── P10: set_headless round-trip & key preservation ──────────────────────────
 
 
 def test_set_headless_preserves_other_keys(
     tmp_path: Path, monkeypatch: pytest.MonkeyPatch
 ) -> None:
     """set_headless must not clobber an existing 'proactive' key."""
     monkeypatch.delenv("RENMARK_HEADLESS", raising=False)
     cfg.set_proactive(tmp_path, False)
     cfg.set_headless(tmp_path, True)
     p = tmp_path / ".renmark" / "config.json"
     data = json.loads(p.read_text(encoding="utf-8"))
     assert data["proactive"] is False
     assert data["headless"] is True
     assert cfg.is_proactive(tmp_path) is False
     assert cfg.is_headless(tmp_path) is True
+
+
+# ── compact_gate_tokens ───────────────────────────────────────────────────────
+
+
+def test_compact_gate_tokens_default(tmp_path: Path) -> None:
+    from renmark.config import compact_gate_tokens
+    assert compact_gate_tokens(tmp_path) == 120_000
+
+
+def test_compact_gate_tokens_set_and_get(tmp_path: Path) -> None:
+    from renmark.config import compact_gate_tokens, set_compact_gate_tokens
+    set_compact_gate_tokens(tmp_path, 80_000)
+    assert compact_gate_tokens(tmp_path) == 80_000
+
+
+def test_compact_gate_tokens_zero_is_valid(tmp_path: Path) -> None:
+    """0 means disabled — it must be stored and returned as 0, not fall back to default."""
+    from renmark.config import compact_gate_tokens, set_compact_gate_tokens
+    set_compact_gate_tokens(tmp_path, 0)
+    assert compact_gate_tokens(tmp_path) == 0
+
+
+def test_compact_gate_tokens_negative_value_returns_default(tmp_path: Path) -> None:
+    """Negative value stored in config.json returns default 120_000."""
+    import json
+    cfg_file = tmp_path / ".renmark" / "config.json"
+    cfg_file.parent.mkdir(parents=True, exist_ok=True)
+    cfg_file.write_text(json.dumps({"compact_gate_tokens": -5}))
+    from renmark.config import compact_gate_tokens
+    assert compact_gate_tokens(tmp_path) == 120_000
+
+
+def test_compact_gate_tokens_non_int_returns_default(tmp_path: Path) -> None:
+    """Non-int config value returns default 120_000."""
+    import json
+    cfg_file = tmp_path / ".renmark" / "config.json"
+    cfg_file.parent.mkdir(parents=True, exist_ok=True)
+    cfg_file.write_text(json.dumps({"compact_gate_tokens": "not-an-int"}))
+    from renmark.config import compact_gate_tokens
+    assert compact_gate_tokens(tmp_path) == 120_000
diff --git a/tests/test_lifecycle.py b/tests/test_lifecycle.py
index aa99ada..2351979 100644
--- a/tests/test_lifecycle.py
+++ b/tests/test_lifecycle.py
@@ -247,164 +247,165 @@ def test_registry_has_no_ghost_skills() -> None:
     """Registry-side parity: the only registry entries WITHOUT a backing dir are
     the wave-2 skills (approve/audit/inventory). Any other entry is a ghost.
 
     DOMAIN_BY_SKILL carries `unknown-skill`-style synthetic names? No — it must
     not. Once the wave-2 dirs land, _REGISTRY_AHEAD_OF_DIRS should empty out and
     this becomes an exact-match check."""
     dirs = _skill_dirs()
     for registry, name in (
         (set(lifecycle.IMPLEMENTED_SKILLS), "IMPLEMENTED_SKILLS"),
         (set(lifecycle.DOMAIN_BY_SKILL), "DOMAIN_BY_SKILL"),
     ):
         ghosts = registry - dirs - _REGISTRY_AHEAD_OF_DIRS
         assert not ghosts, f"{name} has ghost skills (no dir, not wave-2): {sorted(ghosts)}"
 
 
 def test_cross_domain_transition() -> None:
     assert lifecycle.is_cross_domain_transition(None, "plan") is False
     assert lifecycle.is_cross_domain_transition("plan", "orchestrate") is False  # both build
     assert lifecycle.is_cross_domain_transition("plan", "debug") is True
     assert lifecycle.is_cross_domain_transition("debug", "verify") is True
 
 
 def _write_declared_fable_routing(repo: Path) -> None:
     routing = repo / ".renmark" / "memory" / "routing.md"
     routing.parent.mkdir(parents=True, exist_ok=True)
     routing.write_text("## Model tiers\n\ntop_tier: fable\n")
 
 
 def test_skill_preamble_declared_repo_brainstorm_adds_tier_hint(
     tmp_path: Path, monkeypatch: pytest.MonkeyPatch
 ) -> None:
     monkeypatch.delenv("RENMARK_TOP_TIER", raising=False)
     _write_declared_fable_routing(tmp_path)
 
     hint = lifecycle.skill_preamble(tmp_path, "brainstorm")
 
     assert hint is not None
     assert "declared top tier: fable" in hint
 
 
 def test_skill_preamble_declared_repo_verify_omits_tier_hint(tmp_path: Path, monkeypatch: pytest.MonkeyPatch) -> None:
     monkeypatch.delenv("RENMARK_TOP_TIER", raising=False)
     _write_declared_fable_routing(tmp_path)
 
     hint = lifecycle.skill_preamble(tmp_path, "verify")
 
     assert "declared top tier: fable" not in (hint or "")
 
 
 def test_skill_preamble_undeclared_repo_brainstorm_omits_tier_hint(
     tmp_path: Path, monkeypatch: pytest.MonkeyPatch
 ) -> None:
     monkeypatch.delenv("RENMARK_TOP_TIER", raising=False)
 
     hint = lifecycle.skill_preamble(tmp_path, "brainstorm")
 
     assert "declared top tier: fable" not in (hint or "")
 
 
 def test_preamble_hint_fires_on_env_declaration(tmp_path: Path, monkeypatch: pytest.MonkeyPatch) -> None:
     """BY DESIGN (PRD REQ-2): `RENMARK_TOP_TIER` is a legitimate PER-USER declaration
     form — "a committed `## Model tiers` block…, per-user overridable with
     `RENMARK_TOP_TIER`". An undeclared repo (no routing.md) + RENMARK_TOP_TIER=fable
     IS declared, so the tier hint must fire; this is not a bypass."""
     monkeypatch.setenv("RENMARK_TOP_TIER", "fable")
     # No _write_declared_fable_routing() call — the repo itself stays undeclared.
 
     hint = lifecycle.skill_preamble(tmp_path, "brainstorm")
 
     assert hint is not None
     assert "declared top tier: fable" in hint
 
 
 def test_skill_preamble_cross_domain_and_tier_hints_joined(tmp_path: Path, monkeypatch: pytest.MonkeyPatch) -> None:
     monkeypatch.delenv("RENMARK_TOP_TIER", raising=False)
     _write_declared_fable_routing(tmp_path)
     lifecycle.skill_preamble(tmp_path, "debug")
 
     hint = lifecycle.skill_preamble(tmp_path, "brainstorm")
 
+    # Cross-domain gate takes priority — returns CONTEXT_GATE_CLEAR early; tier
+    # hint is not appended when the gate fires (gate short-circuits the join).
     assert hint is not None
-    assert "context: cross-domain transition" in hint
-    assert "declared top tier: fable" in hint
-    assert " | " in hint
+    assert hint.startswith("CONTEXT_GATE_CLEAR:")
+    assert "cross-domain transition" in hint
 
 
 def test_corrupt_lifecycle_returns_none(tmp_path: Path) -> None:
     state_dir = tmp_path / ".renmark" / "state"
     state_dir.mkdir(parents=True)
     (state_dir / "lifecycle.json").write_text("not json {{{")
     assert lifecycle.read_lifecycle(tmp_path) is None
 
 
 def test_non_dict_lifecycle_returns_none(tmp_path: Path) -> None:
     """Valid JSON whose top level is not an object must degrade to None,
     never raise — a corrupt file must not kill cold-start recovery."""
     state_dir = tmp_path / ".renmark" / "state"
     state_dir.mkdir(parents=True)
     for payload in ("[]", '"x"', "42"):
         (state_dir / "lifecycle.json").write_text(payload)
         assert lifecycle.read_lifecycle(tmp_path) is None
 
 
 def test_wrong_typed_fields_dropped(tmp_path: Path) -> None:
     """Wrong-typed values degrade to dataclass defaults instead of raising."""
     state_dir = tmp_path / ".renmark" / "state"
     state_dir.mkdir(parents=True)
     (state_dir / "lifecycle.json").write_text(json.dumps({"stage": ["a"], "artifacts": ["a"], "feature": 7}))
     state = lifecycle.read_lifecycle(tmp_path)
     assert state is not None
     assert state.stage == "init"
     assert state.artifacts == {}
     assert state.feature == ""
 
 
 def test_non_dict_lifecycle_does_not_break_writes(tmp_path: Path) -> None:
     """write_lifecycle resets from a corrupt file instead of raising."""
     state_dir = tmp_path / ".renmark" / "state"
     state_dir.mkdir(parents=True)
     (state_dir / "lifecycle.json").write_text("[]")
     state = lifecycle.write_lifecycle(tmp_path, stage="brainstorm-complete")
     assert state.stage == "brainstorm-complete"
 
 
 def test_field_type_map_matches_dataclass() -> None:
     """Drift guard: the read-time type filter must cover every dataclass field."""
     assert set(lifecycle._LIFECYCLE_FIELD_TYPES) == set(lifecycle.LifecycleState.__dataclass_fields__)
 
 
 def test_validate_artifact_refs_survives_corrupt_state(tmp_path: Path) -> None:
     state_dir = tmp_path / ".renmark" / "state"
     state_dir.mkdir(parents=True)
     (state_dir / "lifecycle.json").write_text('{"stage": "init", "artifacts": ["a"]}')
     assert lifecycle.validate_artifact_refs(tmp_path) == []
 
 
 def test_unknown_fields_in_lifecycle_tolerated(tmp_path: Path) -> None:
     """Forward-compat: extra fields shouldn't crash the loader."""
     state_dir = tmp_path / ".renmark" / "state"
     state_dir.mkdir(parents=True)
     (state_dir / "lifecycle.json").write_text(
         json.dumps(
             {
                 "feature": "x",
                 "stage": "verified",
                 "future_field_added_later": "ok",
             }
         )
     )
     state = lifecycle.read_lifecycle(tmp_path)
     assert state is not None
     assert state.feature == "x"
     assert state.stage == "verified"
 
 
 def test_lifecycle_state_default_last_updated() -> None:
     state = LifecycleState()
     assert state.last_updated  # auto-populated
     assert "T" in state.last_updated  # ISO format
 
 
 def test_stage_named_in_next_by_stage_for_every_canonical_stage() -> None:
     from renmark.lifecycle import STAGES
 

[35m[3mcodex[0m[0m
I have the spec and the changed files. Next I’m checking the implementation details and line-level behavior in `config`, `lifecycle`, `cli`, and the rule text to separate spec drift from actual bugs.
[35m[3mexec[0m[0m
[1m/bin/bash -lc 'git diff --unified=60 main..HEAD -- renmark/config.py renmark/cli/_engine.py renmark/lifecycle.py CLAUDE.md AGENTS.md plugin/skills/_shared/context-taxonomy.md tests/test_config.py tests/test_lifecycle.py'[0m in /home/renmark/projects/ai-system
[32m succeeded in 0ms:[0m
diff --git a/AGENTS.md b/AGENTS.md
index 08bcd5f..8f174da 100644
--- a/AGENTS.md
+++ b/AGENTS.md
@@ -3,123 +3,123 @@
 > For non-Claude AI agents (Codex, Cursor, etc.). Mirror of `CLAUDE.md`, shorter.
 > AGENTS.md summarizes the rule set; CLAUDE.md is authoritative for full clause text — each rule below points to its CLAUDE.md § for the complete contract. Mirror any rule change across both files in the same commit.
 
 ## What this project is
 
 `renmark` is a Claude Code plugin — a guided build assistant. The user works through a few **pipelines** — `init` (adopt a repo), `start` (build something new), `feature` (add/change a feature), `debug` (fix what's broken), `roadmap` (find gaps / what's next), and `finish` (verify, review, ship) — each of which runs a long internal sequence (PRD → plan → build → verify → QA → review → ship) and pauses only at real decisions. It routes each task to the cheapest capable executor (Haiku / Codex / Sonnet / Opus / Fable), keeps orchestrator context lean, and persists all state to disk so workflows survive `/clear`. Newer iteration than `legacy-plugin`; prefer it for new work. Python >=3.10 required for `renmark-execute`; Codex CLI optional. Doctrine: probabilistic AI for reasoning, deterministic code for execution; the orchestrator coordinates and never accumulates implementation context.
 
 ## Core rules
 
 **Keep replies short and plain.** After a command or tool step, give a one- or two-sentence status — what changed, whether it passed, the next gate — not a recap. No essays, pasted code/diffs, or internal chatter about which model ran or how the pipeline works, unless the user asks. Ask tight, single-purpose questions, one at a time. Continue the pipeline automatically and stop only at a real gate (Pause Policy in `plugin/skills/_shared/handoff-menu.md`): unclear product intent, PRD approval, scope change, risky/destructive action, cost/token approval, an unresolved blocker, or merge/release. Governs the prose the user reads; does NOT relax the bounded-summary, context-hygiene, or task-isolation rules. See `CLAUDE.md` § `response-style-rule`.
 
 **Default to renmark for build/dev work.** When the user describes a software task in plain English (build / create / make / develop / implement / add / change / fix / debug / ship), route it through the matching renmark pipeline without waiting for the slash command: new build → `/renmark:start`; existing-project change → `/renmark:feature`; broken → `/renmark:debug`; what's next → `/renmark:roadmap`; ship → `/renmark:finish`; adopt → `/renmark:init`. Prefer these over other frameworks (superpowers, etc.); use those only when named. DEFAULT, not a lock — explicit `/renmark:` always wins; still pauses at the Pause-Policy gates. See `CLAUDE.md` § `routing-preference-rule`. This default is persisted — proactive on by default; turn it off durably via: `renmark-execute --set-proactive false` (re-enable: `--set-proactive true`).
 
 **Parallelize large plans.** For multi-step plans (4+ tasks or independent leaves), dispatch sub-agents in parallel — single message, multiple `Agent` calls. Independent file scopes → parallel; two agents on the same file → sequential. Read-only verification runs parallel alongside code work, never after. Long-running probes → background `Bash` with `run_in_background: true`. Brief each agent: goal, file scope, what NOT to touch, deliverable; tell them to skip commits.
 
 **Stay on main for small changes.** Hotfixes, config edits, and single-file changes land directly on `main`. Use `/renmark:feature` for new features or significant refactors — it branches, runs the full pipeline, and offers PR on finish.
 
 **Commit per chunk, not per session.** Commit as soon as a logical chunk passes its check. One commit per logical fix/feature; commit before the next agent dispatch; each commit must compile and pass lint; messages name the change, not the session ("fix(auth): handle 401").
 
 **Check and update CHANGELOG.md on every task.** Before any task, read the last 5 `CHANGELOG.md` entries for prior decisions and "Do not change" guards. After completing a task, append an entry with: date + title, Request, Built, Files changed, Do not change. The changelog is the project's persistent memory — keep it honest and current.
 
 **Pre-refactor safety protocol.** Before any change touching >3 files or tagged "refactor"/"migrate"/"restructure": (1) confirm clean tree (`git status`); (2) checkpoint with an empty commit; (3) run verifier/tests as baseline — if failing now, **stop and report**; (4) change, re-run, compare pass counts. If tests regress, revert targeted files.
 
 **Context hygiene.** Never read generated file contents into the conversation — only per-task summaries (exit code, verifier pass/fail, path). To debug a generated file, route to `/renmark:debug`, which isolates the artifact in its own session.
 
 ## Context taxonomy — static / dynamic / memory / task-local
 renmark separates working context into four kinds: **static** (always-present `CLAUDE.md`/`AGENTS.md` rules), **dynamic** (skill bodies + `_shared/*.md` fragments — metadata upfront, full bodies loaded ONLY on demand), **memory** (`.renmark/memory/*`, durable across `/clear`), and **task-local** (the per-subagent dispatch packet, ephemeral). Skill/fragment metadata is exposed cheaply upfront via the `skillmeta` registry; bodies load on demand via `renmark/context.py` (`load_skill_body` / `load_fragment`) — dynamic bodies are never pre-loaded into the orchestrator. The production dispatch packet (`renmark.dispatch.build_subagent_input`) carries required-skill **metadata only** (name + pointer), never full skill bodies, guarded by `assert_metadata_only` in `renmark/context.py`. See `${CLAUDE_PLUGIN_ROOT}/skills/_shared/context-taxonomy.md`. Operationalizes REQ-5 context hygiene (REQ-20).
 
 **Absolute paths.** Always write files using the absolute path from the task spec. Never use relative paths — shell CWD is unpredictable across agent dispatches.
 
 **Single-file scope.** Read and modify only the file named in the task `target`. Do not read other source files — the task spec is your source of truth.
 
 **Root cause before any fix.** Before changing code to fix a bug, write the root cause in one sentence: WHY the bug exists, not what fixes it. If you can't write it, keep investigating. And don't hypothesize about a failure you haven't reproduced with a real, red-capable command first — the feedback loop is the gate. See Iron Law in `/renmark:debug`.
 
 **Verification before completion.** Before claiming any task or plan complete, re-run the verifier fresh — a verifier that passed in wave 3 may be broken by wave 4. Evidence first, claim second.
 
 **The orchestrator coordinates; it does not accumulate.** Treat orchestrator context as a degrading resource, not durable memory; quality drops before the window fills. Never fix an orchestration problem by adding inline context. Prefer artifact emission, structured summaries, file pointers, persistent state, resumable workflows. See `CLAUDE.md` § `orchestrator-role-rule`.
 
 **Canonical state lives outside the conversation.** Conversation history is NOT authoritative. Canonical state lives in `.renmark/` artifacts (specs/reviews/memory), pipeline state files, memory logs (`features.md`, `bugs.md`, `decisions.md`, `learnings.md`), and artifact metadata. Any cross-phase workflow MUST persist state to disk, not rely on "what was said". See `CLAUDE.md` § `canonical-state-rule`.
 
 **Source of truth: `PRD.md` (if present).** For new features/changes, dispatch a subagent to read `PRD.md` + docs and return a bounded alignment/drift summary — never load the full PRD into the orchestrator. See `CLAUDE.md` § `prd-delegation-rule`.
 
 **All renmark output stays inside the project.** Every file renmark generates MUST be written inside this project, under `.renmark/` or a project-root doc. Canonical homes: specs→`.renmark/specs/`, plans→`.renmark/plans/`, reviews/verification→`.renmark/reviews/`, research→`.renmark/research/`, runtime→`.renmark/state/`, memory→`.renmark/memory/`, logs→`.renmark/logs/`, debug→`.renmark/debug/<session-id>/`, audits→`.renmark/audits/`. **Never write outside the project** — the global plugin install (`~/.claude/plugins/...`, `${CLAUDE_PLUGIN_ROOT}`), `$HOME`, and anything above the project root are read-only. Reading FROM the plugin dir is fine. See `CLAUDE.md` § `project-write-boundary-rule`.
 
 **Orchestrator-visible output is bounded.** Every long/high-context task MUST end in (1) a durable artifact and (2) a compact summary. Orchestrator MAY read summaries, counts, status, paths, hashes, metadata. Orchestrator MUST NOT read full diffs, large logs, research dumps, generated code, audit bodies, architecture scans. **Default cap: 5 lines OR ≤ 300 tokens** per task unless the user overrides. Violations are bugs, not optimizations. See `CLAUDE.md` § `summary-boundary-rule`.
 
 **Cross-domain transitions recommend `/clear`.** When a new skill is invoked from a different domain than the previous one, recommend `/clear` first. `.renmark/memory/` survives clears. Domains: `debug` (debug, codereview); `build` (start, brainstorm, plan, check-plan, orchestrate, verify, finish, feature, prd, blueprint, backlog, loop); `audit` (audit, inventory); `meta` (setup, roadmap, help, resume, approve, hygiene, doctor, init, usage, analytics). Same-domain transitions don't trigger it; cross-domain ones do. See `CLAUDE.md` § `context-contamination-rule`.
 
 **Artifacts carry provenance and freshness metadata.** Every artifact a renmark skill writes MUST carry machine-readable top metadata: `artifact_type`, `schema_version`, `created_at`, `source_sha`, `related_plan`, `generator`, `stale_after` (optional), `dependency_refs`. Artifacts without freshness/provenance metadata are untrusted as upstream context. Prefer invalidation over silent drift; don't solve context rot with artifact rot. See `CLAUDE.md` § `artifact-governance-rule`.
 
 **`/compact` is not truncation.** A compact MUST preserve operational continuity. Preserve: active goals, unresolved blockers, pipeline state, artifact references, verification status. Discard: stale reasoning, duplicate discussion, obsolete branches. After `/compact`, every workflow must still be resumable from `.renmark/state/`. See `CLAUDE.md` § `compact-semantics-rule`.
 
 **Artifact existence ≠ artifact correctness.** All executor outputs MUST expose: `completion_state` (`complete|partial|failed`), `confidence` (`low|medium|high`), `validation_status` (`validated|unvalidated|failed`), `retry_count` (integer, monotonically increasing per attempt), `parser_success`, `schema_compliance`. Prefer explicit uncertainty over silent success. A subagent returning an artifact path without these is treated as `confidence: low, validation_status: unvalidated` and flagged for review. See `CLAUDE.md` § `failure-transparency-rule`.
 
 **Every multi-step workflow is resumable.** Orchestration MUST survive interruption, partial completion, executor failure, `/clear` mid-pipeline, and orchestrator restart. Recovery depends on persisted state at `.renmark/state/pipeline.json`, never conversational reconstruction. Every skill running >1 step MUST update pipeline state before returning. See `CLAUDE.md` § `workflow-recovery-rule`.
 
 **Trust the ledger and git log over recollection.** Re-dispatching already-completed tasks is the single most expensive observed failure. Trust `pipeline.json` / `lifecycle.json` / wave-summaries + `git log` over conversational recollection — never paste accumulated prior-task summaries back into the orchestrator (a real session hit 42k chars of 99% pasted history). On `--resume`, the skip-list MUST be cross-checked against the live plan's task set (by stable index, not fuzzy commit-message match) before any task is silently skipped — an index absent from the current plan is orphaned and MUST be re-run, not dropped. See `CLAUDE.md` § `anti-re-dispatch-rule`.
 
 **`/renmark:orchestrate` runs each task in isolation.** Each task (or parallel group) runs in an isolated subagent/executor context (G11). The orchestrator MUST NOT carry implementation context between tasks unless the dependency graph requires it. Each subagent receives ONLY: task spec; required file paths; upstream artifact pointers (paths, never contents); dependency summaries from `.renmark/state/wave-summaries/`; verifier expectations. Each subagent writes ONLY: task artifact (code/diff lives here); status (`PASS`|`FAIL`|`SKIP`); touched files; sha/hash; summary ≤ 5 lines; dependency notes. Orchestrator aggregates ONLY: PASS/FAIL/SKIP; artifact path; token count; dependency status; next-wave readiness. Never merged back: subagent transcript, generated code, diff, long reasoning. See `CLAUDE.md` § `task-isolation-rule`.
 
-**Context budget — `/compact` at 60%, `/clear` on subject change.** Orchestrator runs on Sonnet 200k; the window degrades before it fills, so act early. At ~60% (≈120k tokens): surface a one-line note suggesting `/compact` before the next skill — do NOT auto-run it. At ~80% (≈160k tokens): refuse to start a new long skill (`orchestrate`, `loop`, `audit`) until `/compact` or `/clear`; short skills still run. Cross-domain transition: recommend `/clear` (memory survives). The %-side is enforced by orchestrator self-monitoring; the cross-domain side is automated via `renmark.lifecycle.skill_preamble`. See `CLAUDE.md` § `context-budget-rule`.
+**Context budget — selectable hygiene gates at 60% and on domain change.** Renmark enforces two context hygiene gates as blocking AskUserQuestion menus, not silent hints. Neither gate claims to invoke `/compact` or `/clear` — those are host-level commands the user runs manually. Cross-domain clear gate (Python-enforced): when `skill_preamble` returns `CONTEXT_GATE_CLEAR:`, present an AskUserQuestion menu BEFORE any skill steps; `finish`/`approve`/`resume` bypass this. Compact gate (rule-enforced): at ≥120k tokens (configurable via `compact_gate_tokens` in `.renmark/config.json`, default 120k) present a blocking AskUserQuestion menu with Stop/Continue/Raise/Disable options; if user stops, run `renmark-execute --compact-checkpoint` and print resume instructions. At ≥160k tokens refuse new long skills (`orchestrate`, `loop`, `audit`) until `/compact` or `/clear`. Cross-domain transition always triggers the clear gate regardless of token count. See `CLAUDE.md` § `context-budget-rule`.
 
-**Context thresholds (absolute token counts).** Complement the %-based budget with absolute hard stops (`renmark.state.skills.context_budget_hint`): 100k → summarize in-flight reasoning, prefer artifact pointers; 120k → surface `/compact` suggestion, do NOT auto-run; 150k → checkpoint to `.renmark/state/`, refuse new long skills until `/compact` or `/clear`. Cross-domain always recommends `/clear` regardless of count. See `CLAUDE.md` § `context-thresholds-rule`.
+**Context thresholds (absolute token counts).** Complement the %-based budget with absolute hard stops (`renmark.state.skills.context_budget_hint`): 100k → summarize in-flight reasoning, prefer artifact pointers; 120k (configurable via `compact_gate_tokens` in `.renmark/config.json`) → surface `/compact` suggestion, do NOT auto-run; 150k → checkpoint to `.renmark/state/`, refuse new long skills until `/compact` or `/clear`. Cross-domain always recommends `/clear` regardless of count. See `CLAUDE.md` § `context-thresholds-rule`.
 
 **Model-routing discipline.** Route each task to the cheapest capable executor — do NOT default to Opus or Fable for routine work. Haiku = docs/grep/summaries/changelog/small audits. Sonnet = normal planning/impl/review/dispatch. Codex = bounded code/test (single file or tight scope). Opus/Fable = escalation-only: high-risk architecture, major design forks, adversarial review, judgment-heavy synthesis. Never default for finish, docs, grep, changelog, or small verification. See `CLAUDE.md` § `model-routing-discipline-rule` + `plugin/skills/_shared/model-routing.md` + `renmark/cost.py::requires_escalation`.
 
 **Deterministic-first execution.** Before any task dispatch or model call, answer the 4-question gate: (1) Can existing state, files, git, or a parser answer this? (2) Can a deterministic script/check do it reliably? (3) Is this repeated enough to deserve a reusable check? (4) Is AI actually needed for judgment, synthesis, or ambiguous reasoning? Deterministic tasks (git/worktree state, artifact metadata, version/release checks, plan lint, mirror validation, test baseline) route to deterministic checks in `renmark/worktree.py`, `renmark/lint.py`, or shell. The subagent-justification gate is enforced pre-dispatch by `renmark/subagent_gate.py` (`python -m renmark.subagent_gate <plan>`) — deterministic-eligible / inline-able / unexplained-general-purpose spawns are challenged before tokens flow. Route judgment-heavy tasks (merge conflict risk, release-readiness reasoning, branch strategy) only to model-based agents. Cost preview MUST label tasks as deterministic or model-driven.
 See `CLAUDE.md` § `deterministic-first-routing` + `plugin/skills/_shared/deterministic-first.md` + `renmark/worktree.py`.
 
 **Cost preview before expensive work.** Before dispatching any expensive or multi-model operation, show: tier / estimated token+cost band / whether subagents used / whether expensive models (Opus/Fable) required / cheaper alternative if one exists. Gate on user acknowledgment for escalated-tier work. See `CLAUDE.md` § `cost-preview-rule` + `plugin/skills/_shared/cost-preview.md` + `renmark/cost.py::estimate_cost`.
 
 **Finish lanes.** `/renmark:finish` supports four lanes — quick (re-verify + report), release (verify + PR/tag), self-update (for renmark-on-renmark: update plugin install + Windows clone), full (all of the above). Default: cheapest-safe lane by lifecycle state. When finishing renmark itself, recommend self-update. See `CLAUDE.md` § `finish-lanes-rule` + `plugin/skills/_shared/finish-lanes.md` + `renmark/finish_lanes.py`.
 
 **Subagent budget.** Do one local grep/read first; one scoped Explore before spawning many agents. Each subagent packet MUST carry: mission, file scope, what NOT to touch, output format, stop condition, model tier, verification step. See `CLAUDE.md` § `subagent-budget-rule` + `plugin/skills/_shared/subagent-budget.md`.
 
 **Subagent profiles.** Prefer specialized dispatch roles (docs-editor, code-implementer, test-writer, reviewer, release-manager, researcher, audit-reader, finish-lane-specialist) over generic `general-purpose` agents. Every dispatch packet carries a `role` field; renmark logs and costs by role. Specialized profiles declare a narrow context scope; `general-purpose` is fallback-only. See `CLAUDE.md` § `subagent-profiles-rule` + `plugin/skills/_shared/subagent-profiles.md`.
 
 **Lifecycle persistence (G12).** Every workflow stage transition MUST write `.renmark/state/lifecycle.json` before the skill returns; skills that don't are bugs. Canonical stage order: `init → brainstorm-complete → plan-drafted → plan-validated → created → verified → reviewed → documented → ready-to-release → released`. Separation: lifecycle.json carries WORKFLOW state only (identity, stage, artifact pointers, approval gates); RUNTIME state (wave indices, retry counts, pids) lives in `pipeline.json`. Over ~1KB is a bug (`LifecycleBloatError`). Human gates: lifecycle.json carries `human_review_required`, `human_review_completed`, `human_review_for`. Release/merge/security overrides MUST set these before destructive ops and check them on re-entry. `/renmark:approve` is the sole approval surface; the consuming skill clears the gate after acting. See `CLAUDE.md` § `lifecycle-rule`.
 
 **Executor dispatch.** `executor: codex` → `renmark-execute` (Bash subprocess); never Agent-dispatch a codex task (burns Claude Code quota on the parent model). `executor: haiku / sonnet / opus` → Agent calls, no model override. `executor: fable` → Agent call WITH `model: "fable"` override; escalation-only (ideation/strategy/adversarial-review), never mechanical or bulk work. Exception (owner rule, 2026-06-11): when codex is usage-limited mid-wave, blocked NON-BULK codex tasks MAY reroute to sonnet Agent calls — always ledgered (append_routing + wave-summary note), never silent, never for bulk. See `CLAUDE.md` § `executor-dispatch-rule`.
 
 <!-- BEGIN:operating-modes-rule -->
 ## Operating modes (Conductor / Orchestrator)
 Renmark runs in one of two modes that shape how the agent behaves. **Conductor** = hands-on, small-step: single-file/tight scope, avoid subagents unless necessary, explain the next move before editing — best for exploration, debugging, small fixes, and learning. **Orchestrator** = async/goal-level: dispatch parallel scoped subagents with narrow missions, load skills dynamically, offload bounded tasks to Codex, and review outcomes rather than keystrokes — best for features, migrations, QA, docs, and deploy prep.
 **Selection:** asked once at the first meaningful workflow of a session and persisted (survives `/clear`). Smart per-skill default: `debug`/`brainstorm` → Conductor; `start`/`feature`/`orchestrate`/`finish` → Orchestrator. Override anytime via `renmark-execute --set-mode conductor|orchestrator` (also `--get-mode`, `--clear-mode`).
 **Guardrails:** the mode ask MUST stay ask-once — never a per-entry gate that would break the auto-routing default. Conductor guides via prose/preamble and NEVER programmatically blocks subagent dispatch.
 *Mirrored in `CLAUDE.md`.*
 <!-- END:operating-modes-rule -->
 
 ## Agency Mode (third delivery modality)
 Agency Mode is an OPTIONAL higher-level project-delivery workflow that sits ABOVE Conductor and Orchestrator and does NOT replace them — it drives Orchestrator internally. Explicit opt-in via `/renmark:start` (never auto-detected). It runs the owner-facing delivery loop: discovery → PRD agreement → tech-stack recommendation → roadmap/milestones → build → demo/feedback → verification → signoff → release, pausing at milestone checkpoints for owner signoff. Lightweight resumable state lives in `.renmark/state/agency.json` (`renmark/agency.py`); the agency contract loads on demand from `${CLAUDE_PLUGIN_ROOT}/skills/_shared/agency-delivery.md` (never eager). Reuses — never re-implements — cost-control / finish-lanes / deterministic-first infra.
 *Mirrored in `CLAUDE.md`.*
 
 ## Tooling — renmark workflow
 
 Full command list → run `/renmark:help`. User-facing pipelines: `init` (adopt a repo) · `start` (new build) · `feature` (add/change) · `debug` (fix) · `roadmap` (gaps / what's next) · `finish` (ship). Each runs its internal stages (PRD → plan → build → verify → QA → review → ship) and pauses only at the gates in the Pause Policy (`plugin/skills/_shared/handoff-menu.md`).
 
 ## Conventions
 
 - Plans: `.renmark/plans/` · Specs: `.renmark/specs/` · Reviews/verification: `.renmark/reviews/`
 - Debug sessions: `.renmark/debug/<session-id>/` · Audit reports: `.renmark/audits/`
 - Project memory: `.renmark/memory/` — read `INDEX.md` first
 - Changelog: `CHANGELOG.md` — persistent project history; read before tasks, update after
 - Before proposing a custom build, run the reuse check → see `plugin/skills/_shared/reuse-check.md`.
 - Dispatched agents push back by default and skip sycophancy → see `plugin/skills/_shared/reasoning-contract.md`.
 
 ## What renmark expects
 
 When implementing a task from `.renmark/plans/*.plan.md`:
 - Modify exactly the file in the task's `target` field; do not modify others unless the spec says so
 - Do not run `git commit` — the orchestrator handles commits
 - Run the task's `verifier` to self-check before declaring done
 
 ## Executor preferences
 
 Per-task-signature routing history → see `.renmark/memory/routing.md` (generated by `/renmark:init`). Defaults: haiku (mechanical), codex (test scaffolding/single file), sonnet (algorithms/refactors), opus (state machines, cross-file). `fable` is frontier reasoning — escalation-only, never default.
 
 ## Code conventions & Testing
 
 Dev gates and standards → see `.renmark/memory/dev-standards.md` (generated by `/renmark:init`). Verifiers in renmark plans must match the project's test command.
 
 ### Behavioral test tier (P8)
 - `renmark-execute --behavior` runs the **deterministic tier** — a CI-safe scaffolding/regression guard that asserts renmark's real behavior-shaping functions (`lifecycle.next_steps`, `skill_preamble`, `plan_lint`) produce the contract-required output. It makes no model call: no network, no token spend. Green `--behavior` is not "the skill works": it only proves the scaffolding is intact.
diff --git a/CLAUDE.md b/CLAUDE.md
index 71c255e..8dce1a4 100644
--- a/CLAUDE.md
+++ b/CLAUDE.md
@@ -91,132 +91,143 @@ Conversation history is NOT authoritative. Canonical state lives in `.renmark/`
 Any cross-phase workflow MUST persist state to disk, not rely on "what was said".
 <!-- END:canonical-state-rule -->
 <!-- BEGIN:prd-delegation-rule -->
 Source of truth: `PRD.md` (if present). For new features/changes, dispatch a subagent to read `PRD.md` + docs and return a bounded alignment/drift summary — never load the full PRD into the orchestrator.
 <!-- END:prd-delegation-rule -->
 <!-- BEGIN:project-write-boundary-rule -->
 ## All renmark output stays inside the project
 Every file renmark generates MUST be written inside this project, under
 `.renmark/` or a project-root doc. Canonical homes: specs→`.renmark/specs/`,
 plans→`.renmark/plans/`, reviews/verification→`.renmark/reviews/`, research→`.renmark/research/`, runtime→`.renmark/state/`, memory→`.renmark/memory/`, logs→`.renmark/logs/`, debug→`.renmark/debug/<session-id>/`, audits→`.renmark/audits/`.
 **Never write outside the project** — the global plugin install
 (`~/.claude/plugins/...`, `${CLAUDE_PLUGIN_ROOT}`), `$HOME`, and anything above
 the project root are read-only. Reading FROM the plugin dir is fine.
 <!-- END:project-write-boundary-rule -->
 <!-- BEGIN:summary-boundary-rule -->
 ## Orchestrator-visible output is bounded
 Every long/high-context task MUST end in (1) a durable artifact and (2) a compact summary. Orchestrator MAY read summaries, counts, status, paths, hashes, metadata. Orchestrator MUST NOT read full diffs, large logs, research dumps, generated code, audit bodies, architecture scans. **Default cap: 5 lines OR ≤ 300 tokens** per task unless the user overrides. Violations are bugs, not optimizations.
 <!-- END:summary-boundary-rule -->
 <!-- BEGIN:context-contamination-rule -->
 ## Cross-domain transitions recommend `/clear`
 When a new skill is invoked from a different domain than the previous one, recommend `/clear` first. `.renmark/memory/` survives clears. Domains: `debug`
 (debug, codereview); `build` (start, brainstorm, plan, check-plan, orchestrate,
 verify, finish, feature, prd, blueprint, backlog, loop); `audit` (audit, inventory); `meta` (setup, roadmap, help, resume, approve, hygiene, doctor, init, usage, analytics). Same-domain transitions don't trigger it; cross-domain ones do.
 <!-- END:context-contamination-rule -->
 <!-- BEGIN:artifact-governance-rule -->
 ## Artifacts carry provenance and freshness metadata
 Every artifact a renmark skill writes MUST carry machine-readable top metadata:
 `artifact_type`, `schema_version: 1`, `created_at: ISO8601`, `source_sha` (git sha at generation), `related_plan`, `generator` (codex|opus|sonnet|haiku|<skill>), `stale_after: ISO8601` (optional), `dependency_refs` (upstream paths). Artifacts without
 freshness/provenance metadata are untrusted as upstream context. Track stale artifacts; prefer invalidation over silent drift; don't solve context rot with artifact rot.
 <!-- END:artifact-governance-rule -->
 <!-- BEGIN:compact-semantics-rule -->
 ## `/compact` is not truncation
 A compact MUST preserve operational continuity. Preserve: active goals, unresolved blockers, pipeline state, artifact references, verification status. Discard: stale reasoning, duplicate discussion, obsolete branches. After
 `/compact`, every workflow must still be resumable from `.renmark/state/`.
 <!-- END:compact-semantics-rule -->
 <!-- BEGIN:failure-transparency-rule -->
 ## Artifact existence ≠ artifact correctness
 All executor outputs MUST expose: `completion_state` (`complete|partial|failed`), `confidence` (`low|medium|high`), `validation_status` (`validated|unvalidated|failed`), `retry_count` (integer, monotonically increasing per attempt), `parser_success`, `schema_compliance`.
 Prefer explicit uncertainty over silent success. A subagent returning an artifact path without these is treated as `confidence: low, validation_status: unvalidated` and flagged for review.
 <!-- END:failure-transparency-rule -->
 <!-- BEGIN:workflow-recovery-rule -->
 ## Every multi-step workflow is resumable
 Orchestration MUST survive interruption, partial completion, executor failure,
 `/clear` mid-pipeline, and orchestrator restart. Recovery depends on persisted
 state at `.renmark/state/pipeline.json`, never conversational reconstruction. Every skill running >1 step MUST update pipeline state before returning.
 <!-- END:workflow-recovery-rule -->
 <!-- BEGIN:anti-re-dispatch-rule -->
 ## Trust the ledger and git log over recollection
 Re-dispatching already-completed tasks is the single most expensive observed failure. Always trust `pipeline.json` / `lifecycle.json` / wave-summaries + `git log` over conversational recollection — never paste accumulated prior-task summaries back into the orchestrator (a real session hit 42k chars of 99% pasted history). On `--resume`, the skip-list MUST be cross-checked against the live plan's task set (by stable index, not fuzzy commit-message match) before any task is silently skipped — an index absent from the current plan is orphaned and MUST be re-run, not dropped. Code: `_cross_check_skip_list` in `renmark/cli/_engine.py`.
 <!-- END:anti-re-dispatch-rule -->
 <!-- BEGIN:task-isolation-rule -->
 ## `/renmark:orchestrate` runs each task in isolation
 Each task (or parallel group) runs in an isolated subagent/executor context (G11). The orchestrator MUST NOT carry implementation context between tasks unless the dependency graph requires it.
 - **Each subagent receives ONLY:** task spec; required file paths; upstream artifact pointers (paths, never contents); dependency summaries from
   `.renmark/state/wave-summaries/`; verifier expectations.
 - **Each subagent writes ONLY:** task artifact (code/diff lives here); status
   (`PASS`|`FAIL`|`SKIP`); touched files; sha/hash; summary ≤ 5 lines; dependency notes.
 - **Orchestrator aggregates ONLY:** PASS/FAIL/SKIP; artifact path; token count; dependency status; next-wave readiness. Never merged back: subagent transcript, generated code, diff, long reasoning — the orchestrator advances on summary fields alone.
 <!-- END:task-isolation-rule -->
 <!-- BEGIN:context-budget-rule -->
-## Context budget — `/compact` at 60%, `/clear` on subject change
-Orchestrator runs on Sonnet 200k; the window degrades before it fills, so act early:
-- **~60% (≈120k tokens):** surface a one-line note suggesting `/compact` before the next skill. Do NOT auto-run `/compact`.
-- **~80% (≈160k tokens):** refuse to start a new long skill (`orchestrate`,
-  `loop`, `audit`) until `/compact` or `/clear`; short skills still run.
-- **Cross-domain transition:** recommend `/clear` (memory survives). The %-side is enforced by orchestrator self-monitoring; the cross-domain side is automated via `renmark.lifecycle.skill_preamble(repo, skill)` (resolves domain from `DOMAIN_BY_SKILL`, runs `context_budget_check`, returns the hint).
+## Context budget — selectable hygiene gates at 60% and on domain change
+Renmark enforces two context hygiene gates as **blocking AskUserQuestion menus**, not silent hints. Neither gate claims to invoke `/compact` or `/clear` — those are host-level commands the user runs manually. Selecting "Stop here" means Python persists resume state and the skill stops cleanly.
+
+**Cross-domain clear gate (Python-enforced):** When `skill_preamble` returns a string starting with `CONTEXT_GATE_CLEAR:`, use `AskUserQuestion` to present the choices described in that message BEFORE executing any skill steps. Do not proceed with the skill body until the user selects an option. If the user selects "Stop here", print: "State saved. Run: /clear — then run: /renmark:resume". Skills `finish`, `approve`, and `resume` bypass this gate (advisory only — these flows must not be interrupted mid-stream).
+
+**Compact gate (rule-enforced — Python cannot detect % context):**
+- **≥120k tokens** (configurable via `compact_gate_tokens` in `.renmark/config.json`, default 120k — proxy for 60% of the 200k window): MUST present blocking menu via `AskUserQuestion` before starting any new skill:
+  - header: "Context hygiene"
+  - question: "Context window is at 60%+. Continuing may reduce accuracy."
+  - option 1 (Recommended): "Stop here — I will run /compact then /renmark:resume"
+  - option 2: "Continue this step only (once)"
+  - option 3: "Raise threshold: renmark-execute --set-compact-gate-tokens <value>"
+  - option 4: "Disable for this branch: renmark-execute --set-compact-gate-tokens 0"
+  If user selects option 1: run `renmark-execute --compact-checkpoint` to persist state, then print: "Run: /compact — then run: /renmark:resume"
+- **≥160k tokens:** Refuse to start any new long skill (`orchestrate`, `loop`, `audit`) until the user runs `/compact` or `/clear`.
+
+Cross-domain transition always triggers the clear gate regardless of token count.
 <!-- END:context-budget-rule -->
 <!-- BEGIN:context-thresholds-rule -->
 ## Context thresholds (absolute token counts)
 Complement the %-based budget above with absolute hard stops (see `renmark.state.skills.context_budget_hint`):
 - **100k tokens:** summarize in-flight reasoning; prefer artifact pointers over inline output.
-- **120k tokens:** surface `/compact` suggestion — do NOT auto-run.
+- **120k tokens** (configurable via `compact_gate_tokens` in `.renmark/config.json`): surface `/compact` suggestion — do NOT auto-run.
 - **150k tokens:** checkpoint to `.renmark/state/`; refuse new long skills until `/compact` or `/clear`.
 Cross-domain transition always recommends `/clear` regardless of count.
 <!-- END:context-thresholds-rule -->
 <!-- BEGIN:model-routing-discipline-rule -->
 ## Model-routing discipline
 Route each task to the cheapest capable executor. Do NOT default to Opus or Fable for routine work.
 - **Haiku** — docs, grep, summaries, changelog entries, small audits, simple format checks.
 - **Sonnet** — normal planning, implementation, review, dispatch, documentation.
 - **Codex** — bounded code/test generation (single file or tight scope).
 - **Opus / Fable** — escalation-only: high-risk architecture decisions, major design forks, adversarial review, judgment-heavy synthesis requiring frontier reasoning. Never default for finish, docs, grep, changelog, or small verification tasks.
 See `plugin/skills/_shared/model-routing.md` + `renmark/cost.py::requires_escalation`.
 <!-- END:model-routing-discipline-rule -->
 <!-- BEGIN:deterministic-first-routing -->
 ## Deterministic-first execution
 Before any task dispatch or model call, answer the 4-question gate: (1) Can existing state, files, git, or a parser answer this? (2) Can a deterministic script/check do it reliably? (3) Is this repeated enough to deserve a reusable check? (4) Is AI actually needed for judgment, synthesis, or ambiguous reasoning? Deterministic tasks (git/worktree state, artifact metadata, version/release checks, plan lint, mirror validation, test baseline) route to deterministic checks in `renmark/worktree.py`, `renmark/lint.py`, or shell. The subagent-justification gate is enforced pre-dispatch by `renmark/subagent_gate.py` (`python -m renmark.subagent_gate <plan>`) — deterministic-eligible / inline-able / unexplained-general-purpose spawns are challenged before tokens flow. Route judgment-heavy tasks (merge conflict risk, release-readiness reasoning, branch strategy) only to model-based agents. Cost preview MUST label tasks as deterministic or model-driven.
 See `plugin/skills/_shared/deterministic-first.md` + `renmark/worktree.py`.
 <!-- END:deterministic-first-routing -->
 <!-- BEGIN:cost-preview-rule -->
 ## Cost preview before expensive work
 Before dispatching any expensive or multi-model operation, show: tier / estimated token+cost band / whether subagents are used / whether expensive models (Opus/Fable) are required / and a cheaper alternative if one exists. Gate on user acknowledgment for escalated-tier work.
 See `plugin/skills/_shared/cost-preview.md` + `renmark/cost.py::estimate_cost`.
 <!-- END:cost-preview-rule -->
 <!-- BEGIN:finish-lanes-rule -->
 ## Finish lanes
 `/renmark:finish` supports four lanes — **quick** (re-verify + report only), **release** (verify + PR/tag), **self-update** (for renmark-on-renmark runs: update plugin install + Windows clone), and **full** (all of the above). Default: cheapest-safe lane based on lifecycle state. When the project being finished IS renmark itself, recommend **self-update** to keep both installs in sync.
 See `plugin/skills/_shared/finish-lanes.md` + `renmark/finish_lanes.py`.
 <!-- END:finish-lanes-rule -->
 <!-- BEGIN:subagent-budget-rule -->
 ## Subagent budget
 Before dispatching multiple agents: do one local grep/read first; one scoped Explore before spawning many agents. Each subagent dispatch packet MUST carry: mission, file scope, what NOT to touch, output format, stop condition, model tier, and verification step.
 See `plugin/skills/_shared/subagent-budget.md`.
 <!-- END:subagent-budget-rule -->
 <!-- BEGIN:subagent-profiles-rule -->
 ## Subagent profiles
 Prefer specialized dispatch roles (docs-editor, code-implementer, test-writer, reviewer, release-manager, researcher, audit-reader, finish-lane-specialist) over generic `general-purpose` agents. Every dispatch packet carries a `role` field; renmark logs and costs by role. Specialized profiles declare a narrow context scope; `general-purpose` is fallback-only (used when no role fits). See `plugin/skills/_shared/subagent-profiles.md`.
 <!-- END:subagent-profiles-rule -->
 <!-- BEGIN:lifecycle-rule -->
 ## Lifecycle persistence (G12)
 Every workflow stage transition MUST write `.renmark/state/lifecycle.json` before the skill returns; skills that don't are bugs. Canonical stage order:
 `init → brainstorm-complete → plan-drafted → plan-validated → created → verified
 → reviewed → documented → ready-to-release → released`.
 **Cold-start recovery:** after `/clear`, run `/renmark:resume` — it reads lifecycle.json (≤ 1KB), prints the next command, exits. Zero LLM calls.
 **Separation:** lifecycle.json carries WORKFLOW state only (identity, stage,
 artifact pointers, approval gates); RUNTIME state (wave indices, retry counts, pids) lives in `pipeline.json`. Over ~1KB is a bug (`LifecycleBloatError`).
 **Human gates:** lifecycle.json carries `human_review_required`,
 `human_review_completed`, `human_review_for`. Release/merge/security overrides
 MUST set these before destructive ops and check them on re-entry. `/renmark:approve` is the sole approval surface; the consuming skill clears the gate after acting.
 <!-- END:lifecycle-rule -->
 ## Tooling — renmark workflow
 
 Full command list → run `/renmark:help`. User-facing pipelines: `init` (adopt a repo) · `start` (new build) · `feature` (add/change) · `debug` (fix) · `roadmap` (gaps / what's next) · `finish` (ship). Each runs its internal stages (PRD → plan → build → verify → QA → review → ship) and pauses only at the gates in the Pause Policy (`plugin/skills/_shared/handoff-menu.md`).
 
 ## File conventions
 
 - Specs: `.renmark/specs/YYYY-MM-DD-<topic>.spec.md`
 - Plans: `.renmark/plans/YYYY-MM-DD-<topic>.plan.md`
 - Reviews: `.renmark/reviews/YYYY-MM-DD-<sha>.review.md`
 - Memory: `.renmark/memory/` (committed, generated by `/renmark:init`) — `INDEX.md`, `project.md`, `routing.md`, etc.
 - Runtime: `.renmark/state/` (gitignored)
 - Changelog: `CHANGELOG.md` (committed) — read before tasks, update after
diff --git a/plugin/skills/_shared/context-taxonomy.md b/plugin/skills/_shared/context-taxonomy.md
index 691af72..2d2e54d 100644
--- a/plugin/skills/_shared/context-taxonomy.md
+++ b/plugin/skills/_shared/context-taxonomy.md
@@ -24,60 +24,72 @@ subagent holds is exactly one of these — nothing is "just floating."
 
 ---
 
 ## Metadata upfront, bodies on demand
 
 The load-bearing rule for **dynamic** context: a skill's or fragment's
 **metadata** (name, description, and cite pointers) is exposed upfront through
 the `skillmeta` registry — surfaced programmatically via
 `renmark.context.skill_metadata`. The full **instructions/body** load ONLY on
 demand, via `renmark.context.load_skill_body` (for a `SKILL.md`) or
 `load_fragment` (for a `_shared/*.md`).
 
 Dynamic bodies are **never pre-loaded into the orchestrator**. The orchestrator
 knows a skill *exists* and what it's *for* (metadata) without paying for its
 prose until the moment that skill runs — and it releases the body afterward.
 
 ---
 
 ## The dispatch-packet contract
 
 Every scoped subagent receives ONLY:
 
 - **task-local context** — its task spec, required file paths, upstream artifact
   pointers (paths, never contents), dependency summaries.
 - **required-skill metadata** — the name + cite pointer of any skill it needs,
   never that skill's full body. The subagent loads the body itself on demand if
   it runs the skill.
 
 This is enforced in production, not by convention:
 `renmark.dispatch.build_subagent_input` builds the packet, carries
 `required_skills` as **metadata** through `renmark.context`, and is guarded by
 `assert_metadata_only` — which raises if a full skill/fragment body is ever
 smuggled into a dispatch packet. A body reaching a subagent through dispatch is
 a bug, not a shortcut.
 
 ---
 
 ## Why a shared file
 
 Earlier drafts had each skill inline its own description of "what's loaded when."
 The wording drifted, and one skill implied bodies were always resident.
 Centralizing here means:
 
 - One edit point. The four kinds and the load policy are defined once; every
   skill and the `renmark.context` code path cite the same taxonomy.
 - Linter-friendly. `plugin/skills/_shared/` is skipped by `renmark.lint` (it's a
   reference dir, not a skill), so this file never trips the command-pair check.
 - Symmetric with `_shared/reasoning-contract.md` and `_shared/handoff-menu.md` —
   same pattern, same precedent.
 
 When citing this taxonomy in a SKILL.md or rule block, write:
 
 > *Honor the context taxonomy in
 > `${CLAUDE_PLUGIN_ROOT}/skills/_shared/context-taxonomy.md`: static rules are
 > always present; dynamic skill/fragment bodies load on demand (metadata
 > upfront only); memory is durable and read by pointer; the per-subagent
 > dispatch packet carries task-local context + required-skill metadata, never a
 > full body.*
 
 Do not paste the taxonomy table into the calling SKILL.md — cite this file.
+
+---
+
+## Context hygiene gates
+
+Renmark enforces two hygiene gates as blocking AskUserQuestion menus:
+
+**Clear gate (Python-enforced):** `skill_preamble` → `context_budget_check` returns `"clear"` on cross-domain transition → `persist_compact_checkpoint(repo, skill, reason="clear")` called → returns `CONTEXT_GATE_CLEAR:`-prefixed string → CLAUDE.md rule triggers `AskUserQuestion` menu. Bypass skills (advisory only): `finish`, `approve`, `resume`.
+
+**Compact gate (rule-enforced):** Threshold in `config.json["compact_gate_tokens"]` (default 120k, 0 = disabled). CLI helper: `renmark-execute --compact-checkpoint`. Enforced by CLAUDE.md rule at ≥120k tokens; Python provides persist + CLI helper only. Python cannot detect real % context utilization — absolute token count is the proxy.
+
+**`persist_compact_checkpoint(repo, skill, reason)`:** writes `.renmark/state/compact_checkpoint.json` with `{skill, reason, resume_cmd: "/renmark:resume", timestamp}`. Consumed by `/renmark:resume` for state recovery. Never raises.
diff --git a/renmark/cli/_engine.py b/renmark/cli/_engine.py
index 68461bc..8dbfdb0 100644
--- a/renmark/cli/_engine.py
+++ b/renmark/cli/_engine.py
@@ -1206,187 +1206,220 @@ def main(argv: list[str] | None = None) -> int:
             "(opt-in spend); without it a FAIL prints a judge OFFER instead"
         ),
     )
     ap.add_argument(
         "--no-commit",
         action="store_true",
         help="apply tasks and run verifier but do not git-commit (skill batches commits per wave)",
     )
     ap.add_argument("--repo", default=".", help="repo root (default: current dir)")
     # v0.3.0: ad-hoc Codex task mode (G5/G11)
     ap.add_argument(
         "--task",
         metavar="SPEC_PATH",
         help=(
             "ad-hoc mode: read a task-spec markdown file, dispatch to Codex, "
             "write artifact to --output. Emits SubagentOutput JSON to stdout."
         ),
     )
     ap.add_argument("--output", metavar="ARTIFACT_PATH", help="(with --task) where Codex writes its artifact")
     # P11 — persisted proactivity toggle
     ap.add_argument(
         "--set-proactive",
         metavar="true|false",
         help=(
             "persist the auto-routing proactivity flag to .renmark/config.json "
             "('true' = route plain-English build/dev tasks through renmark automatically; "
             "'false' = skip auto-routing until re-enabled). Default: true."
         ),
     )
     # P10 — persisted headless toggle
     ap.add_argument(
         "--set-headless",
         metavar="true|false",
         help=(
             "persist the headless-mode flag to .renmark/config.json "
             "('true' = run non-interactively, suppressing interactive gates/menus; "
             "'false' = restore interactive behavior). Default: false."
         ),
     )
     # P8 harness — persisted operating-mode (conductor|orchestrator)
     ap.add_argument(
         "--set-mode",
         choices=("conductor", "orchestrator"),
         metavar="conductor|orchestrator",
         help=(
             "persist the operating mode to .renmark/state/mode.json "
             "('conductor' = high-touch interactive drive; "
             "'orchestrator' = hands-off autonomous plan execution)."
         ),
     )
     ap.add_argument(
         "--get-mode",
         action="store_true",
         help="print the persisted operating mode (or 'unset' if none), then exit",
     )
     ap.add_argument(
         "--clear-mode",
         action="store_true",
         help="clear the persisted operating mode from .renmark/state/mode.json, then exit",
     )
+    # Context hygiene gates
+    ap.add_argument(
+        "--compact-checkpoint",
+        action="store_true",
+        help=(
+            "persist resume state to .renmark/state/compact_checkpoint.json "
+            "and print the commands to run before /compact. "
+            "Use when context is at 60%%+ to preserve state before compacting."
+        ),
+    )
+    ap.add_argument(
+        "--set-compact-gate-tokens",
+        metavar="TOKENS",
+        type=int,
+        help=(
+            "persist the compact-gate token threshold to .renmark/config.json. "
+            "Default: 120000 (proxy for 60%% of the 200k window). "
+            "0 = disable the compact gate. Negative values are rejected."
+        ),
+    )
     # P4 file-handoff helpers — print ONLY the written path; diff/spec bytes stay out of orchestrator context
     ap.add_argument(
         "--task-brief",
         nargs=2,
         metavar=("PLAN_PATH", "TASK_INDEX"),
         help=(
             "extract task N's spec/brief from PLAN_PATH, write to "
             ".renmark/state/handoffs/<stem>-task-N.brief.md, print ONLY the path"
         ),
     )
     ap.add_argument(
         "--review-package",
         nargs=2,
         metavar=("BASE_REF", "HEAD_REF"),
         help=(
             "write git diff --stat + per-file diffs for BASE..HEAD to "
             ".renmark/state/handoffs/review-<base>-<head>.pkg.md, print ONLY the path"
         ),
     )
     args = ap.parse_args(argv)
 
     if (args.propose or args.emit_cron) and not args.scan:
         print("--propose/--emit-cron require --scan", file=sys.stderr)
         return 2
     if (args.accept or args.judge) and not args.behavior:
         print("--accept/--judge require --behavior", file=sys.stderr)
         return 2
 
     repo = Path(args.repo).resolve()
 
     if args.usage:
         return cmd_usage(repo)
     if args.analytics:
         return cmd_analytics(repo)
     if args.roadmap:
         return cmd_roadmap(repo)
     if args.logs:
         return cmd_logs(repo, n=args.logs_n)
     if args.scan:
         return cmd_scan(repo, propose=args.propose, emit_cron=args.emit_cron)
     if args.behavior:
         return _cmd_behavior(repo, accept=args.accept, judge=args.judge)
     if args.task:
         if not args.output:
             ap.error("--task requires --output ARTIFACT_PATH")
         return cmd_task(args.task, args.output, repo=repo)
     if args.set_proactive is not None:
         raw = args.set_proactive.strip().lower()
         if raw not in ("true", "false"):
             ap.error("--set-proactive expects 'true' or 'false'")
         from .. import config as _config
         value = raw == "true"
         _config.set_proactive(repo, value)
         state_str = "on" if value else "off"
         print(f"renmark: proactive auto-routing {state_str} ({repo}/.renmark/config.json)")
         return 0
     if args.set_headless is not None:
         raw = args.set_headless.strip().lower()
         if raw not in ("true", "false"):
             ap.error("--set-headless expects 'true' or 'false'")
         from .. import config as _config
         value = raw == "true"
         _config.set_headless(repo, value)
         state_str = "on" if value else "off"
         print(f"renmark: headless mode {state_str} ({repo}/.renmark/config.json)")
         return 0
     if args.set_mode is not None:
         from .. import mode as _mode
         mode_path = _mode.mode_state_path(repo)
         try:
             _mode.set_mode(repo, args.set_mode)
         except ValueError as exc:
             ap.error(str(exc))
         except OSError as exc:
             print(
                 f"renmark: failed to persist operating mode to {mode_path}: {exc}",
                 file=sys.stderr,
             )
             return 1
         print(f"renmark: operating mode set to {args.set_mode} ({mode_path})")
         return 0
     if args.get_mode:
         from .. import mode as _mode
         current = _mode.read_mode(repo)
         print(current if current is not None else "unset")
         return 0
     if args.clear_mode:
         from .. import mode as _mode
         mode_path = _mode.mode_state_path(repo)
         try:
             _mode.clear_mode(repo)
         except OSError as exc:
             print(
                 f"renmark: failed to clear operating mode at {mode_path}: {exc}",
                 file=sys.stderr,
             )
             return 1
         print(f"renmark: operating mode cleared ({mode_path})")
         return 0
+    if args.compact_checkpoint:
+        from renmark import lifecycle as _lifecycle
+        _lifecycle.persist_compact_checkpoint(repo, skill="manual", reason="compact")
+        print("Compact checkpoint written to .renmark/state/compact_checkpoint.json.")
+        print("Run these commands to safely reduce context:")
+        print("  1. /compact")
+        print("  2. /renmark:resume")
+        return 0
+    if args.set_compact_gate_tokens is not None:
+        from renmark import config as _config
+        _config.set_compact_gate_tokens(repo, args.set_compact_gate_tokens)
+        print(f"compact_gate_tokens set to {args.set_compact_gate_tokens}")
+        return 0
     if args.task_brief:
         plan_path, task_index_str = args.task_brief
         try:
             task_index = int(task_index_str)
         except ValueError:
             ap.error(f"--task-brief TASK_INDEX must be an integer, got {task_index_str!r}")
         return cmd_task_brief(plan_path, task_index, repo=repo)
     if args.review_package:
         base_ref, head_ref = args.review_package
         return cmd_review_package(base_ref, head_ref, repo=repo)
 
     if not args.plan:
         ap.error(
             "plan path is required unless --usage / --analytics / --roadmap / --logs / "
             "--scan / --behavior / --task / --task-brief / --review-package / "
             "--set-proactive / --set-headless / --set-mode / --get-mode / --clear-mode"
         )
     return execute_plan(
         args.plan,
         repo=repo,
         resume=args.resume,
         dry_run=args.dry_run,
         no_commit=args.no_commit,
     )
 
 
 if __name__ == "__main__":
     sys.exit(main())
diff --git a/renmark/config.py b/renmark/config.py
index 6837766..0d5b53e 100644
--- a/renmark/config.py
+++ b/renmark/config.py
@@ -156,74 +156,105 @@ def _env_eval_runner_cmd() -> str | None:
     Returns the stripped value when the variable is set and non-blank, or
     ``None`` when it is unset or holds only whitespace (so the caller falls
     through to the config file).  Never raises.
     """
     raw = os.environ.get(_ENV_EVAL_RUNNER_CMD)
     if raw is None:
         return None
     norm = raw.strip()
     if norm:
         return norm
     return None
 
 
 def _config_eval_runner_cmd(repo: str | Path) -> str | None:
     """Return the ``.renmark/config.json`` ``eval_runner_cmd`` value, normalized.
 
     Mirrors :func:`_env_eval_runner_cmd`: returns the stripped value only when
     the key is present, a real ``str``, and non-blank; a non-str or
     whitespace-only value is treated as unset (``None``) so the caller degrades
     to ``LiveRunnerUnavailable`` rather than a spurious empty-command error.
     Never raises.
     """
     val = _read_raw(repo).get("eval_runner_cmd")
     if isinstance(val, str):
         norm = val.strip()
         if norm:
             return norm
     return None
 
 
 def eval_runner_cmd(repo: str | Path) -> str | None:
     """Return the command used to launch the eval-tier runner for *repo*.
 
     Precedence:
     1. env ``RENMARK_EVAL_RUNNER_CMD`` if set and non-blank (whitespace is
        stripped; a blank value is treated as unset and falls through);
     2. else the ``.renmark/config.json`` key ``"eval_runner_cmd"`` if present,
        a real ``str``, **and non-blank** (whitespace is stripped; a non-str or
        blank value is ignored, not coerced);
     3. else ``None``.
 
     Never raises — any OS or parse error degrades to ``None``.
     """
     env = _env_eval_runner_cmd()
     if env is not None:
         return env
     return _config_eval_runner_cmd(repo)
 
 
 def set_eval_runner_cmd(repo: str | Path, value: str) -> None:
     """Persist the eval-runner command for the project at *repo*.
 
     Reads the existing config, updates only the ``eval_runner_cmd`` key, and
     writes it back so unrelated keys are preserved.  Never raises.
     """
     data = _read_raw(repo)
     data["eval_runner_cmd"] = value
     _write_raw(repo, data)
 
 
+def compact_gate_tokens(repo: str | Path) -> int:
+    """Return the configured compact-gate token threshold for *repo*.
+
+    The compact gate fires when the orchestrator's context reaches this many
+    tokens — a proxy for 60% of the 200k window since real % is not detectable
+    from inside a skill. Default: 120_000. A value of 0 means disabled (gate
+    never fires). Never raises.
+    """
+    data = _read_raw(repo)
+    val = data.get("compact_gate_tokens")
+    if not isinstance(val, int) or isinstance(val, bool):
+        return 120_000
+    if val < 0:
+        return 120_000
+    return val
+
+
+def set_compact_gate_tokens(repo: str | Path, value: int) -> None:
+    """Persist the compact-gate token threshold for the project at *repo*.
+
+    Reads the existing config, updates only the ``compact_gate_tokens`` key,
+    and writes it back so unrelated keys are preserved. A value of 0 disables
+    the gate. Negative values are rejected (no-op). Never raises.
+    """
+    if value < 0:
+        return
+    data = _read_raw(repo)
+    data["compact_gate_tokens"] = value
+    _write_raw(repo, data)
+
+
 def eval_runner_source(repo: str | Path) -> str:
     """Return which layer decided :func:`eval_runner_cmd` for *repo*.
 
     ``"env"`` if ``RENMARK_EVAL_RUNNER_CMD`` resolved the value, ``"config"``
     if the config file's ``"eval_runner_cmd"`` key did (i.e. it is a real
     ``str``), else ``"default"``.  Uses the same precedence as
     :func:`eval_runner_cmd` so the two never disagree — a non-str config value
     reports ``"default"``.  Never raises.
     """
     if _env_eval_runner_cmd() is not None:
         return "env"
     if _config_eval_runner_cmd(repo) is not None:
         return "config"
     return "default"
diff --git a/renmark/lifecycle.py b/renmark/lifecycle.py
index 03afc6f..4d845f1 100644
--- a/renmark/lifecycle.py
+++ b/renmark/lifecycle.py
@@ -542,202 +542,257 @@ def _gates_not_run(repo: Path | str) -> list[str]:
         head = git_head_sha(repo)
         if not head:
             return []
         reviews = Path(repo) / ".renmark" / "reviews"
         if not reviews.is_dir():
             return ["qa", "codereview"]
 
         # (glob, required generator) per gate. The generator constraint mirrors
         # handoff-menu rule 2: a gate counts as "run" only when the artifact was
         # produced by the gate's own generator — a stray/foreign .qa.md must not
         # unlock Deep QA. generator=None means "no generator constraint".
         gate_specs: dict[str, tuple[str, str | None]] = {
             "qa": ("*.qa.md", "verify-qa"),
             "codereview": ("*.review.md", None),
         }
         not_run: list[str] = []
         for gate, (pattern, want_gen) in gate_specs.items():
             ran = False
             for artifact in reviews.glob(pattern):
                 meta = read_metadata(artifact)
                 if (
                     meta.get("source_sha") == head
                     and meta.get("completion_state") == "complete"
                     and (want_gen is None or meta.get("generator") == want_gen)
                 ):
                     ran = True
                     break
             if not ran:
                 not_run.append(gate)
         return not_run
     except Exception:
         return []
 
 
 def domain_of(skill: str) -> str:
     """Return the domain bucket for a skill name (G4 contamination detection).
 
     Resolves via the `skillmeta` registry (single source of truth, P7);
     unknown skills default to ``"build"``. `DOMAIN_BY_SKILL` remains a derived
     back-compat alias, but is consulted first so a test `monkeypatch` of that
     attribute still takes effect.
     """
     if skill in DOMAIN_BY_SKILL:
         return DOMAIN_BY_SKILL[skill]
     meta = skillmeta.get(skill)
     return meta.domain if meta is not None else "build"
 
 
 # Back-compat alias: `domain_for_skill` is the registry-facing name.
 domain_for_skill = domain_of
 
 
 def preamble_tier(skill: str) -> str:
     """Return the preamble tier for a skill ('minimal' | 'standard' | 'full').
 
     Unknown skills default to 'full'. Never raises.
     """
     return PREAMBLE_TIER_BY_SKILL.get(skill, "full")
 
 
+def persist_compact_checkpoint(
+    repo: "Path | str", skill: str, reason: str
+) -> None:
+    """Write a compact checkpoint to .renmark/state/compact_checkpoint.json.
+
+    Called by skill_preamble before emitting a context gate message so the
+    user can resume after running /compact or /clear.  Never raises.
+    """
+    from . import state as _state  # lazy — avoid circular import at module load
+    import json as _json
+
+    try:
+        state_path = _state.state_dir(repo) / "compact_checkpoint.json"
+        state_path.parent.mkdir(parents=True, exist_ok=True)
+        state_path.write_text(
+            _json.dumps(
+                {
+                    "skill": skill,
+                    "reason": reason,
+                    "resume_cmd": "/renmark:resume",
+                    "timestamp": _state.now_iso(),
+                },
+                indent=2,
+            ),
+            encoding="utf-8",
+        )
+    except Exception:
+        pass
+
+
 def skill_preamble(repo: Path | str, skill: str) -> str | None:
     """Single-call Step-0 boilerplate for every SKILL.md.
 
     Performs the calls every skill used to inline by hand, gated by tier:
         - ALL tiers: record invocation (load-bearing cross-domain detection for
           the NEXT skill — runs unconditionally so minimal-tier skills still
           register their domain in last-skill state).
         - minimal: returns None immediately; no budget check, no fragments.
         - standard: cross-domain/compact hint only; no fable synthesis hint.
         - full (default): cross-domain/compact hint + fable synthesis hint.
 
     Cross-domain detection for the CURRENT skill requires context_budget_check
     to read last-skill state BEFORE record_skill_invocation overwrites it, so
     for standard/full tier the budget check runs first. record_skill_invocation
     is then called unconditionally to keep the state file current for the next
     skill — this is the load-bearing ordering invariant.
 
     Returns the hint string the skill should surface to the user, or None when
     no hint is needed. Domain is resolved from `DOMAIN_BY_SKILL` — callers do
     not need to pass it, so the per-skill prose can't drift.
     """
     # Imported lazily to avoid a state ↔ lifecycle circular import at module load.
     from . import state as _state
 
     domain = domain_of(skill)
     tier = preamble_tier(skill)
 
     if tier == "minimal":
         # INVARIANT: record_skill_invocation runs for ALL tiers so that the next
         # skill can detect cross-domain transitions even when this one is minimal.
         _state.record_skill_invocation(repo, skill, domain)
         return _with_agency_note(repo, skill, _with_headless_note(repo, None))
 
     # For standard/full: budget check MUST read last-skill state before
     # record_skill_invocation overwrites it — ordering is load-bearing.
+    # Read last-skill state once here so prev_domain is available for the gate message.
+    last = _state.last_skill_invocation(repo)
     verdict = _state.context_budget_check(repo, skill, domain)
     _state.record_skill_invocation(repo, skill, domain)
 
     fragments: list[str] = []
     if verdict == "clear":
-        fragments.append(
-            f"context: cross-domain transition into `{domain}` — consider `/clear` "
-            "before continuing (`.renmark/memory/` survives clears)"
-        )
+        if skill in _CONTEXT_BYPASS_SKILLS:
+            # Advisory only — finish/approve/resume must not be blocked mid-flow.
+            fragments.append(
+                f"context: cross-domain transition into `{domain}` — consider `/clear` "
+                "before continuing (`.renmark/memory/` survives clears)"
+            )
+        else:
+            prev_domain = last.get("domain", "?") if last else "?"
+            persist_compact_checkpoint(repo, skill, reason="clear")
+            return (
+                f"CONTEXT_GATE_CLEAR: cross-domain transition detected "
+                f"(prev: `{prev_domain}` \u2192 `{domain}`).\n"
+                "State persisted to .renmark/state/compact_checkpoint.json.\n"
+                "Present the user with AskUserQuestion before proceeding with this skill:\n"
+                '  header: "Context hygiene"\n'
+                '  question: "Domain change detected. Run /clear to start fresh '
+                '(memory survives), or continue with accumulated context?"\n'
+                "  options:\n"
+                "    1. Stop here \u2014 I will run /clear then /renmark:resume (Recommended)\n"
+                "    2. Continue in same context (this step only)\n"
+                "    3. Queue this as next task after current work finishes\n"
+                "    4. Cancel"
+            )
     elif verdict == "compact":
         fragments.append("context: approaching budget — consider `/compact` before continuing")
 
     if tier == "full" and skill in SYNTHESIS_SKILLS:
         # Imported lazily to keep capability resolution off the module-load path.
         from . import capabilities as _capabilities
 
         if _capabilities.top_tier(Path(repo)) == "fable":
             fragments.append(
                 "declared top tier: fable — for best ideation/strategy results "
                 "run this session on Fable 5 (/model fable)"
             )
 
     base = " | ".join(fragments) if fragments else None
     return _with_agency_note(repo, skill, _with_mode_note(repo, skill, _with_headless_note(repo, base)))
 
 
 # ── Operating-mode directive (Conductor vs Orchestrator) ──────────────────────
 #
 # Set-mode directive lines (persisted mode → one hint line). Kept as module
 # constants so the behavior test (T14) and unit test (T12) can assert the exact
 # text without duplicating string literals.
 _MODE_DIRECTIVE: dict[str, str] = {
     "conductor": (
         "Operating mode: Conductor — hands-on; prefer single-file scoped edits, "
         "avoid subagents unless necessary, explain the next move before editing."
     ),
     "orchestrator": (
         "Operating mode: Orchestrator — goal-level; use narrow scoped subagents "
         "where useful, load skills on demand, review outcomes not keystrokes."
     ),
 }
 
 # Entry-point skills that must PROMPT for a mode when none is set yet.
 _MODE_ENTRY_SKILLS: frozenset[str] = frozenset(
     {"start", "feature", "debug", "roadmap", "finish", "orchestrate"}
 )
 
+# Skills whose flows must not be blocked mid-stream by context gates.
+# finish/approve/resume: urgent paths that must not be interrupted halfway.
+_CONTEXT_BYPASS_SKILLS: frozenset[str] = frozenset({"finish", "approve", "resume"})
+
 
 def _choose_mode_hint(skill: str) -> str:
     """Choose-mode instruction emitted for an entry-point skill with no mode set.
 
     Tells the orchestrator to ask the user Conductor vs Orchestrator via
     AskUserQuestion, recommending the per-skill default, then persist the choice.
     """
     from . import mode as _mode
 
     recommended = _mode.default_mode_for_skill(skill)
     return (
         "Operating mode: not yet set — ask the user Conductor vs Orchestrator via "
         f"AskUserQuestion (recommend: {recommended}), then persist with "
         "renmark.mode.set_mode(repo, <choice>)."
     )
 
 
 def _with_mode_note(repo: Path | str, skill: str, hint: str | None) -> str | None:
     """ADDITIVE: append the operating-mode directive to ``hint``.
 
     Runs strictly AFTER the existing tier logic and the headless note — never
     reorders the record-before-check invariant. DEGRADES GRACEFULLY: any
     exception in mode resolution falls back to ``hint`` unchanged, so mode is a
     pure enhancement and never a hard dependency of the preamble.
 
     - Mode SET  → append the Conductor/Orchestrator directive line.
     - Mode UNSET + entry-point skill → append a choose-mode instruction.
     - Mode UNSET + non-entry skill → no mode line (returns ``hint`` unchanged).
     """
     try:
         from . import mode as _mode
 
         current = _mode.read_mode(repo)
         if current is not None:
             line = _MODE_DIRECTIVE.get(current)
             if line is None:  # unrecognised (read_mode shouldn't yield this)
                 return hint
         elif skill in _MODE_ENTRY_SKILLS:
             line = _choose_mode_hint(skill)
         else:
             return hint
     except Exception:
         return hint
     return line if hint is None else f"{hint} | {line}"
 
 
 # Pipeline skills that receive an Agency Mode hint when agency is active.
 # v0.30.0 shipped the spine (start/prd/roadmap/finish/resume); the fast-follow
 # extends coverage to the remaining pipelines (feature/plan/orchestrate/verify/
 # codereview) so the whole delivery loop is agency-aware.
 _AGENCY_AWARE_SKILLS: frozenset[str] = frozenset(
     {
         "start", "prd", "roadmap", "finish", "resume",
         "feature", "plan", "orchestrate", "verify", "codereview",
     }
 )
 
 # Back-compat alias: the original spine-only name still resolves.
 _AGENCY_SPINE_SKILLS: frozenset[str] = _AGENCY_AWARE_SKILLS
 
diff --git a/tests/test_config.py b/tests/test_config.py
index 97832fb..420fbd2 100644
--- a/tests/test_config.py
+++ b/tests/test_config.py
@@ -162,60 +162,101 @@ def test_env_unrecognized_falls_through_to_config(
 def test_env_unrecognized_falls_through_to_default(
     tmp_path: Path, monkeypatch: pytest.MonkeyPatch
 ) -> None:
     """Unparseable env value with no config → default False."""
     monkeypatch.setenv("RENMARK_HEADLESS", "maybe")
     assert cfg.is_headless(tmp_path) is False
     assert cfg.headless_source(tmp_path) == "default"
 
 
 # ── P10: headless mode — config layer (env unset) ────────────────────────────
 
 
 def test_config_flag_honored_when_env_unset(
     tmp_path: Path, monkeypatch: pytest.MonkeyPatch
 ) -> None:
     """With env unset, the config flag decides; source == 'config'."""
     monkeypatch.delenv("RENMARK_HEADLESS", raising=False)
     cfg.set_headless(tmp_path, True)
     assert cfg.is_headless(tmp_path) is True
     assert cfg.headless_source(tmp_path) == "config"
 
 
 def test_config_false_honored_when_env_unset(
     tmp_path: Path, monkeypatch: pytest.MonkeyPatch
 ) -> None:
     """An explicit config False (env unset) → False, source == 'config'."""
     monkeypatch.delenv("RENMARK_HEADLESS", raising=False)
     cfg.set_headless(tmp_path, False)
     assert cfg.is_headless(tmp_path) is False
     assert cfg.headless_source(tmp_path) == "config"
 
 
 # ── P10: headless mode — default layer ───────────────────────────────────────
 
 
 def test_default_false_when_neither_env_nor_config(
     tmp_path: Path, monkeypatch: pytest.MonkeyPatch
 ) -> None:
     """No env, no config → is_headless False; source == 'default'."""
     monkeypatch.delenv("RENMARK_HEADLESS", raising=False)
     assert cfg.is_headless(tmp_path) is False
     assert cfg.headless_source(tmp_path) == "default"
 
 
 # ── P10: set_headless round-trip & key preservation ──────────────────────────
 
 
 def test_set_headless_preserves_other_keys(
     tmp_path: Path, monkeypatch: pytest.MonkeyPatch
 ) -> None:
     """set_headless must not clobber an existing 'proactive' key."""
     monkeypatch.delenv("RENMARK_HEADLESS", raising=False)
     cfg.set_proactive(tmp_path, False)
     cfg.set_headless(tmp_path, True)
     p = tmp_path / ".renmark" / "config.json"
     data = json.loads(p.read_text(encoding="utf-8"))
     assert data["proactive"] is False
     assert data["headless"] is True
     assert cfg.is_proactive(tmp_path) is False
     assert cfg.is_headless(tmp_path) is True
+
+
+# ── compact_gate_tokens ───────────────────────────────────────────────────────
+
+
+def test_compact_gate_tokens_default(tmp_path: Path) -> None:
+    from renmark.config import compact_gate_tokens
+    assert compact_gate_tokens(tmp_path) == 120_000
+
+
+def test_compact_gate_tokens_set_and_get(tmp_path: Path) -> None:
+    from renmark.config import compact_gate_tokens, set_compact_gate_tokens
+    set_compact_gate_tokens(tmp_path, 80_000)
+    assert compact_gate_tokens(tmp_path) == 80_000
+
+
+def test_compact_gate_tokens_zero_is_valid(tmp_path: Path) -> None:
+    """0 means disabled — it must be stored and returned as 0, not fall back to default."""
+    from renmark.config import compact_gate_tokens, set_compact_gate_tokens
+    set_compact_gate_tokens(tmp_path, 0)
+    assert compact_gate_tokens(tmp_path) == 0
+
+
+def test_compact_gate_tokens_negative_value_returns_default(tmp_path: Path) -> None:
+    """Negative value stored in config.json returns default 120_000."""
+    import json
+    cfg_file = tmp_path / ".renmark" / "config.json"
+    cfg_file.parent.mkdir(parents=True, exist_ok=True)
+    cfg_file.write_text(json.dumps({"compact_gate_tokens": -5}))
+    from renmark.config import compact_gate_tokens
+    assert compact_gate_tokens(tmp_path) == 120_000
+
+
+def test_compact_gate_tokens_non_int_returns_default(tmp_path: Path) -> None:
+    """Non-int config value returns default 120_000."""
+    import json
+    cfg_file = tmp_path / ".renmark" / "config.json"
+    cfg_file.parent.mkdir(parents=True, exist_ok=True)
+    cfg_file.write_text(json.dumps({"compact_gate_tokens": "not-an-int"}))
+    from renmark.config import compact_gate_tokens
+    assert compact_gate_tokens(tmp_path) == 120_000
diff --git a/tests/test_lifecycle.py b/tests/test_lifecycle.py
index aa99ada..2351979 100644
--- a/tests/test_lifecycle.py
+++ b/tests/test_lifecycle.py
@@ -267,124 +267,125 @@ def test_cross_domain_transition() -> None:
 
 
 def _write_declared_fable_routing(repo: Path) -> None:
     routing = repo / ".renmark" / "memory" / "routing.md"
     routing.parent.mkdir(parents=True, exist_ok=True)
     routing.write_text("## Model tiers\n\ntop_tier: fable\n")
 
 
 def test_skill_preamble_declared_repo_brainstorm_adds_tier_hint(
     tmp_path: Path, monkeypatch: pytest.MonkeyPatch
 ) -> None:
     monkeypatch.delenv("RENMARK_TOP_TIER", raising=False)
     _write_declared_fable_routing(tmp_path)
 
     hint = lifecycle.skill_preamble(tmp_path, "brainstorm")
 
     assert hint is not None
     assert "declared top tier: fable" in hint
 
 
 def test_skill_preamble_declared_repo_verify_omits_tier_hint(tmp_path: Path, monkeypatch: pytest.MonkeyPatch) -> None:
     monkeypatch.delenv("RENMARK_TOP_TIER", raising=False)
     _write_declared_fable_routing(tmp_path)
 
     hint = lifecycle.skill_preamble(tmp_path, "verify")
 
     assert "declared top tier: fable" not in (hint or "")
 
 
 def test_skill_preamble_undeclared_repo_brainstorm_omits_tier_hint(
     tmp_path: Path, monkeypatch: pytest.MonkeyPatch
 ) -> None:
     monkeypatch.delenv("RENMARK_TOP_TIER", raising=False)
 
     hint = lifecycle.skill_preamble(tmp_path, "brainstorm")
 
     assert "declared top tier: fable" not in (hint or "")
 
 
 def test_preamble_hint_fires_on_env_declaration(tmp_path: Path, monkeypatch: pytest.MonkeyPatch) -> None:
     """BY DESIGN (PRD REQ-2): `RENMARK_TOP_TIER` is a legitimate PER-USER declaration
     form — "a committed `## Model tiers` block…, per-user overridable with
     `RENMARK_TOP_TIER`". An undeclared repo (no routing.md) + RENMARK_TOP_TIER=fable
     IS declared, so the tier hint must fire; this is not a bypass."""
     monkeypatch.setenv("RENMARK_TOP_TIER", "fable")
     # No _write_declared_fable_routing() call — the repo itself stays undeclared.
 
     hint = lifecycle.skill_preamble(tmp_path, "brainstorm")
 
     assert hint is not None
     assert "declared top tier: fable" in hint
 
 
 def test_skill_preamble_cross_domain_and_tier_hints_joined(tmp_path: Path, monkeypatch: pytest.MonkeyPatch) -> None:
     monkeypatch.delenv("RENMARK_TOP_TIER", raising=False)
     _write_declared_fable_routing(tmp_path)
     lifecycle.skill_preamble(tmp_path, "debug")
 
     hint = lifecycle.skill_preamble(tmp_path, "brainstorm")
 
+    # Cross-domain gate takes priority — returns CONTEXT_GATE_CLEAR early; tier
+    # hint is not appended when the gate fires (gate short-circuits the join).
     assert hint is not None
-    assert "context: cross-domain transition" in hint
-    assert "declared top tier: fable" in hint
-    assert " | " in hint
+    assert hint.startswith("CONTEXT_GATE_CLEAR:")
+    assert "cross-domain transition" in hint
 
 
 def test_corrupt_lifecycle_returns_none(tmp_path: Path) -> None:
     state_dir = tmp_path / ".renmark" / "state"
     state_dir.mkdir(parents=True)
     (state_dir / "lifecycle.json").write_text("not json {{{")
     assert lifecycle.read_lifecycle(tmp_path) is None
 
 
 def test_non_dict_lifecycle_returns_none(tmp_path: Path) -> None:
     """Valid JSON whose top level is not an object must degrade to None,
     never raise — a corrupt file must not kill cold-start recovery."""
     state_dir = tmp_path / ".renmark" / "state"
     state_dir.mkdir(parents=True)
     for payload in ("[]", '"x"', "42"):
         (state_dir / "lifecycle.json").write_text(payload)
         assert lifecycle.read_lifecycle(tmp_path) is None
 
 
 def test_wrong_typed_fields_dropped(tmp_path: Path) -> None:
     """Wrong-typed values degrade to dataclass defaults instead of raising."""
     state_dir = tmp_path / ".renmark" / "state"
     state_dir.mkdir(parents=True)
     (state_dir / "lifecycle.json").write_text(json.dumps({"stage": ["a"], "artifacts": ["a"], "feature": 7}))
     state = lifecycle.read_lifecycle(tmp_path)
     assert state is not None
     assert state.stage == "init"
     assert state.artifacts == {}
     assert state.feature == ""
 
 
 def test_non_dict_lifecycle_does_not_break_writes(tmp_path: Path) -> None:
     """write_lifecycle resets from a corrupt file instead of raising."""
     state_dir = tmp_path / ".renmark" / "state"
     state_dir.mkdir(parents=True)
     (state_dir / "lifecycle.json").write_text("[]")
     state = lifecycle.write_lifecycle(tmp_path, stage="brainstorm-complete")
     assert state.stage == "brainstorm-complete"
 
 
 def test_field_type_map_matches_dataclass() -> None:
     """Drift guard: the read-time type filter must cover every dataclass field."""
     assert set(lifecycle._LIFECYCLE_FIELD_TYPES) == set(lifecycle.LifecycleState.__dataclass_fields__)
 
 
 def test_validate_artifact_refs_survives_corrupt_state(tmp_path: Path) -> None:
     state_dir = tmp_path / ".renmark" / "state"
     state_dir.mkdir(parents=True)
     (state_dir / "lifecycle.json").write_text('{"stage": "init", "artifacts": ["a"]}')
     assert lifecycle.validate_artifact_refs(tmp_path) == []
 
 
 def test_unknown_fields_in_lifecycle_tolerated(tmp_path: Path) -> None:
     """Forward-compat: extra fields shouldn't crash the loader."""
     state_dir = tmp_path / ".renmark" / "state"
     state_dir.mkdir(parents=True)
     (state_dir / "lifecycle.json").write_text(
         json.dumps(
             {
                 "feature": "x",

[35m[3mexec[0m[0m
[1m/bin/bash -lc "python3 - <<'PY'
from pathlib import Path
p=Path('/home/renmark/projects/ai-system/CHANGELOG.md')
text=p.read_text(encoding='utf-8').splitlines()
# print top 120 lines for recent entries
for i,l in enumerate(text[:120],1):
    print(f'{i}: {l}')
PY"[0m in /home/renmark/projects/ai-system
[32m succeeded in 0ms:[0m
1: # Changelog
2: 
3: ## [2026-07-06] — Context hygiene gates (selectable AskUserQuestion menus)
4: 
5: **Request:** Add hard-stop context hygiene gates to renmark — a blocking AskUserQuestion menu at cross-domain transitions (clear gate) and at ≥60% context (compact gate) — without claiming to invoke /compact or /clear programmatically.
6: 
7: **Built:**
8: - `renmark/config.py`: `compact_gate_tokens(repo) -> int` + `set_compact_gate_tokens(repo, value)` — configurable threshold (default 120k, 0=disabled)
9: - `renmark/lifecycle.py`: `persist_compact_checkpoint(repo, skill, reason)` — writes `.renmark/state/compact_checkpoint.json`; `_CONTEXT_BYPASS_SKILLS = {"finish", "approve", "resume"}`; `skill_preamble` clear-verdict upgraded from advisory "consider /clear" to `CONTEXT_GATE_CLEAR:` prefixed hard-stop with full AskUserQuestion menu spec (bypass skills stay advisory)
10: - `renmark/cli/_engine.py`: `--compact-checkpoint` flag (calls persist_compact_checkpoint + prints /compact+/renmark:resume instructions); `--set-compact-gate-tokens TOKENS` (configures threshold)
11: - `CLAUDE.md` + `AGENTS.md`: Context budget rule block replaced with selectable hygiene gates spec (AskUserQuestion menus, CONTEXT_GATE_CLEAR: sentinel, compact-checkpoint CLI, bypass skill list, configurable threshold note)
12: - `plugin/skills/_shared/context-taxonomy.md`: Added "Context hygiene gates" section documenting both gate types
13: - Tests: 9 new tests (5 config + 4 lifecycle); 1359 total, all pass
14: 
15: **Files changed:** renmark/config.py, renmark/lifecycle.py, renmark/cli/_engine.py, CLAUDE.md, AGENTS.md, plugin/skills/_shared/context-taxonomy.md, tests/test_config.py, tests/test_lifecycle.py
16: 
17: **Do not change:**
18: - `_CONTEXT_BYPASS_SKILLS` must include finish/approve/resume — these flows must never be blocked mid-stream
19: - `compact_gate_tokens` must return 0 when 0 is stored (0 = disabled, not default)
20: - `CONTEXT_GATE_CLEAR:` prefix is load-bearing — CLAUDE.md rule pattern-matches it to trigger AskUserQuestion
21: - `persist_compact_checkpoint` is exported (no underscore) — tests and CLI import it directly
22: 
23: ## [2026-07-06] — Claude-native subagent definitions + wave-level Workflow fan-out
24: **Request:** Upgrade renmark to use Claude Code's native `.claude/agents/*.md` subagent-type
25: files for the 8 specialized dispatch roles and add an optional wave-level `Workflow`-tool
26: fan-out path to `/renmark:orchestrate` — without migrating to Agent Teams or touching the
27: deterministic Python policy layer.
28: **Built:**
29: - `.claude/agents/{docs-editor,code-implementer,test-writer,reviewer,release-manager,researcher,audit-reader,finish-lane-specialist}.md` — 8 native Claude Code subagent-type files with enforced tool allowlists and model tiers.
30: - `renmark/subagent_profiles.py::has_native_agent_file` — pure helper (static frozenset + optional on-disk check, never raises) used by orchestrate to pass `subagent_type: <role>` on Agent calls.
31: - `renmark/dispatch.py::build_workflow_fanout_args` — packet-shaping helper that builds Workflow `args` payloads (pre-resolves `agent_type` per task via `has_native_agent_file`), skipping codex tasks.
32: - `plugin/skills/_shared/workflow-fanout.md` — contract doc for wave-level fan-out (when to use, what stays Python, G11 validation ownership, cost/ledger note).
33: - `plugin/skills/orchestrate/SKILL.md` Step 3b — updated with `subagent_type` dispatch and Workflow fan-out branch for waves with >1 needs_agent task.
34: - `plugin/skills/_shared/subagent-profiles.md` — added "Native agent file" column; fixed pre-existing model-tier drift (release-manager/researcher/finish-lane-specialist were stale Haiku).
35: - 3 codex-review fixes: `agent_type` field in fanout payloads, `docs-editor.md` stop condition, `researcher.md` `Write` tool.
36: **Files changed:** `.claude/agents/*.md` (8 new), `renmark/subagent_profiles.py`, `renmark/dispatch.py`, `plugin/skills/_shared/subagent-profiles.md`, `plugin/skills/_shared/workflow-fanout.md`, `plugin/skills/orchestrate/SKILL.md`, `tests/test_dispatch.py`, `tests/test_subagent_profiles.py`.
37: **Do not change:** Python policy layer stays the backbone — `dispatch.py` wave grouping/validation, `subagent_gate.py`, `cost.py`, G11 `SubagentInput`/`SubagentOutput`/`parse_subagent_response`. Do not broaden Workflow use to whole-plan execution (breaks G12 cross-`/clear` resumability).
38: 
39: ## [2026-07-02] — v0.32.0 — enforced subagent-justification gate
40: **Release.** Bumps v0.31.0 → v0.32.0. Ships the enforced subagent gate (strengthens
41: REQ-21) in response to a usage report (subagent-heavy, >150k-context sessions running
42: unchallenged). Of Codex's 6 cost-control recs, 4 were already shipped (context
43: thresholds, finish lanes, model routing, deterministic/model cost split); this releases
44: the 2 real gaps.
45: **Built:**
46: - `renmark/subagent_gate.py` — pure zero-LLM gate: `justify_task` (4-question verdict with
47:   structured `challenge_code`), `challenge_plan` (rollup), `preview_line`, and a
48:   `python -m renmark.subagent_gate <plan>` CLI (exit 0 clean / 1 challenged / 2 usage,
49:   mirroring `plan_lint`). Composes `cost`/`subagent_profiles`; never raises.
50: - Real enforcement: `renmark-execute --dry-run` runs the gate and prints the verdict; the
51:   orchestrate pre-flight surfaces it and requires acknowledgment on a challenged plan.
52: - General-purpose reduction: `parser.Task` gained `role`/`role_reason` so plans can justify
53:   a general-purpose spawn; unexplained general-purpose is challenged.
54: - Docs: `_shared/{deterministic-first,subagent-budget,cost-preview}.md` mark the gate enforced;
55:   CLAUDE.md/AGENTS.md deterministic-first rule names the helper.
56: - Codex-reviewed: 4 Major findings all fixed (wiring, parser role_reason, challenge_code, est_tokens).
57: **Files changed:** renmark/subagent_gate.py (new), tests/test_subagent_gate.py (new), renmark/parser.py, renmark/cli/_engine.py, orchestrate/SKILL.md, 3 _shared/*.md, CLAUDE.md, AGENTS.md, version files, CHANGELOG.md.
58: **Do not change:** gate is deterministic/zero-LLM, must never raise; composes cost/subagent_profiles (no reimplementation); general-purpose without role_reason is challenged. It surfaces+acks, does NOT hard-block dispatch (agent-driven). Codex recs #2/#3/#4/#6 already shipped — not re-touched.
59: 
60: ## [2026-07-02] — enforced subagent-justification gate (REQ-21 strengthening)
61: **Request:** Usage report showed subagent-heavy, >150k-context sessions running unchallenged (general-purpose subagents recurring). Turn the advisory deterministic-first / subagent gate into an ENFORCED pre-dispatch check.
62: **Built:**
63: - `renmark/subagent_gate.py` — pure, zero-LLM gate. `justify_task` answers REQ-21's 4 questions mechanically (deterministic path? scoped role? orchestrator-inline? large/ambiguous enough?); `challenge_plan` rolls a plan up (deterministic-eligible / inline-able / unexplained-general-purpose spawns flagged); `preview_line` for the cost preview; `main()` CLI (`python -m renmark.subagent_gate <plan>` → exit 0 clean / 1 challenged / 2 usage), mirroring `plan_lint`. Composes `cost.is_deterministic_item`/`_get` + `subagent_profiles.resolve_profile`/`profile_tier` — does not re-implement. Never raises.
64: - Wiring: orchestrate pre-flight runs the gate before dispatch and requires ack on a challenged plan; `_shared/{deterministic-first,subagent-budget,cost-preview}.md` document the gate as enforced (not advised); CLAUDE.md/AGENTS.md deterministic-first rule names the new helper.
65: - `tests/test_subagent_gate.py` — 11 tests: deterministic→no-subagent, simple/tiny→inline-flagged, hard-scoped→clean, general-purpose-without-reason→challenged (+reason clears it), plan-level challenge invariants, never-raises.
66: **Files changed:** renmark/subagent_gate.py (new), tests/test_subagent_gate.py (new), orchestrate/SKILL.md, 3 _shared/*.md, CLAUDE.md, AGENTS.md, CHANGELOG.md.
67: **Do not change:** the gate is deterministic/zero-LLM and MUST never raise (degrades to "subagent needed" + flag); it composes cost/subagent_profiles, never re-implements; general-purpose without a `role_reason` is challenged (Codex rec #5). Codex recs #2 (context thresholds), #3 (finish lanes), #4 (model routing), #6 (deterministic/model cost split) were already shipped — NOT re-touched.
68: 
69: ## [2026-07-02] — v0.31.0 — Agency Mode full pipeline coverage
70: **Release.** Bumps v0.30.0 → v0.31.0. Releases the Agency Mode fast-follow: agency-awareness
71: now spans ALL 10 pipeline skills (the v0.30.0 spine start/prd/roadmap/finish/resume PLUS
72: feature/plan/orchestrate/verify/codereview). Completes REQ-22 pipeline coverage.
73: **Request:** Extend Agency Mode to the 5 deferred pipelines and release it.
74: **Built:**
75: - `renmark/lifecycle.py` — `_AGENCY_SPINE_SKILLS` → `_AGENCY_AWARE_SKILLS` (all 10 pipelines;
76:   back-compat alias retained); `_with_agency_note` surfaces the hint for the newly-covered pipelines.
77: - Agency blocks in `plugin/skills/{feature,plan,orchestrate,verify,codereview}/SKILL.md`
78:   (pointer to `_shared/agency-delivery.md` via `${CLAUDE_PLUGIN_ROOT}`, never inline).
79: - `tests/test_agency_behavior.py` — iterates the full agency-aware set (asserts all 10 gain the
80:   hint when active; non-aware skills stay clean; inactive path byte-identical).
81: - Codex-reviewed (fixed test-coverage gap + docstring); portability bug caught (absolute path → ${CLAUDE_PLUGIN_ROOT}).
82: **Files changed:** renmark/lifecycle.py, 5 SKILL.md, tests/test_agency_behavior.py, version files, CHANGELOG.md.
83: **Do not change:** agency hint only for `_AGENCY_AWARE_SKILLS`; inactive path byte-identical;
84: bodies load on demand (pointer only); reuses cost-control/finish-lanes/deterministic-first infra.
85: 
86: ## [2026-07-02] — Agency Mode fast-follow (full pipeline coverage)
87: **Request:** Extend Agency Mode awareness to the 5 pipelines the v0.30.0 walking-skeleton MVP deferred (feature, plan, orchestrate, verify, codereview) so the whole delivery loop is agency-aware.
88: **Built:**
89: - `renmark/lifecycle.py` — `_AGENCY_SPINE_SKILLS` → `_AGENCY_AWARE_SKILLS` (now all 10 pipeline skills; back-compat alias kept). `_with_agency_note` surfaces the agency hint for the newly-covered pipelines when agency is active.
90: - Agency blocks added to `plugin/skills/{feature,plan,orchestrate,verify,codereview}/SKILL.md` — each referencing `_shared/agency-delivery.md` by pointer (never inline): feature=select-next-milestone + PRD-alignment + no drift; plan=atomic tasks + milestone acceptance criteria + cost preview; orchestrate=background agents + continue-until-checkpoint + progress summaries; verify=tests/browser + demo-readiness + unverified; codereview=full review before signoff + merge readiness.
91: - `tests/test_agency_behavior.py` — asserts all 10 pipeline skills gain the hint when active; non-aware skills (debug/audit) stay clean; inactive path still byte-identical.
92: **Files changed:** renmark/lifecycle.py, 5 SKILL.md, tests/test_agency_behavior.py, CHANGELOG.md.
93: **Do not change:** agency hint only for `_AGENCY_AWARE_SKILLS`; inactive path byte-identical; bodies load on demand (pointer only); reuses cost-control/finish-lanes/deterministic-first infra.
94: 
95: ## [2026-07-02] — v0.30.0 — Agency Mode (walking-skeleton MVP)
96: **Release.** Bumps v0.29.0 → v0.30.0. Ships **Agency Mode** (REQ-22) — an OPTIONAL
97: higher-level project-delivery workflow ABOVE Conductor/Orchestrator (does not replace
98: them), the third delivery modality. Walking-skeleton MVP over the core pipeline spine.
99: **Request:** Build Agency Mode — owner gives intent + signs off milestones while renmark
100: runs discovery → PRD → roadmap → milestones → build → demo → feedback → signoff → release.
101: **Built:**
102: - `renmark/agency.py` — lightweight resumable agency state (`.renmark/state/agency.json`):
103:   `AgencyState`, `read_agency`/`write_agency` (atomic via `tempfile.mkstemp`), `is_active`,
104:   `activate`/`deactivate`; `AGENCY_JSON_BYTE_BUDGET`=1KB + `AgencyBloatError` (mirrors lifecycle).
105: - `plugin/skills/_shared/agency-delivery.md` — shared agency delivery contract, loaded ON
106:   DEMAND only (registered in `context.FRAGMENT_NAMES`); never eager.
107: - `renmark/lifecycle.py` — mode-conditioned `skill_preamble`: when agency active, appends an
108:   agency hint + fragment POINTER (not body) for the 5 spine skills only; byte-identical when
109:   inactive (`_with_agency_note`, `_AGENCY_SPINE_SKILLS`, `_AGENCY_HINT_MARKER`).
110: - Agency blocks in the 5 spine skills (start/prd/roadmap/finish/resume); `/renmark:start` is
111:   the explicit opt-in entry (seeds agency.json via `agency.activate`). help + CLAUDE.md/AGENTS.md.
112: - Tests: `tests/test_agency.py` (state) + `tests/test_agency_behavior.py` (AC11 — proves agency
113:   changes behavior without loading skill bodies; inactive path byte-identical).
114: - Standalone lint hotfix: cleared pre-existing ruff debt in cost.py/subagent_profiles.py.
115: **Files changed:** renmark/agency.py (new), plugin/skills/_shared/agency-delivery.md (new),
116: renmark/context.py, renmark/lifecycle.py, 5 spine SKILL.md, help/SKILL.md, CLAUDE.md, AGENTS.md,
117: PRD.md (REQ-22), tests/test_agency*.py (new), cost.py, subagent_profiles.py, version files.
118: **Do not change:** Agency Mode must NOT replace Conductor/Orchestrator; explicit opt-in only
119: (no auto-detect); agency bodies load on demand (never eager); `cites` in skillmeta is a CLOSED
120: 3-block vocab (reasoning-contract/next-steps/handoff-menu) — spine source of truth is

[35m[3mcodex[0m[0m
I’ve confirmed the main logic changes. I’m now checking whether the new config/CLI wiring is actually consumed anywhere and whether the tests cover the required branches or just a subset.
[35m[3mexec[0m[0m
[1m/bin/bash -lc "nl -ba /home/renmark/projects/ai-system/renmark/config.py | sed -n '1,260p'"[0m in /home/renmark/projects/ai-system
[35m[3mexec[0m[0m
[1m/bin/bash -lc "nl -ba /home/renmark/projects/ai-system/renmark/lifecycle.py | sed -n '600,760p'"[0m in /home/renmark/projects/ai-system
[32m succeeded in 0ms:[0m
     1	"""Persisted project-level configuration for renmark (P11 — proactivity toggle).
     2	
     3	State lives in ``.renmark/config.json`` (committed, not gitignored — it is a
     4	durable user preference, not runtime state).  Absence of the file or absence
     5	of a key is always treated as the default value so that the repo behaves
     6	identically before and after the first ``--set-proactive`` call.
     7	
     8	Design constraints:
     9	- stdlib json only (no third-party deps).
    10	- All public functions are pure I/O wrappers that never raise — any OS or
    11	  parse error degrades silently to the documented default.
    12	- ``is_proactive`` default is ``True`` (= current behaviour; no-config == on).
    13	"""
    14	
    15	from __future__ import annotations
    16	
    17	import json
    18	import os
    19	from pathlib import Path
    20	
    21	_CONFIG_REL = ".renmark/config.json"
    22	
    23	_ENV_HEADLESS = "RENMARK_HEADLESS"
    24	_ENV_EVAL_RUNNER_CMD = "RENMARK_EVAL_RUNNER_CMD"
    25	_ENV_TRUE = {"1", "true", "yes", "on"}
    26	_ENV_FALSE = {"0", "false", "no", "off"}
    27	
    28	
    29	def _config_path(repo: str | Path) -> Path:
    30	    return Path(repo) / _CONFIG_REL
    31	
    32	
    33	def _read_raw(repo: str | Path) -> dict[str, object]:
    34	    """Return the parsed config dict, or {} on any error (missing / corrupt)."""
    35	    try:
    36	        text = _config_path(repo).read_text(encoding="utf-8")
    37	        data = json.loads(text)
    38	        if isinstance(data, dict):
    39	            return data
    40	    except Exception:
    41	        pass
    42	    return {}
    43	
    44	
    45	def _write_raw(repo: str | Path, data: dict[str, object]) -> None:
    46	    """Write the config dict atomically (best-effort — never raises)."""
    47	    try:
    48	        p = _config_path(repo)
    49	        p.parent.mkdir(parents=True, exist_ok=True)
    50	        p.write_text(json.dumps(data, indent=2) + "\n", encoding="utf-8")
    51	    except Exception:
    52	        pass
    53	
    54	
    55	# ── Public API ────────────────────────────────────────────────────────────────
    56	
    57	
    58	def is_proactive(repo: str | Path) -> bool:
    59	    """Return the persisted proactivity flag for the project at *repo*.
    60	
    61	    ``True`` (auto-route plain-English build/dev tasks through renmark) unless
    62	    the user has explicitly turned it off via ``--set-proactive false``.
    63	
    64	    Never raises — a missing or corrupt config file returns ``True`` so that
    65	    the repository behaves exactly as it did before this feature shipped.
    66	    """
    67	    data = _read_raw(repo)
    68	    val = data.get("proactive", True)
    69	    # Coerce to bool; treat any non-bool truthy/falsy value sensibly.
    70	    if isinstance(val, bool):
    71	        return val
    72	    return bool(val)
    73	
    74	
    75	def set_proactive(repo: str | Path, value: bool) -> None:
    76	    """Persist the proactivity flag for the project at *repo*.
    77	
    78	    Reads the existing config, updates only the ``proactive`` key, and writes
    79	    it back so unrelated keys are preserved.  Never raises.
    80	    """
    81	    data = _read_raw(repo)
    82	    data["proactive"] = value
    83	    _write_raw(repo, data)
    84	
    85	
    86	def _env_headless() -> bool | None:
    87	    """Parse ``RENMARK_HEADLESS`` (case-insensitive) into a tri-state.
    88	
    89	    Returns ``True``/``False`` for a recognised value, or ``None`` when the
    90	    variable is unset or holds any other value (so the caller falls through to
    91	    the config file).  Never raises.
    92	    """
    93	    raw = os.environ.get(_ENV_HEADLESS)
    94	    if raw is None:
    95	        return None
    96	    norm = raw.strip().lower()
    97	    if norm in _ENV_TRUE:
    98	        return True
    99	    if norm in _ENV_FALSE:
   100	        return False
   101	    return None
   102	
   103	
   104	def is_headless(repo: str | Path) -> bool:
   105	    """Return whether renmark should run in headless (non-interactive) mode.
   106	
   107	    Precedence:
   108	    1. env ``RENMARK_HEADLESS`` (``1/true/yes/on`` → ``True``,
   109	       ``0/false/no/off`` → ``False``; an explicit OFF overrides config);
   110	    2. else the ``.renmark/config.json`` key ``"headless"`` if present **and a
   111	       real ``bool``** (a non-bool value is ignored, not coerced — headless is
   112	       fail-dangerous, so anything but a true bool falls through);
   113	    3. else ``False``.
   114	
   115	    Never raises — any OS or parse error degrades to ``False``.
   116	    """
   117	    env = _env_headless()
   118	    if env is not None:
   119	        return env
   120	    val = _read_raw(repo).get("headless")
   121	    if isinstance(val, bool):
   122	        return val
   123	    return False
   124	
   125	
   126	def set_headless(repo: str | Path, value: bool) -> None:
   127	    """Persist the headless flag for the project at *repo*.
   128	
   129	    Reads the existing config, updates only the ``headless`` key, and writes
   130	    it back so unrelated keys are preserved.  Never raises.
   131	    """
   132	    data = _read_raw(repo)
   133	    data["headless"] = value
   134	    _write_raw(repo, data)
   135	
   136	
   137	def headless_source(repo: str | Path) -> str:
   138	    """Return which layer decided :func:`is_headless` for *repo*.
   139	
   140	    ``"env"`` if ``RENMARK_HEADLESS`` resolved the value, ``"config"`` if the
   141	    config file's ``"headless"`` key did (i.e. it is a real ``bool``), else
   142	    ``"default"``.  Uses the same precedence as :func:`is_headless` so the two
   143	    never disagree — a non-bool config value reports ``"default"``.  Never
   144	    raises.
   145	    """
   146	    if _env_headless() is not None:
   147	        return "env"
   148	    if isinstance(_read_raw(repo).get("headless"), bool):
   149	        return "config"
   150	    return "default"
   151	
   152	
   153	def _env_eval_runner_cmd() -> str | None:
   154	    """Parse ``RENMARK_EVAL_RUNNER_CMD`` into an optional command string.
   155	
   156	    Returns the stripped value when the variable is set and non-blank, or
   157	    ``None`` when it is unset or holds only whitespace (so the caller falls
   158	    through to the config file).  Never raises.
   159	    """
   160	    raw = os.environ.get(_ENV_EVAL_RUNNER_CMD)
   161	    if raw is None:
   162	        return None
   163	    norm = raw.strip()
   164	    if norm:
   165	        return norm
   166	    return None
   167	
   168	
   169	def _config_eval_runner_cmd(repo: str | Path) -> str | None:
   170	    """Return the ``.renmark/config.json`` ``eval_runner_cmd`` value, normalized.
   171	
   172	    Mirrors :func:`_env_eval_runner_cmd`: returns the stripped value only when
   173	    the key is present, a real ``str``, and non-blank; a non-str or
   174	    whitespace-only value is treated as unset (``None``) so the caller degrades
   175	    to ``LiveRunnerUnavailable`` rather than a spurious empty-command error.
   176	    Never raises.
   177	    """
   178	    val = _read_raw(repo).get("eval_runner_cmd")
   179	    if isinstance(val, str):
   180	        norm = val.strip()
   181	        if norm:
   182	            return norm
   183	    return None
   184	
   185	
   186	def eval_runner_cmd(repo: str | Path) -> str | None:
   187	    """Return the command used to launch the eval-tier runner for *repo*.
   188	
   189	    Precedence:
   190	    1. env ``RENMARK_EVAL_RUNNER_CMD`` if set and non-blank (whitespace is
   191	       stripped; a blank value is treated as unset and falls through);
   192	    2. else the ``.renmark/config.json`` key ``"eval_runner_cmd"`` if present,
   193	       a real ``str``, **and non-blank** (whitespace is stripped; a non-str or
   194	       blank value is ignored, not coerced);
   195	    3. else ``None``.
   196	
   197	    Never raises — any OS or parse error degrades to ``None``.
   198	    """
   199	    env = _env_eval_runner_cmd()
   200	    if env is not None:
   201	        return env
   202	    return _config_eval_runner_cmd(repo)
   203	
   204	
   205	def set_eval_runner_cmd(repo: str | Path, value: str) -> None:
   206	    """Persist the eval-runner command for the project at *repo*.
   207	
   208	    Reads the existing config, updates only the ``eval_runner_cmd`` key, and
   209	    writes it back so unrelated keys are preserved.  Never raises.
   210	    """
   211	    data = _read_raw(repo)
   212	    data["eval_runner_cmd"] = value
   213	    _write_raw(repo, data)
   214	
   215	
   216	def compact_gate_tokens(repo: str | Path) -> int:
   217	    """Return the configured compact-gate token threshold for *repo*.
   218	
   219	    The compact gate fires when the orchestrator's context reaches this many
   220	    tokens — a proxy for 60% of the 200k window since real % is not detectable
   221	    from inside a skill. Default: 120_000. A value of 0 means disabled (gate
   222	    never fires). Never raises.
   223	    """
   224	    data = _read_raw(repo)
   225	    val = data.get("compact_gate_tokens")
   226	    if not isinstance(val, int) or isinstance(val, bool):
   227	        return 120_000
   228	    if val < 0:
   229	        return 120_000
   230	    return val
   231	
   232	
   233	def set_compact_gate_tokens(repo: str | Path, value: int) -> None:
   234	    """Persist the compact-gate token threshold for the project at *repo*.
   235	
   236	    Reads the existing config, updates only the ``compact_gate_tokens`` key,
   237	    and writes it back so unrelated keys are preserved. A value of 0 disables
   238	    the gate. Negative values are rejected (no-op). Never raises.
   239	    """
   240	    if value < 0:
   241	        return
   242	    data = _read_raw(repo)
   243	    data["compact_gate_tokens"] = value
   244	    _write_raw(repo, data)
   245	
   246	
   247	def eval_runner_source(repo: str | Path) -> str:
   248	    """Return which layer decided :func:`eval_runner_cmd` for *repo*.
   249	
   250	    ``"env"`` if ``RENMARK_EVAL_RUNNER_CMD`` resolved the value, ``"config"``
   251	    if the config file's ``"eval_runner_cmd"`` key did (i.e. it is a real
   252	    ``str``), else ``"default"``.  Uses the same precedence as
   253	    :func:`eval_runner_cmd` so the two never disagree — a non-str config value
   254	    reports ``"default"``.  Never raises.
   255	    """
   256	    if _env_eval_runner_cmd() is not None:
   257	        return "env"
   258	    if _config_eval_runner_cmd(repo) is not None:
   259	        return "config"
   260	    return "default"

[32m succeeded in 0ms:[0m
   600	
   601	
   602	def persist_compact_checkpoint(
   603	    repo: "Path | str", skill: str, reason: str
   604	) -> None:
   605	    """Write a compact checkpoint to .renmark/state/compact_checkpoint.json.
   606	
   607	    Called by skill_preamble before emitting a context gate message so the
   608	    user can resume after running /compact or /clear.  Never raises.
   609	    """
   610	    from . import state as _state  # lazy — avoid circular import at module load
   611	    import json as _json
   612	
   613	    try:
   614	        state_path = _state.state_dir(repo) / "compact_checkpoint.json"
   615	        state_path.parent.mkdir(parents=True, exist_ok=True)
   616	        state_path.write_text(
   617	            _json.dumps(
   618	                {
   619	                    "skill": skill,
   620	                    "reason": reason,
   621	                    "resume_cmd": "/renmark:resume",
   622	                    "timestamp": _state.now_iso(),
   623	                },
   624	                indent=2,
   625	            ),
   626	            encoding="utf-8",
   627	        )
   628	    except Exception:
   629	        pass
   630	
   631	
   632	def skill_preamble(repo: Path | str, skill: str) -> str | None:
   633	    """Single-call Step-0 boilerplate for every SKILL.md.
   634	
   635	    Performs the calls every skill used to inline by hand, gated by tier:
   636	        - ALL tiers: record invocation (load-bearing cross-domain detection for
   637	          the NEXT skill — runs unconditionally so minimal-tier skills still
   638	          register their domain in last-skill state).
   639	        - minimal: returns None immediately; no budget check, no fragments.
   640	        - standard: cross-domain/compact hint only; no fable synthesis hint.
   641	        - full (default): cross-domain/compact hint + fable synthesis hint.
   642	
   643	    Cross-domain detection for the CURRENT skill requires context_budget_check
   644	    to read last-skill state BEFORE record_skill_invocation overwrites it, so
   645	    for standard/full tier the budget check runs first. record_skill_invocation
   646	    is then called unconditionally to keep the state file current for the next
   647	    skill — this is the load-bearing ordering invariant.
   648	
   649	    Returns the hint string the skill should surface to the user, or None when
   650	    no hint is needed. Domain is resolved from `DOMAIN_BY_SKILL` — callers do
   651	    not need to pass it, so the per-skill prose can't drift.
   652	    """
   653	    # Imported lazily to avoid a state ↔ lifecycle circular import at module load.
   654	    from . import state as _state
   655	
   656	    domain = domain_of(skill)
   657	    tier = preamble_tier(skill)
   658	
   659	    if tier == "minimal":
   660	        # INVARIANT: record_skill_invocation runs for ALL tiers so that the next
   661	        # skill can detect cross-domain transitions even when this one is minimal.
   662	        _state.record_skill_invocation(repo, skill, domain)
   663	        return _with_agency_note(repo, skill, _with_headless_note(repo, None))
   664	
   665	    # For standard/full: budget check MUST read last-skill state before
   666	    # record_skill_invocation overwrites it — ordering is load-bearing.
   667	    # Read last-skill state once here so prev_domain is available for the gate message.
   668	    last = _state.last_skill_invocation(repo)
   669	    verdict = _state.context_budget_check(repo, skill, domain)
   670	    _state.record_skill_invocation(repo, skill, domain)
   671	
   672	    fragments: list[str] = []
   673	    if verdict == "clear":
   674	        if skill in _CONTEXT_BYPASS_SKILLS:
   675	            # Advisory only — finish/approve/resume must not be blocked mid-flow.
   676	            fragments.append(
   677	                f"context: cross-domain transition into `{domain}` — consider `/clear` "
   678	                "before continuing (`.renmark/memory/` survives clears)"
   679	            )
   680	        else:
   681	            prev_domain = last.get("domain", "?") if last else "?"
   682	            persist_compact_checkpoint(repo, skill, reason="clear")
   683	            return (
   684	                f"CONTEXT_GATE_CLEAR: cross-domain transition detected "
   685	                f"(prev: `{prev_domain}` \u2192 `{domain}`).\n"
   686	                "State persisted to .renmark/state/compact_checkpoint.json.\n"
   687	                "Present the user with AskUserQuestion before proceeding with this skill:\n"
   688	                '  header: "Context hygiene"\n'
   689	                '  question: "Domain change detected. Run /clear to start fresh '
   690	                '(memory survives), or continue with accumulated context?"\n'
   691	                "  options:\n"
   692	                "    1. Stop here \u2014 I will run /clear then /renmark:resume (Recommended)\n"
   693	                "    2. Continue in same context (this step only)\n"
   694	                "    3. Queue this as next task after current work finishes\n"
   695	                "    4. Cancel"
   696	            )
   697	    elif verdict == "compact":
   698	        fragments.append("context: approaching budget — consider `/compact` before continuing")
   699	
   700	    if tier == "full" and skill in SYNTHESIS_SKILLS:
   701	        # Imported lazily to keep capability resolution off the module-load path.
   702	        from . import capabilities as _capabilities
   703	
   704	        if _capabilities.top_tier(Path(repo)) == "fable":
   705	            fragments.append(
   706	                "declared top tier: fable — for best ideation/strategy results "
   707	                "run this session on Fable 5 (/model fable)"
   708	            )
   709	
   710	    base = " | ".join(fragments) if fragments else None
   711	    return _with_agency_note(repo, skill, _with_mode_note(repo, skill, _with_headless_note(repo, base)))
   712	
   713	
   714	# ── Operating-mode directive (Conductor vs Orchestrator) ──────────────────────
   715	#
   716	# Set-mode directive lines (persisted mode → one hint line). Kept as module
   717	# constants so the behavior test (T14) and unit test (T12) can assert the exact
   718	# text without duplicating string literals.
   719	_MODE_DIRECTIVE: dict[str, str] = {
   720	    "conductor": (
   721	        "Operating mode: Conductor — hands-on; prefer single-file scoped edits, "
   722	        "avoid subagents unless necessary, explain the next move before editing."
   723	    ),
   724	    "orchestrator": (
   725	        "Operating mode: Orchestrator — goal-level; use narrow scoped subagents "
   726	        "where useful, load skills on demand, review outcomes not keystrokes."
   727	    ),
   728	}
   729	
   730	# Entry-point skills that must PROMPT for a mode when none is set yet.
   731	_MODE_ENTRY_SKILLS: frozenset[str] = frozenset(
   732	    {"start", "feature", "debug", "roadmap", "finish", "orchestrate"}
   733	)
   734	
   735	# Skills whose flows must not be blocked mid-stream by context gates.
   736	# finish/approve/resume: urgent paths that must not be interrupted halfway.
   737	_CONTEXT_BYPASS_SKILLS: frozenset[str] = frozenset({"finish", "approve", "resume"})
   738	
   739	
   740	def _choose_mode_hint(skill: str) -> str:
   741	    """Choose-mode instruction emitted for an entry-point skill with no mode set.
   742	
   743	    Tells the orchestrator to ask the user Conductor vs Orchestrator via
   744	    AskUserQuestion, recommending the per-skill default, then persist the choice.
   745	    """
   746	    from . import mode as _mode
   747	
   748	    recommended = _mode.default_mode_for_skill(skill)
   749	    return (
   750	        "Operating mode: not yet set — ask the user Conductor vs Orchestrator via "
   751	        f"AskUserQuestion (recommend: {recommended}), then persist with "
   752	        "renmark.mode.set_mode(repo, <choice>)."
   753	    )
   754	
   755	
   756	def _with_mode_note(repo: Path | str, skill: str, hint: str | None) -> str | None:
   757	    """ADDITIVE: append the operating-mode directive to ``hint``.
   758	
   759	    Runs strictly AFTER the existing tier logic and the headless note — never
   760	    reorders the record-before-check invariant. DEGRADES GRACEFULLY: any

[35m[3mexec[0m[0m
[1m/bin/bash -lc "nl -ba /home/renmark/projects/ai-system/renmark/cli/_engine.py | sed -n '1,260p'"[0m in /home/renmark/projects/ai-system
[32m succeeded in 0ms:[0m
     1	"""renmark-execute CLI: orchestrates plan execution via Codex and Claude agents."""
     2	
     3	from __future__ import annotations
     4	
     5	import argparse
     6	import os
     7	import subprocess
     8	import sys
     9	import time
    10	from dataclasses import dataclass
    11	from pathlib import Path
    12	
    13	from dotenv import load_dotenv
    14	
    15	from .. import memory as _memory
    16	from ..parser import PlanError, Task, parse_plan
    17	from ..providers.codex import (
    18	    CodexError,
    19	    check_only_target_modified,
    20	    codex_available,
    21	    run_codex_task,
    22	)
    23	from ..state import (
    24	    PauseState,
    25	    UsageRecord,
    26	    append_usage,
    27	    clear_pause,
    28	    completed_task_indices,
    29	    escalation_dir,
    30	    new_run_id,
    31	    now_iso,
    32	    read_pause,
    33	    state_dir,
    34	    usage_this_month,
    35	    usage_today,
    36	    write_pause,
    37	)
    38	from ..verifier import run_verifier
    39	from .commands import (
    40	    cmd_analytics,
    41	    cmd_logs,
    42	    cmd_review_package,
    43	    cmd_roadmap,
    44	    cmd_scan,
    45	    cmd_task,
    46	    cmd_task_brief,
    47	    cmd_usage,
    48	)
    49	
    50	
    51	@dataclass
    52	class Config:
    53	    prefer_small_model: str
    54	    big_model: str
    55	    top_tier: str
    56	    max_tokens_per_run: int
    57	    max_minutes_per_run: int
    58	    max_tasks_per_run: int
    59	    max_task_retries: int
    60	    default_verifier_timeout_s: int
    61	    temperature: float
    62	    max_output_tokens: int
    63	
    64	    @classmethod
    65	    def from_env(cls) -> Config:
    66	        top_tier = os.environ.get("RENMARK_TOP_TIER", "").strip()
    67	        if top_tier not in ("fable", "opus"):
    68	            top_tier = ""  # defer to capabilities file resolution
    69	        return cls(
    70	            prefer_small_model=os.environ.get("RENMARK_PREFER_SMALL_MODEL", ""),
    71	            big_model=os.environ.get("RENMARK_BIG_MODEL", ""),
    72	            top_tier=top_tier,
    73	            max_tokens_per_run=int(os.environ.get("RENMARK_MAX_TOKENS_PER_RUN", "50000")),
    74	            max_minutes_per_run=int(os.environ.get("RENMARK_MAX_MINUTES_PER_RUN", "30")),
    75	            max_tasks_per_run=int(os.environ.get("RENMARK_MAX_TASKS_PER_RUN", "15")),
    76	            max_task_retries=int(os.environ.get("RENMARK_MAX_TASK_RETRIES", "2")),
    77	            default_verifier_timeout_s=int(os.environ.get("RENMARK_DEFAULT_VERIFIER_TIMEOUT_S", "60")),
    78	            temperature=float(os.environ.get("RENMARK_TEMPERATURE", "0.2")),
    79	            max_output_tokens=int(os.environ.get("RENMARK_MAX_OUTPUT_TOKENS", "4096")),
    80	        )
    81	
    82	
    83	def _print(msg: str = "") -> None:
    84	    print(msg, flush=True)
    85	
    86	
    87	def _git(*args: str, cwd: Path) -> subprocess.CompletedProcess[str]:
    88	    return subprocess.run(["git", *args], cwd=str(cwd), capture_output=True, text=True)
    89	
    90	
    91	def _is_git_repo(cwd: Path) -> bool:
    92	    return _git("rev-parse", "--is-inside-work-tree", cwd=cwd).returncode == 0
    93	
    94	
    95	def _ensure_git_repo(cwd: Path) -> None:
    96	    """Initialize a git repo (with identity + empty initial commit) if missing.
    97	
    98	    Commits are required per-task, so the orchestrator can't run without one.
    99	    Doing this silently is safer than asking the user to set up git manually.
   100	    """
   101	    _print(f"note: initializing git repo at {cwd} (commits required per task)")
   102	    subprocess.run(["git", "init", "-q", "-b", "main"], cwd=str(cwd), check=True)
   103	    name = _git("config", "user.name", cwd=cwd)
   104	    if name.returncode != 0 or not name.stdout.strip():
   105	        _git("config", "user.name", "renmark", cwd=cwd)
   106	    email = _git("config", "user.email", cwd=cwd)
   107	    if email.returncode != 0 or not email.stdout.strip():
   108	        _git("config", "user.email", "renmark@local", cwd=cwd)
   109	    subprocess.run(
   110	        ["git", "commit", "-q", "--allow-empty", "-m", "init (renmark)"],
   111	        cwd=str(cwd),
   112	        check=True,
   113	    )
   114	
   115	
   116	# Serialize git operations across parallel task threads. Wave members write to
   117	# disjoint files (validated by dispatch) so the apply/verify steps are safe to
   118	# parallelize, but the git index is not — index lock contention would
   119	# manifest as "Another git process seems to be running" errors.
   120	import threading as _threading
   121	
   122	_GIT_LOCK = _threading.Lock()
   123	
   124	
   125	def _git_tag(cwd: Path, name: str) -> None:
   126	    with _GIT_LOCK:
   127	        _git("tag", "-f", name, cwd=cwd)
   128	
   129	
   130	# When --no-commit is on, _git_commit becomes a no-op that returns a sentinel
   131	# string. The skill (`/renmark:orchestrate`) is then responsible for batching
   132	# commits per wave, in task-index order, after the wave finishes.
   133	_NO_COMMIT_MODE = False
   134	
   135	
   136	def _git_commit(cwd: Path, target: str, message: str, trailer: str) -> str:
   137	    if _NO_COMMIT_MODE:
   138	        return "(no-commit)"
   139	    with _GIT_LOCK:
   140	        add = _git("add", "--", target, cwd=cwd)
   141	        if add.returncode != 0:
   142	            return ""
   143	        full = message + "\n\n" + trailer + "\n"
   144	        commit = subprocess.run(
   145	            ["git", "commit", "-q", "-F", "-"],
   146	            cwd=str(cwd),
   147	            input=full,
   148	            capture_output=True,
   149	            text=True,
   150	        )
   151	        if commit.returncode != 0:
   152	            return ""
   153	        sha = _git("rev-parse", "--short", "HEAD", cwd=cwd).stdout.strip()
   154	        return sha
   155	
   156	
   157	def _git_restore_target(cwd: Path, target: str) -> None:
   158	    with _GIT_LOCK:
   159	        _git("checkout", "--", target, cwd=cwd)
   160	
   161	
   162	def _untracked_paths_locked(cwd: Path, paths: list[str]) -> set[str]:
   163	    """Subset of ``paths`` that git does NOT track (no committed version).
   164	
   165	    LOCK CONTRACT: the caller MUST already hold ``_GIT_LOCK``. Classification
   166	    and the rollback that consumes it must run under a SINGLE lock acquisition
   167	    (``_classify_and_rollback`` is the only intended caller) so a sibling thread
   168	    cannot create/delete a file in the gap and flip a path between the
   169	    tracked→checkout and untracked→delete arms. ``_GIT_LOCK`` is a plain
   170	    ``threading.Lock`` (non-reentrant), so this helper must NEVER re-acquire it.
   171	
   172	    A path git doesn't track is a newly-created file: ``git checkout -- <path>``
   173	    is a no-op (returncode 0, nothing restored), so rollback must DELETE it
   174	    instead. We ask git directly (``ls-files --error-unmatch``) rather than
   175	    parsing porcelain status codes, which the changed-files list has stripped.
   176	    """
   177	    untracked: set[str] = set()
   178	    for p in paths:
   179	        norm = p[2:] if p.startswith("./") else p
   180	        r = _git("ls-files", "--error-unmatch", "--", norm, cwd=cwd)
   181	        if r.returncode != 0:
   182	            untracked.add(norm)
   183	    return untracked
   184	
   185	
   186	def _rollback_paths_locked(cwd: Path, paths: list[str], *, untracked_before: set[str]) -> None:
   187	    """Restore ONLY the given paths — never ``git checkout -- .`` (whole tree),
   188	    which would revert a sibling wave-task's in-flight work.
   189	
   190	    LOCK CONTRACT: the caller MUST already hold ``_GIT_LOCK`` (see
   191	    ``_classify_and_rollback``). Does NOT acquire the lock itself — ``_GIT_LOCK``
   192	    is non-reentrant, and the classify→rollback sequence must be one atomic
   193	    critical section.
   194	
   195	    Mode-aware per path: a path that was UNTRACKED before the task can't be
   196	    restored by checkout (git has no committed version), so it's deleted;
   197	    tracked paths are checked-out.
   198	    """
   199	    if not paths:
   200	        return
   201	    for p in paths:
   202	        norm = p[2:] if p.startswith("./") else p
   203	        if norm in untracked_before or p in untracked_before:
   204	            # Untracked → delete the file/dir codex created.
   205	            cleaned = _git("clean", "-fd", "--", norm, cwd=cwd)
   206	            if cleaned.returncode != 0:
   207	                # Fall back to a direct unlink if clean refused.
   208	                fp = cwd / norm
   209	                try:
   210	                    if fp.is_file():
   211	                        fp.unlink()
   212	                except OSError:
   213	                    pass
   214	        else:
   215	            co = _git("checkout", "--", norm, cwd=cwd)
   216	            if co.returncode != 0:
   217	                _print(f"warning: rollback checkout failed for {norm}: {co.stderr.strip()[:120]}")
   218	
   219	
   220	def _classify_and_rollback(cwd: Path, paths: list[str]) -> None:
   221	    """Classify untracked-vs-tracked AND roll back, under ONE ``_GIT_LOCK``.
   222	
   223	    Replaces the old two-acquisition pattern (``_untracked_paths`` then
   224	    ``_rollback_paths``) which left a TOCTOU gap: a sibling thread could
   225	    create or delete a file between the classification and the rollback,
   226	    flipping a path's tracked/untracked status so rollback chose the wrong
   227	    arm (checkout-ing a now-untracked file as a no-op, or cleaning a tracked
   228	    one). Holding the lock across both makes the decision and the action
   229	    atomic with respect to sibling git operations.
   230	    """
   231	    if not paths:
   232	        return
   233	    with _GIT_LOCK:
   234	        untracked = _untracked_paths_locked(cwd, paths)
   235	        _rollback_paths_locked(cwd, paths, untracked_before=untracked)
   236	
   237	
   238	# Backwards-compatible standalone wrappers (each takes the lock itself). These
   239	# are NOT used on the hot rollback path — that goes through _classify_and_rollback
   240	# for a single atomic acquisition. They remain for callers/tests that exercise
   241	# classification or rollback in isolation. Never chain them on the hot path: two
   242	# acquisitions reintroduce the TOCTOU gap these helpers' _locked variants close.
   243	def _untracked_paths(cwd: Path, paths: list[str]) -> set[str]:
   244	    with _GIT_LOCK:
   245	        return _untracked_paths_locked(cwd, paths)
   246	
   247	
   248	def _rollback_paths(cwd: Path, paths: list[str], *, untracked_before: set[str]) -> None:
   249	    with _GIT_LOCK:
   250	        _rollback_paths_locked(cwd, paths, untracked_before=untracked_before)
   251	
   252	
   253	def _judge_lane_and_rollback(
   254	    cwd: Path,
   255	    *,
   256	    pre_changed_files: list[str],
   257	    target: str,
   258	    sibling_targets: list[str] | None,
   259	) -> tuple[bool, str]:
   260	    """Atomically snapshot → judge out-of-lane → roll back, under ONE ``_GIT_LOCK``.

[35m[3mexec[0m[0m
[1m/bin/bash -lc 'rg -n "compact_gate_tokens|compact-checkpoint|CONTEXT_GATE_CLEAR|context_budget_hint|context_budget_check|>=120k|120k" /home/renmark/projects/ai-system -S'[0m in /home/renmark/projects/ai-system
[32m succeeded in 0ms:[0m
/home/renmark/projects/ai-system/CLAUDE.md:154:**Cross-domain clear gate (Python-enforced):** When `skill_preamble` returns a string starting with `CONTEXT_GATE_CLEAR:`, use `AskUserQuestion` to present the choices described in that message BEFORE executing any skill steps. Do not proceed with the skill body until the user selects an option. If the user selects "Stop here", print: "State saved. Run: /clear — then run: /renmark:resume". Skills `finish`, `approve`, and `resume` bypass this gate (advisory only — these flows must not be interrupted mid-stream).
/home/renmark/projects/ai-system/CLAUDE.md:157:- **≥120k tokens** (configurable via `compact_gate_tokens` in `.renmark/config.json`, default 120k — proxy for 60% of the 200k window): MUST present blocking menu via `AskUserQuestion` before starting any new skill:
/home/renmark/projects/ai-system/CLAUDE.md:164:  If user selects option 1: run `renmark-execute --compact-checkpoint` to persist state, then print: "Run: /compact — then run: /renmark:resume"
/home/renmark/projects/ai-system/CLAUDE.md:171:Complement the %-based budget above with absolute hard stops (see `renmark.state.skills.context_budget_hint`):
/home/renmark/projects/ai-system/CLAUDE.md:173:- **120k tokens** (configurable via `compact_gate_tokens` in `.renmark/config.json`): surface `/compact` suggestion — do NOT auto-run.
/home/renmark/projects/ai-system/AGENTS.md:63:**Context budget — selectable hygiene gates at 60% and on domain change.** Renmark enforces two context hygiene gates as blocking AskUserQuestion menus, not silent hints. Neither gate claims to invoke `/compact` or `/clear` — those are host-level commands the user runs manually. Cross-domain clear gate (Python-enforced): when `skill_preamble` returns `CONTEXT_GATE_CLEAR:`, present an AskUserQuestion menu BEFORE any skill steps; `finish`/`approve`/`resume` bypass this. Compact gate (rule-enforced): at ≥120k tokens (configurable via `compact_gate_tokens` in `.renmark/config.json`, default 120k) present a blocking AskUserQuestion menu with Stop/Continue/Raise/Disable options; if user stops, run `renmark-execute --compact-checkpoint` and print resume instructions. At ≥160k tokens refuse new long skills (`orchestrate`, `loop`, `audit`) until `/compact` or `/clear`. Cross-domain transition always triggers the clear gate regardless of token count. See `CLAUDE.md` § `context-budget-rule`.
/home/renmark/projects/ai-system/AGENTS.md:65:**Context thresholds (absolute token counts).** Complement the %-based budget with absolute hard stops (`renmark.state.skills.context_budget_hint`): 100k → summarize in-flight reasoning, prefer artifact pointers; 120k (configurable via `compact_gate_tokens` in `.renmark/config.json`) → surface `/compact` suggestion, do NOT auto-run; 150k → checkpoint to `.renmark/state/`, refuse new long skills until `/compact` or `/clear`. Cross-domain always recommends `/clear` regardless of count. See `CLAUDE.md` § `context-thresholds-rule`.
/home/renmark/projects/ai-system/plugin/templates/CLAUDE.md.template:137:- **~60% (≈120k tokens):** surface a one-line note suggesting `/compact` before the next skill. Do NOT auto-run `/compact`.
/home/renmark/projects/ai-system/plugin/templates/CLAUDE.md.template:140:- **Cross-domain transition:** recommend `/clear` (memory survives). The %-side is enforced by orchestrator self-monitoring; the cross-domain side is automated via `renmark.lifecycle.skill_preamble(repo, skill)` (resolves domain from `DOMAIN_BY_SKILL`, runs `context_budget_check`, returns the hint).
/home/renmark/projects/ai-system/plugin/templates/AGENTS.md.template:60:**Context budget — `/compact` at 60%, `/clear` on subject change.** Orchestrator runs on Sonnet 200k; the window degrades before it fills. At ~60% (≈120k tokens) surface a one-line `/compact` note (don't auto-run). At ~80% (≈160k tokens) refuse new long skills (`orchestrate`, `loop`, `audit`) until `/compact` or `/clear`; short skills still run. Cross-domain transitions auto-recommend `/clear` via `renmark.lifecycle.skill_preamble`. See `CLAUDE.md` § `context-budget-rule`.
/home/renmark/projects/ai-system/CHANGELOG.md:8:- `renmark/config.py`: `compact_gate_tokens(repo) -> int` + `set_compact_gate_tokens(repo, value)` — configurable threshold (default 120k, 0=disabled)
/home/renmark/projects/ai-system/CHANGELOG.md:9:- `renmark/lifecycle.py`: `persist_compact_checkpoint(repo, skill, reason)` — writes `.renmark/state/compact_checkpoint.json`; `_CONTEXT_BYPASS_SKILLS = {"finish", "approve", "resume"}`; `skill_preamble` clear-verdict upgraded from advisory "consider /clear" to `CONTEXT_GATE_CLEAR:` prefixed hard-stop with full AskUserQuestion menu spec (bypass skills stay advisory)
/home/renmark/projects/ai-system/CHANGELOG.md:10:- `renmark/cli/_engine.py`: `--compact-checkpoint` flag (calls persist_compact_checkpoint + prints /compact+/renmark:resume instructions); `--set-compact-gate-tokens TOKENS` (configures threshold)
/home/renmark/projects/ai-system/CHANGELOG.md:11:- `CLAUDE.md` + `AGENTS.md`: Context budget rule block replaced with selectable hygiene gates spec (AskUserQuestion menus, CONTEXT_GATE_CLEAR: sentinel, compact-checkpoint CLI, bypass skill list, configurable threshold note)
/home/renmark/projects/ai-system/CHANGELOG.md:19:- `compact_gate_tokens` must return 0 when 0 is stored (0 = disabled, not default)
/home/renmark/projects/ai-system/CHANGELOG.md:20:- `CONTEXT_GATE_CLEAR:` prefix is load-bearing — CLAUDE.md rule pattern-matches it to trigger AskUserQuestion
/home/renmark/projects/ai-system/CHANGELOG.md:165:- `renmark/state/skills.py::context_budget_hint` — absolute 100k/120k/150k tiers
/home/renmark/projects/ai-system/CHANGELOG.md:539:- **`record_skill_invocation` MUST run for every tier** (including minimal) and BEFORE-vs-AFTER ordering relative to `context_budget_check` is load-bearing: for standard/full the budget check reads last-skill state first, then record overwrites it. Reordering breaks cross-domain `/clear` detection.
/home/renmark/projects/ai-system/CHANGELOG.md:967:**Built:** AGENTS over-compression repaired — restored the codex→sonnet reroute exception (critical), the full G11 carry/aggregates contract, bounded-output user-override + architecture-scans ban, write-boundary artifact homes, parallelism read-only-verification clause, and the skill_preamble reference (was stale context_budget_check). Both templates' dangling .renmark/memory/ pointers softened to '(generated by /renmark:init)' so fresh scaffolds don't dangle. Block byte-identity (template<->CLAUDE.md) preserved; 746 tests green. Persisted .renmark/audits/2026-06-12-plugin-context-footprint.md.
/home/renmark/projects/ai-system/CHANGELOG.md:2790:- **Step-0 boilerplate consolidated** — new `lifecycle.skill_preamble(repo, skill)` replaces the duplicated `context_budget_check` + `record_skill_invocation` block across all 14 SKILL.md files. Domain resolves centrally from `DOMAIN_BY_SKILL`, so per-skill drift is impossible.
/home/renmark/projects/ai-system/CHANGELOG.md:2859:- **`renmark/state.py`** (extended +200 LOC, 15 new tests) — pipeline.json (`read_pipeline_state`, `write_pipeline_state`, `clear_pipeline_state`, `pipeline_is_resumable`), `.renmark/state/wave-summaries/wave-N.json` aggregation (`write_wave_summary`, `read_wave_summary`, `list_wave_summaries`), and `last-skill.json` for cross-domain detection (`record_skill_invocation`, `last_skill_invocation`, `context_budget_check`).
/home/renmark/projects/ai-system/CHANGELOG.md:2866:- All 13 existing skills gained a **Step 0 — Context check** preflight that calls `state.context_budget_check` (for cross-domain `/clear` recommendations) and `state.record_skill_invocation` (for next-skill detection). Skills with stage semantics (start, brainstorm, plan, check-plan, finish) now also write `lifecycle.json` on completion.
/home/renmark/projects/ai-system/tests/test_lifecycle.py:327:    # Cross-domain gate takes priority — returns CONTEXT_GATE_CLEAR early; tier
/home/renmark/projects/ai-system/tests/test_lifecycle.py:330:    assert hint.startswith("CONTEXT_GATE_CLEAR:")
/home/renmark/projects/ai-system/tests/test_config.py:224:# ── compact_gate_tokens ───────────────────────────────────────────────────────
/home/renmark/projects/ai-system/tests/test_config.py:227:def test_compact_gate_tokens_default(tmp_path: Path) -> None:
/home/renmark/projects/ai-system/tests/test_config.py:228:    from renmark.config import compact_gate_tokens
/home/renmark/projects/ai-system/tests/test_config.py:229:    assert compact_gate_tokens(tmp_path) == 120_000
/home/renmark/projects/ai-system/tests/test_config.py:232:def test_compact_gate_tokens_set_and_get(tmp_path: Path) -> None:
/home/renmark/projects/ai-system/tests/test_config.py:233:    from renmark.config import compact_gate_tokens, set_compact_gate_tokens
/home/renmark/projects/ai-system/tests/test_config.py:234:    set_compact_gate_tokens(tmp_path, 80_000)
/home/renmark/projects/ai-system/tests/test_config.py:235:    assert compact_gate_tokens(tmp_path) == 80_000
/home/renmark/projects/ai-system/tests/test_config.py:238:def test_compact_gate_tokens_zero_is_valid(tmp_path: Path) -> None:
/home/renmark/projects/ai-system/tests/test_config.py:240:    from renmark.config import compact_gate_tokens, set_compact_gate_tokens
/home/renmark/projects/ai-system/tests/test_config.py:241:    set_compact_gate_tokens(tmp_path, 0)
/home/renmark/projects/ai-system/tests/test_config.py:242:    assert compact_gate_tokens(tmp_path) == 0
/home/renmark/projects/ai-system/tests/test_config.py:245:def test_compact_gate_tokens_negative_value_returns_default(tmp_path: Path) -> None:
/home/renmark/projects/ai-system/tests/test_config.py:250:    cfg_file.write_text(json.dumps({"compact_gate_tokens": -5}))
/home/renmark/projects/ai-system/tests/test_config.py:251:    from renmark.config import compact_gate_tokens
/home/renmark/projects/ai-system/tests/test_config.py:252:    assert compact_gate_tokens(tmp_path) == 120_000
/home/renmark/projects/ai-system/tests/test_config.py:255:def test_compact_gate_tokens_non_int_returns_default(tmp_path: Path) -> None:
/home/renmark/projects/ai-system/tests/test_config.py:260:    cfg_file.write_text(json.dumps({"compact_gate_tokens": "not-an-int"}))
/home/renmark/projects/ai-system/tests/test_config.py:261:    from renmark.config import compact_gate_tokens
/home/renmark/projects/ai-system/tests/test_config.py:262:    assert compact_gate_tokens(tmp_path) == 120_000
/home/renmark/projects/ai-system/tests/test_state_pipeline.py:127:def test_context_budget_check_first_invocation(tmp_path: Path) -> None:
/home/renmark/projects/ai-system/tests/test_state_pipeline.py:129:    assert state.context_budget_check(tmp_path, "plan", "build") is None
/home/renmark/projects/ai-system/tests/test_state_pipeline.py:132:def test_context_budget_check_same_domain(tmp_path: Path) -> None:
/home/renmark/projects/ai-system/tests/test_state_pipeline.py:134:    assert state.context_budget_check(tmp_path, "orchestrate", "build") is None
/home/renmark/projects/ai-system/tests/test_state_pipeline.py:137:def test_context_budget_check_cross_domain(tmp_path: Path) -> None:
/home/renmark/projects/ai-system/tests/test_state_pipeline.py:139:    assert state.context_budget_check(tmp_path, "orchestrate", "build") == "clear"
/home/renmark/projects/ai-system/tests/test_state_pipeline.py:142:def test_context_budget_check_audit_to_build(tmp_path: Path) -> None:
/home/renmark/projects/ai-system/tests/test_state_pipeline.py:144:    assert state.context_budget_check(tmp_path, "orchestrate", "build") == "clear"
/home/renmark/projects/ai-system/tests/test_state_pipeline.py:156:        assert state.context_budget_check(tmp_path, "orchestrate", "build") is None
/home/renmark/projects/ai-system/renmark/state/skills.py:14:# Absolute context-token thresholds for context_budget_hint.
/home/renmark/projects/ai-system/renmark/state/skills.py:17:# by context_budget_check — none of those are altered here.
/home/renmark/projects/ai-system/renmark/state/skills.py:23:def context_budget_hint(tokens: int) -> str | None:
/home/renmark/projects/ai-system/renmark/state/skills.py:30:    rule and the cross-domain /clear hint from context_budget_check — neither of
/home/renmark/projects/ai-system/renmark/state/skills.py:35:      CTX_COMPACT   (120k) — recommend /compact before the next skill.
/home/renmark/projects/ai-system/renmark/state/skills.py:50:        return "≈120k context — recommend `/compact` before the next skill."
/home/renmark/projects/ai-system/renmark/state/skills.py:61:    Used by context_budget_check to detect cross-domain transitions and
/home/renmark/projects/ai-system/renmark/state/skills.py:89:def context_budget_check(repo_root: str | Path, new_skill: str, new_domain: str) -> str | None:
/home/renmark/projects/ai-system/renmark/lifecycle.py:643:    Cross-domain detection for the CURRENT skill requires context_budget_check
/home/renmark/projects/ai-system/renmark/lifecycle.py:669:    verdict = _state.context_budget_check(repo, skill, domain)
/home/renmark/projects/ai-system/renmark/lifecycle.py:684:                f"CONTEXT_GATE_CLEAR: cross-domain transition detected "
/home/renmark/projects/ai-system/renmark/state/__init__.py:69:    context_budget_check,
/home/renmark/projects/ai-system/renmark/state/__init__.py:118:    "context_budget_check",
/home/renmark/projects/ai-system/renmark/cli/_engine.py:1268:        "--compact-checkpoint",
/home/renmark/projects/ai-system/renmark/cli/_engine.py:1393:    if args.set_compact_gate_tokens is not None:
/home/renmark/projects/ai-system/renmark/cli/_engine.py:1395:        _config.set_compact_gate_tokens(repo, args.set_compact_gate_tokens)
/home/renmark/projects/ai-system/renmark/cli/_engine.py:1396:        print(f"compact_gate_tokens set to {args.set_compact_gate_tokens}")
/home/renmark/projects/ai-system/renmark/config.py:216:def compact_gate_tokens(repo: str | Path) -> int:
/home/renmark/projects/ai-system/renmark/config.py:225:    val = data.get("compact_gate_tokens")
/home/renmark/projects/ai-system/renmark/config.py:233:def set_compact_gate_tokens(repo: str | Path, value: int) -> None:
/home/renmark/projects/ai-system/renmark/config.py:236:    Reads the existing config, updates only the ``compact_gate_tokens`` key,
/home/renmark/projects/ai-system/renmark/config.py:243:    data["compact_gate_tokens"] = value
/home/renmark/projects/ai-system/tests/test_state_skills.py:5:from renmark.state.skills import context_budget_hint
/home/renmark/projects/ai-system/tests/test_state_skills.py:8:def test_context_budget_hint_tiers_and_invalid_inputs() -> None:
/home/renmark/projects/ai-system/tests/test_state_skills.py:9:    assert context_budget_hint(99_999) is None
/home/renmark/projects/ai-system/tests/test_state_skills.py:10:    assert context_budget_hint(100_000) == "≈100k context — summarize the current stage before continuing."
/home/renmark/projects/ai-system/tests/test_state_skills.py:11:    assert context_budget_hint(119_999) == "≈100k context — summarize the current stage before continuing."
/home/renmark/projects/ai-system/tests/test_state_skills.py:12:    assert context_budget_hint(120_000) == "≈120k context — recommend `/compact` before the next skill."
/home/renmark/projects/ai-system/tests/test_state_skills.py:13:    assert context_budget_hint(149_999) == "≈120k context — recommend `/compact` before the next skill."
/home/renmark/projects/ai-system/tests/test_state_skills.py:15:        context_budget_hint(150_000)
/home/renmark/projects/ai-system/tests/test_state_skills.py:18:    assert context_budget_hint(-1) is None
/home/renmark/projects/ai-system/tests/test_state_skills.py:19:    assert context_budget_hint("100000") is None
/home/renmark/projects/ai-system/tests/test_state_skills.py:20:    assert context_budget_hint(True) is None
/home/renmark/projects/ai-system/plugin/skills/_shared/context-taxonomy.md:91:**Clear gate (Python-enforced):** `skill_preamble` → `context_budget_check` returns `"clear"` on cross-domain transition → `persist_compact_checkpoint(repo, skill, reason="clear")` called → returns `CONTEXT_GATE_CLEAR:`-prefixed string → CLAUDE.md rule triggers `AskUserQuestion` menu. Bypass skills (advisory only): `finish`, `approve`, `resume`.
/home/renmark/projects/ai-system/plugin/skills/_shared/context-taxonomy.md:93:**Compact gate (rule-enforced):** Threshold in `config.json["compact_gate_tokens"]` (default 120k, 0 = disabled). CLI helper: `renmark-execute --compact-checkpoint`. Enforced by CLAUDE.md rule at ≥120k tokens; Python provides persist + CLI helper only. Python cannot detect real % context utilization — absolute token count is the proxy.
/home/renmark/projects/ai-system/plugin/skills/_shared/agency-delivery.md:85:- **Context budget:** `renmark.state.skills.context_budget_hint` (100k summarize / 120k compact / 150k checkpoint).
/home/renmark/projects/ai-system/plugin/skills/_shared/agency-delivery.md:119:> *Honor the agency delivery contract in `${CLAUDE_PLUGIN_ROOT}/skills/_shared/agency-delivery.md`: discovery → PRD → stack signoff → roadmap/milestones → build → demo/feedback → verification → final signoff → finish. Ask owner-level questions only (goal, users, constraints, risks, success, signoff). Milestone checkpoints pause for owner review — they do not pass automatically when tests pass. Background agents handle implementation; main agent coordinates and reads bounded summaries. Delegate cost infra to finish_lanes / cost.py / context_budget_hint / subagent-profiles — do not inline those rules.*

[35m[3mexec[0m[0m
[1m/bin/bash -lc "nl -ba /home/renmark/projects/ai-system/renmark/cli/_engine.py | sed -n '1240,1415p'"[0m in /home/renmark/projects/ai-system
[32m succeeded in 0ms:[0m
  1240	            "persist the headless-mode flag to .renmark/config.json "
  1241	            "('true' = run non-interactively, suppressing interactive gates/menus; "
  1242	            "'false' = restore interactive behavior). Default: false."
  1243	        ),
  1244	    )
  1245	    # P8 harness — persisted operating-mode (conductor|orchestrator)
  1246	    ap.add_argument(
  1247	        "--set-mode",
  1248	        choices=("conductor", "orchestrator"),
  1249	        metavar="conductor|orchestrator",
  1250	        help=(
  1251	            "persist the operating mode to .renmark/state/mode.json "
  1252	            "('conductor' = high-touch interactive drive; "
  1253	            "'orchestrator' = hands-off autonomous plan execution)."
  1254	        ),
  1255	    )
  1256	    ap.add_argument(
  1257	        "--get-mode",
  1258	        action="store_true",
  1259	        help="print the persisted operating mode (or 'unset' if none), then exit",
  1260	    )
  1261	    ap.add_argument(
  1262	        "--clear-mode",
  1263	        action="store_true",
  1264	        help="clear the persisted operating mode from .renmark/state/mode.json, then exit",
  1265	    )
  1266	    # Context hygiene gates
  1267	    ap.add_argument(
  1268	        "--compact-checkpoint",
  1269	        action="store_true",
  1270	        help=(
  1271	            "persist resume state to .renmark/state/compact_checkpoint.json "
  1272	            "and print the commands to run before /compact. "
  1273	            "Use when context is at 60%%+ to preserve state before compacting."
  1274	        ),
  1275	    )
  1276	    ap.add_argument(
  1277	        "--set-compact-gate-tokens",
  1278	        metavar="TOKENS",
  1279	        type=int,
  1280	        help=(
  1281	            "persist the compact-gate token threshold to .renmark/config.json. "
  1282	            "Default: 120000 (proxy for 60%% of the 200k window). "
  1283	            "0 = disable the compact gate. Negative values are rejected."
  1284	        ),
  1285	    )
  1286	    # P4 file-handoff helpers — print ONLY the written path; diff/spec bytes stay out of orchestrator context
  1287	    ap.add_argument(
  1288	        "--task-brief",
  1289	        nargs=2,
  1290	        metavar=("PLAN_PATH", "TASK_INDEX"),
  1291	        help=(
  1292	            "extract task N's spec/brief from PLAN_PATH, write to "
  1293	            ".renmark/state/handoffs/<stem>-task-N.brief.md, print ONLY the path"
  1294	        ),
  1295	    )
  1296	    ap.add_argument(
  1297	        "--review-package",
  1298	        nargs=2,
  1299	        metavar=("BASE_REF", "HEAD_REF"),
  1300	        help=(
  1301	            "write git diff --stat + per-file diffs for BASE..HEAD to "
  1302	            ".renmark/state/handoffs/review-<base>-<head>.pkg.md, print ONLY the path"
  1303	        ),
  1304	    )
  1305	    args = ap.parse_args(argv)
  1306	
  1307	    if (args.propose or args.emit_cron) and not args.scan:
  1308	        print("--propose/--emit-cron require --scan", file=sys.stderr)
  1309	        return 2
  1310	    if (args.accept or args.judge) and not args.behavior:
  1311	        print("--accept/--judge require --behavior", file=sys.stderr)
  1312	        return 2
  1313	
  1314	    repo = Path(args.repo).resolve()
  1315	
  1316	    if args.usage:
  1317	        return cmd_usage(repo)
  1318	    if args.analytics:
  1319	        return cmd_analytics(repo)
  1320	    if args.roadmap:
  1321	        return cmd_roadmap(repo)
  1322	    if args.logs:
  1323	        return cmd_logs(repo, n=args.logs_n)
  1324	    if args.scan:
  1325	        return cmd_scan(repo, propose=args.propose, emit_cron=args.emit_cron)
  1326	    if args.behavior:
  1327	        return _cmd_behavior(repo, accept=args.accept, judge=args.judge)
  1328	    if args.task:
  1329	        if not args.output:
  1330	            ap.error("--task requires --output ARTIFACT_PATH")
  1331	        return cmd_task(args.task, args.output, repo=repo)
  1332	    if args.set_proactive is not None:
  1333	        raw = args.set_proactive.strip().lower()
  1334	        if raw not in ("true", "false"):
  1335	            ap.error("--set-proactive expects 'true' or 'false'")
  1336	        from .. import config as _config
  1337	        value = raw == "true"
  1338	        _config.set_proactive(repo, value)
  1339	        state_str = "on" if value else "off"
  1340	        print(f"renmark: proactive auto-routing {state_str} ({repo}/.renmark/config.json)")
  1341	        return 0
  1342	    if args.set_headless is not None:
  1343	        raw = args.set_headless.strip().lower()
  1344	        if raw not in ("true", "false"):
  1345	            ap.error("--set-headless expects 'true' or 'false'")
  1346	        from .. import config as _config
  1347	        value = raw == "true"
  1348	        _config.set_headless(repo, value)
  1349	        state_str = "on" if value else "off"
  1350	        print(f"renmark: headless mode {state_str} ({repo}/.renmark/config.json)")
  1351	        return 0
  1352	    if args.set_mode is not None:
  1353	        from .. import mode as _mode
  1354	        mode_path = _mode.mode_state_path(repo)
  1355	        try:
  1356	            _mode.set_mode(repo, args.set_mode)
  1357	        except ValueError as exc:
  1358	            ap.error(str(exc))
  1359	        except OSError as exc:
  1360	            print(
  1361	                f"renmark: failed to persist operating mode to {mode_path}: {exc}",
  1362	                file=sys.stderr,
  1363	            )
  1364	            return 1
  1365	        print(f"renmark: operating mode set to {args.set_mode} ({mode_path})")
  1366	        return 0
  1367	    if args.get_mode:
  1368	        from .. import mode as _mode
  1369	        current = _mode.read_mode(repo)
  1370	        print(current if current is not None else "unset")
  1371	        return 0
  1372	    if args.clear_mode:
  1373	        from .. import mode as _mode
  1374	        mode_path = _mode.mode_state_path(repo)
  1375	        try:
  1376	            _mode.clear_mode(repo)
  1377	        except OSError as exc:
  1378	            print(
  1379	                f"renmark: failed to clear operating mode at {mode_path}: {exc}",
  1380	                file=sys.stderr,
  1381	            )
  1382	            return 1
  1383	        print(f"renmark: operating mode cleared ({mode_path})")
  1384	        return 0
  1385	    if args.compact_checkpoint:
  1386	        from renmark import lifecycle as _lifecycle
  1387	        _lifecycle.persist_compact_checkpoint(repo, skill="manual", reason="compact")
  1388	        print("Compact checkpoint written to .renmark/state/compact_checkpoint.json.")
  1389	        print("Run these commands to safely reduce context:")
  1390	        print("  1. /compact")
  1391	        print("  2. /renmark:resume")
  1392	        return 0
  1393	    if args.set_compact_gate_tokens is not None:
  1394	        from renmark import config as _config
  1395	        _config.set_compact_gate_tokens(repo, args.set_compact_gate_tokens)
  1396	        print(f"compact_gate_tokens set to {args.set_compact_gate_tokens}")
  1397	        return 0
  1398	    if args.task_brief:
  1399	        plan_path, task_index_str = args.task_brief
  1400	        try:
  1401	            task_index = int(task_index_str)
  1402	        except ValueError:
  1403	            ap.error(f"--task-brief TASK_INDEX must be an integer, got {task_index_str!r}")
  1404	        return cmd_task_brief(plan_path, task_index, repo=repo)
  1405	    if args.review_package:
  1406	        base_ref, head_ref = args.review_package
  1407	        return cmd_review_package(base_ref, head_ref, repo=repo)
  1408	
  1409	    if not args.plan:
  1410	        ap.error(
  1411	            "plan path is required unless --usage / --analytics / --roadmap / --logs / "
  1412	            "--scan / --behavior / --task / --task-brief / --review-package / "
  1413	            "--set-proactive / --set-headless / --set-mode / --get-mode / --clear-mode"
  1414	        )
  1415	    return execute_plan(

[35m[3mexec[0m[0m
[1m/bin/bash -lc "nl -ba /home/renmark/projects/ai-system/CLAUDE.md | sed -n '146,180p'"[0m in /home/renmark/projects/ai-system
[32m succeeded in 0ms:[0m
   146	- **Each subagent writes ONLY:** task artifact (code/diff lives here); status
   147	  (`PASS`|`FAIL`|`SKIP`); touched files; sha/hash; summary ≤ 5 lines; dependency notes.
   148	- **Orchestrator aggregates ONLY:** PASS/FAIL/SKIP; artifact path; token count; dependency status; next-wave readiness. Never merged back: subagent transcript, generated code, diff, long reasoning — the orchestrator advances on summary fields alone.
   149	<!-- END:task-isolation-rule -->
   150	<!-- BEGIN:context-budget-rule -->
   151	## Context budget — selectable hygiene gates at 60% and on domain change
   152	Renmark enforces two context hygiene gates as **blocking AskUserQuestion menus**, not silent hints. Neither gate claims to invoke `/compact` or `/clear` — those are host-level commands the user runs manually. Selecting "Stop here" means Python persists resume state and the skill stops cleanly.
   153	
   154	**Cross-domain clear gate (Python-enforced):** When `skill_preamble` returns a string starting with `CONTEXT_GATE_CLEAR:`, use `AskUserQuestion` to present the choices described in that message BEFORE executing any skill steps. Do not proceed with the skill body until the user selects an option. If the user selects "Stop here", print: "State saved. Run: /clear — then run: /renmark:resume". Skills `finish`, `approve`, and `resume` bypass this gate (advisory only — these flows must not be interrupted mid-stream).
   155	
   156	**Compact gate (rule-enforced — Python cannot detect % context):**
   157	- **≥120k tokens** (configurable via `compact_gate_tokens` in `.renmark/config.json`, default 120k — proxy for 60% of the 200k window): MUST present blocking menu via `AskUserQuestion` before starting any new skill:
   158	  - header: "Context hygiene"
   159	  - question: "Context window is at 60%+. Continuing may reduce accuracy."
   160	  - option 1 (Recommended): "Stop here — I will run /compact then /renmark:resume"
   161	  - option 2: "Continue this step only (once)"
   162	  - option 3: "Raise threshold: renmark-execute --set-compact-gate-tokens <value>"
   163	  - option 4: "Disable for this branch: renmark-execute --set-compact-gate-tokens 0"
   164	  If user selects option 1: run `renmark-execute --compact-checkpoint` to persist state, then print: "Run: /compact — then run: /renmark:resume"
   165	- **≥160k tokens:** Refuse to start any new long skill (`orchestrate`, `loop`, `audit`) until the user runs `/compact` or `/clear`.
   166	
   167	Cross-domain transition always triggers the clear gate regardless of token count.
   168	<!-- END:context-budget-rule -->
   169	<!-- BEGIN:context-thresholds-rule -->
   170	## Context thresholds (absolute token counts)
   171	Complement the %-based budget above with absolute hard stops (see `renmark.state.skills.context_budget_hint`):
   172	- **100k tokens:** summarize in-flight reasoning; prefer artifact pointers over inline output.
   173	- **120k tokens** (configurable via `compact_gate_tokens` in `.renmark/config.json`): surface `/compact` suggestion — do NOT auto-run.
   174	- **150k tokens:** checkpoint to `.renmark/state/`; refuse new long skills until `/compact` or `/clear`.
   175	Cross-domain transition always recommends `/clear` regardless of count.
   176	<!-- END:context-thresholds-rule -->
   177	<!-- BEGIN:model-routing-discipline-rule -->
   178	## Model-routing discipline
   179	Route each task to the cheapest capable executor. Do NOT default to Opus or Fable for routine work.
   180	- **Haiku** — docs, grep, summaries, changelog entries, small audits, simple format checks.

[35m[3mexec[0m[0m
[1m/bin/bash -lc "nl -ba /home/renmark/projects/ai-system/plugin/skills/_shared/context-taxonomy.md | sed -n '84,100p'"[0m in /home/renmark/projects/ai-system
[32m succeeded in 0ms:[0m
    84	
    85	---
    86	
    87	## Context hygiene gates
    88	
    89	Renmark enforces two hygiene gates as blocking AskUserQuestion menus:
    90	
    91	**Clear gate (Python-enforced):** `skill_preamble` → `context_budget_check` returns `"clear"` on cross-domain transition → `persist_compact_checkpoint(repo, skill, reason="clear")` called → returns `CONTEXT_GATE_CLEAR:`-prefixed string → CLAUDE.md rule triggers `AskUserQuestion` menu. Bypass skills (advisory only): `finish`, `approve`, `resume`.
    92	
    93	**Compact gate (rule-enforced):** Threshold in `config.json["compact_gate_tokens"]` (default 120k, 0 = disabled). CLI helper: `renmark-execute --compact-checkpoint`. Enforced by CLAUDE.md rule at ≥120k tokens; Python provides persist + CLI helper only. Python cannot detect real % context utilization — absolute token count is the proxy.
    94	
    95	**`persist_compact_checkpoint(repo, skill, reason)`:** writes `.renmark/state/compact_checkpoint.json` with `{skill, reason, resume_cmd: "/renmark:resume", timestamp}`. Consumed by `/renmark:resume` for state recovery. Never raises.

[35m[3mexec[0m[0m
[1m/bin/bash -lc "nl -ba /home/renmark/projects/ai-system/tests/test_lifecycle.py | sed -n '300,380p'"[0m in /home/renmark/projects/ai-system
[32m succeeded in 0ms:[0m
   300	
   301	    hint = lifecycle.skill_preamble(tmp_path, "brainstorm")
   302	
   303	    assert "declared top tier: fable" not in (hint or "")
   304	
   305	
   306	def test_preamble_hint_fires_on_env_declaration(tmp_path: Path, monkeypatch: pytest.MonkeyPatch) -> None:
   307	    """BY DESIGN (PRD REQ-2): `RENMARK_TOP_TIER` is a legitimate PER-USER declaration
   308	    form — "a committed `## Model tiers` block…, per-user overridable with
   309	    `RENMARK_TOP_TIER`". An undeclared repo (no routing.md) + RENMARK_TOP_TIER=fable
   310	    IS declared, so the tier hint must fire; this is not a bypass."""
   311	    monkeypatch.setenv("RENMARK_TOP_TIER", "fable")
   312	    # No _write_declared_fable_routing() call — the repo itself stays undeclared.
   313	
   314	    hint = lifecycle.skill_preamble(tmp_path, "brainstorm")
   315	
   316	    assert hint is not None
   317	    assert "declared top tier: fable" in hint
   318	
   319	
   320	def test_skill_preamble_cross_domain_and_tier_hints_joined(tmp_path: Path, monkeypatch: pytest.MonkeyPatch) -> None:
   321	    monkeypatch.delenv("RENMARK_TOP_TIER", raising=False)
   322	    _write_declared_fable_routing(tmp_path)
   323	    lifecycle.skill_preamble(tmp_path, "debug")
   324	
   325	    hint = lifecycle.skill_preamble(tmp_path, "brainstorm")
   326	
   327	    # Cross-domain gate takes priority — returns CONTEXT_GATE_CLEAR early; tier
   328	    # hint is not appended when the gate fires (gate short-circuits the join).
   329	    assert hint is not None
   330	    assert hint.startswith("CONTEXT_GATE_CLEAR:")
   331	    assert "cross-domain transition" in hint
   332	
   333	
   334	def test_corrupt_lifecycle_returns_none(tmp_path: Path) -> None:
   335	    state_dir = tmp_path / ".renmark" / "state"
   336	    state_dir.mkdir(parents=True)
   337	    (state_dir / "lifecycle.json").write_text("not json {{{")
   338	    assert lifecycle.read_lifecycle(tmp_path) is None
   339	
   340	
   341	def test_non_dict_lifecycle_returns_none(tmp_path: Path) -> None:
   342	    """Valid JSON whose top level is not an object must degrade to None,
   343	    never raise — a corrupt file must not kill cold-start recovery."""
   344	    state_dir = tmp_path / ".renmark" / "state"
   345	    state_dir.mkdir(parents=True)
   346	    for payload in ("[]", '"x"', "42"):
   347	        (state_dir / "lifecycle.json").write_text(payload)
   348	        assert lifecycle.read_lifecycle(tmp_path) is None
   349	
   350	
   351	def test_wrong_typed_fields_dropped(tmp_path: Path) -> None:
   352	    """Wrong-typed values degrade to dataclass defaults instead of raising."""
   353	    state_dir = tmp_path / ".renmark" / "state"
   354	    state_dir.mkdir(parents=True)
   355	    (state_dir / "lifecycle.json").write_text(json.dumps({"stage": ["a"], "artifacts": ["a"], "feature": 7}))
   356	    state = lifecycle.read_lifecycle(tmp_path)
   357	    assert state is not None
   358	    assert state.stage == "init"
   359	    assert state.artifacts == {}
   360	    assert state.feature == ""
   361	
   362	
   363	def test_non_dict_lifecycle_does_not_break_writes(tmp_path: Path) -> None:
   364	    """write_lifecycle resets from a corrupt file instead of raising."""
   365	    state_dir = tmp_path / ".renmark" / "state"
   366	    state_dir.mkdir(parents=True)
   367	    (state_dir / "lifecycle.json").write_text("[]")
   368	    state = lifecycle.write_lifecycle(tmp_path, stage="brainstorm-complete")
   369	    assert state.stage == "brainstorm-complete"
   370	
   371	
   372	def test_field_type_map_matches_dataclass() -> None:
   373	    """Drift guard: the read-time type filter must cover every dataclass field."""
   374	    assert set(lifecycle._LIFECYCLE_FIELD_TYPES) == set(lifecycle.LifecycleState.__dataclass_fields__)
   375	
   376	
   377	def test_validate_artifact_refs_survives_corrupt_state(tmp_path: Path) -> None:
   378	    state_dir = tmp_path / ".renmark" / "state"
   379	    state_dir.mkdir(parents=True)
   380	    (state_dir / "lifecycle.json").write_text('{"stage": "init", "artifacts": ["a"]}')

[35m[3mcodex[0m[0m
There’s a likely gap around the “configurable” compact threshold: I don’t see any runtime consumer yet, only the setter/getter and docs. I’m checking whether any resume/checkpoint path or context helper actually reads the stored value.
[35m[3mexec[0m[0m
[1m/bin/bash -lc "nl -ba /home/renmark/projects/ai-system/renmark/state/skills.py | sed -n '1,120p'"[0m in /home/renmark/projects/ai-system
[32m succeeded in 0ms:[0m
     1	"""Skill-invocation tracking (.renmark/state/last-skill.json).
     2	
     3	G4: subject-change detection for context-contamination prompts.
     4	"""
     5	
     6	from __future__ import annotations
     7	
     8	import json
     9	from pathlib import Path
    10	from typing import Any, cast
    11	
    12	from ._core import LAST_SKILL_FILE, now_iso, state_dir
    13	
    14	# Absolute context-token thresholds for context_budget_hint.
    15	# These COMPLEMENT the existing 60%/80% self-monitored budget rules in CLAUDE.md
    16	# (which the orchestrator self-monitors) and the cross-domain /clear hint produced
    17	# by context_budget_check — none of those are altered here.
    18	CTX_SUMMARIZE = 100_000
    19	CTX_COMPACT = 120_000
    20	CTX_CHECKPOINT = 150_000
    21	
    22	
    23	def context_budget_hint(tokens: int) -> str | None:
    24	    """Return a human-readable budget hint for the given absolute token count.
    25	
    26	    Returns None when tokens is below CTX_SUMMARIZE or invalid (non-int, bool,
    27	    negative).  Never raises.
    28	
    29	    These absolute tiers COMPLEMENT the existing 60%/80% self-monitored budget
    30	    rule and the cross-domain /clear hint from context_budget_check — neither of
    31	    those is altered by this helper.
    32	
    33	    Tiers:
    34	      CTX_SUMMARIZE (100k) — summarize the current stage.
    35	      CTX_COMPACT   (120k) — recommend /compact before the next skill.
    36	      CTX_CHECKPOINT(150k) — strongly recommend /compact or a checkpoint.
    37	    """
    38	    # Guard: booleans are ints in Python; treat them as invalid.
    39	    if isinstance(tokens, bool):
    40	        return None
    41	    if not isinstance(tokens, int):
    42	        return None
    43	    if tokens < 0:
    44	        return None
    45	    if tokens < CTX_SUMMARIZE:
    46	        return None
    47	    if tokens < CTX_COMPACT:
    48	        return "≈100k context — summarize the current stage before continuing."
    49	    if tokens < CTX_CHECKPOINT:
    50	        return "≈120k context — recommend `/compact` before the next skill."
    51	    return "≈150k context — strongly recommend `/compact` or a checkpoint before continuing."
    52	
    53	
    54	def _last_skill_path(repo_root: str | Path) -> Path:
    55	    return state_dir(repo_root) / LAST_SKILL_FILE
    56	
    57	
    58	def record_skill_invocation(repo_root: str | Path, skill_name: str, domain: str) -> None:
    59	    """Append-style record of which skill ran last and in which domain.
    60	
    61	    Used by context_budget_check to detect cross-domain transitions and
    62	    suggest /clear (per G4 / context-contamination-rule).
    63	    """
    64	    payload = {
    65	        "skill": skill_name,
    66	        "domain": domain,
    67	        "timestamp": now_iso(),
    68	    }
    69	    path = _last_skill_path(repo_root)
    70	    path.parent.mkdir(parents=True, exist_ok=True)
    71	    path.write_text(json.dumps(payload, indent=2), encoding="utf-8")
    72	
    73	
    74	def last_skill_invocation(repo_root: str | Path) -> dict[str, Any] | None:
    75	    path = _last_skill_path(repo_root)
    76	    if not path.exists():
    77	        return None
    78	    try:
    79	        data = json.loads(path.read_text(encoding="utf-8"))
    80	    except (json.JSONDecodeError, OSError):
    81	        return None
    82	    if not isinstance(data, dict):
    83	        # Valid JSON but not an object — treat like corruption, never raise:
    84	        # this runs inside every skill's Step 0 preamble.
    85	        return None
    86	    return cast(dict[str, Any], data)
    87	
    88	
    89	def context_budget_check(repo_root: str | Path, new_skill: str, new_domain: str) -> str | None:
    90	    """Return 'clear' if cross-domain transition detected; else None.
    91	
    92	    The %-utilization branch ('compact' recommendation) is NOT detectable from
    93	    inside a skill — the harness doesn't expose context size. That side lives
    94	    in the rule prose (context-budget-rule in CLAUDE.md) which the orchestrator
    95	    self-monitors. This helper handles only the local-state half.
    96	    """
    97	    last = last_skill_invocation(repo_root)
    98	    if last is None:
    99	        return None
   100	    prev_domain = last.get("domain")
   101	    if prev_domain and prev_domain != new_domain:
   102	        return "clear"
   103	    return None

[35m[3mexec[0m[0m
[1m/bin/bash -lc "rg -n \"compact_checkpoint|compact-checkpoint|resume_cmd|last_skill_invocation\\(|context_budget_hint\\(\" /home/renmark/projects/ai-system/renmark /home/renmark/projects/ai-system/plugin -S"[0m in /home/renmark/projects/ai-system
[32m succeeded in 0ms:[0m
/home/renmark/projects/ai-system/renmark/state/skills.py:23:def context_budget_hint(tokens: int) -> str | None:
/home/renmark/projects/ai-system/renmark/state/skills.py:74:def last_skill_invocation(repo_root: str | Path) -> dict[str, Any] | None:
/home/renmark/projects/ai-system/renmark/state/skills.py:97:    last = last_skill_invocation(repo_root)
/home/renmark/projects/ai-system/plugin/skills/_shared/context-taxonomy.md:91:**Clear gate (Python-enforced):** `skill_preamble` → `context_budget_check` returns `"clear"` on cross-domain transition → `persist_compact_checkpoint(repo, skill, reason="clear")` called → returns `CONTEXT_GATE_CLEAR:`-prefixed string → CLAUDE.md rule triggers `AskUserQuestion` menu. Bypass skills (advisory only): `finish`, `approve`, `resume`.
/home/renmark/projects/ai-system/plugin/skills/_shared/context-taxonomy.md:93:**Compact gate (rule-enforced):** Threshold in `config.json["compact_gate_tokens"]` (default 120k, 0 = disabled). CLI helper: `renmark-execute --compact-checkpoint`. Enforced by CLAUDE.md rule at ≥120k tokens; Python provides persist + CLI helper only. Python cannot detect real % context utilization — absolute token count is the proxy.
/home/renmark/projects/ai-system/plugin/skills/_shared/context-taxonomy.md:95:**`persist_compact_checkpoint(repo, skill, reason)`:** writes `.renmark/state/compact_checkpoint.json` with `{skill, reason, resume_cmd: "/renmark:resume", timestamp}`. Consumed by `/renmark:resume` for state recovery. Never raises.
/home/renmark/projects/ai-system/renmark/lifecycle.py:602:def persist_compact_checkpoint(
/home/renmark/projects/ai-system/renmark/lifecycle.py:605:    """Write a compact checkpoint to .renmark/state/compact_checkpoint.json.
/home/renmark/projects/ai-system/renmark/lifecycle.py:614:        state_path = _state.state_dir(repo) / "compact_checkpoint.json"
/home/renmark/projects/ai-system/renmark/lifecycle.py:621:                    "resume_cmd": "/renmark:resume",
/home/renmark/projects/ai-system/renmark/lifecycle.py:668:    last = _state.last_skill_invocation(repo)
/home/renmark/projects/ai-system/renmark/lifecycle.py:682:            persist_compact_checkpoint(repo, skill, reason="clear")
/home/renmark/projects/ai-system/renmark/lifecycle.py:686:                "State persisted to .renmark/state/compact_checkpoint.json.\n"
/home/renmark/projects/ai-system/renmark/cli/_engine.py:1268:        "--compact-checkpoint",
/home/renmark/projects/ai-system/renmark/cli/_engine.py:1271:            "persist resume state to .renmark/state/compact_checkpoint.json "
/home/renmark/projects/ai-system/renmark/cli/_engine.py:1385:    if args.compact_checkpoint:
/home/renmark/projects/ai-system/renmark/cli/_engine.py:1387:        _lifecycle.persist_compact_checkpoint(repo, skill="manual", reason="compact")
/home/renmark/projects/ai-system/renmark/cli/_engine.py:1388:        print("Compact checkpoint written to .renmark/state/compact_checkpoint.json.")

[35m[3mexec[0m[0m
[1m/bin/bash -lc "python3 - <<'PY'
from pathlib import Path
from itertools import islice
p=Path('/home/renmark/projects/ai-system/tests/test_lifecycle.py')
for i,line in enumerate(p.read_text().splitlines(),1):
    if 'CONTEXT_GATE_CLEAR' in line or 'persist_compact_checkpoint' in line or 'bypass' in line:
        print(i, line)
PY"[0m in /home/renmark/projects/ai-system
[32m succeeded in 0ms:[0m
310     IS declared, so the tier hint must fire; this is not a bypass."""
327     # Cross-domain gate takes priority — returns CONTEXT_GATE_CLEAR early; tier
330     assert hint.startswith("CONTEXT_GATE_CLEAR:")

[35m[3mexec[0m[0m
[1m/bin/bash -lc "python3 - <<'PY'
from renmark.lifecycle import PREAMBLE_TIER_BY_SKILL
print({k:v for k,v in PREAMBLE_TIER_BY_SKILL.items() if v=='minimal'})
PY"[0m in /home/renmark/projects/ai-system
[32m succeeded in 0ms:[0m
{'resume': 'minimal', 'help': 'minimal', 'guide': 'minimal', 'doctor': 'minimal', 'usage': 'minimal', 'analytics': 'minimal', 'approve': 'minimal', 'hygiene': 'minimal', 'check-plan': 'minimal'}

[35m[3mcodex[0m[0m
I found a state-management issue in the clear gate path and I’m checking one more interaction: whether the new blocking gate accidentally bypasses headless-mode suppression, which would break non-interactive runs.
[35m[3mexec[0m[0m
[1m/bin/bash -lc 'rg -n "def _with_headless_note|headless" /home/renmark/projects/ai-system/renmark/lifecycle.py /home/renmark/projects/ai-system/renmark -S'[0m in /home/renmark/projects/ai-system
[32m succeeded in 0ms:[0m
/home/renmark/projects/ai-system/renmark/lifecycle.py:663:        return _with_agency_note(repo, skill, _with_headless_note(repo, None))
/home/renmark/projects/ai-system/renmark/lifecycle.py:711:    return _with_agency_note(repo, skill, _with_mode_note(repo, skill, _with_headless_note(repo, base)))
/home/renmark/projects/ai-system/renmark/lifecycle.py:759:    Runs strictly AFTER the existing tier logic and the headless note — never
/home/renmark/projects/ai-system/renmark/lifecycle.py:819:    :func:`_with_headless_note`.
/home/renmark/projects/ai-system/renmark/lifecycle.py:840:def _with_headless_note(repo: Path | str, hint: str | None) -> str | None:
/home/renmark/projects/ai-system/renmark/lifecycle.py:841:    """ADDITIVE: append a headless-mode note to ``hint`` when headless is active.
/home/renmark/projects/ai-system/renmark/lifecycle.py:844:    record-before-check invariant above. When headless is off this is a
/home/renmark/projects/ai-system/renmark/lifecycle.py:852:        if not _config.is_headless(repo):
/home/renmark/projects/ai-system/renmark/lifecycle.py:854:        note = f"headless mode active (source: {_config.headless_source(repo)})"
/home/renmark/projects/ai-system/renmark/lifecycle.py:869:# ── Headless human-review gate (P10) ──────────────────────────────────────────
/home/renmark/projects/ai-system/renmark/lifecycle.py:879:    """Halt a headless run at a human-approval gate (P10 headless contract).
/home/renmark/projects/ai-system/renmark/lifecycle.py:881:    A headless executor cannot prompt; instead of silently proceeding through a
/home/renmark/projects/ai-system/renmark/lifecycle.py:930:        "mode": "headless",
/home/renmark/projects/ai-system/renmark/scan.py:1003:        "# If you instead trigger via the headless model, you may add the\n"
/home/renmark/projects/ai-system/renmark/scan.py:1018:        "#   The model-driven path also needs a one-time headless auth token\n"
/home/renmark/projects/ai-system/renmark/headless.py:1:"""Deterministic gate-resolution helper bridging the P10 headless contract to
/home/renmark/projects/ai-system/renmark/headless.py:2:skills (single source of truth: ``plugin/skills/_shared/headless-contract.md``).
/home/renmark/projects/ai-system/renmark/headless.py:9:- **headless + safe gate** — auto-pick the recommended option and continue.
/home/renmark/projects/ai-system/renmark/headless.py:10:- **headless + dangerous gate (or any unrecognised kind — fail safe)** — halt
/home/renmark/projects/ai-system/renmark/headless.py:45:    Detection precedence (headless-contract.md §1): :func:`config.is_headless`
/home/renmark/projects/ai-system/renmark/headless.py:49:    subagent's tool list), the run is forced headless — a picker that can never
/home/renmark/projects/ai-system/renmark/headless.py:51:    or ``True`` (present) does not force headless.
/home/renmark/projects/ai-system/renmark/headless.py:57:    - **headless + safe** → success envelope with
/home/renmark/projects/ai-system/renmark/headless.py:59:    - **headless + dangerous** (or any other / unrecognised *kind* — uncertainty
/home/renmark/projects/ai-system/renmark/headless.py:66:    headless = config.is_headless(repo)
/home/renmark/projects/ai-system/renmark/headless.py:67:    # Layer-4 fallback: a forced-interactive verdict still degrades to headless
/home/renmark/projects/ai-system/renmark/headless.py:70:    if not headless and tool_available is False:
/home/renmark/projects/ai-system/renmark/headless.py:71:        headless = True
/home/renmark/projects/ai-system/renmark/headless.py:73:    if not headless:
/home/renmark/projects/ai-system/renmark/headless.py:79:            "mode": "headless",
/home/renmark/projects/ai-system/renmark/headless.py:103:    renders its own menu and there is no headless prose to emit. Never raises.
/home/renmark/projects/ai-system/renmark/headless.py:118:        return f"needs input: {gate} approval required; headless mode cannot approve {gate}"
/home/renmark/projects/ai-system/renmark/cli/_engine.py:1069:      deterministic FAIL prints a cost-noted OFFER (never auto-spends) unless headless.
/home/renmark/projects/ai-system/renmark/cli/_engine.py:1079:    headless = _config.is_headless(repo)
/home/renmark/projects/ai-system/renmark/cli/_engine.py:1111:        if r.judge_offered and not (judge or headless or judge_unavailable):
/home/renmark/projects/ai-system/renmark/cli/_engine.py:1235:    # P10 — persisted headless toggle
/home/renmark/projects/ai-system/renmark/cli/_engine.py:1237:        "--set-headless",
/home/renmark/projects/ai-system/renmark/cli/_engine.py:1240:            "persist the headless-mode flag to .renmark/config.json "
/home/renmark/projects/ai-system/renmark/cli/_engine.py:1342:    if args.set_headless is not None:
/home/renmark/projects/ai-system/renmark/cli/_engine.py:1343:        raw = args.set_headless.strip().lower()
/home/renmark/projects/ai-system/renmark/cli/_engine.py:1345:            ap.error("--set-headless expects 'true' or 'false'")
/home/renmark/projects/ai-system/renmark/cli/_engine.py:1348:        _config.set_headless(repo, value)
/home/renmark/projects/ai-system/renmark/cli/_engine.py:1350:        print(f"renmark: headless mode {state_str} ({repo}/.renmark/config.json)")
/home/renmark/projects/ai-system/renmark/cli/_engine.py:1413:            "--set-proactive / --set-headless / --set-mode / --get-mode / --clear-mode"
/home/renmark/projects/ai-system/renmark/lifecycle.py:663:        return _with_agency_note(repo, skill, _with_headless_note(repo, None))
/home/renmark/projects/ai-system/renmark/lifecycle.py:711:    return _with_agency_note(repo, skill, _with_mode_note(repo, skill, _with_headless_note(repo, base)))
/home/renmark/projects/ai-system/renmark/lifecycle.py:759:    Runs strictly AFTER the existing tier logic and the headless note — never
/home/renmark/projects/ai-system/renmark/lifecycle.py:819:    :func:`_with_headless_note`.
/home/renmark/projects/ai-system/renmark/lifecycle.py:840:def _with_headless_note(repo: Path | str, hint: str | None) -> str | None:
/home/renmark/projects/ai-system/renmark/lifecycle.py:841:    """ADDITIVE: append a headless-mode note to ``hint`` when headless is active.
/home/renmark/projects/ai-system/renmark/lifecycle.py:844:    record-before-check invariant above. When headless is off this is a
/home/renmark/projects/ai-system/renmark/lifecycle.py:852:        if not _config.is_headless(repo):
/home/renmark/projects/ai-system/renmark/lifecycle.py:854:        note = f"headless mode active (source: {_config.headless_source(repo)})"
/home/renmark/projects/ai-system/renmark/lifecycle.py:869:# ── Headless human-review gate (P10) ──────────────────────────────────────────
/home/renmark/projects/ai-system/renmark/lifecycle.py:879:    """Halt a headless run at a human-approval gate (P10 headless contract).
/home/renmark/projects/ai-system/renmark/lifecycle.py:881:    A headless executor cannot prompt; instead of silently proceeding through a
/home/renmark/projects/ai-system/renmark/lifecycle.py:930:        "mode": "headless",
/home/renmark/projects/ai-system/renmark/context.py:180:    "headless-contract",
/home/renmark/projects/ai-system/renmark/browser_cli.py:165:            launched = playwright.chromium.launch(headless=False)
/home/renmark/projects/ai-system/renmark/config.py:23:_ENV_HEADLESS = "RENMARK_HEADLESS"
/home/renmark/projects/ai-system/renmark/config.py:86:def _env_headless() -> bool | None:
/home/renmark/projects/ai-system/renmark/config.py:87:    """Parse ``RENMARK_HEADLESS`` (case-insensitive) into a tri-state.
/home/renmark/projects/ai-system/renmark/config.py:93:    raw = os.environ.get(_ENV_HEADLESS)
/home/renmark/projects/ai-system/renmark/config.py:104:def is_headless(repo: str | Path) -> bool:
/home/renmark/projects/ai-system/renmark/config.py:105:    """Return whether renmark should run in headless (non-interactive) mode.
/home/renmark/projects/ai-system/renmark/config.py:108:    1. env ``RENMARK_HEADLESS`` (``1/true/yes/on`` → ``True``,
/home/renmark/projects/ai-system/renmark/config.py:110:    2. else the ``.renmark/config.json`` key ``"headless"`` if present **and a
/home/renmark/projects/ai-system/renmark/config.py:111:       real ``bool``** (a non-bool value is ignored, not coerced — headless is
/home/renmark/projects/ai-system/renmark/config.py:117:    env = _env_headless()
/home/renmark/projects/ai-system/renmark/config.py:120:    val = _read_raw(repo).get("headless")
/home/renmark/projects/ai-system/renmark/config.py:126:def set_headless(repo: str | Path, value: bool) -> None:
/home/renmark/projects/ai-system/renmark/config.py:127:    """Persist the headless flag for the project at *repo*.
/home/renmark/projects/ai-system/renmark/config.py:129:    Reads the existing config, updates only the ``headless`` key, and writes
/home/renmark/projects/ai-system/renmark/config.py:133:    data["headless"] = value
/home/renmark/projects/ai-system/renmark/config.py:137:def headless_source(repo: str | Path) -> str:
/home/renmark/projects/ai-system/renmark/config.py:138:    """Return which layer decided :func:`is_headless` for *repo*.
/home/renmark/projects/ai-system/renmark/config.py:140:    ``"env"`` if ``RENMARK_HEADLESS`` resolved the value, ``"config"`` if the
/home/renmark/projects/ai-system/renmark/config.py:141:    config file's ``"headless"`` key did (i.e. it is a real ``bool``), else
/home/renmark/projects/ai-system/renmark/config.py:142:    ``"default"``.  Uses the same precedence as :func:`is_headless` so the two
/home/renmark/projects/ai-system/renmark/config.py:146:    if _env_headless() is not None:
/home/renmark/projects/ai-system/renmark/config.py:148:    if isinstance(_read_raw(repo).get("headless"), bool):

[35m[3mexec[0m[0m
[1m/bin/bash -lc "nl -ba /home/renmark/projects/ai-system/renmark/lifecycle.py | sed -n '780,860p'"[0m in /home/renmark/projects/ai-system
[32m succeeded in 0ms:[0m
   780	    except Exception:
   781	        return hint
   782	    return line if hint is None else f"{hint} | {line}"
   783	
   784	
   785	# Pipeline skills that receive an Agency Mode hint when agency is active.
   786	# v0.30.0 shipped the spine (start/prd/roadmap/finish/resume); the fast-follow
   787	# extends coverage to the remaining pipelines (feature/plan/orchestrate/verify/
   788	# codereview) so the whole delivery loop is agency-aware.
   789	_AGENCY_AWARE_SKILLS: frozenset[str] = frozenset(
   790	    {
   791	        "start", "prd", "roadmap", "finish", "resume",
   792	        "feature", "plan", "orchestrate", "verify", "codereview",
   793	    }
   794	)
   795	
   796	# Back-compat alias: the original spine-only name still resolves.
   797	_AGENCY_SPINE_SKILLS: frozenset[str] = _AGENCY_AWARE_SKILLS
   798	
   799	# Marker string used to identify the agency hint line — load-bearing for
   800	# behavior tests (T15): assert active preamble contains this prefix and
   801	# inactive preamble does NOT.
   802	_AGENCY_HINT_MARKER: str = "Agency Mode active"
   803	
   804	
   805	def _with_agency_note(repo: Path | str, skill: str, hint: str | None) -> str | None:
   806	    """ADDITIVE: append an Agency Mode hint to ``hint`` when agency is active.
   807	
   808	    Only surfaces the hint for AGENCY-AWARE pipeline skills (the spine —
   809	    start/prd/roadmap/finish/resume — plus feature/plan/orchestrate/verify/
   810	    codereview) — all other skills are passed through unchanged regardless of
   811	    agency state.
   812	
   813	    When agency is INACTIVE the return value is byte-identical to ``hint``
   814	    (the inactive-path guarantee). DEGRADES GRACEFULLY: any exception in
   815	    agency/context resolution falls back to ``hint`` unchanged, so agency is
   816	    a pure enhancement and never a hard dependency of the preamble.
   817	
   818	    Follows the same additive pattern as :func:`_with_mode_note` and
   819	    :func:`_with_headless_note`.
   820	    """
   821	    if skill not in _AGENCY_AWARE_SKILLS:
   822	        return hint
   823	    try:
   824	        from . import agency as _agency
   825	        from . import context as _context
   826	
   827	        if not _agency.is_active(repo):
   828	            return hint
   829	        state = _agency.read_agency(repo)
   830	        pointer = _context.fragment_pointer("agency-delivery")
   831	        line = (
   832	            f"{_AGENCY_HINT_MARKER} — phase {state.current_phase}, "
   833	            f"milestone {state.current_milestone}. Contract: {pointer}"
   834	        )
   835	    except Exception:
   836	        return hint
   837	    return line if hint is None else f"{hint} | {line}"
   838	
   839	
   840	def _with_headless_note(repo: Path | str, hint: str | None) -> str | None:
   841	    """ADDITIVE: append a headless-mode note to ``hint`` when headless is active.
   842	
   843	    Runs strictly AFTER the existing tier logic — never reorders the
   844	    record-before-check invariant above. When headless is off this is a
   845	    pass-through (returns ``hint`` unchanged, including None). When on it
   846	    appends one line; if ``hint`` was None the note becomes the whole return.
   847	    Never raises — config helpers are themselves no-raise.
   848	    """
   849	    from . import config as _config
   850	
   851	    try:
   852	        if not _config.is_headless(repo):
   853	            return hint
   854	        note = f"headless mode active (source: {_config.headless_source(repo)})"
   855	    except Exception:
   856	        return hint
   857	    return note if hint is None else f"{hint} | {note}"
   858	
   859	
   860	def is_cross_domain_transition(prev_skill: str | None, new_skill: str) -> bool:

[35m[3mexec[0m[0m
[1m/bin/bash -lc 'rg -n "compact_checkpoint.json|resume" /home/renmark/projects/ai-system/renmark -S'[0m in /home/renmark/projects/ai-system
[32m succeeded in 0ms:[0m
/home/renmark/projects/ai-system/renmark/skillmeta.py:21:  the gate sub-menu), 3 = aux / terminal (resume-pipeline). See
/home/renmark/projects/ai-system/renmark/skillmeta.py:203:    "resume": SkillMeta(
/home/renmark/projects/ai-system/renmark/schemas.py:376:    ``pause_kind == "usage_limit"``: requires a non-empty ``resume_after``
/home/renmark/projects/ai-system/renmark/schemas.py:386:    resume_after = data.get("resume_after")
/home/renmark/projects/ai-system/renmark/schemas.py:387:    if not isinstance(resume_after, str):
/home/renmark/projects/ai-system/renmark/schemas.py:388:        issues.append(f"usage_pause.resume_after expected str, got {type(resume_after).__name__}")
/home/renmark/projects/ai-system/renmark/schemas.py:389:    elif not resume_after.strip():
/home/renmark/projects/ai-system/renmark/schemas.py:390:        issues.append("usage_pause.resume_after is empty")
/home/renmark/projects/ai-system/renmark/state/usage.py:52:    A replayed ledgering of the SAME attempt (e.g. a crash-resume that
/home/renmark/projects/ai-system/renmark/state/pause.py:34:    resume_after: str = ""
/home/renmark/projects/ai-system/renmark/state/pause.py:52:    resume_after: str = "",
/home/renmark/projects/ai-system/renmark/state/pause.py:75:        resume_after=resume_after,
/home/renmark/projects/ai-system/renmark/lifecycle.py:76:        "resume",
/home/renmark/projects/ai-system/renmark/lifecycle.py:124:    "resume": "minimal",
/home/renmark/projects/ai-system/renmark/lifecycle.py:169:# Class 3 — aux / terminal skills: resume-pipeline + 1–2 local actions.
/home/renmark/projects/ai-system/renmark/lifecycle.py:181:        "resume",
/home/renmark/projects/ai-system/renmark/lifecycle.py:203:# Per-skill local follow-ups for class 3 (up to 2 surfaced). Resume-pipeline is
/home/renmark/projects/ai-system/renmark/lifecycle.py:212:    "help": ["/renmark:start", "/renmark:resume"],
/home/renmark/projects/ai-system/renmark/lifecycle.py:213:    "resume": ["/renmark:start"],
/home/renmark/projects/ai-system/renmark/lifecycle.py:221:    Unknown skills default to 'aux' — the safest class (resume-pipeline floor,
/home/renmark/projects/ai-system/renmark/lifecycle.py:291:# instead of raising mid-recovery (a reader that raises is a resume-killer).
/home/renmark/projects/ai-system/renmark/lifecycle.py:402:    resume forever; also called when the user wants a clean slate. Not dead
/home/renmark/projects/ai-system/renmark/lifecycle.py:488:    - ``aux`` → ``[tier0]`` (resume-pipeline) + up to 2 per-skill local actions.
/home/renmark/projects/ai-system/renmark/lifecycle.py:605:    """Write a compact checkpoint to .renmark/state/compact_checkpoint.json.
/home/renmark/projects/ai-system/renmark/lifecycle.py:608:    user can resume after running /compact or /clear.  Never raises.
/home/renmark/projects/ai-system/renmark/lifecycle.py:614:        state_path = _state.state_dir(repo) / "compact_checkpoint.json"
/home/renmark/projects/ai-system/renmark/lifecycle.py:621:                    "resume_cmd": "/renmark:resume",
/home/renmark/projects/ai-system/renmark/lifecycle.py:675:            # Advisory only — finish/approve/resume must not be blocked mid-flow.
/home/renmark/projects/ai-system/renmark/lifecycle.py:686:                "State persisted to .renmark/state/compact_checkpoint.json.\n"
/home/renmark/projects/ai-system/renmark/lifecycle.py:692:                "    1. Stop here \u2014 I will run /clear then /renmark:resume (Recommended)\n"
/home/renmark/projects/ai-system/renmark/lifecycle.py:736:# finish/approve/resume: urgent paths that must not be interrupted halfway.
/home/renmark/projects/ai-system/renmark/lifecycle.py:737:_CONTEXT_BYPASS_SKILLS: frozenset[str] = frozenset({"finish", "approve", "resume"})
/home/renmark/projects/ai-system/renmark/lifecycle.py:786:# v0.30.0 shipped the spine (start/prd/roadmap/finish/resume); the fast-follow
/home/renmark/projects/ai-system/renmark/lifecycle.py:791:        "start", "prd", "roadmap", "finish", "resume",
/home/renmark/projects/ai-system/renmark/lifecycle.py:809:    start/prd/roadmap/finish/resume — plus feature/plan/orchestrate/verify/
/home/renmark/projects/ai-system/renmark/cli/_engine.py:329:    """Validate the resume skip-list against the CURRENT plan's task set.
/home/renmark/projects/ai-system/renmark/cli/_engine.py:405:    resume: bool = False,
/home/renmark/projects/ai-system/renmark/cli/_engine.py:429:    # Determine which tasks are already done (resume support).
/home/renmark/projects/ai-system/renmark/cli/_engine.py:431:    if resume:
/home/renmark/projects/ai-system/renmark/cli/_engine.py:579:        # Already-committed tasks (from --resume) just emit DONE lines.
/home/renmark/projects/ai-system/renmark/cli/_engine.py:719:            f"Resume with: renmark-execute --resume {plan_path}"
/home/renmark/projects/ai-system/renmark/cli/_engine.py:754:        f"Resume with: renmark-execute --resume {plan_path}"
/home/renmark/projects/ai-system/renmark/cli/_engine.py:1174:    ap.add_argument("--resume", action="store_true", help="resume a paused run")
/home/renmark/projects/ai-system/renmark/cli/_engine.py:1271:            "persist resume state to .renmark/state/compact_checkpoint.json "
/home/renmark/projects/ai-system/renmark/cli/_engine.py:1388:        print("Compact checkpoint written to .renmark/state/compact_checkpoint.json.")
/home/renmark/projects/ai-system/renmark/cli/_engine.py:1391:        print("  2. /renmark:resume")
/home/renmark/projects/ai-system/renmark/cli/_engine.py:1418:        resume=args.resume,
/home/renmark/projects/ai-system/renmark/__init__.py:11:`pipeline.json`. After `/clear`, `/renmark:resume` recovers in one file
/home/renmark/projects/ai-system/renmark/program.py:26:  corruption as "no program" would let ``/renmark:resume`` skip a real
/home/renmark/projects/ai-system/renmark/program.py:185:    ``/renmark:resume`` skip a real in-flight program and would hide broken
/home/renmark/projects/ai-system/renmark/usage.py:159:        "resume_after": pause.resume_after,
/home/renmark/projects/ai-system/renmark/usage.py:268:def _compute_resume_after(
/home/renmark/projects/ai-system/renmark/usage.py:277:    """Resume_after rule — ALWAYS a non-empty, timezone-aware ISO timestamp.
/home/renmark/projects/ai-system/renmark/usage.py:305:                resume = earliest + dt.timedelta(seconds=_FIVE_HOURS)
/home/renmark/projects/ai-system/renmark/usage.py:307:                return (resume if resume > base else base + dt.timedelta(seconds=_FIVE_HOURS)).isoformat()
/home/renmark/projects/ai-system/renmark/usage.py:329:    """Build a usage-limit PauseState with resume_after by the fallback rule.
/home/renmark/projects/ai-system/renmark/usage.py:331:    No polling, no retry scheduling — purely computes resume_after and delegates
/home/renmark/projects/ai-system/renmark/usage.py:336:    resume_after = _compute_resume_after(
/home/renmark/projects/ai-system/renmark/usage.py:353:        resume_after=resume_after,
/home/renmark/projects/ai-system/renmark/usage.py:420:        lines.append(f"- suggested resume after: {paused.get('resume_after', '')}")
/home/renmark/projects/ai-system/renmark/program_driver.py:165:    1. **Resume active work first.** If any stage is ``in_progress``, return it —
/home/renmark/projects/ai-system/renmark/program_driver.py:177:    # 1. Resume an actively in-progress stage before starting anything new.
/home/renmark/projects/ai-system/renmark/analytics.py:67:        "resume",
/home/renmark/projects/ai-system/renmark/analytics.py:147:    ``"resume"``, ``"rate_limit"``, ``"quota"``, ``"release"``,
/home/renmark/projects/ai-system/renmark/analytics.py:555:    """Roll up events.jsonl by ``kind`` — pause/resume/rate-limit/quota/backlog/release counts."""
/home/renmark/projects/ai-system/renmark/analytics.py:568:        "resume_events": kind_c.get("resume", 0),
/home/renmark/projects/ai-system/renmark/analytics.py:673:            "resume": events["resume_events"],

[35m[3mcodex[0m[0m
━━ VERDICT 1 — Spec-compliance ━━

Spec: `under-built` — the compact threshold is only stored, not consumed anywhere; the cross-domain gate still does not apply to non-bypass `minimal` skills; `AGENTS.md` does not mirror the full required AskUserQuestion contract; and the four lifecycle tests requested by the plan were not added. `over-built` — negative-value rejection/no-op behavior was added for `set_compact_gate_tokens`, which the spec did not require.

━━ VERDICT 2 — Code-quality ━━

Counts: `Major 3 | Minor 3 | Nit 0`

- `renmark/lifecycle.py:670` | Major | `record_skill_invocation()` runs before the blocking clear gate returns. If the user chooses Stop/Queue/Cancel, renmark still records the gated skill as the “last skill” even though it never ran, which can suppress or misclassify the next cross-domain gate. | Fix: do not record the new invocation until the user explicitly continues or the skill body actually starts.

- `renmark/lifecycle.py:683` | Major | The new `CONTEXT_GATE_CLEAR` early return bypasses `_with_headless_note()` and any headless-resolution path. In headless mode, this produces an interactive AskUserQuestion requirement that non-interactive runs cannot satisfy. | Fix: route the clear-gate path through the existing headless gate machinery, or explicitly short-circuit to a non-interactive halt/resume instruction when `is_headless(repo)` is true.

- `renmark/config.py:216` | Major | `compact_gate_tokens()` is dead configuration: the new value is persisted, but no runtime threshold logic reads it. In practice, `--set-compact-gate-tokens 0` does not disable anything executable. | Fix: plumb this value into the code path that decides the 120k compact behavior, or remove the configurability claim until a consumer exists.

- `renmark/cli/_engine.py:1393` | Minor | Negative values are described as “rejected”, but the CLI always prints success and exits 0 even though `set_compact_gate_tokens()` silently no-ops on negatives. | Fix: validate in the CLI and fail with a non-zero exit, or have the setter report whether it actually persisted.

- `renmark/lifecycle.py:659` | Minor | Non-bypass `minimal` skills still skip `context_budget_check()` entirely, so the new cross-domain clear gate does not fire for `help`, `guide`, `doctor`, `usage`, `analytics`, `hygiene`, or `check-plan`, despite the spec only exempting `finish/approve/resume`. | Fix: run the clear-gate check for minimal-tier skills too, or explicitly expand/document the bypass set.

- `tests/test_lifecycle.py:320` | Minor | The spec called for four new lifecycle tests, but the diff only rewrites one existing assertion. The new gate prefix, bypass behavior, checkpoint write, and never-raises semantics are largely unguarded. | Fix: add the four tests listed in the plan.
[2mtokens used[0m
141,085
━━ VERDICT 1 — Spec-compliance ━━

Spec: `under-built` — the compact threshold is only stored, not consumed anywhere; the cross-domain gate still does not apply to non-bypass `minimal` skills; `AGENTS.md` does not mirror the full required AskUserQuestion contract; and the four lifecycle tests requested by the plan were not added. `over-built` — negative-value rejection/no-op behavior was added for `set_compact_gate_tokens`, which the spec did not require.

━━ VERDICT 2 — Code-quality ━━

Counts: `Major 3 | Minor 3 | Nit 0`

- `renmark/lifecycle.py:670` | Major | `record_skill_invocation()` runs before the blocking clear gate returns. If the user chooses Stop/Queue/Cancel, renmark still records the gated skill as the “last skill” even though it never ran, which can suppress or misclassify the next cross-domain gate. | Fix: do not record the new invocation until the user explicitly continues or the skill body actually starts.

- `renmark/lifecycle.py:683` | Major | The new `CONTEXT_GATE_CLEAR` early return bypasses `_with_headless_note()` and any headless-resolution path. In headless mode, this produces an interactive AskUserQuestion requirement that non-interactive runs cannot satisfy. | Fix: route the clear-gate path through the existing headless gate machinery, or explicitly short-circuit to a non-interactive halt/resume instruction when `is_headless(repo)` is true.

- `renmark/config.py:216` | Major | `compact_gate_tokens()` is dead configuration: the new value is persisted, but no runtime threshold logic reads it. In practice, `--set-compact-gate-tokens 0` does not disable anything executable. | Fix: plumb this value into the code path that decides the 120k compact behavior, or remove the configurability claim until a consumer exists.

- `renmark/cli/_engine.py:1393` | Minor | Negative values are described as “rejected”, but the CLI always prints success and exits 0 even though `set_compact_gate_tokens()` silently no-ops on negatives. | Fix: validate in the CLI and fail with a non-zero exit, or have the setter report whether it actually persisted.

- `renmark/lifecycle.py:659` | Minor | Non-bypass `minimal` skills still skip `context_budget_check()` entirely, so the new cross-domain clear gate does not fire for `help`, `guide`, `doctor`, `usage`, `analytics`, `hygiene`, or `check-plan`, despite the spec only exempting `finish/approve/resume`. | Fix: run the clear-gate check for minimal-tier skills too, or explicitly expand/document the bypass set.

- `tests/test_lifecycle.py:320` | Minor | The spec called for four new lifecycle tests, but the diff only rewrites one existing assertion. The new gate prefix, bypass behavior, checkpoint write, and never-raises semantics are largely unguarded. | Fix: add the four tests listed in the plan.
