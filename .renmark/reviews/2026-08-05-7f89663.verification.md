---
artifact_type: verification
schema_version: 1
created_at: 2026-08-05T23:10:57+00:00
source_sha: 7f89663
related_plan: .renmark/plans/2026-08-05-governed-orchestration-assurance-release-13.plan.md
generator: sonnet
stale_after: null
dependency_refs: []
completion_state: complete
confidence: high
validation_status: validated
retry_count: 0
parser_success: true
schema_compliance: true
---

## Goal
Release 13 (Durable-events field completeness + orphan-detection spike #24 +
analytics reconciliation): (a) bounded resume/recovery self-benchmark spike
with a scenario table and a capped, committed fix for whatever gap it found;
(b) additive `schema_version`/`attempt_id`/`correlation_id` fields on the 4
ledger event kinds; (c) analytics.py reads ledger.py as its reconciled source
for guardrail metrics instead of a second parallel event system.

## Bugs cross-referenced
No open bug in `.renmark/memory/bugs.md` overlaps this release's touched files
(`renmark/ledger.py`, `renmark/analytics.py`, `renmark/state/commits.py`,
`renmark/cli/_engine.py`, `renmark/cli/_run_lifecycle.py`). Finding A (the
resume skip-list identity gap the spike surfaced) was fixed in-release, not
logged as a deferred bug.

## Smoke checks (fresh run, this pass)
1. `pytest -q tests/test_engine_resume_crosscheck.py` -- 15 passed (Finding A
   fix: title+index skip-list identity check).
2. `pytest -q tests/test_ledger_field_completeness.py` -- 11 passed (new
   `schema_version`/`attempt_id`/`correlation_id` fields present on
   WorkOrder/WorkResult/InspectionReport/Escalation).
3. `pytest -q tests/test_analytics_ledger_guardrails.py` -- 3 passed
   (`_agg_ledger_guardrails` wired into `aggregate()`'s `summary["guardrails"]`).
4. `pytest -q tests/test_ledger.py tests/test_reports_analytics.py` -- 39
   passed (backward-compat: pre-existing ledger/analytics behavior unchanged).
5. Spike finding document present at
   `.renmark/rethink/governed-orchestration-assurance/release-13-orphan-detection-spike-finding.md`.
6. Full suite: `pytest -q` -- 2080 passed, 32 skipped in 90.13s.

## Requirements verified: 3/3
- (a) spike + capped committed fix for Finding A: verified (skip-list identity
  bug reproduced, fixed, regression-tested).
- (b) additive ledger event fields: verified (field-completeness test green,
  compat suite green).
- (c) analytics reconciliation via ledger: verified (guardrails aggregation
  test green).

## Deferred / open (not blocking this release)
- Spike Findings B/C (no pre-flight working-tree cleanup on `--resume`;
  ledger never consulted during resume) -- logged in `bugs.md` as
  deferrable, out of this release's capped scope.
- Release 11's CLI-wiring gap (`_wave_loop.py`/`_engine.py` don't yet call
  the new `group_tasks_by_wave` scheduling params) -- still open, unrelated
  to Release 13.

## Summary

- verify (smoke): Release 13 durable-events + orphan-detection spike + analytics reconciliation -- 3/3 requirements verified
- Finding A (resume skip-list identity bug) reproduced, fixed, regression-tested
- Full suite: 2080 passed, 32 skipped
