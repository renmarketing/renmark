Summary: `Critical 0 | Major 5 | Minor 0 | Nit 0`

- `plugin/skills/verify/SKILL.md:15`
  - `severity: Major`
  - The top-level mode table still says QA is "via Chrome DevTools MCP," which contradicts the new dual-channel logic below and can send an agent down the wrong path before it reaches the applicability gate.
  - Fix: change the overview row to say QA uses the selected live-browser channel (`Chrome DevTools MCP` or native `claude --chrome`) and scrub remaining MCP-only summaries to match.

- `plugin/skills/verify/SKILL.md:213`
  - `severity: Major`
  - "Native channel connected? ... active/reachable" is not operationally defined, so the first-match decision tree is not actually executable for the native branch the way `list_pages` is for MCP.
  - Fix: specify an explicit native-channel probe/command or tighten the rule to a concrete detectable condition and state what to do if that probe fails.

- `plugin/skills/verify/SKILL.md:216`
  - `severity: Major`
  - The fallback hint is keyed to broad environment buckets (`WSL / CLI` vs `Windows / desktop app`) rather than the selected channel, so a plain CLI session that explicitly chose native `claude --chrome` can be told to install MCP instead of reconnecting the native channel.
  - Fix: make fallback messaging branch on the selected channel first, with platform-specific wording only as a secondary detail.

- `plugin/skills/verify/SKILL.md:217`
  - `severity: Major`
  - The documented MCP install command is malformed for the local `claude` CLI syntax because stdio subprocess commands must be introduced with `--`, so this hint is likely to fail as written.
  - Fix: replace it with `claude mcp add --scope user chrome-devtools -- npx chrome-devtools-mcp@latest`.

- `plugin/skills/verify/SKILL.md:218`
  - `severity: Major`
  - The instruction to "run `/chrome` to reconnect" points to a command that does not exist in this repo's `plugin/commands`, so the recovery path is factually wrong and will fail if followed.
  - Fix: remove `/chrome` unless a real command is added, and point users only to the actual reconnect path such as relaunching with `claude --chrome`.
