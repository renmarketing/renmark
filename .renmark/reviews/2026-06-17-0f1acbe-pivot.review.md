Summary: 2 Critical, 2 Major, 0 Minor, 0 Nit.

- Critical — [renmark/scan.py](/home/renmark/projects/ai-system/renmark/scan.py:71), [renmark/scan.py](/home/renmark/projects/ai-system/renmark/scan.py:232), [renmark/verifier.py](/home/renmark/projects/ai-system/renmark/verifier.py:43): The “structural read-only” claim is false because the scheduled path still executes unrestricted `bash -c` verifier subprocesses (`pytest -q`, `ruff check`, `mypy .`), and at least `pytest` and `mypy` can run repository-controlled code/plugins that mutate files, git state, or external systems. Fix: remove shell verifiers from the trusted scheduled path or run them inside an actually read-only sandbox/container/filesystem mount instead of plain `bash -c`.

- Critical — [renmark/scan.py](/home/renmark/projects/ai-system/renmark/scan.py:630), [renmark/backlog.py](/home/renmark/projects/ai-system/renmark/backlog.py:165), [renmark/scan.py](/home/renmark/projects/ai-system/renmark/scan.py:876): The write-path inventory in the pivot text is also false as written, because proposing findings does more than “write the report, write the ledger, and call `backlog.write_item`” — it also creates reservation files via `backlog.next_id()` and may delete them via `_rollback_reserved()`. Fix: either narrow the claim to “no product-file/git mutation” and explicitly document reservation create/delete side effects, or redesign proposal allocation so no hidden placeholder write/unlink happens.

- Major — [renmark/scan.py](/home/renmark/projects/ai-system/renmark/scan.py:435): `_report_rel_path()` is not collision-free; `os.urandom(4)` is used correctly, but after hashing/truncation to 8 hex chars the path still has only 32 bits of uniqueness and no `O_EXCL`/retry loop, so same-second overwrites remain possible in theory. Fix: generate the filename with a full-width unique token (`uuid4`, larger nonce) and/or create the report atomically with existence-check retry.

- Major — [renmark/scan.py](/home/renmark/projects/ai-system/renmark/scan.py:659): `_rollback_reserved()` can still delete another writer’s real item because its ownership check treats any item with empty `title/source/summary/recommended_action/created_at/evidence_path` as “our placeholder” even if other fields like `risk`, `updated_at`, `served_requirements`, `pending_decision`, `branch`, `loop_id`, or `disposition` were populated. Fix: compare against the exact placeholder payload or add an explicit reservation marker/token and only unlink when that exact marker is still present.

Other checks: `_ledger_lock()` does surface all three degraded paths (`ImportError`, lock-file open failure, `flock` failure) and closes/releases correctly on exception; I did not find a lock leak. `audit.run_audit()` itself appears read-only in this path. `cmd_scan` flag-gating and `--emit-cron` rendering match the new pivot text, but that text currently overstates the actual trust boundary for the reasons above.
