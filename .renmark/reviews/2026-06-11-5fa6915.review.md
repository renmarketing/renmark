---
artifact_type: review
schema_version: 1
created_at: 2026-06-11T14:39:39+00:00
source_sha: 5fa691567602df5b5aa65955ae4009e8341c19d2
related_plan: .renmark/plans/2026-06-10-fable-integration-part1.plan.md
generator: codex
stale_after: null
dependency_refs: []
completion_state: complete
confidence: medium
validation_status: validated
retry_count: 0
parser_success: true
schema_compliance: true
---

**Summary**

- Critical: 0
- Major: 2
- Minor: 2
- Nit: 0

1. `renmark/roadmap.py:185`
   - Severity: Major
   - `_estimate_cost()` never matches `fable`, so any roadmap row ledgered with a fable model string is billed as `0.0` even though `COST_PER_KT["fable"]` was added.
   - Fix suggestion: add a `fable` branch in `_estimate_cost()` or, better, centralize model-to-rate resolution so roadmap cost calculation uses the same executor table everywhere.

2. `renmark/cli/_engine.py:400`
   - Severity: Major
   - The dry-run estimator’s local `cost_per_kt` table still stops at `opus`, so a `fable` task with no explicit `est_cost_usd` is shown as `free` and omitted from the total estimate.
   - Fix suggestion: add `"fable": 0.030` here and update the quota note, or replace this inline table with a shared source of truth from `roadmap.COST_PER_KT`.

3. `plugin/skills/plan/SKILL.md:3`
   - Severity: Minor
   - The planner skill still documents a four-tier executor set and lacks the fable routing/cost guidance, so `/renmark:plan` is still primed to omit or mis-price the new tier despite runtime support.
   - Fix suggestion: apply the planned part-2 sync here: add fable to the description, executor field list, routing table, and cost table, with the escalation-only role spelled out.

4. `plugin/skills/orchestrate/SKILL.md:15`
   - Severity: Minor
   - The orchestrate skill still says only `haiku`, `sonnet`, and `opus` use Agent dispatch, which is now inconsistent with runtime behavior where `fable` is also a Claude executor and needs an explicit model override.
   - Fix suggestion: add a dedicated `fable` dispatch row and update the surrounding executor enumerations so the operator guidance matches the actual dispatch path.

Checked but not flagged: `renmark/debug.py`, `renmark/schemas.py` `LIMITS_PROVIDERS`, `renmark/usage.py` provider loops, and `renmark/sizing.py` all match the plan’s stated out-of-scope.
tokens used
59,560
**Summary**

- Critical: 0
- Major: 2
- Minor: 2
- Nit: 0

1. `renmark/roadmap.py:185`
   - Severity: Major
   - `_estimate_cost()` never matches `fable`, so any roadmap row ledgered with a fable model string is billed as `0.0` even though `COST_PER_KT["fable"]` was added.
   - Fix suggestion: add a `fable` branch in `_estimate_cost()` or, better, centralize model-to-rate resolution so roadmap cost calculation uses the same executor table everywhere.

2. `renmark/cli/_engine.py:400`
   - Severity: Major
   - The dry-run estimator’s local `cost_per_kt` table still stops at `opus`, so a `fable` task with no explicit `est_cost_usd` is shown as `free` and omitted from the total estimate.
   - Fix suggestion: add `"fable": 0.030` here and update the quota note, or replace this inline table with a shared source of truth from `roadmap.COST_PER_KT`.

3. `plugin/skills/plan/SKILL.md:3`
   - Severity: Minor
   - The planner skill still documents a four-tier executor set and lacks the fable routing/cost guidance, so `/renmark:plan` is still primed to omit or mis-price the new tier despite runtime support.
   - Fix suggestion: apply the planned part-2 sync here: add fable to the description, executor field list, routing table, and cost table, with the escalation-only role spelled out.

4. `plugin/skills/orchestrate/SKILL.md:15`
   - Severity: Minor
   - The orchestrate skill still says only `haiku`, `sonnet`, and `opus` use Agent dispatch, which is now inconsistent with runtime behavior where `fable` is also a Claude executor and needs an explicit model override.
   - Fix suggestion: add a dedicated `fable` dispatch row and update the surrounding executor enumerations so the operator guidance matches the actual dispatch path.

Checked but not flagged: `renmark/debug.py`, `renmark/schemas.py` `LIMITS_PROVIDERS`, `renmark/usage.py` provider loops, and `renmark/sizing.py` all match the plan’s stated out-of-scope.

## Summary

- codex full review of c0860c5..HEAD (fable-integration part 1)
- focus: default (correctness + quality)
- see Findings section for severity detail
