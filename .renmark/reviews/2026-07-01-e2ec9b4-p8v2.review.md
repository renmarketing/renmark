---
artifact_type: review
schema_version: 1
created_at: 2026-07-01T18:44:36+00:00
source_sha: e2ec9b4954e08cf09141f63718a1e28ae594f042
related_plan: .renmark/plans/2026-07-01-p8-behavioral-skill-testing-v2.plan.md
generator: codereview
stale_after: null
dependency_refs: []
completion_state: complete
confidence: high
validation_status: validated
retry_count: 0
parser_success: true
schema_compliance: true
---

# P8-v2 code review (codex, full, read-only) — 657495e..HEAD

## Verdict 1 — Spec-compliance
Spec: under-built — the eval/judge tier never executes a real live-model trajectory; the promised load-bearing proof path is not actually implemented (build_subagent_runner is a stub).

## Verdict 2 — Code-quality — Summary: Major 4, Minor 1, Nit 0

- behavior.py:546 | Major | build_subagent_runner() returns build_agent_dispatch(...).prompt (the prompt TEMPLATE), not a model response — so --accept records prompt text as goldens and --judge feeds a prompt into judge_behavior instead of judge JSON. ROOT: a pure-Python process cannot issue the Agent call; the runner needs host/agent mediation. Fix: make the Python live path explicitly unavailable (raise) until a host runner is injected, OR wire a true host-injected str->str runner.
- behavior.py:202 | Major | _case_from_dict() defaults a missing deterministic.assertions to [] → a malformed case vacuously PASSes (empty assertion set = no failures). Fix: require assertions explicitly; reject missing/non-list.
- behavior.py:423 | Major | plan_lint deterministic adapter fabricates its own dispatch-policy string and runs _check_transcript_leak on invented text — does not validate a real roadmap read-only contract. Fix: drive real lint_plan()/real artifact, or narrow the contract wording to what it actually proves.
- tests/test_behavior.py:189 | Major | tests mock judge_behavior and never exercise capture()/build_subagent_runner(), missing the broken live eval path. Fix: cover capture(), the runner factory, and CLI --accept/--judge with an injected runner stub.
- tests/behavioral/roadmap.behavior.json:6 | Minor | roadmap case proves only forbidden-tokens-absent + synthetic marker — weaker than the read-only contract. Fix: add a positive assertion tied to the real read-only surface.

Note: review by code inspection; test suite not run in the read-only session (full suite is 968 green locally).

## Summary

- Spec: under-built — eval tier live path not real (stub runner)
- Quality: 4 Major, 1 Minor, 0 Critical
- key: build_subagent_runner returns prompt text, not model output
- key: missing deterministic.assertions => vacuous PASS
- next: fix honest subset + mark eval runner host-pending
