Critical: 1 | Major: 3 | Minor: 1 | Nit: 0

renmark/doctor.py:485 | Critical | `main()` unconditionally runs `_fix_install_global_routing_rule()` whenever `--fix` is set, so any caller of `python -m renmark.doctor --fix` including the existing installer paths now mutates the user’s global `~/.claude/CLAUDE.md` even when they only intended repo/plugin registration fixes. | Move the global-routing write behind a separate explicit opt-in flag or prompt and keep plain `--fix` limited to the existing settings/registry/cache repairs.

renmark/doctor.py:490 | Major | If the global-rule install fails (permissions, unreadable file, decode error, etc.), doctor only prints `FIX FAILED` and can still exit `0` because the advisory `info` check is excluded from `ok()`, which gives false-success semantics for an explicitly requested fix. | Record global-routing fix failures separately and make `main()` return non-zero whenever the user asked for that write and it did not complete.

renmark/global_routing.py:155 | Major | The append path always writes the backup to `CLAUDE.md.bak`, which will overwrite any existing sibling backup or user-created file with that name and can clobber unrelated content outside the project. | Use a unique non-clobbering backup name (timestamp/nonce or incremented suffix) and fail safely if the chosen backup target already exists.

renmark/global_routing.py:115 | Major | Presence detection is based on `lint.iter_rule_blocks()`, which drops duplicate or unbalanced `renmark-routing` markers, so a malformed pre-existing block is treated as “missing” forever and each subsequent install appends another block instead of converging. | Detect malformed/partial `renmark-routing` markers explicitly and abort with a manual-repair signal or normalize/replace the broken block in place before appending.

renmark/global_routing.py:77 | Minor | The injectable `home` parameter is actually interpreted as the `.claude` directory itself rather than the user home directory, so a future caller using the natural meaning of “home” will write `<home>/CLAUDE.md` instead of `<home>/.claude/CLAUDE.md`. | Rename the parameter to `claude_dir` or accept a true home path and append `/.claude` internally so injected paths cannot target the wrong location.
