---
artifact_type: verification
schema_version: 1
created_at: 2026-08-06T00:30:55+00:00
source_sha: c7adc2a
related_plan: .renmark/plans/2026-08-05-governed-orchestration-assurance-release-14.plan.md
generator: sonnet
stale_after: null
dependency_refs: []
completion_state: complete
confidence: high
validation_status: validated
retry_count: 0
parser_success: true
schema_compliance: true
---

## Goal
Release 14 (Guardrail metrics): extend analytics.py's aggregators with the
proposal's named guardrail fields. Per the Owner's two mid-flight edits
(2026-08-05): AC-13 stays partial (not closed) with 2/5 metrics genuinely
unmeasured rather than fabricated; the 3 measured rates (scope-violation,
unknown-usage, false-pass/reopen) share one aligned trailing window instead
of mixing differently-scoped denominators; scope checks are recorded for
BOTH pass and fail (true denominator, not total-tasks); reopens/resolutions
are tracked as individually timestamped events, not one coarse
per-entry field; and this release carries its own REQ-30 overhead
checklist item.

## Bugs cross-referenced
The only open bug touching this release's files is the one logged BY this
release itself: `.renmark/memory/bugs.md` "AC-13 closure path" (2026-08-05)
-- by design, not a regression, tracking the 2 remaining unmeasured metrics
as a concrete future scoped release.

## Smoke checks (fresh run, this pass)
1. `pytest -q tests/test_dispatch.py` -- 32 passed (scope_check event
   recording for pass+fail, unscoped-dispatch no-op, violation shape
   unchanged, best-effort recording never masks the real outcome).
2. `pytest -q tests/test_recurrence.py` -- 22 passed (reopen_count/
   reopen_timestamps/resolved_timestamps, windowed reopen_rate, the core
   fix: independent windowing of a 60-day-old resolution vs. a 5-day-old
   reopen on the same entry).
3. `pytest -q tests/test_reports_analytics.py tests/test_analytics_ledger_guardrails.py`
   -- 16 passed (guardrail_metrics wiring; fixed a pre-existing exact-key-set
   assertion broken by the new additive key -- same interim-fallout pattern
   as prior releases).
4. `pytest -q tests/test_analytics_guardrail_metrics.py` -- 5 passed
   (scope_violation_rate's denominator is total scope-checks, not total
   tasks; window exclusion of stale rows; empty-repo zero degrade).
5. Full suite: `pytest -q` -- 2097 passed, 32 skipped (up from 2080,
   +17 net new tests, no regressions).

## REQ-30 overhead checklist (verify-time, no new code -- per the plan's
explicit requirement and the Release 7 PRD amendment)
Non-codex (sonnet) dispatches this release, actual output tokens vs. the
`AGENT_OVERHEAD_TOKENS = 10,000` pin (total = output + 10k overhead):
- Task 1 (dispatch.py): 2,200 output -> 12,200 total (~22% over the pin)
- Task 2 (recurrence.py): 3,800 output -> 13,800 total (~38% over the pin)
- Task 3 (analytics.py): 3,200 output -> 13,200 total (~32% over the pin)
All three non-codex dispatches landed over the 10k pin, consistent with
`.renmark/memory/orchestration-baseline.md`'s prior finding (small sample,
both observed dispatches landed at-or-above the pin). Codex dispatches
(tasks 4-6) are recorded as `unknown`-cost -- `renmark-execute`'s ad-hoc
`--task` path self-reports `token_count: 0` and codex does not surface
real token counts (confirmed again, same gap logged throughout this
program) -- never assumed compliant with the pin.

## Requirements verified: 3/3 (this release's own scope)
- Windowed scope_violation_rate, unknown_usage_rate, false_pass_reopen_rate:
  verified (real, window-aligned, correct-denominator measurements).
- scope_check event recording (pass+fail): verified.
- Timestamped reopen/resolve tracking with independent windowing: verified.

## AC-13 (Req 13) status: stays `partial` (by design, not a shortfall)
3/5 named metrics measured; 2/5 (Owner-interruptions-per-milestone,
duplicate-artifact rate) remain `null`+documented gap with a concrete
implementation design, tracked in `.renmark/memory/bugs.md` as its own
future Owner-approved release. This release's OWN scope is fully
delivered and verified -- the release itself is `done`; the requirement
it partially serves stays `partial` until that future release lands.

## Deferred / open (not blocking this release)
- AC-13 closure path (2 metrics) -- tracked, not yet scheduled to a
  specific release number.
- Release 11's CLI-wiring gap -- still open, unrelated to Release 14.
- Codex ad-hoc `--task`/`--output` self-report reliability -- this wave
  showed the OPPOSITE failure mode from prior findings (self-reported
  FAIL/schema_compliance=false on 3/3 dispatches that were all actually
  fine) -- logged in `learnings.md`.

## Summary

- verify (smoke): Release 14 guardrail metrics -- 3/3 (this release's scope) verified
- REQ-30 checklist: 3 non-codex dispatches all landed over the 10k pin (22-38%); 3 codex dispatches recorded unknown-cost
- AC-13 stays partial by design -- 3/5 metrics measured, 2/5 tracked as a future scoped release
- Full suite: 2097 passed, 32 skipped
