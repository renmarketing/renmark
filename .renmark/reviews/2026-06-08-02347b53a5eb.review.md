OpenAI Codex v0.117.0 (research preview)
--------
workdir: /home/renmark/projects/ai-system
model: gpt-5.4
provider: openai
approval: never
sandbox: read-only
reasoning effort: none
reasoning summaries: none
session id: 019ea91c-2e50-7e93-ab24-a3d5b703eabb
--------
user
Review the diff main..HEAD. Focus CLOSEST on renmark/sizing.py — a new deterministic, zero-LLM classifier: classify_plan(tasks)->Tier and classify_diff(repo,base_ref)->Tier returning "lite"|"standard"|"full". Scrutinize: the tier RULES (any hard task never lite; core-module target forces >=standard; task-count/est-token/diff-line thresholds; lite requires no hard/core), the "never raise, degrade to standard" guarantee (git failures, empty input, malformed Task, missing fields, timeouts), the git diff --stat parsing in classify_diff (parse edge cases, base_ref that does not exist, detached HEAD), and any way a risky change could be MISCLASSIFIED as lite (the safety-critical direction — a false lite skips the full review). Also check the two SKILL.md changes (feature lite-lane routing, codereview proportional gating) for logic/consistency, and tests/test_sizing.py for whether it actually covers the misclassification risks. 

For each finding: file:line; severity Critical|Major|Minor|Nit; one-sentence description; one-sentence fix. Top: summary counts per severity. Do not modify files. Do not exit until complete.

warning: Codex could not find system bubblewrap at /usr/bin/bwrap. Please install bubblewrap with your package manager. Codex will use the vendored bubblewrap in the meantime.
codex
I’m reviewing `main..HEAD` with emphasis on `renmark/sizing.py`, then I’ll cross-check the two `SKILL.md` edits and `tests/test_sizing.py` against the safety-critical “no false lite” requirement.
exec
/bin/bash -lc 'git status --short' in /home/renmark/projects/ai-system
 succeeded in 0ms:
exec
/bin/bash -lc "python - <<'PY'
from pathlib import Path
p=Path('/home/renmark/projects/ai-system/CHANGELOG.md')
if p.exists():
    lines=p.read_text().splitlines()
    entries=[]
    cur=[]
    for line in lines:
        if line.startswith('## '):
            if cur:
                entries.append(cur)
            cur=[line]
        elif cur:
            cur.append(line)
    if cur:
        entries.append(cur)
    for entry in entries[-5:]:
        print('\\n'.join(entry))
        print('\\n---ENTRY---\\n')
PY" in /home/renmark/projects/ai-system
 succeeded in 0ms:
## v0.1.1 — 2026-05-12 (logs dir + codereview simplified to codex-only)

**Added: `.renmark/logs/`** for per-invocation troubleshooting logs (gitignored). One log file per command run named `<command>-<run_id>.log`.

- `renmark/state.py`:
  - New constants: `LOGS_SUBDIR = "logs"`
  - `logs_dir(repo)`, `open_log(repo, command, run_id=None)`, `append_log(path, *messages)`, `recent_logs(repo, n=10)`
  - 6 tests
- `renmark-execute --logs` — lists the n most-recent log files with size + mtime
- `renmark-execute --logs-n <N>` — adjust the count (default 10)
- `bootstrap.py` updated: `.gitignore` template now includes `.renmark/logs/`
- `plugin/templates/memory/INDEX.md.template` updated to reference all `.renmark/` subdirs (specs, plans, reviews, state, debug, logs)

**Changed: `/renmark:codereview` is now single-pass (codex-only)**, no Sonnet/Opus passes.

The earlier multi-pass design put code into the conversation, which defeats the context-hygiene goal renmark is built for. Codex stays in its own sandbox; Opus only reads the severity summary. Output format and storage path unchanged (`.renmark/reviews/YYYY-MM-DD-<sha>.review.md`). Recommended cadence: end-of-plan, not per-task.

Tests: 91 passing (up from 85).

**Still pending (v0.1.2+):**

- CLI `execute_plan` integration with `dispatch.group_tasks_by_wave` + `dispatch_wave` — parallel waves not yet wired into the live loop
- `--no-commit` runtime behavior (flag accepted, not yet effective)
- Cost preview in `--dry-run`
- Routing memory auto-updates from run outcomes
- `/renmark:debug` per-step routing actually wired
- Additional native providers (Ollama, OpenRouter, OpenAI-compat) — Phase 4
- ~~LiteLLM plug-in slot — Phase 5~~ (dropped — native providers cover the realistic use cases)


---ENTRY---

## v0.1.0 — 2026-05-12 (Phase 1 module landing + roadmap reporter)

**First minor release.** The Phase 1 modules are all in place with tests; the CLI's `execute_plan` loop still uses the v0.0.x single-task code path. Integrating that loop with the new dispatcher is the v0.1.1 work.

**New modules (with tests):**

- `renmark/dispatch.py` — wave-based parallel dispatcher. `group_tasks_by_wave`, `validate_wave`, `dispatch_wave` (concurrent for nim/codex/litellm, `needs_agent` marker for opus/sonnet). 11 tests including a timing assertion that two slow tasks in the same wave finish in under the serial total.
- `renmark/providers/claude_agent.py` — composer for the Agent-tool prompt when a task is `executor: opus` or `executor: sonnet`. Skill issues the Agent call; this module owns the prompt format and constraints.
- `renmark/bootstrap.py` — empty-folder helper. `is_empty_project(repo)`, `bootstrap(repo, project_name=...)` creates CLAUDE.md / AGENTS.md / `.renmark/` from plugin templates, runs `git init`. Idempotent. 6 tests.
- `renmark/roadmap.py` — synthesizer that builds a per-task `task | llm | status | tokens | $ | commit` table from `features.md` + `usage.jsonl` + git log. `write_roadmap_md(repo)` snapshots to `.renmark/memory/roadmap.md`. 7 tests.

**Parser extensions (v0.0.3+, fully tested):**

- New optional task fields: `complexity` (simple|medium|hard), `parallel_group` (int), `est_tokens` (int), `est_cost_usd` (float).
- `executor` now accepts `opus`, `sonnet`, or any `<provider>/<model>` string (e.g., `ollama_chat/qwen2.5-coder:7b`).
- 9 new tests covering defaults, type validation, and rejection of invalid values.

**New skills:**

- `/renmark:roadmap` — prints the status table; also writes the snapshot to `.renmark/memory/roadmap.md` so it's committed.
- `/renmark:help` (added in v0.0.3) — lists all skills with one-sentence descriptions.

**Wizard-style hand-offs:**

- `/renmark:brainstorm` now ends with an explicit `Y/n/wait` prompt to continue to `/renmark:plan`.
- `/renmark:plan` shows a summary (task count + cost preview) and prompts `[r]eview / [d]ispatch / [e]dit / [n]o` — Dispatch only triggers `/renmark:orchestrate` after explicit user approval.
- `/renmark:orchestrate` offers `[c]ode-review / [s]moke / [n]one` after a clean run.

**CLI:**

- `renmark-execute --roadmap` — prints the status table and writes `roadmap.md` snapshot.
- `renmark-execute --no-commit` — flag added (currently a no-op; v0.1.1 will wire it into the per-task commit code so the skill can batch commits per wave).
- argparse prog name corrected from `nim-execute` to `renmark-execute`.

**Memory templates:**

The eight `.renmark/memory/` files now have proper documentation-grade templates:
- `features.md`, `bugs.md`, `decisions.md` (ADR format), `stack.md`, `architecture.md`, `conventions.md`, `routing.md`, `learnings.md`, plus an auto-maintained `INDEX.md`.

**Plugin manifest now declares 7 skills** (brainstorm, plan, orchestrate, debug, codereview, roadmap, help).

**Tests:** 85 passing (up from 52 in v0.0.3).

**Still pending (v0.1.1+):**
- CLI `execute_plan` actually using `dispatch.group_tasks_by_wave` + `dispatch_wave` (currently the loop still runs single-task serial via the v0.0.x path)
- `--no-commit` wired through per-task commit code
- Cost preview in `--dry-run`
- Routing memory auto-updates from run outcomes
- `/renmark:debug` per-step routing (NIM grep / codex trace / opus reasoning)
- `/renmark:codereview` Sonnet + Opus passes
- Additional native providers (Ollama, OpenRouter, OpenAI-compat) — Phase 4
- ~~LiteLLM plug-in slot — Phase 5~~ (dropped — native providers cover the realistic use cases) (optional)


---ENTRY---

## v0.0.3 — 2026-05-12 (Phase 1, +memory + help)

**Persistent memory module + `/renmark:help` skill.**

- `renmark/memory.py` — read/write helpers for `.renmark/memory/`. Functions: `ensure_memory(repo)`, `read_index(repo)`, `read_file(repo, name)`, `log_feature(...)`, `log_bug(...)`, `log_decision(...)`, `append_routing(...)`, `append_learning(...)`. Section-aware appends (newest-first per CHANGELOG convention). Lessons in `log_bug` auto-cross-post to `learnings.md`. 8 new tests.
- Memory templates rewritten so the files act as **living documentation**:
  - `features.md` — shipped / in-progress / planned (CHANGELOG style)
  - `bugs.md` — open / fixed with severity, symptom, root cause, fix, lesson
  - `decisions.md` — ADR format (context, decision, alternatives, consequences) with auto-numbered IDs
  - `stack.md` — languages, libs, runtime env, external APIs
  - `architecture.md` — components, data flow, module boundaries, invariants
  - `conventions.md`, `routing.md`, `learnings.md` — auto-tuned + hand-edited
  - `INDEX.md` is a cheap top-of-file index loaded first by every skill
- `/renmark:help` skill (new) — prints all six commands with one-sentence descriptions and the typical workflow order. Pure documentation, no API calls.
- `plugin.json` updated to declare 6 skills.

52 tests total (44 from baseline + 8 memory tests).


---ENTRY---

## v0.0.2 — 2026-05-12 (Phase 1, partial — skills visible)

**Plugin manifest + all five `/renmark:*` SKILL.md files** so the commands appear in Claude Code's skill list after install. Template files for empty-folder bootstrap. install.sh hardened.

Added:
- `plugin/plugin.json` declaring the 5 skills
- `plugin/skills/{brainstorm,plan,orchestrate,debug,codereview}/SKILL.md` — workflow docs for each
- `plugin/templates/{CLAUDE.md,AGENTS.md,renmark-readme.md,memory/*.md}.template` — what `/renmark:brainstorm` writes when bootstrapping an empty project
- `install.sh` ran successfully — symlinks live at `~/.claude/plugins/renmark` and `~/.local/bin/renmark-execute`

Fixed:
- `install.sh` v0.0.1 stored the /orchestrator backup at `~/.claude/skills/.orchestrator.bak/` — Claude Code's skill discovery picked it up as a phantom skill named `.orchestrator.bak`. **Backup removed entirely**: the orchestrator source still lives in `/home/renmark/projects/ai-inference/` (and in its git history), so a separate copy under `~/.claude/` was just paranoia and bug surface. install.sh now `rm -rf`s the old skill outright; manual revert is `cd ~/projects/ai-inference && bash install.sh` against the v0.2.0 baseline.

Not yet wired (still Phase 1):
- `renmark/dispatch.py` — wave-based parallel dispatcher (so orchestrate can't yet run opus/sonnet tasks or parallel groups)
- `renmark/memory.py` — `.renmark/memory/` reader/writer
- `renmark/providers/claude_agent.py` — Opus/Sonnet via Agent tool from skill side
- Parser extensions for `complexity`, `parallel_group`, `est_tokens`, `est_cost_usd`
- CLI `--no-commit` mode for batched wave commits
- Cost preview in `--dry-run`
- Empty-folder bootstrap code (skill docs reference it but the brainstorm skill currently does it by hand)

The skills are visible and `/renmark:brainstorm` + `/renmark:plan` are workable today (they're Opus-driven conversations). `/renmark:orchestrate` runs the same single-task path the v0.0.1 baseline supports.


---ENTRY---

## v0.0.1 — 2026-05-12 (Phase 0)

**Bootstrap of the new `ai-system` repo.** Copies the working v0.2.0 baseline from `/home/renmark/projects/ai-inference/` and retargets the Python package from `nim_execute` to `renmark`.

Changes vs. ai-inference v0.2.0:

- Package renamed `nim_execute` → `renmark`
- `nim_client.py` → `renmark/providers/nim.py`
- `codex_exec.py` → `renmark/providers/codex.py`
- New `renmark/providers/__init__.py` with `PROVIDERS` registry stub
- Runtime state dir renamed `.nim-state/` → `.renmark/state/` (with `RENMARK_DIR_NAME`, `STATE_SUBDIR`, `MEMORY_SUBDIR`, `DEBUG_SUBDIR` constants; legacy `STATE_DIR_NAME` aliased for back-compat)
- All test imports updated, 41 tests still passing
- CLI references `renmark-execute` / `.renmark/state/` in user-facing strings

Phase 1 (next): the five `/renmark:*` skills, `plugin/plugin.json`, dispatch layer, memory module, empty-folder bootstrap. See `PLAN.md`.

---ENTRY---


exec
/bin/bash -lc 'git diff --name-only main..HEAD' in /home/renmark/projects/ai-system
 succeeded in 0ms:
.renmark/memory/decisions.md
.renmark/memory/features.md
.renmark/memory/learnings.md
.renmark/memory/routing.md
.renmark/memory/stack.md
.renmark/plans/2026-06-08-proportional-pipeline.plan.md
.renmark/reviews/2026-06-08-634af6df145e82e95692521316732de4a364836c.verification.md
.renmark/specs/2026-06-08-proportional-pipeline.spec.md
CHANGELOG.md
plugin/skills/codereview/SKILL.md
plugin/skills/feature/SKILL.md
renmark/sizing.py
tests/test_sizing.py

codex
I’ve identified the touched files. Next I’m reading the sizing implementation, its tests, and the two skill diffs together so I can check whether the safety rules are actually enforced end-to-end.
exec
/bin/bash -lc 'git diff main..HEAD -- /home/renmark/projects/ai-system/renmark/sizing.py' in /home/renmark/projects/ai-system
 succeeded in 0ms:
diff --git a/renmark/sizing.py b/renmark/sizing.py
new file mode 100644
index 0000000..99d16a9
--- /dev/null
+++ b/renmark/sizing.py
@@ -0,0 +1,315 @@
+"""Proportional-pipeline tier classifier — deterministic, zero-LLM, stdlib-only.
+
+This is the **single source of truth** for "how big/risky is this change?" used
+by two callers:
+
+- the **feature router** — ``classify_plan(tasks)`` runs on the validated plan to
+  pick the lite-vs-full lane before any heavy stage spends tokens.
+- **codereview** — ``classify_diff(repo)`` runs on the working diff to pick the
+  cheap in-context ``/review`` vs the full codex review.
+
+Both return one of three **tiers**:
+
+- ``"lite"``    — small, low-risk, doc/config-dominant. Cheap lane: cheap review,
+  land on ``main``, no PR/codex/release ceremony. Chosen only when the signals
+  are *confidently* small.
+- ``"standard"`` — a moderate code change, the full pipeline. Also the **safe
+  default**: any uncertainty, empty input, or failure degrades to ``"standard"``
+  so we never accidentally drop a real change into the lite lane.
+- ``"full"``    — a ``hard`` task, a core-module edit, many tasks, or a large
+  diff: warrants the full pipeline + full codex review.
+
+Design contract:
+
+- Pure functions of their inputs (``classify_diff`` shells out to ``git`` but
+  never mutates state).
+- **Never raises into the caller.** Every git / IO / parse step is wrapped; on
+  any failure or ambiguity the result is ``"standard"`` — never ``"lite"`` by
+  accident.
+- Every threshold is a documented, tunable module-level constant.
+"""
+
+from __future__ import annotations
+
+import subprocess
+from pathlib import Path
+from typing import Literal
+
+from .parser import Task
+
+# ── Tier type ──────────────────────────────────────────────────────────────────
+
+#: The three proportional-pipeline tiers, in ascending weight.
+Tier = Literal["lite", "standard", "full"]
+
+#: Tier values as plain strings (for callers that want the set, e.g. validation).
+TIER_LITE: Tier = "lite"
+TIER_STANDARD: Tier = "standard"
+TIER_FULL: Tier = "full"
+
+#: The safe default — returned on ANY uncertainty, empty input, or failure.
+#: Never ``"lite"`` by accident; never silently ``"full"`` either.
+DEFAULT_TIER: Tier = "standard"
+
+# ── Thresholds (tunable) ─────────────────────────────────────────────────────
+
+#: Plan classification (``classify_plan``):
+#: A plan with this many tasks or fewer MAY be ``lite`` (if no other signal
+#: bumps it). Above it, the plan is at least ``standard``.
+LITE_MAX_TASKS: int = 3
+
+#: A plan with strictly more tasks than this is ``full`` regardless of content
+#: (many moving parts = real change worth full review).
+FULL_MIN_TASKS: int = 8
+
+#: Summed planner ``est_tokens`` at/under this is "very small" and may stay
+#: ``lite`` even for code (when no harder signal fires). ``None`` est_tokens are
+#: treated as 0 (planner gave no estimate — size is governed by task count).
+LITE_MAX_EST_TOKENS: int = 4_000
+
+#: Summed planner ``est_tokens`` at/over this forces ``full`` (large change).
+FULL_MIN_EST_TOKENS: int = 60_000
+
+#: Diff classification (``classify_diff``):
+#: A diff touching this many files or fewer MAY be ``lite``.
+LITE_MAX_FILES: int = 3
+
+#: A diff touching strictly more files than this is ``full``.
+FULL_MIN_FILES: int = 10
+
+#: Total lines changed (insertions + deletions) at/under this MAY be ``lite``.
+LITE_MAX_LINES: int = 40
+
+#: Total lines changed at/over this forces ``full`` (large diff).
+FULL_MIN_LINES: int = 400
+
+#: ``git`` subprocess timeout, seconds.
+GIT_TIMEOUT_S: int = 10
+
+# ── File-type signals (tunable) ──────────────────────────────────────────────
+
+#: Suffixes treated as docs/config (lean toward ``lite``). ``.gitignore`` has no
+#: "suffix" in the usual sense; it's matched by name in ``_is_doc_or_config``.
+DOC_CONFIG_SUFFIXES: frozenset[str] = frozenset(
+    {".md", ".txt", ".json", ".toml", ".gitignore", ".yaml", ".yml", ".cfg", ".ini"}
+)
+
+#: Exact basenames (no useful suffix) treated as doc/config.
+DOC_CONFIG_BASENAMES: frozenset[str] = frozenset({".gitignore", ".gitattributes"})
+
+#: Path fragments that mark a file as a template (doc/config-leaning).
+TEMPLATE_MARKERS: tuple[str, ...] = ("template", "templates")
+
+#: Core-module path roots: an edit under here is risk-bearing and forces
+#: ``>= standard`` even when small. ``renmark/`` is the runtime package; touching
+#: parser / lifecycle / dispatch / init / sizing etc. must never be ``lite``.
+CORE_MODULE_ROOTS: tuple[str, ...] = ("renmark/", "bin/")
+
+#: Code suffixes — presence of any of these makes a change "code-leaning".
+CODE_SUFFIXES: frozenset[str] = frozenset(
+    {".py", ".pyi", ".js", ".jsx", ".ts", ".tsx", ".sh", ".bash", ".rs", ".go", ".c", ".h"}
+)
+
+
+# ── Public API ──────────────────────────────────────────────────────────────
+
+
+def classify_plan(tasks: list[Task]) -> Tier:
+    """Classify a validated plan into a proportional-pipeline :data:`Tier`.
+
+    Signals (all thresholds are module constants above):
+
+    - any task with ``complexity == "hard"`` → never ``lite`` (>= ``standard``);
+    - any **core-module** target (under :data:`CORE_MODULE_ROOTS`) → >= ``standard``;
+    - many tasks (> :data:`FULL_MIN_TASKS`) or large summed ``est_tokens``
+      (>= :data:`FULL_MIN_EST_TOKENS`) → ``full``;
+    - small (<= :data:`LITE_MAX_TASKS` tasks), no ``hard``, no core-module, and
+      doc/config-dominant **or** very small (summed ``est_tokens`` <=
+      :data:`LITE_MAX_EST_TOKENS`) → ``lite``;
+    - otherwise → ``standard``.
+
+    Never raises: empty input or any unexpected shape yields
+    :data:`DEFAULT_TIER` (``"standard"`` — the safe middle).
+    """
+    try:
+        if not tasks:
+            return DEFAULT_TIER
+
+        n_tasks = len(tasks)
+        has_hard = any(_task_complexity(t) == "hard" for t in tasks)
+        targets = [_task_target(t) for t in tasks]
+        has_core = any(_is_core_module(p) for p in targets)
+        all_doc_config = bool(targets) and all(_is_doc_or_config(p) for p in targets)
+        total_est = sum(_task_est_tokens(t) for t in tasks)
+
+        # ── full: heaviest signals win first ──
+        if has_hard:
+            # hard never goes lite; escalate to full when also big/numerous,
+            # else standard (the "never lite" floor).
+            if n_tasks > FULL_MIN_TASKS or total_est >= FULL_MIN_EST_TOKENS or has_core:
+                return TIER_FULL
+            return TIER_STANDARD
+        if n_tasks > FULL_MIN_TASKS:
+            return TIER_FULL
+        if total_est >= FULL_MIN_EST_TOKENS:
+            return TIER_FULL
+
+        # ── standard floor: core-module edits are risk-bearing ──
+        if has_core:
+            return TIER_STANDARD
+
+        # ── lite: confidently small ──
+        if n_tasks <= LITE_MAX_TASKS and (all_doc_config or total_est <= LITE_MAX_EST_TOKENS):
+            return TIER_LITE
+
+        # ── everything in between ──
+        return TIER_STANDARD
+    except Exception:  # never raise into the caller; degrade safe.
+        return DEFAULT_TIER
+
+
+def classify_diff(repo: Path | str, base_ref: str = "main") -> Tier:
+    """Classify the working diff (``<base_ref>..HEAD``) into a :data:`Tier`.
+
+    Runs ``git diff --stat <base_ref>..HEAD`` via :mod:`subprocess` and classifies
+    by files-changed, lines-changed, and doc-vs-code mix using the same tier
+    rules as :func:`classify_plan`:
+
+    - any **core-module** file touched (under :data:`CORE_MODULE_ROOTS`) → >= ``standard``;
+    - many files (> :data:`FULL_MIN_FILES`) or large line churn
+      (>= :data:`FULL_MIN_LINES`) → ``full``;
+    - few files (<= :data:`LITE_MAX_FILES`), small churn (<= :data:`LITE_MAX_LINES`),
+      no core-module, and doc/config-dominant → ``lite``;
+    - otherwise → ``standard``.
+
+    Never raises: a missing git binary, a non-repo, an unknown ``base_ref``, a
+    timeout, or an unparseable stat all degrade to :data:`DEFAULT_TIER`.
+    """
+    try:
+        stat = _git_diff_stat(repo, base_ref)
+        if stat is None:
+            return DEFAULT_TIER
+        files, total_lines = stat
+        if not files:
+            # No changes detected — nothing to escalate, but don't claim "lite"
+            # confidently from an empty/odd diff; the safe middle is fine.
+            return DEFAULT_TIER
+
+        n_files = len(files)
+        has_core = any(_is_core_module(p) for p in files)
+        all_doc_config = all(_is_doc_or_config(p) for p in files)
+
+        # ── full ──
+        if n_files > FULL_MIN_FILES or total_lines >= FULL_MIN_LINES:
+            return TIER_FULL
+
+        # ── standard floor: core-module edits are risk-bearing ──
+        if has_core:
+            return TIER_STANDARD
+
+        # ── lite: confidently small + doc/config-dominant ──
+        if n_files <= LITE_MAX_FILES and total_lines <= LITE_MAX_LINES and all_doc_config:
+            return TIER_LITE
+
+        return TIER_STANDARD
+    except Exception:  # never raise into the caller; degrade safe.
+        return DEFAULT_TIER
+
+
+# ── Internal: file-type predicates ──────────────────────────────────────────
+
+
+def _is_doc_or_config(path: str) -> bool:
+    """True if ``path`` is a doc / config / template file (lite-leaning)."""
+    name = Path(path).name
+    if name in DOC_CONFIG_BASENAMES:
+        return True
+    lowered = path.lower()
+    if any(marker in lowered for marker in TEMPLATE_MARKERS):
+        return True
+    return Path(path).suffix.lower() in DOC_CONFIG_SUFFIXES
+
+
+def _is_core_module(path: str) -> bool:
+    """True if ``path`` is a core-module edit (forces >= standard).
+
+    A code file under any :data:`CORE_MODULE_ROOTS` prefix. Doc/config files that
+    merely live under those roots (e.g. ``renmark/README.md``) are not core.
+    """
+    norm = path.lstrip("./")
+    if not any(norm.startswith(root) for root in CORE_MODULE_ROOTS):
+        return False
+    return Path(norm).suffix.lower() in CODE_SUFFIXES or Path(norm).suffix == ""
+
+
+# ── Internal: Task field accessors (defensive — never raise) ─────────────────
+
+
+def _task_complexity(task: Task) -> str:
+    value = getattr(task, "complexity", "medium")
+    return value.strip().lower() if isinstance(value, str) else "medium"
+
+
+def _task_target(task: Task) -> str:
+    value = getattr(task, "target", "")
+    return value if isinstance(value, str) else ""
+
+
+def _task_est_tokens(task: Task) -> int:
+    value = getattr(task, "est_tokens", None)
+    return value if isinstance(value, int) and value > 0 else 0
+
+
+# ── Internal: git ─────────────────────────────────────────────────────────────
+
+
+def _git_diff_stat(repo: Path | str, base_ref: str) -> tuple[list[str], int] | None:
+    """Return ``(changed_files, total_lines_changed)`` for ``<base_ref>..HEAD``.
+
+    Parses the trailing summary line of ``git diff --stat`` for line counts and
+    each preceding ``path | N`` row for filenames. Returns ``None`` on any
+    failure (no git, non-repo, unknown ref, timeout, empty output).
+    """
+    try:
+        proc = subprocess.run(
+            ["git", "-C", str(repo), "diff", "--stat", f"{base_ref}..HEAD"],
+            capture_output=True,
+            text=True,
+            check=False,
+            timeout=GIT_TIMEOUT_S,
+        )
+    except (subprocess.TimeoutExpired, FileNotFoundError, OSError):
+        return None
+    if proc.returncode != 0:
+        return None
+
+    files: list[str] = []
+    total_lines = 0
+    for raw in proc.stdout.splitlines():
+        line = raw.strip()
+        if not line:
+            continue
+        if "|" in line:
+            # "path/to/file.py | 12 +++---"  → take the path before the pipe.
+            path = line.split("|", 1)[0].strip()
+            if path:
+                files.append(path)
+        elif "changed" in line and ("insertion" in line or "deletion" in line):
+            total_lines += _parse_summary_line(line)
+    return files, total_lines
+
+
+def _parse_summary_line(line: str) -> int:
+    """Extract insertions+deletions from a ``git diff --stat`` summary line.
+
+    e.g. ``"3 files changed, 12 insertions(+), 4 deletions(-)"`` → ``16``.
+    Returns ``0`` if no counts can be read.
+    """
+    total = 0
+    for chunk in line.split(","):
+        chunk = chunk.strip()
+        if "insertion" in chunk or "deletion" in chunk:
+            head = chunk.split()[0] if chunk.split() else ""
+            if head.isdigit():
+                total += int(head)
+    return total

exec
/bin/bash -lc 'git diff main..HEAD -- /home/renmark/projects/ai-system/tests/test_sizing.py' in /home/renmark/projects/ai-system
 succeeded in 0ms:
diff --git a/tests/test_sizing.py b/tests/test_sizing.py
new file mode 100644
index 0000000..f4073f2
--- /dev/null
+++ b/tests/test_sizing.py
@@ -0,0 +1,203 @@
+"""Unit tests for renmark.sizing — the proportional-pipeline tier classifier.
+
+Hermetic: no network, no real git history dependence (we init throwaway repos
+in tmp_path or monkeypatch the git subprocess). Thresholds are imported as
+module constants so tuning them never silently breaks these tests.
+"""
+from __future__ import annotations
+
+import subprocess
+from pathlib import Path
+
+import pytest
+
+from renmark import sizing
+from renmark.parser import Task
+from renmark.sizing import (
+    DEFAULT_TIER,
+    FULL_MIN_TASKS,
+    LITE_MAX_TASKS,
+    TIER_FULL,
+    TIER_LITE,
+    TIER_STANDARD,
+    classify_diff,
+    classify_plan,
+)
+
+
+def _task(
+    index: int,
+    target: str,
+    *,
+    complexity: str = "medium",
+    est_tokens: int | None = None,
+) -> Task:
+    """Build a minimal valid Task for classification (parser not exercised)."""
+    return Task(
+        index=index,
+        title=f"task {index}",
+        mode="A",
+        target=target,
+        verifier="true",
+        spec="do a thing",
+        complexity=complexity,
+        est_tokens=est_tokens,
+    )
+
+
+# ── classify_plan ────────────────────────────────────────────────────────────
+
+
+def test_all_doc_small_set_is_lite() -> None:
+    """<= LITE_MAX_TASKS doc/config targets, no hard, no core → lite."""
+    tasks = [_task(i + 1, f"docs/page{i}.md") for i in range(LITE_MAX_TASKS)]
+    assert len(tasks) <= LITE_MAX_TASKS
+    assert classify_plan(tasks) == TIER_LITE
+
+
+def test_any_hard_task_is_never_lite() -> None:
+    """A single hard task forbids the lite lane (>= standard)."""
+    tasks = [
+        _task(1, "docs/a.md"),
+        _task(2, "docs/b.md", complexity="hard"),
+    ]
+    result = classify_plan(tasks)
+    assert result in {TIER_STANDARD, TIER_FULL}
+    assert result != TIER_LITE
+
+
+def test_core_module_target_is_at_least_standard() -> None:
+    """A single simple edit to a core module (renmark/) must not be lite."""
+    tasks = [_task(1, "renmark/parser.py", complexity="simple")]
+    result = classify_plan(tasks)
+    assert result in {TIER_STANDARD, TIER_FULL}
+    assert result != TIER_LITE
+
+
+def test_many_tasks_is_full() -> None:
+    """Strictly more than FULL_MIN_TASKS tasks → full regardless of content."""
+    n = FULL_MIN_TASKS + 1
+    tasks = [_task(i + 1, f"docs/page{i}.md") for i in range(n)]
+    assert classify_plan(tasks) == TIER_FULL
+
+
+def test_empty_list_degrades_to_standard() -> None:
+    assert classify_plan([]) == TIER_STANDARD
+    assert DEFAULT_TIER == TIER_STANDARD
+
+
+def test_classify_plan_never_raises_on_malformed_input() -> None:
+    """None-ish / malformed task lists degrade safely, never raise."""
+    assert classify_plan(None) == DEFAULT_TIER  # type: ignore[arg-type]
+    # A list of objects missing the expected attributes must not raise.
+    assert classify_plan([object(), object()]) in {  # type: ignore[list-item]
+        TIER_LITE,
+        TIER_STANDARD,
+        TIER_FULL,
+    }
+
+
+# ── classify_diff ──────────────────────────────────────────────────────────
+
+
+def _init_repo(path: Path) -> None:
+    def run(*args: str) -> None:
+        subprocess.run(
+            ["git", "-C", str(path), *args],
+            check=True,
+            capture_output=True,
+            text=True,
+        )
+
+    run("init", "-q")
+    run("config", "user.email", "test@example.com")
+    run("config", "user.name", "test")
+    run("config", "commit.gpgsign", "false")
+
+
+def _commit_all(path: Path, msg: str) -> None:
+    subprocess.run(
+        ["git", "-C", str(path), "add", "-A"], check=True, capture_output=True, text=True
+    )
+    subprocess.run(
+        ["git", "-C", str(path), "commit", "-q", "-m", msg],
+        check=True,
+        capture_output=True,
+        text=True,
+    )
+
+
+def test_classify_diff_tiny_doc_change_is_lite(tmp_path: Path) -> None:
+    """A throwaway repo whose only diff vs base is a tiny doc edit → lite."""
+    repo = tmp_path / "repo"
+    repo.mkdir()
+    _init_repo(repo)
+    (repo / "README.md").write_text("hello\n", encoding="utf-8")
+    _commit_all(repo, "base")
+
+    # Capture the initial branch name (master or main depending on git config)
+    # BEFORE branching, so the base_ref is unambiguous.
+    base = subprocess.run(
+        ["git", "-C", str(repo), "rev-parse", "--abbrev-ref", "HEAD"],
+        check=True,
+        capture_output=True,
+        text=True,
+    ).stdout.strip()
+
+    # Branch HEAD ahead of base with a tiny doc-only change.
+    subprocess.run(
+        ["git", "-C", str(repo), "checkout", "-q", "-b", "work"],
+        check=True,
+        capture_output=True,
+        text=True,
+    )
+    (repo / "README.md").write_text("hello\nworld\n", encoding="utf-8")
+    _commit_all(repo, "tiny doc edit")
+
+    assert classify_diff(repo, base_ref=base) == TIER_LITE
+
+
+def test_classify_diff_no_git_degrades_to_standard(tmp_path: Path) -> None:
+    """A non-repo directory must degrade to standard without raising."""
+    not_a_repo = tmp_path / "plain"
+    not_a_repo.mkdir()
+    assert classify_diff(not_a_repo) == DEFAULT_TIER
+
+
+def test_classify_diff_monkeypatched_large_diff_is_full(
+    monkeypatch: pytest.MonkeyPatch,
+) -> None:
+    """Monkeypatch the git subprocess to return a large --stat → full."""
+
+    class _Proc:
+        returncode = 0
+        stdout = (
+            " a.py | 200 ++++++++++\n"
+            " b.py | 250 ++++++++++\n"
+            " 2 files changed, 400 insertions(+), 50 deletions(-)\n"
+        )
+
+    def fake_run(*args: object, **kwargs: object) -> _Proc:
+        return _Proc()
+
+    monkeypatch.setattr(sizing.subprocess, "run", fake_run)
+    assert classify_diff("/whatever") == TIER_FULL
+
+
+def test_classify_diff_monkeypatched_tiny_doc_is_lite(
+    monkeypatch: pytest.MonkeyPatch,
+) -> None:
+    """Monkeypatch git to a tiny doc-only --stat → lite (no real repo)."""
+
+    class _Proc:
+        returncode = 0
+        stdout = (
+            " docs/readme.md | 4 ++--\n"
+            " 1 file changed, 2 insertions(+), 2 deletions(-)\n"
+        )
+
+    def fake_run(*args: object, **kwargs: object) -> _Proc:
+        return _Proc()
+
+    monkeypatch.setattr(sizing.subprocess, "run", fake_run)
+    assert classify_diff("/whatever") == TIER_LITE

exec
/bin/bash -lc 'git diff main..HEAD -- /home/renmark/projects/ai-system/plugin/skills/feature/SKILL.md /home/renmark/projects/ai-system/plugin/skills/codereview/SKILL.md' in /home/renmark/projects/ai-system
 succeeded in 0ms:
diff --git a/plugin/skills/codereview/SKILL.md b/plugin/skills/codereview/SKILL.md
index 4614a33..2aca848 100644
--- a/plugin/skills/codereview/SKILL.md
+++ b/plugin/skills/codereview/SKILL.md
@@ -1,26 +1,57 @@
 ---
 name: codereview
-description: Use when the user wants a diff or PR reviewed — typed as /renmark:codereview or phrases like "review this", "review my changes", "check this PR", "code review HEAD~3..HEAD". Runs a single codex-based review pass in a read-only sandbox; codex emits a structured markdown report at .renmark/reviews/YYYY-MM-DD-<sha>.review.md. Opus only reads the severity summary — never the diff itself, to keep context lean. Supports `--focus optimize` and `--focus standards` to swap the prompt template; default is correctness + quality.
+description: Use when the user wants a diff or PR reviewed — typed as /renmark:codereview or phrases like "review this", "review my changes", "check this PR", "code review HEAD~3..HEAD". Review depth is proportional to the diff: a lite/doc diff runs the built-in cheap `/review` in-context by default (then offers a one-keystroke escalate to the full codex pass); a standard/full diff runs the full codex review pass in a read-only sandbox, emitting a structured markdown report at .renmark/reviews/YYYY-MM-DD-<sha>.review.md. Opus only reads the severity summary — never the diff itself, to keep context lean. Supports `--full` (force codex), `--skip` (explicit skip), and `--focus optimize`/`--focus standards` to swap the prompt template; default focus is correctness + quality.
 ---
 
 # codereview
 
 ## Overview
 
-**Single-pass codex review.** Codex runs in `--sandbox read-only` mode, reads the diff, and emits a structured findings report. Opus orchestrates but never ingests the diff body — that's the whole point of routing this to codex.
-
-No Sonnet or Opus passes. Earlier designs included them; experience showed that putting code into the conversation defeats the context-hygiene goal renmark is built for. Codex is purpose-built for adversarial bug-finding and that's the most valuable single lens.
-
-Output: structured markdown at `.renmark/reviews/YYYY-MM-DD-<sha>.review.md` with findings grouped by severity (Critical / Major / Minor / Nit).
+**Review depth is proportional to the diff.** Before choosing a review engine, the
+skill classifies the range with `renmark.sizing.classify_diff(repo, base_ref)` →
+`Tier ∈ {lite, standard, full}` (deterministic, zero-LLM; runs `git diff --stat`;
+degrades to `standard` on any no-git / error). The classified tier picks the lane:
+
+- **`lite` (doc/config-dominant or a very small code diff) → built-in cheap `/review`
+  by DEFAULT.** This runs in-context (~10–25k tokens) and catches the obvious bugs
+  plus cross-file / consistency issues. It is **never silently skipped** — the skill
+  always states which review ran and always **offers a one-keystroke escalate to the
+  full codex pass**.
+- **`standard` / `full` → full codex review** (the heavy lane, below). Codex runs in
+  `--sandbox read-only` mode, reads the diff, and emits a structured findings report.
+  Opus orchestrates but never ingests the diff body — that's the whole point of
+  routing this to codex.
+
+No Sonnet or Opus passes for the heavy lane. Earlier designs included them; experience
+showed that putting code into the conversation defeats the context-hygiene goal renmark
+is built for. Codex is purpose-built for adversarial bug-finding and that's the most
+valuable single lens — but it's overkill for a one-line doc tweak, hence the
+proportional default.
+
+The heavy lane's output: structured markdown at `.renmark/reviews/YYYY-MM-DD-<sha>.review.md`
+with findings grouped by severity (Critical / Major / Minor / Nit). The cheap `/review`
+lane reports its findings in-context (it does not write a `.review.md` artifact).
 
 Recommended cadence: **after a full plan completes**, not after every task. `/renmark:orchestrate` offers a hand-off prompt at the end of a successful run.
 
 ## Argument parsing
 
-- `$ARGUMENTS` may contain a git ref range AND/OR `--focus <mode>`.
-- Recognized modes: `optimize`, `standards`. Anything else (or absent) = default.
-- Parse rule: strip the `--focus <mode>` pair from `$ARGUMENTS`; remaining text is the ref range (passed unchanged to Step 1).
-- Unknown mode → print a one-line note (`unknown --focus <mode> — falling back to default`) and use the default prompt. Do not abort.
+- `$ARGUMENTS` may contain a git ref range AND/OR `--focus <mode>` AND/OR a
+  tier-override flag (`--full` / `--skip`).
+- Recognized focus modes: `optimize`, `standards`. Anything else (or absent) = default.
+- Recognized tier-override flags (mutually exclusive; explicit > inferred):
+  - `--full` → force the **full codex** review regardless of the classified tier
+    (escalate a lite diff up to codex).
+  - `--skip` → **explicitly skip** the review entirely. This is the only sanctioned
+    way to run no review — the skill NEVER skips silently on its own. State that the
+    review was skipped by explicit `--skip` and stop.
+- Parse rule: strip the `--focus <mode>` pair AND any `--full` / `--skip` flag from
+  `$ARGUMENTS`; the remaining text is the ref range (passed unchanged to Step 1).
+- Unknown focus mode → print a one-line note (`unknown --focus <mode> — falling back
+  to default`) and use the default prompt. Do not abort.
+- If both `--full` and `--skip` are present → print a one-line note and treat as
+  `--skip` (refusing to review is the safer of two contradictory explicit asks);
+  the user can re-run with `--full` alone.
 
 ## When to Use
 
@@ -32,9 +63,20 @@ Recommended cadence: **after a full plan completes**, not after every task. `/re
 - For debugging a runtime failure — use `/renmark:debug`
 - For implementing fixes — review only; fixes go through orchestrate or direct edit
 
-## How it runs (one pass, codex)
+## How it runs (proportional: cheap `/review` or full codex)
+
+The lane is chosen by the classified tier (see Steps → Determine scope) and any
+tier-override flag:
+
+- **`--skip`** → no review runs (explicit only). State it and stop.
+- **`lite` tier and no `--full`** → run the built-in cheap **`/review`** skill
+  in-context over the same range. Report findings inline, then offer the one-keystroke
+  escalate to the full codex pass (see Hand off).
+- **`standard` / `full` tier, OR `--full` on any tier** → run the full codex pass
+  below.
 
-The agent selects one of three prompt blocks below based on the parsed focus, then pipes it to `codex exec --sandbox read-only -`.
+For the full codex pass, the agent selects one of three prompt blocks below based on
+the parsed focus, then pipes it to `codex exec --sandbox read-only -`.
 
 ```bash
 codex exec --sandbox read-only -
@@ -137,21 +179,66 @@ Codex writes its review to `.renmark/reviews/YYYY-MM-DD-<sha>.review.md` directl
 
 **Lifecycle note:** Codereview is orthogonal to stage progression but commonly runs as part of the Review stage. Skill should NOT bump `lifecycle.json.stage` directly — the wrapper `/renmark:feature` handles that after both codereview AND any secure audit complete.
 
-### 1. Determine scope
+### 1. Determine scope & classify the tier
 
 If the user gave a ref range (`HEAD~3..HEAD`, `main..feature`), use that. Otherwise default to `git diff --name-only HEAD` (working tree). Show a `git diff --stat <range>` summary and confirm with the user.
 
-### 2. Run codex
+Then classify the diff to make the review **proportional** to its size/risk:
+
+```python
+from renmark.sizing import classify_diff
+tier = classify_diff(repo, base_ref)   # 'lite' | 'standard' | 'full'
+```
+
+`classify_diff` is deterministic and zero-LLM: it runs `git diff --stat` under the
+hood and degrades to `'standard'` (the safe middle) on no-git / error. Resolve the
+final lane:
+
+- `--skip` present → skipped (explicit). State it and stop — no engine runs.
+- `--full` present → **full codex**, regardless of `tier`.
+- else `tier == 'lite'` → cheap **`/review`** (in-context).
+- else (`tier ∈ {standard, full}`) → **full codex**.
 
-Shell out via the renmark CLI (or directly) with the prompt above. Streaming output goes to `.renmark/logs/codereview-<run_id>.log` for troubleshooting if codex misbehaves.
+**State the diff tier and the chosen review BEFORE running anything**, e.g.
+`Diff tier: lite — running cheap /review (escalate to codex with --full).` or
+`Diff tier: standard — running full codex review.` Never start a review without
+surfacing this line.
+
+### 2. Run the chosen review
+
+**Cheap lane (`lite`, no `--full`):** invoke the built-in **`/review`** skill over
+the same range, in-context. Report its findings inline. Do not write a `.review.md`
+artifact — proceed to Hand off, which always offers the escalate.
+
+**Full lane (`standard`/`full`, or `--full`):** shell out via the renmark CLI (or
+directly) with the focus prompt above, piped to `codex exec --sandbox read-only -`.
+Streaming output goes to `.renmark/logs/codereview-<run_id>.log` for troubleshooting
+if codex misbehaves.
 
 ### 3. Capture the review
 
-Codex output is parsed (or written through verbatim) and saved to `.renmark/reviews/YYYY-MM-DD-<sha>.review.md`.
+**Full lane only:** codex output is parsed (or written through verbatim) and saved to
+`.renmark/reviews/YYYY-MM-DD-<sha>.review.md`. The cheap `/review` lane reports
+in-context and writes no artifact — there is no `<path>` to report for it.
 
 ### 4. Hand off
 
-Tell the user — using ONLY the summary, never the diff body. Lead with the codereview-specific actions, then render the shared quality-gate menu so re-testing from a different angle stays one keystroke away:
+Tell the user — using ONLY the summary, never the diff body.
+
+**Cheap-lane hand-off (ran `/review`):** state which review ran and ALWAYS offer the
+escalate. Lead with:
+
+> *"Diff tier: lite — ran cheap `/review` in-context. <N critical, M major, K minor> findings.*
+> *What's next?*
+> *  1. [full] Escalate — re-run as the full codex pass (`/renmark:codereview --full <range>`)*
+> *  2. [fix] Fix — kick off a new /renmark:plan built from the findings"*
+
+The `[full]` escalate is mandatory on the cheap lane — never present the cheap result
+as final without it.
+
+**Full-lane hand-off (ran codex):** lead with the codereview-specific actions, then
+render the shared quality-gate menu so re-testing from a different angle stays one
+keystroke away:
 
 > *"Review at `<path>` (focus: <mode>). <N critical, M major, K minor> findings.*
 > *What's next?*
@@ -194,6 +281,12 @@ Avoid: running codereview after every single task. That creates one review per f
 
 ## Reference
 
+- Proportional classifier: `renmark.sizing.classify_diff(repo, base_ref='main') -> Tier`
+  where `Tier ∈ {lite, standard, full}` — deterministic, zero-LLM, degrades to
+  `standard` on no-git / error. Single source of truth for which lane runs.
 - Codex review syntax: `codex review --help`
-- Existing `review` slash command for inspiration
+- Built-in cheap **`/review`** slash command — the lite lane's in-context engine
+  (also good for inspiration on prompt shape).
+- Tier-override flags: `--full` (force codex), `--skip` (explicit skip) — see
+  Argument parsing. These always win over the inferred tier.
 - Focus modes: see Argument parsing above. Adding a new focus = adding a new `### Prompt: <name>` block; nothing else to change.
diff --git a/plugin/skills/feature/SKILL.md b/plugin/skills/feature/SKILL.md
index d5c239a..76b3600 100644
--- a/plugin/skills/feature/SKILL.md
+++ b/plugin/skills/feature/SKILL.md
@@ -9,16 +9,19 @@ description: Use to start a new feature or significant change with branch isolat
 
 Full feature pipeline with branch isolation. Creates a feature branch, runs the renmark wizard end-to-end, and offers PR or merge on finish.
 
-**Pipeline:**
+**Pipeline (proportional — cost tracks size/risk):**
 ```
-git checkout -b feature/<slug>
-  → /renmark:plan          (Scope Contract + decomposition)
-  → /renmark:check-plan    (auto — validates before tokens flow)
-  → /renmark:orchestrate   (wave execution + commits)
-  → /renmark:verify        (goal-backward smoke test)
-  → /renmark:finish        (PR / merge to main)
+/renmark:plan            (Scope Contract + decomposition)
+  → /renmark:check-plan  (auto — validates before tokens flow; ALWAYS)
+  → sizing.classify_plan(tasks) → tier {lite | standard | full}   (deterministic, surfaced in cost preview)
+  → lite lane:           orchestrate → verify (ALWAYS) → proportional codereview (cheap /review + escalate) → land on main
+     standard / full:    branch → orchestrate → verify (ALWAYS) → full codex codereview → finish (PR / merge / release)
 ```
 
+The **lane decision is made AFTER `plan-validated`** — classification needs the
+validated task list. The `--lite` / `--full` overrides always beat the heuristic.
+**plan validation and goal-backward verify (REQ-7) run on every tier, no exceptions.**
+
 ## When to Use
 
 - New functionality that warrants isolation before merging
@@ -28,6 +31,13 @@ git checkout -b feature/<slug>
 **Use plain `/renmark:plan` + `/renmark:orchestrate` on main instead for:**
 - Small edits, config changes, single-file fixes
 
+**Invocation & overrides:**
+- `/renmark:feature <name>` — classify by heuristic (default; see Step 3.5).
+- `/renmark:feature <name> --full` — force the full pipeline (branch → codex review → PR/release), tier heuristic ignored.
+- `/renmark:feature <name> --lite` — force the **lite lane** (land on main, cheap review, no PR/codex/release).
+- Explicit `--lite` / `--full` ALWAYS beats the classifier. Even with an override,
+  **plan validation and goal-backward verify (REQ-7) still run** — those never skip.
+
 ## Steps
 
 **Step 0 — Context check.** Call `lifecycle.skill_preamble(repo, 'feature')`. If it returns a non-None hint, surface as a one-line note.
@@ -86,13 +96,57 @@ owns the **one** dispatch approval (Step 4). This is the mirror of the contract
 in `plan/SKILL.md` §8b — there must never be two dispatch gates, and orchestrate must
 never be reached without an explicit human approval.
 
+### 3.5. Size-tier classification (lane decision)
+
+After `plan` reaches `plan-validated` (Step 3), classify the validated task list to
+pick the lane. This is a **deterministic, zero-LLM** step — the router calls the
+classifier, it does not reason about size itself:
+
+```python
+from renmark import sizing
+tier = sizing.classify_plan(tasks)   # → 'lite' | 'standard' | 'full'
+```
+
+`sizing.classify_plan` is pure and never raises — it **degrades to `'standard'`
+(the safe middle) on any uncertainty**, so the router never falls into `lite` by
+accident.
+
+**Override resolution (explicit beats the heuristic):**
+
+| Invocation | Effective tier |
+|---|---|
+| `--lite` flag present | `lite` (forced) |
+| `--full` flag present | `full` (forced) |
+| neither | `sizing.classify_plan(tasks)` result |
+
+**Lane routing by tier:**
+
+| Tier | Lane |
+|---|---|
+| `lite` | **Lite lane** (Step 4 → 6, lands on `main`) — orchestrate → verify (ALWAYS) → proportional codereview (cheap `/review`, escalate on demand) → land on `main`. No feature branch ceremony, no codex review, no PR, no release. |
+| `standard` / `full` | **Full lane** (unchanged) — branch → orchestrate → verify → full codex codereview → finish (PR / merge / release). |
+
+**Mechanism note (behavior is what matters; leave exact mechanics to execution):**
+the lane decision happens **after `plan-validated`**, so the branch-vs-main choice is
+made here, not at Step 1. Lite work **lands on `main`** — either by classifying before
+creating the branch, or by branching then fast-forwarding `main` on lite finish. Per the
+**single-branch-rule**, small changes land directly on `main` without a PR. Standard/full
+keep the existing branch → PR/merge → (optional) release flow untouched.
+
+**Always run regardless of tier:** plan validation (Step 3) and goal-backward verify
+(Step 6, REQ-7). The lite lane shrinks the *finish* ceremony and the *review* cost — it
+never skips validation or verification.
+
 ### 4. Execute
 
 **Dispatch gate (the single approval point for the wrapper flow).** Since `plan`
 suppressed its gate in Step 3, `feature` owns the dispatch approval before any tokens
-flow. Present the validated plan's cost summary and require an explicit human approval to
-proceed (orchestrate's own pre-flight cost-preview `Proceed? [y/N]` satisfies this when
-you invoke it — do not bypass it). Never auto-dispatch silently.
+flow. **Surface the classified `tier` (Step 3.5), which stages will run for that tier,
+and the est-token band** as part of this cost preview — never blind, never silent — then
+require an explicit human approval to proceed (orchestrate's own pre-flight cost-preview
+`Proceed? [y/N]` satisfies this when you invoke it — do not bypass it). Never
+auto-dispatch silently. This remains the **single** dispatch gate — adding the tier
+preview does not add a second gate.
 
 On approval, invoke `/renmark:orchestrate` with the produced plan. Orchestrate runs
 check-plan in pre-flight, executes waves, re-verifies on completion, and shows the
@@ -132,11 +186,22 @@ the build — the pipeline continues regardless of the blueprint result.
 
 ### 6. Verify + Finish
 
-From orchestrate's menu:
-- Choose **[v] Verify** → `/renmark:verify` runs goal-backward smoke tests
-- Then **[f] Finish** → `/renmark:finish` shows branch summary and offers PR or merge
+**Verify runs on every tier (REQ-7) — never skipped.**
 
-The branch created in step 1 is the source branch for the PR.
+From orchestrate's menu:
+- Choose **[v] Verify** → `/renmark:verify` runs goal-backward smoke tests (ALWAYS, all tiers)
+- Then route the **review + finish** by the tier resolved in Step 3.5:
+
+**Lite lane (`tier == lite`):**
+- **Proportional codereview** → `/renmark:codereview` runs its cheap built-in `/review`
+  by default and offers a one-keystroke escalate to full codex. Defer the detail to the
+  codereview skill — the router only routes; it does not review. Never silently skipped.
+- **Land on `main`** → no PR, no codex review by default, no release ceremony, per the
+  **single-branch-rule**. `/renmark:finish` closes the work onto `main`.
+
+**Standard / full lane (unchanged):**
+- Full codex codereview → **[f] Finish** → `/renmark:finish` shows branch summary and
+  offers PR / merge / release. The branch created in step 1 is the source branch for the PR.
 
 **Next-step hand-off (pipeline skill, class 1):**
 

codex
I have the introduced logic and tests in view. I’m now checking the exact control flow and parse behavior against the documented guarantees, then I’ll map any gaps to concrete line references.
exec
/bin/bash -lc "nl -ba /home/renmark/projects/ai-system/renmark/sizing.py | sed -n '1,240p'" in /home/renmark/projects/ai-system
 succeeded in 0ms:
     1	"""Proportional-pipeline tier classifier — deterministic, zero-LLM, stdlib-only.
     2	
     3	This is the **single source of truth** for "how big/risky is this change?" used
     4	by two callers:
     5	
     6	- the **feature router** — ``classify_plan(tasks)`` runs on the validated plan to
     7	  pick the lite-vs-full lane before any heavy stage spends tokens.
     8	- **codereview** — ``classify_diff(repo)`` runs on the working diff to pick the
     9	  cheap in-context ``/review`` vs the full codex review.
    10	
    11	Both return one of three **tiers**:
    12	
    13	- ``"lite"``    — small, low-risk, doc/config-dominant. Cheap lane: cheap review,
    14	  land on ``main``, no PR/codex/release ceremony. Chosen only when the signals
    15	  are *confidently* small.
    16	- ``"standard"`` — a moderate code change, the full pipeline. Also the **safe
    17	  default**: any uncertainty, empty input, or failure degrades to ``"standard"``
    18	  so we never accidentally drop a real change into the lite lane.
    19	- ``"full"``    — a ``hard`` task, a core-module edit, many tasks, or a large
    20	  diff: warrants the full pipeline + full codex review.
    21	
    22	Design contract:
    23	
    24	- Pure functions of their inputs (``classify_diff`` shells out to ``git`` but
    25	  never mutates state).
    26	- **Never raises into the caller.** Every git / IO / parse step is wrapped; on
    27	  any failure or ambiguity the result is ``"standard"`` — never ``"lite"`` by
    28	  accident.
    29	- Every threshold is a documented, tunable module-level constant.
    30	"""
    31	
    32	from __future__ import annotations
    33	
    34	import subprocess
    35	from pathlib import Path
    36	from typing import Literal
    37	
    38	from .parser import Task
    39	
    40	# ── Tier type ──────────────────────────────────────────────────────────────────
    41	
    42	#: The three proportional-pipeline tiers, in ascending weight.
    43	Tier = Literal["lite", "standard", "full"]
    44	
    45	#: Tier values as plain strings (for callers that want the set, e.g. validation).
    46	TIER_LITE: Tier = "lite"
    47	TIER_STANDARD: Tier = "standard"
    48	TIER_FULL: Tier = "full"
    49	
    50	#: The safe default — returned on ANY uncertainty, empty input, or failure.
    51	#: Never ``"lite"`` by accident; never silently ``"full"`` either.
    52	DEFAULT_TIER: Tier = "standard"
    53	
    54	# ── Thresholds (tunable) ─────────────────────────────────────────────────────
    55	
    56	#: Plan classification (``classify_plan``):
    57	#: A plan with this many tasks or fewer MAY be ``lite`` (if no other signal
    58	#: bumps it). Above it, the plan is at least ``standard``.
    59	LITE_MAX_TASKS: int = 3
    60	
    61	#: A plan with strictly more tasks than this is ``full`` regardless of content
    62	#: (many moving parts = real change worth full review).
    63	FULL_MIN_TASKS: int = 8
    64	
    65	#: Summed planner ``est_tokens`` at/under this is "very small" and may stay
    66	#: ``lite`` even for code (when no harder signal fires). ``None`` est_tokens are
    67	#: treated as 0 (planner gave no estimate — size is governed by task count).
    68	LITE_MAX_EST_TOKENS: int = 4_000
    69	
    70	#: Summed planner ``est_tokens`` at/over this forces ``full`` (large change).
    71	FULL_MIN_EST_TOKENS: int = 60_000
    72	
    73	#: Diff classification (``classify_diff``):
    74	#: A diff touching this many files or fewer MAY be ``lite``.
    75	LITE_MAX_FILES: int = 3
    76	
    77	#: A diff touching strictly more files than this is ``full``.
    78	FULL_MIN_FILES: int = 10
    79	
    80	#: Total lines changed (insertions + deletions) at/under this MAY be ``lite``.
    81	LITE_MAX_LINES: int = 40
    82	
    83	#: Total lines changed at/over this forces ``full`` (large diff).
    84	FULL_MIN_LINES: int = 400
    85	
    86	#: ``git`` subprocess timeout, seconds.
    87	GIT_TIMEOUT_S: int = 10
    88	
    89	# ── File-type signals (tunable) ──────────────────────────────────────────────
    90	
    91	#: Suffixes treated as docs/config (lean toward ``lite``). ``.gitignore`` has no
    92	#: "suffix" in the usual sense; it's matched by name in ``_is_doc_or_config``.
    93	DOC_CONFIG_SUFFIXES: frozenset[str] = frozenset(
    94	    {".md", ".txt", ".json", ".toml", ".gitignore", ".yaml", ".yml", ".cfg", ".ini"}
    95	)
    96	
    97	#: Exact basenames (no useful suffix) treated as doc/config.
    98	DOC_CONFIG_BASENAMES: frozenset[str] = frozenset({".gitignore", ".gitattributes"})
    99	
   100	#: Path fragments that mark a file as a template (doc/config-leaning).
   101	TEMPLATE_MARKERS: tuple[str, ...] = ("template", "templates")
   102	
   103	#: Core-module path roots: an edit under here is risk-bearing and forces
   104	#: ``>= standard`` even when small. ``renmark/`` is the runtime package; touching
   105	#: parser / lifecycle / dispatch / init / sizing etc. must never be ``lite``.
   106	CORE_MODULE_ROOTS: tuple[str, ...] = ("renmark/", "bin/")
   107	
   108	#: Code suffixes — presence of any of these makes a change "code-leaning".
   109	CODE_SUFFIXES: frozenset[str] = frozenset(
   110	    {".py", ".pyi", ".js", ".jsx", ".ts", ".tsx", ".sh", ".bash", ".rs", ".go", ".c", ".h"}
   111	)
   112	
   113	
   114	# ── Public API ──────────────────────────────────────────────────────────────
   115	
   116	
   117	def classify_plan(tasks: list[Task]) -> Tier:
   118	    """Classify a validated plan into a proportional-pipeline :data:`Tier`.
   119	
   120	    Signals (all thresholds are module constants above):
   121	
   122	    - any task with ``complexity == "hard"`` → never ``lite`` (>= ``standard``);
   123	    - any **core-module** target (under :data:`CORE_MODULE_ROOTS`) → >= ``standard``;
   124	    - many tasks (> :data:`FULL_MIN_TASKS`) or large summed ``est_tokens``
   125	      (>= :data:`FULL_MIN_EST_TOKENS`) → ``full``;
   126	    - small (<= :data:`LITE_MAX_TASKS` tasks), no ``hard``, no core-module, and
   127	      doc/config-dominant **or** very small (summed ``est_tokens`` <=
   128	      :data:`LITE_MAX_EST_TOKENS`) → ``lite``;
   129	    - otherwise → ``standard``.
   130	
   131	    Never raises: empty input or any unexpected shape yields
   132	    :data:`DEFAULT_TIER` (``"standard"`` — the safe middle).
   133	    """
   134	    try:
   135	        if not tasks:
   136	            return DEFAULT_TIER
   137	
   138	        n_tasks = len(tasks)
   139	        has_hard = any(_task_complexity(t) == "hard" for t in tasks)
   140	        targets = [_task_target(t) for t in tasks]
   141	        has_core = any(_is_core_module(p) for p in targets)
   142	        all_doc_config = bool(targets) and all(_is_doc_or_config(p) for p in targets)
   143	        total_est = sum(_task_est_tokens(t) for t in tasks)
   144	
   145	        # ── full: heaviest signals win first ──
   146	        if has_hard:
   147	            # hard never goes lite; escalate to full when also big/numerous,
   148	            # else standard (the "never lite" floor).
   149	            if n_tasks > FULL_MIN_TASKS or total_est >= FULL_MIN_EST_TOKENS or has_core:
   150	                return TIER_FULL
   151	            return TIER_STANDARD
   152	        if n_tasks > FULL_MIN_TASKS:
   153	            return TIER_FULL
   154	        if total_est >= FULL_MIN_EST_TOKENS:
   155	            return TIER_FULL
   156	
   157	        # ── standard floor: core-module edits are risk-bearing ──
   158	        if has_core:
   159	            return TIER_STANDARD
   160	
   161	        # ── lite: confidently small ──
   162	        if n_tasks <= LITE_MAX_TASKS and (all_doc_config or total_est <= LITE_MAX_EST_TOKENS):
   163	            return TIER_LITE
   164	
   165	        # ── everything in between ──
   166	        return TIER_STANDARD
   167	    except Exception:  # never raise into the caller; degrade safe.
   168	        return DEFAULT_TIER
   169	
   170	
   171	def classify_diff(repo: Path | str, base_ref: str = "main") -> Tier:
   172	    """Classify the working diff (``<base_ref>..HEAD``) into a :data:`Tier`.
   173	
   174	    Runs ``git diff --stat <base_ref>..HEAD`` via :mod:`subprocess` and classifies
   175	    by files-changed, lines-changed, and doc-vs-code mix using the same tier
   176	    rules as :func:`classify_plan`:
   177	
   178	    - any **core-module** file touched (under :data:`CORE_MODULE_ROOTS`) → >= ``standard``;
   179	    - many files (> :data:`FULL_MIN_FILES`) or large line churn
   180	      (>= :data:`FULL_MIN_LINES`) → ``full``;
   181	    - few files (<= :data:`LITE_MAX_FILES`), small churn (<= :data:`LITE_MAX_LINES`),
   182	      no core-module, and doc/config-dominant → ``lite``;
   183	    - otherwise → ``standard``.
   184	
   185	    Never raises: a missing git binary, a non-repo, an unknown ``base_ref``, a
   186	    timeout, or an unparseable stat all degrade to :data:`DEFAULT_TIER`.
   187	    """
   188	    try:
   189	        stat = _git_diff_stat(repo, base_ref)
   190	        if stat is None:
   191	            return DEFAULT_TIER
   192	        files, total_lines = stat
   193	        if not files:
   194	            # No changes detected — nothing to escalate, but don't claim "lite"
   195	            # confidently from an empty/odd diff; the safe middle is fine.
   196	            return DEFAULT_TIER
   197	
   198	        n_files = len(files)
   199	        has_core = any(_is_core_module(p) for p in files)
   200	        all_doc_config = all(_is_doc_or_config(p) for p in files)
   201	
   202	        # ── full ──
   203	        if n_files > FULL_MIN_FILES or total_lines >= FULL_MIN_LINES:
   204	            return TIER_FULL
   205	
   206	        # ── standard floor: core-module edits are risk-bearing ──
   207	        if has_core:
   208	            return TIER_STANDARD
   209	
   210	        # ── lite: confidently small + doc/config-dominant ──
   211	        if n_files <= LITE_MAX_FILES and total_lines <= LITE_MAX_LINES and all_doc_config:
   212	            return TIER_LITE
   213	
   214	        return TIER_STANDARD
   215	    except Exception:  # never raise into the caller; degrade safe.
   216	        return DEFAULT_TIER
   217	
   218	
   219	# ── Internal: file-type predicates ──────────────────────────────────────────
   220	
   221	
   222	def _is_doc_or_config(path: str) -> bool:
   223	    """True if ``path`` is a doc / config / template file (lite-leaning)."""
   224	    name = Path(path).name
   225	    if name in DOC_CONFIG_BASENAMES:
   226	        return True
   227	    lowered = path.lower()
   228	    if any(marker in lowered for marker in TEMPLATE_MARKERS):
   229	        return True
   230	    return Path(path).suffix.lower() in DOC_CONFIG_SUFFIXES
   231	
   232	
   233	def _is_core_module(path: str) -> bool:
   234	    """True if ``path`` is a core-module edit (forces >= standard).
   235	
   236	    A code file under any :data:`CORE_MODULE_ROOTS` prefix. Doc/config files that
   237	    merely live under those roots (e.g. ``renmark/README.md``) are not core.
   238	    """
   239	    norm = path.lstrip("./")
   240	    if not any(norm.startswith(root) for root in CORE_MODULE_ROOTS):

exec
/bin/bash -lc "nl -ba /home/renmark/projects/ai-system/plugin/skills/feature/SKILL.md | sed -n '1,260p'" in /home/renmark/projects/ai-system
 succeeded in 0ms:
     1	---
     2	name: feature
     3	description: Use to start a new feature or significant change with branch isolation — typed as /renmark:feature or phrases like "new feature X", "build X", "start feature". Creates a branch then runs the full pipeline: plan → check-plan → orchestrate → verify → finish.
     4	---
     5	
     6	# feature
     7	
     8	## Overview
     9	
    10	Full feature pipeline with branch isolation. Creates a feature branch, runs the renmark wizard end-to-end, and offers PR or merge on finish.
    11	
    12	**Pipeline (proportional — cost tracks size/risk):**
    13	```
    14	/renmark:plan            (Scope Contract + decomposition)
    15	  → /renmark:check-plan  (auto — validates before tokens flow; ALWAYS)
    16	  → sizing.classify_plan(tasks) → tier {lite | standard | full}   (deterministic, surfaced in cost preview)
    17	  → lite lane:           orchestrate → verify (ALWAYS) → proportional codereview (cheap /review + escalate) → land on main
    18	     standard / full:    branch → orchestrate → verify (ALWAYS) → full codex codereview → finish (PR / merge / release)
    19	```
    20	
    21	The **lane decision is made AFTER `plan-validated`** — classification needs the
    22	validated task list. The `--lite` / `--full` overrides always beat the heuristic.
    23	**plan validation and goal-backward verify (REQ-7) run on every tier, no exceptions.**
    24	
    25	## When to Use
    26	
    27	- New functionality that warrants isolation before merging
    28	- Significant refactors or migrations
    29	- Any work where you want a reviewable PR
    30	
    31	**Use plain `/renmark:plan` + `/renmark:orchestrate` on main instead for:**
    32	- Small edits, config changes, single-file fixes
    33	
    34	**Invocation & overrides:**
    35	- `/renmark:feature <name>` — classify by heuristic (default; see Step 3.5).
    36	- `/renmark:feature <name> --full` — force the full pipeline (branch → codex review → PR/release), tier heuristic ignored.
    37	- `/renmark:feature <name> --lite` — force the **lite lane** (land on main, cheap review, no PR/codex/release).
    38	- Explicit `--lite` / `--full` ALWAYS beats the classifier. Even with an override,
    39	  **plan validation and goal-backward verify (REQ-7) still run** — those never skip.
    40	
    41	## Steps
    42	
    43	**Step 0 — Context check.** Call `lifecycle.skill_preamble(repo, 'feature')`. If it returns a non-None hint, surface as a one-line note.
    44	
    45	**Router contract reminder:** `/renmark:feature` is a **workflow router**, not a reasoning agent. It must NOT plan, code, test, review, or document. Its only actions are: read `lifecycle.json` → determine next stage → dispatch the right stage skill → receive a bounded summary → update `lifecycle.json` → recommend next action. See the plan's Section 3 ("/renmark:feature is a router, not an engineer") for the MUST/MUST NOT list.
    46	
    47	### 1. Create branch
    48	
    49	```bash
    50	SLUG=$(echo "<feature name>" | tr '[:upper:]' '[:lower:]' | sed 's/[^a-z0-9]/-/g' | sed 's/--*/-/g')
    51	git checkout -b feature/$SLUG
    52	```
    53	
    54	Confirm branch name with user before continuing.
    55	
    56	**Write the feature identity to `lifecycle.json` (required — do NOT skip).** Immediately after the branch exists (whether newly created or switched to), persist this feature's identity so every downstream stage writes against the correct `feature`/`branch` instead of inheriting the previous feature's:
    57	
    58	```python
    59	from renmark import lifecycle
    60	lifecycle.begin_feature(repo, feature="<slug>", branch="feature/<slug>")
    61	```
    62	
    63	`begin_feature` resets lifecycle to a clean `init` state for the new feature (empty `stages_completed`, empty `artifacts`). Skipping this is the identity bug that left `/renmark:finish`'s ADR and lifecycle pointing at the *prior* feature — the router owns identity; stage skills only advance `stage`.
    64	
    65	### 2. PRD Alignment
    66	
    67	Before planning, dispatch the PRD alignment subagent per
    68	`plugin/skills/_shared/prd-alignment.md` (the single source of truth — do NOT inline its logic here).
    69	
    70	*Dispatch the PRD alignment subagent from
    71	`${CLAUDE_PLUGIN_ROOT}/skills/_shared/prd-alignment.md`: Agent tool call,
    72	passing ONLY `feature_description` + `file_scope`. Receive ONLY the ≤5-line
    73	verdict summary. Do NOT read PRD.md in the orchestrator context.*
    74	
    75	The router MUST NOT read the `PRD.md` body. It passes only the feature description
    76	and file scope to the subagent and receives only the bounded `verdict`.
    77	
    78	**Route on verdict:**
    79	
    80	| Verdict | Action |
    81	|---|---|
    82	| `aligned` | Proceed to Plan (Step 3). |
    83	| `drift` | Route the `proposed_prd_addition` into `/renmark:prd` update mode (human-gated). **Pause the feature plan until the human approves or rejects the PRD addition.** Do not proceed to planning while `drift` is unresolved. |
    84	
    85	`prd-alignment` is the contract key for this gate — see the shared file for the
    86	full bounded-return format and examples.
    87	
    88	### 3. Plan
    89	
    90	Invoke `/renmark:plan <description or spec-path>`. The plan skill runs the Scope Contract discovery (Q1–Q3), writes CHANGELOG + stack.md, then decomposes into tasks.
    91	
    92	**Plan runs in validation-only mode here (single-gate contract).** Because `feature`
    93	is the wrapper, `plan` **suppresses its own Step 8b dispatch gate**: it stops after
    94	auto-validation at stage `plan-validated` and returns control to this router. `feature`
    95	owns the **one** dispatch approval (Step 4). This is the mirror of the contract documented
    96	in `plan/SKILL.md` §8b — there must never be two dispatch gates, and orchestrate must
    97	never be reached without an explicit human approval.
    98	
    99	### 3.5. Size-tier classification (lane decision)
   100	
   101	After `plan` reaches `plan-validated` (Step 3), classify the validated task list to
   102	pick the lane. This is a **deterministic, zero-LLM** step — the router calls the
   103	classifier, it does not reason about size itself:
   104	
   105	```python
   106	from renmark import sizing
   107	tier = sizing.classify_plan(tasks)   # → 'lite' | 'standard' | 'full'
   108	```
   109	
   110	`sizing.classify_plan` is pure and never raises — it **degrades to `'standard'`
   111	(the safe middle) on any uncertainty**, so the router never falls into `lite` by
   112	accident.
   113	
   114	**Override resolution (explicit beats the heuristic):**
   115	
   116	| Invocation | Effective tier |
   117	|---|---|
   118	| `--lite` flag present | `lite` (forced) |
   119	| `--full` flag present | `full` (forced) |
   120	| neither | `sizing.classify_plan(tasks)` result |
   121	
   122	**Lane routing by tier:**
   123	
   124	| Tier | Lane |
   125	|---|---|
   126	| `lite` | **Lite lane** (Step 4 → 6, lands on `main`) — orchestrate → verify (ALWAYS) → proportional codereview (cheap `/review`, escalate on demand) → land on `main`. No feature branch ceremony, no codex review, no PR, no release. |
   127	| `standard` / `full` | **Full lane** (unchanged) — branch → orchestrate → verify → full codex codereview → finish (PR / merge / release). |
   128	
   129	**Mechanism note (behavior is what matters; leave exact mechanics to execution):**
   130	the lane decision happens **after `plan-validated`**, so the branch-vs-main choice is
   131	made here, not at Step 1. Lite work **lands on `main`** — either by classifying before
   132	creating the branch, or by branching then fast-forwarding `main` on lite finish. Per the
   133	**single-branch-rule**, small changes land directly on `main` without a PR. Standard/full
   134	keep the existing branch → PR/merge → (optional) release flow untouched.
   135	
   136	**Always run regardless of tier:** plan validation (Step 3) and goal-backward verify
   137	(Step 6, REQ-7). The lite lane shrinks the *finish* ceremony and the *review* cost — it
   138	never skips validation or verification.
   139	
   140	### 4. Execute
   141	
   142	**Dispatch gate (the single approval point for the wrapper flow).** Since `plan`
   143	suppressed its gate in Step 3, `feature` owns the dispatch approval before any tokens
   144	flow. **Surface the classified `tier` (Step 3.5), which stages will run for that tier,
   145	and the est-token band** as part of this cost preview — never blind, never silent — then
   146	require an explicit human approval to proceed (orchestrate's own pre-flight cost-preview
   147	`Proceed? [y/N]` satisfies this when you invoke it — do not bypass it). Never
   148	auto-dispatch silently. This remains the **single** dispatch gate — adding the tier
   149	preview does not add a second gate.
   150	
   151	On approval, invoke `/renmark:orchestrate` with the produced plan. Orchestrate runs
   152	check-plan in pre-flight, executes waves, re-verifies on completion, and shows the
   153	hand-off menu. If the user declines, stop with the validated plan on disk.
   154	
   155	### 5. Blueprint Update
   156	
   157	After orchestrate completes (whether or not all tasks pass), invoke `/renmark:blueprint`
   158	as a non-blocking touchpoint to keep the living blueprint current.
   159	
   160	**This is an artifact touchpoint, NOT a new lifecycle stage.** It does not gate
   161	the build — the pipeline continues regardless of the blueprint result.
   162	
   163	*Dispatch as a non-blocking subagent call (Agent tool, bounded return):*
   164	
   165	- The subagent simply invokes `/renmark:blueprint` as-is. Blueprint reads
   166	  architecture exclusively from `.renmark/memory/project-map.md` (and `stack.md`);
   167	  do NOT feed it diff content, touched-file lists, or wave summaries — those are
   168	  never architecture inputs.
   169	- The fact that a feature shipped may be used ONLY as a signal for whether it is
   170	  worth re-running blueprint (e.g. skip if no files were changed). It must not
   171	  be used to drive architecture updates.
   172	- It MUST NOT fabricate architecture. If `project-map.md` is missing or stale
   173	  (older than the current feature branch), route to `/renmark:init` first to
   174	  regenerate the map, then re-invoke blueprint.
   175	- The subagent returns ONLY: `updated_artifacts` (list of paths written) +
   176	  `skipped_reason` (if nothing was updated) ≤ 3 lines total. The orchestrator
   177	  does NOT read the blueprint body — only the bounded return.
   178	
   179	**Route on result:**
   180	
   181	| Result | Action |
   182	|---|---|
   183	| `updated` | Continue to Verify + Finish (Step 6). |
   184	| `skipped` (project-map stale / missing) | Dispatch `/renmark:init`, then re-invoke blueprint, then continue. |
   185	| `skipped` (nothing to reconcile) | Continue to Step 6 — no action needed. |
   186	
   187	### 6. Verify + Finish
   188	
   189	**Verify runs on every tier (REQ-7) — never skipped.**
   190	
   191	From orchestrate's menu:
   192	- Choose **[v] Verify** → `/renmark:verify` runs goal-backward smoke tests (ALWAYS, all tiers)
   193	- Then route the **review + finish** by the tier resolved in Step 3.5:
   194	
   195	**Lite lane (`tier == lite`):**
   196	- **Proportional codereview** → `/renmark:codereview` runs its cheap built-in `/review`
   197	  by default and offers a one-keystroke escalate to full codex. Defer the detail to the
   198	  codereview skill — the router only routes; it does not review. Never silently skipped.
   199	- **Land on `main`** → no PR, no codex review by default, no release ceremony, per the
   200	  **single-branch-rule**. `/renmark:finish` closes the work onto `main`.
   201	
   202	**Standard / full lane (unchanged):**
   203	- Full codex codereview → **[f] Finish** → `/renmark:finish` shows branch summary and
   204	  offers PR / merge / release. The branch created in step 1 is the source branch for the PR.
   205	
   206	**Next-step hand-off (pipeline skill, class 1):**
   207	
   208	> *End by calling `renmark.lifecycle.next_steps(repo, "feature")` and render the
   209	> result per `${CLAUDE_PLUGIN_ROOT}/skills/_shared/next-steps.md` (class 1 —
   210	> Tier-0 stage routing). Present via `AskUserQuestion` (handoff-menu.md rules
   211	> 6–9); the state-derived next command is the `(Recommended)` option. Require an
   212	> explicit choice — never auto-proceed.*

exec
/bin/bash -lc "nl -ba /home/renmark/projects/ai-system/plugin/skills/codereview/SKILL.md | sed -n '1,340p'" in /home/renmark/projects/ai-system
 succeeded in 0ms:
     1	---
     2	name: codereview
     3	description: Use when the user wants a diff or PR reviewed — typed as /renmark:codereview or phrases like "review this", "review my changes", "check this PR", "code review HEAD~3..HEAD". Review depth is proportional to the diff: a lite/doc diff runs the built-in cheap `/review` in-context by default (then offers a one-keystroke escalate to the full codex pass); a standard/full diff runs the full codex review pass in a read-only sandbox, emitting a structured markdown report at .renmark/reviews/YYYY-MM-DD-<sha>.review.md. Opus only reads the severity summary — never the diff itself, to keep context lean. Supports `--full` (force codex), `--skip` (explicit skip), and `--focus optimize`/`--focus standards` to swap the prompt template; default focus is correctness + quality.
     4	---
     5	
     6	# codereview
     7	
     8	## Overview
     9	
    10	**Review depth is proportional to the diff.** Before choosing a review engine, the
    11	skill classifies the range with `renmark.sizing.classify_diff(repo, base_ref)` →
    12	`Tier ∈ {lite, standard, full}` (deterministic, zero-LLM; runs `git diff --stat`;
    13	degrades to `standard` on any no-git / error). The classified tier picks the lane:
    14	
    15	- **`lite` (doc/config-dominant or a very small code diff) → built-in cheap `/review`
    16	  by DEFAULT.** This runs in-context (~10–25k tokens) and catches the obvious bugs
    17	  plus cross-file / consistency issues. It is **never silently skipped** — the skill
    18	  always states which review ran and always **offers a one-keystroke escalate to the
    19	  full codex pass**.
    20	- **`standard` / `full` → full codex review** (the heavy lane, below). Codex runs in
    21	  `--sandbox read-only` mode, reads the diff, and emits a structured findings report.
    22	  Opus orchestrates but never ingests the diff body — that's the whole point of
    23	  routing this to codex.
    24	
    25	No Sonnet or Opus passes for the heavy lane. Earlier designs included them; experience
    26	showed that putting code into the conversation defeats the context-hygiene goal renmark
    27	is built for. Codex is purpose-built for adversarial bug-finding and that's the most
    28	valuable single lens — but it's overkill for a one-line doc tweak, hence the
    29	proportional default.
    30	
    31	The heavy lane's output: structured markdown at `.renmark/reviews/YYYY-MM-DD-<sha>.review.md`
    32	with findings grouped by severity (Critical / Major / Minor / Nit). The cheap `/review`
    33	lane reports its findings in-context (it does not write a `.review.md` artifact).
    34	
    35	Recommended cadence: **after a full plan completes**, not after every task. `/renmark:orchestrate` offers a hand-off prompt at the end of a successful run.
    36	
    37	## Argument parsing
    38	
    39	- `$ARGUMENTS` may contain a git ref range AND/OR `--focus <mode>` AND/OR a
    40	  tier-override flag (`--full` / `--skip`).
    41	- Recognized focus modes: `optimize`, `standards`. Anything else (or absent) = default.
    42	- Recognized tier-override flags (mutually exclusive; explicit > inferred):
    43	  - `--full` → force the **full codex** review regardless of the classified tier
    44	    (escalate a lite diff up to codex).
    45	  - `--skip` → **explicitly skip** the review entirely. This is the only sanctioned
    46	    way to run no review — the skill NEVER skips silently on its own. State that the
    47	    review was skipped by explicit `--skip` and stop.
    48	- Parse rule: strip the `--focus <mode>` pair AND any `--full` / `--skip` flag from
    49	  `$ARGUMENTS`; the remaining text is the ref range (passed unchanged to Step 1).
    50	- Unknown focus mode → print a one-line note (`unknown --focus <mode> — falling back
    51	  to default`) and use the default prompt. Do not abort.
    52	- If both `--full` and `--skip` are present → print a one-line note and treat as
    53	  `--skip` (refusing to review is the safer of two contradictory explicit asks);
    54	  the user can re-run with `--full` alone.
    55	
    56	## When to Use
    57	
    58	- "Review my changes" / "review this PR"
    59	- After completing a feature, before merging
    60	- After `/renmark:orchestrate` finishes — sanity check what agents wrote
    61	
    62	**Do NOT use:**
    63	- For debugging a runtime failure — use `/renmark:debug`
    64	- For implementing fixes — review only; fixes go through orchestrate or direct edit
    65	
    66	## How it runs (proportional: cheap `/review` or full codex)
    67	
    68	The lane is chosen by the classified tier (see Steps → Determine scope) and any
    69	tier-override flag:
    70	
    71	- **`--skip`** → no review runs (explicit only). State it and stop.
    72	- **`lite` tier and no `--full`** → run the built-in cheap **`/review`** skill
    73	  in-context over the same range. Report findings inline, then offer the one-keystroke
    74	  escalate to the full codex pass (see Hand off).
    75	- **`standard` / `full` tier, OR `--full` on any tier** → run the full codex pass
    76	  below.
    77	
    78	For the full codex pass, the agent selects one of three prompt blocks below based on
    79	the parsed focus, then pipes it to `codex exec --sandbox read-only -`.
    80	
    81	```bash
    82	codex exec --sandbox read-only -
    83	```
    84	
    85	### Prompt: default (correctness + quality)
    86	
    87	The skill pipes a prompt like:
    88	
    89	```
    90	Review the diff <range>. Find: runtime bugs, logic errors, off-by-ones,
    91	race conditions, security issues (injection, auth, data leaks), bad
    92	assumptions, edge cases the code doesn't handle.
    93	
    94	For each finding:
    95	  - file:line
    96	  - severity: Critical | Major | Minor | Nit
    97	  - one-sentence description
    98	  - one-sentence fix suggestion
    99	
   100	Top of report: summary counts per severity.
   101	Do not modify any files. Do not exit until the review is complete.
   102	```
   103	
   104	### Prompt: optimize
   105	
   106	```
   107	Review the diff <range> for PERFORMANCE and IDIOM issues. Focus on:
   108	  - unnecessary allocations, copies, or work inside hot loops
   109	  - asymptotic complexity surprises (accidental O(n²) over reasonable inputs)
   110	  - repeated computation that could be cached or hoisted
   111	  - blocking calls where async / batching would scale better
   112	  - non-idiomatic constructs that have a clearer, faster language-native form
   113	  - resource lifecycle issues (locks held too long, file handles, sockets)
   114	
   115	Out of scope for this pass: correctness bugs, security, edge cases.
   116	  If you spot a correctness bug while looking at perf, list it as ASIDE
   117	  (severity: Major), but DO NOT exhaustively hunt for them — that's the
   118	  default focus's job.
   119	
   120	For each finding:
   121	  - file:line
   122	  - severity: Critical | Major | Minor | Nit
   123	  - one-sentence description (what's slow / non-idiomatic, and roughly why)
   124	  - one-sentence fix suggestion
   125	
   126	Top of report: summary counts per severity, plus a single bold line
   127	"Focus: optimize" so the reader knows which lens this pass used.
   128	Do not modify any files. Do not exit until the review is complete.
   129	```
   130	
   131	### Prompt: standards
   132	
   133	```
   134	Review the diff <range> for adherence to the project's UNWRITTEN code
   135	standards. Skip what tools/precommit.sh already checks (ruff lint, ruff
   136	format, mypy strict, plugin lint, pytest) — those are the WRITTEN
   137	standards and the gate already enforces them. Look only at the
   138	conventions that exist in the codebase but are not enforced by tooling.
   139	
   140	Sources of truth:
   141	  - Spot-check 3–5 other files in the same module/package for
   142	    conventions: imports (relative vs absolute), error-handling shape
   143	    (raise vs return None vs Result), logging style, naming, type
   144	    annotation density, docstring presence and shape, where helpers go.
   145	  - If .renmark/memory/conventions.md exists, treat it as a hard rubric.
   146	  - If .renmark/memory/dev-standards.md flags any "gap" the diff touches,
   147	    call those out.
   148	
   149	Specifically look for:
   150	  - pathlib.Path vs os.path mixing
   151	  - dict[str, Any] in new code where a TypedDict / dataclass would fit
   152	    the existing pattern
   153	  - new public function without a type annotation when siblings have them
   154	  - error suppression (bare except, except Exception: pass) inconsistent
   155	    with sibling files
   156	  - reinventing a helper that already exists elsewhere in the package
   157	  - naming drift (camelCase function in a snake_case file, etc.)
   158	  - missing or stale CHANGELOG entry when sibling features have them
   159	
   160	For each finding:
   161	  - file:line
   162	  - severity: Critical | Major | Minor | Nit  (most standards findings
   163	    will be Minor or Nit; Major only if it would block merge in a
   164	    maintainer review)
   165	  - one-sentence description (what convention is broken, and what the
   166	    majority pattern looks like)
   167	  - one-sentence fix suggestion
   168	
   169	Top of report: summary counts per severity, plus a single bold line
   170	"Focus: standards" so the reader knows which lens this pass used.
   171	Do not modify any files. Do not exit until the review is complete.
   172	```
   173	
   174	Codex writes its review to `.renmark/reviews/YYYY-MM-DD-<sha>.review.md` directly (or the skill captures stdout and writes it).
   175	
   176	## Steps
   177	
   178	**Step 0 — Context check.** Call `lifecycle.skill_preamble(repo, 'codereview')`. If it returns a non-None hint, surface as a one-line note.
   179	
   180	**Lifecycle note:** Codereview is orthogonal to stage progression but commonly runs as part of the Review stage. Skill should NOT bump `lifecycle.json.stage` directly — the wrapper `/renmark:feature` handles that after both codereview AND any secure audit complete.
   181	
   182	### 1. Determine scope & classify the tier
   183	
   184	If the user gave a ref range (`HEAD~3..HEAD`, `main..feature`), use that. Otherwise default to `git diff --name-only HEAD` (working tree). Show a `git diff --stat <range>` summary and confirm with the user.
   185	
   186	Then classify the diff to make the review **proportional** to its size/risk:
   187	
   188	```python
   189	from renmark.sizing import classify_diff
   190	tier = classify_diff(repo, base_ref)   # 'lite' | 'standard' | 'full'
   191	```
   192	
   193	`classify_diff` is deterministic and zero-LLM: it runs `git diff --stat` under the
   194	hood and degrades to `'standard'` (the safe middle) on no-git / error. Resolve the
   195	final lane:
   196	
   197	- `--skip` present → skipped (explicit). State it and stop — no engine runs.
   198	- `--full` present → **full codex**, regardless of `tier`.
   199	- else `tier == 'lite'` → cheap **`/review`** (in-context).
   200	- else (`tier ∈ {standard, full}`) → **full codex**.
   201	
   202	**State the diff tier and the chosen review BEFORE running anything**, e.g.
   203	`Diff tier: lite — running cheap /review (escalate to codex with --full).` or
   204	`Diff tier: standard — running full codex review.` Never start a review without
   205	surfacing this line.
   206	
   207	### 2. Run the chosen review
   208	
   209	**Cheap lane (`lite`, no `--full`):** invoke the built-in **`/review`** skill over
   210	the same range, in-context. Report its findings inline. Do not write a `.review.md`
   211	artifact — proceed to Hand off, which always offers the escalate.
   212	
   213	**Full lane (`standard`/`full`, or `--full`):** shell out via the renmark CLI (or
   214	directly) with the focus prompt above, piped to `codex exec --sandbox read-only -`.
   215	Streaming output goes to `.renmark/logs/codereview-<run_id>.log` for troubleshooting
   216	if codex misbehaves.
   217	
   218	### 3. Capture the review
   219	
   220	**Full lane only:** codex output is parsed (or written through verbatim) and saved to
   221	`.renmark/reviews/YYYY-MM-DD-<sha>.review.md`. The cheap `/review` lane reports
   222	in-context and writes no artifact — there is no `<path>` to report for it.
   223	
   224	### 4. Hand off
   225	
   226	Tell the user — using ONLY the summary, never the diff body.
   227	
   228	**Cheap-lane hand-off (ran `/review`):** state which review ran and ALWAYS offer the
   229	escalate. Lead with:
   230	
   231	> *"Diff tier: lite — ran cheap `/review` in-context. <N critical, M major, K minor> findings.*
   232	> *What's next?*
   233	> *  1. [full] Escalate — re-run as the full codex pass (`/renmark:codereview --full <range>`)*
   234	> *  2. [fix] Fix — kick off a new /renmark:plan built from the findings"*
   235	
   236	The `[full]` escalate is mandatory on the cheap lane — never present the cheap result
   237	as final without it.
   238	
   239	**Full-lane hand-off (ran codex):** lead with the codereview-specific actions, then
   240	render the shared quality-gate menu so re-testing from a different angle stays one
   241	keystroke away:
   242	
   243	> *"Review at `<path>` (focus: <mode>). <N critical, M major, K minor> findings.*
   244	> *What's next?*
   245	> *  1. [o] Open — open the review file to read the full findings*
   246	> *  2. [fix] Fix — kick off a new /renmark:plan built from the critical findings"*
   247	
   248	Omit the `(focus: <mode>)` parenthetical entirely when mode is default — preserves the existing terse output for the common case.
   249	
   250	Then append the hand-off menu from `${CLAUDE_PLUGIN_ROOT}/skills/_shared/handoff-menu.md`, applying the rendering rules:
   251	
   252	- **Omit `[c] Code review`** — we just ran it.
   253	- **Show `[s] Smoke`** and `[qa] QA` (a finding worth re-verifying live often lives in the just-reviewed diff).
   254	- **Show `[dq] Deep QA`** only if a passing `.qa.md` exists for the current sha.
   255	- **Show `[d] Debug`** only if any Critical findings exist (Major+ alone is informational, not a debug trigger).
   256	- **Show `[f] Finish`** unconditionally and `[n] Nothing` always.
   257	
   258	Treat the `[o]`/`[fix]` actions plus the filtered gate options as ONE combined
   259	menu. **Present it as an interactive `AskUserQuestion` choice when available**
   260	(PRIMARY) — one selectable choice per option (`label` = action + code, e.g.
   261	`Fix [fix]`, `Open [o]`, `Code review`… ). This combined menu usually exceeds
   262	the picker's 4-option cap, so apply handoff-menu.md rule 6's overflow path:
   263	surface the **4 highest-priority** as selectable choices (priority: `[fix]` on
   264	critical findings → `[qa]` → `[f]` → `[o]`/`[s]`, always keep `[n] Nothing`) AND
   265	print the **full combined numbered list** beneath as reference, so the overflow
   266	options stay reachable by typed number/letter. **Fall back** to the numbered list
   267	entirely when `AskUserQuestion` is unavailable / non-interactive / errors. Require
   268	an explicit choice before doing anything.
   269	
   270	Don't auto-fix. The human reads and decides.
   271	
   272	## When to invoke
   273	
   274	Recommended cadence (for context hygiene):
   275	
   276	- **Auto-suggested by `/renmark:orchestrate`** after a successful plan run completes — one review for the whole feature, not one per task.
   277	- **Before merge** when you're about to land work to main.
   278	- **Ad-hoc** when you want a sanity check on a specific range.
   279	
   280	Avoid: running codereview after every single task. That creates one review per file and floods the reviews directory.
   281	
   282	## Reference
   283	
   284	- Proportional classifier: `renmark.sizing.classify_diff(repo, base_ref='main') -> Tier`
   285	  where `Tier ∈ {lite, standard, full}` — deterministic, zero-LLM, degrades to
   286	  `standard` on no-git / error. Single source of truth for which lane runs.
   287	- Codex review syntax: `codex review --help`
   288	- Built-in cheap **`/review`** slash command — the lite lane's in-context engine
   289	  (also good for inspiration on prompt shape).
   290	- Tier-override flags: `--full` (force codex), `--skip` (explicit skip) — see
   291	  Argument parsing. These always win over the inferred tier.
   292	- Focus modes: see Argument parsing above. Adding a new focus = adding a new `### Prompt: <name>` block; nothing else to change.

exec
/bin/bash -lc "nl -ba /home/renmark/projects/ai-system/tests/test_sizing.py | sed -n '1,260p'" in /home/renmark/projects/ai-system
 succeeded in 0ms:
     1	"""Unit tests for renmark.sizing — the proportional-pipeline tier classifier.
     2	
     3	Hermetic: no network, no real git history dependence (we init throwaway repos
     4	in tmp_path or monkeypatch the git subprocess). Thresholds are imported as
     5	module constants so tuning them never silently breaks these tests.
     6	"""
     7	from __future__ import annotations
     8	
     9	import subprocess
    10	from pathlib import Path
    11	
    12	import pytest
    13	
    14	from renmark import sizing
    15	from renmark.parser import Task
    16	from renmark.sizing import (
    17	    DEFAULT_TIER,
    18	    FULL_MIN_TASKS,
    19	    LITE_MAX_TASKS,
    20	    TIER_FULL,
    21	    TIER_LITE,
    22	    TIER_STANDARD,
    23	    classify_diff,
    24	    classify_plan,
    25	)
    26	
    27	
    28	def _task(
    29	    index: int,
    30	    target: str,
    31	    *,
    32	    complexity: str = "medium",
    33	    est_tokens: int | None = None,
    34	) -> Task:
    35	    """Build a minimal valid Task for classification (parser not exercised)."""
    36	    return Task(
    37	        index=index,
    38	        title=f"task {index}",
    39	        mode="A",
    40	        target=target,
    41	        verifier="true",
    42	        spec="do a thing",
    43	        complexity=complexity,
    44	        est_tokens=est_tokens,
    45	    )
    46	
    47	
    48	# ── classify_plan ────────────────────────────────────────────────────────────
    49	
    50	
    51	def test_all_doc_small_set_is_lite() -> None:
    52	    """<= LITE_MAX_TASKS doc/config targets, no hard, no core → lite."""
    53	    tasks = [_task(i + 1, f"docs/page{i}.md") for i in range(LITE_MAX_TASKS)]
    54	    assert len(tasks) <= LITE_MAX_TASKS
    55	    assert classify_plan(tasks) == TIER_LITE
    56	
    57	
    58	def test_any_hard_task_is_never_lite() -> None:
    59	    """A single hard task forbids the lite lane (>= standard)."""
    60	    tasks = [
    61	        _task(1, "docs/a.md"),
    62	        _task(2, "docs/b.md", complexity="hard"),
    63	    ]
    64	    result = classify_plan(tasks)
    65	    assert result in {TIER_STANDARD, TIER_FULL}
    66	    assert result != TIER_LITE
    67	
    68	
    69	def test_core_module_target_is_at_least_standard() -> None:
    70	    """A single simple edit to a core module (renmark/) must not be lite."""
    71	    tasks = [_task(1, "renmark/parser.py", complexity="simple")]
    72	    result = classify_plan(tasks)
    73	    assert result in {TIER_STANDARD, TIER_FULL}
    74	    assert result != TIER_LITE
    75	
    76	
    77	def test_many_tasks_is_full() -> None:
    78	    """Strictly more than FULL_MIN_TASKS tasks → full regardless of content."""
    79	    n = FULL_MIN_TASKS + 1
    80	    tasks = [_task(i + 1, f"docs/page{i}.md") for i in range(n)]
    81	    assert classify_plan(tasks) == TIER_FULL
    82	
    83	
    84	def test_empty_list_degrades_to_standard() -> None:
    85	    assert classify_plan([]) == TIER_STANDARD
    86	    assert DEFAULT_TIER == TIER_STANDARD
    87	
    88	
    89	def test_classify_plan_never_raises_on_malformed_input() -> None:
    90	    """None-ish / malformed task lists degrade safely, never raise."""
    91	    assert classify_plan(None) == DEFAULT_TIER  # type: ignore[arg-type]
    92	    # A list of objects missing the expected attributes must not raise.
    93	    assert classify_plan([object(), object()]) in {  # type: ignore[list-item]
    94	        TIER_LITE,
    95	        TIER_STANDARD,
    96	        TIER_FULL,
    97	    }
    98	
    99	
   100	# ── classify_diff ──────────────────────────────────────────────────────────
   101	
   102	
   103	def _init_repo(path: Path) -> None:
   104	    def run(*args: str) -> None:
   105	        subprocess.run(
   106	            ["git", "-C", str(path), *args],
   107	            check=True,
   108	            capture_output=True,
   109	            text=True,
   110	        )
   111	
   112	    run("init", "-q")
   113	    run("config", "user.email", "test@example.com")
   114	    run("config", "user.name", "test")
   115	    run("config", "commit.gpgsign", "false")
   116	
   117	
   118	def _commit_all(path: Path, msg: str) -> None:
   119	    subprocess.run(
   120	        ["git", "-C", str(path), "add", "-A"], check=True, capture_output=True, text=True
   121	    )
   122	    subprocess.run(
   123	        ["git", "-C", str(path), "commit", "-q", "-m", msg],
   124	        check=True,
   125	        capture_output=True,
   126	        text=True,
   127	    )
   128	
   129	
   130	def test_classify_diff_tiny_doc_change_is_lite(tmp_path: Path) -> None:
   131	    """A throwaway repo whose only diff vs base is a tiny doc edit → lite."""
   132	    repo = tmp_path / "repo"
   133	    repo.mkdir()
   134	    _init_repo(repo)
   135	    (repo / "README.md").write_text("hello\n", encoding="utf-8")
   136	    _commit_all(repo, "base")
   137	
   138	    # Capture the initial branch name (master or main depending on git config)
   139	    # BEFORE branching, so the base_ref is unambiguous.
   140	    base = subprocess.run(
   141	        ["git", "-C", str(repo), "rev-parse", "--abbrev-ref", "HEAD"],
   142	        check=True,
   143	        capture_output=True,
   144	        text=True,
   145	    ).stdout.strip()
   146	
   147	    # Branch HEAD ahead of base with a tiny doc-only change.
   148	    subprocess.run(
   149	        ["git", "-C", str(repo), "checkout", "-q", "-b", "work"],
   150	        check=True,
   151	        capture_output=True,
   152	        text=True,
   153	    )
   154	    (repo / "README.md").write_text("hello\nworld\n", encoding="utf-8")
   155	    _commit_all(repo, "tiny doc edit")
   156	
   157	    assert classify_diff(repo, base_ref=base) == TIER_LITE
   158	
   159	
   160	def test_classify_diff_no_git_degrades_to_standard(tmp_path: Path) -> None:
   161	    """A non-repo directory must degrade to standard without raising."""
   162	    not_a_repo = tmp_path / "plain"
   163	    not_a_repo.mkdir()
   164	    assert classify_diff(not_a_repo) == DEFAULT_TIER
   165	
   166	
   167	def test_classify_diff_monkeypatched_large_diff_is_full(
   168	    monkeypatch: pytest.MonkeyPatch,
   169	) -> None:
   170	    """Monkeypatch the git subprocess to return a large --stat → full."""
   171	
   172	    class _Proc:
   173	        returncode = 0
   174	        stdout = (
   175	            " a.py | 200 ++++++++++\n"
   176	            " b.py | 250 ++++++++++\n"
   177	            " 2 files changed, 400 insertions(+), 50 deletions(-)\n"
   178	        )
   179	
   180	    def fake_run(*args: object, **kwargs: object) -> _Proc:
   181	        return _Proc()
   182	
   183	    monkeypatch.setattr(sizing.subprocess, "run", fake_run)
   184	    assert classify_diff("/whatever") == TIER_FULL
   185	
   186	
   187	def test_classify_diff_monkeypatched_tiny_doc_is_lite(
   188	    monkeypatch: pytest.MonkeyPatch,
   189	) -> None:
   190	    """Monkeypatch git to a tiny doc-only --stat → lite (no real repo)."""
   191	
   192	    class _Proc:
   193	        returncode = 0
   194	        stdout = (
   195	            " docs/readme.md | 4 ++--\n"
   196	            " 1 file changed, 2 insertions(+), 2 deletions(-)\n"
   197	        )
   198	
   199	    def fake_run(*args: object, **kwargs: object) -> _Proc:
   200	        return _Proc()
   201	
   202	    monkeypatch.setattr(sizing.subprocess, "run", fake_run)
   203	    assert classify_diff("/whatever") == TIER_LITE

exec
/bin/bash -lc "nl -ba /home/renmark/projects/ai-system/renmark/sizing.py | sed -n '240,340p'" in /home/renmark/projects/ai-system
 succeeded in 0ms:
   240	    if not any(norm.startswith(root) for root in CORE_MODULE_ROOTS):
   241	        return False
   242	    return Path(norm).suffix.lower() in CODE_SUFFIXES or Path(norm).suffix == ""
   243	
   244	
   245	# ── Internal: Task field accessors (defensive — never raise) ─────────────────
   246	
   247	
   248	def _task_complexity(task: Task) -> str:
   249	    value = getattr(task, "complexity", "medium")
   250	    return value.strip().lower() if isinstance(value, str) else "medium"
   251	
   252	
   253	def _task_target(task: Task) -> str:
   254	    value = getattr(task, "target", "")
   255	    return value if isinstance(value, str) else ""
   256	
   257	
   258	def _task_est_tokens(task: Task) -> int:
   259	    value = getattr(task, "est_tokens", None)
   260	    return value if isinstance(value, int) and value > 0 else 0
   261	
   262	
   263	# ── Internal: git ─────────────────────────────────────────────────────────────
   264	
   265	
   266	def _git_diff_stat(repo: Path | str, base_ref: str) -> tuple[list[str], int] | None:
   267	    """Return ``(changed_files, total_lines_changed)`` for ``<base_ref>..HEAD``.
   268	
   269	    Parses the trailing summary line of ``git diff --stat`` for line counts and
   270	    each preceding ``path | N`` row for filenames. Returns ``None`` on any
   271	    failure (no git, non-repo, unknown ref, timeout, empty output).
   272	    """
   273	    try:
   274	        proc = subprocess.run(
   275	            ["git", "-C", str(repo), "diff", "--stat", f"{base_ref}..HEAD"],
   276	            capture_output=True,
   277	            text=True,
   278	            check=False,
   279	            timeout=GIT_TIMEOUT_S,
   280	        )
   281	    except (subprocess.TimeoutExpired, FileNotFoundError, OSError):
   282	        return None
   283	    if proc.returncode != 0:
   284	        return None
   285	
   286	    files: list[str] = []
   287	    total_lines = 0
   288	    for raw in proc.stdout.splitlines():
   289	        line = raw.strip()
   290	        if not line:
   291	            continue
   292	        if "|" in line:
   293	            # "path/to/file.py | 12 +++---"  → take the path before the pipe.
   294	            path = line.split("|", 1)[0].strip()
   295	            if path:
   296	                files.append(path)
   297	        elif "changed" in line and ("insertion" in line or "deletion" in line):
   298	            total_lines += _parse_summary_line(line)
   299	    return files, total_lines
   300	
   301	
   302	def _parse_summary_line(line: str) -> int:
   303	    """Extract insertions+deletions from a ``git diff --stat`` summary line.
   304	
   305	    e.g. ``"3 files changed, 12 insertions(+), 4 deletions(-)"`` → ``16``.
   306	    Returns ``0`` if no counts can be read.
   307	    """
   308	    total = 0
   309	    for chunk in line.split(","):
   310	        chunk = chunk.strip()
   311	        if "insertion" in chunk or "deletion" in chunk:
   312	            head = chunk.split()[0] if chunk.split() else ""
   313	            if head.isdigit():
   314	                total += int(head)
   315	    return total

codex
I’ve identified a few contract mismatches already. I’m running the sizing tests now to confirm current behavior, then I’ll finalize findings with severity based on whether they can actually drive a false `lite`.
exec
/bin/bash -lc 'pytest -q /home/renmark/projects/ai-system/tests/test_sizing.py' in /home/renmark/projects/ai-system
 exited 1 in 207ms:
Traceback (most recent call last):
  File "/opt/miniconda/envs/claude/bin/pytest", line 6, in <module>
    sys.exit(console_main())
             ~~~~~~~~~~~~^^
  File "/opt/miniconda/envs/claude/lib/python3.14/site-packages/_pytest/config/__init__.py", line 223, in console_main
    code = main()
  File "/opt/miniconda/envs/claude/lib/python3.14/site-packages/_pytest/config/__init__.py", line 193, in main
    config = _prepareconfig(new_args, plugins)
  File "/opt/miniconda/envs/claude/lib/python3.14/site-packages/_pytest/config/__init__.py", line 361, in _prepareconfig
    config: Config = pluginmanager.hook.pytest_cmdline_parse(
                     ~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~^
        pluginmanager=pluginmanager, args=args
        ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^
    )
    ^
  File "/opt/miniconda/envs/claude/lib/python3.14/site-packages/pluggy/_hooks.py", line 512, in __call__
    return self._hookexec(self.name, self._hookimpls.copy(), kwargs, firstresult)
           ~~~~~~~~~~~~~~^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^
  File "/opt/miniconda/envs/claude/lib/python3.14/site-packages/pluggy/_manager.py", line 120, in _hookexec
    return self._inner_hookexec(hook_name, methods, kwargs, firstresult)
           ~~~~~~~~~~~~~~~~~~~~^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^
  File "/opt/miniconda/envs/claude/lib/python3.14/site-packages/pluggy/_callers.py", line 167, in _multicall
    raise exception
  File "/opt/miniconda/envs/claude/lib/python3.14/site-packages/pluggy/_callers.py", line 139, in _multicall
    teardown.throw(exception)
    ~~~~~~~~~~~~~~^^^^^^^^^^^
  File "/opt/miniconda/envs/claude/lib/python3.14/site-packages/_pytest/helpconfig.py", line 124, in pytest_cmdline_parse
    config = yield
             ^^^^^
  File "/opt/miniconda/envs/claude/lib/python3.14/site-packages/pluggy/_callers.py", line 121, in _multicall
    res = hook_impl.function(*args)
  File "/opt/miniconda/envs/claude/lib/python3.14/site-packages/_pytest/config/__init__.py", line 1192, in pytest_cmdline_parse
    self.parse(args)
    ~~~~~~~~~~^^^^^^
  File "/opt/miniconda/envs/claude/lib/python3.14/site-packages/_pytest/config/__init__.py", line 1562, in parse
    self.hook.pytest_load_initial_conftests(
    ~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~^
        early_config=self, args=args, parser=self._parser
        ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^
    )
    ^
  File "/opt/miniconda/envs/claude/lib/python3.14/site-packages/pluggy/_hooks.py", line 512, in __call__
    return self._hookexec(self.name, self._hookimpls.copy(), kwargs, firstresult)
           ~~~~~~~~~~~~~~^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^
  File "/opt/miniconda/envs/claude/lib/python3.14/site-packages/pluggy/_manager.py", line 120, in _hookexec
    return self._inner_hookexec(hook_name, methods, kwargs, firstresult)
           ~~~~~~~~~~~~~~~~~~~~^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^
  File "/opt/miniconda/envs/claude/lib/python3.14/site-packages/pluggy/_callers.py", line 167, in _multicall
    raise exception
  File "/opt/miniconda/envs/claude/lib/python3.14/site-packages/pluggy/_callers.py", line 139, in _multicall
    teardown.throw(exception)
    ~~~~~~~~~~~~~~^^^^^^^^^^^
  File "/opt/miniconda/envs/claude/lib/python3.14/site-packages/_pytest/warnings.py", line 128, in pytest_load_initial_conftests
    return (yield)
            ^^^^^
  File "/opt/miniconda/envs/claude/lib/python3.14/site-packages/pluggy/_callers.py", line 116, in _multicall
    next(function_gen)  # first yield
    ~~~~^^^^^^^^^^^^^^
  File "/opt/miniconda/envs/claude/lib/python3.14/site-packages/_pytest/capture.py", line 170, in pytest_load_initial_conftests
    capman.start_global_capturing()
    ~~~~~~~~~~~~~~~~~~~~~~~~~~~~~^^
  File "/opt/miniconda/envs/claude/lib/python3.14/site-packages/_pytest/capture.py", line 773, in start_global_capturing
    self._global_capturing = _get_multicapture(self._method)
                             ~~~~~~~~~~~~~~~~~^^^^^^^^^^^^^^
  File "/opt/miniconda/envs/claude/lib/python3.14/site-packages/_pytest/capture.py", line 714, in _get_multicapture
    return MultiCapture(in_=FDCapture(0), out=FDCapture(1), err=FDCapture(2))
                                              ~~~~~~~~~^^^
  File "/opt/miniconda/envs/claude/lib/python3.14/site-packages/_pytest/capture.py", line 493, in __init__
    TemporaryFile(buffering=0),
    ~~~~~~~~~~~~~^^^^^^^^^^^^^
  File "/opt/miniconda/envs/claude/lib/python3.14/tempfile.py", line 652, in TemporaryFile
    prefix, suffix, dir, output_type = _sanitize_params(prefix, suffix, dir)
                                       ~~~~~~~~~~~~~~~~^^^^^^^^^^^^^^^^^^^^^
  File "/opt/miniconda/envs/claude/lib/python3.14/tempfile.py", line 126, in _sanitize_params
    dir = gettempdir()
  File "/opt/miniconda/envs/claude/lib/python3.14/tempfile.py", line 316, in gettempdir
    return _os.fsdecode(_gettempdir())
                        ~~~~~~~~~~~^^
  File "/opt/miniconda/envs/claude/lib/python3.14/tempfile.py", line 309, in _gettempdir
    tempdir = _get_default_tempdir()
  File "/opt/miniconda/envs/claude/lib/python3.14/tempfile.py", line 224, in _get_default_tempdir
    raise FileNotFoundError(_errno.ENOENT,
                            "No usable temporary directory found in %s" %
                            dirlist)
FileNotFoundError: [Errno 2] No usable temporary directory found in ['/tmp', '/var/tmp', '/usr/tmp', '/home/renmark/projects/ai-system']

exec
/bin/bash -lc "python - <<'PY'
from renmark.sizing import classify_plan, classify_diff
print('malformed plan:', classify_plan([object()]))
print('none plan:', classify_plan(None))
PY" in /home/renmark/projects/ai-system
 succeeded in 0ms:
malformed plan: lite
none plan: standard

exec
/bin/bash -lc "python - <<'PY'
from pathlib import Path
from renmark.sizing import _is_doc_or_config
for p in ['plugin/templates/runtime.py','src/templates_engine.py','docs/readme.md','renmark/README.md']:
    print(p, _is_doc_or_config(p))
PY" in /home/renmark/projects/ai-system
 succeeded in 0ms:
plugin/templates/runtime.py True
src/templates_engine.py True
docs/readme.md True
renmark/README.md True

codex
The implementation does return `lite` for malformed task objects right now, so the defensive-contract bug is real, not hypothetical. I’m checking a couple of range/parsing edge cases next to decide which ones are genuine safety issues versus just documentation mismatches.
exec
/bin/bash -lc "python - <<'PY'
from renmark.sizing import classify_diff
print('bad base:', classify_diff('/home/renmark/projects/ai-system', base_ref='definitely-not-a-ref'))
PY" in /home/renmark/projects/ai-system
 succeeded in 0ms:
bad base: standard

exec
/bin/bash -lc "python - <<'PY'
from renmark.sizing import _git_diff_stat
print(_git_diff_stat('/home/renmark/projects/ai-system', 'main..HEAD'))
PY" in /home/renmark/projects/ai-system
 succeeded in 0ms:
None

exec
/bin/bash -lc "python - <<'PY'
from renmark.sizing import _parse_summary_line
for line in [
    '1 file changed, 2 insertions(+), 1 deletion(-)',
    '1 file changed, 3 deletions(-)',
    '1 file changed, 4 insertions(+)',
    '1 file changed'
]:
    print(line, '=>', _parse_summary_line(line))
PY" in /home/renmark/projects/ai-system
 succeeded in 0ms:
1 file changed, 2 insertions(+), 1 deletion(-) => 3
1 file changed, 3 deletions(-) => 3
1 file changed, 4 insertions(+) => 4
1 file changed => 0

exec
/bin/bash -lc "python - <<'PY'
from renmark.sizing import classify_diff, _git_diff_stat
print('tier main:', classify_diff('/home/renmark/projects/ai-system', 'main'))
print('stat main:', _git_diff_stat('/home/renmark/projects/ai-system', 'main'))
PY" in /home/renmark/projects/ai-system
 succeeded in 0ms:
tier main: full
stat main: (['.renmark/memory/decisions.md', '.renmark/memory/features.md', '.renmark/memory/learnings.md', '.renmark/memory/routing.md', '.renmark/memory/stack.md', '.../plans/2026-06-08-proportional-pipeline.plan.md', '...45e82e95692521316732de4a364836c.verification.md', '.../specs/2026-06-08-proportional-pipeline.spec.md', 'CHANGELOG.md', 'plugin/skills/codereview/SKILL.md', 'plugin/skills/feature/SKILL.md', 'renmark/sizing.py', 'tests/test_sizing.py'], 1107)

codex
I found a second concrete false-`lite` path: any file path containing the substring `template` is treated as doc/config, even for real code like `src/templates_engine.py`. I’m confirming that it propagates all the way to `classify_diff` before I lock the severity.
exec
/bin/bash -lc "python - <<'PY'
from renmark.sizing import classify_plan
class T:
    complexity='simple'
    target='src/templates_engine.py'
    est_tokens=100
print(classify_plan([T()]))
PY" in /home/renmark/projects/ai-system
 succeeded in 0ms:
lite

exec
/bin/bash -lc "python - <<'PY'
from renmark import sizing
class _Proc:
    returncode = 0
    stdout = ' src/templates_engine.py | 4 ++--\\n 1 file changed, 2 insertions(+), 2 deletions(-)\\n'
def fake_run(*args, **kwargs):
    return _Proc()
orig = sizing.subprocess.run
sizing.subprocess.run = fake_run
try:
    print(sizing.classify_diff('/whatever'))
finally:
    sizing.subprocess.run = orig
PY" in /home/renmark/projects/ai-system
 succeeded in 0ms:
lite

codex
Summary counts: Critical 2, Major 2, Minor 1, Nit 0.

- `renmark/sizing.py:227`; `Critical`; `_is_doc_or_config()` treats any path containing `template`/`templates` as doc/config, so real code like `src/templates_engine.py` is classified as `lite` by both `classify_plan()` and `classify_diff()`, which is exactly the false-`lite` failure mode the safety contract is supposed to prevent.  
  Fix: only treat templates as doc/config when they match a constrained pattern such as an exact path component or known template suffix (for example `.template` / `.j2`), and never let a code-suffixed file become doc/config from a substring match alone.

- `plugin/skills/feature/SKILL.md:37,118`; `Critical`; the new `--lite` override explicitly forces the lite lane even for `hard` tasks or core-module edits, which bypasses the classifier’s “never lite” floors and allows risky changes to skip the full codereview path.  
  Fix: remove forced `--lite`, or gate it behind the same hard/core/full checks as `classify_plan()` so explicit overrides can only narrow `standard` cases, never downgrade safety-signaled work.

- `renmark/sizing.py:161`; `Major`; `classify_plan()` returns `lite` for malformed task objects or tasks with missing targets because `total_est <= LITE_MAX_EST_TOKENS` is sufficient even when target extraction failed, and `classify_plan([object()])` currently returns `lite`.  
  Fix: require every task to have a non-empty valid target (and ideally validated shape) before the lite branch is reachable, otherwise return `DEFAULT_TIER`.

- `tests/test_sizing.py:89`; `Major`; the malformed-input test explicitly allows any tier, so it blesses the current false-`lite` regression instead of enforcing the documented “degrade to standard” contract, and the suite never covers the template-path misclassification either.  
  Fix: change the malformed-task assertion to require `DEFAULT_TIER`, and add regression tests for code paths containing `template` plus invalid-ref / timeout / unsupported-range cases.

- `plugin/skills/codereview/SKILL.md:184`; `Minor`; the skill says it proportionally classifies arbitrary review ranges like `HEAD~3..HEAD`, `main..feature`, and the default working-tree diff, but `classify_diff(repo, base_ref)` only understands `<base_ref>..HEAD`, so the documented lane-selection logic cannot actually classify several advertised scopes.  
  Fix: either extend `classify_diff()` to accept an explicit diff range/worktree mode, or narrow the skill contract to supported `base_ref` inputs and degrade explicitly for anything else.

`pytest -q tests/test_sizing.py` could not be run in this environment because `pytest` failed before collection with `FileNotFoundError: No usable temporary directory found`.
tokens used
50,932
Summary counts: Critical 2, Major 2, Minor 1, Nit 0.

- `renmark/sizing.py:227`; `Critical`; `_is_doc_or_config()` treats any path containing `template`/`templates` as doc/config, so real code like `src/templates_engine.py` is classified as `lite` by both `classify_plan()` and `classify_diff()`, which is exactly the false-`lite` failure mode the safety contract is supposed to prevent.  
  Fix: only treat templates as doc/config when they match a constrained pattern such as an exact path component or known template suffix (for example `.template` / `.j2`), and never let a code-suffixed file become doc/config from a substring match alone.

- `plugin/skills/feature/SKILL.md:37,118`; `Critical`; the new `--lite` override explicitly forces the lite lane even for `hard` tasks or core-module edits, which bypasses the classifier’s “never lite” floors and allows risky changes to skip the full codereview path.  
  Fix: remove forced `--lite`, or gate it behind the same hard/core/full checks as `classify_plan()` so explicit overrides can only narrow `standard` cases, never downgrade safety-signaled work.

- `renmark/sizing.py:161`; `Major`; `classify_plan()` returns `lite` for malformed task objects or tasks with missing targets because `total_est <= LITE_MAX_EST_TOKENS` is sufficient even when target extraction failed, and `classify_plan([object()])` currently returns `lite`.  
  Fix: require every task to have a non-empty valid target (and ideally validated shape) before the lite branch is reachable, otherwise return `DEFAULT_TIER`.

- `tests/test_sizing.py:89`; `Major`; the malformed-input test explicitly allows any tier, so it blesses the current false-`lite` regression instead of enforcing the documented “degrade to standard” contract, and the suite never covers the template-path misclassification either.  
  Fix: change the malformed-task assertion to require `DEFAULT_TIER`, and add regression tests for code paths containing `template` plus invalid-ref / timeout / unsupported-range cases.

- `plugin/skills/codereview/SKILL.md:184`; `Minor`; the skill says it proportionally classifies arbitrary review ranges like `HEAD~3..HEAD`, `main..feature`, and the default working-tree diff, but `classify_diff(repo, base_ref)` only understands `<base_ref>..HEAD`, so the documented lane-selection logic cannot actually classify several advertised scopes.  
  Fix: either extend `classify_diff()` to accept an explicit diff range/worktree mode, or narrow the skill contract to supported `base_ref` inputs and degrade explicitly for anything else.

`pytest -q tests/test_sizing.py` could not be run in this environment because `pytest` failed before collection with `FileNotFoundError: No usable temporary directory found`.
