---
artifact_type: review
schema_version: 1
created_at: 2026-06-09T22:30:00-04:00
source_sha: 564500b
related_plan: .renmark/audits/audit-delta-2026-06-09.md
generator: claude-fable-5 (4 adversarial reviewers: 2 opus, 2 sonnet — codex CLI unavailable on this host, so the full-lane review ran on Claude reviewers at equivalent depth)
dependency_refs:
  - .renmark/audits/audit-delta-2026-06-09.md
completion_state: complete
confidence: high
validation_status: validated
---

# Codereview — v0.9.0 release diff (f4d833c..564500b, 143 files, +4657/−2185)

## Severity summary

| reviewer | critical | major | minor |
|---|---|---|---|
| engine (cli/_engine, codex, commands, state/usage) | 1 | 1 | 2 |
| state (lifecycle, schemas, dispatch, validators) | 0 | 1 | 0 |
| features (audit.py, new skills, small modules) | 0 | 3 | 3 |
| docs/skills/PRD | 1 | 3 | 0 |
| governance gate (root docs + templates) | — | 3 | 5 |
| **total** | **2** | **11** | **10** |

## Critical (both FIXED before release)

1. **cmd_task PASS payload broke the G11 parser contract** — emitted
   validation_status/parser_success/schema_compliance, which
   `parse_subagent_response` rejected as forbidden fields → every successful
   codex task in the orchestrate flow would be classified FAIL. Test suite was
   blind (no round-trip test). FIX: the G9 triplet is now part of
   `SubagentOutput`/`SUBAGENT_OUTPUT_FIELDS`/schemas (per the charter's
   "all six fields" rule); all four early-fail payloads emit it too; two
   round-trip regression tests added; dispatch shadow baseline re-accepted.
2. **orchestrate SKILL event snippets used `analytics` without importing it**
   (2 sites) → literal execution NameError. FIX: imports added.

## Major (all FIXED except #7, which awaits the human PRD gate)

3. parser `_LOOSE_TASK_RE` raised PlanError on legitimate prose headings
   ("### Task overview") — core pipeline breakage on narrative plans. FIX:
   digit-required loose pattern; word-numbered typos revert to
   absorbed-and-caught-by-index-check (documented); tests rewritten.
4. `run_audit` crashed (FileNotFoundError) in user projects without a VERSION
   file — the exact environment the skill ships to. FIX: degrade to advisory
   line; regression test.
5. `write_pipeline_state` raised on legacy out-of-vocab `current_phase`
   (read-modify-write asymmetry vs lifecycle's deliberate tolerance). FIX:
   read-side normalization to "idle"; regression test.
6. finish §4c-post snippet referenced undefined `version` → NameError. FIX:
   defined from VERSION.
7. **prd/SKILL.md's gate-clear writes `human_review_completed=False`, which
   REQ-18's "sole surface for flipping" wording forbids.** NOT FIXED — PRD
   wording amendment proposed to the human gate (see release approval bundle);
   recommended REQ-18 clarification: approve is the sole surface for
   *setting True*; consuming skills clear the bit after acting.
8. cmd_task early-fail paths omitted the G9 triplet (inconsistent with the
   PASS path). FIX: all five payload paths emit all six fields.
9. Governance gate: finish's tooling-table row omitted zip/snapshot behavior
   (4 files) — FIXED; "ships in 0.9.0" future tense in root+template — FIXED;
   NIM (removed provider) seeded into every new project via
   memory/learnings+stack templates — FIXED.
10. bootstrap guard tested `is_empty_project.__doc__` (always truthy) instead
    of the predicate — FIXED to the intended `not created` guard.
11. approve SKILL snippet had a SyntaxError-on-copy (`for d in X if c else []`)
    — FIXED with parentheses.

## Minor — fixed in this pass

`documented` stage now annotated dormant (root+template); "consolidated
v0.3.2" stale note reworded; release.py PACKAGE_ALLOW comment accuracy noted
in code review (comment scope).

## Minor — accepted as known leftovers (not release-blocking)

- `_engine` TOCTOU between `_untracked_paths` and `_rollback_paths` (two lock
  acquisitions; blast radius = this task's own already-rejected artifact).
- Residual sibling-rollback race for *undeclared* concurrent writes (inherent
  to porcelain-based detection; sibling's write is itself rejected).
- `registry_sync` ignores skill dirs lacking SKILL.md (lint covers them).
- Porcelain octal-escape paths for non-ASCII filenames (pre-existing).
- hygiene argument-hint leading example cosmetic.

## Verified clean (refutation attempts failed)

Budget-exit ordering cannot fall through to success; `_skip_all_remaining` has
no off-by-one; resume keys off git-committed task indices; attempt-dedup
semantics; write_lifecycle legacy tolerance ("restored" in stages_completed
loads + rewrites); write_pause never raises on the new budget pauses;
record_event has no caller-dict side effect; analytics validation-stamp is not
self-invalidating; `_insert_after_section` correct across 4 edge cases;
registry⇄dirs exact parity; no circular imports; deleted modules have zero
stranded references; version parity 7/7; help list = 26 = IMPLEMENTED_SKILLS;
no ghost commands anywhere in skills/docs; CHANGELOG claims spot-checked
accurate.

## Verdict

**2 critical + 11 major found; 12 of 13 fixed and regression-tested; 1 (REQ-18
wording) deliberately routed to the human PRD gate.** Release may proceed once
the human approves the PRD diff.
