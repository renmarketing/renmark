<!--
artifact_type: verification
schema_version: 1
created_at: 2026-06-23
source_sha: d571eda
related_plan: 2026-06-23-auto-routing-default
generator: orchestrate
completion_state: complete
validation_status: validated
confidence: high
-->

# Verification — auto-routing-default

**Goal:** renmark becomes the default for build/dev work without the user typing slash commands; opt-in, reversible, never silent.

## Goal-backward checks (all confirmed)
- Triggers broadened on start/feature/debug (+shims) — `audit description-drift=0`.
- `routing-preference-rule` block in both templates + repo CLAUDE.md/AGENTS.md — template lint clean, back-fillable by init.merge_rule_blocks.
- `renmark/global_routing.py` helper — detect/create/append/skip, .bak backup, idempotent, no-clobber — 5/5 hermetic tests pass.
- `/renmark:doctor` advisory `[i]` check + `--fix` install — never flips pass/fail; real ~/.claude untouched by detect.
- init one-time offer + start light nudge + doctor docs (honest constraints: model-followed, explicit wins, next-session, user-approved, per-machine).

## Evidence
- pytest: 861 passed, 28 skipped (was 856 baseline + 5 new).
- ruff: clean. mypy: new files clean (3 pre-existing browser errors unrelated).
- audit: PASS (registry-sync=0, description-drift=0, lint=0, version-drift=0).

**Verdict: 5/5 requirements confirmed. PASS.**
