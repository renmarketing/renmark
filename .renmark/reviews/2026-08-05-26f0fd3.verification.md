---
artifact_type: verification
schema_version: 1
created_at: 2026-08-06T03:38:01+00:00
source_sha: 26f0fd3
related_plan: .renmark/plans/2026-08-05-governed-orchestration-assurance-release-15.plan.md
generator: sonnet
stale_after: null
dependency_refs: []
completion_state: complete
confidence: high
validation_status: validated
retry_count: 0
parser_success: true
schema_compliance: true
---

## Goal
Release 15 (behavioral eval suite): author all missing/incomplete fixture
groups and make the deterministic tier CI-gating. Owner-revised mid-flight
(2026-08-05) after investigation found the roadmap's fixture-split table
overstated reality -- only 1 of 7 claimed-already-authored groups actually
existed. Owner chose to close the gap for real: 9 fixture groups authored
(6 retroactive + 3 originally Release 15's own), then CI-gated.

## Bugs cross-referenced
No open bug touches `renmark/behavior.py`, `renmark/cli/_engine.py`, or
`.github/workflows/test.yml`. The risk_tier_lens_selection fixture bug
(found and fixed pre-plan, 2026-08-05) is closed, not open.

## Smoke checks (fresh run, this pass)
1. `pytest -q` -- 2098 passed, 32 skipped (full suite).
2. `renmark-execute --behavior` -- 16/16 passed, 0 failed (all 9 new
   groups + the 7 pre-existing groups, all wired to real adapters).
3. `pytest -q tests/test_behavior_ci_gating.py` -- 1 passed (subprocess
   regression guard, independent of the GitHub Actions file itself).
4. `.github/workflows/test.yml` -- visually confirmed the new
   "behavioral eval suite (deterministic tier)" step is present, correctly
   positioned after `pytest`, correct indentation, no other step touched.

## Requirements verified: 14/14 tasks
All 9 fixture-authoring tasks, all 3 sequential adapter-wiring tasks
(same file, opus), and both CI-gating tasks landed clean and independently
re-verified -- codex self-reported FAIL/schema_compliance=false on the
one codex dispatch this release (task 14) despite producing a correct,
passing regression test (same known ad-hoc-path unreliability pattern
logged throughout this program).

## Notable in-flight findings (not blocking, all resolved)
- Two genuine fixture defects found and fixed during adapter-wiring, both
  by the task's own investigation, neither hidden: a substring-collision
  assertion (`not_contains:patch` vs. the required field `dispatch_identity`
  containing "patch"), and a schema reality check (no cross-field
  `is_replannable`<->`replan_evidence` enforcement exists -- fixture
  adapted to what the code actually does, not an invented rule).
- Session usage-limit pause mid-Wave-1 (after 6/9 fixture tasks), recorded
  via `usage.classify_usage_pause`, resumed cleanly via
  `/renmark:orchestrate --resume` with zero rework needed.

## AC-8 (Req 8) status: closes
16 real, grounded cases exercise real production code across fast-path,
task-tracking, capability-envelope, judge, failure-rule, resume-identity,
replan-refusal, inspector-scope, and judge-override-refusal. CI now blocks
on any deterministic-tier regression. Honestly NOT "20 cases" as the
roadmap's prose implied -- 16 real cases, not padded.

## Deferred / open (not blocking this release)
- AC-13 closure path (2 guardrail metrics) -- tracked in bugs.md, unrelated
  to Release 15.
- Release 11's CLI-wiring gap -- still open, unrelated.

## Summary

- verify (smoke): Release 15 behavioral eval suite -- 14/14 tasks verified, 16/16 cases passing
- CI-gating live: renmark-execute --behavior now blocks the test workflow on any deterministic-tier FAIL
- AC-8 (Req 8) closes -- 16 real cases, honestly not padded to a fabricated '20' count
- Full suite: 2098 passed, 32 skipped
