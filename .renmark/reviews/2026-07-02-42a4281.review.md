Spec: under-built — `/renmark:eval` adds the new skill and keeps the protected boundaries untouched, but the skill does not fully specify the live `--judge` execution path or the promised `.renmark/reviews/*.eval.md` write, so the in-session eval flow is not documented end-to-end.

Quality: 0 Critical, 2 Major, 1 Minor, 0 Nit.

- [plugin/skills/eval/SKILL.md](/home/renmark/projects/ai-system/.claude/worktrees/feature-agent-turn-runner/plugin/skills/eval/SKILL.md:82) Major The `--judge` flow is not operationally complete: it tells the agent to call `judge.compose_judge_prompt(...)` but never defines how to obtain `baseline`, `golden`, or `actual`, so different agents will have to guess or will fail to run the judge path at all.  
  Fix: add explicit steps for sourcing each judge input, including which transcript is the just-captured with-skill output, where the golden comes from, and whether/how a baseline is produced in this in-session path.

- [plugin/skills/eval/SKILL.md](/home/renmark/projects/ai-system/.claude/worktrees/feature-agent-turn-runner/plugin/skills/eval/SKILL.md:97) Major The skill promises a `.renmark/reviews/*.eval.md` artifact and claims provenance comes from `summary.write_artifact`, but it never actually instructs the agent to call `summary.write_artifact` or defines the artifact contents, so the durable evidence path is left implicit.  
  Fix: add a concrete artifact-writing step with the API call, target path pattern, and the minimum per-case fields to persist.

- [tests/test_eval_agent_turn.py](/home/renmark/projects/ai-system/.claude/worktrees/feature-agent-turn-runner/tests/test_eval_agent_turn.py:54) Minor The new tests only assert substrings/non-empty prompts, so they do not pin the extracted wrappers to the exact byte-for-byte prompt contract this refactor is supposed to preserve.  
  Fix: assert the full expected `compose_eval_prompt()` string and compare `compose_judge_prompt(...)` to a fully specified expected prompt (or another exact contract fixture), not just key substrings.

Manual checks on the code paths themselves were otherwise clean. [`renmark/behavior.py`](/home/renmark/projects/ai-system/.claude/worktrees/feature-agent-turn-runner/renmark/behavior.py:596) preserves the original prompt text exactly, writes the snapshot in the same place, and returns the same transcript; [`renmark/judge.py`](/home/renmark/projects/ai-system/.claude/worktrees/feature-agent-turn-runner/renmark/judge.py:198) adds thin public delegators while `judge_behavior()` still uses `_build_prompt` and `_parse_response` directly. `renmark/providers/eval_runner.py`, the deterministic tier in `behavior.run`, and `renmark/dispatch.py` are not in the diff. The garbage-verdict case is covered by [`tests/test_eval_agent_turn.py`](/home/renmark/projects/ai-system/.claude/worktrees/feature-agent-turn-runner/tests/test_eval_agent_turn.py:157).
