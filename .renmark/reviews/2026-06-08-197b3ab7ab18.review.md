OpenAI Codex v0.117.0 (research preview)
--------
workdir: /home/renmark/projects/ai-system
model: gpt-5.4
provider: openai
approval: never
sandbox: read-only
reasoning effort: none
reasoning summaries: none
session id: 019ea93a-27dd-7793-80d6-fdfe3795fdff
--------
user
Review the diff main..HEAD. Focus CLOSEST on renmark/modularity.py — a new pure-ast, zero-dep, never-raise code-health analyzer: analyze(repo)->list[Gap] with 5 metrics (module LOC counting code-lines-only, function length, cyclomatic branch count, import fan-out, nesting-weighted cognitive complexity), two warn/major bands each, and FP suppression (skip tests/, generated markers, __init__.py for fan-out, dont count dataclass/Enum fields). Scrutinize: (1) the ast metric MATH — are branch/cognitive counts correct (nesting weighting, BoolOp, comprehensions, match/case, try/except), does function-length/LOC counting correctly exclude blanks/comments/docstrings, are nested functions double-counted; (2) the NEVER-RAISE guarantee — every ast.parse / file-read / walk wrapped, returns [] on failure, one bad file does not abort the rest; (3) FP suppression correctness — could a real code file slip the suppression, or a legit file be wrongly suppressed; (4) off-by-one at the warn/major band boundaries; (5) renmark/init.py wiring — does it keep the HEALTH stdout line BOUNDED (summary counts, not 100+ lines), is the dev-standards.md modularity section capped, is it purely additive/advisory (init still exits 0). Also sanity-check tests/test_modularity.py actually covers the boundaries + suppression + never-raise rather than rubber-stamping.

For each finding: file:line; severity Critical|Major|Minor|Nit; one-sentence description; one-sentence fix. Top: summary counts per severity. Do not modify files. Do not exit until complete.

warning: Codex could not find system bubblewrap at /usr/bin/bwrap. Please install bubblewrap with your package manager. Codex will use the vendored bubblewrap in the meantime.
codex
Reviewing `main..HEAD` with emphasis on `renmark/modularity.py`, then I’ll verify the init wiring and test coverage against the specific failure modes you listed.
exec
/bin/bash -lc 'git status --short' in /home/renmark/projects/ai-system
 succeeded in 0ms:
exec
/bin/bash -lc "bash -lc 'tail -n 80 /home/renmark/projects/ai-system/CHANGELOG.md'" in /home/renmark/projects/ai-system
 succeeded in 0ms:
- `renmark-execute --roadmap` — prints the status table and writes `roadmap.md` snapshot.
- `renmark-execute --no-commit` — flag added (currently a no-op; v0.1.1 will wire it into the per-task commit code so the skill can batch commits per wave).
- argparse prog name corrected from `nim-execute` to `renmark-execute`.

**Memory templates:**

The eight `.renmark/memory/` files now have proper documentation-grade templates:
- `features.md`, `bugs.md`, `decisions.md` (ADR format), `stack.md`, `architecture.md`, `conventions.md`, `routing.md`, `learnings.md`, plus an auto-maintained `INDEX.md`.

**Plugin manifest now declares 7 skills** (brainstorm, plan, orchestrate, debug, codereview, roadmap, help).

**Tests:** 85 passing (up from 52 in v0.0.3).

**Still pending (v0.1.1+):**
- CLI `execute_plan` actually using `dispatch.group_tasks_by_wave` + `dispatch_wave` (currently the loop still runs single-task serial via the v0.0.x path)
- `--no-commit` wired through per-task commit code
- Cost preview in `--dry-run`
- Routing memory auto-updates from run outcomes
- `/renmark:debug` per-step routing (NIM grep / codex trace / opus reasoning)
- `/renmark:codereview` Sonnet + Opus passes
- Additional native providers (Ollama, OpenRouter, OpenAI-compat) — Phase 4
- ~~LiteLLM plug-in slot — Phase 5~~ (dropped — native providers cover the realistic use cases) (optional)

## v0.0.3 — 2026-05-12 (Phase 1, +memory + help)

**Persistent memory module + `/renmark:help` skill.**

- `renmark/memory.py` — read/write helpers for `.renmark/memory/`. Functions: `ensure_memory(repo)`, `read_index(repo)`, `read_file(repo, name)`, `log_feature(...)`, `log_bug(...)`, `log_decision(...)`, `append_routing(...)`, `append_learning(...)`. Section-aware appends (newest-first per CHANGELOG convention). Lessons in `log_bug` auto-cross-post to `learnings.md`. 8 new tests.
- Memory templates rewritten so the files act as **living documentation**:
  - `features.md` — shipped / in-progress / planned (CHANGELOG style)
  - `bugs.md` — open / fixed with severity, symptom, root cause, fix, lesson
  - `decisions.md` — ADR format (context, decision, alternatives, consequences) with auto-numbered IDs
  - `stack.md` — languages, libs, runtime env, external APIs
  - `architecture.md` — components, data flow, module boundaries, invariants
  - `conventions.md`, `routing.md`, `learnings.md` — auto-tuned + hand-edited
  - `INDEX.md` is a cheap top-of-file index loaded first by every skill
- `/renmark:help` skill (new) — prints all six commands with one-sentence descriptions and the typical workflow order. Pure documentation, no API calls.
- `plugin.json` updated to declare 6 skills.

52 tests total (44 from baseline + 8 memory tests).

## v0.0.2 — 2026-05-12 (Phase 1, partial — skills visible)

**Plugin manifest + all five `/renmark:*` SKILL.md files** so the commands appear in Claude Code's skill list after install. Template files for empty-folder bootstrap. install.sh hardened.

Added:
- `plugin/plugin.json` declaring the 5 skills
- `plugin/skills/{brainstorm,plan,orchestrate,debug,codereview}/SKILL.md` — workflow docs for each
- `plugin/templates/{CLAUDE.md,AGENTS.md,renmark-readme.md,memory/*.md}.template` — what `/renmark:brainstorm` writes when bootstrapping an empty project
- `install.sh` ran successfully — symlinks live at `~/.claude/plugins/renmark` and `~/.local/bin/renmark-execute`

Fixed:
- `install.sh` v0.0.1 stored the /orchestrator backup at `~/.claude/skills/.orchestrator.bak/` — Claude Code's skill discovery picked it up as a phantom skill named `.orchestrator.bak`. **Backup removed entirely**: the orchestrator source still lives in `/home/renmark/projects/ai-inference/` (and in its git history), so a separate copy under `~/.claude/` was just paranoia and bug surface. install.sh now `rm -rf`s the old skill outright; manual revert is `cd ~/projects/ai-inference && bash install.sh` against the v0.2.0 baseline.

Not yet wired (still Phase 1):
- `renmark/dispatch.py` — wave-based parallel dispatcher (so orchestrate can't yet run opus/sonnet tasks or parallel groups)
- `renmark/memory.py` — `.renmark/memory/` reader/writer
- `renmark/providers/claude_agent.py` — Opus/Sonnet via Agent tool from skill side
- Parser extensions for `complexity`, `parallel_group`, `est_tokens`, `est_cost_usd`
- CLI `--no-commit` mode for batched wave commits
- Cost preview in `--dry-run`
- Empty-folder bootstrap code (skill docs reference it but the brainstorm skill currently does it by hand)

The skills are visible and `/renmark:brainstorm` + `/renmark:plan` are workable today (they're Opus-driven conversations). `/renmark:orchestrate` runs the same single-task path the v0.0.1 baseline supports.

## v0.0.1 — 2026-05-12 (Phase 0)

**Bootstrap of the new `ai-system` repo.** Copies the working v0.2.0 baseline from `/home/renmark/projects/ai-inference/` and retargets the Python package from `nim_execute` to `renmark`.

Changes vs. ai-inference v0.2.0:

- Package renamed `nim_execute` → `renmark`
- `nim_client.py` → `renmark/providers/nim.py`
- `codex_exec.py` → `renmark/providers/codex.py`
- New `renmark/providers/__init__.py` with `PROVIDERS` registry stub
- Runtime state dir renamed `.nim-state/` → `.renmark/state/` (with `RENMARK_DIR_NAME`, `STATE_SUBDIR`, `MEMORY_SUBDIR`, `DEBUG_SUBDIR` constants; legacy `STATE_DIR_NAME` aliased for back-compat)
- All test imports updated, 41 tests still passing
- CLI references `renmark-execute` / `.renmark/state/` in user-facing strings

Phase 1 (next): the five `/renmark:*` skills, `plugin/plugin.json`, dispatch layer, memory module, empty-folder bootstrap. See `PLAN.md`.

exec
/bin/bash -lc 'git diff --stat main..HEAD' in /home/renmark/projects/ai-system
 succeeded in 0ms:
 .renmark/memory/features.md                        |  12 +
 .renmark/memory/learnings.md                       |   3 +
 .renmark/memory/routing.md                         |   3 +
 .renmark/memory/stack.md                           |   3 +
 .../2026-06-08-modularity-health-lens.plan.md      | 119 +++++
 .../2026-06-08-modularity-health-lens.research.md  | 187 ++++++++
 ...8b830c2ef62c79059c8a05cf6062096.verification.md |  26 +
 .../2026-06-08-modularity-health-lens.spec.md      | 122 +++++
 CHANGELOG.md                                       |  14 +
 plugin/skills/init/SKILL.md                        |   2 +-
 renmark/init.py                                    |  84 +++-
 renmark/modularity.py                              | 529 +++++++++++++++++++++
 tests/test_modularity.py                           | 329 +++++++++++++
 13 files changed, 1422 insertions(+), 11 deletions(-)

exec
/bin/bash -lc 'git diff --name-only main..HEAD' in /home/renmark/projects/ai-system
 succeeded in 0ms:
.renmark/memory/features.md
.renmark/memory/learnings.md
.renmark/memory/routing.md
.renmark/memory/stack.md
.renmark/plans/2026-06-08-modularity-health-lens.plan.md
.renmark/research/2026-06-08-modularity-health-lens.research.md
.renmark/reviews/2026-06-08-25e2ee93f8b830c2ef62c79059c8a05cf6062096.verification.md
.renmark/specs/2026-06-08-modularity-health-lens.spec.md
CHANGELOG.md
plugin/skills/init/SKILL.md
renmark/init.py
renmark/modularity.py
tests/test_modularity.py

codex
I have the changed surface. Next I’m reading the modularity analyzer, the init integration, and the tests side by side against the boundary conditions you called out.
exec
/bin/bash -lc 'git diff main..HEAD -- /home/renmark/projects/ai-system/renmark/init.py' in /home/renmark/projects/ai-system
 succeeded in 0ms:
diff --git a/renmark/init.py b/renmark/init.py
index 6523ab6..f8b9462 100644
--- a/renmark/init.py
+++ b/renmark/init.py
@@ -97,6 +97,10 @@ MODULES_CAP_FULL = 40
 LAYOUT_LINES_CAP = 7
 # How many files to extract symbols from (largest first)
 TOP_FILES_FOR_SYMBOLS = 20
+# Cap how many modularity gaps to list in dev-standards.md (majors first,
+# then warns). Remainder is summarized as a "+N more" note so the rendered
+# section can't be flooded by a 100+ gap scan.
+MODULARITY_GAPS_RENDER_CAP = 20
 
 
 # ── Data classes ─────────────────────────────────────────────────────────────
@@ -133,10 +137,18 @@ class Gap:
 
 @dataclass
 class StandardsScan:
-    """Result of scanning the repo for dev standards + health gaps."""
+    """Result of scanning the repo for dev standards + health gaps.
+
+    ``gaps`` are the standards-health gaps (linter/CI/tests/secrets …).
+    ``modularity_gaps`` are file-level modularity breaches from
+    :mod:`renmark.modularity` — kept in a SEPARATE list so the always-loaded
+    stub and the bounded ``HEALTH:`` stdout line can summarize them as counts
+    rather than dumping 100+ per-gap entries. Both feed the total gap count.
+    """
 
     standards: list[Standard] = field(default_factory=list)
     gaps: list[Gap] = field(default_factory=list)
+    modularity_gaps: list[Gap] = field(default_factory=list)
     deep: bool = False
 
 
@@ -1187,7 +1199,13 @@ def scan_standards(repo: Path, files: list[FileInfo], deep: bool = False) -> Sta
     )
     standards = [s for s in (d(repo) for d in detectors) if s is not None]
     gaps = evaluate_health(repo, standards, files, deep)
-    return StandardsScan(standards=standards, gaps=gaps, deep=deep)
+    # Additive: merge in file-level modularity breaches (separate list so the
+    # bounded stub / HEALTH line summarize them as counts, not a 100+ dump).
+    # analyze() never raises and returns [] on failure — keep init zero-LLM.
+    from . import modularity
+
+    modularity_gaps = modularity.analyze(repo)
+    return StandardsScan(standards=standards, gaps=gaps, modularity_gaps=modularity_gaps, deep=deep)
 
 
 # ── Standards rendering ──────────────────────────────────────────────────────
@@ -1216,6 +1234,40 @@ def render_dev_gates_line(standards: StandardsScan) -> str | None:
     return "**Dev gates:** " + " · ".join(parts) if parts else None
 
 
+def _render_modularity_section(modularity_gaps: list[Gap]) -> list[str]:
+    """Render the modularity gaps as a bounded, grouped subsection.
+
+    Majors (``danger``) are listed first, then warns, capped at
+    ``MODULARITY_GAPS_RENDER_CAP`` total with a ``+N more`` note for the
+    remainder. Returns ``[]`` when there are no modularity gaps so the section
+    is omitted entirely (no empty heading).
+    """
+    if not modularity_gaps:
+        return []
+    order = {"danger": 0, "warn": 1, "info": 2}
+    ordered = sorted(modularity_gaps, key=lambda g: order.get(g.severity, 99))
+    n_major = sum(1 for g in modularity_gaps if g.severity == "danger")
+    n_warn = sum(1 for g in modularity_gaps if g.severity == "warn")
+    out: list[str] = []
+    out.append("## Modularity")
+    out.append("")
+    out.append(
+        f"**{len(modularity_gaps)} modularity gap{'s' if len(modularity_gaps) != 1 else ''}** "
+        f"({n_major} major, {n_warn} warn) — file-level size/complexity breaches. "
+        "Advisory: these never block init."
+    )
+    out.append("")
+    shown = ordered[:MODULARITY_GAPS_RENDER_CAP]
+    for g in shown:
+        prefix = _SEVERITY_PREFIX.get(g.severity, "•")
+        out.append(f"- {prefix} **{g.title}** — {g.detail} _{g.recommendation}_")
+    remaining = len(ordered) - len(shown)
+    if remaining > 0:
+        out.append(f"- _… +{remaining} more (re-run for the full list)_")
+    out.append("")
+    return out
+
+
 def render_standards_md(repo_name: str, today: str, git_sha: str | None, standards: StandardsScan) -> str:
     sha = git_sha or "no-git"
     out: list[str] = []
@@ -1272,6 +1324,9 @@ def render_standards_md(repo_name: str, today: str, git_sha: str | None, standar
             out.append(f"**Recommendation:** {g.recommendation}")
             out.append("")
 
+    # ── Modularity (own subsection, majors first, bounded list)
+    out.extend(_render_modularity_section(standards.modularity_gaps))
+
     if not standards.deep:
         out.append("")
         out.append("_Run `python -m renmark.init --deep` for deeper checks (commit-message style, etc.)._")
@@ -1532,20 +1587,29 @@ def run(repo: Path, include_private: bool = False, deep: bool = False) -> tuple[
     sha = scan.git_sha or "no-git"
     langs_summary = ",".join(sorted(scan.lang_counts.keys())) or "—"
     n_gaps = len(scan.standards.gaps)
+    mod_gaps = scan.standards.modularity_gaps
+    n_mod = len(mod_gaps)
     blocks_field = str(n_blocks_added) if n_blocks_added else "unchanged"
     summary_lines = [
         f"OK  stub={stub_status} agents={agents_status} map={map_status} standards={standards_status} "
         f"blocks={blocks_field} modules={len(scan.files)} commands={len(scan.commands)} "
         f"langs={langs_summary} ref={scan.today}@{sha}"
     ]
-    if n_gaps > 0:
-        counts = {"danger": 0, "warn": 0, "info": 0}
-        for g in scan.standards.gaps:
-            counts[g.severity] = counts.get(g.severity, 0) + 1
-        sev_part = ", ".join(f"{n} {sev}" for sev, n in counts.items() if n)
-        summary_lines.append(
-            f"HEALTH: {n_gaps} gap{'s' if n_gaps != 1 else ''} ({sev_part}) — see `.renmark/memory/dev-standards.md`"
-        )
+    # Bounded HEALTH line: counts ONLY — never the per-gap detail. Modularity
+    # can produce 100+ gaps; they live in dev-standards.md, not on stdout.
+    if n_gaps > 0 or n_mod > 0:
+        parts: list[str] = []
+        if n_gaps > 0:
+            counts = {"danger": 0, "warn": 0, "info": 0}
+            for g in scan.standards.gaps:
+                counts[g.severity] = counts.get(g.severity, 0) + 1
+            sev_part = ", ".join(f"{n} {sev}" for sev, n in counts.items() if n)
+            parts.append(f"{n_gaps} standards gap{'s' if n_gaps != 1 else ''} ({sev_part})")
+        if n_mod > 0:
+            n_major = sum(1 for g in mod_gaps if g.severity == "danger")
+            n_warn = sum(1 for g in mod_gaps if g.severity == "warn")
+            parts.append(f"{n_mod} modularity ({n_major} major/{n_warn} warn)")
+        summary_lines.append(f"HEALTH: {', '.join(parts)} — see `.renmark/memory/dev-standards.md`")
     return 0, "\n".join(summary_lines)
 
 

exec
/bin/bash -lc 'git diff main..HEAD -- /home/renmark/projects/ai-system/renmark/modularity.py' in /home/renmark/projects/ai-system
 succeeded in 0ms:
diff --git a/renmark/modularity.py b/renmark/modularity.py
new file mode 100644
index 0000000..8733b77
--- /dev/null
+++ b/renmark/modularity.py
@@ -0,0 +1,529 @@
+"""Modularity / scalability health lens — pure stdlib ``ast``, zero-dep, never-raise.
+
+renmark enforces modularity at *plan time* (one-file-per-task) but never
+*measures* it on the shipped codebase. This module is the missing measurement
+layer: an ADVISORY, deterministic, zero-LLM, stdlib-``ast``-only analyzer that
+surfaces code-health gaps through ``init``'s existing standards-health pipeline.
+
+Its findings are reported via the SAME :class:`renmark.init.Gap` shape the
+standards-health detectors emit, so they render in ``dev-standards.md`` and count
+toward the ``HEALTH:`` stdout line. They are **advisory only** — they never block
+the pipeline and never trigger an auto-refactor.
+
+The 5 metrics, each with a ``warn`` and a ``major`` band (tunable module
+constants below):
+
+1. **module LOC** (code lines — blanks / comments / docstring lines excluded):
+   :data:`MODULE_LOC_WARN` / :data:`MODULE_LOC_MAJOR`.
+2. **function / method length** in code LOC (per ``FunctionDef`` /
+   ``AsyncFunctionDef``): :data:`FUNC_LOC_WARN` / :data:`FUNC_LOC_MAJOR`.
+3. **cyclomatic branch count** per function (``if`` / ``for`` / ``while`` /
+   ``except`` / ``with`` / boolean-op / comprehension-``if`` / ``match`` case,
+   plus one): :data:`CYCLO_WARN` / :data:`CYCLO_MAJOR`.
+4. **import fan-out** per module (``import`` / ``from ... import`` statements;
+   ``__init__.py`` excluded — re-exports): :data:`FANOUT_WARN` / :data:`FANOUT_MAJOR`.
+5. **cognitive complexity** per function (nesting-weighted: each branching
+   construct adds ``1 + current_nesting_level``): :data:`COGNITIVE_WARN` /
+   :data:`COGNITIVE_MAJOR`.
+
+Design contract (mirrors :mod:`renmark.sizing`):
+
+- ``analyze(repo)`` is a pure function of the filesystem; it never mutates state.
+- **Never raises into the caller.** Every walk / read / ``ast.parse`` is wrapped;
+  a syntax-error or otherwise unparseable file is SKIPPED and analysis continues.
+  On any unexpected failure the result degrades to ``[]`` (no gaps reported) —
+  the same safe posture as ``sizing.py``.
+- Every threshold is a documented, tunable module-level constant.
+
+False-positive suppression (mandatory): files under ``tests/`` are skipped;
+generated files (common ``@generated`` / "do not edit" markers) are skipped;
+``__init__.py`` is excluded from the fan-out metric; class-level field
+assignments (dataclass / Enum bodies) are not counted as branches or cognitive
+complexity. Code lines are counted, never raw lines.
+"""
+
+from __future__ import annotations
+
+import ast
+from pathlib import Path
+
+from .init import Gap, _is_excluded
+
+# ── Thresholds (tunable) ─────────────────────────────────────────────────────
+
+#: Module LOC (code lines): pylint ``max-module-lines`` default is 1000.
+MODULE_LOC_WARN: int = 500
+MODULE_LOC_MAJOR: int = 1000
+
+#: Function / method length in code LOC.
+FUNC_LOC_WARN: int = 50
+FUNC_LOC_MAJOR: int = 100
+
+#: Cyclomatic branch count per function (mccabe-style: decision points + 1).
+CYCLO_WARN: int = 10
+CYCLO_MAJOR: int = 20
+
+#: Import fan-out per module (``import`` + ``from`` statements). ``__init__.py``
+#: is excluded from this metric (re-export hubs legitimately import a lot).
+FANOUT_WARN: int = 15
+FANOUT_MAJOR: int = 25
+
+#: Cognitive complexity per function (SonarQube-style nesting-weighted).
+COGNITIVE_WARN: int = 15
+COGNITIVE_MAJOR: int = 30
+
+# ── Suppression signals (tunable) ────────────────────────────────────────────
+
+#: Path components that mark a test tree — files under any of these are skipped.
+TEST_PATH_PARTS: frozenset[str] = frozenset({"tests", "test", "__tests__"})
+
+#: Substrings (checked case-insensitively against the first ~40 lines) that mark
+#: a machine-generated file — such files are skipped entirely.
+GENERATED_MARKERS: tuple[str, ...] = (
+    "@generated",
+    "do not edit",
+    "do not modify",
+    "auto-generated",
+    "autogenerated",
+    "automatically generated",
+    "generated by",
+    "this file is generated",
+    "code generated",
+)
+
+#: How many leading lines to scan for a generated-file marker.
+GENERATED_SCAN_LINES: int = 40
+
+
+# ── Public API ────────────────────────────────────────────────────────────────
+
+
+def analyze(repo: Path | str) -> list[Gap]:
+    """Analyze every Python source file under ``repo`` and return advisory gaps.
+
+    Walks Python files (reusing ``init``'s exclusion set), parses each with
+    :func:`ast.parse` inside a try/except — a syntax-error or any per-file
+    failure SKIPS that file and continues. For each file the 5 modularity
+    metrics are computed and one :class:`~renmark.init.Gap` is emitted per
+    threshold breach, ``severity`` set to ``"warn"`` (warn band) or ``"danger"``
+    (major band).
+
+    Never raises: any unexpected error degrades to ``[]`` (no gaps reported),
+    matching :mod:`renmark.sizing`'s safety posture.
+    """
+    try:
+        repo_path = Path(repo)
+        gaps: list[Gap] = []
+        for file in _walk_python_files(repo_path):
+            try:
+                gaps.extend(_analyze_file(file, repo_path))
+            except Exception:  # one bad file must never sink the whole scan
+                continue
+        # warn band is "warn"; major band is "danger" — sort danger first.
+        order = {"danger": 0, "warn": 1, "info": 2}
+        gaps.sort(key=lambda g: order.get(g.severity, 99))
+        return gaps
+    except Exception:  # never raise into the caller; degrade to no gaps.
+        return []
+
+
+# ── File discovery ─────────────────────────────────────────────────────────────
+
+
+def _walk_python_files(repo: Path) -> list[Path]:
+    """Return Python source files under ``repo``, excluding ``init``'s noise dirs.
+
+    Reuses :func:`renmark.init._is_excluded` so the walker matches the rest of
+    the toolchain (``.git``, ``node_modules``, ``.venv``, renmark runtime, …).
+    Never raises — an unreadable tree degrades to whatever was collected so far.
+    """
+    out: list[Path] = []
+    try:
+        for path in repo.rglob("*.py"):
+            try:
+                if not path.is_file():
+                    continue
+                if _is_excluded(path, repo):
+                    continue
+            except OSError:
+                continue
+            out.append(path)
+    except OSError:
+        return out
+    return out
+
+
+def _rel(path: Path, repo: Path) -> str:
+    """Repo-relative POSIX path string for human-readable messages."""
+    try:
+        return str(path.relative_to(repo)).replace("\\", "/")
+    except ValueError:
+        return path.name
+
+
+# ── Suppression predicates ─────────────────────────────────────────────────────
+
+
+def _is_under_tests(path: Path, repo: Path) -> bool:
+    """True if any path component (relative to repo) is a test-tree directory."""
+    try:
+        parts = path.relative_to(repo).parts
+    except ValueError:
+        parts = path.parts
+    # The file's own name isn't a "directory"; check the parent components.
+    return any(p in TEST_PATH_PARTS for p in parts[:-1])
+
+
+def _is_generated(text: str) -> bool:
+    """True if the file's leading lines carry a common machine-generated marker."""
+    head = "\n".join(text.splitlines()[:GENERATED_SCAN_LINES]).lower()
+    return any(marker in head for marker in GENERATED_MARKERS)
+
+
+# ── Per-file analysis ───────────────────────────────────────────────────────────
+
+
+def _analyze_file(path: Path, repo: Path) -> list[Gap]:
+    """Compute all 5 metrics for one file and return one Gap per breach.
+
+    Returns ``[]`` (file skipped) on read failure, syntax error, a test-tree
+    path, or a generated-file marker. Never raises.
+    """
+    if _is_under_tests(path, repo):
+        return []
+    try:
+        text = path.read_text(encoding="utf-8", errors="replace")
+    except OSError:
+        return []
+    if _is_generated(text):
+        return []
+    try:
+        tree = ast.parse(text)
+    except (SyntaxError, ValueError, RecursionError):
+        return []
+    except Exception:
+        return []
+
+    rel = _rel(path, repo)
+    gaps: list[Gap] = []
+
+    # 1. module LOC (code lines)
+    code_lines = _module_code_lines(text, tree)
+    gaps.extend(_module_loc_gap(rel, code_lines))
+
+    # 4. import fan-out (skip __init__.py — re-export hubs)
+    if path.name != "__init__.py":
+        fan_out = _import_fan_out(tree)
+        gaps.extend(_fan_out_gap(rel, fan_out))
+
+    # 2, 3, 5. per-function metrics
+    for func in _iter_functions(tree):
+        qual = _func_label(func)
+        func_loc = _func_code_lines(func, text)
+        gaps.extend(_func_loc_gap(rel, qual, func_loc))
+        gaps.extend(_cyclomatic_gap(rel, qual, _cyclomatic(func)))
+        gaps.extend(_cognitive_gap(rel, qual, _cognitive(func)))
+
+    return gaps
+
+
+# ── Metric 1: module code lines ────────────────────────────────────────────────
+
+
+def _module_code_lines(text: str, tree: ast.Module) -> int:
+    """Count code lines: exclude blank lines, comment lines, and docstring lines.
+
+    Docstring line numbers (module-, class-, and function-level) are gathered
+    from the AST and subtracted; blank and ``#``-comment physical lines are
+    excluded by inspecting the source text.
+    """
+    docstring_lines = _docstring_line_numbers(tree)
+    count = 0
+    for lineno, raw in enumerate(text.splitlines(), start=1):
+        stripped = raw.strip()
+        if not stripped:
+            continue
+        if stripped.startswith("#"):
+            continue
+        if lineno in docstring_lines:
+            continue
+        count += 1
+    return count
+
+
+def _docstring_line_numbers(tree: ast.Module) -> set[int]:
+    """Physical line numbers occupied by module / class / function docstrings."""
+    lines: set[int] = set()
+    for node in ast.walk(tree):
+        if not isinstance(node, (ast.Module, ast.ClassDef, ast.FunctionDef, ast.AsyncFunctionDef)):
+            continue
+        doc = ast.get_docstring(node, clean=False)
+        if doc is None:
+            continue
+        body = getattr(node, "body", None)
+        if not body:
+            continue
+        expr = body[0]
+        if not isinstance(expr, ast.Expr):
+            continue
+        const = expr.value
+        if not isinstance(const, ast.Constant) or not isinstance(const.value, str):
+            continue
+        start = getattr(const, "lineno", None)
+        end = getattr(const, "end_lineno", start)
+        if start is None:
+            continue
+        if end is None:
+            end = start
+        lines.update(range(start, end + 1))
+    return lines
+
+
+# ── Metric 2: function code lines ───────────────────────────────────────────────
+
+
+def _func_code_lines(func: ast.FunctionDef | ast.AsyncFunctionDef, text: str) -> int:
+    """Count code lines spanned by a function body (blank/comment/docstring excl.).
+
+    Spans the function's first body statement through its ``end_lineno`` so the
+    ``def`` signature itself isn't counted as a body line; blanks, comments, and
+    the function's own docstring lines are excluded.
+    """
+    if not func.body:
+        return 0
+    first = func.body[0]
+    start = getattr(first, "lineno", getattr(func, "lineno", 1))
+    end = getattr(func, "end_lineno", start) or start
+
+    doc_lines: set[int] = set()
+    doc = ast.get_docstring(func, clean=False)
+    if doc is not None and isinstance(first, ast.Expr):
+        const = first.value
+        if isinstance(const, ast.Constant) and isinstance(const.value, str):
+            d_start = getattr(const, "lineno", start)
+            d_end = getattr(const, "end_lineno", d_start) or d_start
+            doc_lines.update(range(d_start, d_end + 1))
+
+    all_lines = text.splitlines()
+    count = 0
+    for lineno in range(start, end + 1):
+        if lineno - 1 < 0 or lineno - 1 >= len(all_lines):
+            continue
+        stripped = all_lines[lineno - 1].strip()
+        if not stripped or stripped.startswith("#"):
+            continue
+        if lineno in doc_lines:
+            continue
+        count += 1
+    return count
+
+
+# ── Metric 4: import fan-out ────────────────────────────────────────────────────
+
+
+def _import_fan_out(tree: ast.Module) -> int:
+    """Count ``Import`` / ``ImportFrom`` statements anywhere in the module."""
+    return sum(1 for node in ast.walk(tree) if isinstance(node, (ast.Import, ast.ImportFrom)))
+
+
+# ── Function iteration ──────────────────────────────────────────────────────────
+
+
+def _iter_functions(tree: ast.Module) -> list[ast.FunctionDef | ast.AsyncFunctionDef]:
+    """All function/method defs in the module (nested ones included)."""
+    return [n for n in ast.walk(tree) if isinstance(n, (ast.FunctionDef, ast.AsyncFunctionDef))]
+
+
+def _func_label(func: ast.FunctionDef | ast.AsyncFunctionDef) -> str:
+    """Function name plus its measured value placeholder, e.g. ``my_func``."""
+    return f"{func.name}()"
+
+
+# ── Metric 3: cyclomatic complexity ─────────────────────────────────────────────
+
+
+def _cyclomatic(func: ast.FunctionDef | ast.AsyncFunctionDef) -> int:
+    """McCabe-style branch count: decision nodes within the function body + 1.
+
+    Counts ``If`` / ``For`` / ``While`` / ``ExceptHandler`` / ``With`` /
+    ``BoolOp`` (and/or) / comprehension-``if`` / ``match`` ``case`` nodes that
+    belong to THIS function (nested functions are excluded — they have their own
+    score). Class-level field assignments inside the function are not branches.
+    """
+    count = 1
+    for node in _walk_own_body(func):
+        if isinstance(
+            node,
+            (ast.If, ast.For, ast.AsyncFor, ast.While, ast.ExceptHandler, ast.With, ast.AsyncWith),
+        ):
+            count += 1
+        elif isinstance(node, ast.BoolOp):
+            # n operands → n-1 additional branch decisions.
+            count += max(len(node.values) - 1, 1)
+        elif isinstance(node, ast.comprehension):
+            count += len(node.ifs)
+        elif _is_match_case(node):
+            count += 1
+    return count
+
+
+# ── Metric 5: cognitive complexity ──────────────────────────────────────────────
+
+
+def _cognitive(func: ast.FunctionDef | ast.AsyncFunctionDef) -> int:
+    """Nesting-weighted cognitive complexity (SonarQube-style approximation).
+
+    Each branching construct adds ``1 + current_nesting_level``; nesting
+    increments inside ``if`` / ``for`` / ``while`` / ``with`` / ``except``
+    bodies. Boolean operators and comprehension ``if``s add a flat increment.
+    Nested function definitions are not descended into (they score separately).
+    """
+    score = 0
+
+    def visit(node: ast.AST, nesting: int) -> None:
+        nonlocal score
+        for child in ast.iter_child_nodes(node):
+            if isinstance(child, (ast.FunctionDef, ast.AsyncFunctionDef)):
+                continue  # nested functions scored on their own
+            is_nesting_construct = isinstance(
+                child,
+                (ast.If, ast.For, ast.AsyncFor, ast.While, ast.With, ast.AsyncWith, ast.ExceptHandler),
+            ) or _is_match_case(child)
+            if is_nesting_construct:
+                score += 1 + nesting
+                visit(child, nesting + 1)
+            elif isinstance(child, ast.BoolOp):
+                score += 1
+                visit(child, nesting)
+            elif isinstance(child, ast.comprehension):
+                score += len(child.ifs)
+                visit(child, nesting)
+            else:
+                visit(child, nesting)
+
+    visit(func, 0)
+    return score
+
+
+# ── AST traversal helpers ───────────────────────────────────────────────────────
+
+
+def _walk_own_body(func: ast.FunctionDef | ast.AsyncFunctionDef) -> list[ast.AST]:
+    """All descendant nodes of ``func`` EXCLUDING nested function bodies.
+
+    A nested ``def``/``async def`` carries its own complexity score, so we don't
+    let its internals inflate the enclosing function's count.
+    """
+    out: list[ast.AST] = []
+
+    def walk(node: ast.AST) -> None:
+        for child in ast.iter_child_nodes(node):
+            if isinstance(child, (ast.FunctionDef, ast.AsyncFunctionDef)):
+                continue
+            out.append(child)
+            walk(child)
+
+    walk(func)
+    return out
+
+
+def _is_match_case(node: ast.AST) -> bool:
+    """True if ``node`` is a ``match`` ``case`` clause (Python 3.10+ ``match_case``)."""
+    match_case = getattr(ast, "match_case", None)
+    return match_case is not None and isinstance(node, match_case)
+
+
+# ── Gap construction (per metric) ───────────────────────────────────────────────
+
+
+def _band(value: int, warn: int, major: int) -> str | None:
+    """Return ``"danger"`` (>= major), ``"warn"`` (>= warn), or ``None`` (ok)."""
+    if value >= major:
+        return "danger"
+    if value >= warn:
+        return "warn"
+    return None
+
+
+def _module_loc_gap(rel: str, code_lines: int) -> list[Gap]:
+    band = _band(code_lines, MODULE_LOC_WARN, MODULE_LOC_MAJOR)
+    if band is None:
+        return []
+    limit = MODULE_LOC_MAJOR if band == "danger" else MODULE_LOC_WARN
+    return [
+        Gap(
+            band,
+            f"Oversized module: `{rel}` ({code_lines} code lines)",
+            f"`{rel}` has {code_lines} code lines (threshold {limit}). Large modules are "
+            "hard to navigate, review, and test, and tend to accrete unrelated responsibilities.",
+            f"Split `{rel}` into focused modules along its natural seams "
+            "(one cohesive responsibility per file). Advisory — never auto-refactored.",
+        )
+    ]
+
+
+def _fan_out_gap(rel: str, fan_out: int) -> list[Gap]:
+    band = _band(fan_out, FANOUT_WARN, FANOUT_MAJOR)
+    if band is None:
+        return []
+    limit = FANOUT_MAJOR if band == "danger" else FANOUT_WARN
+    return [
+        Gap(
+            band,
+            f"High import fan-out: `{rel}` ({fan_out} imports)",
+            f"`{rel}` has {fan_out} import statements (threshold {limit}). High fan-out signals "
+            "a module coupled to many others — a change-amplifier and a refactoring hazard.",
+            f"Reduce coupling in `{rel}`: extract a narrower interface, group related imports "
+            "behind a facade, or move logic closer to its dependencies. Advisory.",
+        )
+    ]
+
+
+def _func_loc_gap(rel: str, qual: str, func_loc: int) -> list[Gap]:
+    band = _band(func_loc, FUNC_LOC_WARN, FUNC_LOC_MAJOR)
+    if band is None:
+        return []
+    limit = FUNC_LOC_MAJOR if band == "danger" else FUNC_LOC_WARN
+    return [
+        Gap(
+            band,
+            f"Long function: `{rel}` → `{qual}` ({func_loc} code lines)",
+            f"`{qual}` in `{rel}` spans {func_loc} code lines (threshold {limit}). Long functions "
+            "are hard to read in one pass and usually do more than one thing.",
+            f"Extract cohesive blocks of `{qual}` into well-named helpers. Advisory.",
+        )
+    ]
+
+
+def _cyclomatic_gap(rel: str, qual: str, value: int) -> list[Gap]:
+    band = _band(value, CYCLO_WARN, CYCLO_MAJOR)
+    if band is None:
+        return []
+    limit = CYCLO_MAJOR if band == "danger" else CYCLO_WARN
+    return [
+        Gap(
+            band,
+            f"High cyclomatic complexity: `{rel}` → `{qual}` ({value} branches)",
+            f"`{qual}` in `{rel}` has a cyclomatic branch count of {value} (threshold {limit}). "
+            "Many decision points mean many paths to test and many ways to be wrong.",
+            f"Reduce branching in `{qual}`: early returns, guard clauses, table/dispatch dictionaries, "
+            "or splitting the function. Advisory.",
+        )
+    ]
+
+
+def _cognitive_gap(rel: str, qual: str, value: int) -> list[Gap]:
+    band = _band(value, COGNITIVE_WARN, COGNITIVE_MAJOR)
+    if band is None:
+        return []
+    limit = COGNITIVE_MAJOR if band == "danger" else COGNITIVE_WARN
+    return [
+        Gap(
+            band,
+            f"High cognitive complexity: `{rel}` → `{qual}` (score {value})",
+            f"`{qual}` in `{rel}` has a nesting-weighted cognitive-complexity score of {value} "
+            f"(threshold {limit}). Deep nesting is disproportionately hard for a human to follow.",
+            f"Flatten nesting in `{qual}`: invert conditions to return early, extract nested blocks "
+            "into helpers, and reduce branching depth. Advisory.",
+        )
+    ]

exec
/bin/bash -lc 'git diff main..HEAD -- /home/renmark/projects/ai-system/tests/test_modularity.py' in /home/renmark/projects/ai-system
 succeeded in 0ms:
diff --git a/tests/test_modularity.py b/tests/test_modularity.py
new file mode 100644
index 0000000..d0dacb3
--- /dev/null
+++ b/tests/test_modularity.py
@@ -0,0 +1,329 @@
+"""Unit tests for renmark.modularity — the advisory code-health analyzer.
+
+Hermetic: every test writes tiny synthetic ``.py`` files into ``tmp_path`` and
+calls :func:`renmark.modularity.analyze`. Thresholds are imported as module
+constants so tuning them never silently breaks (or silently passes) these tests.
+
+For each of the 5 metrics we assert the band boundaries:
+
+- JUST OVER the warn threshold → a Gap with ``severity == "warn"``
+- JUST OVER the major threshold → a Gap with ``severity == "danger"``
+- JUST UNDER the warn threshold → NO gap for that metric
+
+Plus false-positive suppression, the never-raise contract, and a cognitive-vs-
+cyclomatic sanity check that nesting is weighted.
+"""
+
+from __future__ import annotations
+
+from pathlib import Path
+
+from renmark import modularity
+from renmark.modularity import (
+    COGNITIVE_MAJOR,
+    COGNITIVE_WARN,
+    CYCLO_MAJOR,
+    CYCLO_WARN,
+    FANOUT_MAJOR,
+    FANOUT_WARN,
+    FUNC_LOC_MAJOR,
+    FUNC_LOC_WARN,
+    MODULE_LOC_MAJOR,
+    MODULE_LOC_WARN,
+    analyze,
+)
+
+
+# ── helpers ──────────────────────────────────────────────────────────────────
+
+
+def _write(repo: Path, rel: str, body: str) -> Path:
+    """Write ``body`` to ``repo/rel`` (creating parents) and return the path."""
+    p = repo / rel
+    p.parent.mkdir(parents=True, exist_ok=True)
+    p.write_text(body, encoding="utf-8")
+    return p
+
+
+def _gaps_for(repo: Path, *, title_contains: str) -> list:
+    """Gaps whose title contains ``title_contains`` (one metric's gaps)."""
+    return [g for g in analyze(repo) if title_contains in g.title]
+
+
+def _module_of_code_lines(n: int) -> str:
+    """A module whose code-line count is exactly ``n`` (one assignment per line).
+
+    No docstring, no blanks, no comments — every physical line is a code line.
+    """
+    return "\n".join(f"v{i} = {i}" for i in range(n)) + "\n"
+
+
+def _func_with_body_code_lines(n: int) -> str:
+    """A module with one function ``f`` whose BODY has exactly ``n`` code lines.
+
+    The ``def`` signature line is not counted by the analyzer (it spans the
+    function's first body statement onward), so the file is the signature plus
+    ``n`` body assignment statements.
+    """
+    body = "\n".join(f"    s{i} = {i}" for i in range(n))
+    return f"def f():\n{body}\n"
+
+
+def _func_with_flat_ifs(n: int) -> str:
+    """A function with ``n`` sibling (non-nested) ``if`` statements.
+
+    Cyclomatic = n + 1 (base 1 + one per ``if``). Cognitive = n (each flat
+    ``if`` adds ``1 + 0`` nesting). Each body is a single ``pass``.
+    """
+    lines = ["def f():"]
+    for i in range(n):
+        lines.append(f"    if v{i}:")
+        lines.append("        pass")
+    return "\n".join(lines) + "\n"
+
+
+def _func_with_nested_ifs(depth: int) -> str:
+    """A function with ``if`` statements nested ``depth`` levels deep.
+
+    Cyclomatic = depth + 1 (one decision point per ``if``, flat or nested).
+    Cognitive is nesting-weighted: level k (0-indexed) adds ``1 + k``, so total
+    cognitive = sum(1..depth) = depth*(depth+1)/2 — far above the raw branch
+    count. This is the lever that distinguishes cognitive from cyclomatic.
+    """
+    lines = ["def f():"]
+    for k in range(depth):
+        lines.append("    " * (k + 1) + f"if c{k}:")
+    lines.append("    " * (depth + 1) + "pass")
+    return "\n".join(lines) + "\n"
+
+
+def _module_with_imports(n: int) -> str:
+    """A module with ``n`` distinct import statements and nothing else of note."""
+    return "\n".join(f"import os as o{i}" for i in range(n)) + "\n"
+
+
+# ── Metric 1: module LOC ───────────────────────────────────────────────────────
+
+
+def test_module_loc_just_over_warn_is_warn(tmp_path: Path) -> None:
+    repo = tmp_path / "repo"
+    _write(repo, "src/big.py", _module_of_code_lines(MODULE_LOC_WARN + 1))
+    gaps = _gaps_for(repo, title_contains="Oversized module")
+    assert len(gaps) == 1
+    assert gaps[0].severity == "warn"
+
+
+def test_module_loc_just_over_major_is_danger(tmp_path: Path) -> None:
+    repo = tmp_path / "repo"
+    _write(repo, "src/huge.py", _module_of_code_lines(MODULE_LOC_MAJOR + 1))
+    gaps = _gaps_for(repo, title_contains="Oversized module")
+    assert len(gaps) == 1
+    assert gaps[0].severity == "danger"
+
+
+def test_module_loc_just_under_warn_is_clean(tmp_path: Path) -> None:
+    repo = tmp_path / "repo"
+    _write(repo, "src/ok.py", _module_of_code_lines(MODULE_LOC_WARN - 1))
+    assert _gaps_for(repo, title_contains="Oversized module") == []
+
+
+# ── Metric 2: function length ──────────────────────────────────────────────────
+
+
+def test_func_loc_just_over_warn_is_warn(tmp_path: Path) -> None:
+    repo = tmp_path / "repo"
+    _write(repo, "src/longfn.py", _func_with_body_code_lines(FUNC_LOC_WARN + 1))
+    gaps = _gaps_for(repo, title_contains="Long function")
+    assert len(gaps) == 1
+    assert gaps[0].severity == "warn"
+
+
+def test_func_loc_just_over_major_is_danger(tmp_path: Path) -> None:
+    repo = tmp_path / "repo"
+    _write(repo, "src/hugefn.py", _func_with_body_code_lines(FUNC_LOC_MAJOR + 1))
+    gaps = _gaps_for(repo, title_contains="Long function")
+    assert len(gaps) == 1
+    assert gaps[0].severity == "danger"
+
+
+def test_func_loc_just_under_warn_is_clean(tmp_path: Path) -> None:
+    repo = tmp_path / "repo"
+    _write(repo, "src/okfn.py", _func_with_body_code_lines(FUNC_LOC_WARN - 1))
+    assert _gaps_for(repo, title_contains="Long function") == []
+
+
+# ── Metric 3: cyclomatic complexity ────────────────────────────────────────────
+# cyclomatic = (#ifs) + 1, so #ifs == value - 1.
+
+
+def test_cyclomatic_just_over_warn_is_warn(tmp_path: Path) -> None:
+    repo = tmp_path / "repo"
+    # value = CYCLO_WARN + 1  ⇒  ifs = CYCLO_WARN
+    _write(repo, "src/cyclo_w.py", _func_with_flat_ifs(CYCLO_WARN))
+    gaps = _gaps_for(repo, title_contains="cyclomatic")
+    assert len(gaps) == 1
+    assert gaps[0].severity == "warn"
+
+
+def test_cyclomatic_just_over_major_is_danger(tmp_path: Path) -> None:
+    repo = tmp_path / "repo"
+    # value = CYCLO_MAJOR + 1  ⇒  ifs = CYCLO_MAJOR
+    _write(repo, "src/cyclo_m.py", _func_with_flat_ifs(CYCLO_MAJOR))
+    gaps = _gaps_for(repo, title_contains="cyclomatic")
+    assert len(gaps) == 1
+    assert gaps[0].severity == "danger"
+
+
+def test_cyclomatic_just_under_warn_is_clean(tmp_path: Path) -> None:
+    repo = tmp_path / "repo"
+    # value = CYCLO_WARN - 1  ⇒  ifs = CYCLO_WARN - 2
+    _write(repo, "src/cyclo_ok.py", _func_with_flat_ifs(CYCLO_WARN - 2))
+    assert _gaps_for(repo, title_contains="cyclomatic") == []
+
+
+# ── Metric 4: import fan-out ───────────────────────────────────────────────────
+
+
+def test_fanout_just_over_warn_is_warn(tmp_path: Path) -> None:
+    repo = tmp_path / "repo"
+    _write(repo, "src/coupled_w.py", _module_with_imports(FANOUT_WARN + 1))
+    gaps = _gaps_for(repo, title_contains="import fan-out")
+    assert len(gaps) == 1
+    assert gaps[0].severity == "warn"
+
+
+def test_fanout_just_over_major_is_danger(tmp_path: Path) -> None:
+    repo = tmp_path / "repo"
+    _write(repo, "src/coupled_m.py", _module_with_imports(FANOUT_MAJOR + 1))
+    gaps = _gaps_for(repo, title_contains="import fan-out")
+    assert len(gaps) == 1
+    assert gaps[0].severity == "danger"
+
+
+def test_fanout_just_under_warn_is_clean(tmp_path: Path) -> None:
+    repo = tmp_path / "repo"
+    _write(repo, "src/coupled_ok.py", _module_with_imports(FANOUT_WARN - 1))
+    assert _gaps_for(repo, title_contains="import fan-out") == []
+
+
+# ── Metric 5: cognitive complexity ─────────────────────────────────────────────
+# flat ifs: cognitive == #ifs (each adds 1 + 0). So #ifs == value.
+
+
+def test_cognitive_just_over_warn_is_warn(tmp_path: Path) -> None:
+    repo = tmp_path / "repo"
+    _write(repo, "src/cog_w.py", _func_with_flat_ifs(COGNITIVE_WARN + 1))
+    gaps = _gaps_for(repo, title_contains="cognitive")
+    assert len(gaps) == 1
+    assert gaps[0].severity == "warn"
+
+
+def test_cognitive_just_over_major_is_danger(tmp_path: Path) -> None:
+    repo = tmp_path / "repo"
+    _write(repo, "src/cog_m.py", _func_with_flat_ifs(COGNITIVE_MAJOR + 1))
+    gaps = _gaps_for(repo, title_contains="cognitive")
+    assert len(gaps) == 1
+    assert gaps[0].severity == "danger"
+
+
+def test_cognitive_just_under_warn_is_clean(tmp_path: Path) -> None:
+    repo = tmp_path / "repo"
+    # COGNITIVE_WARN - 1 flat ifs.  cyclomatic = (WARN-1)+1 = WARN, which is
+    # itself at the cyclomatic-warn boundary, but here we only assert there is
+    # no COGNITIVE gap. (COGNITIVE_WARN=15 > CYCLO_WARN=10, so a cyclo gap may
+    # legitimately appear — we scope the assertion to the cognitive metric.)
+    _write(repo, "src/cog_ok.py", _func_with_flat_ifs(COGNITIVE_WARN - 1))
+    assert _gaps_for(repo, title_contains="cognitive") == []
+
+
+# ── Cognitive vs cyclomatic: nesting is weighted ───────────────────────────────
+
+
+def test_nesting_trips_cognitive_below_flat_branch_count(tmp_path: Path) -> None:
+    """A deeply-nested function trips cognitive at a lower raw-branch count.
+
+    With ``depth`` nested ``if``s the raw branch count (cyclomatic) is only
+    ``depth + 1``, but cognitive is ``depth*(depth+1)/2``. We pick the smallest
+    depth whose nesting-weighted score clears COGNITIVE_WARN while its raw
+    branch count stays at or below CYCLO_WARN — proving cognitive penalizes
+    nesting that cyclomatic ignores.
+    """
+    # smallest depth with depth*(depth+1)/2 > COGNITIVE_WARN
+    depth = 1
+    while depth * (depth + 1) // 2 <= COGNITIVE_WARN:
+        depth += 1
+    raw_branches = depth + 1  # cyclomatic value for nested ifs
+
+    repo = tmp_path / "repo"
+    _write(repo, "src/nested.py", _func_with_nested_ifs(depth))
+
+    cog = _gaps_for(repo, title_contains="cognitive")
+    assert len(cog) == 1, "deep nesting must trip cognitive complexity"
+
+    # Sanity: the SAME raw branch count laid out FLAT must NOT trip cognitive,
+    # because flat ifs add only 1 each (no nesting weight). raw_branches flat
+    # ifs ⇒ cognitive == raw_branches; this stays under COGNITIVE_WARN.
+    assert raw_branches <= COGNITIVE_WARN, (
+        "test fixture invalid: flat equivalent would itself exceed cognitive warn"
+    )
+    repo_flat = tmp_path / "repo_flat"
+    _write(repo_flat, "src/flat.py", _func_with_flat_ifs(raw_branches))
+    assert _gaps_for(repo_flat, title_contains="cognitive") == [], (
+        "the same branch count laid out flat must NOT trip cognitive — "
+        "this is the proof that nesting is weighted"
+    )
+
+
+# ── False-positive suppression ─────────────────────────────────────────────────
+
+
+def test_oversized_file_under_tests_path_not_flagged(tmp_path: Path) -> None:
+    """A huge file under a ``tests/`` tree is suppressed entirely."""
+    repo = tmp_path / "repo"
+    # Way over the major module threshold, but under tests/ → no gaps at all.
+    _write(repo, "tests/test_huge.py", _module_of_code_lines(MODULE_LOC_MAJOR + 50))
+    assert analyze(repo) == []
+
+
+def test_init_py_excluded_from_fanout(tmp_path: Path) -> None:
+    """An ``__init__.py`` with > FANOUT_MAJOR imports is NOT flagged for fan-out.
+
+    Re-export hubs legitimately import a lot. The module is still otherwise
+    valid; we assert specifically that no fan-out gap is emitted.
+    """
+    repo = tmp_path / "repo"
+    _write(repo, "src/__init__.py", _module_with_imports(FANOUT_MAJOR + 5))
+    assert _gaps_for(repo, title_contains="import fan-out") == []
+
+
+# ── Never-raise contract ───────────────────────────────────────────────────────
+
+
+def test_syntax_error_file_skipped_valid_files_still_analyzed(tmp_path: Path) -> None:
+    """A SyntaxError file is skipped; analyze() does not raise and still flags valid files."""
+    repo = tmp_path / "repo"
+    # Unparseable file in the tree.
+    _write(repo, "src/broken.py", "def f(:\n    this is not python\n")
+    # A valid file that SHOULD be flagged (over module-LOC major).
+    _write(repo, "src/valid_huge.py", _module_of_code_lines(MODULE_LOC_MAJOR + 1))
+
+    gaps = analyze(repo)  # must not raise
+    oversized = [g for g in gaps if "Oversized module" in g.title]
+    assert len(oversized) == 1
+    assert oversized[0].severity == "danger"
+    # And the breach is attributed to the valid file, not the broken one.
+    assert "valid_huge.py" in oversized[0].title
+
+
+def test_empty_repo_returns_no_gaps(tmp_path: Path) -> None:
+    repo = tmp_path / "repo"
+    repo.mkdir()
+    assert analyze(repo) == []
+
+
+def test_analyze_accepts_str_path(tmp_path: Path) -> None:
+    """analyze() accepts a str path as well as a Path."""
+    repo = tmp_path / "repo"
+    _write(repo, "src/big.py", _module_of_code_lines(MODULE_LOC_WARN + 1))
+    gaps = analyze(str(repo))
+    assert any("Oversized module" in g.title for g in gaps)

exec
/bin/bash -lc "bash -lc 'nl -ba /home/renmark/projects/ai-system/renmark/modularity.py | sed -n \"1,260p\"'" in /home/renmark/projects/ai-system
 succeeded in 0ms:
     1	"""Modularity / scalability health lens — pure stdlib ``ast``, zero-dep, never-raise.
     2	
     3	renmark enforces modularity at *plan time* (one-file-per-task) but never
     4	*measures* it on the shipped codebase. This module is the missing measurement
     5	layer: an ADVISORY, deterministic, zero-LLM, stdlib-``ast``-only analyzer that
     6	surfaces code-health gaps through ``init``'s existing standards-health pipeline.
     7	
     8	Its findings are reported via the SAME :class:`renmark.init.Gap` shape the
     9	standards-health detectors emit, so they render in ``dev-standards.md`` and count
    10	toward the ``HEALTH:`` stdout line. They are **advisory only** — they never block
    11	the pipeline and never trigger an auto-refactor.
    12	
    13	The 5 metrics, each with a ``warn`` and a ``major`` band (tunable module
    14	constants below):
    15	
    16	1. **module LOC** (code lines — blanks / comments / docstring lines excluded):
    17	   :data:`MODULE_LOC_WARN` / :data:`MODULE_LOC_MAJOR`.
    18	2. **function / method length** in code LOC (per ``FunctionDef`` /
    19	   ``AsyncFunctionDef``): :data:`FUNC_LOC_WARN` / :data:`FUNC_LOC_MAJOR`.
    20	3. **cyclomatic branch count** per function (``if`` / ``for`` / ``while`` /
    21	   ``except`` / ``with`` / boolean-op / comprehension-``if`` / ``match`` case,
    22	   plus one): :data:`CYCLO_WARN` / :data:`CYCLO_MAJOR`.
    23	4. **import fan-out** per module (``import`` / ``from ... import`` statements;
    24	   ``__init__.py`` excluded — re-exports): :data:`FANOUT_WARN` / :data:`FANOUT_MAJOR`.
    25	5. **cognitive complexity** per function (nesting-weighted: each branching
    26	   construct adds ``1 + current_nesting_level``): :data:`COGNITIVE_WARN` /
    27	   :data:`COGNITIVE_MAJOR`.
    28	
    29	Design contract (mirrors :mod:`renmark.sizing`):
    30	
    31	- ``analyze(repo)`` is a pure function of the filesystem; it never mutates state.
    32	- **Never raises into the caller.** Every walk / read / ``ast.parse`` is wrapped;
    33	  a syntax-error or otherwise unparseable file is SKIPPED and analysis continues.
    34	  On any unexpected failure the result degrades to ``[]`` (no gaps reported) —
    35	  the same safe posture as ``sizing.py``.
    36	- Every threshold is a documented, tunable module-level constant.
    37	
    38	False-positive suppression (mandatory): files under ``tests/`` are skipped;
    39	generated files (common ``@generated`` / "do not edit" markers) are skipped;
    40	``__init__.py`` is excluded from the fan-out metric; class-level field
    41	assignments (dataclass / Enum bodies) are not counted as branches or cognitive
    42	complexity. Code lines are counted, never raw lines.
    43	"""
    44	
    45	from __future__ import annotations
    46	
    47	import ast
    48	from pathlib import Path
    49	
    50	from .init import Gap, _is_excluded
    51	
    52	# ── Thresholds (tunable) ─────────────────────────────────────────────────────
    53	
    54	#: Module LOC (code lines): pylint ``max-module-lines`` default is 1000.
    55	MODULE_LOC_WARN: int = 500
    56	MODULE_LOC_MAJOR: int = 1000
    57	
    58	#: Function / method length in code LOC.
    59	FUNC_LOC_WARN: int = 50
    60	FUNC_LOC_MAJOR: int = 100
    61	
    62	#: Cyclomatic branch count per function (mccabe-style: decision points + 1).
    63	CYCLO_WARN: int = 10
    64	CYCLO_MAJOR: int = 20
    65	
    66	#: Import fan-out per module (``import`` + ``from`` statements). ``__init__.py``
    67	#: is excluded from this metric (re-export hubs legitimately import a lot).
    68	FANOUT_WARN: int = 15
    69	FANOUT_MAJOR: int = 25
    70	
    71	#: Cognitive complexity per function (SonarQube-style nesting-weighted).
    72	COGNITIVE_WARN: int = 15
    73	COGNITIVE_MAJOR: int = 30
    74	
    75	# ── Suppression signals (tunable) ────────────────────────────────────────────
    76	
    77	#: Path components that mark a test tree — files under any of these are skipped.
    78	TEST_PATH_PARTS: frozenset[str] = frozenset({"tests", "test", "__tests__"})
    79	
    80	#: Substrings (checked case-insensitively against the first ~40 lines) that mark
    81	#: a machine-generated file — such files are skipped entirely.
    82	GENERATED_MARKERS: tuple[str, ...] = (
    83	    "@generated",
    84	    "do not edit",
    85	    "do not modify",
    86	    "auto-generated",
    87	    "autogenerated",
    88	    "automatically generated",
    89	    "generated by",
    90	    "this file is generated",
    91	    "code generated",
    92	)
    93	
    94	#: How many leading lines to scan for a generated-file marker.
    95	GENERATED_SCAN_LINES: int = 40
    96	
    97	
    98	# ── Public API ────────────────────────────────────────────────────────────────
    99	
   100	
   101	def analyze(repo: Path | str) -> list[Gap]:
   102	    """Analyze every Python source file under ``repo`` and return advisory gaps.
   103	
   104	    Walks Python files (reusing ``init``'s exclusion set), parses each with
   105	    :func:`ast.parse` inside a try/except — a syntax-error or any per-file
   106	    failure SKIPS that file and continues. For each file the 5 modularity
   107	    metrics are computed and one :class:`~renmark.init.Gap` is emitted per
   108	    threshold breach, ``severity`` set to ``"warn"`` (warn band) or ``"danger"``
   109	    (major band).
   110	
   111	    Never raises: any unexpected error degrades to ``[]`` (no gaps reported),
   112	    matching :mod:`renmark.sizing`'s safety posture.
   113	    """
   114	    try:
   115	        repo_path = Path(repo)
   116	        gaps: list[Gap] = []
   117	        for file in _walk_python_files(repo_path):
   118	            try:
   119	                gaps.extend(_analyze_file(file, repo_path))
   120	            except Exception:  # one bad file must never sink the whole scan
   121	                continue
   122	        # warn band is "warn"; major band is "danger" — sort danger first.
   123	        order = {"danger": 0, "warn": 1, "info": 2}
   124	        gaps.sort(key=lambda g: order.get(g.severity, 99))
   125	        return gaps
   126	    except Exception:  # never raise into the caller; degrade to no gaps.
   127	        return []
   128	
   129	
   130	# ── File discovery ─────────────────────────────────────────────────────────────
   131	
   132	
   133	def _walk_python_files(repo: Path) -> list[Path]:
   134	    """Return Python source files under ``repo``, excluding ``init``'s noise dirs.
   135	
   136	    Reuses :func:`renmark.init._is_excluded` so the walker matches the rest of
   137	    the toolchain (``.git``, ``node_modules``, ``.venv``, renmark runtime, …).
   138	    Never raises — an unreadable tree degrades to whatever was collected so far.
   139	    """
   140	    out: list[Path] = []
   141	    try:
   142	        for path in repo.rglob("*.py"):
   143	            try:
   144	                if not path.is_file():
   145	                    continue
   146	                if _is_excluded(path, repo):
   147	                    continue
   148	            except OSError:
   149	                continue
   150	            out.append(path)
   151	    except OSError:
   152	        return out
   153	    return out
   154	
   155	
   156	def _rel(path: Path, repo: Path) -> str:
   157	    """Repo-relative POSIX path string for human-readable messages."""
   158	    try:
   159	        return str(path.relative_to(repo)).replace("\\", "/")
   160	    except ValueError:
   161	        return path.name
   162	
   163	
   164	# ── Suppression predicates ─────────────────────────────────────────────────────
   165	
   166	
   167	def _is_under_tests(path: Path, repo: Path) -> bool:
   168	    """True if any path component (relative to repo) is a test-tree directory."""
   169	    try:
   170	        parts = path.relative_to(repo).parts
   171	    except ValueError:
   172	        parts = path.parts
   173	    # The file's own name isn't a "directory"; check the parent components.
   174	    return any(p in TEST_PATH_PARTS for p in parts[:-1])
   175	
   176	
   177	def _is_generated(text: str) -> bool:
   178	    """True if the file's leading lines carry a common machine-generated marker."""
   179	    head = "\n".join(text.splitlines()[:GENERATED_SCAN_LINES]).lower()
   180	    return any(marker in head for marker in GENERATED_MARKERS)
   181	
   182	
   183	# ── Per-file analysis ───────────────────────────────────────────────────────────
   184	
   185	
   186	def _analyze_file(path: Path, repo: Path) -> list[Gap]:
   187	    """Compute all 5 metrics for one file and return one Gap per breach.
   188	
   189	    Returns ``[]`` (file skipped) on read failure, syntax error, a test-tree
   190	    path, or a generated-file marker. Never raises.
   191	    """
   192	    if _is_under_tests(path, repo):
   193	        return []
   194	    try:
   195	        text = path.read_text(encoding="utf-8", errors="replace")
   196	    except OSError:
   197	        return []
   198	    if _is_generated(text):
   199	        return []
   200	    try:
   201	        tree = ast.parse(text)
   202	    except (SyntaxError, ValueError, RecursionError):
   203	        return []
   204	    except Exception:
   205	        return []
   206	
   207	    rel = _rel(path, repo)
   208	    gaps: list[Gap] = []
   209	
   210	    # 1. module LOC (code lines)
   211	    code_lines = _module_code_lines(text, tree)
   212	    gaps.extend(_module_loc_gap(rel, code_lines))
   213	
   214	    # 4. import fan-out (skip __init__.py — re-export hubs)
   215	    if path.name != "__init__.py":
   216	        fan_out = _import_fan_out(tree)
   217	        gaps.extend(_fan_out_gap(rel, fan_out))
   218	
   219	    # 2, 3, 5. per-function metrics
   220	    for func in _iter_functions(tree):
   221	        qual = _func_label(func)
   222	        func_loc = _func_code_lines(func, text)
   223	        gaps.extend(_func_loc_gap(rel, qual, func_loc))
   224	        gaps.extend(_cyclomatic_gap(rel, qual, _cyclomatic(func)))
   225	        gaps.extend(_cognitive_gap(rel, qual, _cognitive(func)))
   226	
   227	    return gaps
   228	
   229	
   230	# ── Metric 1: module code lines ────────────────────────────────────────────────
   231	
   232	
   233	def _module_code_lines(text: str, tree: ast.Module) -> int:
   234	    """Count code lines: exclude blank lines, comment lines, and docstring lines.
   235	
   236	    Docstring line numbers (module-, class-, and function-level) are gathered
   237	    from the AST and subtracted; blank and ``#``-comment physical lines are
   238	    excluded by inspecting the source text.
   239	    """
   240	    docstring_lines = _docstring_line_numbers(tree)
   241	    count = 0
   242	    for lineno, raw in enumerate(text.splitlines(), start=1):
   243	        stripped = raw.strip()
   244	        if not stripped:
   245	            continue
   246	        if stripped.startswith("#"):
   247	            continue
   248	        if lineno in docstring_lines:
   249	            continue
   250	        count += 1
   251	    return count
   252	
   253	
   254	def _docstring_line_numbers(tree: ast.Module) -> set[int]:
   255	    """Physical line numbers occupied by module / class / function docstrings."""
   256	    lines: set[int] = set()
   257	    for node in ast.walk(tree):
   258	        if not isinstance(node, (ast.Module, ast.ClassDef, ast.FunctionDef, ast.AsyncFunctionDef)):
   259	            continue
   260	        doc = ast.get_docstring(node, clean=False)

exec
/bin/bash -lc "bash -lc 'nl -ba /home/renmark/projects/ai-system/renmark/modularity.py | sed -n \"261,520p\"'" in /home/renmark/projects/ai-system
 succeeded in 0ms:
   261	        if doc is None:
   262	            continue
   263	        body = getattr(node, "body", None)
   264	        if not body:
   265	            continue
   266	        expr = body[0]
   267	        if not isinstance(expr, ast.Expr):
   268	            continue
   269	        const = expr.value
   270	        if not isinstance(const, ast.Constant) or not isinstance(const.value, str):
   271	            continue
   272	        start = getattr(const, "lineno", None)
   273	        end = getattr(const, "end_lineno", start)
   274	        if start is None:
   275	            continue
   276	        if end is None:
   277	            end = start
   278	        lines.update(range(start, end + 1))
   279	    return lines
   280	
   281	
   282	# ── Metric 2: function code lines ───────────────────────────────────────────────
   283	
   284	
   285	def _func_code_lines(func: ast.FunctionDef | ast.AsyncFunctionDef, text: str) -> int:
   286	    """Count code lines spanned by a function body (blank/comment/docstring excl.).
   287	
   288	    Spans the function's first body statement through its ``end_lineno`` so the
   289	    ``def`` signature itself isn't counted as a body line; blanks, comments, and
   290	    the function's own docstring lines are excluded.
   291	    """
   292	    if not func.body:
   293	        return 0
   294	    first = func.body[0]
   295	    start = getattr(first, "lineno", getattr(func, "lineno", 1))
   296	    end = getattr(func, "end_lineno", start) or start
   297	
   298	    doc_lines: set[int] = set()
   299	    doc = ast.get_docstring(func, clean=False)
   300	    if doc is not None and isinstance(first, ast.Expr):
   301	        const = first.value
   302	        if isinstance(const, ast.Constant) and isinstance(const.value, str):
   303	            d_start = getattr(const, "lineno", start)
   304	            d_end = getattr(const, "end_lineno", d_start) or d_start
   305	            doc_lines.update(range(d_start, d_end + 1))
   306	
   307	    all_lines = text.splitlines()
   308	    count = 0
   309	    for lineno in range(start, end + 1):
   310	        if lineno - 1 < 0 or lineno - 1 >= len(all_lines):
   311	            continue
   312	        stripped = all_lines[lineno - 1].strip()
   313	        if not stripped or stripped.startswith("#"):
   314	            continue
   315	        if lineno in doc_lines:
   316	            continue
   317	        count += 1
   318	    return count
   319	
   320	
   321	# ── Metric 4: import fan-out ────────────────────────────────────────────────────
   322	
   323	
   324	def _import_fan_out(tree: ast.Module) -> int:
   325	    """Count ``Import`` / ``ImportFrom`` statements anywhere in the module."""
   326	    return sum(1 for node in ast.walk(tree) if isinstance(node, (ast.Import, ast.ImportFrom)))
   327	
   328	
   329	# ── Function iteration ──────────────────────────────────────────────────────────
   330	
   331	
   332	def _iter_functions(tree: ast.Module) -> list[ast.FunctionDef | ast.AsyncFunctionDef]:
   333	    """All function/method defs in the module (nested ones included)."""
   334	    return [n for n in ast.walk(tree) if isinstance(n, (ast.FunctionDef, ast.AsyncFunctionDef))]
   335	
   336	
   337	def _func_label(func: ast.FunctionDef | ast.AsyncFunctionDef) -> str:
   338	    """Function name plus its measured value placeholder, e.g. ``my_func``."""
   339	    return f"{func.name}()"
   340	
   341	
   342	# ── Metric 3: cyclomatic complexity ─────────────────────────────────────────────
   343	
   344	
   345	def _cyclomatic(func: ast.FunctionDef | ast.AsyncFunctionDef) -> int:
   346	    """McCabe-style branch count: decision nodes within the function body + 1.
   347	
   348	    Counts ``If`` / ``For`` / ``While`` / ``ExceptHandler`` / ``With`` /
   349	    ``BoolOp`` (and/or) / comprehension-``if`` / ``match`` ``case`` nodes that
   350	    belong to THIS function (nested functions are excluded — they have their own
   351	    score). Class-level field assignments inside the function are not branches.
   352	    """
   353	    count = 1
   354	    for node in _walk_own_body(func):
   355	        if isinstance(
   356	            node,
   357	            (ast.If, ast.For, ast.AsyncFor, ast.While, ast.ExceptHandler, ast.With, ast.AsyncWith),
   358	        ):
   359	            count += 1
   360	        elif isinstance(node, ast.BoolOp):
   361	            # n operands → n-1 additional branch decisions.
   362	            count += max(len(node.values) - 1, 1)
   363	        elif isinstance(node, ast.comprehension):
   364	            count += len(node.ifs)
   365	        elif _is_match_case(node):
   366	            count += 1
   367	    return count
   368	
   369	
   370	# ── Metric 5: cognitive complexity ──────────────────────────────────────────────
   371	
   372	
   373	def _cognitive(func: ast.FunctionDef | ast.AsyncFunctionDef) -> int:
   374	    """Nesting-weighted cognitive complexity (SonarQube-style approximation).
   375	
   376	    Each branching construct adds ``1 + current_nesting_level``; nesting
   377	    increments inside ``if`` / ``for`` / ``while`` / ``with`` / ``except``
   378	    bodies. Boolean operators and comprehension ``if``s add a flat increment.
   379	    Nested function definitions are not descended into (they score separately).
   380	    """
   381	    score = 0
   382	
   383	    def visit(node: ast.AST, nesting: int) -> None:
   384	        nonlocal score
   385	        for child in ast.iter_child_nodes(node):
   386	            if isinstance(child, (ast.FunctionDef, ast.AsyncFunctionDef)):
   387	                continue  # nested functions scored on their own
   388	            is_nesting_construct = isinstance(
   389	                child,
   390	                (ast.If, ast.For, ast.AsyncFor, ast.While, ast.With, ast.AsyncWith, ast.ExceptHandler),
   391	            ) or _is_match_case(child)
   392	            if is_nesting_construct:
   393	                score += 1 + nesting
   394	                visit(child, nesting + 1)
   395	            elif isinstance(child, ast.BoolOp):
   396	                score += 1
   397	                visit(child, nesting)
   398	            elif isinstance(child, ast.comprehension):
   399	                score += len(child.ifs)
   400	                visit(child, nesting)
   401	            else:
   402	                visit(child, nesting)
   403	
   404	    visit(func, 0)
   405	    return score
   406	
   407	
   408	# ── AST traversal helpers ───────────────────────────────────────────────────────
   409	
   410	
   411	def _walk_own_body(func: ast.FunctionDef | ast.AsyncFunctionDef) -> list[ast.AST]:
   412	    """All descendant nodes of ``func`` EXCLUDING nested function bodies.
   413	
   414	    A nested ``def``/``async def`` carries its own complexity score, so we don't
   415	    let its internals inflate the enclosing function's count.
   416	    """
   417	    out: list[ast.AST] = []
   418	
   419	    def walk(node: ast.AST) -> None:
   420	        for child in ast.iter_child_nodes(node):
   421	            if isinstance(child, (ast.FunctionDef, ast.AsyncFunctionDef)):
   422	                continue
   423	            out.append(child)
   424	            walk(child)
   425	
   426	    walk(func)
   427	    return out
   428	
   429	
   430	def _is_match_case(node: ast.AST) -> bool:
   431	    """True if ``node`` is a ``match`` ``case`` clause (Python 3.10+ ``match_case``)."""
   432	    match_case = getattr(ast, "match_case", None)
   433	    return match_case is not None and isinstance(node, match_case)
   434	
   435	
   436	# ── Gap construction (per metric) ───────────────────────────────────────────────
   437	
   438	
   439	def _band(value: int, warn: int, major: int) -> str | None:
   440	    """Return ``"danger"`` (>= major), ``"warn"`` (>= warn), or ``None`` (ok)."""
   441	    if value >= major:
   442	        return "danger"
   443	    if value >= warn:
   444	        return "warn"
   445	    return None
   446	
   447	
   448	def _module_loc_gap(rel: str, code_lines: int) -> list[Gap]:
   449	    band = _band(code_lines, MODULE_LOC_WARN, MODULE_LOC_MAJOR)
   450	    if band is None:
   451	        return []
   452	    limit = MODULE_LOC_MAJOR if band == "danger" else MODULE_LOC_WARN
   453	    return [
   454	        Gap(
   455	            band,
   456	            f"Oversized module: `{rel}` ({code_lines} code lines)",
   457	            f"`{rel}` has {code_lines} code lines (threshold {limit}). Large modules are "
   458	            "hard to navigate, review, and test, and tend to accrete unrelated responsibilities.",
   459	            f"Split `{rel}` into focused modules along its natural seams "
   460	            "(one cohesive responsibility per file). Advisory — never auto-refactored.",
   461	        )
   462	    ]
   463	
   464	
   465	def _fan_out_gap(rel: str, fan_out: int) -> list[Gap]:
   466	    band = _band(fan_out, FANOUT_WARN, FANOUT_MAJOR)
   467	    if band is None:
   468	        return []
   469	    limit = FANOUT_MAJOR if band == "danger" else FANOUT_WARN
   470	    return [
   471	        Gap(
   472	            band,
   473	            f"High import fan-out: `{rel}` ({fan_out} imports)",
   474	            f"`{rel}` has {fan_out} import statements (threshold {limit}). High fan-out signals "
   475	            "a module coupled to many others — a change-amplifier and a refactoring hazard.",
   476	            f"Reduce coupling in `{rel}`: extract a narrower interface, group related imports "
   477	            "behind a facade, or move logic closer to its dependencies. Advisory.",
   478	        )
   479	    ]
   480	
   481	
   482	def _func_loc_gap(rel: str, qual: str, func_loc: int) -> list[Gap]:
   483	    band = _band(func_loc, FUNC_LOC_WARN, FUNC_LOC_MAJOR)
   484	    if band is None:
   485	        return []
   486	    limit = FUNC_LOC_MAJOR if band == "danger" else FUNC_LOC_WARN
   487	    return [
   488	        Gap(
   489	            band,
   490	            f"Long function: `{rel}` → `{qual}` ({func_loc} code lines)",
   491	            f"`{qual}` in `{rel}` spans {func_loc} code lines (threshold {limit}). Long functions "
   492	            "are hard to read in one pass and usually do more than one thing.",
   493	            f"Extract cohesive blocks of `{qual}` into well-named helpers. Advisory.",
   494	        )
   495	    ]
   496	
   497	
   498	def _cyclomatic_gap(rel: str, qual: str, value: int) -> list[Gap]:
   499	    band = _band(value, CYCLO_WARN, CYCLO_MAJOR)
   500	    if band is None:
   501	        return []
   502	    limit = CYCLO_MAJOR if band == "danger" else CYCLO_WARN
   503	    return [
   504	        Gap(
   505	            band,
   506	            f"High cyclomatic complexity: `{rel}` → `{qual}` ({value} branches)",
   507	            f"`{qual}` in `{rel}` has a cyclomatic branch count of {value} (threshold {limit}). "
   508	            "Many decision points mean many paths to test and many ways to be wrong.",
   509	            f"Reduce branching in `{qual}`: early returns, guard clauses, table/dispatch dictionaries, "
   510	            "or splitting the function. Advisory.",
   511	        )
   512	    ]
   513	
   514	
   515	def _cognitive_gap(rel: str, qual: str, value: int) -> list[Gap]:
   516	    band = _band(value, COGNITIVE_WARN, COGNITIVE_MAJOR)
   517	    if band is None:
   518	        return []
   519	    limit = COGNITIVE_MAJOR if band == "danger" else COGNITIVE_WARN
   520	    return [

exec
/bin/bash -lc "bash -lc 'nl -ba /home/renmark/projects/ai-system/renmark/modularity.py | sed -n \"521,760p\"'" in /home/renmark/projects/ai-system
 succeeded in 0ms:
   521	        Gap(
   522	            band,
   523	            f"High cognitive complexity: `{rel}` → `{qual}` (score {value})",
   524	            f"`{qual}` in `{rel}` has a nesting-weighted cognitive-complexity score of {value} "
   525	            f"(threshold {limit}). Deep nesting is disproportionately hard for a human to follow.",
   526	            f"Flatten nesting in `{qual}`: invert conditions to return early, extract nested blocks "
   527	            "into helpers, and reduce branching depth. Advisory.",
   528	        )
   529	    ]

codex
The analyzer itself is loaded. I’m now checking the exact semantics it plugs into in `init.py`, then I’ll validate the claimed “never-raise” and boundary behavior by reading/running the focused tests.
exec
/bin/bash -lc "bash -lc 'nl -ba /home/renmark/projects/ai-system/renmark/init.py | sed -n \"120,180p\"'" in /home/renmark/projects/ai-system
 succeeded in 0ms:
   120	    """One detected dev standard (test, lint, type-check, etc.)."""
   121	
   122	    name: str  # canonical key: "test", "lint", "format", "typecheck", "ci", ...
   123	    command: str | None  # invocation, e.g. "pytest -q" — None if not directly runnable
   124	    config_file: str | None  # where it was detected (relative path)
   125	    detail: str = ""  # extra info for dev-standards.md
   126	
   127	
   128	@dataclass
   129	class Gap:
   130	    """One standards-health gap with a tighten-this recommendation."""
   131	
   132	    severity: str  # "danger", "warn", "info"
   133	    title: str
   134	    detail: str
   135	    recommendation: str
   136	
   137	
   138	@dataclass
   139	class StandardsScan:
   140	    """Result of scanning the repo for dev standards + health gaps.
   141	
   142	    ``gaps`` are the standards-health gaps (linter/CI/tests/secrets …).
   143	    ``modularity_gaps`` are file-level modularity breaches from
   144	    :mod:`renmark.modularity` — kept in a SEPARATE list so the always-loaded
   145	    stub and the bounded ``HEALTH:`` stdout line can summarize them as counts
   146	    rather than dumping 100+ per-gap entries. Both feed the total gap count.
   147	    """
   148	
   149	    standards: list[Standard] = field(default_factory=list)
   150	    gaps: list[Gap] = field(default_factory=list)
   151	    modularity_gaps: list[Gap] = field(default_factory=list)
   152	    deep: bool = False
   153	
   154	
   155	@dataclass
   156	class RepoScan:
   157	    repo: Path
   158	    project_name: str
   159	    stack: str
   160	    entry_points: list[str]
   161	    top_dirs: list[tuple[str, str]]  # (dirname, one-line purpose)
   162	    files: list[FileInfo]
   163	    lang_counts: dict[str, int]
   164	    commands: list[tuple[str, str]]  # (name, one-liner)
   165	    git_sha: str | None
   166	    today: str
   167	    standards: StandardsScan | None = None
   168	
   169	
   170	# ── Discovery helpers ────────────────────────────────────────────────────────
   171	
   172	
   173	def _is_excluded(path: Path, repo: Path) -> bool:
   174	    """True if any path component is in EXCLUDE_DIRS, or path is in renmark runtime."""
   175	    try:
   176	        rel = path.relative_to(repo)
   177	    except ValueError:
   178	        return True
   179	    parts = rel.parts
   180	    if any(p in EXCLUDE_DIRS for p in parts):

exec
/bin/bash -lc "bash -lc 'nl -ba /home/renmark/projects/ai-system/renmark/init.py | sed -n \"1180,1345p\"'" in /home/renmark/projects/ai-system
 succeeded in 0ms:
  1180	    # Severity sort: danger > warn > info
  1181	    order = {"danger": 0, "warn": 1, "info": 2}
  1182	    gaps.sort(key=lambda g: order.get(g.severity, 99))
  1183	    return gaps
  1184	
  1185	
  1186	def scan_standards(repo: Path, files: list[FileInfo], deep: bool = False) -> StandardsScan:
  1187	    detectors = (
  1188	        _detect_test,
  1189	        _detect_lint,
  1190	        _detect_format,
  1191	        _detect_typecheck,
  1192	        _detect_ci,
  1193	        _detect_precommit,
  1194	        _detect_env_schema,
  1195	        _detect_db,
  1196	        _detect_local_dev,
  1197	        _detect_style,
  1198	        _detect_deps_policy,
  1199	    )
  1200	    standards = [s for s in (d(repo) for d in detectors) if s is not None]
  1201	    gaps = evaluate_health(repo, standards, files, deep)
  1202	    # Additive: merge in file-level modularity breaches (separate list so the
  1203	    # bounded stub / HEALTH line summarize them as counts, not a 100+ dump).
  1204	    # analyze() never raises and returns [] on failure — keep init zero-LLM.
  1205	    from . import modularity
  1206	
  1207	    modularity_gaps = modularity.analyze(repo)
  1208	    return StandardsScan(standards=standards, gaps=gaps, modularity_gaps=modularity_gaps, deep=deep)
  1209	
  1210	
  1211	# ── Standards rendering ──────────────────────────────────────────────────────
  1212	
  1213	
  1214	_SEVERITY_PREFIX = {"danger": "🚨", "warn": "⚠", "info": "ℹ"}
  1215	
  1216	
  1217	def render_dev_gates_line(standards: StandardsScan) -> str | None:
  1218	    """A single line for the CLAUDE.md stub, listing the most-important commands.
  1219	
  1220	    Returns None if there are no gates worth surfacing in the always-loaded
  1221	    context (greenfield project with no standards).
  1222	    """
  1223	    by_name = {s.name: s for s in standards.standards}
  1224	    parts: list[str] = []
  1225	    for key, label in (("test", "test"), ("lint", "lint"), ("typecheck", "types")):
  1226	        s = by_name.get(key)
  1227	        if s and s.command:
  1228	            parts.append(f"{label} `{s.command}`")
  1229	    ci = by_name.get("ci")
  1230	    if ci:
  1231	        # ci.detail is like "GitHub Actions: build, test, deploy"
  1232	        wf_part = ci.detail.split(":", 1)[1].strip() if ":" in ci.detail else ci.detail
  1233	        parts.append(f"CI: {wf_part}")
  1234	    return "**Dev gates:** " + " · ".join(parts) if parts else None
  1235	
  1236	
  1237	def _render_modularity_section(modularity_gaps: list[Gap]) -> list[str]:
  1238	    """Render the modularity gaps as a bounded, grouped subsection.
  1239	
  1240	    Majors (``danger``) are listed first, then warns, capped at
  1241	    ``MODULARITY_GAPS_RENDER_CAP`` total with a ``+N more`` note for the
  1242	    remainder. Returns ``[]`` when there are no modularity gaps so the section
  1243	    is omitted entirely (no empty heading).
  1244	    """
  1245	    if not modularity_gaps:
  1246	        return []
  1247	    order = {"danger": 0, "warn": 1, "info": 2}
  1248	    ordered = sorted(modularity_gaps, key=lambda g: order.get(g.severity, 99))
  1249	    n_major = sum(1 for g in modularity_gaps if g.severity == "danger")
  1250	    n_warn = sum(1 for g in modularity_gaps if g.severity == "warn")
  1251	    out: list[str] = []
  1252	    out.append("## Modularity")
  1253	    out.append("")
  1254	    out.append(
  1255	        f"**{len(modularity_gaps)} modularity gap{'s' if len(modularity_gaps) != 1 else ''}** "
  1256	        f"({n_major} major, {n_warn} warn) — file-level size/complexity breaches. "
  1257	        "Advisory: these never block init."
  1258	    )
  1259	    out.append("")
  1260	    shown = ordered[:MODULARITY_GAPS_RENDER_CAP]
  1261	    for g in shown:
  1262	        prefix = _SEVERITY_PREFIX.get(g.severity, "•")
  1263	        out.append(f"- {prefix} **{g.title}** — {g.detail} _{g.recommendation}_")
  1264	    remaining = len(ordered) - len(shown)
  1265	    if remaining > 0:
  1266	        out.append(f"- _… +{remaining} more (re-run for the full list)_")
  1267	    out.append("")
  1268	    return out
  1269	
  1270	
  1271	def render_standards_md(repo_name: str, today: str, git_sha: str | None, standards: StandardsScan) -> str:
  1272	    sha = git_sha or "no-git"
  1273	    out: list[str] = []
  1274	    out.append("<!-- Managed by /renmark:init. Wholly regenerated on each run. Do not hand-edit. -->")
  1275	    out.append(f"<!-- Last refreshed: {today} @ {sha} -->")
  1276	    out.append("")
  1277	    out.append(f"# Dev standards — {repo_name}")
  1278	    out.append("")
  1279	    out.append(
  1280	        "What this project enforces about itself, detected from configuration files. "
  1281	        "Read this before making non-trivial changes so you don't break gates that "
  1282	        "are silently checking your work."
  1283	    )
  1284	    out.append("")
  1285	
  1286	    # ── Detected standards
  1287	    if standards.standards:
  1288	        out.append("## Detected standards")
  1289	        out.append("")
  1290	        out.append("| Standard | Command | Detected in | Notes |")
  1291	        out.append("|---|---|---|---|")
  1292	        for s in standards.standards:
  1293	            cmd = f"`{s.command}`" if s.command else "—"
  1294	            cfg = f"`{s.config_file}`" if s.config_file else "—"
  1295	            detail = (s.detail or "—").replace("|", "\\|")
  1296	            out.append(f"| {s.name} | {cmd} | {cfg} | {detail} |")
  1297	        out.append("")
  1298	    else:
  1299	        out.append("## Detected standards")
  1300	        out.append("")
  1301	        out.append("_None detected._ This is either a greenfield project or one without enforced standards.")
  1302	        out.append("")
  1303	
  1304	    # ── Standards health
  1305	    out.append("## Standards health")
  1306	    out.append("")
  1307	    if not standards.gaps:
  1308	        out.append("✅ **No gaps detected.** Linter, type checker, tests, and CI are all wired up.")
  1309	    else:
  1310	        counts = {"danger": 0, "warn": 0, "info": 0}
  1311	        for g in standards.gaps:
  1312	            counts[g.severity] = counts.get(g.severity, 0) + 1
  1313	        summary = ", ".join(f"{n} {sev}" for sev, n in counts.items() if n)
  1314	        out.append(
  1315	            f"**{len(standards.gaps)} gap{'s' if len(standards.gaps) != 1 else ''} detected** ({summary}). Tightening recommendations below."
  1316	        )
  1317	        out.append("")
  1318	        for g in standards.gaps:
  1319	            prefix = _SEVERITY_PREFIX.get(g.severity, "•")
  1320	            out.append(f"### {prefix} {g.title}")
  1321	            out.append("")
  1322	            out.append(g.detail)
  1323	            out.append("")
  1324	            out.append(f"**Recommendation:** {g.recommendation}")
  1325	            out.append("")
  1326	
  1327	    # ── Modularity (own subsection, majors first, bounded list)
  1328	    out.extend(_render_modularity_section(standards.modularity_gaps))
  1329	
  1330	    if not standards.deep:
  1331	        out.append("")
  1332	        out.append("_Run `python -m renmark.init --deep` for deeper checks (commit-message style, etc.)._")
  1333	
  1334	    return "\n".join(out)
  1335	
  1336	
  1337	def write_standards_md(repo: Path, repo_name: str, today: str, git_sha: str | None, standards: StandardsScan) -> str:
  1338	    """Write .renmark/memory/dev-standards.md. Returns 'created' / 'refreshed' / 'unchanged'."""
  1339	    mem_dir = repo / ".renmark" / "memory"
  1340	    mem_dir.mkdir(parents=True, exist_ok=True)
  1341	    target = mem_dir / "dev-standards.md"
  1342	    new_text = render_standards_md(repo_name, today, git_sha, standards)
  1343	
  1344	    if target.exists():
  1345	        existing = target.read_text(encoding="utf-8", errors="replace")

exec
/bin/bash -lc "bash -lc 'nl -ba /home/renmark/projects/ai-system/renmark/init.py | sed -n \"1520,1605p\"'" in /home/renmark/projects/ai-system
 succeeded in 0ms:
  1520	                nl = text.find("\n", m.end())
  1521	                return len(text) if nl < 0 else nl + 1
  1522	        return None
  1523	
  1524	    # Prefer inserting AFTER the closest preceding present block.
  1525	    for prev in reversed(order[:idx]):
  1526	        pos = _end_line_end(prev)
  1527	        if pos is not None:
  1528	            chunk = block if block.endswith("\n") else block + "\n"
  1529	            return text[:pos] + "\n" + chunk + text[pos:]
  1530	
  1531	    # Else insert BEFORE the closest following present block.
  1532	    for nxt in order[idx + 1 :]:
  1533	        pos = _begin_pos(nxt)
  1534	        if pos is not None:
  1535	            chunk = block if block.endswith("\n") else block + "\n"
  1536	            return text[:pos] + chunk + "\n" + text[pos:]
  1537	
  1538	    # No anchors — append at EOF.
  1539	    suffix = "" if text.endswith("\n") else "\n"
  1540	    chunk = block if block.endswith("\n") else block + "\n"
  1541	    return text + suffix + "\n" + chunk
  1542	
  1543	
  1544	# ── Top-level run ────────────────────────────────────────────────────────────
  1545	
  1546	
  1547	def run(repo: Path, include_private: bool = False, deep: bool = False) -> tuple[int, str]:
  1548	    """Returns (exit_code, summary_line)."""
  1549	    # Scaffold-if-missing FIRST: create CLAUDE.md/AGENTS.md/CHANGELOG.md/.renmark/
  1550	    # from templates (existence-skip), so init initializes rather than dead-ends.
  1551	    try:
  1552	        _scaffold_missing(repo)
  1553	    except RuntimeError as exc:
  1554	        return 1, f"FAIL  {exc}"
  1555	
  1556	    claude_md = repo / "CLAUDE.md"
  1557	    if not claude_md.exists():
  1558	        # Should never happen: scaffold ran above. Only fires if templates were
  1559	        # unavailable and bootstrap silently produced nothing.
  1560	        return 1, "FAIL  CLAUDE.md still absent after scaffold — renmark templates unavailable?"
  1561	
  1562	    # Back-fill any missing canonical rule blocks (verbatim, idempotent).
  1563	    # Two distinct failure classes:
  1564	    #   - MarkerCorruptionError → user-fixable document corruption → exit 2
  1565	    #   - any other RuntimeError (e.g. templates unavailable)      → exit 1
  1566	    try:
  1567	        blocks_added = merge_rule_blocks(repo)
  1568	    except MarkerCorruptionError as exc:
  1569	        return 2, f"FAIL  {exc}"
  1570	    except RuntimeError as exc:
  1571	        return 1, f"FAIL  {exc}"
  1572	    n_blocks_added = sum(blocks_added.values())
  1573	
  1574	    scan = scan_repo(repo, include_private=include_private)
  1575	    # Standards scan runs first so the stub can include the gates line
  1576	    scan.standards = scan_standards(repo, scan.files, deep=deep)
  1577	
  1578	    try:
  1579	        stub_status = merge_stub_into(claude_md, scan)
  1580	        agents_md = repo / "AGENTS.md"
  1581	        agents_status = merge_stub_into(agents_md, scan) if agents_md.exists() else "skipped"
  1582	        map_status = write_full_map(repo, scan)
  1583	        standards_status = write_standards_md(repo, scan.project_name, scan.today, scan.git_sha, scan.standards)
  1584	    except RuntimeError as exc:
  1585	        return 2, f"FAIL  {exc}"
  1586	
  1587	    sha = scan.git_sha or "no-git"
  1588	    langs_summary = ",".join(sorted(scan.lang_counts.keys())) or "—"
  1589	    n_gaps = len(scan.standards.gaps)
  1590	    mod_gaps = scan.standards.modularity_gaps
  1591	    n_mod = len(mod_gaps)
  1592	    blocks_field = str(n_blocks_added) if n_blocks_added else "unchanged"
  1593	    summary_lines = [
  1594	        f"OK  stub={stub_status} agents={agents_status} map={map_status} standards={standards_status} "
  1595	        f"blocks={blocks_field} modules={len(scan.files)} commands={len(scan.commands)} "
  1596	        f"langs={langs_summary} ref={scan.today}@{sha}"
  1597	    ]
  1598	    # Bounded HEALTH line: counts ONLY — never the per-gap detail. Modularity
  1599	    # can produce 100+ gaps; they live in dev-standards.md, not on stdout.
  1600	    if n_gaps > 0 or n_mod > 0:
  1601	        parts: list[str] = []
  1602	        if n_gaps > 0:
  1603	            counts = {"danger": 0, "warn": 0, "info": 0}
  1604	            for g in scan.standards.gaps:
  1605	                counts[g.severity] = counts.get(g.severity, 0) + 1

exec
/bin/bash -lc "bash -lc 'pytest -q /home/renmark/projects/ai-system/tests/test_modularity.py'" in /home/renmark/projects/ai-system
 exited 1 in 247ms:
Traceback (most recent call last):
  File "/opt/miniconda/envs/claude/bin/pytest", line 6, in <module>
    sys.exit(console_main())
             ~~~~~~~~~~~~^^
  File "/opt/miniconda/envs/claude/lib/python3.14/site-packages/_pytest/config/__init__.py", line 223, in console_main
    code = main()
  File "/opt/miniconda/envs/claude/lib/python3.14/site-packages/_pytest/config/__init__.py", line 193, in main
    config = _prepareconfig(new_args, plugins)
  File "/opt/miniconda/envs/claude/lib/python3.14/site-packages/_pytest/config/__init__.py", line 361, in _prepareconfig
    config: Config = pluginmanager.hook.pytest_cmdline_parse(
                     ~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~^
        pluginmanager=pluginmanager, args=args
        ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^
    )
    ^
  File "/opt/miniconda/envs/claude/lib/python3.14/site-packages/pluggy/_hooks.py", line 512, in __call__
    return self._hookexec(self.name, self._hookimpls.copy(), kwargs, firstresult)
           ~~~~~~~~~~~~~~^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^
  File "/opt/miniconda/envs/claude/lib/python3.14/site-packages/pluggy/_manager.py", line 120, in _hookexec
    return self._inner_hookexec(hook_name, methods, kwargs, firstresult)
           ~~~~~~~~~~~~~~~~~~~~^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^
  File "/opt/miniconda/envs/claude/lib/python3.14/site-packages/pluggy/_callers.py", line 167, in _multicall
    raise exception
  File "/opt/miniconda/envs/claude/lib/python3.14/site-packages/pluggy/_callers.py", line 139, in _multicall
    teardown.throw(exception)
    ~~~~~~~~~~~~~~^^^^^^^^^^^
  File "/opt/miniconda/envs/claude/lib/python3.14/site-packages/_pytest/helpconfig.py", line 124, in pytest_cmdline_parse
    config = yield
             ^^^^^
  File "/opt/miniconda/envs/claude/lib/python3.14/site-packages/pluggy/_callers.py", line 121, in _multicall
    res = hook_impl.function(*args)
  File "/opt/miniconda/envs/claude/lib/python3.14/site-packages/_pytest/config/__init__.py", line 1192, in pytest_cmdline_parse
    self.parse(args)
    ~~~~~~~~~~^^^^^^
  File "/opt/miniconda/envs/claude/lib/python3.14/site-packages/_pytest/config/__init__.py", line 1562, in parse
    self.hook.pytest_load_initial_conftests(
    ~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~^
        early_config=self, args=args, parser=self._parser
        ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^
    )
    ^
  File "/opt/miniconda/envs/claude/lib/python3.14/site-packages/pluggy/_hooks.py", line 512, in __call__
    return self._hookexec(self.name, self._hookimpls.copy(), kwargs, firstresult)
           ~~~~~~~~~~~~~~^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^
  File "/opt/miniconda/envs/claude/lib/python3.14/site-packages/pluggy/_manager.py", line 120, in _hookexec
    return self._inner_hookexec(hook_name, methods, kwargs, firstresult)
           ~~~~~~~~~~~~~~~~~~~~^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^
  File "/opt/miniconda/envs/claude/lib/python3.14/site-packages/pluggy/_callers.py", line 167, in _multicall
    raise exception
  File "/opt/miniconda/envs/claude/lib/python3.14/site-packages/pluggy/_callers.py", line 139, in _multicall
    teardown.throw(exception)
    ~~~~~~~~~~~~~~^^^^^^^^^^^
  File "/opt/miniconda/envs/claude/lib/python3.14/site-packages/_pytest/warnings.py", line 128, in pytest_load_initial_conftests
    return (yield)
            ^^^^^
  File "/opt/miniconda/envs/claude/lib/python3.14/site-packages/pluggy/_callers.py", line 116, in _multicall
    next(function_gen)  # first yield
    ~~~~^^^^^^^^^^^^^^
  File "/opt/miniconda/envs/claude/lib/python3.14/site-packages/_pytest/capture.py", line 170, in pytest_load_initial_conftests
    capman.start_global_capturing()
    ~~~~~~~~~~~~~~~~~~~~~~~~~~~~~^^
  File "/opt/miniconda/envs/claude/lib/python3.14/site-packages/_pytest/capture.py", line 773, in start_global_capturing
    self._global_capturing = _get_multicapture(self._method)
                             ~~~~~~~~~~~~~~~~~^^^^^^^^^^^^^^
  File "/opt/miniconda/envs/claude/lib/python3.14/site-packages/_pytest/capture.py", line 714, in _get_multicapture
    return MultiCapture(in_=FDCapture(0), out=FDCapture(1), err=FDCapture(2))
                                              ~~~~~~~~~^^^
  File "/opt/miniconda/envs/claude/lib/python3.14/site-packages/_pytest/capture.py", line 493, in __init__
    TemporaryFile(buffering=0),
    ~~~~~~~~~~~~~^^^^^^^^^^^^^
  File "/opt/miniconda/envs/claude/lib/python3.14/tempfile.py", line 652, in TemporaryFile
    prefix, suffix, dir, output_type = _sanitize_params(prefix, suffix, dir)
                                       ~~~~~~~~~~~~~~~~^^^^^^^^^^^^^^^^^^^^^
  File "/opt/miniconda/envs/claude/lib/python3.14/tempfile.py", line 126, in _sanitize_params
    dir = gettempdir()
  File "/opt/miniconda/envs/claude/lib/python3.14/tempfile.py", line 316, in gettempdir
    return _os.fsdecode(_gettempdir())
                        ~~~~~~~~~~~^^
  File "/opt/miniconda/envs/claude/lib/python3.14/tempfile.py", line 309, in _gettempdir
    tempdir = _get_default_tempdir()
  File "/opt/miniconda/envs/claude/lib/python3.14/tempfile.py", line 224, in _get_default_tempdir
    raise FileNotFoundError(_errno.ENOENT,
                            "No usable temporary directory found in %s" %
                            dirlist)
FileNotFoundError: [Errno 2] No usable temporary directory found in ['/tmp', '/var/tmp', '/usr/tmp', '/home/renmark/projects/ai-system']

codex
`pytest` is blocked by the sandbox’s lack of a writable temp directory, so I’m validating the metric math directly with in-memory AST snippets instead of relying on the test harness.
exec
/bin/bash -lc "python - <<'PY'
import ast
from renmark.modularity import _cyclomatic, _cognitive

samples = {
'bool_chain':'def f():\\n    return a and b and c\\n',
'comp_if':'def f():\\n    return [x for x in xs if a if b]\\n',
'match_guard':'def f(x):\\n    match x:\\n        case 1 if cond:\\n            return 1\\n        case _:\\n            return 0\\n',
'try_except':'def f():\\n    try:\\n        a()\\n    except ValueError:\\n        b()\\n    except TypeError:\\n        c()\\n    else:\\n        d()\\n    finally:\\n        e()\\n',
'nested_def':'def f():\\n    def g():\\n        if x:\\n            pass\\n    if y:\\n        pass\\n',
'nested_class':'def f():\\n    class C:\\n        x = 1\\n        def m(self):\\n            if z:\\n                pass\\n    if y:\\n        pass\\n',
}
for name, src in samples.items():
    func = ast.parse(src).body[0]
    print(name, 'cyclo=', _cyclomatic(func), 'cog=', _cognitive(func))
PY" in /home/renmark/projects/ai-system
 succeeded in 0ms:
bool_chain cyclo= 3 cog= 1
comp_if cyclo= 3 cog= 2
match_guard cyclo= 3 cog= 2
try_except cyclo= 3 cog= 2
nested_def cyclo= 2 cog= 1
nested_class cyclo= 2 cog= 1

exec
/bin/bash -lc "python - <<'PY'
import ast
from renmark.modularity import _module_code_lines, _func_code_lines

src = '''\"\"\"module doc\\nmore\\n\"\"\"\\n\\n# comment\\nclass C:\\n    \"\"\"class doc\"\"\"\\n    x = 1\\n\\n    def f(self):\\n        \"\"\"fn doc\\n        more\\n        \"\"\"\\n        # c\\n        y = 1\\n        def g():\\n            z = 2\\n        return y\\n'''
mod = ast.parse(src)
func = mod.body[2].body[1]
print('module_loc', _module_code_lines(src, mod))
print('func_loc', _func_code_lines(func, src))
PY" in /home/renmark/projects/ai-system
exec
/bin/bash -lc "python - <<'PY'
from pathlib import Path
from renmark.modularity import _is_generated, _is_under_tests
repo = Path('/repo')
for path in [Path('/repo/tests/x.py'), Path('/repo/src/test_utils.py'), Path('/repo/src/__tests__/x.py')]:
    print(path, _is_under_tests(path, repo))
for text in ['# generated by hand, do not edit this paragraph?\\nprint(1)', '\"\"\"Normal docs mentioning generated by parser in prose\"\"\"\\nprint(1)', '# nothing\\nprint(1)']:
    print(_is_generated(text))
PY" in /home/renmark/projects/ai-system
 succeeded in 0ms:
/repo/tests/x.py True
/repo/src/test_utils.py False
/repo/src/__tests__/x.py True
True
True
False

exec
/bin/bash -lc "python - <<'PY'
import ast
from renmark.modularity import _band, CYCLO_WARN, CYCLO_MAJOR
print('band_warn_eq', _band(CYCLO_WARN, CYCLO_WARN, CYCLO_MAJOR))
print('band_major_eq', _band(CYCLO_MAJOR, CYCLO_WARN, CYCLO_MAJOR))
print('band_warn_minus1', _band(CYCLO_WARN-1, CYCLO_WARN, CYCLO_MAJOR))
PY" in /home/renmark/projects/ai-system
 succeeded in 0ms:
band_warn_eq warn
band_major_eq danger
band_warn_minus1 None

 exited 1 in 114ms:
codex
I have enough to confirm some of the counting behavior directly. I’m doing one more pass on edge cases that can hide false positives: nested classes, generated markers, and whether imports/complexity are counted inside inner scopes when the metric says “per module” or “per function.”
exec
/bin/bash -lc "python - <<'PY'
import ast
from renmark.modularity import _cyclomatic, _cognitive
src = '''def f():
    try:
        if a and b:
            return [x for x in xs if p if q]
    except ValueError:
        pass
'''
func = ast.parse(src).body[0]
print('cyclo', _cyclomatic(func))
print('cognitive', _cognitive(func))
PY" in /home/renmark/projects/ai-system
 succeeded in 0ms:
cyclo 6
cognitive 5

exec
/bin/bash -lc "bash -lc 'nl -ba /home/renmark/projects/ai-system/tests/test_modularity.py | sed -n \"1,260p\"'" in /home/renmark/projects/ai-system
 succeeded in 0ms:
     1	"""Unit tests for renmark.modularity — the advisory code-health analyzer.
     2	
     3	Hermetic: every test writes tiny synthetic ``.py`` files into ``tmp_path`` and
     4	calls :func:`renmark.modularity.analyze`. Thresholds are imported as module
     5	constants so tuning them never silently breaks (or silently passes) these tests.
     6	
     7	For each of the 5 metrics we assert the band boundaries:
     8	
     9	- JUST OVER the warn threshold → a Gap with ``severity == "warn"``
    10	- JUST OVER the major threshold → a Gap with ``severity == "danger"``
    11	- JUST UNDER the warn threshold → NO gap for that metric
    12	
    13	Plus false-positive suppression, the never-raise contract, and a cognitive-vs-
    14	cyclomatic sanity check that nesting is weighted.
    15	"""
    16	
    17	from __future__ import annotations
    18	
    19	from pathlib import Path
    20	
    21	from renmark import modularity
    22	from renmark.modularity import (
    23	    COGNITIVE_MAJOR,
    24	    COGNITIVE_WARN,
    25	    CYCLO_MAJOR,
    26	    CYCLO_WARN,
    27	    FANOUT_MAJOR,
    28	    FANOUT_WARN,
    29	    FUNC_LOC_MAJOR,
    30	    FUNC_LOC_WARN,
    31	    MODULE_LOC_MAJOR,
    32	    MODULE_LOC_WARN,
    33	    analyze,
    34	)
    35	
    36	
    37	# ── helpers ──────────────────────────────────────────────────────────────────
    38	
    39	
    40	def _write(repo: Path, rel: str, body: str) -> Path:
    41	    """Write ``body`` to ``repo/rel`` (creating parents) and return the path."""
    42	    p = repo / rel
    43	    p.parent.mkdir(parents=True, exist_ok=True)
    44	    p.write_text(body, encoding="utf-8")
    45	    return p
    46	
    47	
    48	def _gaps_for(repo: Path, *, title_contains: str) -> list:
    49	    """Gaps whose title contains ``title_contains`` (one metric's gaps)."""
    50	    return [g for g in analyze(repo) if title_contains in g.title]
    51	
    52	
    53	def _module_of_code_lines(n: int) -> str:
    54	    """A module whose code-line count is exactly ``n`` (one assignment per line).
    55	
    56	    No docstring, no blanks, no comments — every physical line is a code line.
    57	    """
    58	    return "\n".join(f"v{i} = {i}" for i in range(n)) + "\n"
    59	
    60	
    61	def _func_with_body_code_lines(n: int) -> str:
    62	    """A module with one function ``f`` whose BODY has exactly ``n`` code lines.
    63	
    64	    The ``def`` signature line is not counted by the analyzer (it spans the
    65	    function's first body statement onward), so the file is the signature plus
    66	    ``n`` body assignment statements.
    67	    """
    68	    body = "\n".join(f"    s{i} = {i}" for i in range(n))
    69	    return f"def f():\n{body}\n"
    70	
    71	
    72	def _func_with_flat_ifs(n: int) -> str:
    73	    """A function with ``n`` sibling (non-nested) ``if`` statements.
    74	
    75	    Cyclomatic = n + 1 (base 1 + one per ``if``). Cognitive = n (each flat
    76	    ``if`` adds ``1 + 0`` nesting). Each body is a single ``pass``.
    77	    """
    78	    lines = ["def f():"]
    79	    for i in range(n):
    80	        lines.append(f"    if v{i}:")
    81	        lines.append("        pass")
    82	    return "\n".join(lines) + "\n"
    83	
    84	
    85	def _func_with_nested_ifs(depth: int) -> str:
    86	    """A function with ``if`` statements nested ``depth`` levels deep.
    87	
    88	    Cyclomatic = depth + 1 (one decision point per ``if``, flat or nested).
    89	    Cognitive is nesting-weighted: level k (0-indexed) adds ``1 + k``, so total
    90	    cognitive = sum(1..depth) = depth*(depth+1)/2 — far above the raw branch
    91	    count. This is the lever that distinguishes cognitive from cyclomatic.
    92	    """
    93	    lines = ["def f():"]
    94	    for k in range(depth):
    95	        lines.append("    " * (k + 1) + f"if c{k}:")
    96	    lines.append("    " * (depth + 1) + "pass")
    97	    return "\n".join(lines) + "\n"
    98	
    99	
   100	def _module_with_imports(n: int) -> str:
   101	    """A module with ``n`` distinct import statements and nothing else of note."""
   102	    return "\n".join(f"import os as o{i}" for i in range(n)) + "\n"
   103	
   104	
   105	# ── Metric 1: module LOC ───────────────────────────────────────────────────────
   106	
   107	
   108	def test_module_loc_just_over_warn_is_warn(tmp_path: Path) -> None:
   109	    repo = tmp_path / "repo"
   110	    _write(repo, "src/big.py", _module_of_code_lines(MODULE_LOC_WARN + 1))
   111	    gaps = _gaps_for(repo, title_contains="Oversized module")
   112	    assert len(gaps) == 1
   113	    assert gaps[0].severity == "warn"
   114	
   115	
   116	def test_module_loc_just_over_major_is_danger(tmp_path: Path) -> None:
   117	    repo = tmp_path / "repo"
   118	    _write(repo, "src/huge.py", _module_of_code_lines(MODULE_LOC_MAJOR + 1))
   119	    gaps = _gaps_for(repo, title_contains="Oversized module")
   120	    assert len(gaps) == 1
   121	    assert gaps[0].severity == "danger"
   122	
   123	
   124	def test_module_loc_just_under_warn_is_clean(tmp_path: Path) -> None:
   125	    repo = tmp_path / "repo"
   126	    _write(repo, "src/ok.py", _module_of_code_lines(MODULE_LOC_WARN - 1))
   127	    assert _gaps_for(repo, title_contains="Oversized module") == []
   128	
   129	
   130	# ── Metric 2: function length ──────────────────────────────────────────────────
   131	
   132	
   133	def test_func_loc_just_over_warn_is_warn(tmp_path: Path) -> None:
   134	    repo = tmp_path / "repo"
   135	    _write(repo, "src/longfn.py", _func_with_body_code_lines(FUNC_LOC_WARN + 1))
   136	    gaps = _gaps_for(repo, title_contains="Long function")
   137	    assert len(gaps) == 1
   138	    assert gaps[0].severity == "warn"
   139	
   140	
   141	def test_func_loc_just_over_major_is_danger(tmp_path: Path) -> None:
   142	    repo = tmp_path / "repo"
   143	    _write(repo, "src/hugefn.py", _func_with_body_code_lines(FUNC_LOC_MAJOR + 1))
   144	    gaps = _gaps_for(repo, title_contains="Long function")
   145	    assert len(gaps) == 1
   146	    assert gaps[0].severity == "danger"
   147	
   148	
   149	def test_func_loc_just_under_warn_is_clean(tmp_path: Path) -> None:
   150	    repo = tmp_path / "repo"
   151	    _write(repo, "src/okfn.py", _func_with_body_code_lines(FUNC_LOC_WARN - 1))
   152	    assert _gaps_for(repo, title_contains="Long function") == []
   153	
   154	
   155	# ── Metric 3: cyclomatic complexity ────────────────────────────────────────────
   156	# cyclomatic = (#ifs) + 1, so #ifs == value - 1.
   157	
   158	
   159	def test_cyclomatic_just_over_warn_is_warn(tmp_path: Path) -> None:
   160	    repo = tmp_path / "repo"
   161	    # value = CYCLO_WARN + 1  ⇒  ifs = CYCLO_WARN
   162	    _write(repo, "src/cyclo_w.py", _func_with_flat_ifs(CYCLO_WARN))
   163	    gaps = _gaps_for(repo, title_contains="cyclomatic")
   164	    assert len(gaps) == 1
   165	    assert gaps[0].severity == "warn"
   166	
   167	
   168	def test_cyclomatic_just_over_major_is_danger(tmp_path: Path) -> None:
   169	    repo = tmp_path / "repo"
   170	    # value = CYCLO_MAJOR + 1  ⇒  ifs = CYCLO_MAJOR
   171	    _write(repo, "src/cyclo_m.py", _func_with_flat_ifs(CYCLO_MAJOR))
   172	    gaps = _gaps_for(repo, title_contains="cyclomatic")
   173	    assert len(gaps) == 1
   174	    assert gaps[0].severity == "danger"
   175	
   176	
   177	def test_cyclomatic_just_under_warn_is_clean(tmp_path: Path) -> None:
   178	    repo = tmp_path / "repo"
   179	    # value = CYCLO_WARN - 1  ⇒  ifs = CYCLO_WARN - 2
   180	    _write(repo, "src/cyclo_ok.py", _func_with_flat_ifs(CYCLO_WARN - 2))
   181	    assert _gaps_for(repo, title_contains="cyclomatic") == []
   182	
   183	
   184	# ── Metric 4: import fan-out ───────────────────────────────────────────────────
   185	
   186	
   187	def test_fanout_just_over_warn_is_warn(tmp_path: Path) -> None:
   188	    repo = tmp_path / "repo"
   189	    _write(repo, "src/coupled_w.py", _module_with_imports(FANOUT_WARN + 1))
   190	    gaps = _gaps_for(repo, title_contains="import fan-out")
   191	    assert len(gaps) == 1
   192	    assert gaps[0].severity == "warn"
   193	
   194	
   195	def test_fanout_just_over_major_is_danger(tmp_path: Path) -> None:
   196	    repo = tmp_path / "repo"
   197	    _write(repo, "src/coupled_m.py", _module_with_imports(FANOUT_MAJOR + 1))
   198	    gaps = _gaps_for(repo, title_contains="import fan-out")
   199	    assert len(gaps) == 1
   200	    assert gaps[0].severity == "danger"
   201	
   202	
   203	def test_fanout_just_under_warn_is_clean(tmp_path: Path) -> None:
   204	    repo = tmp_path / "repo"
   205	    _write(repo, "src/coupled_ok.py", _module_with_imports(FANOUT_WARN - 1))
   206	    assert _gaps_for(repo, title_contains="import fan-out") == []
   207	
   208	
   209	# ── Metric 5: cognitive complexity ─────────────────────────────────────────────
   210	# flat ifs: cognitive == #ifs (each adds 1 + 0). So #ifs == value.
   211	
   212	
   213	def test_cognitive_just_over_warn_is_warn(tmp_path: Path) -> None:
   214	    repo = tmp_path / "repo"
   215	    _write(repo, "src/cog_w.py", _func_with_flat_ifs(COGNITIVE_WARN + 1))
   216	    gaps = _gaps_for(repo, title_contains="cognitive")
   217	    assert len(gaps) == 1
   218	    assert gaps[0].severity == "warn"
   219	
   220	
   221	def test_cognitive_just_over_major_is_danger(tmp_path: Path) -> None:
   222	    repo = tmp_path / "repo"
   223	    _write(repo, "src/cog_m.py", _func_with_flat_ifs(COGNITIVE_MAJOR + 1))
   224	    gaps = _gaps_for(repo, title_contains="cognitive")
   225	    assert len(gaps) == 1
   226	    assert gaps[0].severity == "danger"
   227	
   228	
   229	def test_cognitive_just_under_warn_is_clean(tmp_path: Path) -> None:
   230	    repo = tmp_path / "repo"
   231	    # COGNITIVE_WARN - 1 flat ifs.  cyclomatic = (WARN-1)+1 = WARN, which is
   232	    # itself at the cyclomatic-warn boundary, but here we only assert there is
   233	    # no COGNITIVE gap. (COGNITIVE_WARN=15 > CYCLO_WARN=10, so a cyclo gap may
   234	    # legitimately appear — we scope the assertion to the cognitive metric.)
   235	    _write(repo, "src/cog_ok.py", _func_with_flat_ifs(COGNITIVE_WARN - 1))
   236	    assert _gaps_for(repo, title_contains="cognitive") == []
   237	
   238	
   239	# ── Cognitive vs cyclomatic: nesting is weighted ───────────────────────────────
   240	
   241	
   242	def test_nesting_trips_cognitive_below_flat_branch_count(tmp_path: Path) -> None:
   243	    """A deeply-nested function trips cognitive at a lower raw-branch count.
   244	
   245	    With ``depth`` nested ``if``s the raw branch count (cyclomatic) is only
   246	    ``depth + 1``, but cognitive is ``depth*(depth+1)/2``. We pick the smallest
   247	    depth whose nesting-weighted score clears COGNITIVE_WARN while its raw
   248	    branch count stays at or below CYCLO_WARN — proving cognitive penalizes
   249	    nesting that cyclomatic ignores.
   250	    """
   251	    # smallest depth with depth*(depth+1)/2 > COGNITIVE_WARN
   252	    depth = 1
   253	    while depth * (depth + 1) // 2 <= COGNITIVE_WARN:
   254	        depth += 1
   255	    raw_branches = depth + 1  # cyclomatic value for nested ifs
   256	
   257	    repo = tmp_path / "repo"
   258	    _write(repo, "src/nested.py", _func_with_nested_ifs(depth))
   259	
   260	    cog = _gaps_for(repo, title_contains="cognitive")

exec
/bin/bash -lc "python - <<'PY'
import ast
from renmark.modularity import _module_code_lines, _func_code_lines, _import_fan_out

src = '''\"\"\"module doc\\nmore\\n\"\"\"\\n\\n# comment\\nclass C:\\n    \"\"\"class doc\"\"\"\\n    x = 1\\n\\n    def f(self):\\n        \"\"\"fn doc\\n        more\\n        \"\"\"\\n        # c\\n        y = 1\\n        def g():\\n            z = 2\\n        return y\\n'''
mod = ast.parse(src)
cls = mod.body[0]
func = cls.body[1]
print('module_loc', _module_code_lines(src, mod))
print('func_loc', _func_code_lines(func, src))

src2 = 'import os\\n\\ndef f():\\n    import sys\\n    from x import y\\n    return 1\\n'
mod2 = ast.parse(src2)
print('fanout', _import_fan_out(mod2))
PY" in /home/renmark/projects/ai-system
 exited 1 in 0ms:
Traceback (most recent call last):
  File "<stdin>", line 7, in <module>
AttributeError: 'Expr' object has no attribute 'body'

exec
/bin/bash -lc "bash -lc 'nl -ba /home/renmark/projects/ai-system/tests/test_modularity.py | sed -n \"261,360p\"'" in /home/renmark/projects/ai-system
 succeeded in 0ms:
   261	    assert len(cog) == 1, "deep nesting must trip cognitive complexity"
   262	
   263	    # Sanity: the SAME raw branch count laid out FLAT must NOT trip cognitive,
   264	    # because flat ifs add only 1 each (no nesting weight). raw_branches flat
   265	    # ifs ⇒ cognitive == raw_branches; this stays under COGNITIVE_WARN.
   266	    assert raw_branches <= COGNITIVE_WARN, (
   267	        "test fixture invalid: flat equivalent would itself exceed cognitive warn"
   268	    )
   269	    repo_flat = tmp_path / "repo_flat"
   270	    _write(repo_flat, "src/flat.py", _func_with_flat_ifs(raw_branches))
   271	    assert _gaps_for(repo_flat, title_contains="cognitive") == [], (
   272	        "the same branch count laid out flat must NOT trip cognitive — "
   273	        "this is the proof that nesting is weighted"
   274	    )
   275	
   276	
   277	# ── False-positive suppression ─────────────────────────────────────────────────
   278	
   279	
   280	def test_oversized_file_under_tests_path_not_flagged(tmp_path: Path) -> None:
   281	    """A huge file under a ``tests/`` tree is suppressed entirely."""
   282	    repo = tmp_path / "repo"
   283	    # Way over the major module threshold, but under tests/ → no gaps at all.
   284	    _write(repo, "tests/test_huge.py", _module_of_code_lines(MODULE_LOC_MAJOR + 50))
   285	    assert analyze(repo) == []
   286	
   287	
   288	def test_init_py_excluded_from_fanout(tmp_path: Path) -> None:
   289	    """An ``__init__.py`` with > FANOUT_MAJOR imports is NOT flagged for fan-out.
   290	
   291	    Re-export hubs legitimately import a lot. The module is still otherwise
   292	    valid; we assert specifically that no fan-out gap is emitted.
   293	    """
   294	    repo = tmp_path / "repo"
   295	    _write(repo, "src/__init__.py", _module_with_imports(FANOUT_MAJOR + 5))
   296	    assert _gaps_for(repo, title_contains="import fan-out") == []
   297	
   298	
   299	# ── Never-raise contract ───────────────────────────────────────────────────────
   300	
   301	
   302	def test_syntax_error_file_skipped_valid_files_still_analyzed(tmp_path: Path) -> None:
   303	    """A SyntaxError file is skipped; analyze() does not raise and still flags valid files."""
   304	    repo = tmp_path / "repo"
   305	    # Unparseable file in the tree.
   306	    _write(repo, "src/broken.py", "def f(:\n    this is not python\n")
   307	    # A valid file that SHOULD be flagged (over module-LOC major).
   308	    _write(repo, "src/valid_huge.py", _module_of_code_lines(MODULE_LOC_MAJOR + 1))
   309	
   310	    gaps = analyze(repo)  # must not raise
   311	    oversized = [g for g in gaps if "Oversized module" in g.title]
   312	    assert len(oversized) == 1
   313	    assert oversized[0].severity == "danger"
   314	    # And the breach is attributed to the valid file, not the broken one.
   315	    assert "valid_huge.py" in oversized[0].title
   316	
   317	
   318	def test_empty_repo_returns_no_gaps(tmp_path: Path) -> None:
   319	    repo = tmp_path / "repo"
   320	    repo.mkdir()
   321	    assert analyze(repo) == []
   322	
   323	
   324	def test_analyze_accepts_str_path(tmp_path: Path) -> None:
   325	    """analyze() accepts a str path as well as a Path."""
   326	    repo = tmp_path / "repo"
   327	    _write(repo, "src/big.py", _module_of_code_lines(MODULE_LOC_WARN + 1))
   328	    gaps = analyze(str(repo))
   329	    assert any("Oversized module" in g.title for g in gaps)

exec
/bin/bash -lc "python - <<'PY'
import ast
from renmark.modularity import _cyclomatic, _cognitive
src='''def f(x):
    match x:
        case 1:
            pass
        case 2 if guard:
            pass
        case _:
            pass
'''
func = ast.parse(src).body[0]
print('cyclo', _cyclomatic(func))
print('cognitive', _cognitive(func))
PY" in /home/renmark/projects/ai-system
 succeeded in 0ms:
cyclo 4
cognitive 3

exec
/bin/bash -lc "python - <<'PY'
import ast
from renmark.modularity import _func_code_lines
src='''def outer():
    x = 1
    def inner():
        a = 1
        b = 2
    y = 2
'''
mod=ast.parse(src)
outer=mod.body[0]
inner=outer.body[1]
print('outer', _func_code_lines(outer, src))
print('inner', _func_code_lines(inner, src))
PY" in /home/renmark/projects/ai-system
 succeeded in 0ms:
outer 5
inner 2

exec
/bin/bash -lc "python - <<'PY'
import ast
from renmark.modularity import _import_fan_out
src='''import os

def f():
    import sys
    if cond:
        from x import y
    return 1
'''
mod=ast.parse(src)
print(_import_fan_out(mod))
PY" in /home/renmark/projects/ai-system
 succeeded in 115ms:
exec
/bin/bash -lc "python - <<'PY'
import ast
src='''\"\"\"module doc
more
\"\"\"

# comment
class C:
    \"\"\"class doc\"\"\"
    x = 1

    def f(self):
        \"\"\"fn doc
        more
        \"\"\"
        # c
        y = 1
        def g():
            z = 2
        return y
'''
mod=ast.parse(src)
print([type(n).__name__ for n in mod.body])
cls=mod.body[1]
func=cls.body[1]
from renmark.modularity import _module_code_lines, _func_code_lines
print('module_loc', _module_code_lines(src, mod))
print('func_loc', _func_code_lines(func, src))
PY" in /home/renmark/projects/ai-system
 exited 1 in 0ms:
['Expr', 'ClassDef']
module_loc 7
Traceback (most recent call last):
  File "<stdin>", line 27, in <module>
  File "/home/renmark/projects/ai-system/renmark/modularity.py", line 292, in _func_code_lines
    if not func.body:
           ^^^^^^^^^
AttributeError: 'Assign' object has no attribute 'body'

codex
The remaining question is whether the boundary and suppression claims are actually tested. I’m finishing with the test file now; several of the advertised edge cases aren’t exercised.
exec
/bin/bash -lc "python - <<'PY'
import ast
from renmark.modularity import _module_code_lines, _func_code_lines
src='''\"\"\"module doc
more
\"\"\"

# comment
class C:
    \"\"\"class doc\"\"\"
    x = 1

    def f(self):
        \"\"\"fn doc
        more
        \"\"\"
        # c
        y = 1
        def g():
            z = 2
        return y
'''
mod=ast.parse(src)
cls=mod.body[1]
print([type(n).__name__ for n in cls.body])
func=cls.body[2]
print('module_loc', _module_code_lines(src, mod))
print('func_loc', _func_code_lines(func, src))
PY" in /home/renmark/projects/ai-system
 succeeded in 0ms:
['Expr', 'Assign', 'FunctionDef']
module_loc 7
func_loc 4

exec
/bin/bash -lc "bash -lc 'rg -n \"fan-out|generated|nested|cognitive|cyclomatic|function length|double-count|match|BoolOp|comprehension\" /home/renmark/projects/ai-system/.renmark/specs/2026-06-08-modularity-health-lens.spec.md /home/renmark/projects/ai-system/.renmark/plans/2026-06-08-modularity-health-lens.plan.md /home/renmark/projects/ai-system/.renmark/research/2026-06-08-modularity-health-lens.research.md'" in /home/renmark/projects/ai-system
 succeeded in 0ms:
/home/renmark/projects/ai-system/.renmark/plans/2026-06-08-modularity-health-lens.plan.md:8:zero-LLM, no new deps. Task 1 is correctness-critical (ast + cognitive complexity) →
/home/renmark/projects/ai-system/.renmark/plans/2026-06-08-modularity-health-lens.plan.md:27:  `HEALTH:` line so output matches) and renmark/sizing.py (mirror its tunable-
/home/renmark/projects/ai-system/.renmark/plans/2026-06-08-modularity-health-lens.plan.md:32:  - cyclomatic branch count per function (count if/for/while/and/or/except/case/
/home/renmark/projects/ai-system/.renmark/plans/2026-06-08-modularity-health-lens.plan.md:33:    comprehension-if ast nodes): 10 / 20
/home/renmark/projects/ai-system/.renmark/plans/2026-06-08-modularity-health-lens.plan.md:34:  - import fan-out per module (count import statements): 15 / 25
/home/renmark/projects/ai-system/.renmark/plans/2026-06-08-modularity-health-lens.plan.md:35:  - cognitive complexity per function (nesting-WEIGHTED: +1 per branch, +nesting
/home/renmark/projects/ai-system/.renmark/plans/2026-06-08-modularity-health-lens.plan.md:36:    level for nested branches): 15 / 30
/home/renmark/projects/ai-system/.renmark/plans/2026-06-08-modularity-health-lens.plan.md:37:  FALSE-POSITIVE SUPPRESSION (mandatory): skip files under `tests/`, generated
/home/renmark/projects/ai-system/.renmark/plans/2026-06-08-modularity-health-lens.plan.md:38:  markers, and `__init__.py` for the fan-out metric; don't count dataclass/Enum
/home/renmark/projects/ai-system/.renmark/plans/2026-06-08-modularity-health-lens.plan.md:82:  function with > branch-count branches; a module with > fan-out imports; a deeply-
/home/renmark/projects/ai-system/.renmark/plans/2026-06-08-modularity-health-lens.plan.md:83:  nested function over cognitive threshold) → a Gap is reported with the right
/home/renmark/projects/ai-system/.renmark/research/2026-06-08-modularity-health-lens.research.md:21:MODULARITY & SCALABILITY health (God-objects, long functions, high fan-out,
/home/renmark/projects/ai-system/.renmark/research/2026-06-08-modularity-health-lens.research.md:38:| **Nesting depth** | arrow-code | pylint `too-many-nested-blocks` | low |
/home/renmark/projects/ai-system/.renmark/research/2026-06-08-modularity-health-lens.research.md:39:| **Import fan-out (Ce / efferent coupling)** | a module that depends on everything | import-linter, pydeps, madge (JS), SonarQube | low — pure count |
/home/renmark/projects/ai-system/.renmark/research/2026-06-08-modularity-health-lens.research.md:46:length, cyclomatic-ish branch count, module LOC, and import fan-out** — all
/home/renmark/projects/ai-system/.renmark/research/2026-06-08-modularity-health-lens.research.md:66:| pylint `max-nested-blocks` | **5** | pylint |
/home/renmark/projects/ai-system/.renmark/research/2026-06-08-modularity-health-lens.research.md:72:| SonarQube cognitive complexity (per function) | **15** | SonarSource S3776 |
/home/renmark/projects/ai-system/.renmark/research/2026-06-08-modularity-health-lens.research.md:73:| "long function" rule-of-thumb | ~50 LOC (matches pylint max-statements) | industry / SonarQube |
/home/renmark/projects/ai-system/.renmark/research/2026-06-08-modularity-health-lens.research.md:77:cognitive-complexity rule (sonarsource.com); flake8/mccabe docs; pydeps /
/home/renmark/projects/ai-system/.renmark/research/2026-06-08-modularity-health-lens.research.md:81:- **CodeRabbit / Sourcery** — flag long functions, high cyclomatic/cognitive
/home/renmark/projects/ai-system/.renmark/research/2026-06-08-modularity-health-lens.research.md:91:3. **`__init__.py`** — re-export hubs have huge import fan-out by design — exempt from fan-out.
/home/renmark/projects/ai-system/.renmark/research/2026-06-08-modularity-health-lens.research.md:94:6. **Match statements / big dispatch dicts** — inflate branch count but are flat & readable; cognitive complexity (nesting-weighted) is fairer than raw CC here.
/home/renmark/projects/ai-system/.renmark/research/2026-06-08-modularity-health-lens.research.md:102:- function length (lineno → end_lineno, py3.8+)
/home/renmark/projects/ai-system/.renmark/research/2026-06-08-modularity-health-lens.research.md:105:- **cyclomatic-ish** = 1 + count of `If/For/While/ExceptHandler/With/BoolOp(extra and/or)/comprehension-if/IfExp/match-case/assert` nodes in a fn
/home/renmark/projects/ai-system/.renmark/research/2026-06-08-modularity-health-lens.research.md:106:- **cognitive complexity** ≈ branch nodes weighted by `ast` nesting depth (track depth in a NodeVisitor) — the Sonar model is reproducible from `ast`
/home/renmark/projects/ai-system/.renmark/research/2026-06-08-modularity-health-lens.research.md:107:- **nesting depth** = max depth of nested If/For/While/With/Try via NodeVisitor
/home/renmark/projects/ai-system/.renmark/research/2026-06-08-modularity-health-lens.research.md:108:- **import fan-out** = count distinct `ast.Import`/`ast.ImportFrom` targets per file
/home/renmark/projects/ai-system/.renmark/research/2026-06-08-modularity-health-lens.research.md:149:  There is NO existing per-function length / param / branch / nesting / fan-out
/home/renmark/projects/ai-system/.renmark/research/2026-06-08-modularity-health-lens.research.md:152:  computing fn length, CC, cognitive complexity, nesting, fan-out, class size)
/home/renmark/projects/ai-system/.renmark/research/2026-06-08-modularity-health-lens.research.md:155:- **False-positive suppression** (test/generated/`__init__`/dataclass/data-module skips) — new.
/home/renmark/projects/ai-system/.renmark/research/2026-06-08-modularity-health-lens.research.md:167:1. function length (warn >50 LOC, danger >~100)
/home/renmark/projects/ai-system/.renmark/research/2026-06-08-modularity-health-lens.research.md:168:2. cyclomatic-ish branch count per fn (warn >10, danger >20 — mccabe/radon bands)
/home/renmark/projects/ai-system/.renmark/research/2026-06-08-modularity-health-lens.research.md:170:4. import fan-out per file (warn band; `__init__.py` exempt)
/home/renmark/projects/ai-system/.renmark/research/2026-06-08-modularity-health-lens.research.md:171:Plus cognitive complexity (nesting-weighted, Sonar=15) as the 5th if budget allows —
/home/renmark/projects/ai-system/.renmark/research/2026-06-08-modularity-health-lens.research.md:183:- Ship 4-5 pure-ast metrics: fn length (>50/100), cyclomatic branch count (>10/20), module LOC (>500/1000), import fan-out (+cognitive complexity, Sonar=15); zero-dep, deterministic.
/home/renmark/projects/ai-system/.renmark/research/2026-06-08-modularity-health-lens.research.md:184:- Mirror these defaults: pylint max-statements=50, max-branches=12, max-args=5, max-attributes=7, max-module-lines=1000; mccabe=10; radon CC bands; Sonar cognitive=15.
/home/renmark/projects/ai-system/.renmark/research/2026-06-08-modularity-health-lens.research.md:185:- FP suppression mandatory: skip tests, generated code, __init__.py fan-out, dataclass/Enum field counts, big data/config modules; count code lines not raw.
/home/renmark/projects/ai-system/.renmark/specs/2026-06-08-modularity-health-lens.spec.md:39:3. Low false-positive: tests/generated/`__init__` and data/config suppressed;
/home/renmark/projects/ai-system/.renmark/specs/2026-06-08-modularity-health-lens.spec.md:49:- Cross-file maintainability-index / true cognitive-complexity parity — ship a
/home/renmark/projects/ai-system/.renmark/specs/2026-06-08-modularity-health-lens.spec.md:64:  | function length (LOC) | 50 | 100 | common |
/home/renmark/projects/ai-system/.renmark/specs/2026-06-08-modularity-health-lens.spec.md:65:  | cyclomatic branch count / fn | 10 | 20 | mccabe |
/home/renmark/projects/ai-system/.renmark/specs/2026-06-08-modularity-health-lens.spec.md:66:  | import fan-out / module | 15 | 25 | tunable |
/home/renmark/projects/ai-system/.renmark/specs/2026-06-08-modularity-health-lens.spec.md:67:  | cognitive complexity / fn (nesting-weighted) | 15 | 30 | SonarQube |
/home/renmark/projects/ai-system/.renmark/specs/2026-06-08-modularity-health-lens.spec.md:69:- **False-positive suppression:** skip `tests/`, generated markers, `__init__.py`
/home/renmark/projects/ai-system/.renmark/specs/2026-06-08-modularity-health-lens.spec.md:70:  for the fan-out metric, dataclass/Enum field-count effects; count code lines
/home/renmark/projects/ai-system/.renmark/specs/2026-06-08-modularity-health-lens.spec.md:100:- A file legitimately above threshold (e.g. a big generated/data module) →
/home/renmark/projects/ai-system/.renmark/specs/2026-06-08-modularity-health-lens.spec.md:102:- `__init__.py` re-export fan-out → excluded from the fan-out metric.
/home/renmark/projects/ai-system/.renmark/specs/2026-06-08-modularity-health-lens.spec.md:110:  keep tests/generated/`__init__` out of the noise.

exec
/bin/bash -lc "bash -lc 'nl -ba /home/renmark/projects/ai-system/.renmark/specs/2026-06-08-modularity-health-lens.spec.md | sed -n \"1,240p\"'" in /home/renmark/projects/ai-system
 succeeded in 0ms:
     1	---
     2	artifact_type: spec
     3	schema_version: 1
     4	created_at: 2026-06-08
     5	source_sha: TBD
     6	generator: brainstorm
     7	related_plan: TBD
     8	dependency_refs:
     9	  - .renmark/research/2026-06-08-modularity-health-lens.research.md
    10	  - PRD.md
    11	  - renmark/init.py        # existing standards-health pipeline (reuse)
    12	  - renmark/sizing.py      # constants + never-raise style to mirror
    13	status: draft
    14	---
    15	
    16	# Spec — modularity-health-lens (advisory code-health gaps)
    17	
    18	## Context
    19	
    20	renmark enforces modularity at **plan time** (one-file-per-task, no mode C) but
    21	never **measures** it on the shipped codebase — `init`'s standards-health flags
    22	missing linter/types/tests/CI, never oversized files / god-objects / coupling.
    23	This adds an ADVISORY, deterministic, zero-LLM, **stdlib-`ast`-only** lens that
    24	surfaces those gaps through the existing standards-health mechanism.
    25	
    26	Research (see artifact): mirror established defaults (pylint, mccabe, radon,
    27	SonarQube); suppress known false-positives; the parsing layer is **net-new**
    28	(`init` uses regex, `sizing` only reads `git --stat`) but everything downstream —
    29	the `Gap` dataclass, `evaluate_health`, `dev-standards.md` render, the `HEALTH:`
    30	stdout line — is REUSED. PRD alignment: **aligned** (same advisory-gap pattern
    31	`init` ships; REQ-7 quality; no new dep; never auto-refactors).
    32	
    33	## Goals
    34	
    35	1. `python -m renmark.init` surfaces modularity/scalability health gaps alongside
    36	   the existing standards-health gaps — **advisory, never blocking, never
    37	   auto-refactors**.
    38	2. Deterministic + zero-LLM + **no new runtime deps** (pure `ast` + stdlib).
    39	3. Low false-positive: tests/generated/`__init__` and data/config suppressed;
    40	   count code lines, not raw.
    41	4. Tunable thresholds as documented constants; never raises (degrade to "no gaps
    42	   reported" on any parse failure — same safety posture as `sizing.py`).
    43	
    44	## Non-goals (feature-scoped)
    45	
    46	- Blocking the pipeline / auto-refactoring / rewriting code.
    47	- A separate `/renmark:hygiene` surface (rides `init` standards-health only).
    48	- Cross-tool deps (radon/wily/pylint) — implement a stdlib `ast` subset.
    49	- Cross-file maintainability-index / true cognitive-complexity parity — ship a
    50	  faithful `ast`-feasible approximation.
    51	
    52	## Architecture
    53	
    54	**New `renmark/modularity.py`** — pure-`ast`, zero-dep, never-raise analyzer
    55	(mirror `sizing.py`: module constants, defensive, returns data not prose):
    56	- Walks Python source files (reuse `init`'s source-file walker / exclusion set if
    57	  present; else stdlib `pathlib` with the same excludes).
    58	- Per file / per function computes the **5 metrics**, each with two bands
    59	  (warn / major) from tunable constants:
    60	
    61	  | Metric | warn | major | source |
    62	  |---|---|---|---|
    63	  | module LOC (code lines) | 500 | 1000 | pylint `max-module-lines` |
    64	  | function length (LOC) | 50 | 100 | common |
    65	  | cyclomatic branch count / fn | 10 | 20 | mccabe |
    66	  | import fan-out / module | 15 | 25 | tunable |
    67	  | cognitive complexity / fn (nesting-weighted) | 15 | 30 | SonarQube |
    68	
    69	- **False-positive suppression:** skip `tests/`, generated markers, `__init__.py`
    70	  for the fan-out metric, dataclass/Enum field-count effects; count code lines
    71	  (exclude blanks/comments/docstrings).
    72	- Returns a list of findings in the shape `init`'s standards-health expects (a
    73	  `Gap` — reuse `init`'s `Gap` dataclass; do NOT invent a parallel shape).
    74	- `analyze(repo) -> list[Gap]` (or equivalent) — never raises; on any per-file
    75	  parse error, skip that file and continue.
    76	
    77	**`renmark/init.py` wiring** — call `modularity.analyze(repo)` inside the existing
    78	`evaluate_health` flow; merge its `Gap`s into the standards-health gap list so
    79	they render in `dev-standards.md` and count toward the `HEALTH:` stdout line.
    80	Additive only — do not alter existing standards-health detectors.
    81	
    82	**`init/SKILL.md`** — one line noting init's health report now includes advisory
    83	modularity/scalability gaps (oversized files, long/complex functions, coupling).
    84	
    85	## Data flow
    86	
    87	```
    88	python -m renmark.init
    89	  → evaluate_health(repo):
    90	      existing detectors (linter/types/tests/CI...) → Gaps
    91	      + modularity.analyze(repo) → Gaps   (5 ast metrics, FP-suppressed, never-raise)
    92	  → write_standards_md (renders all Gaps) + "HEALTH: <n gaps>" stdout line
    93	  (advisory only — init still exits 0; nothing blocks)
    94	```
    95	
    96	## Error handling / edge cases
    97	
    98	- Unparseable / syntax-error file → skipped (never raises); analysis continues.
    99	- Non-Python repo / no source files → zero modularity gaps (clean), no error.
   100	- A file legitimately above threshold (e.g. a big generated/data module) →
   101	  suppressed by the exclusion rules; document how to exclude.
   102	- `__init__.py` re-export fan-out → excluded from the fan-out metric.
   103	- Thresholds tunable via constants; advisory bands never escalate to a hard fail.
   104	
   105	## Success criteria
   106	
   107	- `python -m renmark.init` on this repo reports modularity gaps (if any) in the
   108	  `HEALTH:` line + `dev-standards.md`, and still exits 0 (advisory).
   109	- All 5 metrics computed via `ast` with no third-party import; suppression rules
   110	  keep tests/generated/`__init__` out of the noise.
   111	- `modularity.analyze` is pure + never raises (parse-error file skipped) —
   112	  unit-tested at each metric's warn/major boundary + the suppression cases.
   113	- `pytest -q`, `ruff`, `mypy`, `lint_all` green; no new dependency in pyproject.
   114	
   115	## Prior art & references
   116	
   117	- Research artifact: `.renmark/research/2026-06-08-modularity-health-lens.research.md`
   118	  (external metric/threshold table + sources; FP list; stdlib-`ast` feasibility;
   119	  internal reuse map).
   120	- Reuse: `renmark/init.py` (`Gap`, `evaluate_health`, `write_standards_md`,
   121	  `HEALTH:` line, source-file walker); `renmark/sizing.py` (tunable-constants +
   122	  never-raise style). Build new: the `ast` metric walker.

codex
Summary: Critical 0, Major 3, Minor 2, Nit 0

- `renmark/modularity.py:285` — Major — `_func_code_lines()` measures from the first body statement to `end_lineno`, so it undercounts every function by dropping decorator/signature lines and also double-counts nested `def` bodies inside the parent function’s LOC.  
  Fix: Measure from the function/decorator start (`func.lineno` / first decorator) and exclude docstring/comment/blank lines plus spans owned by nested functions/classes so each function owns exactly its own LOC.

- `renmark/modularity.py:395` — Major — cognitive complexity undercounts boolean chains because every `BoolOp` adds a flat `1`, so `a and b and c` scores the same as `a and b` even though the plan/spec define cognitive as `+1 per branch` with nesting weight.  
  Fix: Increment by `len(child.values) - 1` for `BoolOp` sequences (and keep the existing nesting treatment around the enclosing construct).

- `renmark/modularity.py:82` — Major — generated-file suppression is far too broad: unanchored substrings like `generated by` and `do not edit` anywhere in the first 40 lines will silently suppress normal hand-written modules whose comments/docstrings merely mention those phrases.  
  Fix: Restrict suppression to explicit header markers on comment-only lines (or a much tighter allowlist of sentinel strings) and add negative tests for prose/docstring mentions.

- `renmark/modularity.py:78` — Minor — test-tree suppression skips any parent directory named `test`, not just `tests/`, so legitimate production code under paths like `src/test/...` is dropped from analysis despite the stated contract being “skip `tests/`”.  
  Fix: Narrow the matcher to the intended directory names/patterns (`tests`, maybe `__tests__`) or make it root/pattern based instead of blanket component matching.

- `tests/test_modularity.py:7` — Minor — the test suite claims boundary/suppression/never-raise coverage, but it never exercises exact `== warn/major` boundaries, generated-marker suppression, read/walk failures, or the branch-math cases called out in the design (`BoolOp`, comprehensions, `match/case`, `try/except`).  
  Fix: Add focused tests for equality boundaries, generated false positives/true positives, monkeypatched read/walk failures, and direct metric fixtures for each advertised AST construct.

`renmark/init.py` looked acceptable: the `HEALTH:` stdout line stays bounded to summary counts, the modularity section is capped, and modularity remains advisory in normal operation (`run()` still returns 0 absent pre-existing init failures). I couldn’t execute `pytest` end-to-end here because the sandbox has no writable temp directory, so the test assessment above is from source inspection plus in-memory AST probes.
tokens used
61,433
Summary: Critical 0, Major 3, Minor 2, Nit 0

- `renmark/modularity.py:285` — Major — `_func_code_lines()` measures from the first body statement to `end_lineno`, so it undercounts every function by dropping decorator/signature lines and also double-counts nested `def` bodies inside the parent function’s LOC.  
  Fix: Measure from the function/decorator start (`func.lineno` / first decorator) and exclude docstring/comment/blank lines plus spans owned by nested functions/classes so each function owns exactly its own LOC.

- `renmark/modularity.py:395` — Major — cognitive complexity undercounts boolean chains because every `BoolOp` adds a flat `1`, so `a and b and c` scores the same as `a and b` even though the plan/spec define cognitive as `+1 per branch` with nesting weight.  
  Fix: Increment by `len(child.values) - 1` for `BoolOp` sequences (and keep the existing nesting treatment around the enclosing construct).

- `renmark/modularity.py:82` — Major — generated-file suppression is far too broad: unanchored substrings like `generated by` and `do not edit` anywhere in the first 40 lines will silently suppress normal hand-written modules whose comments/docstrings merely mention those phrases.  
  Fix: Restrict suppression to explicit header markers on comment-only lines (or a much tighter allowlist of sentinel strings) and add negative tests for prose/docstring mentions.

- `renmark/modularity.py:78` — Minor — test-tree suppression skips any parent directory named `test`, not just `tests/`, so legitimate production code under paths like `src/test/...` is dropped from analysis despite the stated contract being “skip `tests/`”.  
  Fix: Narrow the matcher to the intended directory names/patterns (`tests`, maybe `__tests__`) or make it root/pattern based instead of blanket component matching.

- `tests/test_modularity.py:7` — Minor — the test suite claims boundary/suppression/never-raise coverage, but it never exercises exact `== warn/major` boundaries, generated-marker suppression, read/walk failures, or the branch-math cases called out in the design (`BoolOp`, comprehensions, `match/case`, `try/except`).  
  Fix: Add focused tests for equality boundaries, generated false positives/true positives, monkeypatched read/walk failures, and direct metric fixtures for each advertised AST construct.

`renmark/init.py` looked acceptable: the `HEALTH:` stdout line stays bounded to summary counts, the modularity section is capped, and modularity remains advisory in normal operation (`run()` still returns 0 absent pre-existing init failures). I couldn’t execute `pytest` end-to-end here because the sandbox has no writable temp directory, so the test assessment above is from source inspection plus in-memory AST probes.
