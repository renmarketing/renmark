---
artifact_type: review
schema_version: 1
created_at: 2026-06-17T02:54:19+00:00
source_sha: e6c015039b6e32dbb6ada6014cf03cc929575f02
related_plan: .renmark/plans/2026-06-16-finish-branch-disposition.plan.md
generator: codereview
stale_after: null
dependency_refs: []
completion_state: complete
confidence: high
validation_status: validated
retry_count: 0
parser_success: true
schema_compliance: true
---

# Code review — finish-branch-disposition (codex, focus: default)

Range: main..HEAD · tier: standard · engine: codex exec --sandbox read-only

## Summary: Critical 0 | Major 3 | Minor 1 | Nit 0

### Major
1. **renmark/analytics.py:287** — `close_feature_disposition()` fallback matches by `feature` alone; the feature slug is reusable (not a unique run id), so a feature-only fallback could close the wrong / multiple non-terminal rows.
   - Disposition: **FIX** — narrow on `branch` (stable across a merge, unlike sha; unique per run). Callers pass feature+branch.
2. **renmark/analytics.py:286** — read-modify-write ledger rewrite has no lock/CAS; a concurrent `record_feature_run` append or close-out between `read_jsonl` and `os.replace` could be dropped.
   - Disposition: **DEFER** — pre-existing, intentional property of the whole analytics module (`_append` + `write_summary` are also lock-free); analytics is observational/non-load-bearing; finish/merge/release run single-writer-serial. Locking only here = inconsistent re-architecture, out of scope.
3. **plugin/skills/finish/SKILL.md:234** — merge-path inline snippet uses `repo` without defining it or importing `Path`; run standalone it raises NameError, swallowed by the broad except → silent failure to close (reintroduces the bug).
   - Disposition: **FIX** — make the snippet self-contained (`from pathlib import Path; repo = Path('.')`), matching the release block.

### Minor
4. **renmark/analytics.py:308** — no `flush()`/`fsync()` before `os.replace`; a crash could lose ledger contents though the rename is atomic.
   - Disposition: **DEFER** — sibling `write_summary` (analytics.py:588-601) also omits fsync; matching the established pattern; observational ledger, durability-across-power-loss not required.

## Summary

- Critical 0 | Major 3 | Minor 1 | Nit 0
- focus: default (codex read-only)
- FIX: #1 feature-slug over-close, #3 SKILL repo NameError
- DEFER: #2 lock-free RMW (module-wide design), #4 fsync (sibling-consistent)
- next: apply fixes #1 + #3
