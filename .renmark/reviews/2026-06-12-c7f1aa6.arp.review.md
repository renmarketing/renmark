---
artifact_type: review
schema_version: 1
created_at: 2026-06-12T17:41:01+00:00
source_sha: c7f1aa64dfea195cad2d27a31059bd1ce1613336
related_plan: .renmark/plans/2026-06-12-agent-routing-policy.plan.md
generator: codex
stale_after: null
dependency_refs: []
completion_state: complete
confidence: medium
validation_status: validated
retry_count: 0
parser_success: true
schema_compliance: true
---

Summary: Critical 0 | Major 8 | Minor 2 | Nit 0

No broken frontmatter or Markdown structure stood out in the touched files.

- Major — [plugin/skills/_shared/reasoning-contract.md](/home/renmark/projects/ai-system/plugin/skills/_shared/reasoning-contract.md:3): The file claims the contract is “shared by every skill that dispatches subagents” and that skills “cite the blockquote below,” but uncited subagent lanes still exist in `codereview`, `audit`, and brainstorm’s Step 3 research dispatch, so the rollout is not actually universal. Fix: either narrow this claim to the surfaces updated in this wave or update every remaining subagent-dispatch section to carry the exact shared citation.

- Major — [.renmark/memory/routing.md](/home/renmark/projects/ai-system/.renmark/memory/routing.md:30): The new routing row makes `qa-review|acceptance-validation|release-readiness` look like a default `fable` route, which contradicts both CLAUDE’s “fable escalation only, never default” rule and the optional `MAY`/`Recommended` wording in `verify` and `finish`. Fix: rewrite the row as an optional escalation lane gated by the calling skill’s explicit choice, not a default route.

- Major — [plugin/skills/verify/SKILL.md](/home/renmark/projects/ai-system/plugin/skills/verify/SKILL.md:51): The skill says the hand-off “MAY offer” a fable QA-review subagent, but the documented hand-off menus later only render `[s]`, `[qa]`, `[dq]`, `[c]`, `[d]`, `[f]`, and `[n]`, so the promised surface is not actually wired anywhere. Fix: add this lane to the verify/QA hand-off sections with explicit dispatch behavior, or remove the promise until the menu exists.

- Major — [plugin/skills/verify/SKILL.md](/home/renmark/projects/ai-system/plugin/skills/verify/SKILL.md:309): This section hard-codes Chrome DevTools MCP for QA subagents and says they can coordinate through the singleton browser session, which conflicts with verify’s existing two-channel contract (`chrome-devtools` or native `claude --chrome`) and its earlier “no subagent fan-out” rationale. Fix: refer to the active browser channel selected by verify and keep rendered validation in the main agent unless a real subagent/browser coordination mechanism is specified.

- Major — [plugin/skills/verify/SKILL.md](/home/renmark/projects/ai-system/plugin/skills/verify/SKILL.md:541): The governance note still says G11 is satisfied for `--qa`/`--deep-qa` because there is “no subagent dispatch,” which is no longer true once the optional fable QA-review lane exists. Fix: update the governance text to distinguish serial browser flows from the optional review subagent and describe the actual G11 boundary.

- Major — [plugin/skills/prd/SKILL.md](/home/renmark/projects/ai-system/plugin/skills/prd/SKILL.md:156): The G11 row still says “This skill itself dispatches no sub-tasks,” which is now contradicted by the new optional fable reconcile subagent at line 81. Fix: revise the governance row to describe the isolated one-shot fable analysis lane and its bounded output.

- Major — [plugin/skills/codereview/SKILL.md](/home/renmark/projects/ai-system/plugin/skills/codereview/SKILL.md:78): `codereview` is named in the new shared contract as a covered dispatching skill, but its adversarial fable lane still lacks the required reasoning-contract citation/instruction, so this surface can drift immediately. Fix: add the exact shared citation blockquote and tell the dispatch prompt to append the canonical instruction at runtime.

- Major — [plugin/skills/audit/SKILL.md](/home/renmark/projects/ai-system/plugin/skills/audit/SKILL.md:63): `audit` has the same rollout gap as `codereview`; the refutation subagent lane is named in the shared contract but never updated to cite or require it. Fix: add the exact shared citation blockquote to the audit refutation-dispatch section.

- Minor — [plugin/skills/finish/SKILL.md](/home/renmark/projects/ai-system/plugin/skills/finish/SKILL.md:54): This surface says to “cite the blockquote” but does not itself use the exact citable blockquote from the shared file, while `brainstorm` does, so the supposedly single citable text is already inconsistent across touched surfaces. Fix: standardize `orchestrate`/`verify`/`finish`/`prd` on the exact shared blockquote text, byte-for-byte.

- Minor — [plugin/skills/orchestrate/SKILL.md](/home/renmark/projects/ai-system/plugin/skills/orchestrate/SKILL.md:151): “the reasoning/output-discipline blockquote from `reasoning-contract.md`” is ambiguous because that file contains both the canonical runtime instruction and a separate skill-author citation blockquote. Fix: name the exact source section to append at runtime (`The canonical reasoning instruction`) and reserve the citable blockquote for the SKILL prose only.
tokens used
73,062
Summary: Critical 0 | Major 8 | Minor 2 | Nit 0

No broken frontmatter or Markdown structure stood out in the touched files.

- Major — [plugin/skills/_shared/reasoning-contract.md](/home/renmark/projects/ai-system/plugin/skills/_shared/reasoning-contract.md:3): The file claims the contract is “shared by every skill that dispatches subagents” and that skills “cite the blockquote below,” but uncited subagent lanes still exist in `codereview`, `audit`, and brainstorm’s Step 3 research dispatch, so the rollout is not actually universal. Fix: either narrow this claim to the surfaces updated in this wave or update every remaining subagent-dispatch section to carry the exact shared citation.

- Major — [.renmark/memory/routing.md](/home/renmark/projects/ai-system/.renmark/memory/routing.md:30): The new routing row makes `qa-review|acceptance-validation|release-readiness` look like a default `fable` route, which contradicts both CLAUDE’s “fable escalation only, never default” rule and the optional `MAY`/`Recommended` wording in `verify` and `finish`. Fix: rewrite the row as an optional escalation lane gated by the calling skill’s explicit choice, not a default route.

- Major — [plugin/skills/verify/SKILL.md](/home/renmark/projects/ai-system/plugin/skills/verify/SKILL.md:51): The skill says the hand-off “MAY offer” a fable QA-review subagent, but the documented hand-off menus later only render `[s]`, `[qa]`, `[dq]`, `[c]`, `[d]`, `[f]`, and `[n]`, so the promised surface is not actually wired anywhere. Fix: add this lane to the verify/QA hand-off sections with explicit dispatch behavior, or remove the promise until the menu exists.

- Major — [plugin/skills/verify/SKILL.md](/home/renmark/projects/ai-system/plugin/skills/verify/SKILL.md:309): This section hard-codes Chrome DevTools MCP for QA subagents and says they can coordinate through the singleton browser session, which conflicts with verify’s existing two-channel contract (`chrome-devtools` or native `claude --chrome`) and its earlier “no subagent fan-out” rationale. Fix: refer to the active browser channel selected by verify and keep rendered validation in the main agent unless a real subagent/browser coordination mechanism is specified.

- Major — [plugin/skills/verify/SKILL.md](/home/renmark/projects/ai-system/plugin/skills/verify/SKILL.md:541): The governance note still says G11 is satisfied for `--qa`/`--deep-qa` because there is “no subagent dispatch,” which is no longer true once the optional fable QA-review lane exists. Fix: update the governance text to distinguish serial browser flows from the optional review subagent and describe the actual G11 boundary.

- Major — [plugin/skills/prd/SKILL.md](/home/renmark/projects/ai-system/plugin/skills/prd/SKILL.md:156): The G11 row still says “This skill itself dispatches no sub-tasks,” which is now contradicted by the new optional fable reconcile subagent at line 81. Fix: revise the governance row to describe the isolated one-shot fable analysis lane and its bounded output.

- Major — [plugin/skills/codereview/SKILL.md](/home/renmark/projects/ai-system/plugin/skills/codereview/SKILL.md:78): `codereview` is named in the new shared contract as a covered dispatching skill, but its adversarial fable lane still lacks the required reasoning-contract citation/instruction, so this surface can drift immediately. Fix: add the exact shared citation blockquote and tell the dispatch prompt to append the canonical instruction at runtime.

- Major — [plugin/skills/audit/SKILL.md](/home/renmark/projects/ai-system/plugin/skills/audit/SKILL.md:63): `audit` has the same rollout gap as `codereview`; the refutation subagent lane is named in the shared contract but never updated to cite or require it. Fix: add the exact shared citation blockquote to the audit refutation-dispatch section.

- Minor — [plugin/skills/finish/SKILL.md](/home/renmark/projects/ai-system/plugin/skills/finish/SKILL.md:54): This surface says to “cite the blockquote” but does not itself use the exact citable blockquote from the shared file, while `brainstorm` does, so the supposedly single citable text is already inconsistent across touched surfaces. Fix: standardize `orchestrate`/`verify`/`finish`/`prd` on the exact shared blockquote text, byte-for-byte.

- Minor — [plugin/skills/orchestrate/SKILL.md](/home/renmark/projects/ai-system/plugin/skills/orchestrate/SKILL.md:151): “the reasoning/output-discipline blockquote from `reasoning-contract.md`” is ambiguous because that file contains both the canonical runtime instruction and a separate skill-author citation blockquote. Fix: name the exact source section to append at runtime (`The canonical reasoning instruction`) and reserve the citable blockquote for the SKILL prose only.

## Summary

- codex review of main..HEAD (agent-routing-policy)
- focus: contract contradictions + overpromising wording
