---
artifact_type: review
schema_version: 1
created_at: 2026-07-01T20:20:51+00:00
source_sha: 48e6bfe6341fd2cecad1d147ebe6ebaeec42edae
related_plan: .renmark/plans/2026-07-01-harness-operating-modes.plan.md
generator: codereview
stale_after: null
dependency_refs: []
completion_state: complete
confidence: high
validation_status: validated
retry_count: 0
parser_success: true
schema_compliance: true
---

# Codereview — harness-operating-modes (codex, full, read-only) — main..HEAD

## VERDICT 1 — Spec-compliance
Spec: compliant — mode.py, skill_preamble integration, CLI flags, docs framing, and tests all match the MVP goal; non-goals (dynamic loading, hard dispatch guards) correctly absent.

## VERDICT 2 — Code-quality — Critical 0, Major 1, Minor 2, Nit 0
- renmark/mode.py | Major | set_mode()/clear_mode() swallow filesystem errors while the CLI prints success + exits 0 — mode persistence can silently fail (read-only FS / ENOSPC). Fix: mutators raise (or return success) on write/delete failure; CLI verifies postcondition before claiming success.
- renmark/cli/_engine.py | Minor | wrong path surfaced beyond the known deferred message: --set-mode/--clear-mode help text AND success output print .renmark/mode.json instead of the real .renmark/state/mode.json. Fix: source the path from a shared constant in renmark.mode.
- renmark/mode.py | Minor | non-atomic write; a concurrent reader can observe a partial mode.json and transiently fall back to None (re-triggers unset-mode flow). Fix: write temp + os.replace().

## Note
Codex sandbox was read-only, so its --behavior run reported the mode case failing (skill_preamble writes last-skill.json). In the real writable env the behavior tier passes 3/3 and the full suite is 1205 green — the sandbox failure is an environment artifact, not a real defect.

## Summary
- Spec: compliant
- Quality: 0 Critical, 1 Major, 2 Minor
- Major: set_mode/clear_mode silent write-failure (CLI reports false success)
- Minor: wrong state path in help+success text; non-atomic write
- next: fix the 3 findings (all in mode.py + _engine.py), then /renmark:finish

## Summary

- Spec: compliant
- Quality: 0 Critical, 1 Major, 2 Minor
- Major: silent write-failure in set_mode/clear_mode
- Minor: wrong state path in help/success; non-atomic write
- next: fix 3 findings then finish
