OpenAI Codex v0.117.0 (research preview)
--------
workdir: /home/renmark/projects/ai-system
model: gpt-5.4
provider: openai
approval: never
sandbox: read-only
reasoning effort: none
reasoning summaries: none
session id: 019eacd3-f253-7593-89e8-95e8c4930a06
--------
user
Review the diff main..HEAD. This adds Loop Mode. Focus CLOSEST on renmark/loop.py — a bounded-loop state machine: LoopState + loop.json read/write (atomic), parse_budget (tokens|$), build_decision (from verify .verification.md metadata + ledger spend delta), stop_reason (terminal: done/budget-hit/max-iter/awaiting-approval/stalled), refresh_spent (via state.usage_by_run_id), budget_remaining. Constants DEFAULT_MAX_ITERATIONS=5, DEFAULT_BUDGET_TOKENS=300000, COST_PER_KTOKEN_USD=0.01.

Scrutinize: (1) the NEVER-RAISE guarantee (read_loop/write_loop/refresh_spent/parse_budget on missing/corrupt/malformed input — does anything raise?); (2) the budget gate correctness — could a loop EXCEED budget (off-by-one, gate checked at wrong time, spent computed wrong) or never stop; (3) the STALLED-HANDLING SPLIT — stop_reason does NOT derive stalled from a blank next_action (the SKILL/driver must set state.status=stalled first): is that a latent bug where a loop with no next_action but status still "running" would loop forever / not terminate? does anything enforce it; (4) parse_budget edge cases ($ parsing, negative, zero, non-numeric, huge); (5) atomic write correctness (tmp+replace, partial write); (6) loop.json schema round-trip (enum/status validity on read); (7) state.usage_by_run_id aggregation correctness. Also check the loop SKILL (plugin/skills/loop/SKILL.md) for the single-gate + REQ-12 (no autonomous merge/release) + context-hygiene claims, and whether tests/test_loop.py actually covers the stalled-forever risk + budget-exceed + never-raise.

For each finding: file:line; severity Critical|Major|Minor|Nit; one-sentence description; one-sentence fix. Top: summary counts per severity. Do not modify files. Do not exit until complete.

warning: Codex could not find system bubblewrap at /usr/bin/bwrap. Please install bubblewrap with your package manager. Codex will use the vendored bubblewrap in the meantime.
codex
Reviewing `main..HEAD` with emphasis on `renmark/loop.py`, the loop skill, and `tests/test_loop.py`. I’m reading the recent changelog entries and the relevant diff/context first, then I’ll trace the budget/stall/state-machine paths against the tests.
exec
/bin/bash -lc 'tail -n 80 /home/renmark/projects/ai-system/CHANGELOG.md' in /home/renmark/projects/ai-system
 succeeded in 0ms:
- `renmark-execute --roadmap` — prints the status table and writes `roadmap.md` snapshot.
- `renmark-execute --no-commit` — flag added (currently a no-op; v0.1.1 will wire it into the per-task commit code so the skill can batch commits per wave).
- argparse prog name corrected from `nim-execute` to `renmark-execute`.

**Memory templates:**

The eight `.renmark/memory/` files now have proper documentation-grade templates:
- `features.md`, `bugs.md`, `decisions.md` (ADR format), `stack.md`, `architecture.md`, `conventions.md`, `routing.md`, `learnings.md`, plus an auto-maintained `INDEX.md`.

**Plugin manifest now declares 7 skills** (brainstorm, plan, orchestrate, debug, codereview, roadmap, help).

**Tests:** 85 passing (up from 52 in v0.0.3).

**Still pending (v0.1.1+):**
- CLI `execute_plan` actually using `dispatch.group_tasks_by_wave` + `dispatch_wave` (currently the loop still runs single-task serial via the v0.0.x path)
- `--no-commit` wired through per-task commit code
- Cost preview in `--dry-run`
- Routing memory auto-updates from run outcomes
- `/renmark:debug` per-step routing (NIM grep / codex trace / opus reasoning)
- `/renmark:codereview` Sonnet + Opus passes
- Additional native providers (Ollama, OpenRouter, OpenAI-compat) — Phase 4
- ~~LiteLLM plug-in slot — Phase 5~~ (dropped — native providers cover the realistic use cases) (optional)

## v0.0.3 — 2026-05-12 (Phase 1, +memory + help)

**Persistent memory module + `/renmark:help` skill.**

- `renmark/memory.py` — read/write helpers for `.renmark/memory/`. Functions: `ensure_memory(repo)`, `read_index(repo)`, `read_file(repo, name)`, `log_feature(...)`, `log_bug(...)`, `log_decision(...)`, `append_routing(...)`, `append_learning(...)`. Section-aware appends (newest-first per CHANGELOG convention). Lessons in `log_bug` auto-cross-post to `learnings.md`. 8 new tests.
- Memory templates rewritten so the files act as **living documentation**:
  - `features.md` — shipped / in-progress / planned (CHANGELOG style)
  - `bugs.md` — open / fixed with severity, symptom, root cause, fix, lesson
  - `decisions.md` — ADR format (context, decision, alternatives, consequences) with auto-numbered IDs
  - `stack.md` — languages, libs, runtime env, external APIs
  - `architecture.md` — components, data flow, module boundaries, invariants
  - `conventions.md`, `routing.md`, `learnings.md` — auto-tuned + hand-edited
  - `INDEX.md` is a cheap top-of-file index loaded first by every skill
- `/renmark:help` skill (new) — prints all six commands with one-sentence descriptions and the typical workflow order. Pure documentation, no API calls.
- `plugin.json` updated to declare 6 skills.

52 tests total (44 from baseline + 8 memory tests).

## v0.0.2 — 2026-05-12 (Phase 1, partial — skills visible)

**Plugin manifest + all five `/renmark:*` SKILL.md files** so the commands appear in Claude Code's skill list after install. Template files for empty-folder bootstrap. install.sh hardened.

Added:
- `plugin/plugin.json` declaring the 5 skills
- `plugin/skills/{brainstorm,plan,orchestrate,debug,codereview}/SKILL.md` — workflow docs for each
- `plugin/templates/{CLAUDE.md,AGENTS.md,renmark-readme.md,memory/*.md}.template` — what `/renmark:brainstorm` writes when bootstrapping an empty project
- `install.sh` ran successfully — symlinks live at `~/.claude/plugins/renmark` and `~/.local/bin/renmark-execute`

Fixed:
- `install.sh` v0.0.1 stored the /orchestrator backup at `~/.claude/skills/.orchestrator.bak/` — Claude Code's skill discovery picked it up as a phantom skill named `.orchestrator.bak`. **Backup removed entirely**: the orchestrator source still lives in `/home/renmark/projects/ai-inference/` (and in its git history), so a separate copy under `~/.claude/` was just paranoia and bug surface. install.sh now `rm -rf`s the old skill outright; manual revert is `cd ~/projects/ai-inference && bash install.sh` against the v0.2.0 baseline.

Not yet wired (still Phase 1):
- `renmark/dispatch.py` — wave-based parallel dispatcher (so orchestrate can't yet run opus/sonnet tasks or parallel groups)
- `renmark/memory.py` — `.renmark/memory/` reader/writer
- `renmark/providers/claude_agent.py` — Opus/Sonnet via Agent tool from skill side
- Parser extensions for `complexity`, `parallel_group`, `est_tokens`, `est_cost_usd`
- CLI `--no-commit` mode for batched wave commits
- Cost preview in `--dry-run`
- Empty-folder bootstrap code (skill docs reference it but the brainstorm skill currently does it by hand)

The skills are visible and `/renmark:brainstorm` + `/renmark:plan` are workable today (they're Opus-driven conversations). `/renmark:orchestrate` runs the same single-task path the v0.0.1 baseline supports.

## v0.0.1 — 2026-05-12 (Phase 0)

**Bootstrap of the new `ai-system` repo.** Copies the working v0.2.0 baseline from `/home/renmark/projects/ai-inference/` and retargets the Python package from `nim_execute` to `renmark`.

Changes vs. ai-inference v0.2.0:

- Package renamed `nim_execute` → `renmark`
- `nim_client.py` → `renmark/providers/nim.py`
- `codex_exec.py` → `renmark/providers/codex.py`
- New `renmark/providers/__init__.py` with `PROVIDERS` registry stub
- Runtime state dir renamed `.nim-state/` → `.renmark/state/` (with `RENMARK_DIR_NAME`, `STATE_SUBDIR`, `MEMORY_SUBDIR`, `DEBUG_SUBDIR` constants; legacy `STATE_DIR_NAME` aliased for back-compat)
- All test imports updated, 41 tests still passing
- CLI references `renmark-execute` / `.renmark/state/` in user-facing strings

Phase 1 (next): the five `/renmark:*` skills, `plugin/plugin.json`, dispatch layer, memory module, empty-folder bootstrap. See `PLAN.md`.

exec
/bin/bash -lc "git diff --stat main..HEAD && printf '\\n---DIFFNAMES---\\n' && git diff --name-only main..HEAD" in /home/renmark/projects/ai-system
 succeeded in 0ms:
 .renmark/memory/features.md                        |  40 +-
 .renmark/memory/learnings.md                       |   3 +
 .renmark/memory/routing.md                         |   3 +
 .renmark/memory/stack.md                           |   4 +
 .renmark/plans/2026-06-09-loop-mode.plan.md        | 186 +++++++++
 .renmark/research/2026-06-09-loop-mode.research.md | 204 +++++++++
 ...925debfe185b1c240cae49ec3a70328.verification.md |  26 ++
 .renmark/specs/2026-06-09-loop-mode.spec.md        | 150 +++++++
 CHANGELOG.md                                       |  20 +
 PRD.md                                             |  36 +-
 plugin/commands/loop.md                            |   8 +
 plugin/skills/loop/SKILL.md                        | 175 ++++++++
 plugin/skills/resume/SKILL.md                      |  47 ++-
 plugin/skills/start/SKILL.md                       |  18 +
 renmark/loop.py                                    | 461 +++++++++++++++++++++
 renmark/state/__init__.py                          |   2 +
 renmark/state/usage.py                             |  23 +
 tests/test_loop.py                                 | 360 ++++++++++++++++
 18 files changed, 1761 insertions(+), 5 deletions(-)

---DIFFNAMES---
.renmark/memory/features.md
.renmark/memory/learnings.md
.renmark/memory/routing.md
.renmark/memory/stack.md
.renmark/plans/2026-06-09-loop-mode.plan.md
.renmark/research/2026-06-09-loop-mode.research.md
.renmark/reviews/2026-06-09-41121c499925debfe185b1c240cae49ec3a70328.verification.md
.renmark/specs/2026-06-09-loop-mode.spec.md
CHANGELOG.md
PRD.md
plugin/commands/loop.md
plugin/skills/loop/SKILL.md
plugin/skills/resume/SKILL.md
plugin/skills/start/SKILL.md
renmark/loop.py
renmark/state/__init__.py
renmark/state/usage.py
tests/test_loop.py

exec
/bin/bash -lc 'git diff --unified=80 main..HEAD -- /home/renmark/projects/ai-system/renmark/loop.py /home/renmark/projects/ai-system/plugin/skills/loop/SKILL.md /home/renmark/projects/ai-system/tests/test_loop.py' in /home/renmark/projects/ai-system
 succeeded in 0ms:
diff --git a/plugin/skills/loop/SKILL.md b/plugin/skills/loop/SKILL.md
new file mode 100644
index 0000000..c7c1d52
--- /dev/null
+++ b/plugin/skills/loop/SKILL.md
@@ -0,0 +1,175 @@
+---
+name: loop
+description: Use to run a bounded, verified, resumable agentic loop — `/renmark:loop "<goal>"` or "keep iterating until X works", "loop until the goal is met". One upfront approval gate (goal + verify cmd + budget + max-iterations + cost preview), then autonomously cycles orchestrate → verify → decide, committing each passing iteration to the branch, until a terminal status (done / budget-hit / max-iter / awaiting-approval / stalled). Never merges, releases, PRs, or makes destructive changes on its own (REQ-12) — it stops at the gate and hands off to /renmark:finish. Reads only verify metadata + the spend ledger + bounded summaries — never code or diffs.
+---
+
+# loop
+
+## Overview
+
+`/renmark:loop` is renmark's bounded execution engine: a **single upfront approval, then
+autonomous** loop that wraps `orchestrate → verify → decide` and runs until it reaches a
+terminal state. It honors the single-dispatch-gate doctrine — there is **exactly one
+approval prompt** for the whole run, never a per-iteration prompt.
+
+The deterministic state machine lives in `renmark/loop.py` (`LoopState`, `parse_budget`,
+`build_decision`, `stop_reason`, `read_loop`/`write_loop`, `refresh_spent`). **This skill
+DRIVES the loop; the module OWNS the state.** The driver invokes orchestrate/verify and
+builds the decision; the module persists `loop.json`, parses the budget, and evaluates
+stop conditions.
+
+**Context hygiene (REQ-5/11):** the loop reads ONLY verify metadata (`.verification.md`),
+the spend ledger (`usage.jsonl` via `usage_by_run_id`), and bounded per-iteration
+summaries. It NEVER reads generated code, diffs, or artifact bodies into conversation.
+
+> Vibe coders never type `/renmark:loop` — `/renmark:start` routes them into Loop Mode
+> under the hood and never says "loop." This is the expert surface.
+
+## When to Use
+
+- The user wants something built and iterated on **until it verifiably works**, within a
+  cost/iteration ceiling, without babysitting each step.
+- "Keep going until the tests pass", "loop until the goal is met", "iterate autonomously".
+
+**Do NOT use:**
+- For a single well-scoped task → `/renmark:orchestrate` once.
+- To brainstorm/design → `/renmark:brainstorm`.
+- To merge/release/PR → that's `/renmark:finish` (the loop stops *before* those, by design).
+
+## Arguments
+
+| Flag | Default | Meaning |
+|---|---|---|
+| `--goal "<text>"` (or positional) | required | The goal the loop verifies backward against. |
+| `--verify <cmd>` | (goal-backward only) | A shell verifier run alongside verify's goal-backward smoke. |
+| `--budget <tokens\|$amount>` | **300000 tokens** (`DEFAULT_BUDGET_TOKENS`) | Spend ceiling. `parse_budget` accepts a token count (`300000`, `300_000`, `300,000`) OR a `$` amount (`$3.00`) — both resolve to tokens, the measurable unit, with a `$` estimate shown. |
+| `--max-iterations <n>` | **5** (`DEFAULT_MAX_ITERATIONS`) | Hard iteration ceiling. |
+
+Parse the budget once via `renmark.loop.parse_budget(value)` → `(budget_tokens, usd_estimate)`.
+A blank / unparseable / non-positive value degrades to the bounded default — never unbounded.
+
+## Steps
+
+**Step 0 — Context check.** Call `lifecycle.skill_preamble(repo, 'loop')`. If it returns a
+non-None hint, surface it as a one-line note. Also call `read_loop(repo, loop_id)` for any
+in-flight loop — if one is `running` or `awaiting-approval`, surface the resume command and
+iteration / budget-remaining instead of starting fresh.
+
+### 1. Single upfront approval gate
+
+This is the **ONLY approval during the run.** Resolve the budget with `parse_budget`, then
+present a cost preview and ask once:
+
+```
+Loop Mode — about to run autonomously to a terminal state.
+  Goal:           <goal>
+  Verify cmd:     <--verify cmd, or "goal-backward smoke only">
+  Budget:         <budget_tokens> tokens  (~<usd_estimate>)
+  Max iterations: <n>
+  Cost preview:   up to <n> × orchestrate+verify cycles, capped at the budget above
+Proceed? [y/N]
+```
+
+If declined, stop — write nothing. **There are NO further prompts until a terminal status**
+(single-dispatch-gate doctrine).
+
+### 2. Initialize the loop
+
+```python
+from renmark import loop
+lid = loop.loop_id(date, slug)                 # caller passes date/slug — deterministic
+state = loop.LoopState(
+    goal=<goal>, verify_cmd=<verify_cmd>,
+    budget_tokens=<budget_tokens>, budget_usd_estimate=<usd_estimate>,
+    run_id=<run_id>, max_iterations=<n>, iteration=0, status="running",
+)
+loop.write_loop(repo, lid, state)              # creates .renmark/loops/<id>/, status=running
+```
+
+Also write sibling `goal.md` for provenance. `loop.json` is the runtime sibling of
+`pipeline.json` — NOT `lifecycle.json` (G12 / 1KB guard).
+
+### 3. Drive the loop (autonomous, no prompts)
+
+While `stop_reason(state)` is `None`:
+
+1. **Orchestrate** — invoke `/renmark:orchestrate` on the current plan / `next_action`. It
+   plans + dispatches, **commits passing work to the feature branch**, and ledgers spend
+   under the loop's `run_id`.
+2. **Verify** — invoke `/renmark:verify` (goal-backward + the `--verify` cmd). It writes a
+   `.verification.md` with machine-readable metadata.
+3. **Decide** — read ONLY that metadata (`renmark.summary.read_metadata`) and the ledger
+   spend delta; build the decision:
+   ```python
+   state = loop.refresh_spent(repo, state)     # spent_tokens from usage.jsonl (real spend)
+   decision = loop.build_decision(verification_meta, spent_delta)
+   ```
+4. **Record** — write `iterations/NNN-summary.md` (bounded) and update `loop.json`:
+   ```python
+   state.iteration += 1
+   if decision["goal_reached"]:
+       state.status = "done"
+   elif not decision["next_action"].strip():
+       state.status = "stalled"     # IMPORTANT: the driver derives 'stalled' from an
+                                     # empty next_action BEFORE calling stop_reason —
+                                     # loop.py delegates that derivation to the driver.
+   loop.write_loop(repo, lid, state)           # written before the iteration returns → resumable
+   ```
+5. **Check** — `stop_reason(state)`. If non-None, the loop is terminal; break.
+6. **Progress line** — emit **ONE bounded line** per iteration (≤ summary cap), e.g.
+   `iter 2/5 · verify FAIL · spent 80k/300k (~$0.80) · next: add null-guard`.
+   **No per-iteration prompt.**
+
+The budget ceiling is checked first among live stop conditions — an approved budget is
+never exceeded; raising it requires a *new* upfront approval.
+
+### 4. Terminal status → bounded verdict → hand off
+
+Stop conditions (all terminal):
+
+| `status` | Trigger |
+|---|---|
+| `done` | verify PASS — goal verified backward |
+| `budget-hit` | ledger spend ≥ approved budget |
+| `max-iter` | iteration ≥ `--max-iterations` |
+| `awaiting-approval` | a REQ-12 gate (merge/release/PR/destructive/budget-escalation) is pending |
+| `stalled` | verify FAIL with no actionable `next_action` / no fresh evidence |
+
+Report a bounded ≤5-line verdict (status, iterations used, spend vs budget, last evidence),
+then hand off to `/renmark:finish` — which carries the **REQ-12 merge/release approval gate**.
+
+### REQ-12 — autonomous within bounds, gated at the edge
+
+The loop **commits each passing iteration** to the feature branch (safe / revertable) but
+**NEVER merges, releases, opens a PR, escalates the budget, or makes destructive changes
+autonomously.** When such a step would be required it sets `pending_step`, reaches
+`awaiting-approval`, and stops. AI may generate and commit code; the human owns merges and
+releases. Only `/renmark:approve` flips the human-approval bit.
+
+## Resumability (REQ-10)
+
+`loop.json` is written before each iteration returns, so a crash / `/clear` / `/compact` /
+new session recovers the iteration index, remaining budget, and pending step. After a
+clear, `/renmark:resume` detects a `running` / `awaiting-approval` `loop.json` and prints
+the resume command + iteration / budget-remaining (zero LLM calls). The loop driver never
+raises into chat — a ledger/verify read failure degrades to a clean STOP with a status, not
+a crash.
+
+## Governance compliance
+
+Upholds G3/G5/G11/G12 and REQ-5/9/10/11/12. The load-bearing invariants are enforced in
+`renmark/loop.py`: stop logic degrades toward stopping (never unbounded), `build_decision`
+consumes only metadata + the ledger (never code/diffs), `refresh_spent` enforces the budget
+against measured spend, and `loop.json` carries runtime state only (not `lifecycle.json`).
+
+## Next step
+
+*End by calling `renmark.lifecycle.next_steps(repo, "loop")` and render the
+result per `${CLAUDE_PLUGIN_ROOT}/skills/_shared/next-steps.md` (class 1 —
+Tier-0 stage routing). Present via `AskUserQuestion` (handoff-menu.md rules
+6–9); the state-derived next command (`/renmark:finish` at a terminal loop) is
+the `(Recommended)` option. Require an explicit choice — never auto-proceed.*
+
+Do not paste the rendering rules — cite the file.
+
+*Mirror any rule changes in `AGENTS.md` in the same commit.*
diff --git a/renmark/loop.py b/renmark/loop.py
new file mode 100644
index 0000000..31e99f4
--- /dev/null
+++ b/renmark/loop.py
@@ -0,0 +1,461 @@
+"""Loop Mode state machine — the deterministic core of renmark's bounded,
+verified, resumable agentic loop (REQ-9/10/11; spec
+``.renmark/specs/2026-06-09-loop-mode.spec.md``).
+
+The loop wraps ``orchestrate → verify → decide`` and runs autonomously within
+a single upfront-approved budget + iteration ceiling until it reaches a
+terminal state. **This module owns the STATE, not the work**: the
+``/renmark:loop`` SKILL drives the actual orchestrate/verify invocations; this
+module persists ``loop.json``, parses the budget, builds the per-iteration
+decision object, and evaluates the stop conditions.
+
+Design contract (mirrors ``renmark/sizing.py`` + ``renmark/lifecycle.py``):
+
+- **Reads ONLY metadata + ledger.** ``build_decision`` consumes a verification
+  *metadata dict* (already parsed by ``renmark.summary.read_metadata``) — it
+  never opens source files, diffs, or artifact bodies (REQ-5/11). The budget
+  gate reads the token ledger via ``usage_by_run_id`` — never the conversation.
+- **Never raises into the caller.** Every IO / parse / read step is wrapped; a
+  missing or corrupt ``loop.json`` degrades to a fresh default (or ``None``),
+  not an exception. Stop logic degrades toward stopping, never toward running
+  unbounded.
+- **Deterministic / testable.** No ``datetime.now()``, no ``random``: the
+  caller passes the ``date``/``slug`` for the loop id. Every threshold is a
+  documented, tunable module-level constant.
+- **Bounded + resumable.** ``loop.json`` is written before each iteration
+  returns, so a crash / ``/clear`` / new session can recover the iteration
+  index, remaining budget, and pending step. This is the runtime sibling of
+  ``pipeline.json`` — NOT ``lifecycle.json`` (G12 / the 1KB workflow-state
+  guard; loop runtime state must not leak into lifecycle.json).
+"""
+
+from __future__ import annotations
+
+import json
+import re
+from dataclasses import asdict, dataclass
+from pathlib import Path
+from typing import Literal
+
+from .state import RENMARK_DIR_NAME, usage_by_run_id
+
+# ── Tunable constants ──────────────────────────────────────────────────────────
+
+#: Default iteration ceiling when ``--max-iterations`` is not supplied.
+DEFAULT_MAX_ITERATIONS: int = 5
+
+#: Default token budget when ``--budget`` is not supplied (300k tokens).
+DEFAULT_BUDGET_TOKENS: int = 300_000
+
+#: Blended cost assumption for the token<->dollar conversion, USD per 1k tokens.
+#:
+#: Loop Mode tracks spend in TOKENS (the measurable unit the ledger records);
+#: the ``$`` figure is only an *estimate* shown to the human. A renmark loop is
+#: orchestrated by Sonnet/Opus but does the bulk emit on Codex, so the realised
+#: blend sits between the per-model rates in ``roadmap.COST_PER_KT``
+#: (haiku 0.0001 · sonnet 0.003 · opus 0.015 · codex 0.05 per 1k). We assume a
+#: single conservative blended rate of **$0.01 / 1k tokens** — deliberately on
+#: the high side so a ``$`` budget never under-buys iterations and the estimate
+#: errs toward caution. Tune as the realised mix / pricing shifts.
+COST_PER_KTOKEN_USD: float = 0.01
+
+#: Terminal + active loop statuses persisted in ``loop.json``.
+LoopStatus = Literal[
+    "running",  # active; not yet terminal
+    "done",  # goal verified (terminal)
+    "budget-hit",  # spend reached the approved ceiling (terminal)
+    "max-iter",  # iteration ceiling reached (terminal)
+    "awaiting-approval",  # a REQ-12 gate is pending (terminal until /renmark:approve)
+    "stalled",  # no fresh evidence / empty next_action (terminal)
+]
+
+#: The non-running statuses — reaching any of these ends the loop.
+TERMINAL_STATUSES: frozenset[str] = frozenset(
+    {"done", "budget-hit", "max-iter", "awaiting-approval", "stalled"}
+)
+
+#: ``loop.json`` filename inside each loop directory.
+LOOP_JSON: str = "loop.json"
+
+#: Subdir under ``.renmark/`` that holds every loop's directory.
+LOOPS_SUBDIR: str = "loops"
+
+#: Verification ``completion_state`` value that signals the run finished cleanly.
+_COMPLETE_STATE: str = "complete"
+
+#: Verification metadata values that count as a PASS-equivalent verdict. The
+#: verify skill records its goal-backward result in ``validation_status``
+#: (``validated`` == PASS) and the body summary; we treat ``validated`` as the
+#: machine-readable PASS signal and require ``completion_state == complete``.
+_PASS_VALIDATION: frozenset[str] = frozenset({"validated"})
+
+
+# ── Loop state ──────────────────────────────────────────────────────────────
+
+
+@dataclass
+class LoopState:
+    """Runtime state of one bounded loop. Persisted to ``loop.json``.
+
+    Pure runtime — the loop's identity/goal plus the live counters. The inner
+    orchestrate run keeps its own ``pipeline.json``; this is the *outer* loop's
+    state. Kept JSON-trivial (str/int/bool only) so read/write never needs a
+    serializer beyond ``json``.
+    """
+
+    goal: str = ""
+    verify_cmd: str = ""
+    budget_tokens: int = DEFAULT_BUDGET_TOKENS
+    budget_usd_estimate: str = ""
+    spent_tokens: int = 0
+    run_id: str = ""
+    max_iterations: int = DEFAULT_MAX_ITERATIONS
+    iteration: int = 0
+    status: str = "running"  # one of LoopStatus
+    pending_step: str = ""  # the REQ-12 gate awaiting approval, if any
+
+    def to_json(self) -> str:
+        return json.dumps(asdict(self), indent=2, sort_keys=False)
+
+
+# ── Paths ────────────────────────────────────────────────────────────────────
+
+
+def loop_id(date: str, slug: str) -> str:
+    """Build the canonical loop id ``loop-<date>-<slug>``.
+
+    ``date`` and ``slug`` are passed in by the caller (no ``datetime.now()`` —
+    the loop stays deterministic/testable). The slug is sanitised to a safe
+    path component: lowercased, non-alphanumerics collapsed to ``-``, trimmed.
+    A blank slug degrades to ``loop``.
+    """
+    safe = re.sub(r"[^a-z0-9]+", "-", str(slug).strip().lower()).strip("-")
+    safe = safe or "loop"
+    return f"loop-{str(date).strip()}-{safe}"
+
+
+def loop_dir(repo: str | Path, loop_id_value: str) -> Path:
+    """Return ``.renmark/loops/<id>/`` for ``loop_id_value`` (not created)."""
+    return Path(repo) / RENMARK_DIR_NAME / LOOPS_SUBDIR / loop_id_value
+
+
+def _loop_json_path(repo: str | Path, loop_id_value: str) -> Path:
+    return loop_dir(repo, loop_id_value) / LOOP_JSON
+
+
+# ── loop.json read / write ─────────────────────────────────────────────────
+
+
+def read_loop(repo: str | Path, loop_id_value: str) -> LoopState | None:
+    """Return the persisted :class:`LoopState`, or ``None`` if absent/corrupt.
+
+    Never raises: a missing directory, missing file, unreadable bytes, invalid
+    JSON, or a non-dict payload all yield ``None`` (the caller then knows there
+    is no resumable loop). Unknown fields are dropped so schema drift can't
+    crash the constructor.
+    """
+    path = _loop_json_path(repo, loop_id_value)
+    try:
+        if not path.exists():
+            return None
+        data = json.loads(path.read_text(encoding="utf-8"))
+    except (OSError, ValueError):
+        return None
+    if not isinstance(data, dict):
+        return None
+    known = set(LoopState.__dataclass_fields__)
+    filtered = {k: v for k, v in data.items() if k in known}
+    try:
+        return LoopState(**filtered)
+    except (TypeError, ValueError):
+        return None
+
+
+def write_loop(repo: str | Path, loop_id_value: str, state: LoopState) -> Path | None:
+    """Persist ``state`` to ``.renmark/loops/<id>/loop.json`` (atomic-ish).
+
+    Writes to a sibling ``.tmp`` file then ``os.replace``-style renames it over
+    the target, so a crash mid-write never leaves a half-written ``loop.json``
+    (the loop stays resumable from the last good state). Returns the written
+    path, or ``None`` on any IO failure — never raises into the caller.
+    """
+    path = _loop_json_path(repo, loop_id_value)
+    tmp = path.with_suffix(path.suffix + ".tmp")
+    try:
+        path.parent.mkdir(parents=True, exist_ok=True)
+        tmp.write_text(state.to_json(), encoding="utf-8")
+        tmp.replace(path)  # atomic on the same filesystem
+    except OSError:
+        try:
+            if tmp.exists():
+                tmp.unlink()
+        except OSError:
+            pass
+        return None
+    return path
+
+
+# ── Budget parsing ─────────────────────────────────────────────────────────
+
+
+def parse_budget(value: str | int) -> tuple[int, str]:
+    """Parse a budget given as EITHER a token count OR a ``$`` amount.
+
+    Accepts:
+    - an ``int`` (e.g. ``300000``) → that many tokens;
+    - a numeric string (e.g. ``"300000"``, ``"300_000"``, ``"300,000"``) →
+      tokens;
+    - a ``$`` string (e.g. ``"$3.00"``, ``"$3"``) → converted to tokens via
+      :data:`COST_PER_KTOKEN_USD`.
+
+    Returns ``(budget_tokens, usd_estimate_str)``. The estimate is always
+    derived from the resolved token count via :func:`estimate_usd`, so both the
+    token-input and ``$``-input paths report a consistent ``$`` figure.
+
+    Never raises: a blank / unparseable / non-positive value degrades to
+    :data:`DEFAULT_BUDGET_TOKENS` (the safe, bounded default — never unbounded).
+    """
+    tokens = _coerce_budget_tokens(value)
+    if tokens <= 0:
+        tokens = DEFAULT_BUDGET_TOKENS
+    return tokens, estimate_usd(tokens)
+
+
+def estimate_usd(tokens: int) -> str:
+    """Return the ``$`` estimate string for a token budget (``"$N.NN"``).
+
+    Uses the blended :data:`COST_PER_KTOKEN_USD` rate. Never raises — a
+    non-positive / non-int token count yields ``"$0.00"``.
+    """
+    try:
+        t = int(tokens)
+    except (TypeError, ValueError):
+        return "$0.00"
+    if t <= 0:
+        return "$0.00"
+    dollars = (t / 1000.0) * COST_PER_KTOKEN_USD
+    return f"${dollars:.2f}"
+
+
+def _coerce_budget_tokens(value: str | int) -> int:
+    """Resolve a raw budget value to a token count. ``<= 0`` / unparseable → 0
+    (the caller substitutes the default). Never raises."""
+    if isinstance(value, bool):  # bool is an int subclass — reject explicitly.
+        return 0
+    if isinstance(value, int):
+        return value
+    if not isinstance(value, str):
+        # Defensive: callers are typed str|int, but a runtime caller may pass
+        # something else. Degrade to 0 rather than raising on ``.strip()``.
+        return 0  # type: ignore[unreachable]
+
+    raw = value.strip()
+    if not raw:
+        return 0
+
+    is_dollar = raw.startswith("$")
+    if is_dollar:
+        raw = raw[1:].strip()
+
+    # Allow human-friendly separators in either form.
+    cleaned = raw.replace("_", "").replace(",", "")
+    if not cleaned:
+        return 0
+
+    try:
+        amount = float(cleaned)
+    except ValueError:
+        return 0
+    if amount <= 0:
+        return 0
+
+    if is_dollar:
+        if COST_PER_KTOKEN_USD <= 0:
+            return 0
+        return round((amount / COST_PER_KTOKEN_USD) * 1000.0)
+    # Bare number → token count (truncate any fractional tokens).
+    return int(amount)
+
+
+# ── Decision object ─────────────────────────────────────────────────────────
+
+
+def build_decision(verification_meta: dict[str, object], spent_delta: int) -> dict[str, object]:
+    """Build the SubagentOutput-shaped decision for one iteration.
+
+    Consumes ONLY the verification *metadata dict* (as returned by
+    ``renmark.summary.read_metadata`` over the iteration's ``.verification.md``)
+    plus the ledger ``spent_delta`` for this iteration. It NEVER opens source
+    files, diffs, or artifact bodies (REQ-5/11) — the verdict is read straight
+    from the metadata fields the verify skill already records.
+
+    Returns::
+
+        {
+          "goal_reached": bool,            # completion_state==complete AND PASS
+          "evidence": list[str],           # bounded evidence lines from meta
+          "next_action": str,              # "" when goal reached / no step
+          "model_recommendation": str,     # suggested executor for next iter
+          "estimated_next_cost": str,      # "$N.NN" for the next iteration
+        }
+
+    Never raises: a missing/odd metadata dict degrades to ``goal_reached=False``
+    with empty evidence (the stop logic then treats an empty ``next_action`` as
+    ``stalled`` — the safe, bounded direction).
+    """
+    meta = verification_meta if isinstance(verification_meta, dict) else {}
+
+    completion = _meta_str(meta, "completion_state")
+    validation = _meta_str(meta, "validation_status")
+    goal_reached = completion == _COMPLETE_STATE and validation in _PASS_VALIDATION
+
+    evidence = _evidence_lines(meta)
+
+    # When the goal is reached there is no next action; otherwise carry whatever
+    # actionable next step the verify metadata recorded. A blank next_action is
+    # the signal for the stalled stop condition.
+    next_action = "" if goal_reached else _meta_str(meta, "next_action")
+
+    model_recommendation = _meta_str(meta, "model_recommendation") or "sonnet"
+
+    # Cost estimate for the *next* iteration: reuse this iteration's spend delta
+    # as a forward proxy (the loop's best deterministic estimate without
+    # guessing). A non-positive / unknown delta yields "$0.00".
+    try:
+        delta = int(spent_delta)
+    except (TypeError, ValueError):
+        delta = 0
+    estimated_next_cost = estimate_usd(delta) if delta > 0 else "$0.00"
+
+    return {
+        "goal_reached": goal_reached,
+        "evidence": evidence,
+        "next_action": next_action,
+        "model_recommendation": model_recommendation,
+        "estimated_next_cost": estimated_next_cost,
+    }
+
+
+def _evidence_lines(meta: dict[str, object]) -> list[str]:
+    """Pull bounded evidence lines from verification metadata.
+
+    Prefers an explicit ``summary_lines`` list; falls back to a single
+    ``summary`` string. Coerces every entry to ``str`` and drops blanks. Capped
+    at 5 lines to honour the G3 summary boundary — never returns the body.
+    """
+    raw = meta.get("summary_lines")
+    lines: list[str] = []
+    if isinstance(raw, list):
+        for item in raw:
+            text = str(item).strip()
+            if text:
+                lines.append(text)
+    if not lines:
+        single = meta.get("summary")
+        if isinstance(single, str) and single.strip():
+            lines.append(single.strip())
+    return lines[:5]
+
+
+def _meta_str(meta: dict[str, object], key: str) -> str:
+    """Return ``meta[key]`` as a stripped ``str`` (``""`` if absent/non-str)."""
+    value = meta.get(key)
+    return value.strip() if isinstance(value, str) else ""
+
+
+# ── Stop logic ───────────────────────────────────────────────────────────────
+
+
+def stop_reason(state: LoopState) -> str | None:
+    """Return the terminal status the loop should adopt, or ``None`` to continue.
+
+    Evaluated in safety order — the first matching condition wins:
+
+    1. ``status`` already terminal → return it (idempotent re-entry).
+    2. spend ≥ budget → ``"budget-hit"`` (checked first among live conditions so
+       an approved ceiling is never exceeded).
+    3. iteration ≥ max_iterations → ``"max-iter"``.
+    4. a pending REQ-12 gate (non-empty ``pending_step``) → ``"awaiting-approval"``.
+    5. goal reached (status already flipped to ``"done"`` by the caller, OR an
+       empty ``pending_step`` with status ``"done"``) → ``"done"``.
+
+    The caller sets ``status="done"`` when ``build_decision`` returns
+    ``goal_reached=True``; this function then confirms it as terminal. A blank
+    ``next_action`` (no fresh evidence) maps to ``"stalled"`` — but that signal
+    lives in the decision object, so the caller records it on ``state.status``
+    before calling here. To keep the rule total and never raise, this function
+    treats a ``"stalled"`` status as terminal too.
+
+    Never raises: a malformed ``state`` (non-int counters) degrades to a stop
+    (``"stalled"``) rather than looping unbounded.
+    """
+    try:
+        status = getattr(state, "status", "") or ""
+        if status in TERMINAL_STATUSES:
+            return status
+
+        spent = int(getattr(state, "spent_tokens", 0) or 0)
+        budget = int(getattr(state, "budget_tokens", 0) or 0)
+        iteration = int(getattr(state, "iteration", 0) or 0)
+        max_iter = int(getattr(state, "max_iterations", 0) or 0)
+        pending = (getattr(state, "pending_step", "") or "").strip()
+
+        # Budget ceiling is checked first — never exceed the approved spend.
+        if budget > 0 and spent >= budget:
+            return "budget-hit"
+        if max_iter > 0 and iteration >= max_iter:
+            return "max-iter"
+        if pending:
+            return "awaiting-approval"
+        return None
+    except (TypeError, ValueError):
+        # A malformed state must stop the loop, not run it unbounded.
+        return "stalled"
+
+
+# ── Budget gate ───────────────────────────────────────────────────────────────
+
+
+def refresh_spent(repo: str | Path, state: LoopState) -> LoopState:
+    """Recompute ``state.spent_tokens`` from the real ledger for this run_id.
+
+    The budget gate is enforced against measured spend, not an estimate: this
+    reads ``usage.jsonl`` via :func:`usage_by_run_id` (which itself never
+    raises → 0). Mutates and returns ``state`` for caller convenience. A blank
+    ``run_id`` leaves ``spent_tokens`` untouched (nothing to attribute yet).
+    """
+    run_id = (getattr(state, "run_id", "") or "").strip()
+    if run_id:
+        state.spent_tokens = usage_by_run_id(repo, run_id)
+    return state
+
+
+def budget_remaining(state: LoopState) -> int:
+    """Tokens left before the budget ceiling (never negative). 0 == exhausted."""
+    try:
+        remaining = int(state.budget_tokens) - int(state.spent_tokens)
+    except (TypeError, ValueError):
+        return 0
+    return max(0, remaining)
+
+
+__all__ = [
+    "COST_PER_KTOKEN_USD",
+    "DEFAULT_BUDGET_TOKENS",
+    "DEFAULT_MAX_ITERATIONS",
+    "LOOPS_SUBDIR",
+    "LOOP_JSON",
+    "TERMINAL_STATUSES",
+    "LoopState",
+    "LoopStatus",
+    "budget_remaining",
+    "build_decision",
+    "estimate_usd",
+    "loop_dir",
+    "loop_id",
+    "parse_budget",
+    "read_loop",
+    "refresh_spent",
+    "stop_reason",
+    "write_loop",
+]
diff --git a/tests/test_loop.py b/tests/test_loop.py
new file mode 100644
index 0000000..1922603
--- /dev/null
+++ b/tests/test_loop.py
@@ -0,0 +1,360 @@
+"""Unit tests for renmark.loop (Loop Mode state machine).
+
+Hermetic: every test runs under pytest's ``tmp_path`` and seeds the usage
+ledger through the real :func:`renmark.state.append_usage` API rather than
+hardcoding the on-disk layout. All magic numbers reference module constants
+from ``renmark.loop`` (DEFAULT_BUDGET_TOKENS / DEFAULT_MAX_ITERATIONS /
+COST_PER_KTOKEN_USD) so the suite tracks the source of truth.
+"""
+
+from __future__ import annotations
+
+from pathlib import Path
+
+import pytest
+
+from renmark import loop
+from renmark.loop import (
+    COST_PER_KTOKEN_USD,
+    DEFAULT_BUDGET_TOKENS,
+    DEFAULT_MAX_ITERATIONS,
+    LoopState,
+    budget_remaining,
+    build_decision,
+    estimate_usd,
+    loop_id,
+    parse_budget,
+    read_loop,
+    refresh_spent,
+    stop_reason,
+    write_loop,
+)
+from renmark.state import UsageRecord, append_usage
+
+
+# ── write_loop / read_loop round-trip ──────────────────────────────────────
+
+
+def test_write_then_read_loop_round_trip(tmp_path: Path) -> None:
+    """All fields survive a write -> read cycle under .renmark/loops/<id>/."""
+    lid = loop_id("2026-06-09", "Add Auth!")
+    state = LoopState(
+        goal="ship auth",
+        verify_cmd="pytest -q",
+        budget_tokens=123_456,
+        budget_usd_estimate="$1.23",
+        spent_tokens=42,
+        run_id="run-abc",
+        max_iterations=7,
+        iteration=3,
+        status="running",
+        pending_step="",
+    )
+
+    written = write_loop(tmp_path, lid, state)
+    assert written is not None
+    # Lands under the canonical .renmark/loops/<id>/loop.json location.
+    assert written == loop.loop_dir(tmp_path, lid) / loop.LOOP_JSON
+    assert written.exists()
+
+    loaded = read_loop(tmp_path, lid)
+    assert loaded is not None
+    assert loaded.goal == "ship auth"
+    assert loaded.verify_cmd == "pytest -q"
+    assert loaded.budget_tokens == 123_456
+    assert loaded.budget_usd_estimate == "$1.23"
+    assert loaded.spent_tokens == 42
+    assert loaded.run_id == "run-abc"
+    assert loaded.max_iterations == 7
+    assert loaded.iteration == 3
+    assert loaded.status == "running"
+    assert loaded.pending_step == ""
+
+
+def test_loop_id_sanitises_slug() -> None:
+    assert loop_id("2026-06-09", "Add Auth!") == "loop-2026-06-09-add-auth"
+    assert loop_id("2026-06-09", "   ") == "loop-2026-06-09-loop"
+
+
+def test_read_loop_missing_returns_none(tmp_path: Path) -> None:
+    assert read_loop(tmp_path, "loop-does-not-exist") is None
+
+
+def test_read_loop_corrupt_returns_none_no_raise(tmp_path: Path) -> None:
+    lid = "loop-2026-06-09-corrupt"
+    path = loop.loop_dir(tmp_path, lid) / loop.LOOP_JSON
+    path.parent.mkdir(parents=True, exist_ok=True)
+    path.write_text("{ this is not json", encoding="utf-8")
+    assert read_loop(tmp_path, lid) is None
+
+
+def test_read_loop_non_dict_payload_returns_none(tmp_path: Path) -> None:
+    lid = "loop-2026-06-09-list"
+    path = loop.loop_dir(tmp_path, lid) / loop.LOOP_JSON
+    path.parent.mkdir(parents=True, exist_ok=True)
+    path.write_text("[1, 2, 3]", encoding="utf-8")
+    assert read_loop(tmp_path, lid) is None
+
+
+def test_read_loop_drops_unknown_fields(tmp_path: Path) -> None:
+    """Schema drift (an extra key) must not crash the constructor."""
+    lid = "loop-2026-06-09-extra"
+    path = loop.loop_dir(tmp_path, lid) / loop.LOOP_JSON
+    path.parent.mkdir(parents=True, exist_ok=True)
+    path.write_text('{"goal": "g", "totally_new_field": 99}', encoding="utf-8")
+    loaded = read_loop(tmp_path, lid)
+    assert loaded is not None
+    assert loaded.goal == "g"
+
+
+# ── parse_budget / estimate_usd ─────────────────────────────────────────────
+
+
+def test_parse_budget_token_int() -> None:
+    tokens, usd = parse_budget(250_000)
+    assert tokens == 250_000
+    assert usd == estimate_usd(250_000)
+
+
+def test_parse_budget_numeric_string() -> None:
+    assert parse_budget("300000")[0] == 300_000
+    assert parse_budget("300_000")[0] == 300_000
+    assert parse_budget("300,000")[0] == 300_000
+
+
+def test_parse_budget_dollar_string_matches_default() -> None:
+    """At $0.01/1k tokens, a $3.00 budget resolves to the 300k default."""
+    # Guard: this equivalence only holds if the constant is the assumed rate.
+    assert COST_PER_KTOKEN_USD == pytest.approx(0.01)
+    expected = round((3.00 / COST_PER_KTOKEN_USD) * 1000.0)
+    tokens, usd = parse_budget("$3.00")
+    assert tokens == expected
+    assert tokens == DEFAULT_BUDGET_TOKENS
+    assert usd == estimate_usd(tokens)
+    # Bare "$3" parses identically.
+    assert parse_budget("$3")[0] == expected
+
+
+def test_parse_budget_blank_and_garbage_default() -> None:
+    for bad in ("", "   ", "not-a-number", "$", "-5", 0, -100):
+        tokens, usd = parse_budget(bad)  # type: ignore[arg-type]
+        assert tokens == DEFAULT_BUDGET_TOKENS
+        assert usd == estimate_usd(DEFAULT_BUDGET_TOKENS)
+
+
+def test_parse_budget_rejects_bool() -> None:
+    """bool is an int subclass but must not be read as a token count."""
+    assert parse_budget(True)[0] == DEFAULT_BUDGET_TOKENS  # type: ignore[arg-type]
+
+
+def test_estimate_usd_consistency_both_directions() -> None:
+    # token -> $ derivation follows the documented blended rate.
+    assert estimate_usd(DEFAULT_BUDGET_TOKENS) == "$3.00"
+    assert estimate_usd(100_000) == "$1.00"
+    assert estimate_usd(0) == "$0.00"
+    assert estimate_usd(-50) == "$0.00"
+    # $-string -> tokens -> $ round-trips to the same figure.
+    tokens, usd = parse_budget("$3.00")
+    assert estimate_usd(tokens) == usd == "$3.00"
+
+
+# ── build_decision ──────────────────────────────────────────────────────────
+
+
+def test_build_decision_goal_reached() -> None:
+    meta = {
+        "completion_state": "complete",
+        "validation_status": "validated",
+        "summary_lines": ["all 12 tests pass", "smoke ok"],
+        "model_recommendation": "opus",
+    }
+    decision = build_decision(meta, spent_delta=50_000)
+    assert decision["goal_reached"] is True
+    assert decision["next_action"] == ""  # no further step when reached
+    assert decision["evidence"] == ["all 12 tests pass", "smoke ok"]
+    assert decision["model_recommendation"] == "opus"
+    assert decision["estimated_next_cost"] == estimate_usd(50_000)
+
+
+def test_build_decision_fail_partial_completion() -> None:
+    meta = {
+        "completion_state": "partial",
+        "validation_status": "validated",
+        "next_action": "fix the failing import",
+        "summary": "3 of 5 tasks done",
+    }
+    decision = build_decision(meta, spent_delta=10_000)
+    assert decision["goal_reached"] is False
+    assert decision["next_action"] == "fix the failing import"
+    assert decision["evidence"] == ["3 of 5 tasks done"]
+
+
+def test_build_decision_fail_unvalidated() -> None:
+    meta = {
+        "completion_state": "complete",
+        "validation_status": "failed",
+        "next_action": "verifier reported 2 failures",
+        "summary_lines": ["FAIL: test_auth"],
+    }
+    decision = build_decision(meta, spent_delta=0)
+    assert decision["goal_reached"] is False
+    assert decision["next_action"] == "verifier reported 2 failures"
+    assert decision["evidence"] == ["FAIL: test_auth"]
+    # Non-positive delta yields the $0.00 estimate.
+    assert decision["estimated_next_cost"] == "$0.00"
+
+
+def test_build_decision_reads_only_passed_dict() -> None:
+    """No filesystem access: a bare dict in, a decision out. Defaults applied."""
+    decision = build_decision({}, spent_delta=0)
+    assert decision["goal_reached"] is False
+    assert decision["evidence"] == []
+    assert decision["model_recommendation"] == "sonnet"  # documented default
+
+
+def test_build_decision_non_dict_meta_degrades() -> None:
+    decision = build_decision("not a dict", spent_delta=0)  # type: ignore[arg-type]
+    assert decision["goal_reached"] is False
+    assert decision["evidence"] == []
+
+
+# ── stop_reason ─────────────────────────────────────────────────────────────
+
+
+@pytest.mark.parametrize(
+    "status",
+    ["done", "budget-hit", "max-iter", "awaiting-approval", "stalled"],
+)
+def test_stop_reason_each_terminal_status_is_terminal(status: str) -> None:
+    """Every terminal status (including SKILL-set 'stalled') stops the loop."""
+    state = LoopState(status=status)
+    assert stop_reason(state) == status
+
+
+def test_stop_reason_running_with_room_continues() -> None:
+    state = LoopState(
+        status="running",
+        spent_tokens=10,
+        budget_tokens=DEFAULT_BUDGET_TOKENS,
+        iteration=1,
+        max_iterations=DEFAULT_MAX_ITERATIONS,
+        pending_step="",
+    )
+    assert stop_reason(state) is None
+
+
+def test_stop_reason_budget_hit_when_spent_reaches_budget() -> None:
+    state = LoopState(
+        status="running",
+        spent_tokens=DEFAULT_BUDGET_TOKENS,
+        budget_tokens=DEFAULT_BUDGET_TOKENS,
+        iteration=0,
+        max_iterations=DEFAULT_MAX_ITERATIONS,
+    )
+    assert stop_reason(state) == "budget-hit"
+
+
+def test_stop_reason_max_iter_when_iteration_reaches_ceiling() -> None:
+    state = LoopState(
+        status="running",
+        spent_tokens=0,
+        budget_tokens=DEFAULT_BUDGET_TOKENS,
+        iteration=DEFAULT_MAX_ITERATIONS,
+        max_iterations=DEFAULT_MAX_ITERATIONS,
+    )
+    assert stop_reason(state) == "max-iter"
+
+
+def test_stop_reason_budget_checked_before_max_iter() -> None:
+    """Budget ceiling wins when both conditions trip — never exceed spend."""
+    state = LoopState(
+        status="running",
+        spent_tokens=DEFAULT_BUDGET_TOKENS,
+        budget_tokens=DEFAULT_BUDGET_TOKENS,
+        iteration=DEFAULT_MAX_ITERATIONS,
+        max_iterations=DEFAULT_MAX_ITERATIONS,
+    )
+    assert stop_reason(state) == "budget-hit"
+
+
+def test_stop_reason_pending_step_awaits_approval() -> None:
+    state = LoopState(
+        status="running",
+        spent_tokens=0,
+        budget_tokens=DEFAULT_BUDGET_TOKENS,
+        iteration=0,
+        max_iterations=DEFAULT_MAX_ITERATIONS,
+        pending_step="destructive migration",
+    )
+    assert stop_reason(state) == "awaiting-approval"
+
+
+def test_stop_reason_does_not_derive_stalled_from_blank_next_action() -> None:
+    """A running state with room is NOT stalled — stop_reason never inspects
+    next_action; 'stalled' is set onto state.status by the driver, not derived
+    here. This is the stalled-handling split."""
+    state = LoopState(status="running", spent_tokens=0, budget_tokens=DEFAULT_BUDGET_TOKENS)
+    assert stop_reason(state) is None
+
+
+# ── refresh_spent / budget_remaining ────────────────────────────────────────
+
+
+def _seed_usage(repo: Path, run_id: str, pairs: list[tuple[int, int]], *, task_id: int = 1) -> None:
+    """Append usage records for ``run_id`` via the real ledger API (hermetic)."""
+    for i, (pin, pout) in enumerate(pairs):
+        append_usage(
+            repo,
+            UsageRecord(
+                ts="2026-06-09T00:00:0%d+00:00" % (i % 10),
+                run_id=run_id,
+                task_id=task_id,
+                model="codex",
+                prompt_tokens=pin,
+                completion_tokens=pout,
+            ),
+        )
+
+
+def test_refresh_spent_aggregates_run_id(tmp_path: Path) -> None:
+    state = LoopState(run_id="run-xyz", budget_tokens=DEFAULT_BUDGET_TOKENS, spent_tokens=0)
+    _seed_usage(tmp_path, "run-xyz", [(1000, 500), (2000, 1500)])
+    # A record under a DIFFERENT run_id must not be counted.
+    _seed_usage(tmp_path, "run-other", [(9999, 9999)])
+
+    refreshed = refresh_spent(tmp_path, state)
+    assert refreshed is state  # mutates + returns the same object
+    assert refreshed.spent_tokens == 1000 + 500 + 2000 + 1500
+    assert budget_remaining(refreshed) == DEFAULT_BUDGET_TOKENS - 5000
+
+
+def test_refresh_spent_blank_run_id_untouched(tmp_path: Path) -> None:
+    state = LoopState(run_id="", spent_tokens=777)
+    refreshed = refresh_spent(tmp_path, state)
+    assert refreshed.spent_tokens == 777  # nothing to attribute yet
+
+
+def test_refresh_spent_missing_ledger_no_raise(tmp_path: Path) -> None:
+    """No usage.jsonl on disk → spend degrades to 0, never raises."""
+    state = LoopState(run_id="run-none", spent_tokens=123)
+    refreshed = refresh_spent(tmp_path, state)
+    assert refreshed.spent_tokens == 0
+
+
+def test_budget_remaining_never_negative() -> None:
+    state = LoopState(budget_tokens=100, spent_tokens=250)
+    assert budget_remaining(state) == 0
+
+
+def test_budget_remaining_normal() -> None:
+    state = LoopState(budget_tokens=DEFAULT_BUDGET_TOKENS, spent_tokens=100_000)
+    assert budget_remaining(state) == DEFAULT_BUDGET_TOKENS - 100_000
+
+
+# ── constant sanity (no hardcoded magic numbers leaked) ─────────────────────
+
+
+def test_module_constants_have_expected_defaults() -> None:
+    assert DEFAULT_MAX_ITERATIONS == 5
+    assert DEFAULT_BUDGET_TOKENS == 300_000
+    assert COST_PER_KTOKEN_USD == pytest.approx(0.01)

codex
I have the Loop Mode diff. Next I’m checking the supporting ledger/metadata paths and the exact line numbers so the review can distinguish real state-machine bugs from driver assumptions.
exec
/bin/bash -lc "nl -ba /home/renmark/projects/ai-system/renmark/loop.py | sed -n '261,520p'" in /home/renmark/projects/ai-system
exec
/bin/bash -lc "nl -ba /home/renmark/projects/ai-system/renmark/loop.py | sed -n '1,260p'" in /home/renmark/projects/ai-system
 succeeded in 0ms:
   261	    cleaned = raw.replace("_", "").replace(",", "")
   262	    if not cleaned:
   263	        return 0
   264	
   265	    try:
   266	        amount = float(cleaned)
   267	    except ValueError:
   268	        return 0
   269	    if amount <= 0:
   270	        return 0
   271	
   272	    if is_dollar:
   273	        if COST_PER_KTOKEN_USD <= 0:
   274	            return 0
   275	        return round((amount / COST_PER_KTOKEN_USD) * 1000.0)
   276	    # Bare number → token count (truncate any fractional tokens).
   277	    return int(amount)
   278	
   279	
   280	# ── Decision object ─────────────────────────────────────────────────────────
   281	
   282	
   283	def build_decision(verification_meta: dict[str, object], spent_delta: int) -> dict[str, object]:
   284	    """Build the SubagentOutput-shaped decision for one iteration.
   285	
   286	    Consumes ONLY the verification *metadata dict* (as returned by
   287	    ``renmark.summary.read_metadata`` over the iteration's ``.verification.md``)
   288	    plus the ledger ``spent_delta`` for this iteration. It NEVER opens source
   289	    files, diffs, or artifact bodies (REQ-5/11) — the verdict is read straight
   290	    from the metadata fields the verify skill already records.
   291	
   292	    Returns::
   293	
   294	        {
   295	          "goal_reached": bool,            # completion_state==complete AND PASS
   296	          "evidence": list[str],           # bounded evidence lines from meta
   297	          "next_action": str,              # "" when goal reached / no step
   298	          "model_recommendation": str,     # suggested executor for next iter
   299	          "estimated_next_cost": str,      # "$N.NN" for the next iteration
   300	        }
   301	
   302	    Never raises: a missing/odd metadata dict degrades to ``goal_reached=False``
   303	    with empty evidence (the stop logic then treats an empty ``next_action`` as
   304	    ``stalled`` — the safe, bounded direction).
   305	    """
   306	    meta = verification_meta if isinstance(verification_meta, dict) else {}
   307	
   308	    completion = _meta_str(meta, "completion_state")
   309	    validation = _meta_str(meta, "validation_status")
   310	    goal_reached = completion == _COMPLETE_STATE and validation in _PASS_VALIDATION
   311	
   312	    evidence = _evidence_lines(meta)
   313	
   314	    # When the goal is reached there is no next action; otherwise carry whatever
   315	    # actionable next step the verify metadata recorded. A blank next_action is
   316	    # the signal for the stalled stop condition.
   317	    next_action = "" if goal_reached else _meta_str(meta, "next_action")
   318	
   319	    model_recommendation = _meta_str(meta, "model_recommendation") or "sonnet"
   320	
   321	    # Cost estimate for the *next* iteration: reuse this iteration's spend delta
   322	    # as a forward proxy (the loop's best deterministic estimate without
   323	    # guessing). A non-positive / unknown delta yields "$0.00".
   324	    try:
   325	        delta = int(spent_delta)
   326	    except (TypeError, ValueError):
   327	        delta = 0
   328	    estimated_next_cost = estimate_usd(delta) if delta > 0 else "$0.00"
   329	
   330	    return {
   331	        "goal_reached": goal_reached,
   332	        "evidence": evidence,
   333	        "next_action": next_action,
   334	        "model_recommendation": model_recommendation,
   335	        "estimated_next_cost": estimated_next_cost,
   336	    }
   337	
   338	
   339	def _evidence_lines(meta: dict[str, object]) -> list[str]:
   340	    """Pull bounded evidence lines from verification metadata.
   341	
   342	    Prefers an explicit ``summary_lines`` list; falls back to a single
   343	    ``summary`` string. Coerces every entry to ``str`` and drops blanks. Capped
   344	    at 5 lines to honour the G3 summary boundary — never returns the body.
   345	    """
   346	    raw = meta.get("summary_lines")
   347	    lines: list[str] = []
   348	    if isinstance(raw, list):
   349	        for item in raw:
   350	            text = str(item).strip()
   351	            if text:
   352	                lines.append(text)
   353	    if not lines:
   354	        single = meta.get("summary")
   355	        if isinstance(single, str) and single.strip():
   356	            lines.append(single.strip())
   357	    return lines[:5]
   358	
   359	
   360	def _meta_str(meta: dict[str, object], key: str) -> str:
   361	    """Return ``meta[key]`` as a stripped ``str`` (``""`` if absent/non-str)."""
   362	    value = meta.get(key)
   363	    return value.strip() if isinstance(value, str) else ""
   364	
   365	
   366	# ── Stop logic ───────────────────────────────────────────────────────────────
   367	
   368	
   369	def stop_reason(state: LoopState) -> str | None:
   370	    """Return the terminal status the loop should adopt, or ``None`` to continue.
   371	
   372	    Evaluated in safety order — the first matching condition wins:
   373	
   374	    1. ``status`` already terminal → return it (idempotent re-entry).
   375	    2. spend ≥ budget → ``"budget-hit"`` (checked first among live conditions so
   376	       an approved ceiling is never exceeded).
   377	    3. iteration ≥ max_iterations → ``"max-iter"``.
   378	    4. a pending REQ-12 gate (non-empty ``pending_step``) → ``"awaiting-approval"``.
   379	    5. goal reached (status already flipped to ``"done"`` by the caller, OR an
   380	       empty ``pending_step`` with status ``"done"``) → ``"done"``.
   381	
   382	    The caller sets ``status="done"`` when ``build_decision`` returns
   383	    ``goal_reached=True``; this function then confirms it as terminal. A blank
   384	    ``next_action`` (no fresh evidence) maps to ``"stalled"`` — but that signal
   385	    lives in the decision object, so the caller records it on ``state.status``
   386	    before calling here. To keep the rule total and never raise, this function
   387	    treats a ``"stalled"`` status as terminal too.
   388	
   389	    Never raises: a malformed ``state`` (non-int counters) degrades to a stop
   390	    (``"stalled"``) rather than looping unbounded.
   391	    """
   392	    try:
   393	        status = getattr(state, "status", "") or ""
   394	        if status in TERMINAL_STATUSES:
   395	            return status
   396	
   397	        spent = int(getattr(state, "spent_tokens", 0) or 0)
   398	        budget = int(getattr(state, "budget_tokens", 0) or 0)
   399	        iteration = int(getattr(state, "iteration", 0) or 0)
   400	        max_iter = int(getattr(state, "max_iterations", 0) or 0)
   401	        pending = (getattr(state, "pending_step", "") or "").strip()
   402	
   403	        # Budget ceiling is checked first — never exceed the approved spend.
   404	        if budget > 0 and spent >= budget:
   405	            return "budget-hit"
   406	        if max_iter > 0 and iteration >= max_iter:
   407	            return "max-iter"
   408	        if pending:
   409	            return "awaiting-approval"
   410	        return None
   411	    except (TypeError, ValueError):
   412	        # A malformed state must stop the loop, not run it unbounded.
   413	        return "stalled"
   414	
   415	
   416	# ── Budget gate ───────────────────────────────────────────────────────────────
   417	
   418	
   419	def refresh_spent(repo: str | Path, state: LoopState) -> LoopState:
   420	    """Recompute ``state.spent_tokens`` from the real ledger for this run_id.
   421	
   422	    The budget gate is enforced against measured spend, not an estimate: this
   423	    reads ``usage.jsonl`` via :func:`usage_by_run_id` (which itself never
   424	    raises → 0). Mutates and returns ``state`` for caller convenience. A blank
   425	    ``run_id`` leaves ``spent_tokens`` untouched (nothing to attribute yet).
   426	    """
   427	    run_id = (getattr(state, "run_id", "") or "").strip()
   428	    if run_id:
   429	        state.spent_tokens = usage_by_run_id(repo, run_id)
   430	    return state
   431	
   432	
   433	def budget_remaining(state: LoopState) -> int:
   434	    """Tokens left before the budget ceiling (never negative). 0 == exhausted."""
   435	    try:
   436	        remaining = int(state.budget_tokens) - int(state.spent_tokens)
   437	    except (TypeError, ValueError):
   438	        return 0
   439	    return max(0, remaining)
   440	
   441	
   442	__all__ = [
   443	    "COST_PER_KTOKEN_USD",
   444	    "DEFAULT_BUDGET_TOKENS",
   445	    "DEFAULT_MAX_ITERATIONS",
   446	    "LOOPS_SUBDIR",
   447	    "LOOP_JSON",
   448	    "TERMINAL_STATUSES",
   449	    "LoopState",
   450	    "LoopStatus",
   451	    "budget_remaining",
   452	    "build_decision",
   453	    "estimate_usd",
   454	    "loop_dir",
   455	    "loop_id",
   456	    "parse_budget",
   457	    "read_loop",
   458	    "refresh_spent",
   459	    "stop_reason",
   460	    "write_loop",
   461	]

 succeeded in 0ms:
     1	"""Loop Mode state machine — the deterministic core of renmark's bounded,
     2	verified, resumable agentic loop (REQ-9/10/11; spec
     3	``.renmark/specs/2026-06-09-loop-mode.spec.md``).
     4	
     5	The loop wraps ``orchestrate → verify → decide`` and runs autonomously within
     6	a single upfront-approved budget + iteration ceiling until it reaches a
     7	terminal state. **This module owns the STATE, not the work**: the
     8	``/renmark:loop`` SKILL drives the actual orchestrate/verify invocations; this
     9	module persists ``loop.json``, parses the budget, builds the per-iteration
    10	decision object, and evaluates the stop conditions.
    11	
    12	Design contract (mirrors ``renmark/sizing.py`` + ``renmark/lifecycle.py``):
    13	
    14	- **Reads ONLY metadata + ledger.** ``build_decision`` consumes a verification
    15	  *metadata dict* (already parsed by ``renmark.summary.read_metadata``) — it
    16	  never opens source files, diffs, or artifact bodies (REQ-5/11). The budget
    17	  gate reads the token ledger via ``usage_by_run_id`` — never the conversation.
    18	- **Never raises into the caller.** Every IO / parse / read step is wrapped; a
    19	  missing or corrupt ``loop.json`` degrades to a fresh default (or ``None``),
    20	  not an exception. Stop logic degrades toward stopping, never toward running
    21	  unbounded.
    22	- **Deterministic / testable.** No ``datetime.now()``, no ``random``: the
    23	  caller passes the ``date``/``slug`` for the loop id. Every threshold is a
    24	  documented, tunable module-level constant.
    25	- **Bounded + resumable.** ``loop.json`` is written before each iteration
    26	  returns, so a crash / ``/clear`` / new session can recover the iteration
    27	  index, remaining budget, and pending step. This is the runtime sibling of
    28	  ``pipeline.json`` — NOT ``lifecycle.json`` (G12 / the 1KB workflow-state
    29	  guard; loop runtime state must not leak into lifecycle.json).
    30	"""
    31	
    32	from __future__ import annotations
    33	
    34	import json
    35	import re
    36	from dataclasses import asdict, dataclass
    37	from pathlib import Path
    38	from typing import Literal
    39	
    40	from .state import RENMARK_DIR_NAME, usage_by_run_id
    41	
    42	# ── Tunable constants ──────────────────────────────────────────────────────────
    43	
    44	#: Default iteration ceiling when ``--max-iterations`` is not supplied.
    45	DEFAULT_MAX_ITERATIONS: int = 5
    46	
    47	#: Default token budget when ``--budget`` is not supplied (300k tokens).
    48	DEFAULT_BUDGET_TOKENS: int = 300_000
    49	
    50	#: Blended cost assumption for the token<->dollar conversion, USD per 1k tokens.
    51	#:
    52	#: Loop Mode tracks spend in TOKENS (the measurable unit the ledger records);
    53	#: the ``$`` figure is only an *estimate* shown to the human. A renmark loop is
    54	#: orchestrated by Sonnet/Opus but does the bulk emit on Codex, so the realised
    55	#: blend sits between the per-model rates in ``roadmap.COST_PER_KT``
    56	#: (haiku 0.0001 · sonnet 0.003 · opus 0.015 · codex 0.05 per 1k). We assume a
    57	#: single conservative blended rate of **$0.01 / 1k tokens** — deliberately on
    58	#: the high side so a ``$`` budget never under-buys iterations and the estimate
    59	#: errs toward caution. Tune as the realised mix / pricing shifts.
    60	COST_PER_KTOKEN_USD: float = 0.01
    61	
    62	#: Terminal + active loop statuses persisted in ``loop.json``.
    63	LoopStatus = Literal[
    64	    "running",  # active; not yet terminal
    65	    "done",  # goal verified (terminal)
    66	    "budget-hit",  # spend reached the approved ceiling (terminal)
    67	    "max-iter",  # iteration ceiling reached (terminal)
    68	    "awaiting-approval",  # a REQ-12 gate is pending (terminal until /renmark:approve)
    69	    "stalled",  # no fresh evidence / empty next_action (terminal)
    70	]
    71	
    72	#: The non-running statuses — reaching any of these ends the loop.
    73	TERMINAL_STATUSES: frozenset[str] = frozenset(
    74	    {"done", "budget-hit", "max-iter", "awaiting-approval", "stalled"}
    75	)
    76	
    77	#: ``loop.json`` filename inside each loop directory.
    78	LOOP_JSON: str = "loop.json"
    79	
    80	#: Subdir under ``.renmark/`` that holds every loop's directory.
    81	LOOPS_SUBDIR: str = "loops"
    82	
    83	#: Verification ``completion_state`` value that signals the run finished cleanly.
    84	_COMPLETE_STATE: str = "complete"
    85	
    86	#: Verification metadata values that count as a PASS-equivalent verdict. The
    87	#: verify skill records its goal-backward result in ``validation_status``
    88	#: (``validated`` == PASS) and the body summary; we treat ``validated`` as the
    89	#: machine-readable PASS signal and require ``completion_state == complete``.
    90	_PASS_VALIDATION: frozenset[str] = frozenset({"validated"})
    91	
    92	
    93	# ── Loop state ──────────────────────────────────────────────────────────────
    94	
    95	
    96	@dataclass
    97	class LoopState:
    98	    """Runtime state of one bounded loop. Persisted to ``loop.json``.
    99	
   100	    Pure runtime — the loop's identity/goal plus the live counters. The inner
   101	    orchestrate run keeps its own ``pipeline.json``; this is the *outer* loop's
   102	    state. Kept JSON-trivial (str/int/bool only) so read/write never needs a
   103	    serializer beyond ``json``.
   104	    """
   105	
   106	    goal: str = ""
   107	    verify_cmd: str = ""
   108	    budget_tokens: int = DEFAULT_BUDGET_TOKENS
   109	    budget_usd_estimate: str = ""
   110	    spent_tokens: int = 0
   111	    run_id: str = ""
   112	    max_iterations: int = DEFAULT_MAX_ITERATIONS
   113	    iteration: int = 0
   114	    status: str = "running"  # one of LoopStatus
   115	    pending_step: str = ""  # the REQ-12 gate awaiting approval, if any
   116	
   117	    def to_json(self) -> str:
   118	        return json.dumps(asdict(self), indent=2, sort_keys=False)
   119	
   120	
   121	# ── Paths ────────────────────────────────────────────────────────────────────
   122	
   123	
   124	def loop_id(date: str, slug: str) -> str:
   125	    """Build the canonical loop id ``loop-<date>-<slug>``.
   126	
   127	    ``date`` and ``slug`` are passed in by the caller (no ``datetime.now()`` —
   128	    the loop stays deterministic/testable). The slug is sanitised to a safe
   129	    path component: lowercased, non-alphanumerics collapsed to ``-``, trimmed.
   130	    A blank slug degrades to ``loop``.
   131	    """
   132	    safe = re.sub(r"[^a-z0-9]+", "-", str(slug).strip().lower()).strip("-")
   133	    safe = safe or "loop"
   134	    return f"loop-{str(date).strip()}-{safe}"
   135	
   136	
   137	def loop_dir(repo: str | Path, loop_id_value: str) -> Path:
   138	    """Return ``.renmark/loops/<id>/`` for ``loop_id_value`` (not created)."""
   139	    return Path(repo) / RENMARK_DIR_NAME / LOOPS_SUBDIR / loop_id_value
   140	
   141	
   142	def _loop_json_path(repo: str | Path, loop_id_value: str) -> Path:
   143	    return loop_dir(repo, loop_id_value) / LOOP_JSON
   144	
   145	
   146	# ── loop.json read / write ─────────────────────────────────────────────────
   147	
   148	
   149	def read_loop(repo: str | Path, loop_id_value: str) -> LoopState | None:
   150	    """Return the persisted :class:`LoopState`, or ``None`` if absent/corrupt.
   151	
   152	    Never raises: a missing directory, missing file, unreadable bytes, invalid
   153	    JSON, or a non-dict payload all yield ``None`` (the caller then knows there
   154	    is no resumable loop). Unknown fields are dropped so schema drift can't
   155	    crash the constructor.
   156	    """
   157	    path = _loop_json_path(repo, loop_id_value)
   158	    try:
   159	        if not path.exists():
   160	            return None
   161	        data = json.loads(path.read_text(encoding="utf-8"))
   162	    except (OSError, ValueError):
   163	        return None
   164	    if not isinstance(data, dict):
   165	        return None
   166	    known = set(LoopState.__dataclass_fields__)
   167	    filtered = {k: v for k, v in data.items() if k in known}
   168	    try:
   169	        return LoopState(**filtered)
   170	    except (TypeError, ValueError):
   171	        return None
   172	
   173	
   174	def write_loop(repo: str | Path, loop_id_value: str, state: LoopState) -> Path | None:
   175	    """Persist ``state`` to ``.renmark/loops/<id>/loop.json`` (atomic-ish).
   176	
   177	    Writes to a sibling ``.tmp`` file then ``os.replace``-style renames it over
   178	    the target, so a crash mid-write never leaves a half-written ``loop.json``
   179	    (the loop stays resumable from the last good state). Returns the written
   180	    path, or ``None`` on any IO failure — never raises into the caller.
   181	    """
   182	    path = _loop_json_path(repo, loop_id_value)
   183	    tmp = path.with_suffix(path.suffix + ".tmp")
   184	    try:
   185	        path.parent.mkdir(parents=True, exist_ok=True)
   186	        tmp.write_text(state.to_json(), encoding="utf-8")
   187	        tmp.replace(path)  # atomic on the same filesystem
   188	    except OSError:
   189	        try:
   190	            if tmp.exists():
   191	                tmp.unlink()
   192	        except OSError:
   193	            pass
   194	        return None
   195	    return path
   196	
   197	
   198	# ── Budget parsing ─────────────────────────────────────────────────────────
   199	
   200	
   201	def parse_budget(value: str | int) -> tuple[int, str]:
   202	    """Parse a budget given as EITHER a token count OR a ``$`` amount.
   203	
   204	    Accepts:
   205	    - an ``int`` (e.g. ``300000``) → that many tokens;
   206	    - a numeric string (e.g. ``"300000"``, ``"300_000"``, ``"300,000"``) →
   207	      tokens;
   208	    - a ``$`` string (e.g. ``"$3.00"``, ``"$3"``) → converted to tokens via
   209	      :data:`COST_PER_KTOKEN_USD`.
   210	
   211	    Returns ``(budget_tokens, usd_estimate_str)``. The estimate is always
   212	    derived from the resolved token count via :func:`estimate_usd`, so both the
   213	    token-input and ``$``-input paths report a consistent ``$`` figure.
   214	
   215	    Never raises: a blank / unparseable / non-positive value degrades to
   216	    :data:`DEFAULT_BUDGET_TOKENS` (the safe, bounded default — never unbounded).
   217	    """
   218	    tokens = _coerce_budget_tokens(value)
   219	    if tokens <= 0:
   220	        tokens = DEFAULT_BUDGET_TOKENS
   221	    return tokens, estimate_usd(tokens)
   222	
   223	
   224	def estimate_usd(tokens: int) -> str:
   225	    """Return the ``$`` estimate string for a token budget (``"$N.NN"``).
   226	
   227	    Uses the blended :data:`COST_PER_KTOKEN_USD` rate. Never raises — a
   228	    non-positive / non-int token count yields ``"$0.00"``.
   229	    """
   230	    try:
   231	        t = int(tokens)
   232	    except (TypeError, ValueError):
   233	        return "$0.00"
   234	    if t <= 0:
   235	        return "$0.00"
   236	    dollars = (t / 1000.0) * COST_PER_KTOKEN_USD
   237	    return f"${dollars:.2f}"
   238	
   239	
   240	def _coerce_budget_tokens(value: str | int) -> int:
   241	    """Resolve a raw budget value to a token count. ``<= 0`` / unparseable → 0
   242	    (the caller substitutes the default). Never raises."""
   243	    if isinstance(value, bool):  # bool is an int subclass — reject explicitly.
   244	        return 0
   245	    if isinstance(value, int):
   246	        return value
   247	    if not isinstance(value, str):
   248	        # Defensive: callers are typed str|int, but a runtime caller may pass
   249	        # something else. Degrade to 0 rather than raising on ``.strip()``.
   250	        return 0  # type: ignore[unreachable]
   251	
   252	    raw = value.strip()
   253	    if not raw:
   254	        return 0
   255	
   256	    is_dollar = raw.startswith("$")
   257	    if is_dollar:
   258	        raw = raw[1:].strip()
   259	
   260	    # Allow human-friendly separators in either form.

exec
/bin/bash -lc "nl -ba /home/renmark/projects/ai-system/tests/test_loop.py | sed -n '1,260p'" in /home/renmark/projects/ai-system
 succeeded in 0ms:
     1	"""Unit tests for renmark.loop (Loop Mode state machine).
     2	
     3	Hermetic: every test runs under pytest's ``tmp_path`` and seeds the usage
     4	ledger through the real :func:`renmark.state.append_usage` API rather than
     5	hardcoding the on-disk layout. All magic numbers reference module constants
     6	from ``renmark.loop`` (DEFAULT_BUDGET_TOKENS / DEFAULT_MAX_ITERATIONS /
     7	COST_PER_KTOKEN_USD) so the suite tracks the source of truth.
     8	"""
     9	
    10	from __future__ import annotations
    11	
    12	from pathlib import Path
    13	
    14	import pytest
    15	
    16	from renmark import loop
    17	from renmark.loop import (
    18	    COST_PER_KTOKEN_USD,
    19	    DEFAULT_BUDGET_TOKENS,
    20	    DEFAULT_MAX_ITERATIONS,
    21	    LoopState,
    22	    budget_remaining,
    23	    build_decision,
    24	    estimate_usd,
    25	    loop_id,
    26	    parse_budget,
    27	    read_loop,
    28	    refresh_spent,
    29	    stop_reason,
    30	    write_loop,
    31	)
    32	from renmark.state import UsageRecord, append_usage
    33	
    34	
    35	# ── write_loop / read_loop round-trip ──────────────────────────────────────
    36	
    37	
    38	def test_write_then_read_loop_round_trip(tmp_path: Path) -> None:
    39	    """All fields survive a write -> read cycle under .renmark/loops/<id>/."""
    40	    lid = loop_id("2026-06-09", "Add Auth!")
    41	    state = LoopState(
    42	        goal="ship auth",
    43	        verify_cmd="pytest -q",
    44	        budget_tokens=123_456,
    45	        budget_usd_estimate="$1.23",
    46	        spent_tokens=42,
    47	        run_id="run-abc",
    48	        max_iterations=7,
    49	        iteration=3,
    50	        status="running",
    51	        pending_step="",
    52	    )
    53	
    54	    written = write_loop(tmp_path, lid, state)
    55	    assert written is not None
    56	    # Lands under the canonical .renmark/loops/<id>/loop.json location.
    57	    assert written == loop.loop_dir(tmp_path, lid) / loop.LOOP_JSON
    58	    assert written.exists()
    59	
    60	    loaded = read_loop(tmp_path, lid)
    61	    assert loaded is not None
    62	    assert loaded.goal == "ship auth"
    63	    assert loaded.verify_cmd == "pytest -q"
    64	    assert loaded.budget_tokens == 123_456
    65	    assert loaded.budget_usd_estimate == "$1.23"
    66	    assert loaded.spent_tokens == 42
    67	    assert loaded.run_id == "run-abc"
    68	    assert loaded.max_iterations == 7
    69	    assert loaded.iteration == 3
    70	    assert loaded.status == "running"
    71	    assert loaded.pending_step == ""
    72	
    73	
    74	def test_loop_id_sanitises_slug() -> None:
    75	    assert loop_id("2026-06-09", "Add Auth!") == "loop-2026-06-09-add-auth"
    76	    assert loop_id("2026-06-09", "   ") == "loop-2026-06-09-loop"
    77	
    78	
    79	def test_read_loop_missing_returns_none(tmp_path: Path) -> None:
    80	    assert read_loop(tmp_path, "loop-does-not-exist") is None
    81	
    82	
    83	def test_read_loop_corrupt_returns_none_no_raise(tmp_path: Path) -> None:
    84	    lid = "loop-2026-06-09-corrupt"
    85	    path = loop.loop_dir(tmp_path, lid) / loop.LOOP_JSON
    86	    path.parent.mkdir(parents=True, exist_ok=True)
    87	    path.write_text("{ this is not json", encoding="utf-8")
    88	    assert read_loop(tmp_path, lid) is None
    89	
    90	
    91	def test_read_loop_non_dict_payload_returns_none(tmp_path: Path) -> None:
    92	    lid = "loop-2026-06-09-list"
    93	    path = loop.loop_dir(tmp_path, lid) / loop.LOOP_JSON
    94	    path.parent.mkdir(parents=True, exist_ok=True)
    95	    path.write_text("[1, 2, 3]", encoding="utf-8")
    96	    assert read_loop(tmp_path, lid) is None
    97	
    98	
    99	def test_read_loop_drops_unknown_fields(tmp_path: Path) -> None:
   100	    """Schema drift (an extra key) must not crash the constructor."""
   101	    lid = "loop-2026-06-09-extra"
   102	    path = loop.loop_dir(tmp_path, lid) / loop.LOOP_JSON
   103	    path.parent.mkdir(parents=True, exist_ok=True)
   104	    path.write_text('{"goal": "g", "totally_new_field": 99}', encoding="utf-8")
   105	    loaded = read_loop(tmp_path, lid)
   106	    assert loaded is not None
   107	    assert loaded.goal == "g"
   108	
   109	
   110	# ── parse_budget / estimate_usd ─────────────────────────────────────────────
   111	
   112	
   113	def test_parse_budget_token_int() -> None:
   114	    tokens, usd = parse_budget(250_000)
   115	    assert tokens == 250_000
   116	    assert usd == estimate_usd(250_000)
   117	
   118	
   119	def test_parse_budget_numeric_string() -> None:
   120	    assert parse_budget("300000")[0] == 300_000
   121	    assert parse_budget("300_000")[0] == 300_000
   122	    assert parse_budget("300,000")[0] == 300_000
   123	
   124	
   125	def test_parse_budget_dollar_string_matches_default() -> None:
   126	    """At $0.01/1k tokens, a $3.00 budget resolves to the 300k default."""
   127	    # Guard: this equivalence only holds if the constant is the assumed rate.
   128	    assert COST_PER_KTOKEN_USD == pytest.approx(0.01)
   129	    expected = round((3.00 / COST_PER_KTOKEN_USD) * 1000.0)
   130	    tokens, usd = parse_budget("$3.00")
   131	    assert tokens == expected
   132	    assert tokens == DEFAULT_BUDGET_TOKENS
   133	    assert usd == estimate_usd(tokens)
   134	    # Bare "$3" parses identically.
   135	    assert parse_budget("$3")[0] == expected
   136	
   137	
   138	def test_parse_budget_blank_and_garbage_default() -> None:
   139	    for bad in ("", "   ", "not-a-number", "$", "-5", 0, -100):
   140	        tokens, usd = parse_budget(bad)  # type: ignore[arg-type]
   141	        assert tokens == DEFAULT_BUDGET_TOKENS
   142	        assert usd == estimate_usd(DEFAULT_BUDGET_TOKENS)
   143	
   144	
   145	def test_parse_budget_rejects_bool() -> None:
   146	    """bool is an int subclass but must not be read as a token count."""
   147	    assert parse_budget(True)[0] == DEFAULT_BUDGET_TOKENS  # type: ignore[arg-type]
   148	
   149	
   150	def test_estimate_usd_consistency_both_directions() -> None:
   151	    # token -> $ derivation follows the documented blended rate.
   152	    assert estimate_usd(DEFAULT_BUDGET_TOKENS) == "$3.00"
   153	    assert estimate_usd(100_000) == "$1.00"
   154	    assert estimate_usd(0) == "$0.00"
   155	    assert estimate_usd(-50) == "$0.00"
   156	    # $-string -> tokens -> $ round-trips to the same figure.
   157	    tokens, usd = parse_budget("$3.00")
   158	    assert estimate_usd(tokens) == usd == "$3.00"
   159	
   160	
   161	# ── build_decision ──────────────────────────────────────────────────────────
   162	
   163	
   164	def test_build_decision_goal_reached() -> None:
   165	    meta = {
   166	        "completion_state": "complete",
   167	        "validation_status": "validated",
   168	        "summary_lines": ["all 12 tests pass", "smoke ok"],
   169	        "model_recommendation": "opus",
   170	    }
   171	    decision = build_decision(meta, spent_delta=50_000)
   172	    assert decision["goal_reached"] is True
   173	    assert decision["next_action"] == ""  # no further step when reached
   174	    assert decision["evidence"] == ["all 12 tests pass", "smoke ok"]
   175	    assert decision["model_recommendation"] == "opus"
   176	    assert decision["estimated_next_cost"] == estimate_usd(50_000)
   177	
   178	
   179	def test_build_decision_fail_partial_completion() -> None:
   180	    meta = {
   181	        "completion_state": "partial",
   182	        "validation_status": "validated",
   183	        "next_action": "fix the failing import",
   184	        "summary": "3 of 5 tasks done",
   185	    }
   186	    decision = build_decision(meta, spent_delta=10_000)
   187	    assert decision["goal_reached"] is False
   188	    assert decision["next_action"] == "fix the failing import"
   189	    assert decision["evidence"] == ["3 of 5 tasks done"]
   190	
   191	
   192	def test_build_decision_fail_unvalidated() -> None:
   193	    meta = {
   194	        "completion_state": "complete",
   195	        "validation_status": "failed",
   196	        "next_action": "verifier reported 2 failures",
   197	        "summary_lines": ["FAIL: test_auth"],
   198	    }
   199	    decision = build_decision(meta, spent_delta=0)
   200	    assert decision["goal_reached"] is False
   201	    assert decision["next_action"] == "verifier reported 2 failures"
   202	    assert decision["evidence"] == ["FAIL: test_auth"]
   203	    # Non-positive delta yields the $0.00 estimate.
   204	    assert decision["estimated_next_cost"] == "$0.00"
   205	
   206	
   207	def test_build_decision_reads_only_passed_dict() -> None:
   208	    """No filesystem access: a bare dict in, a decision out. Defaults applied."""
   209	    decision = build_decision({}, spent_delta=0)
   210	    assert decision["goal_reached"] is False
   211	    assert decision["evidence"] == []
   212	    assert decision["model_recommendation"] == "sonnet"  # documented default
   213	
   214	
   215	def test_build_decision_non_dict_meta_degrades() -> None:
   216	    decision = build_decision("not a dict", spent_delta=0)  # type: ignore[arg-type]
   217	    assert decision["goal_reached"] is False
   218	    assert decision["evidence"] == []
   219	
   220	
   221	# ── stop_reason ─────────────────────────────────────────────────────────────
   222	
   223	
   224	@pytest.mark.parametrize(
   225	    "status",
   226	    ["done", "budget-hit", "max-iter", "awaiting-approval", "stalled"],
   227	)
   228	def test_stop_reason_each_terminal_status_is_terminal(status: str) -> None:
   229	    """Every terminal status (including SKILL-set 'stalled') stops the loop."""
   230	    state = LoopState(status=status)
   231	    assert stop_reason(state) == status
   232	
   233	
   234	def test_stop_reason_running_with_room_continues() -> None:
   235	    state = LoopState(
   236	        status="running",
   237	        spent_tokens=10,
   238	        budget_tokens=DEFAULT_BUDGET_TOKENS,
   239	        iteration=1,
   240	        max_iterations=DEFAULT_MAX_ITERATIONS,
   241	        pending_step="",
   242	    )
   243	    assert stop_reason(state) is None
   244	
   245	
   246	def test_stop_reason_budget_hit_when_spent_reaches_budget() -> None:
   247	    state = LoopState(
   248	        status="running",
   249	        spent_tokens=DEFAULT_BUDGET_TOKENS,
   250	        budget_tokens=DEFAULT_BUDGET_TOKENS,
   251	        iteration=0,
   252	        max_iterations=DEFAULT_MAX_ITERATIONS,
   253	    )
   254	    assert stop_reason(state) == "budget-hit"
   255	
   256	
   257	def test_stop_reason_max_iter_when_iteration_reaches_ceiling() -> None:
   258	    state = LoopState(
   259	        status="running",
   260	        spent_tokens=0,

exec
/bin/bash -lc "nl -ba /home/renmark/projects/ai-system/tests/test_loop.py | sed -n '261,420p'" in /home/renmark/projects/ai-system
 succeeded in 0ms:
   261	        budget_tokens=DEFAULT_BUDGET_TOKENS,
   262	        iteration=DEFAULT_MAX_ITERATIONS,
   263	        max_iterations=DEFAULT_MAX_ITERATIONS,
   264	    )
   265	    assert stop_reason(state) == "max-iter"
   266	
   267	
   268	def test_stop_reason_budget_checked_before_max_iter() -> None:
   269	    """Budget ceiling wins when both conditions trip — never exceed spend."""
   270	    state = LoopState(
   271	        status="running",
   272	        spent_tokens=DEFAULT_BUDGET_TOKENS,
   273	        budget_tokens=DEFAULT_BUDGET_TOKENS,
   274	        iteration=DEFAULT_MAX_ITERATIONS,
   275	        max_iterations=DEFAULT_MAX_ITERATIONS,
   276	    )
   277	    assert stop_reason(state) == "budget-hit"
   278	
   279	
   280	def test_stop_reason_pending_step_awaits_approval() -> None:
   281	    state = LoopState(
   282	        status="running",
   283	        spent_tokens=0,
   284	        budget_tokens=DEFAULT_BUDGET_TOKENS,
   285	        iteration=0,
   286	        max_iterations=DEFAULT_MAX_ITERATIONS,
   287	        pending_step="destructive migration",
   288	    )
   289	    assert stop_reason(state) == "awaiting-approval"
   290	
   291	
   292	def test_stop_reason_does_not_derive_stalled_from_blank_next_action() -> None:
   293	    """A running state with room is NOT stalled — stop_reason never inspects
   294	    next_action; 'stalled' is set onto state.status by the driver, not derived
   295	    here. This is the stalled-handling split."""
   296	    state = LoopState(status="running", spent_tokens=0, budget_tokens=DEFAULT_BUDGET_TOKENS)
   297	    assert stop_reason(state) is None
   298	
   299	
   300	# ── refresh_spent / budget_remaining ────────────────────────────────────────
   301	
   302	
   303	def _seed_usage(repo: Path, run_id: str, pairs: list[tuple[int, int]], *, task_id: int = 1) -> None:
   304	    """Append usage records for ``run_id`` via the real ledger API (hermetic)."""
   305	    for i, (pin, pout) in enumerate(pairs):
   306	        append_usage(
   307	            repo,
   308	            UsageRecord(
   309	                ts="2026-06-09T00:00:0%d+00:00" % (i % 10),
   310	                run_id=run_id,
   311	                task_id=task_id,
   312	                model="codex",
   313	                prompt_tokens=pin,
   314	                completion_tokens=pout,
   315	            ),
   316	        )
   317	
   318	
   319	def test_refresh_spent_aggregates_run_id(tmp_path: Path) -> None:
   320	    state = LoopState(run_id="run-xyz", budget_tokens=DEFAULT_BUDGET_TOKENS, spent_tokens=0)
   321	    _seed_usage(tmp_path, "run-xyz", [(1000, 500), (2000, 1500)])
   322	    # A record under a DIFFERENT run_id must not be counted.
   323	    _seed_usage(tmp_path, "run-other", [(9999, 9999)])
   324	
   325	    refreshed = refresh_spent(tmp_path, state)
   326	    assert refreshed is state  # mutates + returns the same object
   327	    assert refreshed.spent_tokens == 1000 + 500 + 2000 + 1500
   328	    assert budget_remaining(refreshed) == DEFAULT_BUDGET_TOKENS - 5000
   329	
   330	
   331	def test_refresh_spent_blank_run_id_untouched(tmp_path: Path) -> None:
   332	    state = LoopState(run_id="", spent_tokens=777)
   333	    refreshed = refresh_spent(tmp_path, state)
   334	    assert refreshed.spent_tokens == 777  # nothing to attribute yet
   335	
   336	
   337	def test_refresh_spent_missing_ledger_no_raise(tmp_path: Path) -> None:
   338	    """No usage.jsonl on disk → spend degrades to 0, never raises."""
   339	    state = LoopState(run_id="run-none", spent_tokens=123)
   340	    refreshed = refresh_spent(tmp_path, state)
   341	    assert refreshed.spent_tokens == 0
   342	
   343	
   344	def test_budget_remaining_never_negative() -> None:
   345	    state = LoopState(budget_tokens=100, spent_tokens=250)
   346	    assert budget_remaining(state) == 0
   347	
   348	
   349	def test_budget_remaining_normal() -> None:
   350	    state = LoopState(budget_tokens=DEFAULT_BUDGET_TOKENS, spent_tokens=100_000)
   351	    assert budget_remaining(state) == DEFAULT_BUDGET_TOKENS - 100_000
   352	
   353	
   354	# ── constant sanity (no hardcoded magic numbers leaked) ─────────────────────
   355	
   356	
   357	def test_module_constants_have_expected_defaults() -> None:
   358	    assert DEFAULT_MAX_ITERATIONS == 5
   359	    assert DEFAULT_BUDGET_TOKENS == 300_000
   360	    assert COST_PER_KTOKEN_USD == pytest.approx(0.01)

exec
/bin/bash -lc "nl -ba /home/renmark/projects/ai-system/plugin/skills/loop/SKILL.md | sed -n '1,240p'" in /home/renmark/projects/ai-system
 succeeded in 0ms:
     1	---
     2	name: loop
     3	description: Use to run a bounded, verified, resumable agentic loop — `/renmark:loop "<goal>"` or "keep iterating until X works", "loop until the goal is met". One upfront approval gate (goal + verify cmd + budget + max-iterations + cost preview), then autonomously cycles orchestrate → verify → decide, committing each passing iteration to the branch, until a terminal status (done / budget-hit / max-iter / awaiting-approval / stalled). Never merges, releases, PRs, or makes destructive changes on its own (REQ-12) — it stops at the gate and hands off to /renmark:finish. Reads only verify metadata + the spend ledger + bounded summaries — never code or diffs.
     4	---
     5	
     6	# loop
     7	
     8	## Overview
     9	
    10	`/renmark:loop` is renmark's bounded execution engine: a **single upfront approval, then
    11	autonomous** loop that wraps `orchestrate → verify → decide` and runs until it reaches a
    12	terminal state. It honors the single-dispatch-gate doctrine — there is **exactly one
    13	approval prompt** for the whole run, never a per-iteration prompt.
    14	
    15	The deterministic state machine lives in `renmark/loop.py` (`LoopState`, `parse_budget`,
    16	`build_decision`, `stop_reason`, `read_loop`/`write_loop`, `refresh_spent`). **This skill
    17	DRIVES the loop; the module OWNS the state.** The driver invokes orchestrate/verify and
    18	builds the decision; the module persists `loop.json`, parses the budget, and evaluates
    19	stop conditions.
    20	
    21	**Context hygiene (REQ-5/11):** the loop reads ONLY verify metadata (`.verification.md`),
    22	the spend ledger (`usage.jsonl` via `usage_by_run_id`), and bounded per-iteration
    23	summaries. It NEVER reads generated code, diffs, or artifact bodies into conversation.
    24	
    25	> Vibe coders never type `/renmark:loop` — `/renmark:start` routes them into Loop Mode
    26	> under the hood and never says "loop." This is the expert surface.
    27	
    28	## When to Use
    29	
    30	- The user wants something built and iterated on **until it verifiably works**, within a
    31	  cost/iteration ceiling, without babysitting each step.
    32	- "Keep going until the tests pass", "loop until the goal is met", "iterate autonomously".
    33	
    34	**Do NOT use:**
    35	- For a single well-scoped task → `/renmark:orchestrate` once.
    36	- To brainstorm/design → `/renmark:brainstorm`.
    37	- To merge/release/PR → that's `/renmark:finish` (the loop stops *before* those, by design).
    38	
    39	## Arguments
    40	
    41	| Flag | Default | Meaning |
    42	|---|---|---|
    43	| `--goal "<text>"` (or positional) | required | The goal the loop verifies backward against. |
    44	| `--verify <cmd>` | (goal-backward only) | A shell verifier run alongside verify's goal-backward smoke. |
    45	| `--budget <tokens\|$amount>` | **300000 tokens** (`DEFAULT_BUDGET_TOKENS`) | Spend ceiling. `parse_budget` accepts a token count (`300000`, `300_000`, `300,000`) OR a `$` amount (`$3.00`) — both resolve to tokens, the measurable unit, with a `$` estimate shown. |
    46	| `--max-iterations <n>` | **5** (`DEFAULT_MAX_ITERATIONS`) | Hard iteration ceiling. |
    47	
    48	Parse the budget once via `renmark.loop.parse_budget(value)` → `(budget_tokens, usd_estimate)`.
    49	A blank / unparseable / non-positive value degrades to the bounded default — never unbounded.
    50	
    51	## Steps
    52	
    53	**Step 0 — Context check.** Call `lifecycle.skill_preamble(repo, 'loop')`. If it returns a
    54	non-None hint, surface it as a one-line note. Also call `read_loop(repo, loop_id)` for any
    55	in-flight loop — if one is `running` or `awaiting-approval`, surface the resume command and
    56	iteration / budget-remaining instead of starting fresh.
    57	
    58	### 1. Single upfront approval gate
    59	
    60	This is the **ONLY approval during the run.** Resolve the budget with `parse_budget`, then
    61	present a cost preview and ask once:
    62	
    63	```
    64	Loop Mode — about to run autonomously to a terminal state.
    65	  Goal:           <goal>
    66	  Verify cmd:     <--verify cmd, or "goal-backward smoke only">
    67	  Budget:         <budget_tokens> tokens  (~<usd_estimate>)
    68	  Max iterations: <n>
    69	  Cost preview:   up to <n> × orchestrate+verify cycles, capped at the budget above
    70	Proceed? [y/N]
    71	```
    72	
    73	If declined, stop — write nothing. **There are NO further prompts until a terminal status**
    74	(single-dispatch-gate doctrine).
    75	
    76	### 2. Initialize the loop
    77	
    78	```python
    79	from renmark import loop
    80	lid = loop.loop_id(date, slug)                 # caller passes date/slug — deterministic
    81	state = loop.LoopState(
    82	    goal=<goal>, verify_cmd=<verify_cmd>,
    83	    budget_tokens=<budget_tokens>, budget_usd_estimate=<usd_estimate>,
    84	    run_id=<run_id>, max_iterations=<n>, iteration=0, status="running",
    85	)
    86	loop.write_loop(repo, lid, state)              # creates .renmark/loops/<id>/, status=running
    87	```
    88	
    89	Also write sibling `goal.md` for provenance. `loop.json` is the runtime sibling of
    90	`pipeline.json` — NOT `lifecycle.json` (G12 / 1KB guard).
    91	
    92	### 3. Drive the loop (autonomous, no prompts)
    93	
    94	While `stop_reason(state)` is `None`:
    95	
    96	1. **Orchestrate** — invoke `/renmark:orchestrate` on the current plan / `next_action`. It
    97	   plans + dispatches, **commits passing work to the feature branch**, and ledgers spend
    98	   under the loop's `run_id`.
    99	2. **Verify** — invoke `/renmark:verify` (goal-backward + the `--verify` cmd). It writes a
   100	   `.verification.md` with machine-readable metadata.
   101	3. **Decide** — read ONLY that metadata (`renmark.summary.read_metadata`) and the ledger
   102	   spend delta; build the decision:
   103	   ```python
   104	   state = loop.refresh_spent(repo, state)     # spent_tokens from usage.jsonl (real spend)
   105	   decision = loop.build_decision(verification_meta, spent_delta)
   106	   ```
   107	4. **Record** — write `iterations/NNN-summary.md` (bounded) and update `loop.json`:
   108	   ```python
   109	   state.iteration += 1
   110	   if decision["goal_reached"]:
   111	       state.status = "done"
   112	   elif not decision["next_action"].strip():
   113	       state.status = "stalled"     # IMPORTANT: the driver derives 'stalled' from an
   114	                                     # empty next_action BEFORE calling stop_reason —
   115	                                     # loop.py delegates that derivation to the driver.
   116	   loop.write_loop(repo, lid, state)           # written before the iteration returns → resumable
   117	   ```
   118	5. **Check** — `stop_reason(state)`. If non-None, the loop is terminal; break.
   119	6. **Progress line** — emit **ONE bounded line** per iteration (≤ summary cap), e.g.
   120	   `iter 2/5 · verify FAIL · spent 80k/300k (~$0.80) · next: add null-guard`.
   121	   **No per-iteration prompt.**
   122	
   123	The budget ceiling is checked first among live stop conditions — an approved budget is
   124	never exceeded; raising it requires a *new* upfront approval.
   125	
   126	### 4. Terminal status → bounded verdict → hand off
   127	
   128	Stop conditions (all terminal):
   129	
   130	| `status` | Trigger |
   131	|---|---|
   132	| `done` | verify PASS — goal verified backward |
   133	| `budget-hit` | ledger spend ≥ approved budget |
   134	| `max-iter` | iteration ≥ `--max-iterations` |
   135	| `awaiting-approval` | a REQ-12 gate (merge/release/PR/destructive/budget-escalation) is pending |
   136	| `stalled` | verify FAIL with no actionable `next_action` / no fresh evidence |
   137	
   138	Report a bounded ≤5-line verdict (status, iterations used, spend vs budget, last evidence),
   139	then hand off to `/renmark:finish` — which carries the **REQ-12 merge/release approval gate**.
   140	
   141	### REQ-12 — autonomous within bounds, gated at the edge
   142	
   143	The loop **commits each passing iteration** to the feature branch (safe / revertable) but
   144	**NEVER merges, releases, opens a PR, escalates the budget, or makes destructive changes
   145	autonomously.** When such a step would be required it sets `pending_step`, reaches
   146	`awaiting-approval`, and stops. AI may generate and commit code; the human owns merges and
   147	releases. Only `/renmark:approve` flips the human-approval bit.
   148	
   149	## Resumability (REQ-10)
   150	
   151	`loop.json` is written before each iteration returns, so a crash / `/clear` / `/compact` /
   152	new session recovers the iteration index, remaining budget, and pending step. After a
   153	clear, `/renmark:resume` detects a `running` / `awaiting-approval` `loop.json` and prints
   154	the resume command + iteration / budget-remaining (zero LLM calls). The loop driver never
   155	raises into chat — a ledger/verify read failure degrades to a clean STOP with a status, not
   156	a crash.
   157	
   158	## Governance compliance
   159	
   160	Upholds G3/G5/G11/G12 and REQ-5/9/10/11/12. The load-bearing invariants are enforced in
   161	`renmark/loop.py`: stop logic degrades toward stopping (never unbounded), `build_decision`
   162	consumes only metadata + the ledger (never code/diffs), `refresh_spent` enforces the budget
   163	against measured spend, and `loop.json` carries runtime state only (not `lifecycle.json`).
   164	
   165	## Next step
   166	
   167	*End by calling `renmark.lifecycle.next_steps(repo, "loop")` and render the
   168	result per `${CLAUDE_PLUGIN_ROOT}/skills/_shared/next-steps.md` (class 1 —
   169	Tier-0 stage routing). Present via `AskUserQuestion` (handoff-menu.md rules
   170	6–9); the state-derived next command (`/renmark:finish` at a terminal loop) is
   171	the `(Recommended)` option. Require an explicit choice — never auto-proceed.*
   172	
   173	Do not paste the rendering rules — cite the file.
   174	
   175	*Mirror any rule changes in `AGENTS.md` in the same commit.*

exec
/bin/bash -lc "nl -ba /home/renmark/projects/ai-system/renmark/state/usage.py | sed -n '1,220p'" in /home/renmark/projects/ai-system
 succeeded in 0ms:
     1	"""Token-usage ledger (.renmark/state/usage.jsonl).
     2	
     3	Codex tasks are ledgered by renmark-execute; Claude-model Agent calls via
     4	`log_agent_call`. /renmark:roadmap reads this file for spend reporting.
     5	"""
     6	
     7	from __future__ import annotations
     8	
     9	import datetime as dt
    10	import json
    11	from dataclasses import asdict, dataclass
    12	from pathlib import Path
    13	from typing import Any
    14	
    15	from ._core import USAGE_LEDGER, new_run_id, now_iso, state_dir
    16	
    17	
    18	@dataclass
    19	class UsageRecord:
    20	    ts: str
    21	    run_id: str
    22	    task_id: int
    23	    model: str
    24	    prompt_tokens: int
    25	    completion_tokens: int
    26	
    27	    def as_jsonl(self) -> str:
    28	        return json.dumps(asdict(self), separators=(",", ":"))
    29	
    30	
    31	def append_usage(repo_root: str | Path, rec: UsageRecord) -> None:
    32	    path = state_dir(repo_root) / USAGE_LEDGER
    33	    with path.open("a", encoding="utf-8") as fh:
    34	        fh.write(rec.as_jsonl() + "\n")
    35	
    36	
    37	def log_agent_call(
    38	    repo_root: str | Path,
    39	    *,
    40	    task_id: int,
    41	    model: str,
    42	    tokens_in: int = 0,
    43	    tokens_out: int = 0,
    44	    run_id: str | None = None,
    45	) -> UsageRecord:
    46	    """Record one Agent-tool call (haiku/sonnet/opus) in usage.jsonl.
    47	
    48	    The orchestrate skill calls this after every Agent return so
    49	    /renmark:roadmap reports honest spend. Codex tasks are ledgered by
    50	    renmark-execute directly; this helper is for the Claude-model path.
    51	
    52	    If `tokens_in` is 0 but `tokens_out` is set, the caller likely only has
    53	    SubagentOutput.token_count (total) — pass it as tokens_out and treat
    54	    tokens_in as the Agent-overhead constant in render code.
    55	    """
    56	    rec = UsageRecord(
    57	        ts=now_iso(),
    58	        run_id=run_id or new_run_id(),
    59	        task_id=int(task_id),
    60	        model=model,
    61	        prompt_tokens=int(tokens_in),
    62	        completion_tokens=int(tokens_out),
    63	    )
    64	    append_usage(repo_root, rec)
    65	    return rec
    66	
    67	
    68	def read_usage(repo_root: str | Path) -> list[dict[str, Any]]:
    69	    path = state_dir(repo_root) / USAGE_LEDGER
    70	    if not path.exists():
    71	        return []
    72	    out: list[dict[str, Any]] = []
    73	    for raw in path.read_text(encoding="utf-8").splitlines():
    74	        raw = raw.strip()
    75	        if not raw:
    76	            continue
    77	        try:
    78	            out.append(json.loads(raw))
    79	        except json.JSONDecodeError:
    80	            continue
    81	    return out
    82	
    83	
    84	def usage_today(repo_root: str | Path) -> int:
    85	    today = dt.datetime.now(dt.timezone.utc).date().isoformat()
    86	    total = 0
    87	    for r in read_usage(repo_root):
    88	        if r.get("ts", "").startswith(today):
    89	            total += int(r.get("prompt_tokens", 0)) + int(r.get("completion_tokens", 0))
    90	    return total
    91	
    92	
    93	def usage_this_month(repo_root: str | Path) -> int:
    94	    prefix = dt.datetime.now(dt.timezone.utc).strftime("%Y-%m")
    95	    total = 0
    96	    for r in read_usage(repo_root):
    97	        if r.get("ts", "").startswith(prefix):
    98	            total += int(r.get("prompt_tokens", 0)) + int(r.get("completion_tokens", 0))
    99	    return total
   100	
   101	
   102	def usage_by_run_id(repo: str | Path, run_id: str) -> int:
   103	    """Total tokens (prompt + completion) ledgered under one run_id.
   104	
   105	    The spend-measurement primitive behind the loop budget gate. Pure and
   106	    defensive: a missing, empty, or corrupt ledger — or any unreadable line —
   107	    yields 0 rather than raising. Bad lines are skipped (`read_usage` already
   108	    drops JSON-undecodable rows); malformed token fields are coerced to 0.
   109	    """
   110	    total = 0
   111	    try:
   112	        records = read_usage(repo)
   113	    except OSError:
   114	        return 0
   115	    for r in records:
   116	        if r.get("run_id") != run_id:
   117	            continue
   118	        try:
   119	            total += int(r.get("prompt_tokens", 0)) + int(r.get("completion_tokens", 0))
   120	        except (TypeError, ValueError):
   121	            continue
   122	    return total

exec
/bin/bash -lc "nl -ba /home/renmark/projects/ai-system/renmark/state/__init__.py | sed -n '1,200p'" in /home/renmark/projects/ai-system
 succeeded in 0ms:
     1	"""Persistent state for renmark-execute.
     2	
     3	Split into cohesive submodules (v0.3.2). This package re-exports the full
     4	public surface so `from renmark import state; state.X` and
     5	`from renmark.state import X` both keep working exactly as before the split.
     6	
     7	Submodules:
     8	- _core     paths, time helpers, dir-rotation primitive
     9	- usage     token-usage ledger
    10	- pause     PAUSED file + escalation buckets
    11	- pipeline  pipeline.json runtime state + wave summaries
    12	- logs      per-invocation troubleshooting logs
    13	- commits   completed-task detection via git log
    14	- skills    skill-invocation tracking + context-budget check
    15	"""
    16	
    17	from __future__ import annotations
    18	
    19	from ._core import (
    20	    ARCHIVE_SUBDIR,
    21	    DEBUG_SUBDIR,
    22	    ESCALATIONS_DIR,
    23	    ESCALATIONS_KEEP,
    24	    LAST_SKILL_FILE,
    25	    LOGS_KEEP,
    26	    LOGS_SUBDIR,
    27	    MEMORY_SUBDIR,
    28	    PAUSED_FILE,
    29	    PIPELINE_JSON,
    30	    RENMARK_DIR_NAME,
    31	    STATE_DIR_NAME,
    32	    STATE_SUBDIR,
    33	    USAGE_LEDGER,
    34	    WAVE_SUMMARIES_KEEP,
    35	    WAVE_SUMMARIES_SUBDIR,
    36	    new_run_id,
    37	    now_iso,
    38	    rotate_dir,
    39	    state_dir,
    40	)
    41	from .commits import completed_task_indices
    42	from .logs import (
    43	    append_log,
    44	    logs_dir,
    45	    open_log,
    46	    recent_logs,
    47	)
    48	from .pause import (
    49	    PauseState,
    50	    clear_pause,
    51	    escalation_dir,
    52	    read_pause,
    53	    write_pause,
    54	)
    55	from .pipeline import (
    56	    PipelineState,
    57	    clear_pipeline_state,
    58	    list_wave_summaries,
    59	    pipeline_is_resumable,
    60	    read_pipeline_state,
    61	    read_wave_summary,
    62	    write_pipeline_state,
    63	    write_wave_summary,
    64	)
    65	from .skills import (
    66	    context_budget_check,
    67	    last_skill_invocation,
    68	    record_skill_invocation,
    69	)
    70	from .usage import (
    71	    UsageRecord,
    72	    append_usage,
    73	    log_agent_call,
    74	    read_usage,
    75	    usage_by_run_id,
    76	    usage_this_month,
    77	    usage_today,
    78	)
    79	
    80	__all__ = [
    81	    "ARCHIVE_SUBDIR",
    82	    "DEBUG_SUBDIR",
    83	    "ESCALATIONS_DIR",
    84	    "ESCALATIONS_KEEP",
    85	    "LAST_SKILL_FILE",
    86	    "LOGS_KEEP",
    87	    "LOGS_SUBDIR",
    88	    "MEMORY_SUBDIR",
    89	    "PAUSED_FILE",
    90	    "PIPELINE_JSON",
    91	    # _core
    92	    "RENMARK_DIR_NAME",
    93	    "STATE_DIR_NAME",
    94	    "STATE_SUBDIR",
    95	    "USAGE_LEDGER",
    96	    "WAVE_SUMMARIES_KEEP",
    97	    "WAVE_SUMMARIES_SUBDIR",
    98	    # pause
    99	    "PauseState",
   100	    # pipeline
   101	    "PipelineState",
   102	    # usage
   103	    "UsageRecord",
   104	    "append_log",
   105	    "append_usage",
   106	    "clear_pause",
   107	    "clear_pipeline_state",
   108	    # commits
   109	    "completed_task_indices",
   110	    "context_budget_check",
   111	    "escalation_dir",
   112	    "last_skill_invocation",
   113	    "list_wave_summaries",
   114	    "log_agent_call",
   115	    # logs
   116	    "logs_dir",
   117	    "new_run_id",
   118	    "now_iso",
   119	    "open_log",
   120	    "pipeline_is_resumable",
   121	    "read_pause",
   122	    "read_pipeline_state",
   123	    "read_usage",
   124	    "read_wave_summary",
   125	    "recent_logs",
   126	    # skills
   127	    "record_skill_invocation",
   128	    "rotate_dir",
   129	    "state_dir",
   130	    "usage_by_run_id",
   131	    "usage_this_month",
   132	    "usage_today",
   133	    "write_pause",
   134	    "write_pipeline_state",
   135	    "write_wave_summary",
   136	]

exec
/bin/bash -lc "rg -n \"next_action|validation_status|completion_state|summary_lines|usage_by_run_id\" /home/renmark/projects/ai-system/renmark /home/renmark/projects/ai-system/plugin/skills -g '"'!plugin/skills/loop/SKILL.md'"'" in /home/renmark/projects/ai-system
 succeeded in 0ms:
/home/renmark/projects/ai-system/plugin/skills/orchestrate/SKILL.md:22:- Each subagent emits only the `SubagentOutput` schema (status, artifact_path, touched_files, sha, summary_lines ≤ 5, dependency_notes, token_count, completion_state, confidence, retry_count).
/home/renmark/projects/ai-system/plugin/skills/orchestrate/SKILL.md:121:>  "summary_lines": ["<≤5 lines>"], "dependency_notes": "<what downstream tasks need>",
/home/renmark/projects/ai-system/plugin/skills/orchestrate/SKILL.md:122:>  "token_count": <int>, "completion_state": "complete|partial|failed",
/home/renmark/projects/ai-system/plugin/skills/orchestrate/SKILL.md:169:**Built:** <what was implemented (from task's summary_lines)>
/home/renmark/projects/ai-system/plugin/skills/orchestrate/SKILL.md:176:Use the task's `summary_lines` and `touched_files` from `SubagentOutput` to fill these — never go read the actual file diff.
/home/renmark/projects/ai-system/plugin/skills/CONTRIBUTING.md:19:| G9 | Failure transparency | Artifact metadata sets `completion_state`, `confidence`, `validation_status`, `retry_count`, `parser_success`, `schema_compliance` honestly. Skills that succeed half-way return `partial`, not `complete`. |
/home/renmark/projects/ai-system/plugin/skills/CONTRIBUTING.md:33:7. **Does my output say `validation_status: validated` when nothing actually validated it?** If yes, redesign — be honest about uncertainty.
/home/renmark/projects/ai-system/plugin/skills/_shared/handoff-menu.md:56:   `source_sha == git_head_sha(repo)` and `completion_state == "complete"`.
/home/renmark/projects/ai-system/plugin/skills/verify/SKILL.md:123:    summary_lines=[
/home/renmark/projects/ai-system/plugin/skills/verify/SKILL.md:133:    completion_state="complete" if passed == total else "partial",
/home/renmark/projects/ai-system/plugin/skills/verify/SKILL.md:135:    validation_status="validated",
/home/renmark/projects/ai-system/plugin/skills/verify/SKILL.md:330:    summary_lines=verdict_lines,  # the ≤5-line block above
/home/renmark/projects/ai-system/plugin/skills/verify/SKILL.md:334:    completion_state="complete" if all_hard_pass else "partial",
/home/renmark/projects/ai-system/plugin/skills/verify/SKILL.md:336:    validation_status="validated",
/home/renmark/projects/ai-system/plugin/skills/verify/SKILL.md:404:    and md.get("completion_state") == "complete"
/home/renmark/projects/ai-system/plugin/skills/verify/SKILL.md:497:    summary_lines=verdict_lines,  # the ≤5 lines above
/home/renmark/projects/ai-system/plugin/skills/verify/SKILL.md:501:    completion_state="complete" if all_pass else "partial",
/home/renmark/projects/ai-system/plugin/skills/verify/SKILL.md:503:    validation_status="validated",
/home/renmark/projects/ai-system/plugin/skills/brainstorm/SKILL.md:113:    summary_lines=[                    # ≤5 lines — the ONLY thing the conversation sees
/home/renmark/projects/ai-system/plugin/skills/brainstorm/SKILL.md:122:    validation_status="unvalidated",
/home/renmark/projects/ai-system/renmark/dispatch.py:183:    or when its summary_lines exceed the G3 cap. The orchestrator refuses to
/home/renmark/projects/ai-system/renmark/dispatch.py:195:        "summary_lines",
/home/renmark/projects/ai-system/renmark/dispatch.py:198:        "completion_state",
/home/renmark/projects/ai-system/renmark/dispatch.py:245:    summary_lines: list[str] = field(default_factory=list)
/home/renmark/projects/ai-system/renmark/dispatch.py:248:    completion_state: Literal["complete", "partial", "failed"] = "complete"
/home/renmark/projects/ai-system/renmark/dispatch.py:255:        if len(self.summary_lines) > SUBAGENT_MAX_SUMMARY_LINES:
/home/renmark/projects/ai-system/renmark/dispatch.py:257:                f"SubagentOutput.summary_lines has {len(self.summary_lines)} entries; "
/home/renmark/projects/ai-system/renmark/dispatch.py:260:        for i, line in enumerate(self.summary_lines):
/home/renmark/projects/ai-system/renmark/dispatch.py:262:                raise IsolationViolation(f"SubagentOutput.summary_lines[{i}] is {type(line).__name__}; expected str")
/home/renmark/projects/ai-system/renmark/dispatch.py:265:                    f"SubagentOutput.summary_lines[{i}] is {len(line)} chars; max {SUBAGENT_MAX_CHARS_PER_LINE} (G3)"
/home/renmark/projects/ai-system/renmark/dispatch.py:269:        if self.completion_state not in SUBAGENT_OUTPUT_COMPLETION_STATES:
/home/renmark/projects/ai-system/renmark/dispatch.py:270:            raise IsolationViolation(f"SubagentOutput.completion_state={self.completion_state!r} invalid")
/home/renmark/projects/ai-system/renmark/dispatch.py:280:            "summary_lines": list(self.summary_lines),
/home/renmark/projects/ai-system/renmark/dispatch.py:283:            "completion_state": self.completion_state,
/home/renmark/projects/ai-system/renmark/loop.py:17:  gate reads the token ledger via ``usage_by_run_id`` — never the conversation.
/home/renmark/projects/ai-system/renmark/loop.py:40:from .state import RENMARK_DIR_NAME, usage_by_run_id
/home/renmark/projects/ai-system/renmark/loop.py:69:    "stalled",  # no fresh evidence / empty next_action (terminal)
/home/renmark/projects/ai-system/renmark/loop.py:83:#: Verification ``completion_state`` value that signals the run finished cleanly.
/home/renmark/projects/ai-system/renmark/loop.py:87:#: verify skill records its goal-backward result in ``validation_status``
/home/renmark/projects/ai-system/renmark/loop.py:89:#: machine-readable PASS signal and require ``completion_state == complete``.
/home/renmark/projects/ai-system/renmark/loop.py:295:          "goal_reached": bool,            # completion_state==complete AND PASS
/home/renmark/projects/ai-system/renmark/loop.py:297:          "next_action": str,              # "" when goal reached / no step
/home/renmark/projects/ai-system/renmark/loop.py:303:    with empty evidence (the stop logic then treats an empty ``next_action`` as
/home/renmark/projects/ai-system/renmark/loop.py:308:    completion = _meta_str(meta, "completion_state")
/home/renmark/projects/ai-system/renmark/loop.py:309:    validation = _meta_str(meta, "validation_status")
/home/renmark/projects/ai-system/renmark/loop.py:315:    # actionable next step the verify metadata recorded. A blank next_action is
/home/renmark/projects/ai-system/renmark/loop.py:317:    next_action = "" if goal_reached else _meta_str(meta, "next_action")
/home/renmark/projects/ai-system/renmark/loop.py:333:        "next_action": next_action,
/home/renmark/projects/ai-system/renmark/loop.py:342:    Prefers an explicit ``summary_lines`` list; falls back to a single
/home/renmark/projects/ai-system/renmark/loop.py:346:    raw = meta.get("summary_lines")
/home/renmark/projects/ai-system/renmark/loop.py:384:    ``next_action`` (no fresh evidence) maps to ``"stalled"`` — but that signal
/home/renmark/projects/ai-system/renmark/loop.py:423:    reads ``usage.jsonl`` via :func:`usage_by_run_id` (which itself never
/home/renmark/projects/ai-system/renmark/loop.py:429:        state.spent_tokens = usage_by_run_id(repo, run_id)
/home/renmark/projects/ai-system/renmark/state/__init__.py:75:    usage_by_run_id,
/home/renmark/projects/ai-system/renmark/state/__init__.py:130:    "usage_by_run_id",
/home/renmark/projects/ai-system/renmark/state/usage.py:102:def usage_by_run_id(repo: str | Path, run_id: str) -> int:
/home/renmark/projects/ai-system/renmark/init.py:1593:    summary_lines = [
/home/renmark/projects/ai-system/renmark/init.py:1612:        summary_lines.append(f"HEALTH: {', '.join(parts)} — see `.renmark/memory/dev-standards.md`")
/home/renmark/projects/ai-system/renmark/init.py:1613:    return 0, "\n".join(summary_lines)
/home/renmark/projects/ai-system/renmark/schemas.py:130:    "summary_lines": list,
/home/renmark/projects/ai-system/renmark/schemas.py:133:    "completion_state": str,
/home/renmark/projects/ai-system/renmark/schemas.py:168:    cs = data.get("completion_state")
/home/renmark/projects/ai-system/renmark/schemas.py:170:        issues.append(f"subagent.completion_state={cs!r} not in {sorted(SUBAGENT_OUTPUT_COMPLETION_STATES)}")
/home/renmark/projects/ai-system/renmark/schemas.py:176:    summary = data.get("summary_lines")
/home/renmark/projects/ai-system/renmark/schemas.py:179:            issues.append(f"subagent.summary_lines has {len(summary)} entries — G3 cap is 5")
/home/renmark/projects/ai-system/renmark/schemas.py:182:                issues.append(f"subagent.summary_lines[{i}] expected str, got {type(line).__name__}")
/home/renmark/projects/ai-system/renmark/schemas.py:184:                issues.append(f"subagent.summary_lines[{i}] is {len(line)} chars — G3 cap is 1200")
/home/renmark/projects/ai-system/renmark/schemas.py:200:    "completion_state": (str, False),
/home/renmark/projects/ai-system/renmark/schemas.py:202:    "validation_status": (str, False),
/home/renmark/projects/ai-system/renmark/schemas.py:220:    cs = data.get("completion_state")
/home/renmark/projects/ai-system/renmark/schemas.py:222:        issues.append(f"artifact.completion_state={cs!r} not in {sorted(ARTIFACT_COMPLETION_STATES)}")
/home/renmark/projects/ai-system/renmark/schemas.py:226:    vs = data.get("validation_status")
/home/renmark/projects/ai-system/renmark/schemas.py:228:        issues.append(f"artifact.validation_status={vs!r} not in {sorted(ARTIFACT_VALIDATION_STATES)}")
/home/renmark/projects/ai-system/renmark/cli/commands.py:123:                    "summary_lines": [f"task spec not found at {spec_path}"],
/home/renmark/projects/ai-system/renmark/cli/commands.py:124:                    "completion_state": "failed",
/home/renmark/projects/ai-system/renmark/cli/commands.py:145:                    "summary_lines": [
/home/renmark/projects/ai-system/renmark/cli/commands.py:149:                    "completion_state": "failed",
/home/renmark/projects/ai-system/renmark/cli/commands.py:188:                    "summary_lines": ["codex timed out after 600s"],
/home/renmark/projects/ai-system/renmark/cli/commands.py:189:                    "completion_state": "failed",
/home/renmark/projects/ai-system/renmark/cli/commands.py:205:                    "summary_lines": [
/home/renmark/projects/ai-system/renmark/cli/commands.py:209:                    "completion_state": "failed",
/home/renmark/projects/ai-system/renmark/cli/commands.py:226:        "summary_lines": pointer.splitlines()[1:6],  # skip header line, take ≤5
/home/renmark/projects/ai-system/renmark/cli/commands.py:229:        "completion_state": "complete",
/home/renmark/projects/ai-system/renmark/state/pipeline.py:133:    artifact_path, summary_lines, dependency_notes, etc.). The orchestrator
/home/renmark/projects/ai-system/renmark/summary.py:35:    """Raised when summary_lines exceeds the G3 cap (lines or per-line tokens)."""
/home/renmark/projects/ai-system/renmark/summary.py:54:    completion_state: str = "complete"  # complete | partial | failed
/home/renmark/projects/ai-system/renmark/summary.py:56:    validation_status: str = "unvalidated"  # validated | unvalidated | failed
/home/renmark/projects/ai-system/renmark/summary.py:81:        lines.append(f"completion_state: {self.completion_state}")
/home/renmark/projects/ai-system/renmark/summary.py:83:        lines.append(f"validation_status: {self.validation_status}")
/home/renmark/projects/ai-system/renmark/summary.py:99:    summary_lines: Iterable[str],
/home/renmark/projects/ai-system/renmark/summary.py:105:    completion_state: str = "complete",
/home/renmark/projects/ai-system/renmark/summary.py:107:    validation_status: str = "unvalidated",
/home/renmark/projects/ai-system/renmark/summary.py:114:    Raises SummaryBoundaryError if summary_lines exceeds the G3 cap.
/home/renmark/projects/ai-system/renmark/summary.py:117:    summary_lines_list = list(summary_lines)
/home/renmark/projects/ai-system/renmark/summary.py:118:    _validate_summary(summary_lines_list)
/home/renmark/projects/ai-system/renmark/summary.py:127:        completion_state=completion_state,
/home/renmark/projects/ai-system/renmark/summary.py:129:        validation_status=validation_status,
/home/renmark/projects/ai-system/renmark/summary.py:135:    summary_block = "\n".join(f"- {line}" for line in summary_lines_list)
/home/renmark/projects/ai-system/renmark/summary.py:179:        f"({meta.get('completion_state', '?')}, "
/home/renmark/projects/ai-system/renmark/summary.py:310:def _validate_summary(summary_lines: list[str]) -> None:
/home/renmark/projects/ai-system/renmark/summary.py:311:    if len(summary_lines) > MAX_SUMMARY_LINES:
/home/renmark/projects/ai-system/renmark/summary.py:312:        raise SummaryBoundaryError(f"summary_lines has {len(summary_lines)} entries; max {MAX_SUMMARY_LINES} (G3)")
/home/renmark/projects/ai-system/renmark/summary.py:313:    for i, line in enumerate(summary_lines):
/home/renmark/projects/ai-system/renmark/summary.py:316:                f"summary_lines[{i}] is {len(line)} chars; "
/home/renmark/projects/ai-system/renmark/lifecycle.py:464:    ``completion_state == 'complete'``; any gate without one is "not run".
/home/renmark/projects/ai-system/renmark/lifecycle.py:493:                    and meta.get("completion_state") == "complete"

codex
I found the loop code’s immediate dependencies. I’m now checking whether `next_action` is actually emitted by the verify path and whether the artifact schema accepts it, because that is the key place the stalled split can turn from “documented contract” into “silent infinite loop.”
exec
/bin/bash -lc "nl -ba /home/renmark/projects/ai-system/renmark/summary.py | sed -n '1,220p'" in /home/renmark/projects/ai-system
 succeeded in 0ms:
     1	"""Artifact hygiene helper — enforces G3 (summary boundary), G6 (artifact
     2	governance metadata), G9 (failure transparency).
     3	
     4	Every renmark skill that emits a long-running result writes through this
     5	module. Artifacts are written to disk with YAML frontmatter metadata + body
     6	+ a structured ``## Summary`` section. The orchestrator reads only the
     7	metadata + Summary (never the body).
     8	
     9	Default orchestrator-visible output cap: 5 lines OR ~300 tokens per line.
    10	Violations raise ValueError at write time — they are bugs, not warnings.
    11	"""
    12	
    13	from __future__ import annotations
    14	
    15	import hashlib
    16	import subprocess
    17	from collections.abc import Iterable
    18	from dataclasses import dataclass, field
    19	from datetime import datetime, timezone
    20	from pathlib import Path
    21	from typing import Any
    22	
    23	from renmark.verifier import run_verifier
    24	
    25	SCHEMA_VERSION = "1"
    26	MAX_SUMMARY_LINES = 5
    27	MAX_TOKENS_PER_LINE = 300  # ~4 chars/token rule-of-thumb
    28	MAX_CHARS_PER_LINE = MAX_TOKENS_PER_LINE * 4  # 1200 chars
    29	
    30	
    31	# ── Errors ────────────────────────────────────────────────────────────────────
    32	
    33	
    34	class SummaryBoundaryError(ValueError):
    35	    """Raised when summary_lines exceeds the G3 cap (lines or per-line tokens)."""
    36	
    37	
    38	# ── Metadata block ────────────────────────────────────────────────────────────
    39	
    40	
    41	@dataclass
    42	class ArtifactMetadata:
    43	    """G6 + G9 metadata carried at the top of every artifact."""
    44	
    45	    artifact_type: str
    46	    schema_version: str = SCHEMA_VERSION
    47	    created_at: str = ""
    48	    source_sha: str | None = None
    49	    related_plan: str | None = None
    50	    generator: str = "unknown"
    51	    stale_after: str | None = None
    52	    dependency_refs: list[str] = field(default_factory=list)
    53	    # G9 transparency:
    54	    completion_state: str = "complete"  # complete | partial | failed
    55	    confidence: str = "medium"  # low | medium | high
    56	    validation_status: str = "unvalidated"  # validated | unvalidated | failed
    57	    retry_count: int = 0
    58	    parser_success: bool = True
    59	    schema_compliance: bool = True
    60	
    61	    def __post_init__(self) -> None:
    62	        if not self.created_at:
    63	            self.created_at = datetime.now(timezone.utc).isoformat(timespec="seconds")
    64	
    65	    def to_yaml(self) -> str:
    66	        """Render as the YAML frontmatter block of an artifact."""
    67	        lines = ["---"]
    68	        lines.append(f"artifact_type: {self.artifact_type}")
    69	        lines.append(f"schema_version: {self.schema_version}")
    70	        lines.append(f"created_at: {self.created_at}")
    71	        lines.append(f"source_sha: {self.source_sha or 'null'}")
    72	        lines.append(f"related_plan: {self.related_plan or 'null'}")
    73	        lines.append(f"generator: {self.generator}")
    74	        lines.append(f"stale_after: {self.stale_after or 'null'}")
    75	        if self.dependency_refs:
    76	            lines.append("dependency_refs:")
    77	            for ref in self.dependency_refs:
    78	                lines.append(f"  - {ref}")
    79	        else:
    80	            lines.append("dependency_refs: []")
    81	        lines.append(f"completion_state: {self.completion_state}")
    82	        lines.append(f"confidence: {self.confidence}")
    83	        lines.append(f"validation_status: {self.validation_status}")
    84	        lines.append(f"retry_count: {self.retry_count}")
    85	        lines.append(f"parser_success: {str(self.parser_success).lower()}")
    86	        lines.append(f"schema_compliance: {str(self.schema_compliance).lower()}")
    87	        lines.append("---")
    88	        return "\n".join(lines)
    89	
    90	
    91	# ── Public API ────────────────────────────────────────────────────────────────
    92	
    93	
    94	def write_artifact(
    95	    path: Path | str,
    96	    *,
    97	    artifact_type: str,
    98	    body: str,
    99	    summary_lines: Iterable[str],
   100	    related_plan: str | None = None,
   101	    source_sha: str | None = None,
   102	    generator: str = "unknown",
   103	    stale_after: str | None = None,
   104	    dependency_refs: list[str] | None = None,
   105	    completion_state: str = "complete",
   106	    confidence: str = "medium",
   107	    validation_status: str = "unvalidated",
   108	    retry_count: int = 0,
   109	    parser_success: bool = True,
   110	    schema_compliance: bool = True,
   111	) -> Path:
   112	    """Write a renmark artifact: metadata + body + ``## Summary`` section.
   113	
   114	    Raises SummaryBoundaryError if summary_lines exceeds the G3 cap.
   115	    Returns the resolved Path.
   116	    """
   117	    summary_lines_list = list(summary_lines)
   118	    _validate_summary(summary_lines_list)
   119	
   120	    meta = ArtifactMetadata(
   121	        artifact_type=artifact_type,
   122	        related_plan=related_plan,
   123	        source_sha=source_sha,
   124	        generator=generator,
   125	        stale_after=stale_after,
   126	        dependency_refs=list(dependency_refs or []),
   127	        completion_state=completion_state,
   128	        confidence=confidence,
   129	        validation_status=validation_status,
   130	        retry_count=retry_count,
   131	        parser_success=parser_success,
   132	        schema_compliance=schema_compliance,
   133	    )
   134	
   135	    summary_block = "\n".join(f"- {line}" for line in summary_lines_list)
   136	    content = f"{meta.to_yaml()}\n\n{body.rstrip()}\n\n## Summary\n\n{summary_block}\n"
   137	
   138	    out = Path(path)
   139	    out.parent.mkdir(parents=True, exist_ok=True)
   140	    out.write_text(content, encoding="utf-8")
   141	    return out
   142	
   143	
   144	def emit_pointer(path: Path | str, label: str, *, n_lines: int = MAX_SUMMARY_LINES) -> str:
   145	    """Read ONLY the metadata + ``## Summary`` section. Returns a bounded
   146	    string suitable for orchestrator output. Caps at G3 limits regardless of
   147	    what's in the file — never returns the full body.
   148	    """
   149	    p = Path(path)
   150	    if not p.exists():
   151	        return f"{label}: artifact missing at {p}"
   152	
   153	    text = p.read_text(encoding="utf-8")
   154	    meta = read_metadata(p)
   155	
   156	    # Extract Summary section
   157	    summary_idx = text.find("## Summary")
   158	    if summary_idx == -1:
   159	        return f"{label}: artifact {p} missing required '## Summary' section"
   160	
   161	    summary_block = text[summary_idx:].splitlines()
   162	    # Skip the "## Summary" header and any blank lines; take next n_lines bullet lines
   163	    bullets: list[str] = []
   164	    for line in summary_block[1:]:
   165	        stripped = line.strip()
   166	        if not stripped:
   167	            continue
   168	        if stripped.startswith("##"):
   169	            break
   170	        bullets.append(stripped)
   171	        if len(bullets) >= n_lines:
   172	            break
   173	
   174	    # Truncate over-long bullets to stay within token cap
   175	    bullets = [(b[: MAX_CHARS_PER_LINE - 1] + "…") if len(b) > MAX_CHARS_PER_LINE else b for b in bullets]
   176	
   177	    header = (
   178	        f"{label}: {meta.get('artifact_type', '?')} "
   179	        f"({meta.get('completion_state', '?')}, "
   180	        f"confidence={meta.get('confidence', '?')}) → {p}"
   181	    )
   182	    return "\n".join([header, *bullets])
   183	
   184	
   185	def read_metadata(path: Path | str) -> dict[str, Any]:
   186	    """Parse the YAML frontmatter block at the top of an artifact.
   187	
   188	    Returns an empty dict[str, Any] if the file lacks frontmatter. Does NOT pull in
   189	    a YAML library — we control the writer's format, so a tiny line parser
   190	    is sufficient and dependency-free.
   191	    """
   192	    p = Path(path)
   193	    if not p.exists():
   194	        return {}
   195	
   196	    text = p.read_text(encoding="utf-8")
   197	    if not text.startswith("---"):
   198	        return {}
   199	
   200	    end = text.find("\n---", 3)
   201	    if end == -1:
   202	        return {}
   203	
   204	    block = text[3:end].strip("\n")
   205	    result: dict[str, Any] = {}
   206	    current_list_key: str | None = None
   207	    for raw in block.splitlines():
   208	        line = raw.rstrip()
   209	        if not line:
   210	            continue
   211	        if line.startswith("  - ") and current_list_key:
   212	            result[current_list_key].append(line[4:].strip())
   213	            continue
   214	        current_list_key = None
   215	        if ": " not in line and not line.endswith(":"):
   216	            continue
   217	        if line.endswith(":"):
   218	            key = line[:-1].strip()
   219	            result[key] = []
   220	            current_list_key = key

exec
/bin/bash -lc "nl -ba /home/renmark/projects/ai-system/renmark/schemas.py | sed -n '180,260p'" in /home/renmark/projects/ai-system
 succeeded in 0ms:
   180	        for i, line in enumerate(summary):
   181	            if not isinstance(line, str):
   182	                issues.append(f"subagent.summary_lines[{i}] expected str, got {type(line).__name__}")
   183	            elif len(line) > 1200:
   184	                issues.append(f"subagent.summary_lines[{i}] is {len(line)} chars — G3 cap is 1200")
   185	
   186	    return issues
   187	
   188	
   189	# ── ArtifactMetadata (G6) ────────────────────────────────────────────────────
   190	
   191	ARTIFACT_METADATA_FIELDS = {
   192	    "artifact_type": (str, True),
   193	    "schema_version": ((str, int), True),
   194	    "created_at": (str, True),
   195	    "source_sha": ((str, type(None)), False),
   196	    "related_plan": ((str, type(None)), False),
   197	    "generator": (str, True),
   198	    "stale_after": ((str, type(None)), False),
   199	    "dependency_refs": (list, False),
   200	    "completion_state": (str, False),
   201	    "confidence": (str, False),
   202	    "validation_status": (str, False),
   203	    "retry_count": (int, False),
   204	    "parser_success": (bool, False),
   205	    "schema_compliance": (bool, False),
   206	}
   207	
   208	ARTIFACT_COMPLETION_STATES = {"complete", "partial", "failed"}
   209	ARTIFACT_CONFIDENCES = {"low", "medium", "high"}
   210	ARTIFACT_VALIDATION_STATES = {"validated", "unvalidated", "failed"}
   211	
   212	
   213	def validate_artifact_metadata(data: Any) -> list[str]:
   214	    issues: list[str] = []
   215	    if not isinstance(data, dict):
   216	        return [f"artifact: expected object, got {type(data).__name__}"]
   217	
   218	    issues.extend(_check_fields(data, ARTIFACT_METADATA_FIELDS, "artifact"))
   219	
   220	    cs = data.get("completion_state")
   221	    if isinstance(cs, str) and cs not in ARTIFACT_COMPLETION_STATES:
   222	        issues.append(f"artifact.completion_state={cs!r} not in {sorted(ARTIFACT_COMPLETION_STATES)}")
   223	    conf = data.get("confidence")
   224	    if isinstance(conf, str) and conf not in ARTIFACT_CONFIDENCES:
   225	        issues.append(f"artifact.confidence={conf!r} not in {sorted(ARTIFACT_CONFIDENCES)}")
   226	    vs = data.get("validation_status")
   227	    if isinstance(vs, str) and vs not in ARTIFACT_VALIDATION_STATES:
   228	        issues.append(f"artifact.validation_status={vs!r} not in {sorted(ARTIFACT_VALIDATION_STATES)}")
   229	
   230	    return issues
   231	
   232	
   233	# ── Helpers ──────────────────────────────────────────────────────────────────
   234	
   235	
   236	def _check_fields(data: dict[str, Any], spec: dict[str, tuple[Any, ...]], scope: str) -> list[str]:
   237	    issues: list[str] = []
   238	    for field, (expected_type, required) in spec.items():
   239	        if field not in data:
   240	            if required:
   241	                issues.append(f"{scope}: required field {field!r} missing")
   242	            continue
   243	        if not _isinstance(data[field], expected_type):
   244	            issues.append(f"{scope}.{field} expected {_typename(expected_type)}, got {type(data[field]).__name__}")
   245	    return issues
   246	
   247	
   248	def _isinstance(value: Any, expected: Any) -> bool:
   249	    # bool is a subclass of int — exclude it when expecting int.
   250	    if expected is int and isinstance(value, bool):
   251	        return False
   252	    if isinstance(expected, tuple):
   253	        if int in expected and bool not in expected and isinstance(value, bool):
   254	            return False
   255	        return isinstance(value, expected)
   256	    return isinstance(value, expected)
   257	
   258	
   259	def _typename(expected: Any) -> str:
   260	    if isinstance(expected, tuple):

exec
/bin/bash -lc "nl -ba /home/renmark/projects/ai-system/plugin/skills/verify/SKILL.md | sed -n '110,180p'" in /home/renmark/projects/ai-system
 succeeded in 0ms:
   110	Failed: search entries — run /renmark:debug with symptom: "search exits 1: no such table: entries"
   111	```
   112	
   113	### 4. Emit verification artifact (G6)
   114	
   115	Write `.renmark/reviews/YYYY-MM-DD-<sha>.verification.md` via `summary.write_artifact`:
   116	
   117	```python
   118	from renmark import summary
   119	summary.write_artifact(
   120	    artifact_path,
   121	    artifact_type="verification",
   122	    body=full_test_log,  # all commands + bounded outputs
   123	    summary_lines=[
   124	        f"{passed}/{total} behaviors verified",
   125	        f"feature: {feature_name}",
   126	        f"failed: {', '.join(failed_names) or 'none'}",
   127	        f"regressions checked: {n_regressions}",
   128	        f"next: {recommended_next_command}",
   129	    ],
   130	    related_plan=plan_path,
   131	    source_sha=summary.git_head_sha(repo),
   132	    generator="verify",
   133	    completion_state="complete" if passed == total else "partial",
   134	    confidence="high" if passed == total else "medium",
   135	    validation_status="validated",
   136	)
   137	```
   138	
   139	Emit only the pointer to the orchestrator's conversation via `summary.emit_pointer(artifact_path, "verify")`.
   140	
   141	### 5. Compounding learnings + bug logging (G8)
   142	
   143	On EVERY run (pass or fail):
   144	
   145	```python
   146	from renmark.memory import append_learning
   147	append_learning(
   148	    repo,
   149	    signal=f"verify-{feature_name}",
   150	    observation=(
   151	        f"{passed}/{total} behaviors verified; "
   152	        f"failed: {','.join(failed_names) or 'none'}; "
   153	        f"regressions: {n_regressions}"
   154	    ),
   155	    source=str(artifact_path),
   156	    model="verify",
   157	)
   158	```
   159	
   160	On any FAIL:
   161	
   162	```python
   163	from renmark.memory import log_bug
   164	for failed in failed_tests:
   165	    log_bug(
   166	        repo,
   167	        title=f"verify failure: {failed.name}",
   168	        severity="medium",  # promote to high if it's a regression of a closed bug
   169	        symptom=failed.bounded_output,
   170	        root_cause="(unknown — route to /renmark:debug)",
   171	        fix="(pending)",
   172	        lesson=f"smoke test '{failed.name}' failed during verify of {feature_name}",
   173	    )
   174	```
   175	
   176	This is what makes verification compound. Every failed verify expands the next verify's regression set; every passed verify accrues confidence in the routing memory.
   177	
   178	### 6. Lifecycle update
   179	
   180	```python

exec
/bin/bash -lc "nl -ba /home/renmark/projects/ai-system/plugin/skills/verify/SKILL.md | sed -n '320,360p'" in /home/renmark/projects/ai-system
 succeeded in 0ms:
   320	
   321	### Artifact (G6)
   322	
   323	Write `.renmark/reviews/YYYY-MM-DD-<sha>.qa.md` via `summary.write_artifact`:
   324	
   325	```python
   326	summary.write_artifact(
   327	    qa_artifact_path,
   328	    artifact_type="qa",
   329	    body=full_step_log,  # screenshot paths + console slice + network slice + step-by-step
   330	    summary_lines=verdict_lines,  # the ≤5-line block above
   331	    related_plan=plan_path,
   332	    source_sha=summary.git_head_sha(repo),
   333	    generator="verify-qa",
   334	    completion_state="complete" if all_hard_pass else "partial",
   335	    confidence="high" if all_hard_pass else "medium",
   336	    validation_status="validated",
   337	)
   338	summary.emit_pointer(qa_artifact_path, "verify --qa")
   339	```
   340	
   341	### Stop and report on break (could-not-finish)
   342	
   343	If the flow cannot finish, the run **STOPS at that step**, marks the flow FAILED, names the failing step + criterion in the verdict, and does **NOT** silently continue or report success. Trigger conditions:
   344	
   345	- the page fails to load,
   346	- an action hangs (infinite spinner / never reaches the expected state within the bounded `wait_for`),
   347	- the browser becomes unresponsive,
   348	- an uncaught exception fires,
   349	- the layout is visibly broken (overlap / clipping — criterion 6).
   350	
   351	Such visual/layout and "could-not-finish" failures are logged to `bugs.md` via the existing `memory.log_bug` convergence loop below, exactly like functional failures — a reproducible finding (symptom + offending criterion + screenshot path + repro steps). The early exit ties into the same bounded-verdict + artifact + learnings flow; do not invent a parallel reporting path.
   352	
   353	### DURING QA — run the selected flow
   354	
   355	Run the **selected flow** (from *BEFORE QA — flow selection from memory*) using the existing browser-channel behavior already documented above — do not duplicate or weaken those steps:
   356	
   357	- Drive it through the active channel's tools per *The single happy-path flow* (navigate / snapshot / click / fill / wait / screenshot / console / network).
   358	- Capture the **before/after screenshots** around the main action and the agent-observed visual comparison exactly as documented there.
   359	- Capture `list_console_messages` / `list_network_requests` to the artifact body.
   360	- Enforce the **visual/layout integrity** check (criterion 6 — overlapping / clipped controls, off-viewport elements) per *Happy-path pass criteria*.

exec
/bin/bash -lc "nl -ba /home/renmark/projects/ai-system/plugin/skills/verify/SKILL.md | sed -n '488,515p'" in /home/renmark/projects/ai-system
 succeeded in 0ms:
   488	### Deep QA artifact
   489	
   490	Write `.renmark/reviews/YYYY-MM-DD-<sha>.deep-qa.md` via `summary.write_artifact`:
   491	
   492	```python
   493	summary.write_artifact(
   494	    deep_artifact_path,
   495	    artifact_type="deep-qa",
   496	    body=full_run_log,  # the full ranked candidate list + per-case screenshots + console/network slices
   497	    summary_lines=verdict_lines,  # the ≤5 lines above
   498	    related_plan=plan_path,
   499	    source_sha=summary.git_head_sha(repo),
   500	    generator="verify-deep-qa",
   501	    completion_state="complete" if all_pass else "partial",
   502	    confidence="medium",  # edge cases are inherently lower-confidence than the happy path
   503	    validation_status="validated",
   504	)
   505	```
   506	
   507	### Deep QA convergence
   508	
   509	Same as `--qa`: every fail (including a stop-on-break early exit) logs a reproducible `bugs.md` entry via `memory.log_bug` (symptom + offending criterion + screenshot path + repro steps); every run appends a learning. The next `verify --qa` picks up the new bug entry in its regression set, so a Deep QA finding becomes a recurring smoke check too.
   510	
   511	### Why serial-in-main, not subagents
   512	
   513	At 1 (`--qa`) + 3 (`--deep-qa`) flows that each dump evidence to disk and return only verdict lines, the main context never holds heavy payloads. Subagent fan-out buys nothing here and adds coordination cost against a singleton browser. Revisit only if a future version needs *many* journeys where verdict-line + orchestration overhead pressure the window.
   514	
   515	### Deep QA hand-off

exec
/bin/bash -lc 'rg -n "next_action" /home/renmark/projects/ai-system/plugin/skills/verify/SKILL.md /home/renmark/projects/ai-system/renmark' in /home/renmark/projects/ai-system
 succeeded in 0ms:
/home/renmark/projects/ai-system/renmark/loop.py:69:    "stalled",  # no fresh evidence / empty next_action (terminal)
/home/renmark/projects/ai-system/renmark/loop.py:297:          "next_action": str,              # "" when goal reached / no step
/home/renmark/projects/ai-system/renmark/loop.py:303:    with empty evidence (the stop logic then treats an empty ``next_action`` as
/home/renmark/projects/ai-system/renmark/loop.py:315:    # actionable next step the verify metadata recorded. A blank next_action is
/home/renmark/projects/ai-system/renmark/loop.py:317:    next_action = "" if goal_reached else _meta_str(meta, "next_action")
/home/renmark/projects/ai-system/renmark/loop.py:333:        "next_action": next_action,
/home/renmark/projects/ai-system/renmark/loop.py:384:    ``next_action`` (no fresh evidence) maps to ``"stalled"`` — but that signal

codex
I found one likely hard bug in `parse_budget`: huge numeric inputs can go through `float(...)` to `inf`/`nan`, and the later `int(...)`/`round(...)` path is not caught. I’m confirming the exact Python behavior before I classify it.
exec
/bin/bash -lc 'git diff --unified=40 main..HEAD -- /home/renmark/projects/ai-system/plugin/commands/loop.md /home/renmark/projects/ai-system/plugin/skills/start/SKILL.md /home/renmark/projects/ai-system/plugin/skills/resume/SKILL.md' in /home/renmark/projects/ai-system
 succeeded in 0ms:
diff --git a/plugin/commands/loop.md b/plugin/commands/loop.md
new file mode 100644
index 0000000..afc3cd5
--- /dev/null
+++ b/plugin/commands/loop.md
@@ -0,0 +1,8 @@
+---
+description: Use to run a bounded agentic loop — `/renmark:loop` or "loop on this until it passes", "keep iterating until the verifier is green". Drives a goal toward a verifier under an explicit budget and max-iteration cap, re-running the verifier each pass and stopping on success, budget exhaustion, or the iteration ceiling. Persists loop state so the run survives `/clear`.
+argument-hint: '[goal] [--verifier <cmd>] [--budget <n>] [--max-iterations <n>]'
+---
+
+Read `${CLAUDE_PLUGIN_ROOT}/skills/loop/SKILL.md` and follow its instructions exactly. The user provided this input: $ARGUMENTS
+
+If `$ARGUMENTS` is empty, begin the loop skill's flow.
diff --git a/plugin/skills/resume/SKILL.md b/plugin/skills/resume/SKILL.md
index 91cfe4d..6b79971 100644
--- a/plugin/skills/resume/SKILL.md
+++ b/plugin/skills/resume/SKILL.md
@@ -35,90 +35,135 @@ Call `lifecycle.skill_preamble(repo, 'resume')`. Resume is a `meta` domain skill
 python3 -c "
 from pathlib import Path
 from renmark import lifecycle
 state = lifecycle.read_lifecycle(Path('.'))
 if state is None:
     print('No in-flight feature.')
     print('Recommended: /renmark:start  (begin a new feature)')
 else:
     print(f'Feature: {state.feature}')
     print(f'Branch:  {state.branch}')
     print(f'Stage:   {state.stage}')
     if state.stages_completed:
         print(f'Done:    {\", \".join(state.stages_completed)}')
     if state.human_review_required and not state.human_review_completed:
         print(f'⚠  Awaiting human approval for: {state.human_review_for}')
         print(f'Recommended: /renmark:approve')
     else:
         print(f'Recommended: {state.next_recommended}')
 "
 ```
 
 ### 1.5 Validate artifact freshness
 
 Cross-check that every artifact referenced in lifecycle state still exists and is current. Still **zero LLM calls** — one extra file read per artifact (frontmatter only).
 
 ```bash
 python3 -c "
 from pathlib import Path
 from renmark.lifecycle import validate_artifact_refs
 issues = validate_artifact_refs(Path('.'))
 for i in issues:
     icon = '❌ BLOCK' if i['severity'] == 'BLOCK' else '⚠ WARN'
     print(f\"{icon} {i['kind']}: {i['artifact']} @ {i['path']} — {i['detail']}\")
 if any(i['severity'] == 'BLOCK' for i in issues):
     raise SystemExit(2)
 "
 ```
 
 Each WARN prints as `⚠ <kind>: <artifact> @ <path>`; each BLOCK prints as `❌ BLOCK <kind>: <artifact> @ <path>`. On any BLOCK the skill exits non-zero (code 2) so the user investigates before continuing.
 
+### 1.75 Surface any in-flight loop
+
+A `/renmark:loop` run persists its runtime state to `.renmark/loops/<id>/loop.json`
+(the outer-loop sibling of `pipeline.json` — never `lifecycle.json`). On resume,
+if any loop directory holds a `loop.json` whose `status` is `running` or
+`awaiting-approval`, surface it so a `/clear` mid-loop is recoverable. Still
+**zero LLM calls** — pure file IO: glob the loop dirs, read each `loop.json` via
+`renmark.loop.read_loop`, no source reads, no analysis.
+
+```bash
+python3 -c "
+from pathlib import Path
+from renmark.loop import read_loop, budget_remaining, estimate_usd, LOOPS_SUBDIR
+from renmark.state import RENMARK_DIR_NAME
+loops_root = Path('.') / RENMARK_DIR_NAME / LOOPS_SUBDIR
+for d in sorted(loops_root.glob('loop-*')) if loops_root.is_dir() else []:
+    st = read_loop('.', d.name)
+    if st is None or st.status not in ('running', 'awaiting-approval'):
+        continue
+    remaining = budget_remaining(st)
+    print(f'⟳  Loop in flight: {d.name}  [{st.status}]')
+    print(f'   Iteration: {st.iteration}/{st.max_iterations}')
+    print(f'   Budget:    {remaining} tokens left ({estimate_usd(remaining)} of {estimate_usd(st.budget_tokens)})')
+    if st.pending_step:
+        print(f'   Pending:   {st.pending_step}')
+    print(f'   Resume:    /renmark:loop --resume {d.name}')
+"
+```
+
+Output, when a loop is in flight (one block per active loop, ≤5 lines each):
+
+```
+⟳  Loop in flight: <loop-id>  [running|awaiting-approval]
+   Iteration: <iteration>/<max_iterations>
+   Budget:    <remaining> tokens left (<$remaining> of <$budget>)
+   Pending:   <pending_step>            # only if a REQ-12 gate is awaiting approval
+   Resume:    /renmark:loop --resume <loop-id>
+```
+
+If **no** loop is in flight, this step prints nothing and resume behaves exactly
+as it does today — the lifecycle recovery below is unchanged. A loop in
+`awaiting-approval` is a pending gate: the lifecycle path (Step 2) still drives
+the `/renmark:approve` recommendation; this block only adds the `--resume` hint
+so the user knows which loop to re-enter after approving.
+
 ### 2. Surface pending approval gates
 
 If `human_review_required` is true and `human_review_completed` is false, the user MUST be told about the pending gate before any other recommendation. The next action is always `/renmark:approve` until the gate is cleared.
 
 ### 3. Print the recommendation
 
 Surface ≤ 5 lines total. The orchestrator's output for `/renmark:resume` MUST stay within G3 bounds — this is a one-glance status check, not a report.
 
 Format:
 
 ```
 Feature: <feature-name>
 Stage:   <stage>
 Done:    <comma-separated completed stages>
 Next:    <recommended command>
 ```
 
 If pending approval:
 
 ```
 Feature: <feature-name>
 Stage:   <stage>
 ⚠  Approval pending: <human_review_for>
 Next:    /renmark:approve
 ```
 
 If no feature in flight:
 
 ```
 No feature in flight.
 Next: /renmark:start
 ```
 
 ### 4. No state changes
 
 `/renmark:resume` writes nothing except the skill-invocation marker (Step 0). It does not mutate `lifecycle.json`, does not advance any stage, does not start any subprocess. The user reads the output and decides what to do.
 
 ## Hand off (wizard step)
 
 Resume is a reporting skill — there is no automatic handoff. The user reads the recommendation and invokes it themselves. This is intentional: the human is the final merge authority (principle #7), and any automatic chaining from resume would undermine that.
 
 Resume is a **class 3 (aux / terminal) skill** in the next-step contract. It already derives and prints the recommended next command from `lifecycle.json` (Steps 1–3) — that recommendation IS the contract's class-3 **resume-pipeline** option. For the canonical hand-off format and class definition, see — by static reference, never pasted, and **without changing resume's zero-LLM / pure-file-IO cold-start logic**:
 
 > *Render the printed recommendation per `${CLAUDE_PLUGIN_ROOT}/skills/_shared/next-steps.md` (class 3 — resume-pipeline + 1–2 local actions). The in-flight feature's next command is `(Recommended)`. Resume is zero-LLM, so it uses the printed numbered form (handoff-menu.md rule 7), not the interactive `AskUserQuestion` picker — and never auto-proceeds.*
 
 ## Governance compliance
 
-Resume IS the G7/G10/G12 recovery surface — it reads `lifecycle.json` (+ `pipeline.json` for `--resume` hints) and recommends the next step in ≤5 lines (G3), zero LLM calls, writing no workflow state. Other G-rules are N/A (it dispatches nothing and emits no artifact). See `CLAUDE.md` governance rules for definitions.
+Resume IS the G7/G10/G12 recovery surface — it reads `lifecycle.json` (+ `pipeline.json` for `--resume` hints, + each `.renmark/loops/<id>/loop.json` for in-flight loop recovery) and recommends the next step in ≤5 lines per surface (G3), zero LLM calls, writing no workflow state. Other G-rules are N/A (it dispatches nothing and emits no artifact). See `CLAUDE.md` governance rules for definitions.
 
 - Step 1.5 reads frontmatter from each referenced artifact (`spec`, `plan`, …) to cross-check `source_sha` + `stale_after`. Still zero LLM calls; bounded output (one line per issue, at most a few issues per healthy lifecycle).
diff --git a/plugin/skills/start/SKILL.md b/plugin/skills/start/SKILL.md
index 3293221..8fa50cc 100644
--- a/plugin/skills/start/SKILL.md
+++ b/plugin/skills/start/SKILL.md
@@ -69,80 +69,98 @@ Skip both questions for clearly scoped requests. Never ask about tech stack, arc
 Apply silently using these defaults:
 
 | Signal | Default stack |
 |---|---|
 | "script", "automate", "batch", "process" | Python |
 | "CLI", "command line", "developer tool" | Python or Node.js CLI |
 | "web app", "dashboard", "CRUD", "admin" | Node.js + Express + SQLite |
 | "data", "CSV", "analysis", "ML" | Python |
 | "page", "site", "landing" | Static HTML/JS + Vite |
 | "real-time", "chat", "live" | Node.js + Socket.io |
 | "mobile", "iOS", "Android" | React Native |
 
 **Deployment default:**
 - Local unless "others", "public", "team", "online", or "server" was mentioned
 - If local: no auth, no deployment config, no cloud setup
 - If deployable: include basic deployment notes and .env config
 
 **MVP scope default:**
 - First version = simplest thing that does the described job
 - Exclude: admin panels, analytics, payment integrations, user roles, mobile apps — unless explicitly requested
 
 ---
 
 ### 5. Confirm in plain language
 
 Present a summary before building:
 
 > "Here's what I'll build:
 >
 > **[1–2 sentence plain-English description of the output]**
 >
 > Stack: [inferred stack, briefly]
 > Scope: [what's included, what's not for this version]
 >
 > I'll also include error handling, a README with setup instructions, and basic tests so it works out of the box.
 >
 > Ready? [Y/n — or tell me if anything looks off]"
 
 Wait for explicit confirmation before continuing. If the user redirects, adjust the summary and confirm again. Do not proceed on silence.
 
+**Implementation note (internal — never shown to the user):** the vibe-coder
+build path MAY run as a bounded **Loop Mode** under the hood — the
+iterate-until-verified engine that keeps refining until the smoke test passes or
+a budget is hit. This is an implementation detail of how `start` drives the
+build; it does NOT add a third question. Fold the loop's two knobs into the
+single Step-5 confirmation already shown above:
+
+- **Budget** — a sensible default effort ceiling for the iterate cycle.
+- **Max iterations** — a sensible default cap on refinement passes.
+
+Pick sensible defaults silently and state them plainly inside the existing
+summary (e.g. *"I'll keep refining until the tests pass, within a sensible
+effort budget"*). Confirm goal + budget + max-iterations in that ONE plain-English
+confirmation — do NOT add a fourth bullet that reads like a new interrogation,
+and do NOT surface the word "loop", "iteration budget", or any engine jargon to
+the user. Once confirmed, drive the bounded loop and route normally (Step 7).
+The word "loop" stays inside these internal notes only.
+
 ---
 
 ### 5a. Offer PRD creation (new projects only)
 
 Check whether `PRD.md` exists at the project root. If it does **not** exist, offer once — do not block:
 
 > "Would you like me to create a `PRD.md` for this project? It captures the goal, users, and success criteria as a living source of truth — useful when the project grows or when handing off context.
 >
 > 1. [a] Yes — run `/renmark:prd` now before we continue
 > 2. [b] Skip — continue with the normal next step"
 
 - If the user chooses **[a]**: invoke `/renmark:prd` (create mode), then return here and continue to Step 6 once it completes.
 - If the user chooses **[b]**, or if `PRD.md` already exists: skip silently and continue.
 
 Do not mention this step if `PRD.md` already exists. Do not block or repeat the offer.
 
 ---
 
 ### 5b. Offer blueprint generation (new projects only)
 
 Check whether `SCHEMATIC.md` exists at the project root. If it does **not** exist, offer once — do not block:
 
 > "Would you like me to generate a project blueprint via `/renmark:blueprint`? It produces a living `SCHEMATIC.md` (architecture overview, module map, data flow) — and a `PROTOTYPE.html` interactive mock-up if the build has a UI.
 >
 > 1. [a] Yes — run `/renmark:blueprint` now before we continue
 > 2. [b] Skip — continue with the normal next step"
 
 - If the user chooses **[a]**: invoke `/renmark:blueprint`, then return here and continue to Step 6 once it completes.
 - If the user chooses **[b]**, or if `SCHEMATIC.md` already exists: skip silently and continue.
 
 Do not mention this step if `SCHEMATIC.md` already exists. Do not block or repeat the offer.
 
 ---
 
 ### 6. Inject best practices into plan context
 
 Before routing to the next step, establish these as non-negotiable task requirements. Do NOT add them as separate tasks — weave them into the task specs for the relevant implementation tasks:
 
 **Always:**
 - Error handling with plain-language user messages (no raw stack traces)

exec
/bin/bash -lc "python - <<'PY'
vals = ['1e309', '"'$1e309'"', 'nan', '"'$nan'"', 'inf', '"'$inf'"']
for raw in vals:
    s = raw[1:] if raw.startswith('"'$'"') else raw
    try:
        amount = float(s)
        print(raw, 'float', amount)
        try:
            if raw.startswith('"'$'"'):
                x = round((amount / 0.01) * 1000.0)
            else:
                x = int(amount)
            print('  coerced', x)
        except Exception as e:
            print('  coerce_exc', type(e).__name__, str(e))
    except Exception as e:
        print(raw, 'float_exc', type(e).__name__, str(e))
PY" in /home/renmark/projects/ai-system
 succeeded in 0ms:
1e309 float inf
  coerce_exc OverflowError cannot convert float infinity to integer
$1e309 float inf
  coerce_exc OverflowError cannot convert float infinity to integer
nan float nan
  coerce_exc ValueError cannot convert float NaN to integer
$nan float nan
  coerce_exc ValueError cannot convert float NaN to integer
inf float inf
  coerce_exc OverflowError cannot convert float infinity to integer
$inf float inf
  coerce_exc OverflowError cannot convert float infinity to integer

exec
/bin/bash -lc "rg -n \"awaiting-approval|pending_step|loop.json|read_loop\\(|write_loop\\(|refresh_spent\\(|stop_reason\\(|parse_budget\\(\" /home/renmark/projects/ai-system/plugin /home/renmark/projects/ai-system/renmark" in /home/renmark/projects/ai-system
 succeeded in 0ms:
/home/renmark/projects/ai-system/renmark/loop.py:9:module persists ``loop.json``, parses the budget, builds the per-iteration
/home/renmark/projects/ai-system/renmark/loop.py:19:  missing or corrupt ``loop.json`` degrades to a fresh default (or ``None``),
/home/renmark/projects/ai-system/renmark/loop.py:25:- **Bounded + resumable.** ``loop.json`` is written before each iteration
/home/renmark/projects/ai-system/renmark/loop.py:62:#: Terminal + active loop statuses persisted in ``loop.json``.
/home/renmark/projects/ai-system/renmark/loop.py:68:    "awaiting-approval",  # a REQ-12 gate is pending (terminal until /renmark:approve)
/home/renmark/projects/ai-system/renmark/loop.py:74:    {"done", "budget-hit", "max-iter", "awaiting-approval", "stalled"}
/home/renmark/projects/ai-system/renmark/loop.py:77:#: ``loop.json`` filename inside each loop directory.
/home/renmark/projects/ai-system/renmark/loop.py:78:LOOP_JSON: str = "loop.json"
/home/renmark/projects/ai-system/renmark/loop.py:98:    """Runtime state of one bounded loop. Persisted to ``loop.json``.
/home/renmark/projects/ai-system/renmark/loop.py:115:    pending_step: str = ""  # the REQ-12 gate awaiting approval, if any
/home/renmark/projects/ai-system/renmark/loop.py:142:def _loop_json_path(repo: str | Path, loop_id_value: str) -> Path:
/home/renmark/projects/ai-system/renmark/loop.py:146:# ── loop.json read / write ─────────────────────────────────────────────────
/home/renmark/projects/ai-system/renmark/loop.py:149:def read_loop(repo: str | Path, loop_id_value: str) -> LoopState | None:
/home/renmark/projects/ai-system/renmark/loop.py:157:    path = _loop_json_path(repo, loop_id_value)
/home/renmark/projects/ai-system/renmark/loop.py:174:def write_loop(repo: str | Path, loop_id_value: str, state: LoopState) -> Path | None:
/home/renmark/projects/ai-system/renmark/loop.py:175:    """Persist ``state`` to ``.renmark/loops/<id>/loop.json`` (atomic-ish).
/home/renmark/projects/ai-system/renmark/loop.py:178:    the target, so a crash mid-write never leaves a half-written ``loop.json``
/home/renmark/projects/ai-system/renmark/loop.py:182:    path = _loop_json_path(repo, loop_id_value)
/home/renmark/projects/ai-system/renmark/loop.py:201:def parse_budget(value: str | int) -> tuple[int, str]:
/home/renmark/projects/ai-system/renmark/loop.py:369:def stop_reason(state: LoopState) -> str | None:
/home/renmark/projects/ai-system/renmark/loop.py:378:    4. a pending REQ-12 gate (non-empty ``pending_step``) → ``"awaiting-approval"``.
/home/renmark/projects/ai-system/renmark/loop.py:380:       empty ``pending_step`` with status ``"done"``) → ``"done"``.
/home/renmark/projects/ai-system/renmark/loop.py:401:        pending = (getattr(state, "pending_step", "") or "").strip()
/home/renmark/projects/ai-system/renmark/loop.py:409:            return "awaiting-approval"
/home/renmark/projects/ai-system/renmark/loop.py:419:def refresh_spent(repo: str | Path, state: LoopState) -> LoopState:
/home/renmark/projects/ai-system/plugin/skills/loop/SKILL.md:3:description: Use to run a bounded, verified, resumable agentic loop — `/renmark:loop "<goal>"` or "keep iterating until X works", "loop until the goal is met". One upfront approval gate (goal + verify cmd + budget + max-iterations + cost preview), then autonomously cycles orchestrate → verify → decide, committing each passing iteration to the branch, until a terminal status (done / budget-hit / max-iter / awaiting-approval / stalled). Never merges, releases, PRs, or makes destructive changes on its own (REQ-12) — it stops at the gate and hands off to /renmark:finish. Reads only verify metadata + the spend ledger + bounded summaries — never code or diffs.
/home/renmark/projects/ai-system/plugin/skills/loop/SKILL.md:18:builds the decision; the module persists `loop.json`, parses the budget, and evaluates
/home/renmark/projects/ai-system/plugin/skills/loop/SKILL.md:48:Parse the budget once via `renmark.loop.parse_budget(value)` → `(budget_tokens, usd_estimate)`.
/home/renmark/projects/ai-system/plugin/skills/loop/SKILL.md:54:non-None hint, surface it as a one-line note. Also call `read_loop(repo, loop_id)` for any
/home/renmark/projects/ai-system/plugin/skills/loop/SKILL.md:55:in-flight loop — if one is `running` or `awaiting-approval`, surface the resume command and
/home/renmark/projects/ai-system/plugin/skills/loop/SKILL.md:86:loop.write_loop(repo, lid, state)              # creates .renmark/loops/<id>/, status=running
/home/renmark/projects/ai-system/plugin/skills/loop/SKILL.md:89:Also write sibling `goal.md` for provenance. `loop.json` is the runtime sibling of
/home/renmark/projects/ai-system/plugin/skills/loop/SKILL.md:94:While `stop_reason(state)` is `None`:
/home/renmark/projects/ai-system/plugin/skills/loop/SKILL.md:104:   state = loop.refresh_spent(repo, state)     # spent_tokens from usage.jsonl (real spend)
/home/renmark/projects/ai-system/plugin/skills/loop/SKILL.md:107:4. **Record** — write `iterations/NNN-summary.md` (bounded) and update `loop.json`:
/home/renmark/projects/ai-system/plugin/skills/loop/SKILL.md:116:   loop.write_loop(repo, lid, state)           # written before the iteration returns → resumable
/home/renmark/projects/ai-system/plugin/skills/loop/SKILL.md:118:5. **Check** — `stop_reason(state)`. If non-None, the loop is terminal; break.
/home/renmark/projects/ai-system/plugin/skills/loop/SKILL.md:135:| `awaiting-approval` | a REQ-12 gate (merge/release/PR/destructive/budget-escalation) is pending |
/home/renmark/projects/ai-system/plugin/skills/loop/SKILL.md:145:autonomously.** When such a step would be required it sets `pending_step`, reaches
/home/renmark/projects/ai-system/plugin/skills/loop/SKILL.md:146:`awaiting-approval`, and stops. AI may generate and commit code; the human owns merges and
/home/renmark/projects/ai-system/plugin/skills/loop/SKILL.md:151:`loop.json` is written before each iteration returns, so a crash / `/clear` / `/compact` /
/home/renmark/projects/ai-system/plugin/skills/loop/SKILL.md:153:clear, `/renmark:resume` detects a `running` / `awaiting-approval` `loop.json` and prints
/home/renmark/projects/ai-system/plugin/skills/loop/SKILL.md:163:against measured spend, and `loop.json` carries runtime state only (not `lifecycle.json`).
/home/renmark/projects/ai-system/plugin/skills/resume/SKILL.md:77:A `/renmark:loop` run persists its runtime state to `.renmark/loops/<id>/loop.json`
/home/renmark/projects/ai-system/plugin/skills/resume/SKILL.md:79:if any loop directory holds a `loop.json` whose `status` is `running` or
/home/renmark/projects/ai-system/plugin/skills/resume/SKILL.md:80:`awaiting-approval`, surface it so a `/clear` mid-loop is recoverable. Still
/home/renmark/projects/ai-system/plugin/skills/resume/SKILL.md:81:**zero LLM calls** — pure file IO: glob the loop dirs, read each `loop.json` via
/home/renmark/projects/ai-system/plugin/skills/resume/SKILL.md:91:    st = read_loop('.', d.name)
/home/renmark/projects/ai-system/plugin/skills/resume/SKILL.md:92:    if st is None or st.status not in ('running', 'awaiting-approval'):
/home/renmark/projects/ai-system/plugin/skills/resume/SKILL.md:98:    if st.pending_step:
/home/renmark/projects/ai-system/plugin/skills/resume/SKILL.md:99:        print(f'   Pending:   {st.pending_step}')
/home/renmark/projects/ai-system/plugin/skills/resume/SKILL.md:107:⟳  Loop in flight: <loop-id>  [running|awaiting-approval]
/home/renmark/projects/ai-system/plugin/skills/resume/SKILL.md:110:   Pending:   <pending_step>            # only if a REQ-12 gate is awaiting approval
/home/renmark/projects/ai-system/plugin/skills/resume/SKILL.md:116:`awaiting-approval` is a pending gate: the lifecycle path (Step 2) still drives
/home/renmark/projects/ai-system/plugin/skills/resume/SKILL.md:167:Resume IS the G7/G10/G12 recovery surface — it reads `lifecycle.json` (+ `pipeline.json` for `--resume` hints, + each `.renmark/loops/<id>/loop.json` for in-flight loop recovery) and recommends the next step in ≤5 lines per surface (G3), zero LLM calls, writing no workflow state. Other G-rules are N/A (it dispatches nothing and emits no artifact). See `CLAUDE.md` governance rules for definitions.

codex
I have enough static context for the main bugs. I’m running the focused loop tests once to confirm the current suite passes while still missing the failure modes above; that helps separate “untested” from “currently failing.”
exec
/bin/bash -lc 'pytest -q /home/renmark/projects/ai-system/tests/test_loop.py' in /home/renmark/projects/ai-system
 exited 1 in 378ms:
Traceback (most recent call last):
  File "/opt/miniconda/envs/claude/bin/pytest", line 6, in <module>
    sys.exit(console_main())
             ~~~~~~~~~~~~^^
  File "/opt/miniconda/envs/claude/lib/python3.14/site-packages/_pytest/config/__init__.py", line 223, in console_main
    code = main()
  File "/opt/miniconda/envs/claude/lib/python3.14/site-packages/_pytest/config/__init__.py", line 193, in main
    config = _prepareconfig(new_args, plugins)
  File "/opt/miniconda/envs/claude/lib/python3.14/site-packages/_pytest/config/__init__.py", line 361, in _prepareconfig
    config: Config = pluginmanager.hook.pytest_cmdline_parse(
                     ~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~^
        pluginmanager=pluginmanager, args=args
        ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^
    )
    ^
  File "/opt/miniconda/envs/claude/lib/python3.14/site-packages/pluggy/_hooks.py", line 512, in __call__
    return self._hookexec(self.name, self._hookimpls.copy(), kwargs, firstresult)
           ~~~~~~~~~~~~~~^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^
  File "/opt/miniconda/envs/claude/lib/python3.14/site-packages/pluggy/_manager.py", line 120, in _hookexec
    return self._inner_hookexec(hook_name, methods, kwargs, firstresult)
           ~~~~~~~~~~~~~~~~~~~~^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^
  File "/opt/miniconda/envs/claude/lib/python3.14/site-packages/pluggy/_callers.py", line 167, in _multicall
    raise exception
  File "/opt/miniconda/envs/claude/lib/python3.14/site-packages/pluggy/_callers.py", line 139, in _multicall
    teardown.throw(exception)
    ~~~~~~~~~~~~~~^^^^^^^^^^^
  File "/opt/miniconda/envs/claude/lib/python3.14/site-packages/_pytest/helpconfig.py", line 124, in pytest_cmdline_parse
    config = yield
             ^^^^^
  File "/opt/miniconda/envs/claude/lib/python3.14/site-packages/pluggy/_callers.py", line 121, in _multicall
    res = hook_impl.function(*args)
  File "/opt/miniconda/envs/claude/lib/python3.14/site-packages/_pytest/config/__init__.py", line 1192, in pytest_cmdline_parse
    self.parse(args)
    ~~~~~~~~~~^^^^^^
  File "/opt/miniconda/envs/claude/lib/python3.14/site-packages/_pytest/config/__init__.py", line 1562, in parse
    self.hook.pytest_load_initial_conftests(
    ~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~^
        early_config=self, args=args, parser=self._parser
        ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^
    )
    ^
  File "/opt/miniconda/envs/claude/lib/python3.14/site-packages/pluggy/_hooks.py", line 512, in __call__
    return self._hookexec(self.name, self._hookimpls.copy(), kwargs, firstresult)
           ~~~~~~~~~~~~~~^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^
  File "/opt/miniconda/envs/claude/lib/python3.14/site-packages/pluggy/_manager.py", line 120, in _hookexec
    return self._inner_hookexec(hook_name, methods, kwargs, firstresult)
           ~~~~~~~~~~~~~~~~~~~~^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^
  File "/opt/miniconda/envs/claude/lib/python3.14/site-packages/pluggy/_callers.py", line 167, in _multicall
    raise exception
  File "/opt/miniconda/envs/claude/lib/python3.14/site-packages/pluggy/_callers.py", line 139, in _multicall
    teardown.throw(exception)
    ~~~~~~~~~~~~~~^^^^^^^^^^^
  File "/opt/miniconda/envs/claude/lib/python3.14/site-packages/_pytest/warnings.py", line 128, in pytest_load_initial_conftests
    return (yield)
            ^^^^^
  File "/opt/miniconda/envs/claude/lib/python3.14/site-packages/pluggy/_callers.py", line 116, in _multicall
    next(function_gen)  # first yield
    ~~~~^^^^^^^^^^^^^^
  File "/opt/miniconda/envs/claude/lib/python3.14/site-packages/_pytest/capture.py", line 170, in pytest_load_initial_conftests
    capman.start_global_capturing()
    ~~~~~~~~~~~~~~~~~~~~~~~~~~~~~^^
  File "/opt/miniconda/envs/claude/lib/python3.14/site-packages/_pytest/capture.py", line 773, in start_global_capturing
    self._global_capturing = _get_multicapture(self._method)
                             ~~~~~~~~~~~~~~~~~^^^^^^^^^^^^^^
  File "/opt/miniconda/envs/claude/lib/python3.14/site-packages/_pytest/capture.py", line 714, in _get_multicapture
    return MultiCapture(in_=FDCapture(0), out=FDCapture(1), err=FDCapture(2))
                                              ~~~~~~~~~^^^
  File "/opt/miniconda/envs/claude/lib/python3.14/site-packages/_pytest/capture.py", line 493, in __init__
    TemporaryFile(buffering=0),
    ~~~~~~~~~~~~~^^^^^^^^^^^^^
  File "/opt/miniconda/envs/claude/lib/python3.14/tempfile.py", line 652, in TemporaryFile
    prefix, suffix, dir, output_type = _sanitize_params(prefix, suffix, dir)
                                       ~~~~~~~~~~~~~~~~^^^^^^^^^^^^^^^^^^^^^
  File "/opt/miniconda/envs/claude/lib/python3.14/tempfile.py", line 126, in _sanitize_params
    dir = gettempdir()
  File "/opt/miniconda/envs/claude/lib/python3.14/tempfile.py", line 316, in gettempdir
    return _os.fsdecode(_gettempdir())
                        ~~~~~~~~~~~^^
  File "/opt/miniconda/envs/claude/lib/python3.14/tempfile.py", line 309, in _gettempdir
    tempdir = _get_default_tempdir()
  File "/opt/miniconda/envs/claude/lib/python3.14/tempfile.py", line 224, in _get_default_tempdir
    raise FileNotFoundError(_errno.ENOENT,
                            "No usable temporary directory found in %s" %
                            dirlist)
FileNotFoundError: [Errno 2] No usable temporary directory found in ['/tmp', '/var/tmp', '/usr/tmp', '/home/renmark/projects/ai-system']

codex
Summary: Critical 0, Major 5, Minor 2, Nit 1.

- `renmark/loop.py:314-317`, `plugin/skills/verify/SKILL.md:119-136` — Major: `build_decision()` expects verification metadata to carry `next_action`, but the verify skill only writes standard artifact metadata plus summary lines, so every failed iteration produces `next_action == ""` and the loop skill immediately marks the run `stalled` on the first non-pass. Fix: add a real `next_action` metadata field to verification artifacts/schema or teach `build_decision()` to derive it from an existing persisted field, then add a failing-then-continue test.

- `plugin/skills/loop/SKILL.md:94-124` — Major: the budget gate is sequenced after `orchestrate → verify → refresh_spent`, so a run that is already near its ceiling can overspend the approved budget by one full iteration even though the skill claims the budget is “never exceeded.” Fix: preflight each iteration against current spend/budget remaining before dispatch, or explicitly downgrade the contract to “stops after detecting the overrun” if that is the intended behavior.

- `renmark/loop.py:265-277` — Major: `_coerce_budget_tokens()` can raise on `1e309`, `$1e309`, `nan`, `$nan`, `inf`, or `$inf` because `float()` accepts them and the later `int()`/`round()` conversion is unguarded, which breaks the never-raise promise for malformed budget input. Fix: reject non-finite floats with `math.isfinite()` and catch `OverflowError`/`ValueError` around the final numeric conversion, degrading to the default budget.

- `renmark/loop.py:166-170`, `renmark/loop.py:397-401`, `renmark/loop.py:427-429` — Major: `read_loop()` accepts arbitrary JSON types/status values into `LoopState`, and `stop_reason()`/`refresh_spent()` later call `.strip()` on `pending_step`/`run_id`, so a corrupt `loop.json` can still raise or be treated as runnable instead of degrading safely. Fix: validate/coerce persisted fields on read, reject unknown statuses, and broaden the downstream guards so malformed state resolves to `None` or `stalled` rather than escaping.

- `renmark/state/usage.py:68-81`, `renmark/state/usage.py:111-120` — Major: `usage_by_run_id()` only catches `OSError` from `read_usage()` and sums token fields verbatim, so a malformed ledger can still crash on undecodable bytes or undercount spend via negative token values, which can let the loop run past budget. Fix: make ledger reads swallow decode errors and clamp each token field at `max(0, int(...))` before summing.

- `tests/test_loop.py:292-297` — Minor: the suite asserts the current stalled split, but it never proves that a blank actionable step cannot leave the loop re-entering `while stop_reason(...) is None` when the driver forgets to set `state.status = "stalled"`. Fix: add a driver-level or integration-style test that feeds a failed verification with no actionable next step through the real loop contract and asserts terminal behavior.

- `tests/test_loop.py:125-158`, `tests/test_loop.py:246-277`, `tests/test_loop.py:319-341` — Minor: the tests do not cover non-finite/huge budgets, malformed `loop.json` field types/statuses, corrupt ledger bytes, negative ledger tokens, or an actual budget-overshoot scenario, so the never-raise and budget-ceiling guarantees are not pinned down. Fix: add targeted cases for `nan`/`inf`/`1e309`, invalid persisted state, bad ledger content, and a pre/post-iteration budget gate.

- `renmark/loop.py:182-187` — Nit: `write_loop()` prevents partial JSON via temp-file replacement, but it is not crash-durable because it never `fsync()`s the temp file or parent directory despite the docs implying the state is safely persisted before the iteration returns. Fix: either add `fsync()` on the temp file and directory around the rename or soften the comment to describe this as best-effort atomic replacement, not durable persistence.

`pytest -q tests/test_loop.py` was not runnable here because the environment has no usable temporary directory (`/tmp`, `/var/tmp`, `/usr/tmp`, and the repo root were all unavailable to `tempfile`).
tokens used
113,992
Summary: Critical 0, Major 5, Minor 2, Nit 1.

- `renmark/loop.py:314-317`, `plugin/skills/verify/SKILL.md:119-136` — Major: `build_decision()` expects verification metadata to carry `next_action`, but the verify skill only writes standard artifact metadata plus summary lines, so every failed iteration produces `next_action == ""` and the loop skill immediately marks the run `stalled` on the first non-pass. Fix: add a real `next_action` metadata field to verification artifacts/schema or teach `build_decision()` to derive it from an existing persisted field, then add a failing-then-continue test.

- `plugin/skills/loop/SKILL.md:94-124` — Major: the budget gate is sequenced after `orchestrate → verify → refresh_spent`, so a run that is already near its ceiling can overspend the approved budget by one full iteration even though the skill claims the budget is “never exceeded.” Fix: preflight each iteration against current spend/budget remaining before dispatch, or explicitly downgrade the contract to “stops after detecting the overrun” if that is the intended behavior.

- `renmark/loop.py:265-277` — Major: `_coerce_budget_tokens()` can raise on `1e309`, `$1e309`, `nan`, `$nan`, `inf`, or `$inf` because `float()` accepts them and the later `int()`/`round()` conversion is unguarded, which breaks the never-raise promise for malformed budget input. Fix: reject non-finite floats with `math.isfinite()` and catch `OverflowError`/`ValueError` around the final numeric conversion, degrading to the default budget.

- `renmark/loop.py:166-170`, `renmark/loop.py:397-401`, `renmark/loop.py:427-429` — Major: `read_loop()` accepts arbitrary JSON types/status values into `LoopState`, and `stop_reason()`/`refresh_spent()` later call `.strip()` on `pending_step`/`run_id`, so a corrupt `loop.json` can still raise or be treated as runnable instead of degrading safely. Fix: validate/coerce persisted fields on read, reject unknown statuses, and broaden the downstream guards so malformed state resolves to `None` or `stalled` rather than escaping.

- `renmark/state/usage.py:68-81`, `renmark/state/usage.py:111-120` — Major: `usage_by_run_id()` only catches `OSError` from `read_usage()` and sums token fields verbatim, so a malformed ledger can still crash on undecodable bytes or undercount spend via negative token values, which can let the loop run past budget. Fix: make ledger reads swallow decode errors and clamp each token field at `max(0, int(...))` before summing.

- `tests/test_loop.py:292-297` — Minor: the suite asserts the current stalled split, but it never proves that a blank actionable step cannot leave the loop re-entering `while stop_reason(...) is None` when the driver forgets to set `state.status = "stalled"`. Fix: add a driver-level or integration-style test that feeds a failed verification with no actionable next step through the real loop contract and asserts terminal behavior.

- `tests/test_loop.py:125-158`, `tests/test_loop.py:246-277`, `tests/test_loop.py:319-341` — Minor: the tests do not cover non-finite/huge budgets, malformed `loop.json` field types/statuses, corrupt ledger bytes, negative ledger tokens, or an actual budget-overshoot scenario, so the never-raise and budget-ceiling guarantees are not pinned down. Fix: add targeted cases for `nan`/`inf`/`1e309`, invalid persisted state, bad ledger content, and a pre/post-iteration budget gate.

- `renmark/loop.py:182-187` — Nit: `write_loop()` prevents partial JSON via temp-file replacement, but it is not crash-durable because it never `fsync()`s the temp file or parent directory despite the docs implying the state is safely persisted before the iteration returns. Fix: either add `fsync()` on the temp file and directory around the rename or soften the comment to describe this as best-effort atomic replacement, not durable persistence.

`pytest -q tests/test_loop.py` was not runnable here because the environment has no usable temporary directory (`/tmp`, `/var/tmp`, `/usr/tmp`, and the repo root were all unavailable to `tempfile`).
