---
artifact_type: rethink-classification
schema_version: 1
created_at: 2026-08-06T00:00:00Z
source_sha: da164692c2ebb9f5094497cfc9ddca9e5217b169
related_plan: cross-host-native-tool-leverage
generator: sonnet
stage: 6
dependency_refs:
  - .renmark/rethink/cross-host-native-tool-leverage/survey.md
  - .renmark/rethink/cross-host-native-tool-leverage/baseline.md
  - .renmark/rethink/cross-host-native-tool-leverage/prd-acceptance-map.md
  - .renmark/rethink/cross-host-native-tool-leverage/external-benchmark.md
  - .renmark/rethink/cross-host-native-tool-leverage/modularity-assessment.md
completion_state: complete
confidence: medium
validation_status: validated
retry_count: 0
parser_success: true
schema_compliance: true
---

# Stage 6 Classification — cross-host native-tool leverage

Scope is the approved Discovery Direction (modularity-assessment.md, Discovery
Direction Gate, 2026-08-06): targeted native-tool adoption within the existing
architecture, no new abstraction layer.

| Component | Classification | Evidence citations | Rationale |
|---|---|---|---|
| `renmark/dispatch.py`'s `HostName` Literal + validation (dispatch.py ~1057-1080, no import of `renmark.hosts`) | **Improve** | Survey §2 (dispatch.py listed as a host-branch point but not delegating); Modularity §2 ("dispatch.py does NOT import renmark.hosts at all... genuine second, independent encoding," §2 closing table: "4 of 5 decision points form one coherent layer... dispatch.py is the one outlier"); Modularity Target-state rec #1 ("Fix dispatch.py's independent HostName encoding... not a design flaw needing a new abstraction, just an unwired import"); PRD REQ-23 (host parity) — a second independent host-identity encoding is exactly the kind of drift REQ-23 warns against | Boundary/interface fix, not a rewrite: replace local `Literal["claude","codex"]` + hand-rolled `ValueError` check with `renmark.hosts.HostKind` (or thin re-export), so `dispatch.py` picks up `_HOST_ALIASES` and `HostKind.UNKNOWN`'s conservative fallback for free. In scope item 1 of the approved direction. |
| `renmark/hosts.py` / `capabilities_for` / `HostKind` | **Keep** | Modularity §1 ("cleanly separates two concerns... small, bounded, single-file change" for a 3rd host); Modularity §2 (4 of 5 consumers delegate cleanly); External-benchmark §1 confirms Claude's real native primitives (Tasks, EnterWorktree/ExitWorktree) exist as documented, validating that `hosts.py`'s capability-table shape is the right level of abstraction to gate them by | This is the canonical source dispatch.py should consolidate onto (scope item 1) — no change to `hosts.py` itself beyond the two new optional capability fields needed for scope items 2-3 (see below), which are additive, not structural. |
| `renmark/usage.py`'s `classify_usage_pause` / `PauseState` | **Keep** (pause-state persistence is unchanged) | Survey §5 ("usage.py... does not itself write state or resume anything... No polling, no retry scheduling — purely computes resume_after and delegates"); Baseline §"Measured baseline to hold" (REQ-30 protects dispatch/gate frequency — `PauseState`/`pipeline.json` persistence is the load-bearing survival mechanism across `/clear`); Discovery Direction Gate scope item 2 explicit: "complementing, never replacing, the existing PauseState/pipeline.json persistence (ScheduleWakeup is session-scoped only, does not survive /clear)" | `classify_usage_pause`/`PauseState` stay exactly as-is; `ScheduleWakeup` is bolted on beside them, not into them, because ScheduleWakeup cannot survive `/clear` and PauseState must. |
| **NEW: `ScheduleWakeup` additive nudge in the usage-pause path** | **Improve** (net-new capability added to an existing module, scoped) | Survey §5 ("No evidence of ScheduleWakeup... awareness... while renmark hand-rolls the equivalent functionality itself"); External-benchmark §1 flags "Scheduled/cron agent execution: not found in official docs... insufficiently sourced, flagged in Unknowns" — i.e. ScheduleWakeup as a Claude Code CLI-invokable primitive is NOT confirmed by primary-source external research, only inferred; Modularity §3 ("Adoption's smallest blast radius is renmark/heartbeat.py alone... does not need to touch state/pause.py or cli/"); Discovery Direction Gate scope item 2 | Classified Improve, not Unknown, because the Direction Gate already approved it as additive/optional (a nudge alongside, never instead of, PauseState) — the external-benchmark uncertainty about whether ScheduleWakeup exists as a live-session-invokable primitive is a build-time detail (host either supports it or the call no-ops via a new `HostCapabilities.supports_schedule_wakeup` gate defaulting False), not a blocker to scoping the change. If a live implementer hits an actual "tool does not exist" wall, that narrows to a spike — see Unknown-needs-spike entry below for the bounded fallback. |
| `renmark/heartbeat.py`'s cron-emission mechanism (`emit_cron`, `is_cron_installed`, `auto_resume`) | **Keep** (complementary, not obsoleted) | Survey §5 (`emit_cron` "only prints crontab/Windows Task Scheduler text; it never writes a file or installs anything," `auto_resume` "pure subprocess delegation... zero LLM calls itself"); Discovery Direction Gate scope item 2 explicitly frames ScheduleWakeup as additive; the unattended-machine use case (no live agent session at all, e.g. overnight cron) has no session for `ScheduleWakeup` to run in, so cron remains the only mechanism there | Two mechanisms serve two disjoint operating modes: `ScheduleWakeup` only works inside a live, in-session agent (Claude Code); cron/`heartbeat.py --auto-resume` works with zero live session at all. Neither obsoletes the other — heartbeat.py gains a `capabilities_for(host).supports_schedule_wakeup`-gated branch (per Modularity §3) but its cron path stays live as the no-session fallback. |
| `finish/SKILL.md` §3.6 worktree removal shell-out (`git worktree remove <path>`, lines 378-397) | **Improve** | Survey §4 ("EnterWorktree/ExitWorktree... no hits found... renmark's own worktree handling is entirely custom subprocess-driven git worktree inspection, with no reference to a host-native worktree tool at all"); External-benchmark §1 ("EnterWorktree/ExitWorktree are native Claude Code tools (introduced v2.1.72)... verified fact"); Modularity §3 ("the mutating git worktree remove shell-out lives in finish/SKILL.md §3.6... Adoption's smallest blast radius is that one skill body... No ripple into state.py or cli/"); Discovery Direction Gate scope item 3 | Confirmed exact call site (finish/SKILL.md:387, "self-update and full lanes ONLY"). Swap the raw `git worktree remove` shell-out for `ExitWorktree` when the resolved host is Claude Code and supports it (new `HostCapabilities.supports_exit_worktree` gate); keep the existing safe (non-`--force`) shell-out as the fallback for hosts/lanes without native support. `renmark/worktree.py` itself is untouched — it is read-only inspection only (Survey §5) and was never the removal call site. |
| `renmark/task_tracking.py` (the Python mirror, domain logic: evidence requirements, self-approval guard, atomic write) | **Keep** | Survey §5 (full read: `MissingEvidenceError` guard, `ledger.check_dispatch_independence` no-self-approval guard, atomic tempfile+`os.replace` write); PRD-acceptance-map REQ-31 Owner decision ("Both are honest, both are visible to someone... REQ-31 is already met under this reading"); External-benchmark §2 ("no official Codex-CLI-native equivalent to Claude's TaskCreate/TaskUpdate... found in this search pass") confirms this is genuine value neither host provides, not duplicated scaffolding; Discovery Direction Gate scope item 5 explicitly keeps "task_tracking's domain logic... confirmed by stage 4's external research as genuine value neither host provides natively" | Domain logic (evidence enforcement, self-approval guard, atomic persistence) is unconditionally correct and stays as-is regardless of wiring changes below. |
| **Wiring of `renmark.task_tracking` into the live-Codex dispatch path** (i.e., `renmark/dispatch.py::_build_codex_host_calls` / `codex_routing.py::build_native_dispatch`, which currently build `spawn_agent`/`wait_agent`/`followup_task` payloads with no accompanying `task_tracking` calls) | **Improve** | Survey §5 ("Real call sites are exclusively in renmark/cli/_wave_loop.py... only the headless/codex-subprocess execution path uses this Python mirror... for Claude Code, task tracking correctness depends entirely on the live model actually making the tool calls"); confirmed by this session's own grep: `task_tracking` has zero call sites in `renmark/dispatch.py` or `renmark/codex_routing.py` — only in `renmark/cli/_wave_loop.py` (the `renmark-execute` headless subprocess path, which already exists and already handles `executor: codex` *tasks dispatched from a Claude session*); PRD-acceptance-map flagged issue 1 ("REQ-31 mechanism (i)... untestable for interactive Codex... there's no stated fixture or acceptance evidence" for a *live, interactive Codex session as host*); Discovery Direction Gate scope item 4 ("Wire renmark.task_tracking into the live-Codex dispatch path, closing REQ-31's Codex gap for real... this closes the code side") | Distinguish the two Codex paths precisely: (a) `executor: codex` tasks dispatched *from* a live Claude session — already wired via `_wave_loop.py`, no gap. (b) a live, interactive Codex CLI session acting *as host* (`resolve_host()` returns `CODEX`, `dispatch.py`'s codex branch builds `spawn_agent`/`wait_agent`/`followup_task` calls) — this path has no `task_tracking` calls anywhere today. The Improve is: wire `task_tracking.create_or_reuse_task`/`mark_in_progress`/`complete_task` calls around `_build_codex_host_calls`'s payload construction (or the point where a live Codex session would invoke `spawn_agent`), so REQ-31's Owner-approved reading ("Codex gets renmark's own task_tracking.py bookkeeping") is actually true for interactive Codex, not only for the headless subprocess path. |
| `renmark/interaction.py::build_selector` | **Keep** | Baseline §1 (full documented current behavior: single-choice guard, no-native-selector fallback, `SelectorBypassError` guard, per-host page sizing, per-host native-argument shape — all already correctly host-conditional); Survey §4 (`AskUserQuestion` "code-backed as a name... but never itself invoked from Python — by design"; `request_user_input` "code-backed as a name/flag, not as an invoked tool" with `hosts.py` self-documenting the Codex Default-mode gap); External-benchmark corroborates no contradicting new primitive; Discovery Direction Gate scope item 5 keeps this implicitly (not in the 4 change items, and the Direction rejects a new abstraction layer that would touch this) | Already correctly wraps both native selectors behind `HostCapabilities`, already has the bypass guard against the documented "picker not re-rendered on continuation turn" bug class, already falls back safely when a native tool is unavailable (Codex's `request_user_input` today). No functional gap found in stages 1-5. Only touched indirectly if `dispatch.py`'s HostName fix ever changes what `host` string values flow into it — that's dispatch.py's fix, not a change to `build_selector` itself. |
| `plugin/skills/.shared/workflow-fanout.md` + `renmark/dispatch.py::build_workflow_fanout_args` | **Keep** | Survey §5 (full read of workflow-fanout.md: explicit adoption threshold `>1` non-codex task, explicit "thin dispatch shim... no validation" design so it can't become "a second, driftable copy of the G11 contract," explicit rejection of extending to `executor: codex` and to cross-`/clear` resumability, explicit rejection of the Workflow tool's own token accounting to avoid double-counting against `cost.py`); Baseline §2 (`_build_claude_host_calls` confirmed: >1 task → `Workflow` tool call, exactly 1 task → plain `Agent` call, matches the documented threshold exactly — code and doc agree); Modularity Discovery Direction Gate explicitly keeps this unchanged (scope item 5) | This is the model instance of "already correct native-tool adoption" cited in the Direction Gate itself: bounded scope (fan-out shape only), no validation duplication, explicit and reasoned rejections of over-adoption (codex tasks, cross-clear resume, native cost tracking). No further action — cited as the pattern the other Improve items (task_tracking wiring, ScheduleWakeup, ExitWorktree) should follow: additive/scoped, never a parallel validation or accounting path. |
| `WorkOrder` / capability-envelope / G11 isolation contract | **Keep** | Survey (implicit throughout — G11 isolation is the load-bearing contract every dispatch path, Claude and Codex, is built around; not itself in scope for stages 1-5's host-branch inventory because it's host-agnostic by design); PRD-acceptance-map REQ-5/REQ-20 ("met" — context hygiene / task-local isolation); External-benchmark §1 direct citation: "renmark's own completion_state/confidence/validation_status contract on every subagent output is therefore not redundant scaffolding — it's covering a real, currently-unfilled gap on both hosts, at the level (per-subagent) neither host guarantees natively" (structured-output gap: Claude Code's `outputFormat` schema enforcement exists only at the top-level `query()` call, not per-subagent — open GitHub issue #20625 confirmed still unresolved; Codex CLI has no equivalent primitive at all); Discovery Direction Gate scope item 5 explicitly keeps this unchanged | The clearest Keep in this classification: external research directly falsifies the premise that a host-native primitive could replace it — neither host offers per-subagent structured-output enforcement today, so G11's own artifact-metadata contract (`completion_state`/`confidence`/`validation_status`/etc.) is filling a real, currently-unfilled gap on both hosts, not duplicating one that already exists natively. |
| `renmark/subagent_gate.py`'s per-control × per-host `ENVELOPE_CONTROL_STATUS` / `host` parameter threading | **Keep** | Survey §2 ("host is threaded through mostly for logging/status text... not for a different gate decision. Worth flagging as a shallow host parameter... vs. the deep forks above"); Modularity §2 ("a different concern (capability enforcement status per control) than hosts.py's render/selector capabilities — not the same fact duplicated, so not counted as drift risk here") | Not in the approved scope (not one of the 4 change items, not flagged as duplication by modularity assessment). Shallow, non-differentiating `host` plumbing is not a defect worth touching under "targeted adoption, no new abstraction layer." |
| `lifecycle/stage.py`'s local `_lifecycle_host` precedence-order duplication (mirrors but does not call `hosts.resolve_host`'s precedence) | **Unknown — needs a spike** | Modularity §2 ("a real but narrow duplication: if resolve_host's precedence order ever changes, _lifecycle_host must be updated in lockstep or the two diverge silently... a Codex-hosted test runner nuance documented in its docstring") | **Question:** can `lifecycle/stage.py`'s `_lifecycle_host` be replaced with a direct call to `hosts.resolve_host()`, or does its documented "Codex-hosted test runner nuance" require it to stay independent? **Scope:** read `lifecycle/stage.py`'s docstring and the specific test-runner scenario it guards, plus `tests/test_hosts.py`'s coverage of `resolve_host`'s precedence, to determine if the two precedence orders are actually identical today or have already silently diverged. **Evidence requirement:** a diff of the two precedence orders (explicit-arg > env-var > process-marker, or whatever `_lifecycle_host` actually encodes) against `resolve_host`'s documented order (hosts.py:91-111), plus the specific CI/test scenario that motivated keeping it separate. **Budget:** 30 minutes, single-file read + one test run, no code changes. **Stop condition:** if the precedence orders are already identical and the "nuance" no longer applies (or never did), route to a follow-up Improve (delete the duplication, call `resolve_host` directly); if a genuine test-runner-only distinction is confirmed and still needed, close as Keep with a comment documenting why it must stay separate. This item was NOT in the Direction Gate's 4 approved scope items — it surfaces here only because stage 5 flagged it as a narrow, real duplication; it should be logged for a future rethink pass or bundled cheaply into the dispatch.py fix's PR if the spike is quick, not blocked on this classification. |

## Counts

- Keep: 8 (`hosts.py`/`capabilities_for`, `usage.py` pause persistence, `heartbeat.py` cron mechanism, `task_tracking.py` domain logic, `interaction.py::build_selector`, `workflow-fanout.md`/`build_workflow_fanout_args`, `WorkOrder`/capability-envelope/G11, `subagent_gate.py` host plumbing)
- Improve: 4 (`dispatch.py` HostName consolidation, new `ScheduleWakeup` nudge in usage-pause path, `finish/SKILL.md` §3.6 → `ExitWorktree`, `task_tracking` wiring into live-Codex dispatch path)
- Replace: 0
- Remove: 0
- Unknown-needs-spike: 1 (`lifecycle/stage.py::_lifecycle_host` precedence duplication — out-of-scope spike, bounded, not blocking)
